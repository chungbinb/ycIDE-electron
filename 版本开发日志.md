# ycIDE 版本开发日志

> 用途：先记录开发变化，再在发版时据此更新 `版本更新说明.md`，避免遗漏。

## 记录规范
- 每次功能开发/修复/优化完成后，追加到“待发版条目”。
- 分类只使用：`[新增]`、`[修复]`、`[优化]`。
- 每条尽量包含：变化摘要、影响范围、验证方式。
- 版本号统一使用 `x.y.z-beta.n` 预发布格式（示例：`0.0.3-beta.54`），不再使用 `0.0.3.54` 这类纯数字补丁格式。
- 发版改版本号时，需同步修改：`src/renderer/src/components/StatusBar/StatusBar.tsx` 与 `src/renderer/src/App.tsx`（`aiIdeContext` 版本字符串）。
- 发版后将对应条目移动到“已发布归档”。
- **提交标题规范**：提交/推送标题以版本号开头，格式 `v<版本号> - <变化摘要>`，示例：`v0.0.3-beta.60 - WebView2 组件体系与工具箱分类、编辑框属性补全、编辑器性能优化`。
- **测试包打包规范**：发给测试人员的包统一用 `npm run package:win:zip` 生成（`scripts/package-win-zip.mjs`），脚本会自动排除 `compiler/` 目录（编译工具链 3GB+ 单独分发）。装有 7-Zip 时产出 7z 格式极限压缩包 `dist/ycIDE-win-unpacked-v<版本>.7z`（约 77MB，LZMA2 -mx=9 solid）；未装 7-Zip 时回退 `.zip`（约 123MB）。注意 .7z 在 Win10 上需要 7-Zip/WinRAR 解压。

## 待发版条目

（暂无）

## 已发布归档

### v0.0.5-beta.22（2026-07-20）

- [修复] **折叠子程序后内部流程线泄漏到上一个子程序**：`displayFlowLines` 的隐藏标记重映射趟（隐藏否则/默认锚上一可见行、隐藏如果真结束 endArrowOnly、判断分支 inner-link 链）对"不可见行"一视同仁——子程序折叠后其全部行不可见，这些趟就把折叠范围内的 branch/end 段锚到 `findPrevVisibleCodeLine*` 找到的**上一个子程序**的可见行上，塞进 has-inner-link/outer-resume/outer-horz/inner-vert 等幽灵段。修：map 构建后把 `collapsedSubRanges` 范围内（startLine+1..endLine）的行段整体 `map.delete`——重映射趟取不到段自然跳过、渲染无源；deps 补 collapsedSubRanges。折叠范围外的正常隐藏标记重映射不受影响。验证：`.tmp-harness/repro-fold-flowleak.mjs` 三场景（如果否则/循环+如果真/判断多分支）整行 flow 元素类串对比 + 展开恢复 + A/B stash 坐实（无修复时真泄漏幽灵段）。**提取粒度教训**：只抓 `.eyc-flow-seg` 类名太窄（污染多在 flow 容器兄弟元素上），要抓整行 `[class*="eyc-flow"]`；第一版最小场景（两个如果真）根本不触发泄漏，A/B 双 PASS 差点漏掉。
- [修复] **删除/剪切折叠的子程序只删声明行、暴露变量与代码**：删除/剪切/复制都基于可见选区行下标——折叠后子程序只剩声明行可见，三条删除路径（右键删除行/Ctrl+X/Delete）只删这一行，隐藏的局部变量/代码暴露残留；**连带隐患**：Ctrl+X 剪切折叠子程序删了声明但剪贴板只有声明行，粘回静默丢失整个子程序体。修：新增 `expandSelectionWithCollapsedSubs`（选区含折叠范围 startLine 时并入 startLine+1..endLine，读 collapsedSubRangesRef，未折叠不受影响），注入五处：`getDeletableSelection`（键盘 Ctrl+X 删除+剪贴板与 Delete）、Ctrl+C sorted、`getSelectedSourceText`（右键复制/剪切）、`deleteLineSelection`（右键删除行）。验证：`.tmp-harness/repro-fold-del.mjs` 两场景 + A/B stash 坐实（无修复残留 变量A/调试输出、剪贴板缺体）。**通用模式**（两坑同源）：折叠让一段行"不可见"，凡「就近可见行锚定/重映射」「基于可见选区行下标」的逻辑都必须把折叠范围当特殊情况。

### v0.0.5-beta.21（2026-07-19）

- [修复] **辅助窗口视觉补齐（多窗口 v1 缺口）**：v1 只主窗画窗体背景图、辅助窗 `YcSubWinProc` 无 WM_PAINT/WM_DRAWITEM。补齐四项：①**背景图**：per-window 字节内嵌 + `g_ycSubBackImages` 表（按窗序号）+ YcSubWinProc WM_PAINT 懒解码绘制（5 种底图方式，复用主窗 backImageDrawBlock）②**GDI+ 初始化门控**：`hasAnySubBackImage`/`hasAnySubPicBtnImage` 必须**同时**进 `g_gdiplusToken` 声明门控**和 WinMain 里 GdiplusStartup 调用门控**——只加声明会让「主窗无图但辅窗有图」时 GDI+ 未初始化、辅窗 `Image::FromStream` 崩溃（主窗有背景图会掩盖，最小项目才暴露）③**按钮底色/文本色**：根因是辅助窗按钮构建 ownerDraw 参数**恒传 false**→无 BS_OWNERDRAW；改为按底色/文本色算 ownerDraw + 颜色并入同一 `g_ycButtonDraws`（数字 ID，ID 全局唯一与主窗共表）+ YcSubWinProc WM_DRAWITEM 查表自绘 ④**图形按钮显示**：unsupported 去掉图形按钮（style 从 window-units.json 自动带 `BS_OWNERDRAW`）+ 四态图片 per-window per-control 内嵌 + 并入 `g_ycPicBtns` + WM_DRAWITEM 绘制块（鼠标悬停点燃/点击切换选中仍 v1 未接）。**owner-draw 通式**：辅助窗自绘控件（按钮/图形按钮/外形框/选择列表框）都靠 YcSubWinProc WM_DRAWITEM + ID 全局唯一共表，补一类=收集条目并入主窗表（数字 ID）+ WM_DRAWITEM 加绘制块 + 构建带 owner-draw style + GDI+ 门控。验证：P/Invoke `EnumWindows`/`EnumChildWindows` + `GetPixel` 真像素（辅窗背景图/图形按钮 #0000FF、按钮 #00FF00、`HasExited` 验不崩）。
- [修复] **透明控件重绘擦除统一**：透明背景控件（效果=透明，WM_CTLCOLORSTATIC 返 NULL_BRUSH，叠在图形按钮/背景图上）改内容后自身不擦旧像素 → 文本叠成两层、字号动画一帧帧叠成一团。抽 `yc_ctrl_parent_erase(h)`（GetWindowRect→ScreenToClient 映射后 InvalidateRect 父窗该区域擦背景），`krnln_ctrl_set_text` / `krnln_ctrl_invalidate`（刷新显示）/ `krnln_ctrl_invalidate_rect` / `krnln_ctrl_set_font_size` 统一调用。不透明控件无害（父窗重画后自身盖住）。
- [修复] **`选择`(iif) 命令返回文本被当指针整数**：「选择」=`krnln.iif`、returnType 通用型、无专门 codegen → 默认 native 生成把文本值（`const char*`）当指针整数返回，赋给标签标题显示成地址整数（如 2820598）。加 `YCMD_CUSTOM_NATIVE_EXPRS.krnln_iif` 纯 codegen IIFE（条件三元 + 两项 `yc_value_to_text` 返回 YC_TEXT），与 `多项选择`(krnln_choose) 同款。已知限制：两项一律文本化（数值项转文本）。回归用例进 `textCodepointRuntime.spec.ts`（真编译真跑）。
- [修复] **设计器：铺满窗口的控件挡住新控件绘制**：`handleControlMouseDown` 无条件 `stopPropagation()` 走选中/拖动、不看工具箱是否已选待放置控件 → 拉满整窗的图片框吃掉画布上所有 mousedown，`handleCanvasMouseDown` 的 activeTool 绘制分支永远进不去。改用 CSS 穿透 `.vd-crosshair .vd-control { pointer-events: none }`：选中工具时控件不接收鼠标、事件穿透到画布（光标自然是画布十字、mousedown 只触发一次）。**注**：先试过在控件里转发画布逻辑，副作用是光标不变十字 + 绘制分支双触发画出两个控件，已改用穿透。
- [修复] **设计器标签不按「是否自动折行」渲染**：运行时行为本就正确（按属性，默认 false 不折行），问题在设计器**恒折行**渲染 → 设计时多行、运行后单行的假 bug。标签预览改为按属性设 `--vd-preview-wrap`（真=pre-wrap / 假=nowrap），`.vd-preview-label` 用 `white-space: var(...)` + `overflow:hidden`，默认与 `buildStdLabelCodegen` 的 false 对齐。

### v0.0.5-beta.20（2026-07-19）

- [优化] **文本命令族按 Unicode 码点（阶段0，文本内核 UTF-8 everywhere 长期路线第一步）**：文本命令从「按 UTF-8 字节」改「按 Unicode 码点」，中文/emoji 的长度/切分/位置正确且跨平台一致（此前 `krnln_len=strlen` 让 `取文本长度("中")=3`，`left/mid` 按字节切会切坏中文成乱码）。`impl/windows.cpp` 加一组 UTF-8 码点工具（`yc_utf8_seq_len` 首字节→字节数续字节容错、`yc_utf8_cp_count` 数码点、`yc_utf8_byte_off` 第N码点字节偏移、`yc_utf8_encode_cp` 码点→UTF-8、`yc_utf8_decode_cp` UTF-8→码点），改 **9 命令**：取文本长度（`"中"`=1）、取文本左/右/中（按码点切）、字符（参数 ycmd 字节型→整数型放宽，`字符(20013)`=中、`字符(128512)`=😀 补充平面）、取代码（`"中"`=20013 码点，此前返回首字节 228）、寻找/倒找文本（返回码点位置、起始位置按码点）、文本替换/子文本替换（起始位置/替换长度按码点）。`ignoreCase` 时在**原始串**上数码点避 `toLowerAscii` 干扰。**不动存储**（YC_TEXT 仍 wstring，UTF-8 存储是阶段1）。未受影响命令确认过（删首尾空=空格 ASCII、文本比较/子串匹配=UTF-8 保序、取重复/分割文本）。新增 `tests/unit/textCodepointRuntime.spec.ts`（3 用例真编译真跑读中文/emoji/往返）；同步更新 `CORE_LIBRARY_EXPECTED_SHA256`（impl/windows.cpp + krnln.commands.ycmd.json）、`TRANSPILE_CACHE_VERSION`→50。遗留：到大写/到小写仍只 ASCII（Unicode 大小写非位置问题，不属阶段0）。
- [优化] **eslint 忽略 `.claude/**`**：`.claude/worktrees/`（并行任务留下的 git worktree）存多个候选 tsconfigRootDir，会让 typescript-eslint parser 对每个文件报 parsing error（632 个）干扰本地 lint；CI 是干净 checkout 无 worktree 故一直没暴露。

### v0.0.5-beta.19（2026-07-19）

- [修复] **返回文本型的命令用 `＋` 连接 / 比较时类型分诊失效**（`字符 (214) ＋ 字符 (208)` 报「运算符两边类型不能这样运算」）：`＋` 判文本连接靠 `isTextExpression`，而它是**硬编码 helper 前缀白名单**，覆盖不到「字符」「取文本左边」「到大写」等**所有返回文本型的核心库命令**——命令转译成 native 调用（`字符`→`([&]()->YC_TEXT{ return yc_utf8_to_wide(krnln_chr(...)); })()`，krnln_chr 返回 `const char*` 故包 IIFE 转 wide）后不在白名单，`＋` 退化成数值加法生成 `YC_TEXT + YC_TEXT`，被 C++ 拒（`invalid operands`，经 `friendlyCompileError` 译成「拿文本做减法」——是 **C++ 编译层报错、非转译期检查**）。修法：新增 `isTextReturningCommandCall(rawExpr, commandMap)`，用**转译前**的易语言子表达式取命令名、查 ycmd `returnType==='文本型'`（数据驱动，自动覆盖全部返回文本命令），接入三处：`＋` 文本连接、六比较符 `yc_text_compare`、相等/不等等比较命令。`TRANSPILE_CACHE_VERSION` 48→49。验证：用户文本操作测试项目由 116 行 invalid-operands → 0 错整程编译过、生成 `yc_text_concat`；typecheck + 260 单测全绿（顺带修正 `objectMemberGuard` 陈旧用例——`按钮1.移动` 已于 beta.18 接入，样例改用编辑框未接方法「取选中文本」）。
- [修复] **取命令行命令**（此前把数组变量当文本转 UTF-8 → 编译错）：帮助权威为「〈无返回值〉 取命令行（文本型变量数组）」——执行后把命令行各段填入变量数组、原有内容销毁；impl 却是残缺的 `krnln_GetCmdLine()` 无参、返回 `const char*`。改 impl 为 `void krnln_GetCmdLine(void*)`（`CommandLineToArgvW` 解析、跳 argv[0]、`_wcsdup` 元素填 `std::vector<long long>*`、原有 clear），codegen 加 `YCMD_ARRAY_PARAM_KINDS['krnln_GetCmdLine']=['arrayptr']` 生成 `(void*)&变量`。sha 已核对（impl/windows.cpp 与 `CORE_LIBRARY_EXPECTED_SHA256` 一致）。headless 真编译运行验证（无参运行=0 段）。
- [修复] **流程头参数编辑误触发溶解**（不展开参数行、直接在计次循环等流程命令括号里改参数，会把循环体和循环尾挪到流程外）：`.计次循环首 (10, i)` 只改括号内参数（关键字不变）时，`existingFlowStructureIsIntact` 会因 body 缩进不规范被误判为「结构不完整」→ 走溶解路径。改为 `preservesExistingFlowStructure`（编辑既有流程头 + 结构完整或同关键字编辑 + 命令名未变 → 保结构）与 `shouldDissolveExistingFlow`（仅关键字真的变了才溶解）双闸——纯改参数绝不溶解。验证 `repro-flowparam.mjs` + A/B stash。

### v0.0.5-beta.18（2026-07-19）

- [新增] **编译报错双行号导航（表格行(文本行) + 自动跳首错 + 可点击跳转）**：编译输出里带位置的报错就地改写为「表格行(文本行)」（如 `选题窗口.eyc:94(103)`），编译出错时自动打开并定位到首个报错行，每条报错可点击跳转到对应代码行。文本→表格行映射只有当前打开(活动)文件能拿到（`getVisibleLineForSourceLine`），故跳转/点击打开某文件后按活动文件解析并**累积缓存**（`errorTableLines`，切走不丢），未打开文件保持文本行号、打开后即改写。并列支持两种报错格式：签名错误 `文件:行号` 与 zig 编译错回溯 `文件 第 N 行`。影响：`App.tsx`、`OutputPanel.tsx`+css。
- [新增] **多项选择命令（对齐易语言 choose，尾参可重复）**：`多项选择(索引, 候选…)` 按 1 基索引从候选列表返回一个值。**纯 codegen 内联三元选择**（候选经 `yc_value_to_text` 归一为文本、全部求值、按索引三元选一个），绕过 impl `krnln_choose` 只支持 2 候选且丢类型的残缺占位；源 ycmd 末参标 `repeatable` 放开参数个数校验（不触发逐值展开——那是独立的 `YCMD_REPEAT_TAIL_SYMBOLS` 白名单）。已知限制：候选一律文本化（数值候选转文本）。headless 真编译+运行验证（`多项选择(2,甲,乙,丙)=乙`、`(5,A..E)=E`）。
- [新增] **列表框「列表项目」字节集属性代码读写**（列表框/组合框/选择列表框）：帮助定义「列表项目」为字节集(bin)属性，`列表框.列表项目 ＝ { }` 清空、赋字节集变量整体恢复。自洽序列化格式（`[u32 项数]+每项[u32 字符数][UTF-16LE]`，空字节集=清空，get/set 往返一致），新增 `{vbin}` 模板占位 + propSet 字节集分支（字节集字面量 `{ }`→YC_BIN）。
- [新增] **对象.移动 + 15 个窗口基类公共方法（`*` 通用）**：帮助「系统核心支持库->窗口」分类的可视组件公共方法——移动（省略参数保持当前）、取窗口句柄、获取焦点、可有焦点、取用户区宽/高、禁止/允许重画、重画、部分重画、取消重画、刷新显示、调整层次、发送/投递信息、激活。走 `*` 通用 `controlMethodBindings` + impl `krnln_ctrl_*`。**同时修通用省略参数机制**：`移动(左,顶,,)` 的中间空参此前落 `'0'`（宽高被设 0），改为走占位默认（哨兵保持当前）——`translateControlMethodCall` 空参保留 `''` + `applyMemberTemplate` 的 `{N|默认}` 遇空取默认。7 个需菜单/托盘/组件设施的公共方法暂缓。
- [新增] **控件.字体.字体大小 子属性（复合子对象三级链）**：读写控件字体的字号（WM_GETFONT→LOGFONT.lfHeight↔pt 换算、CreateFontIndirect+WM_SETFONT，`g_ycCtrlFonts` 表删旧防泄漏）。读/写两路都**先于两级属性正则**特判 `控件.字体.子属性`（`FONT_SUBPROP_CALLS` 专表，可扩展字体名/粗体等），否则两级正则会把 `.字体` 当普通属性、`.字体大小` 掉队。
- [新增] **补核心库常量 150 个（键代码族等）**：从用户导出的 `lib/krnln/核心常量表.txt`（GBK，335 条）补齐 `krnln.constants.json` 缺失的 148 数值 + 2 文本常量（`#回车键`=13、退格/Tab/Esc/空格/光标键/F1-F16/A-Z/0-9/Shift·Ctrl·Alt 键等），此前只 187 条、键代码族几乎全缺。帮助说明文件不含常量定义（键代码是编辑器内置）。`#pi`/`#e` 原有 double 全精度，「只补缺失、不覆盖现有」策略保护其不被常量表低精度值污染。同步更新 `CORE_LIBRARY_EXPECTED_SHA256`（impl/windows.cpp、window-units.json、krnln.commands.ycmd.json、krnln.constants.json 四文件）。
- [修复] **用户自定义常量带引号数值被当文本**（`int == const char*` 编译错）：`.常量 加, "1"`（易语言把数值常量也序列化成带引号形态）铺成 `#define 加 ("1")` → `加` 沦为 const char\*，`方法 ＝ #加` 报 comparison between pointer and integer。`#define` 生成（compiler.ts:6946）时值为引号包纯数字则去引号按整数展开，文本常量保留。
- [修复] **注释含 `*/` 撑爆块注释导致编译失败**：易语言注释转 C++ **块注释** `/* … */`，注释内容含 `*/`（如 `将每组数组用+-*/ \(除数…)`）会**提前闭合块注释**、后面泄漏成非法代码（expected expression）。整行注释（compiler.ts:7440）与未识别命令兜底（7644）改为 `//` 行注释（去行尾反斜杠防 `//` 续行）；行尾注释仍由 `stripTrailingEycComment` 直接剥除、不生成。
- **里程碑**：用户 24 点计算器项目（多窗口 + 脚本组件 + 自定义常量 + 列表框/字体/移动等）经上述补齐后**编译错误 22→0、完全通过**（每步 headless 真编译验证）。

### v0.0.5-beta.17（2026-07-18）

- [新增] **脚本语言支持组件（对齐易语言官方 script.fne）**：新增「脚本组件」非可视功能组件，基于系统 IActiveScript 引擎运行 JScript / VBScript（方法 `执行`/`计算表达式`/`运行`/`清除`，属性 `语言`/`错误信息`/`超时`，自由命令 `执行脚本表达式`，常量 `无超时`=-1），无需易语言运行时或外部 DLL；自定义 GUID 免 luuid、Win32 看门狗控制超时。新增支持库目录 `lib/script/`（script.commands.ycmd.json / constants / library / window-units + impl + 帮助文件），工具箱归「第三方组件 / 功能组件」。
- [新增] **图形按钮控件（对齐易语言 PicBtn，四态图片按钮）**：BS_OWNERDRAW BUTTON，正常/点燃/按下/禁止四态图片内嵌字节 GDI+ 懒解码（`g_ycPicBtns` 表）、WM_DRAWITEM 绘制、透明色 SetColorKey（-1 哨兵=无透明）；「类型」按钮=单击触发 / 选择框=单击 `yc_picbtn_toggle` 切换选中（按下图片表现选中态）；与图片框共用 `YcPicBoxMouseProc`/`g_ycPicBoxMouse`（7 鼠标事件 + 悬停点燃 TrackMouseEvent）；「选中」经声明式成员绑定 `yc_picbtn_get/set_checked`；「被单击」默认事件走 BN_CLICKED。并删除 krnln window-units.json 中的重复占位条目（解决下方 beta.16 遗留：占位版去重胜出→归错分类且无法创建），同步更新 `CORE_LIBRARY_EXPECTED_SHA256`。辅助窗口 v1 暂不支持。
- [优化] **可视化设计器工具箱分类改为两级（核心组件 / 第三方组件 → 型子类）**：
  - 第一层「核心组件 / 第三方组件」由 IDE 按 `isCore`（是否核心库 krnln）自动判定——主进程 `parseWindowUnits`/`parseWindowUnitsFromProtocol` 给每个窗口单元注入 `isCore`；第二层「型」由 `resolveToolboxCategory` 定稿，优先级：单元显式 `toolboxCategory` 声明 > 无窗口类名判「功能组件」 > 核心库 GDI 名单(DrawPanel/ShapeBox)判「GDI控件」 > 缺省「Win32控件」。旧值 `通用Win32`→`Win32控件`、`WebView2`→`WebView2控件` 归一化。
  - 核心库 manifest **零改动**（型按 className 空/非空 + GDI 名单在主进程计算），故 `CORE_LIBRARY_EXPECTED_SHA256` 不变、无需 bump 缓存版本。第三方 manifest：script 删显式分类（无类名→功能组件）、webview2 `WebView2`→`WebView2控件`。
  - 渲染层 `VisualDesigner` 工具箱下拉框改 `optgroup` 两级；复合分类 key（`顶层型`）持久化 localStorage，失效回退首个可用分类；CSS 补 optgroup/option 深色主题着色。第三方支持库只需在 window-units.json 的 `toolboxCategory` 声明自己的型即可自动归入「第三方组件」对应子类。
  - 影响范围：`src/main/libraryManager.ts`、`src/renderer/src/components/Editor/VisualDesigner.tsx`+`.css`、`lib/script/script.window-units.json`、`lib/webview2/webview2.window-units.json`。
  - 验证：`npm run typecheck` 通过；分类逻辑用真实 manifest 数据复刻核对（核心 GDI 2 / Win32 19 / 功能 25，第三方 WebView2 3 / 功能 1，均符合预期）。

### v0.0.5-beta.16（2026-07-17）

- [新增] **多维数组支持 + 数组命令语义对齐易语言**（用户全量对拍程序暴露 5 处不齐，30 项断言现与易语言参考逐字一致）：
  - 重定义数组「维上限可重复」：ycmd 元数据加 repeatable；YCMD_CUSTOM_NATIVE_EXPRS 定制成 `krnln_ReDimEx(数组, 保留, 维数组, 维数)`；转译期把该数组 dims 置 [0×N]（多维尺寸交运行时）或 []（一维）。
  - **运行时维度登记表**（impl unordered_map，键=vector 地址）：读取恒校验「维度乘积==成员数」，失配即弃——地址复用/加入成员改长自动退化一维，无需析构钩子。取数组下标→krnln_UBound 查表（一维=成员总数）；链式下标静态声明维走编译期折算、动态维走 yc_ary_lin→krnln_AryLinIdx 运行时折算；静态多维局部声明后发 krnln_AryRegDims 登记（传参进子程序仍取得到维度）；复制数组连维度。
  - 语义修正（此前全错、无测试锁定，以易语言实测输出为权威）：重定义数组(a,假,5)=5 个成员（原 +1 成 6）；取数组下标=该维成员数（原 count-1）；插入成员/删除成员 位置一基（原 0 基）。
  - 验证：.tmp-flow-verify/arycmd-test（Console 22 项含变量维数运行时折算/维度随复制传递/加入成员后取下标漂移）+ aryuser-test（用户原程序 GUI BM_CLICK+WM_GETTEXT，一致=30 不一致=0）。
- [修复] **逻辑型走 krnln_set 族崩溃**（用户实测 连续赋值(真,逻辑1,逻辑2) 即崩）：yc_vt_of 缺 bool 重载，bool 经整型提升落 int 重载 → 按 4 字节读写 1 字节对象（UBSan misaligned + 写侧踩邻近栈内存）。加 YC_VT_BOOL=10 三处同步（impl krnln_set/krnln_XchgVar 两 switch + 生成侧 define/重载）。通用教训：mapTypeToVarCType 分侧的类型（bool/YC_DATE）在每张「按 C++ 重载解析类型」的派发表都要有显式重载，缺了不报错、静默落错档。验证：boolstore-test（赋值/连续赋值/交换/强交换 逻辑型 + 整/文/双回归）。
- [修复] **真/假/且/或 裸词替换吃进字符串字面量**：replaceBooleanLiterals/replaceLogicalOperatorAliases 的正则不感知引号，用户显示文本 “(a, 假, 5)” 被印成 “(a, 0, 5)”。新增 applyOutsideStringLiterals（直引号+全角引号两族感知）包住替换；同类新增裸词替换一律走这层。**坑：字面量修复后忘 bump TRANSPILE_CACHE_VERSION，缓存命中旧产物、验证结果是旧的**——本次连升 36→38（36=YC_VT_BOOL / 37=多维数组 prelude / 38=字面量引号感知）。
- [修复] **表格编辑器退格两缺陷**（用户报障）：① 非空代码行行首退格此前无任何处理路径——applyEmptyDeleteKey 顶部加分支：光标0+无选区+上一源行空白 → 删上一行、当前行上移、flushSync+startEditLine 保编辑态（live 值先写回防 24ms 节流丢键；最小结构体占位保护与空行路径同判据）。② 空锚点行退格误删下一内容行——「保留锚点改删下一普通行」只在下一普通行也是空行时才等价，内容行（无点 如果真 头算"普通行"）被重定向删除后 如果真结束 成孤儿显露；现删空锚点行自身（保底不变量是「子程序保有≥1普通行」非「锚点下标不变」）。复现/回归：repro-bs-ifhead{,2}.mjs + 既有 repro-bs-empty/emptydel-head/editjudge-empty 全过。
- [修复] **默认区嵌套结构内键入「判断」被误提为外层 case**（用户报障，Enter/失焦两路同病）：analyzeEnclosingJudge 只按 judge 段找最内层判断、漏「行被更深非判断结构包裹」——段表 maxDepth > judge depth 时返回 null，键入判断走正常嵌套自动展开。合法行为不变（默认区直接键入=加 case、判断开始首 body=嵌套）。复现：repro-judge-in-iftrue{,2,3}.mjs（3=用户完整键入序列+LIFO 配对断言）；注意结束标记行无 .eyc-code-line DOM 不可点击，驱动脚本点其后空行。

### v0.0.5-beta.15（2026-07-17）

- [新增] **图片框 7 个鼠标事件（首个「带参数+逻辑型返回可拦截」的控件级事件）**，并移除原「被单击」事件（用户确认，易语言图片框无此事件；旧工程 `_图片框N_被单击` 编译照过、永不触发）。实现三块：
  - 元数据：window-units.json 图片框 events 落 7 事件（returnType/args 字段，事件顺序即双击默认事件=鼠标左键被按下）；改 lib/krnln 同步 CORE_LIBRARY_EXPECTED_SHA256。
  - 运行时（compiler.ts 窗口 codegen，不涉转译缓存）：CreateControls 前 emit 处理函数原型 + g_ycPicBoxMouse 表 + YcPicBoxMouseProc（SetWindowSubclass，ref=表项），weak `bool f(int,int,int)` 空实现、用户 .eyc 强符号覆盖（两侧签名必须严格一致，Itanium mangling 含参不含返回）；功能键状态按易语言常量编组（Ctrl=1/Shift=2/Alt=4，非 Windows MK_*，Alt 走 GetKeyState）；滚轮只发焦点窗，主窗 WM_MOUSEWHEEL 用 WindowFromPoint+GetDlgCtrlID 查表**直调**（绝不能 SendMessage 回子窗——DefWindowProc 会把滚轮弹回父窗死循环）；事件绑定循环跳过这 7 名（否则「被双击」被 STN_DBLCLK 兜底误绑成无参 void）。实测 STATIC 的 STN_CLICKED 在按下时即发。
  - 验证：typecheck + 260 单测全绿；.tmp-flow-verify/picboxevt-test 无头编译过 zig + verify-picboxevt.ps1 消息探针（SendMessage 注入 7 类消息，断言坐标/功能键编组/正反滚轮/返回真拦截/被单击不再派发）。
- [新增] **事件存根 returnType 链（通用机制）**：window-units.json events[].returnType → libraryManager（LibraryWindowUnitEvent 必填字段，parseWindowUnitEvents + parseWindowUnitsFromProtocol 两处构造）→ LibUnitEvent → Editor.tsx（控件/窗体双击、事件栏导航、挂起导航 PendingSubNavigation）→ EycTableEditor.navigateOrCreateSub 第 3 参 → 生成 `.子程序 名, 逻辑型, , `。任何库事件声明 returnType 即自动生效。
- [优化] **README**：贡献人员名单 +3（ssxiaoqiao 捐赠和测试 / TermINatOr 项目主要测试员 / Tel: 小林子 喵~ 项目测试员，并把「其它群里朋友们」行并回表格）；许可证醒目化——顶部 MIT 徽章+一句话摘要、许可证章节 `> [!IMPORTANT]` 提示框强调「衍生作品须保留版权与许可声明并注明基于 ycIDE」。

### v0.0.5-beta.14（2026-07-17）

- [修复] **到文本(字节集) 印成调试形态 `字节集:41{232,191,…}`**（用户磁盘操作程序对拍暴露）：易语言把字节当文本解（GBK），本 IDE 全程 UTF-8 故按 UTF-8 解码，与 到字节集(文本) 互为往返。调试输出/数组打印保留调试形态（拆出 `yc_bin_debug_text`）。
  - 验证：tests/unit/diskTextDateRuntime.spec.ts（真编译真跑）；filePathEncoding/arrayReturnRuntime 旧断言同步更新。

- [修复] **到文本(日期时间型) 印裸 OA 数字 46220.41…**：日期时间型 与 双精度 在 C++ 里同为 double、`yc_value_to_text` 重载分不开。照 逻辑型→bool 的分侧方子：变量侧引入 `YC_DATE` 强类型（struct{double} 双向隐式转换，ABI 侧仍 double），到文本 印「2026年7月17日9时56分37秒」、时刻全零只印日期（历法换算 civil_from_days 纯算术、不依赖平台 API）。dateTimeTypeAbi.spec.ts 断言同步为 YC_DATE。

- [修复] **寻找文件 无迭代状态、无属性过滤**（句柄开完即关——续查恒空、`.` 目录项也混出）：重写为 首参非空开新一轮/首参空续查/找完收句柄；属性按「文件特殊属性 ⊆ 请求掩码」过滤（显式 0 连存档都排除，易语言原版行为）；省略属性经 YCMD_OMITTED_PARAM_EXPRS 发 -1 哨兵＝除子目录外所有文件。
  - 验证：diskTextDateRuntime.spec.ts 四种掩码（32/0/省略/48 含 `.`/`..`）完整迭代。

- [新增] **取变量地址/取变量数据地址（spec 库）**：此前只有元数据没有实现（spec 无自动桩，谁用谁链接失败 undefined symbol）。实现按「变量地址 + yc_vt_of 类型标签」编组（新增 YCMD_CUSTOM_NATIVE_EXPRS：通用编组表达不了的表达式命令按符号定制，所有调用形态汇到 generateYcmdNativeCommandExpr 拦一处即全覆盖）；文本/字节集/数组给数据缓冲区地址（空则 0）。**x64 适配**：返回类型定为 长整数型（32 位装不下地址），与 指针到* 收长整数型指针的约定对齐。
  - 配套①：**「地址赋给非长整数型变量」双层提前拦截**（用户点名要的）——editorDiagnosticsShared 问题面板/行内红标/全项目扫描三面同源报错 + compiler.ts 转译期 throwSourceError 硬拦截，文案一致。
  - 配套②：**指针到* 家族加 VirtualQuery 可读性防护**——32 位移植代码用整数型接地址截断成垃圾指针时返回零值/空，不再整个程序崩掉。
  - 验证：varAddressRuntime.spec.ts（地址↔指针到整数/字节集往返、空字节集=0、垃圾指针不崩、整数型接地址编译失败）+ editorDiagnosticsParamTypes.spec.ts 4 例。

- [修复] **到字节集(数值) 给的是数字文本的字节**（`到字节集(到整数(123))` 得 "123" 的 3 个 ASCII 字节而非 {123,0,0,0}）：通用编组先转文本把类型丢光。改为定制生成 `yc_to_bin(实参)` 由 C++ 重载分诊（同 到文本 的方子）——整数4/短整数2/字节1/长整数8/小数4/双精度8 字节，文本仍 UTF-8，字节集恒等；整数字面量生成侧带 LL 后缀须包 (int) 拨回 4 字节；数组参数转译期友好报错。

- [修复] **取字节集数据 起始索引按 0 基处理**：帮助明说「索引值从 1 开始，省略默认 1」——已改一基（省略/0 钳位到 1）。注意 取/置字节集内整数 的参数是「偏移」（0 基），两族基准不同、均以帮助原文为准。

- [修复] **命令表达式 IIFE 返回类型漏吃 YCMD_NATIVE_RETURN_TYPE_OVERRIDES**（通用缺陷）：lambda 口只用清单 returnType（通用型→默认 int），声明是 long long → 8 字节返回值静默截成低 32 位（取字节集数据 #长整数型 拿到 9876543210 的低 32 位 1286608618；「差值恰为 2^32 倍数」是这类截断的铁证）。

- [修复] **到文本(小数型) 印出 double 噪声位**（3.14159 → 3.14159011840820）：float 只有 ~7 位有效数字，改 %.7g（调试输出同步）。
  - 以上三条验证：binAbiRuntime.spec.ts 新增 12 项断言（各类型字节数/一基读回 4+2+1 布局/省略默认 1/取变量地址→指针到字节集→取字节集数据 8 字节完整往返/float 印字）。

- [修复] **AI diff 高亮的表格行，数据类型列与备注列没有底色**：这两列 td 挂着语法渐变字技术（background-clip: text），它会把单元格自身全部背景（含 diff 绿/蓝底）剪进字形导致整格底色消失（选中行用 background 简写整组重置了这些长属性所以无此问题）。diff 行内这两列退回纯色文字、恢复正常裁剪。Electron 真像素截图验证（修复前精确复现、修复后整行连续）。

- [新增] **AI 面板编辑进行中状态**：Agent 模式 edit_file 开始时流式播报并把执行状态行切为「正在编辑 xx…」（完成/失败自动恢复）；Edit 模式「正在编辑」状态从仅应用阶段扩展到生成+应用全程。

- [新增] **AI 面板聊天区贴底自动滚动**：此前四个模式都不跟随流式输出。现仅当用户贴在底部（48px 阈值）时跟随、上翻阅读不打扰、滚回底部恢复、切换模式重新贴底（useLayoutEffect 无闪跳）。

- 工程：TRANSPILE_CACHE_VERSION 32→35；krnln 完整性哈希同步；全量单测 42 文件 260 用例绿。

### v0.0.5-beta.13（2026-07-16）

- [新增] **通用对话框组件完整接入**（用户报「通用对话框“打开文件通用对话框”的方法“打开”暂不支持在代码中调用」）。按系统核心支持库帮助说明文件 5790–5931 行实现：非可视组件（无 HWND），照时钟的按实例名派发模式做全套。
  - **运行时**（lib/krnln/impl/windows.cpp）：库内按实例名状态表 `g_ycCommDlgs` + `krnln_commdlg_get_int/set_int/set_text/get_text(名,propId)` + `krnln_commdlg_open(名)`——按「类型」分派 GetOpenFileNameW / GetSaveFileNameW / ChooseFontW / WinHelpW；过滤器「显示文本|匹配符」竖线格式转 NUL 双结尾；初始过滤器 0 基↔nFilterIndex 1 基；确定后回写 文件名/初始过滤器/字体族属性；取消返回假。
  - **声明式协议**（window-units.json）：22 个属性带 access get/set 模板（非可视组件全用 `{n}` 名占位）+ 顶层 controlMethodBindings「打开」；属性面板/补全/点击提示同步可用。
  - **编译器**：创建循环非可视分支（不 CreateWindow，设计器改过默认值的属性在窗口创建期灌入库状态表）；前导 externs + main.cpp `yc_commdlg_get_text→YC_TEXT` 薄封装；isTextExpression 收录。
  - 验证：tests/unit/commonDlg.spec.ts（编译派发 + 运行时状态往返真跑）+ 真 GUI 驱动（点按钮→系统打开文件对话框带设计期标题弹出→取消→打开() 返回假不进分支）。

- [修复] **文件命令打不开中文路径**（用户对话框选中桌面「订阅链接狗狗云.txt」后 打开文件 返回 0、编辑框无内容）：编组交给支持库的路径是 UTF-8 字节，但 `打开文件` 用窄字符 `_fsopen`、`读入文件`/`写到文件` 用 char* ifstream/ofstream——按 ANSI 代码页解释 UTF-8，中文路径/文件名必然打开失败。库里其它路径命令（创建目录/删除目录/取文件时间/取节名）早就 utf8ToWide + 宽 API，这三个是漏网。修：统一转宽字符走 `_wfsopen` / `std::filesystem::path`。**约定：凡新加吃路径的 krnln 命令，char* 入参必须先 utf8ToWide。**

- [修复] **读入文本/读入一行 读「ANSI」编码 txt 整段乱码**：此前把文件字节一律当 UTF-8 交回。新增 ycFileTextToUtf8 自动识别：合法 UTF-8 原样（剥 BOM）；FF FE/FE FF 按 UTF-16 LE/BE 解码；其余按系统代码页（简中=GBK）解码——记事本三种常见编码全覆盖。写出文本 仍写 UTF-8（自产自读一致，外部 GBK 只保证读）。
  - 验证：tests/unit/filePathEncoding.spec.ts（中文目录+中文文件名打开/读文、GBK/UTF-8 BOM/UTF-16 内容解码、写到文件/读入文件中文路径字节集往返，真编译真跑）。

- [修复] **无点形态的语句级流程命令（`结束 ()`/`返回 ()`/`跳出循环 ()`/`到循环尾 ()`）静默无操作**：krnln 命令表里有同名变参空桩（krnln_end 等），无点形态被当普通命令派发到空桩——程序不退出/不返回/不跳出（在通用对话框运行时测试中实测暴露）。修：语句级流程命令两种形态同义（先于支持库命令派发拦截）；块结构关键字（如果/循环首尾等）无点直接给带行号的中文报错，不放行空桩让块静默失效。
  - 验证：tests/unit/flowCommandForms.spec.ts（两种形态逐行同义真跑 + 无点块关键字报错）。

### v0.0.5-beta.12（2026-07-16）

- [修复] **代码行先敲空格再敲流程命令（如「如果」）回车，流程线与文本错位**：输入归一化 `normalizeCodeLineInput` 不剥行首空白，手敲的前导空格被 `formatCommandLine` 当成结构缩进落盘（如果/否则/如果结束整体落在错误缩进上），还残留一行纯空格垃圾行。修：归一化开头剥除行首空白——表格模式缩进由编辑器按流程结构管理，空格只在文本字面量内有意义，而字面量不可能出现在行首空白里。现在行首按空格直接无效。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`.tmp-harness/repro-userspaces.mjs`（敲入式场景）；npm test 全绿

- [修复] **嵌套结构里的注释失焦后向右漂移、获得焦点又左移复位**：编辑态输入框永远紧跟流程线格起排，但显示态只跳过「树线格」——注释行的缩进被 colorize 渲染成普通空格 span（非 eTreeLine），一个都跳不掉，流程线格＋全部字面缩进叠加渲染；而编辑器自己就会造出「缩进＞4×流程深度」的行（嵌套体 flowIndent=头缩进+4），失焦必右漂。修：有流程线的行，前导缩进（树线格/余数空格/注释缩进）全部由流程线格替代，显示态与编辑态文本 X 恒一致（= 4ch×流程格数）；对文件里已存在的病态缩进（如带前导空格的旧结构）显示也直接自愈，不改源码。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`.tmp-harness/repro-userspaces.mjs`（失焦/聚焦像素级同位）+ `repro-usersrc.mjs`（用户病态原文自愈截图）；流程线审计基线 repro-flow-audit/audit2 无回归

- [修复] **焦点在表格单元格时粘贴多行代码，整段代码被压成一行挤进单元格**（如 `.局部变量 i信息框 （1） 信息框 （2）, 整数型`）：单元格输入框的 onPaste 无条件拦截事件走原生粘贴。修：与代码行输入框同规则——单行剪贴板原生进单元格，多行改走文档级整行粘贴（这些输入框的值都只住在单一源码行里，装不下多行）；粘贴后统一退出编辑态并压掉 blur 回写，避免行号位移后旧值写回错行。参数行输入框同批覆盖。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/useEditorInteractionHandlers.ts`
  - 验证：`.tmp-harness/repro-cellpaste.mjs` 三场景（单元格多行→文档、单元格单行→原生、代码行多行→回归）

- [新增] **问题面板双行号**：此前只显示文本模式（源码）行号，与表格模式行号槽对不上（隐藏的 .版本/空行/流程标记不占表格行号）。现在显示「第 X 行, 第 Y 列（文本 第 Z 行）」——X 为表格行号槽编号（与行号槽同一份映射，构造上必一致），Z 为文本模式行号；文本模式下的诊断、设计时问题保持原样。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`OutputPanel.css`
  - 验证：`.tmp-harness/repro-problemline.mjs`（displayLine=行号槽实测值）+ `repro-problempanel.mjs`（面板三形态渲染）

- [修复] **表格行鼠标 hover 只有类型/备注列有背景色**：行 hover 背景设在 tr 上，但每个 td 自带不透明表格背景把它盖住，只有着色类背景透明的列能透出。修：hover 时放开 td 背景让 tr 色透出（与选中/编辑态既有的 td 覆盖规则同族；选透明而非按格重涂，保住渐变主题整行连续、避免半透明双重叠加）。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`.tmp-harness/repro-tablehover.mjs`（全列 computed 背景 + 选中蓝覆盖不受影响）

- [修复] **双击问题面板定位后，表格行闪烁高亮各列深浅不一**（类型/备注深、其它列浅）：闪烁的半透明 box-shadow 同时打在行容器（tr）和每个 td 两层——透明背景格双层叠色更深、不透明格单层更浅。修：只在叶子层（td/代码行）闪，行容器不再闪；顺带把代码行区域此前「容器+代码行」的双层叠色一并归一。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`.tmp-harness/repro-problemflash.mjs`（animationName 断言 + 闪烁中段截图全列同深）

### v0.0.5-beta.11（2026-07-16）

- [修复] **新建的编辑框运行后是黑底**（用户：设计器显示白底 #ffffff，运行后变黑；未设过任何颜色属性）。
  **①根因是哨兵撞车，不是颜色属性**：颜色表为空时填的占位项 id 写的是 **0**，而查表处 `colorParentId = GetDlgCtrlID(父窗口)`——**顶层窗体没有控件 ID，GetDlgCtrlID 对它返回 0**，占位项于是被 colorParentId 撞上，未配色控件全部拿到占位项的 `backColor=0` → `CreateSolidBrush(0)` → 黑底。建表处原注释写的是「空占位 id=0 **永不匹配**」——这个前提本身就是错的。（顺带排除嫌疑：兜底那句 `DefWindowProcW` 没错，单独测它确实返回系统白刷；错的是它前面的循环。）
  **②触发条件很刁**：占位项只在**没有任何控件产生颜色表条目**时才有。标签/选择框/单选框/分组框默认白底恒进表（见 beta.6），编辑框/列表框默认色则不进表——所以「按钮+编辑框」的空项目全黑，**往窗口上拖一个标签就又白了**。`defaultBgColor.spec.ts` 早在断言色表内容，却照样漏掉——因为错的不是表、是查表。
  **③修法**：占位项 id 改 `-1`（真实项走 `IDC_<名>` 宏、从 1001 起，永不为 0 或负）+ 查表处加 `if (id <= 0) continue` 守卫，把「0 不是任何真实控件的 ID」这条不变量写在读者需要它的地方；顺带改掉那句错误的注释。
  **验证**：新增 `ctlColorFallback.spec.ts`——真编译 GUI 程序、真跑、`GetPixel` 量编辑框与列表框客户区（`#000000` → `#FFFFFF`，与系统 COLOR_WINDOW 一致）。**回填旧逻辑验证过测试是承重的**（实报 `expected '#000000' to be '#FFFFFF'`，不靠 codegen 字符串匹配）。用例特意不放标签 + 加前提自检，确认真的命中「空表→占位项」那条路。窗口 .cpp 不走转译缓存，无需 bump。

- [修复] **`≈`（近似等于）运算符压根没接线**，原样漏进 C++ 报 `unexpected character <U+2248>`（用户对拍逻辑比较时暴露；后续的「类型不兼容」「expected ';'」都是它的连锁反应）。命令形态 `近似等于 (a, b)` 与实现 `krnln_like` 一直是好的，缺的只是运算符。
  **修法**：`≈` 同 `÷` 一样**不是 C 运算符**（它落成命令调用），故原样带过 `convertFullWidthOps`，由 `findTopLevelComparison` 认出（与 `＝`/`＜`/`＞` 同级）、比较分支落成 `近似等于` 的命令调用——走命令机器而非自己拼 `krnln_like`，因为「文本型 → const char*」那套编组（YC_TEXT 经 yc_wide_to_utf8）都在那边。顺带接帮助里写明的同义写法 **`?=`**（须先归一成 ≈，否则它里面的 `=` 会被比较拆分器切成 `a ?` = `b`），以及编辑器三处：`formatOps` 的 `?=`→`≈` 归一+空格规范、着色的运算符切分、token 排除集。
  **语义按帮助原文钉死**：「当比较文本在被比较文本的**首部被包容**时返回真，运算符号为『?=』或『≈』」——即 `A ≈ B` = 「B 是 A 的前缀」，**大小写敏感、不做全角半角归一**。用户原以为是「忽略大小写」，**易语言实测证实帮助是对的**（`“Hello” ≈ “hello”` 得假）。
  **验证**：新增 `approxEqual.spec.ts`——真编译真跑，帮助语义逐条钉（前缀命中/不中/全等/比较文本更长/空比较文本/中文前缀），外加「运算符与命令形态结果必须相同」、`?=` 同义、括号内/且混排/取反/当条件，并用三条断言钉住「**≈ 不是模糊匹配**」免得后人想当然改。另有 `formatOps` 单测钉 `?=` 归一且不动字面量里的 `?=`。

- [修复] **`到文本 (逻辑型)` 印「1」/「0」，易语言印「真」/「假」**（用户对拍：约 25 行受影响）。
  **①根因**：`逻辑型` 与 `整数型` 在 C++ 里**同为 int**，`yc_value_to_text` 的重载根本分不开，只能一律印数字。
  **②修法（用户选的「彻底做」而非转译期打补丁）**：拆出 `mapTypeToVarCType`（**用户代码侧**：逻辑型 → C++ `bool`）与 `mapTypeToCType`（**原生 ABI 侧**：逻辑型 恒 `int`），16 处切 var 侧、5 处 ABI 侧原样保留。配 `yc_value_to_text(bool)` 重载后，**分诊全交给 C++ 重载解析**——变量／子程序返回／命令返回／比较表达式（`==` 本就产出 bool）一律自动落到「真/假」，转译期一个特判都不写。
  **③为什么默认留在 ABI 语义那边**：漏改一处 var 侧只是印成「1」（看得见）；漏改一处 ABI 侧却是**静默的内存错位**（`krnln_and(int,int)` 换成 bool 后 1 字节 vs 4 字节）。两侧在调用点由 C++ 隐式转换衔接，无需额外编组。已确认自定义结构体不跨 ABI、无任何按字节读写，故字段随之变 bool 是安全的。
  **旁证**：先前为 `≈` 写的测试**自己挂了**——`近似等于` 返回逻辑型，改完自动印「假」而不是「0」，正是易语言的行为，也正是这套改法该有的效果（测试预期已更新）。

- [修复] **文本型的 `＜` `＞` `≤` `≥` 是「指针比较」**（用户对拍：四行结果全反）。比较分支此前只放行 `==` 与 `!=` 走 `yc_text_compare`，其余四个落到裸 C 的 `(left op right)`——`YC_TEXT` 有 `operator const wchar_t*()`，于是 C++ 拿两个**地址**比大小。
  **症状不是「按数值比了」而是随栈布局变的垃圾**：曾出现 `“10” ≤ “10”` 为假、同时 `“10” ≥ “10”` 为真这种自相矛盾——两个互补运算符对同一对值给出矛盾结论，正是「比的不是内容」的铁证。**这是不确定行为，换个编译版本结果就可能变。**
  **修法**：六个运算符全走 `yc_text_compare`（= `lstrcmpW`，返回 <0/0/>0，故 `yc_text_compare(a,b) < 0` 即字典序 `a<b`）。易语言的文本比较正是字典序：`“5”` 首字符 `'5'`(0x35) > `“10”` 的 `'1'`(0x31)，故 `“5” > “10”`，与数值大小相反。
  **同批修「同一个比较换个书写位置结论就变」**：`formatArgForC`（**23 个调用点**）与 `COMMAND_EXPR_GENERATORS` 的签名里根本没有 `variableTypeResolver`，`isTextRawOperand` 认不出操作数是文本型 → `到文本 (甲 ＝ 乙)` 退化成指针比较，而 `结果 ＝ 甲 ＝ 乙` 却是对的。逐个穿参数不现实，故照本文件既有的 `currentTranspileArrayVars` 路子挂文件级兜底 `currentVariableTypeResolver`（转译入口清空防跨文件泄漏；闭包读实时 `visibleDebugVars`，作用域自动跟进）。
  **验证**：新增 `logicCompare.spec.ts`——真编译真跑，**断言值全部取自易语言实测输出**（22 条逐行对齐），含反向钉子「整数型仍印数字 10」（逻辑型改 bool 不许把它带跑）与「命令实参形态必须与赋值形态结论一致」。

- [修复] **控制台工程里只要写文本比较就链接失败**：`未定义符号: yc_text_compare`。`yc_text_compare` / `yc_text_starts_with` 是纯文本函数（`lstrcmpW` / `wcsncmp`、与窗口无关），定义却被关在 `if (isWindowsApp)` 分支里，而转译前导里的声明是**无条件**发的。已搬到窗口分支之外。（是它挡住上一条的控制台回归测试才暴露的；回填改动验证过是老问题，非本批引入。）

### v0.0.5-beta.10（2026-07-16）

- [修复] **命令返回值直接当条件（`.如果 (是否运算正确 (局_结果小数))`）生成出不合法的 C++**——用户拿到的是难懂的 `value of type '(lambda …)' is not contextually convertible to 'bool'` + `expected expression`。
  **①根因**：`wrapConditionForC` 用「首字符是 `(` 且尾字符是 `)`」判断条件已被括号包住，但**这俩括号未必互相配对**。命令表达式的通用生成形态是 IIFE `([&]() -> int { … })()`：首尾都是括号，可开头那个 `(` 配的是 `})(` 里的 `)`，末尾那个 `)` 属于调用括号。于是 `if ${cond} {` 拼出 `if ([&]() -> int { … })() {`——条件在 lambda 收尾处提前闭合。
  **②影响范围**：所有「把命令返回值直接当条件」的写法，即走 `wrapConditionForC` 的 4 个生成点 / 7 个流程语句：`.如果`、`.如果真`、`.判断`、`.判断开始`、`.判断`（else-if 分支）、`.判断循环首`、`.循环判断尾`。比较条件 `(a == b)` 因首尾括号本就配对，一直没事——所以这个 bug 藏到现在。
  **③修法**：新增 `isSingleParenGroup()` 做真实配对扫描（跳过字符串/字符字面量里的括号，如 `yc_text_compare(x, L"(")`），判不准就多包一层（只多余不出错）。**不能无脑全包**——`if ((a == b))` 会触发 clang 的 `-Wparentheses-equality` 警告刷屏，那个「已带括号就不再包」的分支是有用的，错的只是它的判据。
  **验证**：新增 `conditionParens.spec.ts` 2 条（IIFE 条件真编译过 + 断言不再出现 `if ([&]`；普通比较条件不被多包一层、无 parentheses-equality 警告）。回填旧逻辑跑测试可当场复现原报错，确认测试是承重的。用户实际项目（`windows窗口程序-新项目1` 的数学运算测试）现 0 错 0 警告编译通过。

- [新增] **C++ 编译诊断 → 易语言源码位置 + 中文说明**（用户反馈「这错误提示能不能友好一点，这是难懂的 c++ 错误」）。此前编译器报错原样透传给用户：英文 clang 诊断 + 指向 `temp/*.cpp` 这个用户从没写过的中间产物的行号。
  **①行号映射**：`appendSubLine` 给生成的每行 C++ 打 `/*@<eyc行号>*/` 前缀。标记**留在 .cpp 文件里**不剥掉——转译缓存命中时压根不重跑转译，映射必须能从产物自身恢复（`loadEycOrigin` 只在报错时按需回读 .cpp 重建 `cpp行号 → eyc行号`，成功编译零开销）；顺带让人工翻 `temp/*.cpp` 排查时能直接看出每行出处。`TRANSPILE_CACHE_VERSION` 28→29（旧缓存产物没标记）。
  **②面板输出**：`_启动窗口.cpp:2164:9: error: …` → 「错误: _启动窗口.eyc 第 152 行」+ 回显那行易语言源码 + 中文说明 + 归咎方。clang 的源码回显行/插入符行/`note:` 全挡掉（秀的都是用户没写过的 C++），**C++ 原文改为无删减进编译诊断日志**（此前 stderr 压根没进日志，只有 zig 命令行参数进了）。
  **③归因分三档，不瞎认锅**：`user`（中日韩名字的 undeclared identifier = 用户拼错，如 `.当循环首` 其实该写 `.判断循环首`——流程语句名写错会被当自由命令调用生成出去，转译期的「未定义裸标识符」友好报错只 gate 裸标识符、管不到调用形态）／`codegen`（`expected …`、lambda 转 bool 失败等，用户再怎么写也写不出来，必是 ycIDE 的锅→「不是你的代码写错了，请反馈」）／`mixed`（类型对不上：多半是用户自己类型用错了、只是转译期没拦住，**这一档绝不能说「不是你的代码写错了」**，那句话在这里多半是假的）。命不中翻译规则就保留英文原文 + 「ycIDE 还没认识这条错误」，不装作知道。
  **【坑】zig 的 stderr 是 CRLF**：JS 正则里 `\r` 是行终止符、`.` 不匹配它，行尾留着 `\r` 会让所有以 `$` 收尾的诊断正则**静默全失配**——功能不报错，只是悄悄退化成透传英文原文。必须先 `trimEnd()`。（本次实现里踩到，靠插桩才定位。）
  **验证**：新增 `friendlyCompileErrors.spec.ts` 4 条（回溯行号+回显源码+中文说明+C++ 噪音不进面板／类型不匹配不甩锅给 ycIDE／原文留在诊断日志／生成代码带来源标记）。全量 235 绿、tsc 0 错、lint 警告 111 未增。
  **遗留**：翻译规则只覆盖实测见过的形态（undeclared identifier／not contextually convertible／expected …／no matching function／no member named／参数与赋值类型不匹配／invalid operands）；没覆盖的走原文兜底。类型不匹配理应在转译期就拦住（那才是根治），本条只是让漏到 C++ 层的报错可读。

- [修复] **相除 `÷` 丢掉小数部分**（用户拿数学运算测试对拍易语言发现：`局_整数1 ÷ 局_整数2`，20 ÷ 7 得 **2**，易语言是 2.857142857143）。
  **①根因**：`convertFullWidthOps` 把 `÷`(相除) 与 `＼`(整除) **映射成同一个 C 的 `/`**。C 的 `/` 在两个整数操作数下就是截断除法，小数部分整个丢掉。易语言里 `÷` **恒返回双精度小数**、`＼` 才是截断整数商——两者语义不同，不能同形。左操作数已是小数时（如 `30 × #pi ÷ 180`）C 会自动提升为浮点除法，所以这个 bug 只在**两边都是整数**时现形，藏到现在。
  **②修法**：`÷` 先记成私用区哨兵 `REAL_DIV_MARK='\uE000'`（类别 Co，不被 `\p{L}` 与本文件标识符区间命中）带过表达式拆分，`findTopLevelMultiplicative` 认它，乘除分支再按操作数类型生成 `((double)(左) / (double)(右))`。大数(YC_BIG)分支显式换回 `/`——大数没有小数表示，`÷` 与 `＼` 在大数上同为整数商。`findTopLevelAdditive` 判「`-` 是一元还是二元」的 prev 字符集也补上哨兵，否则 `a ÷ －b` 会被切成 `(a ÷) - b`。**哨兵不是合法 C**：凡没走到乘除分支的路径（旧的 fne 实参编组 `formatArgForYcCommand` ×2、项目常量 `#define`、拆分兜底）一律过 `inlineRealDiv()` 落成 `a /(double) b`（合法 C 且语义仍对——`(double)` 结合力高于 `/`、`*`）。`TRANSPILE_CACHE_VERSION` 29→30（旧产物里是截断的整数除法）。
  **③连带修**：**整体被一层括号包住的表达式此前会绕过全部翻译、原样落进 C++**——`parseCommandCall` 见首字符是 `(` 就 name='' 返回 null，四个 `findTopLevel*` 又全在 depth>0 上空转，于是掉到函数尾 `return translated` 原样吐出。症状：`(“甲” ＋ “乙”)` **根本编不过**（全角引号漏进 C++）、`(20 ＋ 7)` 的字面量拿不到 `LL` 后缀、`(a ＝ b)` 不走 `yc_text_compare`；不修的话 `(a ÷ b)` 还会把哨兵漏出去。新增 `matchEnclosingParens()`（首个 `(` 的配对括号必须正好是末字符，认全角括号/引号）+ 剥掉递归再**原样包回**——括号是调用方维持优先级的，剥没了 `(a ＋ b) × c` 会变成 `a + b * c`。
  **验证**：新增 `realDivision.spec.ts` 3 条——真编译控制台 exe、真跑、读 stdout（codegen 断言证不了优先级/结合性/一元负号，那些只有跑起来才现形）：`20 ÷ 7`=2.857…／`(i1 ÷ i2)` 带括号／`100 ÷ i1 ÷ 2`=2.5 左结合／`1 ＋ 20 ÷ 8`=3.5 优先级／`20 ÷ －8`=-2.5 一元负号／`20 ÷ 4`=5 除尽仍印整数形态，外加 `＼`=2 与 `％`=6 两条**反向钉子**（修 ÷ 不许把整除和求余带偏）。全量 239 绿、tsc 0 错、lint 0 错。

- [修复] **字符串字面量里的全角运算符被当成运算符改写**（同上对拍暴露：用户写 `“20 ＋ 7 ＝ 27”`，打印出来是 `20 + 7 == 27`）。
  **①根因**：`convertFullWidthOps` 对整串无脑正则 replace，**不区分引号内外**——`＋→+`、`＝→==`、`×→*`、`÷→/` 连字面量一起改。引号里的符号是要原样打印给用户的**文本**，不是运算符。因为「看着只是符号长得不一样」，对拍时极易被当成无关紧要放过。
  **②为何必须同批修**：`÷` 改走哨兵后这条从「错得可见」变成「错得看不见」——`“ ÷ ”` 会被打成 REAL_DIV_MARK 那个私用区字符（终端里是个空白/方块）。
  **③修法**：`convertFullWidthOps` 改成按引号分段（`“…”` 与 `"…"`，同 `findTopLevel*` 系列的约定、都不支持转义），只对引号外的片段跑 `convertFullWidthOpsInCode`；引号未闭合则剩下整段按字面量原样带走。
  **验证**：`realDivision.spec.ts` 第 3 条（`“20 ＋ 7 ＝ ” ＋ 到文本(n)` 拼接式里字面量与真运算并存、`“10 ÷ 2 与 20 ＼ 7”`、比较符 `＝＜≥≠` 全部原样）。用户那份数学测试现与易语言**逐字一致**（除 `#pi` 相关四行，见下）。
  **遗留**：`replaceConstantRefs` 剥 `#` 时**同样**没有字面量感知（`“#123”` → `“123”`），本批未修。

- [修复] **`四舍五入` 的「被舍入的位置」参数被完全忽略**——`四舍五入 (3.14159, 2)` 恒返回 **3** 而非 3.14（用户对拍时归类成了「精度问题」，实为**没实现**）。
  **①根因**：`krnln_round` 的实现签名是 `(double value, ...)`，第二参数声明成变参后压根没读，函数体只有 `std::round(value)`，于是任何位置参数都等价于舍入到整数。转译侧生成的声明一直是 `(double, int)`、实参 `2` 也一直在传，是**被调用方丢了**。
  **②修法**：签名改成 `(double value, int digits)`（与转译侧生成的声明本就一致），按帮助实现三档语义：>0 保留小数点右边该位数、=0 舍入到整数、<0 舍入到小数点左边该位置。省略该参数时转译侧已按帮助默认填 0，行为不变。挡住 `|digits| > 308`——`std::pow(10, ±400)` 会溢出成 inf/0，不挡会得到 nan。改 `lib/krnln/impl/windows.cpp` 故同步 `CORE_LIBRARY_EXPECTED_SHA256`（`krnln_round` 只在 windows.cpp 有，linux/macos 无此条）。
  **验证**：新增 `roundDigits.spec.ts`（真编译真跑）逐条对齐帮助示例：`(1056.65, 1)`→1056.7、`(…, 0)`→1057、`(…, -1)`→1060，外加保留 2/4 位、省略参数默认 0、负数、位数极大/极小不产 nan。
  **注**：`#pi` 相关的四行（`求正弦(30°)`／`求余弦(60°)`／`求正切(45°)`／`求反正切(1)转角度`）与易语言仍不同，**是易语言错、我们对**：反推出易语言的 `#pi` 被截断到 `3.1415926535`（10 位小数），用它重算能把易语言那四个值**逐位复现**；ycIDE 的 `#pi` 是完整双精度 `3.141592653589793`，故 sin(30°) 就是 0.5、tan(45°) 就是 1。经用户确认**保持现状不对齐**（不为兼容而故意引入误差）。

### v0.0.5-beta.9（2026-07-15）

- [新增] **数组返回 ABI + `分割文本`/`取配置节名`/`多文件对话框` 真实现 + `取选中项`→`取所有被选择项目` 正名**（承接上条「分割文本 定性为未实现」的后续；上条判「须先设计数组返回 ABI」，本条即是）。
  **①摸底纠正上条的两处判断**：数组支持其实**只缺返回方向**——传参方向早就完整（`YCMD_ARRAY_PARAM_KINDS` 已在跨 TU 传 `(void*)&数组变量` → impl `reinterpret_cast<std::vector<long long>*>`，元素三态存储 int 直存/f64 位模式/text 指针位模式俱全，读回是 `((wchar_t*)(intptr_t)yc_ary_at(a,i))`）；且 `列表框.取所有被选择项目` 的 `yc_lb_get_sel_items` **本就返回 `std::vector<long long>`**，数组返回的先例已在跑。
  **②ABI 设计（零结构改动接进现有机器）**：现有调用生成本就是 `([&]() -> <mapTypeToCType(returnType)> { return <returnType.expr(callExpr)>; })()`，故只需两处补位——`mapTypeToCType` 补 `YCMD_ARRAY_RETURN_TYPES` 行（`X数组`→`std::vector<long long>`，**又是「缺行掉默认 int」这个咬过三次的病**）+ `mapYcmdNativeReturnType` 加数组分支（impl 堆上 `new std::vector<long long>` 以 `void*` 交回 → 生成侧 `yc_ary_take` 移走内容并 `delete`）。跨 TU 传 vector* 的假设**是既有契约，不新增风险**。配套 `yc_ary_tmp` 轮转槽（8 槽，同 `yc_c_str_slot` 路子）让帮助里的规范用法 `复制数组(目标, 分割文本(…))` 成立——临时量取不了址。
  **③白名单必须按符号给（关键安全约束）**：krnln 512 条里 **379 条是自动桩**，桩体 `return keepUtf8("[]")` 是 `const char*`。若让数组 ABI 只看类型名生效，`取所有发音` 会自动吃到 → `yc_ary_take` 把字符串字面量当 `vector*` 解引用并 `delete` → **直接崩，比原先的静默垃圾更糟**。故 `YCMD_ARRAY_RETURN_SYMBOLS` 只收有真实现的 3 条；其余数组返回命令由 `assertYcmdArrayReturnSupported` 转译期友好报错（`取所有发音`→「尚未实现」、`分割字节集`→「暂不支持该数组元素类型」）。桩签名同步收敛成 `void*`/`nullptr`（`yc_ary_take(nullptr)`=空数组），让声明与实现不错位、万一被绕过也只退化不崩。
  **④省略 vs 显式空文本**：通用文本映射把两者都译成 `(const char*)""`，但帮助里 `分割文本` 的分隔符**省略→默认半角逗号、显式空文本→整段不分割**，语义不同。转译期本就知道（实参没写 vs 写了 `""`）→ 新增 `YCMD_NULL_WHEN_OMITTED_PARAMS`（省略发 `nullptr`）。
  **⑤顺带修**：`fmtOne` 与 `mapYcmdNativeParamType` 通用型分支里**重复了两遍**的元素类别判定 → 抽成 `arrayValueElemKind()`，并让它认得「返回数组的命令」——`调试输出(分割文本(…))` 此前会把文本指针当整数印。
  **⑥审计救场两次**：`分割文本` 的参数名与默认分隔符我是从**乱码终端输出**转录的（帮助文件是 UTF-8，乱的是控制台代码页，不是文件），元数据审计当场打脸——真名是「待分割文本/用作分割的文本」，且**默认分隔符是半角逗号不是空格**（impl 里已是真语义 bug，改 `L","`）。
  **成果**：元数据审计 **1 条 → 0 条**（ycmd 里每条能唯一匹配帮助块的命令现已全部与帮助一致）；签名审计不一致 **120 → 115**（D1 52→48、D2 32→31：split/GetAllPY/GetSectionNames/OpenManyFileDialog/GetSelItems 五条声明与实现对齐，SplitBin 未动）。链接补 `-lcomdlg32`（`GetOpenFileNameW`，被现有测试当场抓出）。`TRANSPILE_CACHE_VERSION` 27→28、两个核心库 SHA 同步。
  **验证**：新增 `arrayReturnCommands.spec.ts`（6 条 codegen+编译断言，含**声明/实现对齐**的永久回归钉）+ `arrayReturnRuntime.spec.ts`（**真编译控制台 exe、真跑、读 stdout**，7 条语义逐条对齐帮助：普通/省略分隔符/显式空分隔符/空文本/限 n 段/多字符分隔符/复制数组走临时槽）。全量 214 绿、lint 0 错。
  **遗留**：`分割字节集` 需先给元素存储加 bin 类别（「字节集数组」变量本身就不支持）；`取所有发音` 需国标汉字→多音字拼音表（数据活，非 ABI 活）——两条现在都会友好报错而非静默出错。（`取所有发音` 已在下条落地。）

- [新增] **「拼音处理」整类 5 条落地：内置 6763 字国标拼音表**（承接上条遗留的「取所有发音 需拼音表」）。
  **①一张表解锁 5 条，不止 1 条**：帮助的「拼音处理」类共 5 条且共用同一张表——`取所有发音`〈文本型数组〉、`取发音数目`〈整数型〉、`取拼音`/`取声母`/`取韵母`〈文本型〉(汉字,索引)。**5 条元数据本来就全对，5 条 impl 全是桩**（`取发音数目` 恒返回 1；`取拼音`/`取声母`/`取韵母` 返回写死的 `"PY"`/`"SM"`/`"YM"`）——即 5 条静默错误一次清掉。
  **②数据源**：`pinyin-pro` 已是项目依赖（渲染进程的命令补全在用，但那边只取 `[0]` 单音）。`multiple:true` 给多音字，首项即常用音，与帮助「多音字的第一个发音为常用音」一致。新增生成器 `scripts/krnln/generate-pinyin-table.mjs`（npm script `krnln:pinyin:generate`）。
  **③【坑】iconv-lite 的 `'gb2312'` 实际走 GBK**：按往返编码判定「是否国标汉字」会让整个 U+4E00..U+9FA5 全通过 → 得 **20902 字**（表 483KB），而 GB2312 只有 6763 字。必须按 **GB2312 字节范围**判定（首字节 0xB0-0xF7、次字节 0xA1-0xFE = 一级 3755 + 二级 3008）。改判据后正好 6763 字、699 个多音字、156KB。**保守跟帮助走**（「目前仅支持国标汉字」+「非国标汉字返回 0 成员空数组」是用户探测国标与否的手段）；要放宽到 GB18030 只是生成器一行。
  **④表放独立文件**：`lib/krnln/impl/pinyin-table.inc`，由 windows.cpp `#include`。可行性已核实——支持库源码是**原地编译**的（`compiler.ts` 直接 `args.push(lib.libraryPath)`），同级 `#include` 能解析；打包侧 `scripts/package-libraries.mjs` 用 `walkFiles` 递归整个 `lib/`，新文件自动进包；`CORE_LIBRARY_EXPECTED_SHA256` 是逐文件校验（非完备性检查），加条目即可。**没塞进 windows.cpp**：156KB 数据会让这个手写文件一半是噪音（windows.cpp 反而从 155KB 瘦到 153KB）。
  **⑤取声母/取韵母的语言学约定**：按汉语拼音方案的 **21 声母表**取最长前缀（**zh/ch/sh 必须先于 z/c/s 试**），y/w 不在表内（零声母）→ `一`(yi) 取声母得空文本，与帮助「该汉字此发音无声母，将返回空文本」一致；韵母 = 声母之后的部分（零声母则整串，`一`→`yi`）。
  **验证**：`arrayReturnRuntime.spec.ts` 加一条**真编译 exe 真跑**的用例，8 项断言全绿：多音字 `行`→`3|xing/hang/heng`（常用音在首）、单音 `中`→`1|zhong`、非汉字→`0`、只取首字 `行走`→`3|xing`、`取发音数目` `长/中/A`→`2/1/0`、`取拼音` 越界→空文本、`取声母` `重/行(2)/一`→`zh/h/(空)`、`取韵母`→`ong/ang/yi`。全量 215 绿、lint 0 错。
  **遗留**：只剩 `分割字节集`（需元素存储加 bin 类别）。（已在下条落地。）

- [修复/新增] **数组元素存储加第 4 类 bin（字节集数组）+ `分割字节集` 落地 + 顺带修好「文本数组根本存不进值」**（承接上条遗留）。
  **①【重磅顺带】`加入成员(文本数组, 值)` 与 `文本数组[i] ＝ 值` 此前根本编译不过**——两处都写作 `(long long)(intptr_t)yc_value_to_text(...)`，clang 报 `no matching conversion from 'YC_TEXT' to 'intptr_t'`（C 风格转换不肯把 YC_TEXT 连着经 `operator const wchar_t*()` 再转 intptr_t）。即**文本数组建得起来却存不进东西**，两条最常见写法全死，且无任何测试覆盖。修法照 `buildArrayLiteralExpr` 已有的正确写法：先显式 `(const wchar_t*)`，再 `yc_wcsdup_text` 堆拷贝（直接存 YC_TEXT 临时量的内部指针会在整表达式结束时悬垂）。
  **②bin 元素类别**：字节集（`YC_BIN`=`std::vector<unsigned char>`，值类型、装不进 long long）与文本型同策——元素存堆上 YC_BIN 的指针位模式，读回 `(*(YC_BIN*)(intptr_t)yc_ary_at(a,i))`。接入点 14 处：`ARRAY_ELEM_*`/`arrayElemKindOf`/下标读/`buildArrayLiteralExpr`/`mapYcmdArrayElemValueParam`/通用型编组/`fmtOne`/下标赋值/`YCMD_ARRAY_RETURN_TYPES`/prelude（`yc_bin_dup`/`yc_ary_lit_bin`/`yc_ary_to_text_bin`）/三处元素类型门禁（抽成 `isSupportedArrayElemType`）。注意别和 `ARRAY_ELEM_INTEGER_TYPES` 里的**「字节型」**（0-255 的数）混了。
  **③【硬伤发现】字节集的通用 ABI 是 0 字节不安全的**：krnln 的「字节集」跨 ABI 一律 `const char*`——`yc_bin_to_cstr` 用 `out.c_str()`（长度丢失）、`yc_cstr_to_bin`/`krnln_BinLen` 全按 `strlen` 算。**而 `分割字节集` 省略分隔符时默认分隔符正是字节 0**，用那套 ABI 根本表达不了。故按符号特办：新增 `binptr` 参数类别（`YCMD_ARRAY_PARAM_KINDS['krnln_SplitBin'] = ['binptr','binptr','int']`），字节集经 `const void*` 传 `YC_BIN*`（复用数组那套既有的跨 TU 指针契约），配 `yc_bin_tmp` 轮转槽解决临时量取址；省略 → `nullptr`，impl 据此取默认字节 0。
  **④能测到哪、测不到哪（诚实边界）**：`分割字节集` 的**显式分隔符**路径已真跑验证；**「默认按字节 0 分」实现了但喂不进料**——`到字节集`/`取空白字节集`/`取重复字节集` 等**全部字节集命令都走那条 `const char*` ABI**，造不出含 0x00 的字节集；且 `YC_BIN` 连 `operator+` 都没有（`到字节集(…) ＋ 到字节集(…)` 编不过），字节集连接这个基本操作也是缺的。要真正解锁须把**整个字节集 ABI 改成带长度传递**（约 20 条字节集命令全要动），是独立的一大件。
  **验证**：`arrayReturnRuntime.spec.ts` 加真编译真跑用例——字节集数组 `加入成员`+下标读（`2|字节集:1{65}/字节集:2{66,67}`）、`分割字节集` 显式分隔（`3|字节集:1{65}/字节集:1{67}`）、文本数组 `加入成员`+下标赋值（`2|甲/丙`，此前编不过）；`arrayReturnCommands.spec.ts` 钉住 `extern "C" void* krnln_SplitBin(const void*, const void*, int);` 与「不支持的元素类型仍友好报错」。全量 217 绿、lint 0 错。
  **遗留**：字节集 ABI 的 0 字节安全化（约 20 条命令）+ `YC_BIN operator+`。（已在下条落地，实为 34 条。）

- [修复] **【字节集 ABI v2】字节集从「NUL 结尾的 const char*」改为按长度传递——34 条命令，0 字节不再截断**（承接上条遗留）。
  **①病灶**：krnln 的「字节集」跨 ABI 一律 `const char*`——`yc_bin_to_cstr` 返回 `out.c_str()`（长度丢失）、`yc_cstr_to_bin` 与 `krnln_BinLen` 全按 `strlen` 算。可字节集本就是**任意二进制**，含 `0x00` 即整条截断。**这不是边角问题，是一整片命令在真实数据上直接错**：`取空白字节集(5)` 长度得 0；`文本到UTF16(“A”)` 的 4 字节（`41 00 00 00`，UTF-16 里 ASCII 的高字节全是 0）只剩 1；`写到文件`/`读入文件` 处理不了任何真二进制文件。
  **②新 ABI**（复用数组那套既有的跨 TU vector 指针契约，不引入新假设）：入参 `const void*` = `const YC_BIN*`（生成侧 `(const void*)&yc_bin_tmp(…)`，省略 → `nullptr`）；返回 `void*` = 堆上 `new YC_BIN`（生成侧 `yc_bin_take` 接管：移走内容后 `delete`，同 `yc_ary_take`）。删掉 `yc_bin_to_cstr`/`yc_cstr_to_bin`（此前只有那两个映射点在用）。**范围只在 Windows**——已核实 `linux.cpp`/`macos.mm` 里字节集命令是 **0 条**。
  **③`YC_BIN operator+`**：`字节集 ＋ 字节集` 是易语言的基本操作，但 `YC_BIN` 是 `std::vector` 别名、没有 `operator+` → `到字节集(…) ＋ 到字节集(…)` 此前**直接编译失败**（invalid operands to binary expression）。补在 typedef 旁。
  **④「参考」形态的字节集实参**：`置字节集内整数`〈无返回值〉要**就地改写**入参，但通用编组交的是 `yc_bin_tmp` 轮转槽里的临时副本 → 写进去就丢，**这条命令此前是彻底的空操作**。新增 `binref` 形参类别（`YCMD_ARRAY_PARAM_KINDS['krnln_SetIntInsideBin']`）绑到用户变量本身，靠 `yc_bin_ref(YC_BIN&)` 借 C++ 重载解析当类型闸（实参不是 YC_BIN 左值就编译失败，而不是 `(void*)&` 出个错类型指针后在运行时踩内存）。另：旧实现 `memcpy(binData + offset, …)` **没有任何边界检查**，offset 靠近末尾即缓冲区溢出——v2 补上了。
  **⑤「省略 ≠ 零值」机制推广**：上一轮为 `分割文本` 的分隔符加的 `YCMD_NULL_WHEN_OMITTED_PARAMS` 推广成 `YCMD_OMITTED_PARAM_EXPRS`（符号→形参序号→省略时发的 C 表达式）。新用户：`取统一文本` 的「转换到宽文本」「添加结束零字符」——帮助明说**省略时默认均为真**，而通用映射把省略一律译成 0（假），两个开关此前完全不认（且实现只收 1 个参数、清单有 3 个，声明与实现错位）。
  **⑥顺带按帮助修正**：`文本到UTF8` 没加帮助明说的「结束零字符」；`取字节集数据` 的类型常量**整个错位**（帮助 1=#字节型 2=#短整数型 3=#整数型 4=#长整数型 5=#小数型 6=#双精度小数型 7=#逻辑型 8=#日期时间型 9=#子程序指针型 10=#文本型；旧实现按 1=int/2=float/3=double/其余=首字节，每个类型都取错），并给它加 `YCMD_NATIVE_RETURN_TYPE_OVERRIDES`= 长整数型 对齐 impl 的 `long long`（清单标〈通用型〉会掉默认 int）。
  **⑦8 条字节集返回的占位桩必须同步收敛成 `void*`/`nullptr`**（`取字节集注册项`/`快照`/`取数据`/`取字节集`/`画板.取图片`/`接收`/`保存数据源`/`保存数据源单元`）——否则 `yc_bin_take` 会把 `keepUtf8(...)` 的缓冲区（或被当指针的 `long long`）当 `YC_BIN*` 解引用并 `delete`，**直接崩**。与 `取所有发音` 那次同一个雷。
  **验证**：新增 `binAbiRuntime.spec.ts`（**真编译 exe、真跑、读 stdout**），9 项断言**全是旧实现必错、新实现才对**的：`取空白字节集(5)`→5（旧 0）、含 0 字节连接→5（旧编不过）、`文本到UTF16(“A”)`→4（旧 1）、UTF16 往返 `甲乙/6`、文件写 5 字节(含 3 个 0)读回→5（旧 1）、**`分割字节集` 省略分隔符按字节 0 分→2 段**（上一轮实现了却造不出 0 字节数据、测不了）、`置字节集内整数` 读回 305419896（旧恒 0）、含 0 字节数据上 `取字节集右边`/`寻找字节集` 正确。**签名审计：字节集族 34 条不一致 0（全对齐）**；四轮碰过的 14 个符号全对齐。全量 218 绿、lint 0 错。
  **遗留**：`krnln_set`/`krnln_store`/`krnln_ZeroAry` 等另一族也是 `const char*` vs `void*` 的同类病（属数组/指针族，独立处理）。（已在下条落地——但结论是「不该对齐 ABI，该做成内建」。）

- [修复] **补全的多音字匹配：只认常用音 → 认全部读音**（用户反馈：`#换行符` 只能 `#hxf` 搜到，敲正确读法 `#hhf` 反而搜不到）。
  **①病因**：`pinyin.ts` 的 `getCharPinyin` 逐字只取 `pinyin(ch, …)[0]`（**常用音**）。「行」的常用音是 xíng，于是 `换行符` 的首字母恒为 `hxf`——可这个词里「行」**实际读 háng**，用户敲的正确读法反而落空。补全匹配要的是**宽容**：任一读音命中即算命中，两种读法都该搜得到。
  **②改动**：`getCharPinyin(ch): string` → `getCharPinyins(ch): string[]`（`multiple: true`，去重保序、首项仍是常用音）；5 个匹配点全部改为多读音：`matchFrom` / `matchFromCoverage` / `pinyinTokenMatchEnds` / `canPinyinAnchorAtStart`，以及 `matchScore` 的首字母检查。首字母分支按 `getCharInitials(ch)`（全读音首字母去重）判一次即可，不必逐读音重复递归。
  **③顺带优化**：`getInitials`+`initialsCache`（把整名首字母拼成串再 `===`/`startsWith`）→ `initialsMatch(input, name)` 逐字 1:1 比对——多音字天然支持，且**不匹配立即短路**，比原来每次按键给每个候选构整串更快，缓存可以直接去掉。
  **④排查「还有没有别的多音字问题」**：`pinyin-pro` 有词典、能给出**词内上下文读音**，拿它与「逐字取常用音」对比即可自动找出旧代码算错的名字（`scratchpad/scan-polyphone.mjs`）。扫描 925 个中文名（命令/常量/控件成员），**18 个受影响**：**行**(xíng/háng) 10 个（#换行符、插入文本行、插入行、读入一行、加入行、取行高、写文本行、置行高、是否允许多行、允许多行表头）、**重**(zhòng/chóng) 6 个（#重试钮、#重试取消钮、#放弃重试忽略钮、取重复文本、取重复字节集、重新查询）、**奇**(qí/jī) 1 个（#仅打印奇数页）、**调**(tiáo/diào) 1 个（DLL命令调用转向）。消费者只有 `editorCompletionDisplayUtils.ts` 一处，改这一个文件即全覆盖；生成 krnln 拼音表的脚本本就用 `multiple: true`，不受影响。
  **⑤性能**（补全每次按键要对全部候选打分）：实测 513 个候选全打一遍分 **0.37ms/按键**，无退化。
  **⑥【坑】`pinyin.ts` 是 CRLF、且中文正则以 `\uXXXX` 转义形式存盘**——Edit 工具按 `\n` 匹配会一直失败，须用脚本按原样改。
  **验证**：新增 `pinyinPolyphone.spec.ts`——18 个受影响名字**两种读法都要命中且同分**（不该因选了哪个读音而排序不同）；不误伤（`xyz`→0 分、`xxx` 不匹配 `换行符`）；中文精确/前缀仍优先于拼音。全量 229 绿、lint 0 错。

- [修复] **堵掉「对象.xxx」当自由命令调用的坏路（24 条）+ 分诊两次自我推翻的更正**。
  **①真问题**：ycmd 里有 **24 条 displayName 以「对象.」开头**的命令（`对象.移动`/`对象.获取焦点`/`对象.发送信息`…）。帮助里的「对象．」是**占位符**（=任意对象），不是能写出来的命令名——但 displayName 字面如此，于是它们进了命令表、补全补得出来、**写出来还能编过**；而 impl 收的是「前导对象句柄 + 清单那几个参数」，清单不含句柄 → 生成的调用**少一个参数**，运行时按错位的栈/寄存器取值。实测确认：`对象.移动 (1,2,3,4)` **编译通过**（`按钮1.移动(…)` 则是友好报错「暂不支持在代码中调用」——那条路不可达、安全）。
  **修法**：`assertNotBareObjectMemberCommand` 在两处派发点（Expr/Call）拦下，并**指路正确写法**「控件名.移动(…)」。只堵「当自由命令调」这一条路——提示面板走 `findControlMethodEntry`（剥「对象.」前缀匹配 `编辑框1.加入文本`），不受影响。
  **②【更正上条】分诊数字被我自己推翻了两次**：上条说「115 条里 60 条有真实现」——**错**。分诊启发式（按函数体长度<90 且以 `return` 开头判桩）漏掉了一整类：**「假状态桩」**——形如 `krnln_SetName(...) { runtimeDbState().fieldName = "field-updated"; }`，操作的是编造的 `runtimeDbState()`/`runtimeEditorState()`/`runtimePrintState()`/`runtimeNetState()` 单例，看着有函数体、其实全是假的。按「有没有碰那套假单例」重新分：**纯占位桩 29 条 / 假状态桩 44 条 / 真实现且错位 42 条**。而这 42 条里 **18 条正是本条堵掉的 `对象.xxx`**（现已不可达）。
  **③【更正上条】「D 组（通用型按引用）有现成解法」也是错的**——那组根本不是同一种病，摊开看是三类：
  - **ycmd/帮助/impl 三方各说各话**：`取文本`（ycmd「取文本(成员索引)」/ 帮助「对象．取文本」有**数据源与变体型两块** / impl 是窗口文本）、`启用`（帮助真名 **`对象．允许`**，选择列表框）、`是否启用`（真名 **`对象．是否被允许`**）、`取高度`/`取宽度`（帮助是画板/打印机的「通用型 数据」，impl 却收 hwnd）。要修得先定夺该认哪一条，属元数据考据。
  - **「通用型 = 窗口/组件」需要「组件→句柄」编组**：`是否已创建`/`取组件名称`（ycmd 与帮助一致，只是通用编组给的是文本形态）。
  - **真·按引用但缺的是实现，不是 ABI**：`读入数据` 的 impl 只 `fgetc` **一个字节**，而其对偶 `写出数据` 写的是**文本形态的 strlen 字节**——**两者根本不是对偶**，写进去再读出来必错；要修须先定「类型化二进制格式」。`输入框` 的 impl 是**假的且会破坏内存**：弹的是 `MessageBoxA(MB_OKCANCEL)`（根本没有输入框），OK 后 `*(const char**)outVar = keepUtf8(初始文本)`——把 `char*` 写进 YC_TEXT 变量。要修须自建输入对话框（Win32 无现成 InputBox）。
  **结论**：这「115 条不一致」里绝大多数是**桩**——真正的工作是**把命令实现出来**，不是对齐签名。签名审计从此只作为「改 ABI 后的回归对表」用，不当 KPI 追。
  **验证**：新增 `objectMemberGuard.spec.ts`——语句形态/表达式形态都要拦且指路；**不误伤**普通命令（`取文本中间`）与 `控件名.方法名`（仍是「暂不支持在代码中调用」，真缺口是这些控件方法没接进 window-units）。全量 223 绿、lint 0 错。

- [修复] **【纠正上条的分层取舍】变量操作族的实现搬回支持库：转译期只交「变量地址 + 类型标签」**（用户复盘时点出：「你这意思好像是把核心支持库的 .cpp 实现改到转译文件来了」——正是如此，且这是我上条没摆出来问就自作主张的取舍）。
  **①先澄清范围**（前四轮**没有**搬实现）：数组返回/拼音表/字节集 ABI v2 的实现**一直在 `lib/krnln/impl/`**，改的只是**编组方式**；转译文件里多出来的 `yc_ary_take`/`yc_bin_take`/`yc_bin_tmp` 是**编组胶水**，与原来就在那儿的 `yc_bin_to_cstr`/`yc_utf8_to_wide` 同层。**只有上条的变量操作 5 条真的把实现搬进了转译器**。
  **②为什么内建是错的**：它把「支持库负责实现」这条分层破了——别的库无法复用、krnln 里那 5 个实现成了死代码、以后 krnln 想自己演进也接不上。**而且它并非唯一解**：类型转译期本就知道，交给支持库即可。
  **③新设计（实现回 krnln）**：native 签名改为 `krnln_set(void* target, const void* value, int dataType)` / `krnln_store(const void* value, void* target, int dataType)` / `krnln_XchgVar(void* a, void* b, int dataType)` / `krnln_ForceXchgVar(同)` / `krnln_ZeroAry(void* arrayVar)`。生成侧发 `{ decltype(目标) __yc_v = (值); krnln_set((void*)&目标, (const void*)&__yc_v, yc_vt_of(目标)); }`——值参用 `decltype(目标)` 落临时量、类型转换交给 C++ 完成后再取址。
  **④标签怎么来**：新增 `yc_vt_of()` 的一组重载（int/long long/short/unsigned char/float/double/`const YC_TEXT&`/`const YC_BIN&`/`const std::vector<long long>&`）→ **标签由 C++ 重载解析得出**，转译期不必再查一次变量类型表，且不支持的类型**直接编译失败**（诚实），不会静默按错类型写内存。标签独立编号 `YC_VT_*`、**不复用 `YC_SDT_*`**——那套是易语言的数据类型 ID（`YC_MDATA_INF` 在用），字节集/数组的真实 ID 我们没有确证，不瞎认领。
  **⑤【关键】krnln 不必认识 `YC_TEXT`**：`YC_TEXT` 是 `struct { std::wstring s; …只有成员函数 }`——单数据成员、无虚函数、无基类 → **标准布局**，对象地址即首成员地址，krnln 按 `std::wstring*` 处理即可。与 `YcBin`(=`std::vector<unsigned char>`) 是同一类布局假设，**不新增耦合、不用同步第三份结构定义**（`main.cpp` 已有一份 YC_TEXT 拷贝，再加一份就是三处要同步）。
  **⑥【不能按字节交换】**：`强制交换变量` 帮助说它「不检查类型、仅要求尺寸一致、文本/字节集只交换指针值」。但 `std::wstring`/`std::vector` 的短串优化(SSO)里存在**指向对象自身缓冲的指针**（libstdc++ 的 `_M_p` 指向 `_M_local_buf`），整体 memcpy 交换后两个变量的指针都指进对方、双双坏掉。而我们的 `std::swap` 本就是 O(1) 指针交换——原命令那点性能优势在这里不存在，放开类型检查只剩风险。故两者等价，类型一致性由转译期 `static_assert(std::is_same<…>)` 挡在编译期（比原语义严，已文档化）。
  **⑦声明**：这 5 条的「地址+标签」签名通用映射译不出 → 在 prelude 手写 `extern` 块，并让 `generateYcmdNativeDeclarations` 跳过它们（否则会按清单的「通用型」再发一份 `const char*` 的错声明）。签名审计：声明 546→551、**一致 419→424、不一致仍 115**——5 条全部纳入统计且全对齐。
  **验证**：`varOpsIntrinsics.spec.ts` 改名 `varOpsByRef.spec.ts`；运行时断言不变（赋值→`7/甲`、连续赋值→`5/5/5`、交换变量→`2/1`、强制交换变量(文本)→`乙/甲`、数组清零→`2|0/0`），**新增「架构钉」codegen 断言**：必须发 `krnln_set((void*)&a, (const void*)&__yc_v, yc_vt_of(a))` / `krnln_store` / `krnln_XchgVar` / `krnln_ZeroAry`，**不许内联成 `=`/`std::swap`/`std::fill`**——防止以后又被「顺手简化」回内建。全量 220 绿、lint 0 错。

- [修复] **「变量操作」族改为转译期内建 + 审计分诊（危险≠值得修）**（承接上条遗留）。
  **①先分诊，别照着审计数字瞎修**：签名审计 115 条不一致里，**31 条 D2 良性**（声明 void、实现有返回值 → 值被忽略）、**24 条虽危险但 impl 是桩**（对齐了签名命令照样不能用，真正的修法是让它诚实报错）、**60 条才是「有真实现且错位」= 用户真会踩的坑**。修 stub 的签名是给审计做面子，不产生用户价值——这条判断决定了本轮只动最后一类。
  **②【定性推翻上条】`krnln_set`/`store`/`XchgVar`/`ForceXchgVar`/`ZeroAry` 不该对齐 ABI**：帮助里这族的参数写作「**通用型变量/变量数组**」（vs 值参的「通用型数组/非数组」）——是**按引用**语义。而 `void*` 不带类型信息，通用型赋值/交换根本表达不了；现 impl 是 `*(uintptr_t*)目标 = (uintptr_t)值指针`（把指针值当数据写），且通用编组交给它的还是**值的文本形态**、连地址都不是——两头都错，对齐 ABI 也救不活。**转译期本来就知道每个变量的类型** → 做成内建 `YCMD_INTRINSIC_CALLS`（在 `generateYcmdNativeCommandCall` 开头一处拦下），直接发 `a = b;` / `std::swap(a,b);` / `std::fill(a.begin(),a.end(),0);`，完全绕开 krnln。顺带 `generateYcmdNativeDeclarations` 对内建**不发声明**（发了也没人调，白留一份与 impl 错位的声明）→ 审计 120→115。注：`COMMAND_EXPR_GENERATORS`/`COMMAND_CODE_GENERATORS` 这套机制早就在、但**两个表都是空的**，且 `isYcmdNativeCommand` 分支抢在它们之前 return——对 ycmd 命令从未生效过。
  **③又一条张冠李戴（且是元数据审计抓不到的那种）**：`赋值`(krnln.set) 的 ycmd 是 `〈逻辑型〉(通用型 欲写入数据, 整数型 成员索引)`，帮助是 `〈无返回值〉(通用型变量/变量数组 被赋值的变量或变量数组, 通用型数组/非数组 用作赋于的值或资源)`。审计报 0 是因为「赋值」在帮助里有多个同名块（26 条「需靠归属消歧」之一），落不进「同名块不符」的判定。`连续赋值` 的尾参也补了 `repeatable`（帮助调用格式带「，... 」）。
  **④顺带按帮助对齐 4 条**：`取统一文本长度` 清单 2 参、impl 只收 1 参且恒按宽文本算（「转换到宽文本」开关完全不认，与 `取统一文本` 同病，一并进 `YCMD_OMITTED_PARAM_EXPRS` 默认真）；`字符` 形参 `int`→`unsigned char`（帮助为字节型）；`到短整数` 返回 `int`→`short`；`指针到文本` 形参 `uintptr_t`→`long long`（帮助为长整数型）。
  **⑤审计脚本自身的坑**（记两条，重建时会再踩）：① 解析形参**必须先剥形参名再规范化**——`norm` 里的 `\s*\*\s*`→`*` 会把 `const void* p` 粘成 `const void*p`，名字就剥不掉（曾误报 235 条）；② impl 形如 `f(a,b,...)` 的**尾部 `...` 只是宽容用、不是真形参**，按形参比会把 `krnln_InStr(…4 个具名, ...)` 误报成「5 个形参」（`寻找文本`/`倒找文本`/`文本替换`/`取代码` 的清单其实与帮助完全一致，全是假阳性）。
  **【已被下条修正】**②里「做成转译期内建」是错的取舍——那等于把支持库的实现搬进转译器、破坏分层。下条改为「实现留在 krnln，转译期只交『地址+类型标签』」。
  **验证**：新增 `varOpsIntrinsics.spec.ts`（真编译 exe、真跑）：`赋值`→`7/甲`、`连续赋值(5,a,b,c)`→`5/5/5`、`交换变量`→`2/1`、`强制交换变量`(文本)→`乙/甲`、`数组清零`→`2|0/0`（成员数不变）；负例 `交换变量(1,2)`→「需要变量作为参数」、`数组清零(文本数组)`→「只支持数值数组」（帮助明说只收数值数组，文本数组会把指针位模式冲成 0）。**注意负例的校验必须用标识符正则**——只判「没有空格括号」的话字面量 `1`/`2` 会被当变量名放行，最后炸在 C++ 的 `no matching function for call to 'swap'`。全量 220 绿、lint 0 错；元数据审计仍 0；签名审计 124→115（有真实现且错位 69→60）。

- [修复] **展开参数行按回车丢焦点 + 可重复参数回车规则改为「恒新增」**（用户反馈「回车没新建而是光标下移，再回车就丢光标」→ 复审后明确要求「光标要落到下一行去；可重复的不管空不空、回车就继续加」）。**定性**：①「没新建」非 bug——`加入文本(a,)` 源码尾逗号本就造出第 2 个空实参、展开即 2 行，故第 1 行回车走的是「有下一行→`advanceParamEdit` 前进」；②**真 bug 是焦点**：最后一行回车落到 `commit()` 分支 → 参数输入框随即卸载而无人接管焦点 → 掉到 `<body>`，键盘导航断。harness 对照实测**这是通病**：普通命令 `信息框` 最后一行回车同样丢焦点，非可重复参数特有。**新规则（按用户要求重定）**：**可重复参数行 → 回车恒 `appendRepeatParamRow`（去掉「值非空才追加」守卫），空值也继续加，退出用 Esc/点别处**；不可重复 → 不是最后行则前进，是最后行则走新增的 `finishParamEditToNextLine()`：写回本槽（照 `advanceParamEdit`：仅值真改动才写）后 `beginCodeLineEdit(lineIndex+1)`+`focusCodeInputAt(0)` 把**光标落到下一代码行**继续编辑（已是末行才退回 `focusWrapper()`）。**不改 `commit()` 自身**——blur 提交时不该抢焦点。验证（harness 实测）：可重复 回车 2→3→4→5 行、焦点恒在 `eyc-param-val-input`；`信息框` 最后一行回车 → 焦点落到 li+1 行的 `eyc-inline-input` 且内容为下一行源码。空实参堆积无害：codegen 的 `callEach`/`buildYcmdRepeatTailCalls` 均跳过空值。

- [修复] **元数据张冠李戴续修 3 条 + 撞名实为 52 组（大小写不敏感）+ `分割文本` 定性为「未实现」**。①续修（均按帮助对齐；这 3 条是**对象成员命令**，派发走 window-units 绑定、ycmd 只驱动提示，改元数据零 ABI 风险）：`画板.取图片`（ycmd 无参 → 帮助 `〈字节集〉(［输出宽度］,［输出高度］)`）、`选择列表框.选中项目`（ycmd `〈整数型〉(欲选择的项目文本)` → 帮助 `〈逻辑型〉(项目索引,［选择状态］)`）、`打印机.置自定义纸张类型`（ycmd `〈文本型数组〉(纸张定义)` → 帮助 `〈逻辑型〉(名称,宽,高,［上/下/左/右边距］)`——**顺带消掉一条假的数组返回**，签名审计不一致 121→120）。**元数据审计：5 条 → 1 条**。②**撞名机制更正**：英文名撞名精确匹配 50 组，**大小写不敏感 52 组**——`分割文本` 正是漏网的那种（帮助 798 行 `英文名称：split` = 分割文本/文本操作；7984 行 `英文名称：Split` = `对象．分解`/数据源），生成器大小写不敏感匹配后取错块。③**`分割文本` 不修（定性：不是元数据问题）**：impl 是**桩** `extern "C" const char* krnln_split(...) { return keepUtf8("[]"); }`（永远返回空数组），且 **`mapTypeToCType` 根本不认「文本型数组」→ 数组返回一律掉默认 `int`**；ycmd 里 6 条返回数组的命令（分割字节集/取所有发音/取配置节名/多文件对话框/取选中项/置自定义纸张类型）**恰好全在签名审计 D1 类**，坏法相同。**光改元数据反而更糟**：现在标 `无返回值` 时垃圾值被忽略，改成 `文本型数组` 后垃圾值会被当数组用。要修须先设计「数组返回 ABI」（一次可修 7 条）+ 补真实现，属功能级工作。

- [修复] **【元数据张冠李戴】`打开文件`(krnln.open) 挂着「外部数据库.打开(ODBC)」的说明与参数 + 实现的打开/共享方式全错**（用户反馈「打开文件这个命令不对」并贴出帮助原文）。**根因**：`krnln.library.json` 写明"命令说明与分类由帮助说明文件对齐生成"，而生成器**按英文名匹配**——帮助里 `英文名称：open` 有 **3 处**（`〈整数型〉打开文件(…)-文件读写`、`〈逻辑型〉对象．打开()-通用对话框`、`〈逻辑型〉对象．打开(［ODBC数据源连接文本］…)-外部数据库`），撞名后取了最后一个 → ycmd 的 打开文件 变成 `逻辑型 + [ODBC数据源连接文本,是否只读,不显示ODBC连接对话框] + 控制台操作`。**中文名 `打开文件` 本是唯一的，按中文名匹配就不会错**。**系统性普查**（脚本：帮助按 `调用格式：` 切块 → 与 ycmd 按**中文名**比对返回值/参数名/参数型）：**513 条 ycmd 中 5 条元数据与同名帮助块不符**——`打开文件`、`分割文本`(ycmd 竟是 `无返回值+[行号,列号]`、帮助为 `文本型数组+3参`)、`取图片`(画板)、`选中项目`(选择列表框)、`置自定义纸张类型`(打印机)；**帮助里英文名撞名共 50 组**（即生成器的雷区）。本次先修 `打开文件`（用户撞到的、且是常用普通命令），另 4 条待修（后 3 条是成员命令、只影响提示）。**顺带发现 impl 的方式映射也全错**并一并修：`#写出`→`wb`(帮助要求不存在则失败，wb 却会新建)、`#重写`→`ab`(帮助要求清空，ab 是追加)、`#改写(5)/#改读(6)` 掉进 `default:"rb"` 退化成只读、省略打开方式(0) 也变只读(帮助默认 `#读写`)、而 `#读写` 反倒会 `w+b` 创建(帮助要求不存在则失败)。改为按常量语义映射（1#读入 rb / 2#写出 r+b / 3#读写含省略 r+b / 4#重写 wb / 5#改写·6#改读 r+b 打不开再回退 w+b 创建）；**「共享方式」此前被完全忽略**（形参写成 `int /*shareMode*/`），改用 Windows `_fsopen`+`_SH_DENY*` 落实（已实测 zig/mingw 可编译，需 `#include <share.h>`）。验证 `openFileCommand.spec.ts`（元数据断言不含 ODBC、常量编号 1..6/1..4、真 zig 编译且声明 `int krnln_open(const char*,int,int)` 与 impl 一致）；两文件 SHA 同步（已用脚本核对 7 个核心库 SHA 全部一致）。

- [修复] **焦点停在某参数行时该行无法 Ctrl/Shift 多选**（用户反馈：回车新增出来的空参数行想选中删掉却选不了，必须先把焦点挪开才选得中）。根因就一行：参数行渲染处 `onClick={isEditingParam ? undefined : onParamRowClick}` —— **正在编辑的行把整个 onClick 置成了 undefined**，Ctrl/Shift 点击压根进不了 `onParamRowClick` 里那段齐全的多选逻辑（该逻辑本就会自己 `setEditCell(null)` 退编辑）。修复：改为 `if (isEditingParam && !(ctrl||meta||shift)) return; onParamRowClick(e)` —— **正在编辑的行只接管 Ctrl/Shift 多选**，普通点击仍留给输入框落光标（不重启编辑）。顺带把 Ctrl/Shift 分支的 `setEditCell(null)` 后补 `focusWrapper()`（与既有拖选路径一致；退编辑后输入框卸载、不接管焦点会掉 `<body>`）。验证（harness 实测）：焦点停在末尾空参数行 → Ctrl+点该行 → 选中=1/编辑中=0/焦点=wrapper → Delete → `加入文本(a,)` → `加入文本(a)` 实参被删。
  - **补修：普通左键拖选同样受阻**（用户复测：「Shift/Ctrl+左键都可以了，但普通按住左键拖过去还是不行」）。根因同源、在 mousedown：`if (isEditingParam || e.button !== 0) return` —— 正在编辑的行直接 return，**连拖选锚点都不记**。但这里不能像 Ctrl/Shift 那样直接抢：普通左键按在输入框里本该落光标/框选文字。**复用既有 `pendingInputDragRef` 的套路**（"mousedown 起于输入框内 → 等鼠标拖出输入框边界再转成拖选"）：新增 `pendingParamDragRef`，编辑态 mousedown 只记待定（不 preventDefault，输入框照常落光标），全局 mousemove 里一旦鼠标**拖出 `paramInputRef` 边界**即转成 `paramDragRef` 走原拖选逻辑；mouseup 清待定（没拖出去就松手＝普通点击/框选文字，待定作废）。**非编辑态路径字节级不变**。验证：焦点在第1行 → 普通左键拖到第3行 → 选中=3/编辑中=0/焦点=wrapper → Delete 删掉三个实参；对照非编辑态拖选 1→2 仍选中=2（无回归）。

### v0.0.5-beta.8（2026-07-15）

- [新增] **「尾参可重复」体系落地：编辑框.加入文本 + 写出族 8 条**（用户反馈「在展开的欲加入文本参数行上回车应重复添加该参数行」，并要求扫描帮助文件普查同类命令）。**普查**：脚本扫 `系统核心支持库帮助说明文件.txt` 579 个命令块，两标志（调用格式里的 `，...` 与「命令参数表中最后一个参数可以被重复添加」句）**交叉校验 0 分歧** → **51 条尾参可重复**（26 普通命令 + 25 对象成员）。**根因**：`repeatable` 管线（编译器参数个数校验 / `resolveParamAt` 取尾参类型 / UI 展开行回车追加）全通，但**只有 spec 库填了该标记**（调试输出/验证/取文本…），**krnln 库 513 条一个都没填** —— 系统性缺口；且 `krnln_AddElement` 的多值 codegen 特例因此成了**不可达死代码**。**实现**：①控件方法侧 `controlMethodBindings` 新增 **`callEach`** 字段（逐实参各展开一次模板、占位 `{arg}`、逗号表达式串成单表达式、**跳过空实参**——展开行回车会在源码留尾部空实参如 `加入文本(“甲”,乙,)`），`编辑框.加入文本` 改用之，CONTROL_TYPE_METHODS 尾参标 `repeatable`；②普通命令侧把原 `krnln_AddElement` 单命令 hack **泛化**成 `YCMD_REPEAT_TAIL_SYMBOLS` + `buildYcmdRepeatTailCalls()`（前 k-1 实参复用、末参逐值各发一次；语句路发 `{ (void)f(a); (void)f(b); }`，表达式路用逗号表达式取最后一次的值），ycmd 给 **写出文本/写文本行/插入文本/插入文本行/写出字节集/插入字节集/标准输出/输出调试文本/写出数据** 标 `repeatable`。**只收「ycmd 元数据与原生签名一致且语义确为逐值重复执行」的命令**——写到文件（重复=覆盖?语义存疑）、多项选择（语义是「选一个」，且 impl 已是变参）故意不收。**血泪坑**：`加入文本` 最初做成变参 helper `append_v(h,a,b,NULL)`，报 `cannot pass object of non-trivial type 'YC_TEXT' through variadic function` —— 文本型实参是 C++ 对象，定长形参能隐式转 `const wchar_t*` 而**过 C variadic 不能**，已回退成逐值调用。**验证**：`repeatTailCommands.spec.ts`（多值各发一次 / 单值仍一次 / **未标的 写到文件 给多参仍报「参数数量不匹配」**防误放开）、`controlMemberDispatch.spec.ts` 补多值+尾部空实参用例；harness 实测展开行回车逐个追加。

- [修复] **【重大】日期时间型 ABI 错位：一行修根、连带 21 条命令**（签名审计 C 类）。根因不是 14 个 bug 而是**一个遗漏**：`mapTypeToCType` 的类型映射表**根本没收 `日期时间型`** → 掉进默认分支 `|| 'int'`。而易语言日期时间型 = OLE 自动化日期（**double**），krnln impl 全按 double 收发。于是编译器按 ycmd 生成的声明是 `int krnln_year(int)`、实现却是 `int krnln_year(double)`——**C 链接不跨 TU 校验签名，故一直能编能链，只在调用时静默出错**（传参把 double 当 int、取返回值读错寄存器，`取现行时间` 必拿垃圾）。补一行 `'日期时间型': 'double'` → **签名一致 397→418、不一致 142→121，C 类整类清零**，连带修好 D1 里所有返回日期时间型的命令。**该表也决定变量声明**：日期时间型局部变量此前被声明成 `int`（连日期都装不下），现为 `double`；全套单测零回归。验证 `dateTimeTypeAbi.spec.ts`。

- [修复] **原生签名对齐（签名审计 A 类 8 条）**：`相加`、`等于/不等于/小于/大于/小于或等于/大于或等于` 的 ycmd 标「通用型」→ 通用映射把实参转成**文本指针**，而 impl 收 `double`/`long long`（`int krnln_add(const char*,const char*)` vs `long long krnln_add(long long,long long)`）→ **把指针当数值算**。新增 `YCMD_NATIVE_PARAM_TYPE_OVERRIDES` / `YCMD_NATIVE_RETURN_TYPE_OVERRIDES`（与既有 `YCMD_ARRAY_PARAM_KINDS` 同思路：按符号显式给形参/返回类型）对齐。**`写出数据` 是 ycmd 元数据本身错**：帮助与 impl 都是 `〈逻辑型〉(整数型 文件号, 通用型 数据…)`，ycmd 却写成 `〈无返回值〉(通用型 数据)` 且描述串到了别的命令上 → 直接改 ycmd 补回文件号/改返回值/标尾参可重复。**限制**：这只对齐 ABI，易语言「通用型」的文本形态（`相加("a","b")` 文本连接、`等于("a","a")` 文本比较）仍不支持，要支持须把 impl 改成 `const char*` 泛型实现。验证 `nativeSignatureAlign.spec.ts`（断言生成声明与 windows.cpp 逐字一致 + 相加实参不再经 `yc_value_to_text` 变指针）。

- [审计] **ycmd 声明 vs 原生实现 全量签名审计**（工具与明细留存 `.tmp-harness/sig-audit.txt`、`sig-audit.spec.ts.bak`，放回 `tests/` 可复跑）。方法：`generateYcmdNativeDeclarations()` 会把**全部** ycmd-native 的 `extern "C"` 声明写进**每个**生成的 cpp，故编任一最小项目即得编译器侧「真声明」，再正则抓 `lib/{krnln,spec,webview2}/impl/windows.cpp` 的定义，按 ABI 等价类（i32/i64/f64/ptr/void）比对。**结果：551 声明 / 552 实现 → 起初一致仅 389、不一致 150**；本版修掉 29 条（A 类 8 + 日期族 21）→ **一致 418 / 不一致 121**。余 121 中 **32 条 D2 良性**（声明 void 而实现有返回值，只是忽略返回值）+ **18 条 B 类不可达**（`对象.销毁/获取焦点/移动/发送信息…` 等对象成员命令，impl 收隐式 `void*` 对象首参而 ycmd 未声明；今天走 window-units 绑定→未绑定即友好报错，故调不到，要修须先做「对象成员命令原生 ABI」）→ **真正待修 ~71 条**，多为 COM/数据库/打印机族等未接入命令。**安全性洞察：参数个数不符的方向决定危害——impl 比声明「多」收才危险（读没设过的寄存器→野指针），impl「少」收是良性的（x64 下多余实参在寄存器里被忽略）**。**经验**：两个根因型缺陷同一气味——元数据表里少一行然后**静默降级到默认值**（`mapTypeToCType` 漏 `日期时间型`→默认 int；krnln ycmd 一个 `repeatable` 都没填）；`|| 'int'` 这类兜底把「没配置」和「配成 int」混为一谈，以后加类型/元数据表须警惕。已验证 **commandId→符号 撞车 0 组**。

### v0.0.5-beta.7（2026-07-14）

- [新增] **集成终端从"管道伪终端"升级为真伪终端（node-pty ConPTY + xterm.js）**（用户反馈"终端里跑 copilot 没反应""只有一种文本色"）。后端 `child_process.spawn` 管道换 `node-pty`（Windows 走 ConPTY，交互式程序 isatty 为真、能正常渲染），前端 `<pre>`+自写 ANSI 解析换 `@xterm/xterm`+addon-fit/web-links（真 PTY 的光标/重绘控制序列 `<pre>` 表达不了）。node-pty 1.1.0 自带 N-API 预编译（免 electron-rebuild、ABI 稳定）；OutputPanel 独占 xterm 直接订阅 `terminal:output`（App 层不再累积输出、避免高频重渲染卡顿），主进程输出块加单调 seq、xterm 回放快照后按 seq 精确丢弃"快照 vs 实时订阅"重叠块（既不丢也不重）；新增 IPC `terminal:input`(原始按键透传)/`terminal:resize`(FitAddon→pty.resize)，`terminal:interrupt` 改写 `\x03`（中断前台程序不杀 shell），注入 xterm 的换行用 `\r\n`。Ctrl+C 有选区=复制/无选区=中断、Ctrl+V=粘贴，右键复制/粘贴/全选/清屏。打包：node-pty 进 electron-builder `files`+`asarUnpack`（排 .pdb/darwin prebuilds），`externalizeDepsPlugin` 已外部化成运行时 require。删旧 `ansi.ts`+`ansi.spec.ts`（xterm 自带解析）。**验证**：全套单测绿；Playwright 启真 exe 实测 copilot 交互 TUI（Session/Issues 标签+`❯`+框线）完整渲染、echo/ver 执行、中文正确、右键菜单到位（截图）。

- [新增] **编辑框「加入文本」方法可在代码中调用**（用户反馈 `编辑框1.加入文本(a)` 报"暂不支持在代码中调用"）。编辑框唯一方法、此前是已知缺口。windows.cpp 加 `krnln_ctrl_append_text`（krnln_edit_target 定位 EDIT/组合框子Edit→EM_SETSEL 到末尾→EM_REPLACESEL 追加留撤销）；window-units.json **controlMethodBindings**（方法绑定在这个数组、不是 controlMemberBindings）加 `{unit:编辑框, member:加入文本, call:krnln_ctrl_append_text({h},{0})}`；compiler.ts 转译前导补 `extern "C"` 声明否则用户 cpp undeclared。TRANSPILE_CACHE_VERSION 25→26、windows.cpp+window-units.json 双 SHA 同步。**验证**：controlMemberDispatch.spec.ts 正例（加入文本 zig 编译+生成 helper）、负例改 `标签.加入文本`（标签无此绑定→友好报错）。

- [修复] **新建空窗口项目（无控件/无命令）直接运行链接崩溃**（用户反馈"新建项目什么都不做运行报错"，链接期 `未定义符号 krnln_ctrl_get_text/free_text/get_tag/get_date/get_seltext/krnln_ll_get_text`）。pre-existing bug（beta.2-4 声明式重构埋下）：窗口程序 main.cpp 无条件发窗口运行时封装（`yc_ctrl_get_text→krnln_ctrl_*`、`yc_ll_get_text→krnln_ll_*`），但 `collectUsedLibraryFileNames` 只按源码命令+窗口控件类型收集"使用的库"→空项目 used 为空→krnln 不进 libsToLink→windows.cpp 不编不链→那些封装引用的 krnln_* 全 undefined（日志"实际使用 0 个"）。含控件/命令的项目一直没暴露（used 自然带 krnln）；beta.7 缓存版本 bump 刷掉产物缓存才让空项目走真编译暴露。修复：`collectUsedLibraryFileNames` 末尾 `if (project.outputType==='WindowsApp') used.add('krnln')`（窗口程序无条件强制链接核心运行时）。**验证**：controlMemberDispatch.spec.ts 空窗口用例（controls:[]、空事件）复现同款 6 个 undefined 符号→修后链接过。

- [修复] **图片框「显示方式=缩放图片」运行后图片框被图片原始尺寸撑大**（用户反馈"设计器预览正常，运行后图片框不正常变大"）。图片框=STATIC + `SS_BITMAP`，SS_BITMAP 默认把控件自动缩放到位图原始尺寸（撑大来源），且"缩放"从未实现（buildStdPicBoxCodegen 只发 SS_BITMAP）。修复（compiler.ts）：①有图片时加 `SS_REALSIZECONTROL`（禁控件随位图放大、恒守设计尺寸，对所有显示方式生效）；②显示方式=1(缩放)时 STM_SETIMAGE 前用 GDI+ `DrawImage` 把位图拉伸到 `GetClientRect(hCtrl)` 客户区大小的新 `Gdiplus::Bitmap`（SS_ 样式本身无法拉伸位图，只能预缩放），居左上/居中走原始位图。窗口 codegen 不进转译缓存、改完即生效。**验证**：picBoxScale.spec.ts（缩放断言 SS_REALSIZECONTROL+_scaledPic+DrawImage，居左上有 REALSIZECONTROL 无 _scaledPic，均 zig 编译过）；真 exe 跑起来 PrintWindow 截图确认两个图片框守 200x92、320x200 大图缩满。

- [修复] **点击控件方法（如 `编辑框1.加入文本`）提示面板显示"未在已加载的支持库中找到此命令"+空参数**（用户反馈）。控件方法有三套真源各自为政：①编译派发(window-units.json controlMethodBindings)②编辑器补全(CONTROL_TYPE_METHODS)③点击提示(App.handleCommandClick)。补全侧早有 加入文本 带 `欲加入文本` 参数，但提示面板只查库命令目录(getAllCommands)→控件方法查不到→落 NOT_FOUND_DESC(空参数)。修复：`editorCoreUtils` 加 `resolveControlMethodDetail(methodName)`（跨 CONTROL_TYPE_METHODS 按方法名查、构造 CommandDetail），App.handleCommandClick 未命中库命令的兜底分支先查它、命中就显示带参数详情。**验证**：controlMethodHint.spec.ts（加入文本→带欲加入文本参数、未知方法/空名→null）。

- [修复] **控件方法行（`编辑框1.加入文本(a)`）左边不出 +/- 参数展开按钮、参数展不开**（用户反馈"点击到这一行上左边的展开收缩按钮没有显示出来"）。与上条同一家族的**第四处漏点**：表格编辑器判定「这行有命令且有参数→画 +/- 展开按钮 + 渲染参数行」的三条路——`getCodeRenderMeta`(算 lineCmd→hasExpandableDetail→画按钮)、`findCmdWithParams`、`findCommandCallWithParams`——都只认 库命令(allCommandsRef) / DLL(dllCompletionItemsRef) / 项目类公开方法(findProjectClassMethodByName)，控件方法一律查不到 → lineCmd=null → hasExpandableDetail=false → 按钮不画、参数行（`lineCmd && !assignDetail && isExpanded` 才渲染）也出不来。修复：editorCoreUtils 抽出共享的 `findControlMethodEntry`（剥对象前缀 `编辑框1.加入文本`→`加入文本`、跨 CONTROL_TYPE_METHODS 按方法名查），新增 `findControlMethodCompletion` 返回 CompletionItem 形态，上述三处各补一路 `|| findControlMethodCompletion(...)`；`resolveControlMethodDetail`（提示面板）改为复用同一 finder。**验证**：controlMethodHint.spec.ts 加补全项用例（params 非空=画按钮前提、ownerTypeName=编辑框、带对象前缀也认）；harness 实测点 `编辑框1.加入文本 (a)` 行左侧出「+」、点开渲染出 `※欲加入文本：a` 参数行（截图 expand-append-2.png）。

**遗留待办**：控件方法真源现已散在四处（编译派发 window-units.json、补全 CONTROL_TYPE_METHODS、提示面板、参数展开），后两处已统一到 CONTROL_TYPE_METHODS，但与编译侧仍是两套手工同步——应改协议单一真源同时驱动，避免每加一个方法要同步多处。

- [优化] **新建控件默认给白色背景**（用户反馈"新建标签属性显示默认但色块是白色，运行后跟窗口色变，应默认白色"）。背景颜色默认哨兵从 -1（融入窗口）改 16777215（白）：compiler.ts 3 处 `readIntProp(extraProps['背景颜色'], …)` 默认值、window-units.json 4 处 defaultValue、VisualDesigner 3 处。透明另有"透明"属性负责。**验证**：defaultBgColor.spec.ts + controlMemberDispatch.spec.ts「未设背景默认白也进运行时颜色表」用例。

### v0.0.5-beta.6（2026-07-14）

- [新增] **颜色字面量 `#RRGGBB` / `#红色` / `rgb(r,g,b)` / `rgba(r,g,b,a)`（通用整数表达式，哪都能用）+ 代码里给控件「文本颜色」赋值**（用户反馈「代码里给文本颜色赋值，还想支持 rgb/rgba/十六进制」）。四种写法统一折成 COLORREF 整数：`#hex`（web #RRGGBB 自动 R/B 交换成 COLORREF、支持 3/6/8 位、8 位丢 alpha）在常量剥离前预处理；`#红色` 9 名色；`rgb()`/`rgba()`（丢 alpha）拦到 `RGB` 宏、参数可用变量。运行时给标签/编辑框/选择框/单选框/列表框/组合框/分组框（共用 WM_CTLCOLOR 覆盖表+重绘）+ 月历（类名派发 MCM_SETCOLOR）加 `文本颜色` set 绑定（生成 `yc_ctrl_set_text_color` helper + HWND→COLORREF 覆盖表，WM_CTLCOLOR 注入且返回真实刷子避开黑底陷阱）；画板本就支持；超级链接框存值但 SysLink 链接文字色暂改不了（已文档化）。编辑器给 `#hex`/`#名色`/`rgb()` 加行内色块（Monaco DocumentColorProvider 原生取色器 + 表格编辑器色块），字符串内 `#` 不误当颜色、`rgb(` 不误报未知命令。工程：TRANSPILE_CACHE_VERSION 24→25、window-units.json 哈希同步；新增共享模块 `src/shared/colorNames.ts` 单一真源（fontSpec 改 re-export）。**验证**：`tests/unit/textColor.spec.ts` 真 zig 编译（`#ff0000`与`#红色`同折 `(COLORREF)(255LL)`、RGB 宏、main.cpp 覆盖表/月历 MCM/WM_CTLCOLOR 注入，负例无该属性控件仍友好报错）；表格色块 harness 探针+截图；188 单测/32 契约/双工程 typecheck/生产构建全绿。

- [新增] **韩文/日文等做标识符（变量名/子程序名）** + [优化] **未定义标识符给友好中文报错**（用户反馈 `a ＝ 중국어의` 报难懂的 C++ `undeclared identifier`）。①标识符字符类从「中文+英文」**追加** CJK扩展A(3400-4dbf)+谚文韩(ac00-d7a3)+假名日(3040-30ff) 三段范围（58 处正则/137 处/13 文件，脚本扫；**故意不用 `\p{L}`/`u` 标志**以保持既有正则语义与 Monaco Monarch 兼容；唯一保持中文-only 的是 `pinyin.ts` 的「是不是汉字」判定）——声明（本就分隔符切分）/引用/着色/诊断/补全/改名全认。②转译器兜底出口（`return translated` 前，gate 在有变量解析器时）对「排除已知变量/数组/命令/子程序/常量/控件/真假空后仍不认识的裸标识符」抛 `未定义的变量或标识符“X”（若这是一段文本，请用引号…）`（由主循环 try/catch 补文件:行号前缀），替代难懂 C++ 报错；编辑器诊断放宽后也红标未定义标识符。**验证**：`tests/unit/unicodeIdentifiers.spec.ts`（负例 중국어의→友好报错含 `_启动窗口.eyc:行号`、无 undeclared identifier；正例 声明韩文 `변수` 赋值/加法/传数值属性 zig 编过、변수 进 C++）；全量 188 单测+32 契约无回归、生产构建通过；harness `?src=` 截图确认韩文变量进变量面板+变量色、未定义韩文红波浪线。

- [新增] **展开参数行「回车前进到下一参数行」**（用户反馈「在展开的参数行上连续回车不行」）。此前展开参数行按回车走 commit（写值后退出编辑、无焦点），连按第二下没反应。改为：不是最后一行→提交本行并把编辑焦点前进到下一参数行（可一路回车逐个填参数）；最后一行→提交收尾（可重复尾参且值非空则追加下一个值行，保留原逻辑）。`advanceParamEdit` 仅当值真改动才写回，穿行不重格式化/挪动未触碰的实参。顺带堵住 `replaceCallArg`「空值写不存在的尾参」凭空补 `,)` 的老坑。**验证**：组件级实测（.tmp-harness Playwright）四参数命令连续回车逐个前进、未改动整行不变、边改边回车只改动过的参数变、空尾参填值正确追加、全空命令穿行不凭空补参；replaceCallArg 空尾参守卫入单测。

- [优化] **编辑行背景色与选中行背景色区分开 + 参数行拖动跨行多选**（用户反馈「编辑行跟选中行同色看不出选中」「点击/拖动不能跨行多选参数行」）。①配色：编辑态与选中态原都用同一选中蓝（散在块行/命令行本体/表格行 6 处规则），新增中性「当前行」变量 `--eyc-current-line-bg`（文字色 9% 淡叠加），编辑态全改成它、选中态保持蓝——编辑=中性、选中=蓝一眼分清；「正在编辑的参数行」加独立高亮（中性+左竖条）不再整块发蓝。②参数行拖选：普通左键 mousedown 记锚点+阻断冒泡到行拖选+提交活跃编辑，复用全局 mousemove/mouseup（过阈值→elementFromPoint 找同命令行参数行→按 argIdx 区间选中+退编辑+focusWrapper），真拖过吞掉随后 click；Ctrl/Shift 点选老路径保留、普通单击进编辑不变。**验证**：harness 读 computed 背景色（编辑=中性、选中=蓝）+ 拖 提示信息→父窗口 四行全选/Delete 删/单击仍进编辑，均截图确认。

### v0.0.5-beta.5（2026-07-12）

- [修复] **命令悬停提示弹窗点击别处后不消失、偶尔弹到 IDE 左上角(0,0)**（用户反馈）。①**卡住不消失**：弹窗此前只在命令 span 的 `onMouseLeave` 与滚动时关闭；点击命令触发乐观编辑会把 span 换成输入框、`mouseLeave` 不触发，弹窗遂卡住。补一个「弹窗显示期间」的全局 `mousedown/keydown/wheel`(capture) 监听，任意点击/按键/滚轮即关闭（弹窗本就 `pointer-events:none`，点击穿透到下方元素同时关掉提示）。②**弹到左上角**：悬停后延时 420ms 才 `getBoundingClientRect()` 定位，到点时 span 若已被重渲染/卸载（乐观编辑换输入框、滚动虚拟化）则 rect 全为 0 → 弹到 (0,0)。加 `target.isConnected` 与零尺寸守卫，脱离布局则不弹。**验证**：组件级实测（.tmp-harness Playwright）——正常悬停弹窗定位正确(93,166)非左上角、点击别处/按键/悬停未满即点击(触发 span 卸载) 三种情形弹窗均正确消失且绝不出现在 (0,0)；对照 stash 前后正常悬停定位一致（守卫不误伤正常悬停）。185 单测全绿、tsc/eslint 0 错。

- [新增] **展开参数行支持多选删除（Ctrl/Shift 点选 → Delete/Backspace/空格 删除选中实参）**（用户反馈；配合上一条修复清理旧 bug 堆出的重复参数）。展开参数面板此前每行只能点进编辑、无从批量清理。本次给参数行加多选：**Ctrl/Cmd+点**切换单行选中、**Shift+点**从锚点连选一段（普通点仍进编辑并清选区），有参数行选区且焦点不在输入框时 **Delete/Backspace/空格**删除选中实参（`removeCallArgs` 按下标剔除、其余按原分隔符重连、删空留 `()`，一步 pushUndo 可撤销），**Esc** 清空选区。参数行多选与整行多选互斥（选整行即清参数选区）；修饰键点击 `stopPropagation` 阻断冒泡到整行 mousedown，不误触发行选择。新增纯函数 `removeCallArgs`（editorTextUtils）。**选中可辨性（用户反馈「整块都像被选中、参数行底色跟编辑器底色不一样不好判断」）**：①选中参数行改用**左侧 3px 强调条 + 26% 强调底**（`--border-focus`），在错误粉底/行选蓝底/普通底上都清晰可辨，区别于整行选择的浅色底；②**行选中不再把选择底色透进展开参数区**（`.eyc-line-selected > .eyc-param-expand` 恢复编辑器底色），杜绝「命令行+全部参数行像被一起选中」；③点任意参数行即清整行选区。**验证**：`removeCallArgs` 7 例（删中间/首尾/删光留空括号/全角分隔/嵌套逗号不误伤/越界忽略 + 清理重复参数回归）入单测（editorTextUtils 47 例、185 单测全绿、tsc/eslint 0 错）；**组件级实测**（.tmp-harness Playwright）：Ctrl 点选 3 行删→`(1,变量,变量,bian,bi)`、Shift 连选 2..7 删→`(1,变量)`、全选删→`调试输出 ()`；选中样式探针——选中行 `box-shadow=inset 3px #007fd4`、行选中时参数区 `bg=rgb(30,30,30)`（编辑器底色非蓝），截图确认 3 行里只高亮所点的 2 行。

- [修复] **展开参数行输入多值时命令行括号内堆出大量重复参数（可重复参数如「调试输出」）+ 按易语言接线「回车/逗号新增当前层级参数行」**（用户反馈）。根因：展开参数面板按「参数定义」逐行渲染（`lineCmd.params.map`），可重复参数（调试输出的「欲输出值」）只出一行；用户往这一行里打逗号分隔多值，`liveUpdate` 每次把含逗号的整串经 `replaceCallArg` 写回单槽，重解析后尾部旧实参残留、每敲一键堆一组——行内爆出 `调试输出(1,变量,bian1,bian,,变量,"3",…)` 式重复。修法（逐值一行，从根上杜绝多值挤单槽）：①**渲染**：末尾「可重复」参数改按实际实参逐个成行（`argIdx` 绑定具体实参，至少一行空槽）；非重复参数仍每定义一行。②**新增参数行**：可重复参数行上回车/顶层逗号（值非空）→ `appendRepeatParamRow`：配平格式化写回本槽 + `insertCallArgAfter` 在其后插空槽 + 焦点移入新行（压制过渡期旧输入框卸载触发的 blur 提交，防聚焦落空）；其余情形顶层逗号一律吞掉（单槽不容纳多值）。嵌套调用内的逗号（深度>0，`isArgParensBalanced` 判否）与字符串内逗号照常放行。新增纯函数 `insertCallArgAfter`（editorTextUtils）。**验证**：`insertCallArgAfter` 6 例 + 逐值追加无重复回归（`调试输出()`→逐值→`调试输出(1,变量,bian1,)`）入单测（editorTextUtils 40 例全绿、178 单测全绿、tsc/eslint 0 错）；**组件级实测**（.tmp-harness Playwright 挂 EycTableEditor + 调试输出 可重复参数）：逐值+回车 `()`→`(变量)`→回车`(变量,)`(2 行)→`(变量,值2,)`(3 行)→Esc`(变量,值2,尾值)`；**旧 bug 触发方式**在单行连打「a,b,c」现得干净 `调试输出 (a,b,c)`(3 行) 无任何重复。

- [新增] **标签「渐变背景」属性落地（设计器预览 + 编译运行时，此前是声明占位——面板能选方式/配 3 色但两侧都不显示）**（用户反馈「渐变背景没作用」）。模型：`渐变背景方式`（0无/1从上到下/2从左到右/3左上→右下/4右上→左下/5从下到上/6从右到左/7右下→左上/8左下→右上）+ `渐变背景颜色1/2/3` 三色（COLORREF，0 基默认黑），**未设底图时生效**（易语言「未设定底图时的渐变背景」语义，底图优先）。落地：①设计器预览——标签无底图且方式≠0 时 `backgroundImage: linear-gradient(方向, 色1, 色2, 色3)`（colorFromNumber 与运行时同为 COLORREF 低字节=R，所见即所得）。②运行时——复用标签底图子类机制：`g_ycLblBgs` 表 + `YcLblBgProc` 从「仅底图」扩为「底图或渐变」都挂子类（表项加 gradMode/g1/g2/g3；无底图项 data/size=NULL,0），WM_ERASEBKGND 有底图画底图、否则按方式用 GDI+ `LinearGradientBrush`+`SetInterpolationColors`（3 色 0/0.5/1，8 方向对应两端点）填充客户区；渐变标签同底图标签一样强制 `transparent=1`（文字透明叠加，否则 STATIC 底色矩形在渐变上打洞）。工程：与「图片缩放」同批共用已 bump 的 `TRANSPILE_CACHE_VERSION`（未改 window-units.json，无需更哈希）。**验证**：真实 zig 编译「纯渐变标签（红→黄→蓝/左上到右下）+ 底图叠渐变标签（验底图优先）+ 普通标签」——编译过、生成 `LinearGradientBrush lgb`/`SetInterpolationColors`/`e->gradMode != 0` 分支，表项无底图记 `NULL, 0`、有底图记 `g_ctrlImg_*`，恰 2 处挂子类（普通标签不挂）、两背景标签色表 transparent=1；tsc 双工程 0 错、既有 9 例编译回归全绿。

- [新增] **标签/画板「底图方式」增加「图片缩放」模式（拉伸铺满控件客户区）**（用户反馈）。此前标签/画板底图方式仅 3 项（0居左上/1平铺/2居中），窗体底图方式已有缩放（其索引另有一套 0平铺/1居左上/2居中/3居右下/4缩放，照易语言各控件自有约定不强并）。本次给标签、画板底图方式 pickOptions 各追加「图片缩放」为 index 3，三处同步落地：①设计器预览——标签走 `backgroundSize: 100% 100%`、画板新增 `--vd-preview-bgsize` CSS 变量（.vd-preview-drawpanel 补 background-size）；②运行时 codegen——标签 `YcLblBgProc` 补 `e->mode==3 → DrawImage(0,0,cw,ch)`、画板 `yc_dp_fill_back` 补 `st.bgMode==3 → DrawImage(0,0,st.cw,st.ch)`（均拉伸到客户区，与既有 mode 1/2 分支同构）。工程：`TRANSPILE_CACHE_VERSION` 23→24（生成物模板变动）、window-units.json 哈希同步。**验证**：新增标签+画板双控件（内嵌 1x1 PNG 底图、底图方式=3）真实 zig 编译过、生成 main.cpp 含 `e->mode == 3` 与 `st.bgMode==3` 缩放分支及 mode=3 表项；tsc 双工程 0 错、既有编译回归全绿。

- [优化] **属性面板：渐变边框/底图方式/渐变背景子属性缩进 + 字体色与独立文本颜色双向同步**（用户反馈）。①**子属性缩进**（照易语言 UW_HAS_INDENT）：控件面板给 底图方式、渐变边框宽度/颜色1-3、渐变背景方式/颜色1-3 加 prop-name-sub 缩进（原仅 停留顺序/调节器 有），并按父项禁用——底图方式(无底图禁)、渐变边框颜色(边框≠渐变镜框禁)、渐变背景颜色(渐变背景方式=通常禁)；配套给 EditableColorCell 补 disabled 支持（**对抗性审查抓到的 FAIL**：颜色单元格原不接受 disabled，6 个颜色子项的禁用会失效，同组内 宽度灰显、颜色仍可点的不一致）。②**字体↔文本颜色同步**：10 个同时具备「字体」与独立「文本颜色」的控件（标签/编辑框/选择框/单选框/列表框/组合框/分组框/画板/月历/超级链接框），二者代表同一文本色但运行时多数控件用独立「文本颜色」上色、字体对话框只显示 font.color——不同步则所见≠所得。在 updateFormProperty 单次 setTabs 内原子写两属性（一条撤销记录）：字体改→仅当 font.color 相对旧值真变了才镜像到文本颜色（改字族/字号不误重置；**清整个字体不动文本颜色**）；文本颜色改→写回已有字体的 color（**无字体不凭空建宋体9 避免锁死系统默认字体外观**）。审查确认 codegen 一致（多数控件读独立文本颜色、按钮走 font.color 不在同步集、画板 dpTextColor 读独立文本颜色均正确）。纯渲染层改动，重载即生效；171 单测全绿、tsc/eslint 0 错。

- [新增] **标签「底图」属性落地（设计器预览 + 编译运行时，此前是声明占位——面板能选但两侧都不显示）**（用户反馈）。设计器：标签预览 div 直接 backgroundImage（data URL），底图方式 0居左上/1平铺/2居中 映射 repeat/position。运行时：标签底图字节并入 controlImageBytes 内嵌（收集特判：画板/标签用「底图」，图片框/按钮用「图片」）→ `g_ycLblBgs` 表（懒解码 GDI+ Image）+ `YcLblBgProc` 子类：WM_ERASEBKGND 先填窗体背景刷（图透明区/未铺满透出窗体）再按底图方式画图返回 1；**文字叠加**经颜色表 transparent 路径（有底图的标签强制 transparent=1 → SetBkMode(TRANSPARENT)+NULL_BRUSH，否则 STATIC 文字自带底色矩形会在图上打洞）。**定论级验证**：红底窗+纯蓝 8x8 平铺底图标签（stdlib 手搓 PNG），窗口 DC GetPixel——标签区=RGB(30,60,220) 蓝 ✓ 窗体=红 ✓。**踩坑**：SetWindowSubclass 发射点初版按「第一个 ctrlId++」定位落进了时钟分支（时钟 `ctrlId++; continue` 在前）——生成物无子类挂载、像素探针红色（=只有透明路径生效）暴露；改锚定创建语句 `(HMENU)${ctrlId++}` 之后。GdiplusStartup 在 wWinMain 建窗前 ✓ 首帧懒解码即成功。

- [修复] **进度条在设计器里颜色不对（深色主题下槽显示成深灰近黑）**（用户反馈）。根因：进度条预览的槽/填充/边框用的是 **IDE 主题变量**（--bg-hover/--success/--button-secondary-border），深色主题下随主题变深——但设计器画布模拟的是编译后的 Win32 窗口，这类固定观感控件的预览必须与 IDE 主题无关。范围核查同类共 12 条 CSS 规则一并改为 Win32 固定配色：进度条（#e6e6e6 槽/#bcbcbc 边/#06b025 绿填充，块状/平滑两式）、滚动条（#f0f0f0 槽钮/#cdcdcd 滑块/#505050 箭头）、选择夹表头（#f0f0f0 页签+显式 #000 字色——原继承画布文字色，深色主题下浅色字浮在浅灰页签上不可读）、月历/列表框边框固定灰。滑块条经查已是固定色（controlColors 常量 #f2f2f2/#111827）无需改。纯 CSS 改动，重载即生效。

- [修复] **可视化设计器：窗口带菜单栏时纵向标尺的选中高亮/鼠标游标整体上移一条菜单栏**（用户反馈并自行定位触发条件=菜单栏）。根因：设计器菜单栏条（.vd-fmenu-bar，22px，border-box 含底边框）渲染在标题栏与客户区之间，把画布整体下顶 22px，但「客户区坐标→工作区/标尺坐标」的换算只加了 formTitlebarHeight。修法：新增 `FORM_MENUBAR_HEIGHT=22` + 派生 `formMenuBarHeight`（判定与 renderFormMenuBar 一致：menu 数组至少一个 visible !== false 顶级项）+ `formChromeHeight = 标题栏 + 菜单栏`，几何链 8 处（标尺选中高亮 y1/y2、标尺鼠标游标、scaledFormHeight、zoomTo 新旧高度、zoomToCentered、缩放适配比、--vd-shell-height）统一换用 formChromeHeight（含 5 处 hook 依赖数组同步）。顺带修正的隐性偏差：有菜单时窗体垂直居中、缩放适配、滚动范围此前都少算 22px。zoom 是对整个 form-wrapper 的 transform:scale，菜单高度随缩放，常量在 *zoom 括号内与标题栏同位。lint 警告数不增（5→4）、typecheck 过、170 单测全绿。**经验**：给设计器窗体加任何「铬条」（标题栏/菜单栏类，位于客户区上方的横条）都必须走 formChromeHeight，别单独加 formTitlebarHeight。
- [修复] **标签背景颜色设为白色在设计器预览显示成透明**（用户反馈；与 beta.1 图片框白底修复同类的「白色=透明」启发式残留）。范围核查发现同款启发式共 4 处预览分支（标签/选择框/单选框共享/分组框/外形框）+ 3 个 codegen 建造器（buildStdLabel/Checkable/GroupBoxCodegen 的 `backColor !== 0xffffff` 进表条件——显式白底在运行时也不进颜色表、显示成 BTNFACE 灰）。修法分两型：
  - **融入型控件（标签/选择框/单选框/分组框）**：「默认」用 **-1 哨兵**（首版沿用按钮.底色的「0=默认」惯例，用户当场发现 **#000000 纯黑被吞**——0 恰是纯黑的合法 COLORREF 不能当哨兵；-1 在 24 位色域外任何真实颜色都可表示，等价易语言 SDK 的 CLR_DEFAULT 思路）——json 背景颜色 defaultValue 16777215→-1；预览 <0→transparent、显式白/黑→真白/真黑；codegen 进表条件 `backColor >= 0`、负值回退 0xffffff（保留既有"只设文本色=白底"运行时行为不变）。面板 EditableColorCell 增哨兵支持：负值显示「默认」、清空或输入"默认"回车=复位到该属性声明的默认值（resetValue=prop.defaultValue，对按钮 0=默认 语义同样正确）。**未动过的控件外观不变**（默认仍融入窗口），只有显式设白才变白——设计器与运行时一致。标签透明仍由「效果=透明」独立驱动（json 描述本就写明"效果为透明时背景颜色不起作用"）。
  - **实底型（外形框）**：运行时自绘恒填 backColor（无透明态），预览改白=白与运行时对齐（同图片框先例）；json 默认白保留。
  - 验证：新增编译回归（白底标签/选择框进 g_ycEditColors 表 16777215、素标签不进表），171 单测全绿；lint 警告数不增。窗口 .cpp 不走转译缓存，无需 bump TRANSPILE_CACHE_VERSION；window-units.json 哈希已同步。
- [修复] **滑块条运行后有黑色背景**（用户反馈，并提议"给所有组件加背景色/透明属性"——经诊断是 bug 非缺属性，修复后滑块条自动融入窗体底色）。根因（静态分析生成物证据链）：应用清单启用 comctl32 v6 → 滑块条走主题绘制，其背景经 DrawThemeParentBackground **向父窗发 WM_PRINTCLIENT 索要**；生成的主窗 WndProc 没有 WM_PRINTCLIENT 处理且 DefWindowProc 不处理该消息 → 主题引擎的内存位图保持初始全黑 → 滑块条黑底（Win32 上"自定义父窗+主题 trackbar 黑背景"的知名坑）。修法：①窗体背景刷全局化 `g_hFormBgBrush`（自定义底色 CreateSolidBrush / 默认 GetSysColorBrush(COLOR_BTNFACE)，类注册与消息处理共用）；②主窗 WndProc 补 `case WM_PRINTCLIENT`：FillRect 背景刷 + 有底图连底图一起画（底图绘制块抽成 WM_PAINT/WM_PRINTCLIENT 共用）。红底/默认底两变体探针项目过 zig 编译、生成物含修复；本会话桌面无渲染能力（GetPixel=CLR_INVALID、PrintWindow 全白），运行时视觉效果待用户确认。**用户复测仍黑→二跳定位真根因（独立最小复现 repro.cpp + 消息日志 + 生成物对比）**：主题化 trackbar 的背景其实走 **WM_CTLCOLORSTATIC**（消息日志实证，全程无 WM_PRINTCLIENT——那是 TBS_TRANSPARENTBKGND 的路径，第一跳修复方向错了但保留无害）；生成 WndProc 的 WM_CTLCOLOR* 查表不中后 `break`，而 **switch 之后是 `return 0`（NULL 背景刷）**→ 主题双缓冲位图保持全黑。repro 里不中走 DefWindowProc 显示灰、用户项目有带色控件（case 存在）显示黑——两态差异即铁证。真修法：①颜色表+case **始终生成**（空表占位）；②查表不中绝不 break——STATIC 通道且运行期类名非 EDIT（滑块条/标签/选择框/单选框/分组框）→ 返回窗体背景刷 g_hFormBgBrush（真正融入任意窗体底色，易语言行为）；EDIT/LISTBOX 通道及只读编辑框（走 STATIC 通道但类名 EDIT）→ 显式 DefWindowProc（保持系统默认观感，防只读编辑框被染成窗体色的回归）。**定论级验证**：红底+滑块条+带色标签（复刻用户触发场景）编译后运行，跨进程窗口 DC GetPixel——滑块背景区三点 = RGB(200,30,30) = 窗体红 ✓ 标签白底=白 ✓。（环境经验：本会话屏幕 DC GetPixel/PrintWindow/CopyFromScreen 全不可用，但**窗口 DC 跨进程 GetPixel 可用**——运行时像素验证走 GetDC(hwnd) 而非 GetDC(NULL)。）**评估结论（回应"全组件加背景色属性"）**：不建议全量加——易语言正版滑块条/进度条本就无背景颜色属性（加了偏离兼容）；主题化原生控件大多不吃 WM_CTLCOLOR 背景、真自定义需逐控件 owner-draw（工程量大观感差）；此 bug 修复后控件背景自动跟随窗体底色即是正确行为，确有需求的控件后续按易语言权威清单逐个加。

### v0.0.5-beta.4（2026-07-12）

- [修复] **剩余「高频运行时」控件属性读写补齐 + 时钟周期事件接线 + 浏览框「地址」回归修复**（用户「两项一起做」续 beta.3 缺口清单；实现前 5 路并行勘探：时钟 codegen/选择夹 helper 现状/转译错误机制/画板状态可变性/帮助文件权威语义）。本批全部经声明式协议落地（krnln controlMemberBindings 37→60 条）：
  - **选择夹「现行子夹」**（0 基，帮助文件明示“索引值从 0 开始”）：勘探发现 `yc_tab_get_cur/set_cur` 已定义已 extern 但**从未接线**（死代码），本批只加绑定即通（set 自带 yc_tab_sync 子控件显隐同步）。
  - **时钟「时钟周期」+「周期事件」**：时钟无 HWND（SetTimer 定时器 id=控件 IDC 宏值），且**周期事件此前完全没接**（无 WM_TIMER 派发、json events 为空、时钟属半成品）。本批：生成 `g_ycTimers` 表（名→id→当前周期，空占位保链接）+ `yc_timer_get/set_period`（置 0=KillTimer 不产生事件，毫秒，帮助文件语义）+ WndProc `case WM_TIMER` 按定时器 id 派发 `_名_周期事件()`（WEAK stub 保链接）+ json 声明周期事件（编译器特判跳过通用 WM_COMMAND/NOTIFY 绑定循环，同画板「绘画」策略）。
  - **编辑框/组合框选区族**：起始选择位置/被选择字符数（0 基；置起始位置=光标定位收拢选区、-1=移文本尾；置字符数 -1=全选——承 VB6 语义，帮助文件印证）+ 被选择文本（读=选区拷贝、写=EM_REPLACESEL 替换选中）+ 最大允许长度/最大文本长度（0=不限制，EM_LIMITTEXT 天然吻合；读侧把 Win32 未限制巨值映射回 0）。组合框经 `krnln_edit_target`（FindWindowExW 子 Edit）路由，不可编辑下拉式无子 Edit 自然无效（同易语言）。库侧新增 `krnln_ctrl_get/set_seltext` + main.cpp `yc_ctrl_get_seltext`→YC_TEXT 封装（入 isTextExpression）。
  - **画板 GDI 状态属性 11 个**（画笔类型/粗细/画出方式/刷子类型/绘画单位/自动重画/画笔颜色/刷子颜色/画板背景色/文本颜色/文本背景颜色）：勘探确认 YcDrawPanelState 存**原始枚举值**、GDI 笔刷每次绘图现建现毁 → 直接改字段即对下次绘图生效；`yc_dp_get/set_prop(名, propId)` 复用 YC_DP_V/R 宏，自动重画/背景色置值附带 InvalidateRect。画图程序运行时改画笔是高频用法。
  - **月历「滚动月数/最多选择天数」**（MCM_SETMONTHDELTA/SETMAXSELCOUNT）。
  - **窗体名注册为「窗口」类型控件**：`yc_get_control_handle_by_name` 窗体名→g_hMainWnd，`窗口名.标题/宽度/可视/左边…` 经通用绑定直接作用于窗口本身（beta.2 声明式化前「窗口.标题」是静默空操作，现在真正生效；左边/顶边对顶层窗口即屏幕坐标，语义正确）。
  - **浏览框「地址」回归修复**：beta.2 重构删掉了旧写侧 isTextProp 里的「地址」特例但未补绑定，`浏览框1.地址 ＝ 网址`（WM_SETTEXT 即导航）编译报错。本批在 **webview2 自己的 window-units.json** 里声明 controlMemberBindings（get/set 走窗口文本）——恰为「第三方库自声明控件成员」协议的第一个真实第三方用例；顺带新增地址可读。
- [优化] **未绑定控件成员的转译期友好中文报错**（覆盖剩余 ~141 个设计期样式属性与无 helper 方法）：确属控件（且未被同名变量遮蔽）的 `控件.属性` 读写/`控件.方法()` 调用未命中任何绑定时，不再原样发裸 C++（难懂的 undeclared identifier），改报「类型“名”的属性/方法“X”暂不支持在代码中读取/赋值/调用」；转译主循环包 try/catch 统一为深层错误补「文件名:行号:」前缀（throwSourceError 先例格式，数组类深层裸错误一并受益）。**对抗性审查抓到并已修**：①方法未命中守卫须双重纯标识符校验——`控件.属性 ＋ 函数(x)` 会被 parseCommandCall 整段吞进 call.name，无校验会误报（已加回归测试行）；②`yc_ctrl_get_seltext` 漏入 isTextExpression（选区文本互比会退化成指针比较）。已知取舍：主循环 catch 会把编译器内部异常也标成 源文件:行号（丢原栈，调试性小损）；控件与窗体同名时句柄解析优先窗体（设计器应防同名，边缘）。
- 工程：`TRANSPILE_CACHE_VERSION` 22→23；CORE_LIBRARY_EXPECTED_SHA256 双更新；`controlMemberDispatch.spec.ts` 增 2 例（正向全属性含窗口标题/WM_TIMER 派发断言 + 负向双友好错误断言）至 5 例；170 单测全绿、tsc/eslint 0 错、windows.cpp 独立链接过。另：清理误入库的主题损坏备份 + `themes/*.corrupted.*` 入 gitignore（独立小提交）。

### v0.0.5-beta.3（2026-07-11）

- [修复] **一大批控件属性在代码里读写编译报错（undeclared identifier）——与进度条数值属性同类的系统性缺口**（用户：看下除了进度条别的有没有同样的问题 → 审计发现是一类，非孤例；实测 `按钮1.可视/宽度/标记`、`选择框1.选中`、`组合框1.现行选中项` 均报 undeclared identifier）。根因同进度条：属性在面板 / 易语言里存在、用户会在代码里读写，但无运行时翻译通道 → 转译器原样发成 C++ 标识符。经声明式协议（beta.2 机制）补齐运行时读写通道：
  - **公共属性（所有控件，`*` 通用绑定）**：可视 / 禁止（ShowWindow / EnableWindow）、左边 / 顶边 / 宽度 / 高度（GetWindowRect 相对父窗客户区坐标 / SetWindowPos）、标记（库侧 `unordered_map<HWND,wstring>` 应用级存储）。
  - **选中**（选择框 / 单选框，BM_GETCHECK / SETCHECK）、**现行选中项**（组合框 / 列表框 / 选择列表框，CB / LB_GETCURSEL / SETCURSEL）——扩 `krnln_ctrl_get / set_number` 的类名分派（新增 BUTTON / COMBOBOX / LISTBOX 分支 + 通用属性前置）。
  - **日期属性**（日期框 今天 / 最小日期 / 最大日期；月历 另含 首选择日 / 尾选择日）——新增 `krnln_ctrl_get / set_date`（文本「年 / 月 / 日 [时:分:秒]」↔ SYSTEMTIME，DTM_ / MCM_ 消息）+ 编译器侧 YC_TEXT 薄封装。
  - 标记 / 日期文本读取沿用一期 owned `wchar_t*` → YC_TEXT 封装（isTextExpression 认 `yc_ctrl_get_tag` / `yc_ctrl_get_date`）。共 20 条新绑定（controlMemberBindings 17→37）。`CORE_LIBRARY_EXPECTED_SHA256`（windows.cpp + window-units.json）、`TRANSPILE_CACHE_VERSION` 21→22 同步。**验证**：`controlMemberDispatch.spec.ts` 加一例（按钮 可视 / 宽度 / 左边 / 标记 + 选择框 选中 + 组合框 现行选中项 + 日期框 今天，读写两向过 zig 编译）；windows.cpp 独立链接 exit 0；168 单测全绿、tsc / eslint 0 错。**留后续（低频 / 纠缠）**：现行子夹（与 tab_sync 纠缠）、时钟周期（SetTimer 重设）、编辑框 / 组合框 选区属性（起始选择位置 / 被选择文本…）、~141 个设计期样式属性（边框 / 方向 / 对齐 / 显示方式，代码里几乎不访问）。

### v0.0.5-beta.2（2026-07-11）

- [修复] **进度条 / 滑块条 / 横向 / 纵向滚动条的「位置 / 最小位置 / 最大位置」等数值属性在代码里读写编译报错**（用户反馈：`进度条1.位置 ＝ 进度条1.位置 ＋ 1` 报 `use of undeclared identifier '进度条1'`）。根因：这些数值属性此前只有创建期初值 codegen、运行时读写无通道，转译器把 `控件.属性` 原样发成 C++ 左值 / 标识符。现经下条的声明式协议译成 `krnln_ctrl_get_number` / `krnln_ctrl_set_number` 运行时消息派发（进度条 PBM_ / 滑块条 TBM_ / 滚动条 SCROLLINFO，按运行期类名分派）。验证见下条集成测试（覆盖进度条位置读写循环）。
- [优化] **控件成员访问改为支持库声明式协议驱动（属性 + 方法），运行时下沉 krnln 库（三期重构）**（用户：控件成员硬编码在 compiler.ts，第三方支持库开发者无法扩展 → 决定整块改声明式 + 运行时搬库，参考易语言 fne SDK 形式 `D:\chungbin\ycide\易语言的功能库`）。此前 `控件.属性` 读写与 `控件.方法()` 调用**全部硬编码在 compiler.ts**（`replaceControlTextPropertyRefs` / `NUMERIC_CONTROL_PROPS` / `LISTLIKE_METHOD_NAMES` / `translateListLikeMethodCall`），协议层只驱动建窗类名 / 样式；第三方库无法为控件加属性 / 方法。本次分三期改造：
  - **一期·属性**：window-units.json 新增顶层 `controlMemberBindings`（`{unit, member, access:{get,set}}`，`unit` 可为 `*` 通用）+ 支持 `windowUnits[].properties[].access`；模板占位 `{h}`=控件句柄、`{v}`=原始值、`{vtext}`=文本化值。转译前从项目所有 .efw 灌 `currentProjectControls`（控件名→类型表）→ **按控件类型键控派发**（根治旧版「只按成员名匹配、与自定义数据类型的 `.位置` 等成员撞名」隐患）。文本属性（`krnln_ctrl_set_text`；getter 库返 owned `wchar_t*` + 编译器侧 `yc_ctrl_get_text` 薄封装成 `YC_TEXT`，因库是 `extern "C"` 纯 C ABI 不宜直出 C++ 类型）、数值属性（`krnln_ctrl_get/set_number` 运行期 `GetClassNameW` 分派 PBM_ / TBM_ / SCROLLINFO）helper 搬进 `lib/krnln/impl/windows.cpp`。17 条属性绑定（文本类 `*` 通用 标题 / 内容 / 文本；进度条 / 滑块条 / 横纵滚动条数值属性）。
  - **二期·方法派发**：新增顶层 `controlMethodBindings`（50 条：组合框 12 / 列表框 18 / 选择列表框 16 / 选择夹 3 / 超级链接框 1）+ 模板占位 `{n}`=控件名、`{N|默认}`=第 N 实参缺省取默认；`translateControlMethodCall` 在表达式与语句两处接入点均「声明式协议 ?? 旧路兜底」。`applyMemberTemplate` 改为实参由调用方预转译（方法参数须带上下文翻译）。
  - **三期·方法搬库**：组合框 / 列表框项目方法族 `yc_ll_*`(12) / `yc_lb_*`(6) → `krnln_ll_*` / `krnln_lb_*`（HWND 版，纯 Win32 消息，运行期按 COMBOBOX / LISTBOX 分派 CB_* / LB_*）搬进 windows.cpp；`取项目文本` 走库 owned `wchar_t*` + main.cpp YC_TEXT 薄封装、`取所有被选择项目`（返回 `std::vector`，C++ 型不跨库 ABI）留 main.cpp；删旧路 `translateListLikeMethodCall` 的 ll / lb 分支 + `LISTLIKE_METHOD_NAMES` 里 ll / lb 名（helper 已搬走）。
  - **边界（留后续）**：超链接 / 选择列表框勾选 / 选择夹 / 画板方法的**派发已声明式**，但运行时 helper 仍在生成的 main.cpp——它们与项目专属的 WndProc / owner-draw 自绘 / 自注册窗口类 / 绘画事件回调深度纠缠（状态表由 WndProc 填、`YcDrawPanelProc` 调项目绘画函数），搬库需逐控件「状态→库侧 HWND-map + 编译器发注册调用」手术，风险高、只为参考整洁，单独逐个焦点验证地做。补全侧 `CONTROL_TYPE_METHODS`（renderer editorCoreUtils.ts）与编译侧目前仍两套手工同步，改由协议单一真源亦留后续。
  - **工程**：改 `lib/krnln/` 下文件同步更新 `libraryManager.ts` 的 `CORE_LIBRARY_EXPECTED_SHA256`（windows.cpp + window-units.json）；`TRANSPILE_CACHE_VERSION` 18→21（转译输出三次变更）。**验证**：新增 `tests/unit/controlMemberDispatch.spec.ts`（真 `compileProject` 过 zig 编译含进度条 + 编辑框 + 组合框 + 列表框的窗口项目，断言生成物含 `krnln_ctrl_*` / `krnln_ll_*` / `yc_ctrl_get_text` 且无 undeclared identifier）；windows.cpp 独立链接 exit 0；167 单测全绿、tsc / eslint 0 错。**第三方扩展闭环**：第三方库往自己 window-units.json 写 `controlMemberBindings` / `controlMethodBindings`、在自己 impl 实现 `mylib_*(HWND,…)`，零改编译器即可为控件加属性 / 方法。

### v0.0.5-beta.1（2026-07-11）

- [新增] **画板（DrawPanel）完整落地：自注册窗口类 + 离屏 backbuffer + 15 属性 + 28 绘图方法 + 绘画事件（含绘图运行时）**（用户：易语言画板有这么多属性 → 选「完整落地含绘图运行时」）。画板是易语言最重的可视控件、本项目**首个需自注册窗口类**的控件。规划见 plans/logical-swimming-dragonfly.md（3 Explore 子代理勘探事件派发/窗口类创建循环/设计器方法派发 + 1 Plan 子代理校验架构给出 9 条修正）。**架构**：扁平模型不变（画板是 g_hMainWnd 子窗口、绝对坐标、GetDlgItem 解析名）；自注册 `YCDRAWPANEL` 类 + 专属 `YcDrawPanelProc`；每画板状态存 `std::map<HWND,YcDrawPanelState>`（backbuffer memDC/memBmp + GDI 画笔/刷子/文本状态 + 底图 Image* + 绘画事件函数指针）；**WM_PAINT 按 autoRedraw 分支**——真=直接 BitBlt backbuffer（持久、不产生绘画事件），假=先 `yc_dp_fill_back` 清成 背景色+底图 再产生「绘画」事件再 BitBlt（**F4 关键坑：不先清会累积成 autoRedraw=真 的效果**）；WM_ERASEBKGND→1 免闪、WM_SIZE 重建 backbuffer 保留旧内容、WM_DESTROY 释放（SelectObject 复原 oldBmp→DeleteObject→DeleteDC→delete bgImage→erase）。**绘画事件**（首个带参控件事件 `_画板名_绘画(int 左,int 上,int 右,int 下)`）：仿窗口级事件位置传参；每画板**函数指针**存 state（因 proc 被所有画板共享、只有指针能区分该调哪个符号，F1），原型在 CreateControls 前 emit（F2、plain 原型即可取址），通用事件循环特判跳过免「未解析事件绑定」警告（F3），weak stub 特殊 emit 带 4 int 参（F6）。**取设备句柄**成员调用 objName 已知→直接查 memDC（F5），x64 上 HDC 64 位截断为 int32 无损（Windows 保证 USER/GDI 句柄 32 位有效）。**P1**：window-units.json 画板 15 控件专属可编辑属性（枚举 pickOptions：画笔类型7/画出方式16/**刷子类型56**/边框6/绘画单位6/底图方式3）+颜色/图片/字体单元格+绘画事件（4 参），className YCDRAWPANEL，哈希 5b5f→fdf2；CONTROL_TYPE_METHODS 加画板 28 方法；VisualDesigner DEFAULT_SIZES[200,150]+renderControlPreview（带边框/背景色/底图平铺的画布 + 角标名）。只读属性 画板宽度/画板高度按面板 `.filter(!isReadOnly)` 约定过滤、且读取形态编译器未落地，v1 不入 catalog（列延后）。**P2**：buildStdDrawPanelCodegen（边框→style/exStyle）+ state/proc/prototypes（始终 emit，同超链接/选择夹策略保跨编译单元链接）+ isStdDrawPanel 创建分支（填 state：颜色/笔刷/rop/单位/autoRedraw + 底图 `controlImageBytes['底图']` GDI+ 解码 + 字体存 st.hFont + 绑 paintHandler + fill_back + Invalidate）+ RegisterClassExW(YCDRAWPANEL) 门控 hasDrawPanel。**P3 28 方法**：LISTLIKE_METHOD_NAMES + translateListLikeMethodCall 28 case→`yc_dp_*`（省略参数用 `(-2147483647-1)`=INT_MIN 哨兵、通用型文本参经 `yc_value_to_text`、点数组走既有 `vector<long long>` ABI）+ extern 两站点 + main.cpp helper（enum→GDI 表 PS_*/R2_*/HS_* + `yc_dp_u2px/px2u` 单位换算 + 画笔刷子应用 + 28 方法体，坐标全经换算、画进 memDC 后 InvalidateRect；取图片经 GDI+ 编码 PNG→YC_BIN）。**验证**：①面板 E2E（15 属性+56 项刷子下拉+只读过滤+预览背景色 rgb(192,220,192)）②补全 E2E（画板1. 列 28 方法）③编译 E2E（生成注册类/proc/backbuffer/绘画原型+stub+绑定/底图解码 + 28 方法全链路清缓存重编 **success 过 zig**）④**运行时像素探针**（跑 exe：红底画板在 创建完毕 画绿矩形，GetPixel 矩形内=绿 65280、矩形外=红 255，证 YCDRAWPANEL 创建+backbuffer+画矩形+背景色贴图全生效）；165 单测全绿、build+typecheck 通过。**v1 完整（26 方法）**：取设备句柄/清除/取点/画点/画直线/画椭圆/画弧线/画弦/画饼/画矩形/画渐变矩形/填充矩形/画圆角矩形/翻转矩形区/画多边形/置写出位置/写文本行/滚动写行/写出/定位写出/取宽度/取高度/画图片(字节集)/取图片宽度/取图片高度/取图片(PNG字节集)/单位转换。**best-effort/延后**：复制（跨画板需画板对象传参，v1 no-op）；画图片的**图片号**形态（需「载入图片」注册表，仅支持字节集）；绘画事件重画区参数为像素非绘画单位（一般不处理）；粗虚线退实线（GDI 限制）；刷子 56 类仅 6 原生 HS_* 其余近似实心。**至此 22 个可视控件全部属性/成员补全 + 运行时落地。** window-units.json/主进程改动需重启 dev。
- [优化] **主题管理器颜色选择器按住滑动卡顿**（用户续反馈：崩溃虽已修，但主题管理器里的取色器按住滑动还是很卡）。承接上一条颜色选择器节流修复——那条只覆盖属性面板 EditableColorCell 与字体对话框的取色 input，**主题管理器是另一套**（且开在独立 window.open 弹窗里）：内有三个 `input[type=color]`（流程线主色 / 渐变第二色 / token 主色），其中 token 主色 onChange 直接 `onTokenChange`→整套主题 CSS 变量全量重应用（applyThemeTokenValuesToRoot，牵动整个 IDE 重绘），系统取色器按住滑动时每帧连发 onChange→高频全量重应用→卡顿。修：抽出可复用组件 **ThrottledColorInput**（滑动本地 setDraft 即时预览[cheap]、向父级的提交节流到 ~90ms 一次、useEffect 尾随 flush + onBlur flush 保证末值必达），主题管理器三处取色 input 全部替换为它（沿用与 EditableColorCell 崩溃修复同构的节流范式）。验证：E2E 经 菜单 工具→主题管理器 打开独立弹窗（Playwright `app.waitForEvent('window')` 捕获该 window.open 窗口，在其 Page 上操作）——对第一个 token 取色 input 连发 60 次不同颜色（模拟滑动），**已提交仅 5 次（节流约 12×）**、本地 input draft 实时=末值（滑块视觉即时跟手）、尾随 flush 使最终色正确落到预览 chip（rgb(236,0,204)）、弹窗不崩仍存活；顺带实证 React onChange 能跨文档 portal（主窗 React 树 → 弹窗 document）响应合成 input 事件。改动纯渲染层（重载应用即生效）、未落盘任何主题文件（草稿改动随弹窗关闭丢弃）；165 单测全绿、build + typecheck 通过。**测试要点**：主题管理器不在主窗 DOM 而在 `window.open` 弹窗（App.tsx:2396 createPortal 到 popup.document#theme-manager-root），E2E 必须捕获第二个 window 而非在主窗找 `.theme-manager-*`。
- [修正] **颜色选择器按住滑动导致整个 IDE 崩溃**（用户反馈：标签文本颜色频繁切换/滑动取色区崩溃）。根因：原生 input[type=color] 在系统取色对话框里滑动时连发大量 onChange，每次都触发 updateFormProperty→setTabs 整窗体更新 + recordUndo 里对整个 DesignForm 做 2 次 JSON.stringify 比较（echo 检查 + before/after 去重，compiler 无关的 Editor.tsx:491/498）；叠加窗体里的底图/图片 base64（几十~几百 KB），每秒上百次全量序列化把渲染进程拖垮直至 Electron 崩溃。修：EditableColorCell 与字体对话框的文本颜色 input 都改为**节流提交**——滑块本地 setDraft 即时预览，但向父级（→撤销/整窗体更新）的提交节流到 ~90ms 一次，并用尾随 flush + onBlur flush 保证最后一个颜色一定被提交。验证：E2E 用原生 value setter 连发 80 个不同颜色（模拟滑动）——应用存活不崩、最终颜色(#ed0000)正确提交到控件；165 单测全绿、build 通过。（撤销 600ms 合并逻辑已限制撤销栈条数，此修专治高频提交风暴。）- [修正] **图片框背景色白色被误当透明（设计器透出窗口底色）**（用户反馈）。图片框预览与 codegen 沿用了标签的「白色=透明」启发式（标签在易语言默认透明透出父窗），但图片框是实底控件、无透明态，白色应显白。修：①VisualDesigner 图片框预览 `--vd-preview-bg` 改为恒 `colorFromNumber(bgNum)||'#ffffff'`（不再把 16777215 当 transparent）；②buildStdPicBoxCodegen colorEntry 改为恒 `{textColor:0,backColor}`（原 `backColor!==0xffffff?…:null` 会让白色图片框不进颜色表→STATIC 透出父窗口底色），使白色也经 WM_CTLCOLORSTATIC 上白。验证：设计器 E2E 白底图片框预览 `--vd-preview-bg=rgb(255,255,255)`；清缓存重编白底图片框生成颜色表项 `{IDC_白图2,0,16777215,…}`；165 单测全绿、build 通过。附：同批核查用户反馈的「窗口底图设计器不显示」——经 E2E（磁盘加载 + 面板 file input 实时设入两路）确认现版本设计器画布**能**实时显示窗口底图（vd-form-canvas-bgimage 类 + --vd-form-bg-image 变量均正确落，CSS 规则消费之），非当前代码缺陷（用户端应为旧渲染构建，重载/重启 dev 即生效）。
- [新增] **选择夹「子夹页」完整落地（WYSIWYG 容器 + 运行时切页 + 子夹管理弹窗）**（易语言控件体系最后一块，规划见 plans/logical-swimming-dragonfly.md）。选择夹此前只创建空标签条。**架构判断**（3 个 Explore 子代理勘探）：全 App 扁平控件模型（控件皆挂 g_hMainWnd，yc_get_control_handle_by_name 靠 GetDlgItem 解析），故子夹页用**扁平显隐**（子控件仍挂主窗、切页 ShowWindow），子控件保窗体绝对坐标、容器=「按当前页过滤/显隐」覆盖层，无需重定父/坐标换算。**数据模型走 properties**：选择夹 properties['子夹标题']（换行页名）+['现行子夹']；子控件 properties['所属选择夹']（选择夹名）+['所属子夹']（页索引）。**Phase2 运行时(compiler.ts)**：子夹逐页 TCM_INSERTITEMW + 现行子夹 TCM_SETCURSEL；g_ycTabPages 表{childId,tabId,pageIndex}；yc_tab_sync(tabId) 按 TabCtrl_GetCurSel ShowWindow 子控件；WM_NOTIFY TCN_SELCHANGE→yc_tab_sync（与 hyperlink_do 并列，内置无条件）；CreateControls 后逐选择夹初始同步；子控件初始可视=（所属子夹==现行子夹）故非当前页 born hidden；隐藏自身=真则选择夹自身不可视；取子夹数目/取子夹名称/置子夹名称 方法→yc_tab_*（TCM_GETITEMCOUNT/GETITEMW/SETITEMW，索引 1 基）。**Phase1 catalog+弹窗**：window-units.json 选择夹 子夹管理→子夹标题(typeName 新增 子夹表)+哈希；DEFAULT_SIZES 加 选择夹[220,150]/月历/滑块条；新建 ChildTabEditorDialog（仿 MenuEditorDialog 扁平，增删改名上下移，复用 MenuEditorDialog.css）；Sidebar renderEditableCell 加 子夹表 分支 EditableTabListCell 开弹窗。**Phase3 WYSIWYG 设计器(VisualDesigner.tsx)**：activeTabPage 状态(key=选择夹名)+ getActivePage；选择夹预览标签头可点切当前页；画布 map 过滤——子控件仅当所属子夹==当前页才渲染；几何归属 ownershipForRect（中心点落入选择夹体区去顶 22px→写归属，否则清）接 addControl（拖入即归属当前页）+单/多控件拖动 mouseUp reassignTabOwnership（activeTabPageRef 避 [] 闭包陈旧）。验证：清缓存重编——`TCM_INSERTITEMW`×2+`TCM_SETCURSEL`+`g_ycTabPages{IDC_页2按钮,IDC_选夹1,1}`+`TCN_SELCHANGE→yc_tab_sync`+页2按钮 born hidden+`yc_tab_get_name`；面板/弹窗 E2E（子夹标题按钮开弹窗2行、预览标签头[甲夹,乙夹]）；WYSIWYG E2E（点标签头切页、按页显隐子控件、拖出选择夹清归属）；无选择夹项目回归编译 success；165 单测全绿、build 通过。**遗留(小)**：拖动选择夹本身时子控件不随动(可后补)；z序靠数组序(新控件追加在选择夹后，正常)；重命名选择夹不自动同步子控件所属选择夹。**至此 21 个可视控件全部属性/成员补全齐全且运行时落地。**
- [新增] **日期框/月历日期属性落地（今天/最小日期/最大日期/首尾选择日）**（follow-up 续做）。此前这些 date 属性声明占位（缺文本→SYSTEMTIME 解析）。本轮：main.cpp 前导加 `yc_parse_systemtime(s, SYSTEMTIME*)`——swscanf `%d%*[-/.]%d%*[-/.]%d %d:%d:%d` 解析「年/月/日 [时:分:秒]」（分隔符 /、-、. 通用，时间可省），基本校验后填 SYSTEMTIME。buildStdDatePickerCodegen：今天→DTM_SETSYSTEMTIME(GDT_VALID)、最小/最大日期→DTM_SETRANGE(GDTR_MIN|MAX)。buildStdMonthCalCodegen：今天→MCM_SETTODAY、最小/最大→MCM_SETRANGE、首尾选择日→多选 MCM_SETSELRANGE / 单选 MCM_SETCURSEL。均仅当属性非空才发消息（空则保留控件默认=当前日期）。验证：真实窗放 日期1(今天2026/07/11、最小2020/1/1、最大2030/12/31)+月历1(今天2026-07-11、首选择日2026/07/15)，清缓存重编 success——生成 DTM_SETSYSTEMTIME/DTM_SETRANGE + MCM_SETTODAY/MCM_SETCURSEL，解析器兼容 / 与 - 两种分隔；无日期控件项目回归编译 success；165 单测全绿、build 通过。**至此 follow-up 仅剩 选择夹子夹页**（TCM_INSERTITEM + 每页子窗口 + 设计器控件归属子夹，是跨设计器/运行时的大功能，非纯 codegen，另开专项）。21 个可视控件属性/成员补全全齐，20 个有运行时落地。
- [新增] **列表框「取所有被选择项目」返回整数数组（多选列表框方法集补齐 18/18）**（follow-up 续做）。此前该方法只在补全、不能编译（返回整数型数组需数组封送）。本轮：main.cpp 前导 `yc_lb_get_sel_items(名)→std::vector<long long>`（LB_GETSELCOUNT 取数、LB_GETSELITEMS 取索引，单选/无选返回空数组），main.cpp 补 `#include <vector>`；转译文件加 extern 声明；控件方法派发加 '取所有被选择项目'→yc_lb_get_sel_items(L"名")。数组变量赋值天然吻合——`整数型数组 变量 ＝ 列表框1.取所有被选择项目()` 译成 `变量 = yc_lb_get_sel_items(...)`（std::vector=std::vector）。验证：真实窗放 多选列(允许选择多项=真)+ 局部 选中项(整数型数组) ＝ 多选列.取所有被选择项目()，清缓存重编 success；无该控件项目回归编译 success；165 单测全绿。**坑**：接收变量须声明为数组（.局部变量 名,整数型,,"0" 带维度字段），声明成标量整数型会「int = std::vector」编译失败（测试侧问题，非 codegen）。至此组合框/列表框/选择列表框成员方法运行时全通。**follow-up 剩余**：选择夹子夹页(TCM_INSERTITEM+每页子窗口，需设计器支持控件归属子夹，是跨设计器/运行时的大功能)、日期框日期属性(今天/最小/最大日期，需文本→SYSTEMTIME 解析器)。
- [修正] **独立横向/纵向滚动条滑块可跟随点击/拖动移动**（follow-up 续做）。原生 Win32 SCROLLBAR 控件的滑块不会自己动，必须父窗口在 WM_?SCROLL 里 SetScrollInfo，之前只调用户「位置被改变」事件、滑块永远不动。本轮：①编译期收集 g_ycScrollBars 表{id,行改变值,页改变值}（min/max/pos 已由创建期 SetScrollInfo 落，处理器经 GetScrollInfo 读回）；②WM_HSCROLL/WM_VSCROLL 处理器在用户事件 switch 之前加滑块跟随——按 LOWORD(wParam) 滚动码：SB_LINELEFT/RIGHT ±行改变值、SB_PAGELEFT/RIGHT ±页改变值、SB_THUMBTRACK/THUMBPOSITION→nTrackPos、SB_TOP/BOTTOM→nMin/nMax，夹取 [nMin,nMax] 后 SetScrollInfo(SB_CTL,SIF_POS)。H/V 滚动码同值故一套逻辑通用。验证：真实窗放 纵向滚动1(最大100/页10/行2/位置30)，清缓存重编 success——生成 `g_ycScrollBars{IDC_滚动1,2,10}` + GetScrollInfo→按码调 p→SetScrollInfo 滑块跟随；无滚动条项目回归编译 success（表与逻辑均 gate 在 entries>0）；165 单测全绿、build 通过。**follow-up 剩余**：选择夹子夹页(TCM_INSERTITEM+子窗口)、日期框日期属性(需文本→SYSTEMTIME 解析)、列表框取所有被选择项目(数组返回)、SB_THUMBTRACK 时按「允许拖动跟踪」门控事件（当前拖动即触发事件，未按该属性抑制）。
- [新增] **选择列表框自绘复选框可勾选（是否被选中/选中项目/是否被允许/允许 四方法可用）**（follow-up 续做）。此前 选择列表框只当普通 LISTBOX、无复选框。本轮 owner-draw 落地：①buildStdListBoxCodegen isChecked 分支加 LBS_OWNERDRAWFIXED；②创建后 SetWindowSubclass(YcChkListProc) 挂子类——WM_LBUTTONDOWN 用 LB_ITEMFROMPOINT 命中项 + WM_CHAR 空格 → yc_chk_toggle(切换勾选+InvalidateRect)；③勾选/禁止状态用运行时 `std::map<HWND, std::map<int,int>>`(g_ycChkChecked/g_ycChkDisabled，按控件+索引，随动态项目增长)；④WM_DRAWITEM 加 ODT_LISTBOX 分支(仅 yc_is_chklist 命中)——FillRect 背景(选中态 COLOR_HIGHLIGHT) + DrawFrameControl(DFC_BUTTON，勾选 DFCS_CHECKED/禁止 DFCS_INACTIVE) + DrawTextW 项目文本(禁止灰/选中高亮字)；⑤WM_MEASUREITEM 定项高 18(LBS_OWNERDRAWFIXED 必需)；⑥四方法进控件方法派发：是否被选中→yc_chk_is_checked、选中项目→yc_chk_set_checked、是否被允许→yc_chk_is_enabled、允许→yc_chk_enable(均按名取 HWND 操作 map + 重绘)；`#include <map>`。WM_DRAWITEM 现按钮/外形框/选择列表框三分支共用一个 case(guard 加 chkListIds)。验证：真实窗放 选列1 + 代码 加入项目/选中项目(0,真)/允许(1,假)/是否被选中(0)，清缓存重编 success——生成 LBS_OWNERDRAWFIXED + SetWindowSubclass(YcChkListProc) + ODT_LISTBOX 自绘 DrawFrameControl + WM_MEASUREITEM=18 + yc_chk_set_checked/enable/is_checked 调用；无选择列表框项目回归编译 success(表/助手始终生成、空时占位)；165 单测全绿、build 通过。**已知局限**：勾选状态按索引存 map，删除项目后索引位移会与状态错位(加/勾选/读为主流，删较少)；取所有被选择项目(数组返回)未做。**follow-up 剩余**：选择夹子夹页(TCM_INSERTITEM+子窗口)、日期框日期属性(需文本→SYSTEMTIME)、滚动条滑块跟随+WM_?SCROLL 事件门控、取所有被选择项目。
- [新增] **超级链接框可点击并打开邮件/网址（含「跳转」方法）**（follow-up 续做）。此前仅创建为 SysLink 显示纯文本、不可点。本轮用原生 SysLink 路线（比 owner-draw 省，悬停手型/键盘/下划线全由控件负责）：①创建时标题包 `<a>…</a>` 标记使 SysLink 渲染为链接；②编译期收集 g_ycHyperLinks 表{id,类型,电子邮件地址,Internet地址}（始终生成，空时占位项 id=0，使「跳转」跨编译单元恒可链接）；③main.cpp 前导 yc_hyperlink_do(id)（类型0→ShellExecuteW mailto:邮件，否则→打开网址）+ yc_hyperlink_jump(名)（名→HWND→id→do），加 #include <shellapi.h>；④WM_NOTIFY 处理器加 `if(pnm->code==NM_CLICK||NM_RETURN) yc_hyperlink_do(idFrom)`——点击/回车即打开；⑤「跳转」方法进控件方法派发（translateListLikeMethodCall 加 '跳转'→yc_hyperlink_jump(L"名")），`超级链接框1.跳转()` 编译期可用。验证：真实窗放 链接1(类型=网址/Internet地址=example.com)+创建完毕写 链接1.跳转()，清缓存重编 success——生成 `CreateWindowExW(0,L"SysLink",L"<a>访问官网</a>")` + 表项 `{IDC_链接1,1,L"",L"https://example.com"}` + NM_CLICK 处理 + `yc_hyperlink_jump(L"链接1")`；无超链接项目回归编译 success（占位表兜底）；165 单测全绿、build 通过。**follow-up 剩余**：文本/访问后/热点颜色（SysLink 原生不易改色，需 owner-draw 或 LM_）；选择列表框勾选专属方法(owner-draw 复选框状态表)、取所有被选择项目(数组返回)、选择夹子夹页、日期框日期属性、滚动条滑块跟随。
- [新增] **组合框/列表框/选择列表框 项目成员方法运行时落地（加入项目/取项目文本等 17 个真正可用）**（follow-up 续做）。此前这些方法只进补全不能编译；本轮打通转译器「控件.方法(参数)」派发 + 运行时助手：①main.cpp 前导新增 yc_ll_*/yc_lb_* 一组助手(yc_get_control_handle_by_name 取 HWND，运行期 GetClassNameW 判 COMBOBOX/LISTBOX 分派 CB_*/LB_* 消息)——加入项目 CB/LB_ADDSTRING+SETITEMDATA、插入项目 INSERTSTRING、删除项目 DELETESTRING、清空 RESETCONTENT、取项目数 GETCOUNT、取/置项目文本 GET(LB)TEXT/删+插保数值、取/置项目数值 GET/SETITEMDATA、取/置顶端可见项目 GET/SETTOPINDEX、选择 FINDSTRING+SETCURSEL、列表框专属 取已选择项目数/取焦点项目/置焦点项目/是否被选择/选择项目(LB_GETSELCOUNT/CARETINDEX/GETSEL/SETSEL)；转译文件前导加 extern 声明；②转译器 translateListLikeMethodCall(LISTLIKE_METHOD_NAMES 判据)接两处——表达式上下文(translateExpressionToC，如 变量=组合框1.取项目文本(0))和语句上下文(语句级 else 分支，如 组合框1.加入项目("x"))，参数递归翻译；isTextExpression 认 yc_ll_get_text 使文本赋值正确。验证：真实 .eyc 写 组合框1.加入项目("甲项")/列表框1.插入项目(0,"列项零")/文本值=组合框1.取项目文本(0)/数量=组合框1.取项目数() 等，清缓存重编 success——生成 yc_ll_add_item(L"组合框1",L"甲项",0)/yc_ll_insert_item/yc_ll_get_text/yc_ll_count/yc_ll_delete_item/yc_ll_clear 全对；165 单测全绿、build 通过。**修的坑**：C++ 注释里 `CB_*/LB_*` 的 `*/` 提前闭合注释→残码编译失败(改成「CB 或 LB 消息」)；测试变量 变量2 被前面数组字面量 `变量2={1,2}` 污染成 std::vector 致 int 赋值失败(换干净变量，非 codegen 问题)。**仍 follow-up**：选择列表框 勾选专属方法(是否被选中/选中项目/是否被允许/允许)需 owner-draw 复选框并行状态表；取所有被选择项目返回整数数组;选择夹子夹页、超级链接框跳转自绘、日期框日期属性。
- [新增] **子代理并行 + 主体集成，一次补齐剩余 12 个空壳可视控件**（用户「逐个补...弄子代理做完」）。用 workflow 并行铺 12 个子代理，各读权威帮助文件产出结构化规格(属性映射/成员方法签名/Win32 codegen 计划/预览计划，1.5M token/0 错)，主体串行集成 + 逐个编译验证(仅主仓库有 zig/electron)。**齐全(面板+补全)**：window-units.json 一次性补 进度条6/滑块条12/横纵滚动条各6/日期框9/月历20/选择夹8/超级链接框9/组合框16/列表框13/选择列表框9/时钟1 属性(单次哈希 fd64→c4f4，4 控件补 className)；CONTROL_TYPE_METHODS 补 组合框12/列表框18/选择列表框16/选择夹3/超级链接框1 共 50 个成员方法。**编译落地**：新增 8 个 buildStd*Codegen——进度条(PBS_*/PBM_SETRANGE32/SETPOS)、滑块条(TBS_*/TBM_SETRANGE/TIC/PAGE/LINE/SEL/POS)、滚动条(SBS_*/SetScrollInfo)、日期框(SysDateTimePick32/DTS_*)、月历(SysMonthCal32/MCS_*/MCM_SETFIRSTDAYOFWEEK/COLOR)、组合框(CBS_SIMPLE/DROPDOWN/DROPDOWNLIST/CB_SETCURSEL+颜色表)、列表框&选择列表框(LBS_*/LB_SETCURSEL+颜色表)；抽 ctrlBorderExStyle 复用 6 路边框；主循环加 8 个 isStd* 分派 + 统一 post-create 拦截分支(原生类忽略 WM_APP+1，须拦截免落 WCM_SETPROP)；时钟 special-case(SetTimer，非可视不建窗)；WM_CTLCOLORLISTBOX + 组合框子控件 GetParent 反查上色；InitCommonControlsEx 补 ICC_DATE_CLASSES|ICC_LINK_CLASSES(后者 mingw 缺，加 #ifndef 守卫 0x8000)。**设计器预览**：进度条(按位置填充/纵向/分块)、滑块条(SVG 槽+拇指)、横纵滚动条(箭头+滑块按量程定位)、日期框(输入框+下拉/调节钮)、月历(迷你日历网格)、组合框(反映选中项/色)、列表框(项目行+选中高亮)、超级链接框(下划线)、选择夹(选项卡头) + 对应 CSS。验证：全 11 控件同窗清缓存重编 success(核对 PBM_SETPOS/TBM_SETPOS/SetScrollInfo/SysDateTimePick32/SysMonthCal32/CB_SETCURSEL/LB_SETCURSEL/SetTimer/ICC_DATE_CLASSES 生成)；面板 E2E(进度条6属性/组合框类型现行选中项/月历31行)、补全 E2E(组合框加入项目/取项目文本、进度条位置+获取焦点)、预览 E2E 7 项(进度60%/滑块/滚动/月历31天/列表选中/日期调节钮)全过；164+1 单测全绿、build 通过。至此 21 个可视控件全部有完整属性面板+成员补全，19 个有创建期编译落地。**follow-up(声明占位)**：组合框/列表框/选择列表框动态成员方法(加入项目等)运行时需转译器方法派发+项目表；选择夹子夹页、超级链接框 owner-draw/跳转、日期框日期属性(需文本→SYSTEMTIME)、滚动条滑块跟随+事件。window-units.json 改动需重启 dev。
- [新增] **逐个补空壳控件·图片框/外形框（STATIC 系）完整落地**（用户「逐个补」）。图片框(PicBox,STATIC,7属性：边框/背景颜色/图片/显示方式(居左上/缩放/居中)/播放动画/数据源/数据列)：buildStdPicBoxCodegen——边框走 exStyle 6 路复用、背景色经 WM_CTLCOLORSTATIC、有图 SS_BITMAP + 创建后 GDI+ 解码 STM_SETIMAGE、显示方式居中→SS_CENTERIMAGE（缩放/播放动画/数据绑定声明占位）；vd-preview-image 用 <img>(object-fit/position 反映显示方式)+边框+背景。外形框(ShapeBox,7属性：背景颜色/外形(8形状)/线条效果(通常/凹入/凸出)/线型(6种)/线宽/线条颜色/填充颜色)：className 空→设为 STATIC + SS_OWNERDRAW，编译期收集 g_ycShapeBoxes 表，WM_DRAWITEM 扩 ODT_STATIC 分支自绘——FillRect 背景→CreatePen(线型映射 PS_NULL/DASH/DOT/DASHDOT/DASHDOTDOT/SOLID + 线宽 + 线色)+CreateSolidBrush(填充色)→按外形 Rectangle/Ellipse/RoundRect/MoveToEx+LineTo(横纵线)（正方/圆/圆角正方取 min 边长）→DrawEdge 立体效果；vd-preview-shape 用 SVG(rect/ellipse/line + strokeDasharray 反映线型)。哈希同步；WM_DRAWITEM 现由 按钮自绘+外形框 共用一个 case（拆共享开头/结尾避免重复 case 标签）。验证：清缓存重编——图片框 `WS_EX_WINDOWEDGE STATIC` + `SS_BITMAP|SS_CENTERIMAGE` + `STM_SETIMAGE`，外形框 `g_ycShapeBoxes{2椭圆,1凹入,2划线,3宽,255红,65535黄}` + `SS_OWNERDRAW` + `ODT_STATIC`→`Ellipse`；面板/预览/补全 E2E 7 项全过(图片框 img、外形框 svg 椭圆渲染)；164 单测全绿、build 通过。window-units.json 改动需重启 dev。进度：已完整落地 窗口/按钮/编辑框/标签/分组框/选择框/单选框/图片框/外形框（9 个），待补 组合框/列表框/进度条/滑块条/滚动条/日期框/月历/选择夹/超级链接框/选择列表框 等。
- [新增] **逐个补空壳控件·分组框/选择框/单选框（BUTTON 族）完整落地**（用户「逐个补」）。依权威帮助文件补 window-units.json：分组框(GroupBox,4 专属属性：对齐方式/文本颜色/背景颜色/字体)、选择框(CheckBox,12：图片/按钮形式/平面/标题居左/横向对齐/纵向对齐/文本颜色/背景颜色/字体/选中/数据源/数据列)、单选框(RadioBox,10：同选择框去数据源/数据列)，哈希同步。**编译落地**三新函数：buildStdGroupBoxCodegen(BS_GROUPBOX + 标题对齐 BS_LEFT/CENTER/RIGHT)、buildStdCheckableCodegen(isRadio 切 BS_AUTOCHECKBOX/BS_AUTORADIOBUTTON + 按钮形式 BS_PUSHLIKE/平面 BS_FLAT/标题居左 BS_LEFTTEXT/横向 BS_LEFT|CENTER|RIGHT/纵向 BS_TOP|VCENTER|BOTTOM + WS_TABSTOP)；选中态创建后 BM_SETCHECK；三者文本/背景色复用 g_ycEditColors + WM_CTLCOLORSTATIC，字体走通用 CreateFontW 路径。控制循环加 else-if 分支拦截（BUTTON 忽略 WM_APP+1，故不落到 WCM_SETPROP 通用属性通道）。**设计器预览**：选择框/单选框合并分支加 选中态(vd-preview-check-on 实心)/标题居左(row-reverse)/文本背景色；分组框加标题对齐(text-align 由对齐方式驱动)/文本背景色。图片/数据源/数据列声明占位。验证：清缓存重编 proj-winprops——分组框生成 `BS_GROUPBOX|BS_CENTER`+颜色表红字项，选择框 `BS_AUTOCHECKBOX|BS_FLAT|BS_LEFTTEXT|BS_LEFT|BS_TOP`+`BM_SETCHECK BST_CHECKED`，单选框 `BS_AUTORADIOBUTTON|BS_PUSHLIKE`+`BM_SETCHECK`；面板 E2E 三控件属性齐全(单选框正确无数据源/数据列)；补全过滤复验 选中/横向对齐方式属性 + 获取焦点通用方法(选择框/单选框/分组框)；164 单测全绿、build 通过。window-units.json 改动需重启 dev。进度：已完整落地 窗口/按钮/编辑框/标签/分组框/选择框/单选框，待补 图片框/外形框/组合框/列表框/进度条/滑块条/滚动条/日期框/月历/选择夹/超级链接框 等。
- [新增] **逐个补齐空壳控件·标签（Label）完整落地**（用户「逐个补」，依权威帮助文件）。此前 window-units.json 里 标签 为 0 属性空壳，现补 20 个专属属性（对照易语言 Label 数据类型 29 号成员，去 9 公共）：效果(通常/凹入/凸出/阴影/透明)、边框(7 项含渐变镜框式)、渐变边框宽度/颜色1-3、字体、文本颜色、背景颜色、底图、底图方式(居左上/平铺/居中)、渐变背景方式(9 向)/颜色1-3、横向对齐方式、是否自动折行、纵向对齐方式、数据源、数据列，哈希同步。**编译落地** buildStdLabelCodegen（STATIC 类）：横向对齐→SS_LEFT/CENTER/RIGHT（左对齐时按「是否自动折行」选 SS_LEFT/SS_LEFTNOWORDWRAP）、纵向居中→SS_CENTERIMAGE、边框→exStyle 复用编辑框 6 路映射（凹入CLIENTEDGE/凸出WINDOWEDGE/浅凹STATICEDGE/镜框&渐变镜框DLGMODALFRAME/单线WS_BORDER）、文本/背景色与「效果=透明」经 WM_CTLCOLORSTATIC 查表（复用 g_ycEditColors，结构加 transparent 字段；透明=SetBkMode(TRANSPARENT)+NULL_BRUSH 让父窗口透出）、字体走通用 CreateFontW 路径。**成员方法** 标签专属「调用反馈事件(参数一,参数二,事件传递方式)→整数型」(SendLabelMsg) 入 CONTROL_TYPE_METHODS。**设计器预览** vd-preview-label 应用对齐(justify/align-items)/文本色/背景色(默认白或透明时透明)/边框(凹入-sunken/凸出-raised/其余-bordered 三态)。**渐变边框/背景、底图、数据源/数据列 暂声明占位**（需自绘/数据绑定，后续补），效果的凹入/凸出/阴影亦暂声明。验证：加单测(标签有调用反馈事件、有效果属性、有通用对象方法、不串编辑框专属方法)，163+1 全绿；清缓存重编——标签设 居中/凹入式/红字/透明 生成 `CreateWindowExW(WS_EX_CLIENTEDGE,L"STATIC",…) WS_CHILD|SS_NOTIFY|SS_CENTER` + 颜色表项 `{IDC_标签测试,255,…,1}`(transparent=1) + WM_CTLCOLORSTATIC 透明分支；面板 E2E——选中标签控件面板含全部 20 专属属性(效果枚举5项正确)+11公共，`标签.` 补全含 效果/边框属性、获取焦点通用方法、调用反馈事件专属方法；typecheck+build 通过。**关键排错**：手写 .efw 造控件须带 id/left/top/enabled/text（缺 id 则设计器无法选中→属性面板空白，但编译/补全仍正常，易误判面板 bug）；帮助文件解析器原 regex 强制要「英文名称为」段，漏掉无英文名的成员属性（标签的渐变*/是否自动折行、组合框的列表项目/项目数值），已改英文名可选重抓。window-units.json 改动需重启 dev。
- [修正] **对照易语言权威帮助文件查漏补缺控件成员枚举（编辑框边框/转换方式/对齐、按钮对齐）**（用户提示 lib/krnln/系统核心支持库帮助说明文件.txt 从易语言导出、较全面）。解析该文件 46 个数据类型段（数据类型名称：X 英文名称：Y + 成员属性<N>/可供选择的属性值/组件事件/对象．方法），与 window-units.json 已落地的 窗口/按钮/编辑框 逐属性+枚举对照，修正 6 处枚举偏差：①编辑框「边框」原仅 3 项且错序 [无边框/单线边框/凹入式] → 权威 6 项正序 [无边框/凹入式/凸出式/浅凹入式/镜框式/单线边框式]，默认值 2→1(凹入式，视觉不变)；②编辑框「转换方式」与易语言 ConvertMode 索引对齐 [无/大写->小写/小写->大写]（原 [无/转换为大写/转换为小写] 语义反了）；③编辑框「对齐方式」居中对齐→居中；④编辑框「输入方式」只读文本→只读方式；⑤⑥按钮「横向/纵向对齐方式」去掉标签里多余的 "0./1./2." 前缀。编译器 buildStdEditCodegen 同步：边框 0无/1凹入式→WS_EX_CLIENTEDGE/2凸出式→WS_EX_WINDOWEDGE/3浅凹入式→WS_EX_STATICEDGE/4镜框式→WS_EX_DLGMODALFRAME/5单线边框式→WS_BORDER；转换方式 1→ES_LOWERCASE、2→ES_UPPERCASE（原反）。window-units.json 哈希 66da4dce→89588c2a 同步。验证：无头清缓存重编 proj-winprops——边框=3 生成 WS_EX_STATICEDGE、转换方式=2 生成 ES_UPPERCASE（旧码会是空/ES_LOWERCASE，证明新映射生效）；属性面板 E2E——选中编辑框1 面板含 字体 行、边框 6 项正序、转换方式易语言标签、对齐方式=居中、输入方式=只读方式，选中按钮1 横向对齐无 "0." 前缀（8 项全过）；typecheck+build 通过。附注：类型异（我方 typeName=颜色/图片/选择整数 vs 权威 字节集/整数型）为面板控件类型 vs ABI 类型，非缺陷不动。**盲区**：帮助文件解析确认 window-units.json 除 窗口/按钮/编辑框 外，标签/选择框/单选框/组合框/列表框/分组框/图片框/进度条/滑块条/选择夹等 20+ 可视控件均为 0 属性空壳（仅继承 9 公共属性），类型专属属性/方法尚未落地，待后续按控件逐个补（权威规模见帮助文件，如 组合框 22 属性+12 方法、列表框 19 属性+18 方法、标签 20 属性）。window-units.json 改动需重启 dev。
- [新增] **编辑框成员补全对齐易语言（补 4 属性 + 专属方法 加入文本）**（用户贴编辑框成员列表）。对照易语言编辑框成员：①window-units.json 编辑框单元补 字体(字体型，复用字体对话框)、数据源/数据列(数据绑定，暂声明)、被选择文本(读写选区，暂声明)——24 属性，哈希同步；②新增控件类型专属方法机制 CONTROL_TYPE_METHODS(editorCoreUtils)——编辑框专属「加入文本(欲加入文本 文本型)→无返回值」(对应 krnln_AddText，易语言只对文本控件显示，不进通用对象方法)，buildMemberCompletionSource 按 typeName 追加；③设计期属性 备注/可停留焦点/停留顺序 仍经 DESIGN_ONLY 排除不作成员。至此 编辑框. 成员含 内容/边框/字体/滚动条/数据源/数据列/文本颜色/背景颜色/隐藏选择/对齐方式/输入方式/转换方式/调节器方式/被选择文本/最大允许长度/是否允许多行/密码遮盖字符/调节器上下限/起始选择位置/被选择字符数(+公共) + 23 对象方法 + 加入文本。验证：新增单测(编辑框有加入文本、按钮无但有通用对象方法)，163 单测全绿；真实编辑器 E2E——编辑框1. 弹窗含 内容/边框/字体属性、获取焦点/加入文本方法、被选择文本、数据源/数据列，备注不作成员；typecheck 通过。附注：window-units.json 需重启 dev；数据源/数据列/被选择文本为声明占位(运行时数据绑定/选区待后续)，字体+加入文本(krnln_AddText impl)功能可用。
- [优化] **编辑框设计器预览显示调节器**（用户：选了调节器预览里没出现）。上一批只做了运行时/面板，设计器预览未画。VisualDesigner 编辑框预览：调节器方式≠0(无) 时右侧显示上下箭头(▴▾)——flex 布局文字占左、调节器贴右边框(负外边距抵消 padding 铺满上下)，与 Win32 msctls_updown32 外观近似；无调节器时不显示。验证：E2E——自动/手动调节器预览均显 2 个箭头按钮+spin 类，无调节器不显；typecheck+162 单测全绿。
- [新增] **编辑框调节器三模式 + 上/下限值作调节器方式子项**（用户）。①window-units.json 编辑框「调节器方式」pickOptions 由 无调节器/数值调节器(2项) 扩为 无调节器/自动调节器/手动调节器(3项)，哈希同步；②属性面板——调节器上限值/底限值 作「调节器方式」下级子项(缩进 prop-name-sub)，**仅自动调节器(1)时可编辑**，无调节器(0)/手动调节器(2) 均禁用；照既有子项模式(停留顺序/圆角半径等)；③编译器 buildStdEditCodegen——spinMode 1(自动)=msctls_updown32 带 UDS_SETBUDDYINT+UDM_SETRANGE32(上下键在上/下限内自动增减编辑框整数)，spinMode 2(手动)=创建 updown 贴附编辑框但不绑整数、不设范围(由程序 WM_VSCROLL 事件自理)。验证：E2E 无头编译 自动(上50下5)生成 UDS_SETBUDDYINT+UDM_SETRANGE32(5,50)、手动生成 updown 但无 UDS_SETBUDDYINT/无 SETRANGE；面板 UI 探针 8 项全过(上/下限缩进子项、无调节器禁用、自动可编辑、手动禁用)；162 单测全绿，typecheck 通过。附注：window-units.json+编译器改动需重启 dev。
- [修复] **文本型改为 std::wstring 值类型（消除内存泄漏，RAII 自动管理）**（用户：文本型换 std::wstring）。此前 文本型=裸 wchar_t*，各文本函数 malloc 返回、从不 free → 泄漏。迁移方案（低风险）：新增 struct YC_TEXT 包裹 std::wstring（拷贝即值语义、出作用域自动析构、无泄漏），并带 operator const wchar_t*() 使其无缝落进所有既有 const wchar_t* 调用点与原生/支持库 ABI，**无需改动数十处消费点**。①类型映射 文本型→YC_TEXT，默认值 YC_TEXT()；②所有产文本的运行时函数改返回 YC_TEXT 并用 std::wstring 直接构建（不再 malloc）：yc_text_concat/yc_value_to_text(全重载)/yc_utf8_to_wide/yc_format_win32_error/yc_ary_to_text_str/yc_dbg_fmt(全重载)/yc_get_control_text；③消费函数(yc_text_compare/yc_text_starts_with/yc_set_control_text/yc_wide_to_utf8)保持 const wchar_t* 参数不变(YC_TEXT 隐式转换)；④DLL 命令边界保持 wchar_t* 原生 ABI 隔离；⑤main.cpp 同步 YC_TEXT 定义(+<string>)供 yc_get_control_text 跨编译单元按值返回；TRANSPILE_CACHE_VERSION 16→17。**类型审计**：其余类型均自动管理(值类型/字节集 std::vector/大整数 YC_BIG 内含 std::string/数值数组 std::vector)，唯文本作数组元素仍存指针位(独立遗留)。验证：文本重项目(文本变量/到文本/文本拼接/文本数组/字节集/多值调试输出)编译通过+运行时调试输出**逐字节完全一致**(全角引号/数组:N{}/字节集:N{}/双空格多值全对)；控件文本写(yc_set_control_text)+读(yc_get_control_text 返回 YC_TEXT)round-trip 正确；162 单测全绿，typecheck 通过。附注：编译器改动需重启 dev。
- [优化] **颜色选择器支持直接输入/粘贴/复制 #RRGGBB**（用户）。EditableColorCell 在系统取色器旁加十六进制文本输入(可粘贴 #ffffff、失焦/回车提交、无效回退、等宽字体便于复制)；字体对话框「文本颜色」行同样加 hex 输入；colorrefToHexStr/hexStrToColorref helper。验证：E2E——底色单元格 hex 输入粘贴 #00ff00 → 按钮预览变绿、回显可复制；162 单测全绿，typecheck 通过。
- [新增] **文本颜色（并入字体）+ 按钮底色（面板独立属性）+ 自绘按钮落地**（用户：文本颜色加字体里、按钮底色单独加面板）。①文本颜色——FontSpec 加 color(COLORREF)，字体对话框加「文本颜色」选择行(原生取色器+默认按钮)，colorref↔hex helper，fontSpecToCss 输出 color，设计器控件预览经 --vd-control-color 上文字色(字体色优先于控件前景色)；②按钮底色——window-units.json 按钮单元加 底色(颜色型,0=默认)，哈希同步，设计器按钮预览背景取底色(0 回落)；③编译器自绘按钮——按钮设了底色或字体文本色则 BS_OWNERDRAW，收集 g_ycButtonDraws 表(id/底色/文本色/横纵对齐/是否默认)，WndProc WM_DRAWITEM 自绘：FillRect 底色、DrawEdge 边框(按下 EDGE_SUNKEN)、默认按钮加 FrameRect、SelectObject 控件字体、SetTextColor(文本色或默认)、DrawTextW 按对齐(按下偏移1px)、焦点虚框；自绘按钮让位图片(不走 BS_BITMAP)，标准按钮维持原样式路径。验证：E2E 无头编译 按钮1 底色红+文本白，生成 BS_OWNERDRAW/g_ycButtonDraws/WM_DRAWITEM/DrawTextW；运行时 Win32 取按钮中心像素=0x0000FF(红底自绘生效)；设计器+对话框 UI 探针 4 项全过(预览底色红/文本白、字体对话框文本颜色选择行、值=#ffffff)；162 单测全绿，typecheck 通过。文本颜色为字体通用能力(所有控件复用)，底色目前按钮专属。附注：主进程+window-units.json 改动需重启 dev。
- [优化] **字体对话框改可拖动浮动窗（原居中固定不可移动）**（用户：应能在 IDE 窗口内随意移动）。去掉全屏遮罩，改为 portal 到 body 的 fixed 浮动对话框：带标题栏(字体 + ✕关闭)，**按住标题栏可拖动**(document mousemove，夹取在窗口内不越界)，打开时居中、Esc/✕/完成 关闭；无遮罩故不挡 IDE、可边看边调。验证：E2E——对话框 portal 到 body/fixed/无遮罩、拖标题栏后位置改变(524,433→364,553)且仍完整可见、改字体设计器预览跟随、✕ 关闭；162 单测全绿，typecheck 通过。
- [优化] **字体选择器改模态对话框（避免被侧栏裁剪）**（用户：应弹窗设置不然会被遮挡）。原字体选择器是单元格内绝对定位弹层，处于侧栏属性表内会被 overflow 裁掉。改为 createPortal 到 document.body 的模态对话框：全屏半透明遮罩(fixed，z-index 3000)+居中对话框(字体下拉/字号/粗斜下划删除线四勾选/字体示例预览区/清除+完成)，点遮罩或 Esc 关闭，改动仍实时应用(设计器预览跟随)。验证：E2E——字体对话框 portal 到 body(非侧栏内、position:fixed、getBoundingClientRect 完整在视口内不被裁剪)、改字体名/字号/粗体设计器预览实时跟随、点完成关闭；162 单测全绿，typecheck 通过。
- [新增] **控件「字体」属性（可复用字体选择器 + 编译落地）**（用户：先做字体，后面别的控件复用）。①共享数据模型 fontSpec.ts——FontSpec{name/size/bold/italic/underline/strikeout}，值以 JSON 字符串存 properties['字体']，parseFontSpec/stringifyFontSpec/formatFontSummary/fontSpecToCss/常用中英文字体表；②属性面板 EditableFontCell——自绘字体选择器弹层(字体下拉[常用字体，含当前非常用字体]+字号数值+粗体/斜体/下划线/删除线四勾选+清除默认)，摘要按钮显示「字体名 字号 粗/斜」，点击外部关闭；renderEditableCell '字体' 型接入，任意控件的字体属性通用；③设计器预览——控件外层 .vd-control 注入字体 CSS 变量(family/size/weight/style/decoration)，.vd-preview 消费，未设时回落原默认外观；④编译器——parseControlFont 解析 JSON，控件有字体则 CreateFontW(高度=-MulDiv(磅,LOGPIXELSY,72)/weight 700或400/italic/underline/strikeout/DEFAULT_CHARSET/faceName)替代默认 WM_SETFONT，通用于所有控件。验证：E2E 无头编译 按钮1 字体=微软雅黑16粗斜，生成 CreateFontW(...,700,TRUE,...,L"微软雅黑")；运行时 Win32 GetObject(LOGFONT)——face=微软雅黑/weight=700/italic=1/height=-21(16pt@96dpi)全对；设计器+面板 UI 探针 8 项全过(预览字体名/字号21px/粗/斜、面板字体单元格、摘要「微软雅黑 16 粗/斜」、点击弹层、弹层含下拉+字号+4勾选)；162 单测全绿，typecheck 通过。字体选择器为控件通用组件，后续标签/编辑框等控件的字体属性直接复用。附注：window-units.json 之前已给按钮加字体成员，本次补全其编辑器与落地；主进程改动需重启 dev。
- [优化] **按钮成员补全对齐易语言（补字体 + 剔设计期属性）**（用户贴易语言按钮成员列表：14 属性 + 23 方法）。①补 字体——window-units.json 按钮单元加 字体(font, typeName '字体')，成为运行时成员；属性面板 '字体' 型渲染占位禁用格「（默认字体）」(字体选择器待后续)；②剔设计期属性——备注/可停留焦点/停留顺序(及窗口的 圆角半径)是设计期专用，出现在属性面板但**不是代码成员**，buildMemberCompletionSource 用 DESIGN_ONLY_PROPERTY_NAMES 集合从 unitMembers 过滤，与易语言运行时成员对齐(易语言按钮 14 成员不含这三者)。至此 按钮1. 成员 = 14 属性(标题/左边/顶边/宽度/高度/标记/可视/禁止/鼠标指针/图片/类型/横向对齐方式/纵向对齐方式/字体) + 23 对象方法，与易语言一致。验证：新增单测(设计期属性不作成员、类型/字体属性在、获取焦点方法在)，共 162 单测全绿；真实编辑器 E2E——按钮1. 弹窗含 图片/类型/字体 属性 + 获取焦点/销毁 方法，不含 备注/可停留焦点/停留顺序，横向对齐方式过滤后 present；typecheck 通过。附注：window-units.json 改动需重启 dev；字体编辑器(字体选择器 UI + CreateFont codegen)仍待后续。
- [新增] **按钮控件属性（照易语言按钮属性表）**（用户：窗口做完，现在做按钮）。按易语言「按钮」数据类型(14 成员)补齐：①window-units.json 按钮单元加 备注/可停留焦点/停留顺序/图片/类型(0通常 1默认)/横向对齐方式(0左 1中 2右)/纵向对齐方式(0顶 1中 2底)——标题/左边/顶边/宽度/高度/标记/可视/禁止/鼠标指针 由公共属性合并；字体暂缓(需字体选择器基础设施)；CRLF+哈希同步；②属性面板——控件分支加 停留顺序 作 可停留焦点 的下级(缩进+可停留焦点=假时禁用，照窗口 控制按钮/圆角半径 模式)；图片走既有图片选择器；③设计器预览——按钮 类型=默认(加粗强调色边框)、横向/纵向对齐方式(flex justify/align 文字定位)、图片(按钮上显示图片替代文字)；④编译器——buildStdButtonCodegen(标准 BUTTON 类同 EDIT 忽略 WM_APP+1，属性落创建样式)：类型→BS_DEFPUSHBUTTON/BS_PUSHBUTTON、横向→BS_LEFT/CENTER/RIGHT、纵向→BS_TOP/VCENTER/BOTTOM、可停留焦点→WS_TABSTOP；图片→BS_BITMAP + 内嵌每控件图片字节(g_ctrlImg_<idx>，与 controls 同序) + 运行时 GDI+ Bitmap::FromStream→GetHBITMAP→BM_SETIMAGE(GDI+ 启动门控加"任一控件有图")；仅 ctrl.type==='按钮' 走此路径(复选框/单选框/分组框同为 BUTTON 类但样式不同，不套用)；停留顺序沿用既有"控件按停留顺序排序=Tab 序"。验证：E2E 无头编译 按钮1(类型默认/横向左/纵向顶/带图) 成功，生成 `BS_DEFPUSHBUTTON|BS_LEFT|BS_TOP|WS_TABSTOP`、BS_BITMAP/g_ctrlImg_/GetHBITMAP/BM_SETIMAGE 全命中；运行时 Win32 探针 GWL_STYLE=0x50010501 四位(默认/左/顶/TABSTOP)全对；设计器 UI 探针 4 项全过(默认加粗类、横向 justify=flex-start、纵向 align=flex-start、图片显示 img)；161 单测全绿，typecheck 通过。附注：主进程+window-units.json 改动需重启 dev。遗留：字体(需字体选择器 UI + CreateFont codegen)。
- [新增] **窗口「图标」属性落地（设计器预览 + 编译，任意图片格式，无需 .ico）**（用户：图标加图片后预览和运行都没反应，是不是必须 ico）。此前 图标 仅声明进属性表、无渲染无编译。修复：①设计器——标题栏图标处若设了「图标」(data URL)则显示该图片(vd-form-titlebar-icon-img)，否则用默认窗体图标；②编译器——WindowFileInfo 加 iconImage 并解析，图片字节内嵌为数组；GDI+ 启动改为「底图或图标任一存在」即启，图标经 Gdiplus::Bitmap::FromStream 解码→GetHICON 转 HICON(g_hWindowIcon)；wcex.hIcon/hIconSm 用之，CreateWindow 后再 WM_SETICON(ICON_SMALL+ICON_BIG) 显式设置(任务栏/标题栏都生效)。**任意图片(PNG 等)自动转 HICON，不必是 .ico**——复用 底图 的 GDI+ 通道，故不做格式限制。验证：E2E 无头编译 图标=64x64品红PNG 项目成功，生成 GetHICON/Bitmap::FromStream/WM_SETICON ICON_BIG/wcex.hIcon=g_hWindowIcon 全命中；运行时 Win32——WM_GETICON 返回自定义 HICON(≠默认 IDI_APPLICATION)、GetIconInfo 位图 64x64(来自 PNG，证明 PNG→HICON 转换生效)；设计器 UI——标题栏显示自定义图标 img；161 单测全绿，typecheck 通过。附注：主进程改动需重启 dev。备注：GDI+ Bitmap 缩放由 Windows 处理，超大图标显示时会被系统缩放到 16/32 像素。
- [优化] **窗口去掉「背景音乐」「播放次数」属性**（用户：不需要）。这两个属性上一批仅为成员列表补全声明进 window-units.json（无编译器/设计器逻辑），现移除；窗口属性 32→30，libraryManager 哈希同步。验证：fresh 实例窗口属性不再含这两项；161 单测全绿(含 window-units.json 哈希校验)。
- [新增] **跨窗口成员补全（在任一窗口代码里引用别的窗口名解析其成员）**（用户续需）。此前成员补全仅当前 .eyc 关联的那个窗口；现扩展为项目内任意窗口。①Editor.tsx——projectWindowControlsMap/projectWindows 读盘时连带解析每个窗口关联 .eyc 的 `.子程序` 行得子程序名(除控件表外)，整表 projectWindows[{名,控件,子程序}] 下传 EycTableEditor；②EycTableEditor——projectWindowMemberMap(名→{控件,子程序})并入 memberParams；③buildMemberCompletionSource——objName 命中项目窗口名则 mappedType='窗口'(属性/方法随之出)；列子控件/子程序时判定用哪个窗口：当前窗口(windowControlTypeMap 里 '窗口' 项)用实时数据，引用的别的窗口用读盘表的控件+子程序，互不串。验证：新增 3 项跨窗口单测(打 窗口B. 出 B 的控件/子程序不串当前窗口、打当前窗口仍用实时数据、不存在窗口名不弹窗)，共 161 单测全绿；**真实编辑器 E2E**——仅开 _启动窗口.eyc，打 `窗口1.`(另一窗口)弹窗含 属性(标题/底图/图标)+方法(销毁/重画/移动/激活)+窗口1 独有控件(网页按钮1/网页编辑框1，_启动窗口 没有→证明取的是窗口1数据)+窗口1 子程序(__窗口1_创建完毕)，且不串当前窗口子程序(_按钮1_被单击)，无异常。typecheck 通过。至此窗口成员补全：当前/跨窗口、开/不开设计器、四类成员(属性/方法/子控件/子程序)全覆盖。
- [修复] **窗口成员补全不再依赖关联 .efw 打开（读盘回退）**（用户：单独开 .eyc 代码不开设计器时 窗口. 也要能解析成员；只要项目里有这个窗口就应解析）。原 activeWindowControls(Editor.tsx) 只在已打开标签页里找匹配的 .efw formData，.efw 未打开则控件表为空 → 窗口名解析不出类型、成员全无。修复：新增项目级窗口控件表读盘缓存 projectWindowControlsMap——useEffect 读项目目录所有 .efw(window.api.file.readDir + project.readFile)，解析 formData 得 {窗口名, 控件表}，按 sourceFile / 窗口名.eyc / 文件名.eyc 三种键入库(随 projectDir+debouncedTabs 刷新，窗口保存后自动更新)；activeWindowControls 改为：①优先已打开 .efw 标签页(含未落盘实时编辑)②否则按当前 .eyc 关联源文件名从读盘缓存解析。既保留开着设计器时的实时性，又让关闭设计器/仅开代码时照常补全。验证：**真实编辑器 E2E**——会话只开 _启动窗口.eyc(不开 .efw)，代码行输入 `_启动窗口.` 弹窗仍含 属性(标题/底图/图标/背景音乐/播放次数)+方法(销毁/重画/移动/激活)+子控件(按钮1；编辑框1 过滤后)+子程序(_按钮1_被单击 过滤后)，无页面异常；158 单测全绿，typecheck 通过。备注：跨窗口(在 A 窗口代码里打 B 窗口名)仍限当前窗口作用域，如需可后续把整表按窗口分组下传。
- [新增] **窗口成员补全再补：子控件 + 窗口子程序**（用户：把 窗口.按钮1 子控件、窗口._按钮1_被单击 子程序也加进成员列表）。buildMemberCompletionSource 在访问对象为「窗口」本身(typeName==='窗口')时，额外注入：①子控件——从 windowControlTypeMap 取该窗口的控件(排除窗口自身及窗口类型项)，作 category='组件' 成员，returnType=控件类型(供链式类型传播)；②子程序——从当前 .eyc 的 userSubNames(经新增 windowSubNames 参数传入 userSubNamesRef)作 category='子程序' 成员。仅窗口对象注入，控件访问(编辑框1.)不列同级控件/窗口子程序。数据链既有：activeWindowControls(Editor.tsx，含 add(窗口名,窗口)+各控件)、userSubNames(EycTableEditor 解析 .子程序 行)，均已随附给表格编辑器。验证：新增 2 项补全单测(窗口成员含子控件按钮1[类=组件/返回控件类型]与子程序_按钮1_被单击[类=子程序]、窗口自身不作自己子控件；控件访问不列同级控件/窗口子程序)，共 158 单测全绿；**真实编辑器 E2E**——开 efw+eyc、代码行单击进编辑、fill 中文名+type 句点触发成员补全，弹窗实测含 属性(标题/底图/图标/背景音乐/播放次数)+方法(销毁/重画/移动/激活)+子控件(按钮1；编辑框1 打字过滤后present)+子程序(_按钮1_被单击 过滤后present)，无页面异常。附注：成员补全需关联 efw 打开(activeWindowControls 靠匹配 efw 的 formData)；弹窗有显示上限(~31)，子控件/子程序排属性方法之后、超限需打字过滤或滚动(既有 UX)。至此 窗口. 成员四类(属性/方法/子控件/子程序)与易语言对齐。typecheck 通过。
- [新增] **窗口成员补全补齐：6 个属性 + 23 个对象方法**（用户对照易语言 `窗口名.` 成员列表指出缺失）。①属性——window-units.json 窗口单元补 图标(图片)/背景音乐(文本)/播放次数(整数)/帮助文件名(文本)/帮助标志值(整数)/F1键打开帮助(逻辑)，共 32 项(合并公共属性后面板/成员列表呈现 34)，CRLF+哈希同步；②方法(核心缺口)——`窗口名.` 原本只弹属性、方法一个不弹：根因是 WINDOW_METHOD_WHITELIST(23) 从命令目录过滤方法，但这些方法根本不在自动补全目录(krnln.commands.ycmd.json)，只存在于 ABI 映射(command-mapping.windows.json，键为「对象.获取焦点」等，对象=窗口/控件基类，全部已实现)。修复：从 ABI 映射提取 23 个对象方法的返回类型/参数(含易语言帮助文本)，在 editorCoreUtils 内置静态 WINDOW_OBJECT_METHODS，buildMemberCompletionSource 对「窗口单元类型(窗口本身或任一控件)」注入为「方法」类成员(isMember)；非窗口/控件类型(类实例/自定义型)不注入避免污染；保留原白名单目录过滤路径(未来清单补入自动去重)。CompletionParam 的 isVariable/isArray 改可选(消费方本就 `!!p.x` 兜底)。方法族：销毁/重画/移动/激活/获取焦点/可有焦点/禁止重画/允许重画/部分重画/取消重画/刷新显示/调整层次/弹出菜单/发送信息/投递信息/置父窗口/取窗口句柄/取标记组件/置外形图片/置托盘图标/取用户区宽度/取用户区高度/弹出托盘菜单。验证：新增 5 项补全单测(窗口成员含 23 方法且类=方法/isMember、方法带返回类型与参数、属性方法并存、控件也获对象方法、非窗口类型不注入)全过，共 156 单测全绿；fresh 实例 getWindowUnits 窗口属性 34 项、6 新属性齐全；typecheck 通过。附注：window-units.json 改动需重启 dev。遗留：子控件(窗口.按钮1)与窗口子程序(窗口._按钮1_被单击)作为成员尚未接入(另一类来源，待续)；新增 6 属性中 图标/背景音乐/播放次数/帮助族 仅声明进成员列表，运行时资源功能属后续批次。
- [新增] **窗口「底图方式」属性（底图显示方式，5 种）**（用户：有底图就能切底图方式，无底图禁用）。①window-units.json 加 底图方式(选择整数,紧跟底图,default 0)——pickOptions=图片平铺/图片居左上/图片居中/图片居右下/缩放图片；26 属性，CRLF+哈希同步；②属性面板——EditablePickCell 加 disabled(下拉禁用)，renderEditableCell pickOptions 分支转发；底图方式作「底图」的下级子选项(缩进 prop-name-sub)，**仅设置了底图(data URL)时可切换**，无底图时禁用；③设计器——BACK_IMAGE_MODE_CSS 映射五方式到画布背景图 CSS(平铺 repeat/auto、居左上/居中/居右下 no-repeat+auto+对应 position、缩放 100% 100%)，经 --vd-form-bg-size/repeat/position 变量注入 .vd-form-canvas-bgimage(图片层变量、网格层恒定)，改方式实时反映；④编译器——WindowFileInfo 加 backImageMode 并解析，WM_PAINT 按转译期方式发对应 GDI+ 绘制：缩放=DrawImage 拉伸满、平铺=TextureBrush.FillRectangle、居左上/居中/居右下=按 GetWidth/GetHeight 原尺寸 DrawImage 定位。验证：E2E 无头编译 底图+居中 项目成功，生成 `ix=(cw-iw)/2` 原尺寸居中绘制；运行时 Win32 探针——64x64品红图居中于 592x384 窗，客户区中心 GetPixel=0xFF00FF、左上角=0xF0F0F0(默认底色，图未覆盖，证明非拉伸)；设计器 UI 探针 11 项全过(底图方式缩进子选项、有图可切/无图禁用、居中 no-repeat+auto+center、实时切缩放→100% 100%、切平铺→repeat、清底图无崩溃且方式禁用+画布回落)；typecheck+151 单测全绿。附注：window-units.json+主进程改动需重启 dev。
- [修复] **外形从「圆角矩形」切到「矩形」时属性面板崩溃**（用户报）。根因：EditableIntCell 上一改动为支持 disabled 加了 `if (disabled) return <灰显span>` 早返回，但**误置于 useCallback(commitEdit) 之前**——外形切到矩形/椭圆时圆角半径的 disabled 由 false 变 true，该分支在 commitEdit 这个 Hook 之前返回，React 本次渲染的 Hook 数量少于上次（违反 Hook 规则），抛 "rendered fewer hooks than expected"（Minified React error #310）致整个属性面板白屏。修复：把 disabled 早返回移到所有 Hook（含 commitEdit useCallback）之后，Hook 调用序恒定。附注：先前圆角半径验证用整页 reload 分状态测，未复现——崩溃只在同一活动面板里 disabled 翻转的重渲染中发生；新增 E2E 直接在实时面板切「外形」下拉，断言无 Hook 崩溃/面板存活（复现即回归）。验证：8 项切换断言全过（圆角矩形↔矩形↔椭圆 各向切换无崩溃、禁用/可编辑态正确）；151 单测全绿，typecheck 通过。
- [新增] **窗口「圆角半径」属性（圆角矩形外形可调圆角）**（用户：选圆角矩形/椭圆后要能设圆角值）。①window-units.json 加 圆角半径(整数型,default 20,紧跟外形)，25 属性，CRLF+哈希同步；②属性面板——EditableIntCell 加 disabled 支持(禁用时渲染灰显只读 span .prop-value-disabled)，renderEditableCell 整数分支转发 disabled；圆角半径按「外形」的下级子选项处理(缩进 prop-name-sub)，**仅外形=圆角矩形(1)时可编辑**，矩形(0)/椭圆(2)时禁用——椭圆曲率由窗口宽高完全决定、无可调半径，圆角矩形想要更圆可增大半径(方窗足够大即成圆)；③设计器——formCornerRadius 经 --vd-form-corner-radius 传入，.vd-form-shape-1 的 border-radius 改用该变量(原硬编码 14px)，改半径画布实时跟随；④编译器——WindowFileInfo 加 cornerRadius 并解析，CreateRoundRectRgn 改用「圆角半径」(夹取到不超过短边一半；注意该 API 末两参是圆角椭圆的全宽高，故传 半径*2 与 CSS border-radius 语义对齐)。验证：E2E 无头编译 圆角矩形+半径40 项目成功，生成 `rr=40`/`CreateRoundRectRgn(...,80,80)`；运行时 Win32 探针——GetWindowRgn 复杂区域，PtInRegion (5,5)角在外(半径40切掉)、(20,20)在内、左边中点/中心在内(半径确实生效)；设计器 UI 探针 6 项全过(圆角半径缩进子选项、圆角矩形时可编辑、画布圆角=40px、改10→10px、矩形/椭圆时禁用)；typecheck+151 单测全绿。附注：window-units.json 改动需重启 dev。
- [新增] **窗口属性批2：底图 + 外形（含编译落地全链路）**（用户：窗口属性还缺底图和外形，要做到编译落地）。①window-units.json 窗口单元加 底图(图片型，紧跟底色) + 外形(选择整数：0矩形/1圆角矩形/2椭圆形，紧跟边框)，24 属性，CRLF+哈希同步；②属性面板 EditableImageCell——隐藏 `<input type=file accept=image/*>` + FileReader 读为 base64 data URL 存 properties['底图']，显示缩略图(棋盘格底衬透明)+更换/清除按钮，renderEditableCell 新增'图片'型分支；写回走既有 properties 兜底零改动；③可视化设计器所见即所得——底图=画布 background-image 拉伸铺满(覆盖网格，与编译期 StretchBlt 一致)，外形=新增 .vd-form-body 本体层(包裹标题栏+客户区，调整句柄留在其外不被裁)承载 border-radius(圆角14px/椭圆50%)+overflow:hidden 裁剪，shape 类置于边框类之后同特异性覆盖 border-radius:0；④编译器落地——WindowFileInfo 加 shape/backImage，applyWindowProperties 解析；底图经 decodeImageDataUrl(data URL→文件字节)内嵌为 C unsigned char 数组，WinMain 用 GdiplusStartup+CreateStreamOnHGlobal+Image::FromStream 从内存解码，WM_PAINT 用 Gdiplus::Graphics.DrawImage 高质量插值拉伸铺满客户区(透明区由类背景刷透出)；外形 CreateWindowExW 后按整窗矩形 CreateRoundRectRgn(圆角半径取短边1/8夹取12~60)/CreateEllipticRgn + SetWindowRgn 裁剪；链接加 -lgdiplus，main.cpp 加 #include<gdiplus.h>(编译在 worker 线程无法用 Electron nativeImage，故走运行时 GDI+ 解码)。验证：E2E 无头编译含底图(64x64品红PNG)+外形椭圆项目成功，生成 main.cpp 七项特征全命中；运行时 Win32 探针——客户区中心 GetPixel=0xFF00FF(底图品红渲染)、GetWindowRgn ret=3 且 PtInRegion 中心在内/左上角在外(外形椭圆裁剪)；设计器 UI 探针 11 项全过(面板底图图片单元格+缩略图/外形下拉、画布底图背景类+background-image、本体层 shape-2 椭圆 50% overflow hidden、shape-1 圆角14px、清底图回落、外形0无裁剪)；typecheck+151 单测全绿。附注：主进程改动+window-units.json 需重启 dev 生效。遗留批3：平铺底图、图标、背景音乐、F1帮助、自定义鼠标指针图片等。
- [新增] **窗口属性批1：支持库窗口单元属性补全 + 编译器全链路落地**（用户：照易语言窗口属性表完善窗口属性，属支持库改动）。①lib/krnln/window-units.json 窗口单元从 6 属性补至 22（新增：备注/左边/顶边/标记/可视/禁止/鼠标指针[16 枚举]/标题/底色[颜色型]/可否移动/回车下移焦点/Esc键关闭/在任务条中显示/随意移动/保持标题条激活/窗口类名；保留既有：边框/控制按钮/最大化按钮/最小化按钮/位置/总在最前），CRLF 行尾，libraryManager CORE_LIBRARY_EXPECTED_SHA256 哈希同步；加载合并后面板呈现 24 项（FIXED_COMMON 9 项前置注入宽度/高度，json 同名项覆盖其描述与 pickOptions）；属性面板（Sidebar 窗口单元通用渲染）与写回路由（宽度/高度/标题走 form 固有字段，其余进 form.properties）零改动自动生效。②编译器：WindowFileInfo 扩展 11 字段（left/top/backColor/mousePointer/movable/enterAsTab/escClose/showInTaskbar/dragMove/keepCaptionActive/wndClassName），efw 属性解析提公共函数 applyWindowProperties 消除编辑器内存/磁盘两路重复；生成侧落地：位置=手工且左边/顶边非零时按属性定位（否则维持 CW_USEDEFAULT 兼容老项目）、底色非 0 时 wcex.hbrBackground=CreateSolidBrush(COLORREF)、鼠标指针枚举映射 16 种系统光标（15 自定义型暂按箭头）、自定义窗口类名（escapeCString 转义）、在任务条中显示=假加 WS_EX_TOOLWINDOW（与工具窗边框去重）、可否移动=假在 WndProc 拦 WM_SYSCOMMAND SC_MOVE（标题栏拖动一处拦全断）、随意移动=真且可移动时 WM_NCHITTEST HTCLIENT→HTCAPTION（控件为子窗口不受影响）、保持标题条激活 WM_NCACTIVATE 恒按激活绘制、Esc键关闭与回车下移焦点在消息循环预检（焦点在子控件时按键消息不经主窗口 WndProc；回车对多行编辑框保留换行语义，GetNextDlgTabItem 下移焦点）。备注/标记为设计期数据编译不消费。验证：E2E——scratchpad 拷贝项目设 10 项新属性无头编译，生成 main.cpp 九项特征全命中；运行时 Win32 探针 5 项全过（EnumWindows 按 PID 取窗 GetClassNameW=YcPropTestClass、GetWindowRect=(120,80)、GWL_EXSTYLE 含 WS_EX_TOOLWINDOW、窗口置顶后 GetPixel 客户区=0x0000FF 红、Post VK_ESCAPE 后进程退出）；回归——未设新属性的项目生成物无任何新分支（默认类名/光标/COLOR_BTNFACE 维持）；typecheck 通过，npm test 全绿(151 单测)。遗留批2：图标/底图/背景音乐/外形/F1帮助等资源类属性。**二轮补（设计器所见即所得）**（用户：改无边框等边框样式和底色后可视化设计器没变化，也应该变化）。VisualDesigner 窗体外观接线窗口属性：①「边框」→ resolveFormChromeStyle 派生（0 无边框=不渲染标题栏+虚线轮廓仅作设计期边界提示；4/5 工具窗=22px 窄标题栏/小字/无图标/仅关闭钮；1/3/4 直角，3 对话框粗描边模拟 DLGMODALFRAME），标题栏高度从常量 32 变为按边框样式派生（无边框 0/工具窗 22），布局链 9 处（缩放外壳高/选中框/画布坐标换算/zoom 适配）换用动态值并补 5 个 hook 依赖；②「底色」→ 画布背景（COLORREF 转 rgb；0=默认底色回落原灰，不能进通用颜色读取否则 0 被当黑色）；③「控制按钮/最大化按钮/最小化按钮」→ 标题栏按钮组按 Win32 语义显隐（控制按钮=假整组消失；最小/最大都假只剩关闭钮；只关一个则该钮灰显 vd-form-btn-off）。无边框时窗体选中/双击跳事件交互不受损（画布空白点击本就选中窗体、画布双击本就挂事件跳转）。属性面板改动经 updateFormProperty 写回 tab.formData 实时反映画布。验证：Playwright UI 级 8 项断言全过（无边框标题栏消失/border-0 类、工具窗窄标题栏+仅关闭钮、底色 255 画布 rgb(255,0,0)、最小化=假灰显、控制按钮=假按钮组消失）；151 单测全绿；typecheck 通过。坑：测试项目 .ycide-session.json 会残留先前项目的 efw 路径（切项目时旧标签被记入新项目会话），恢复的设计器是别的项目窗体导致断言全错——验证脚本每轮显式写会话文件直达目标 efw。

- [修复] **切换主题后局部残留上一主题配色（重启才恢复）+ 默认深色「仅部分生效」警告**（用户：从樱花粉-渐变切回默认深色后界面有问题、重启 dev 就好）。根因：主题应用把 theme.colors 逐键 setProperty 到根节点，但**只设不清**——从 59 键主题切到 31 键主题时，多出的 28 个界面变量（--accent/--panel-bg/--bg-input 等）残留上一主题的值；重启后从头加载才正常。修复：①applyTheme 记账式清除——appliedThemeColorKeysRef 记录上次写入的键集，切换时先 removeProperty 新主题没有的旧键再写入（根节点上的 IDE 设置变量/菜单坐标等非主题变量不受影响）；②默认深色.json 补全 28 个界面键至 59 键（默认浅色已全），消除「仅部分生效，缺失 --bg-input/--border-color/--accent」警告。附注：控制台 libpng iCCP cHRM 警告是 PNG 图标色彩配置块的无害提示，与主题无关。**连带修默认浅色**（用户点名检查）：①--table-cell-text 又回浅灰 #d4d4d4（白底不可见，先前修正被运行中 IDE 的写回覆盖）→ #1f2328；②语法八色沿用深色主题值（米黄函数/浅灰运算符/浅蓝变量在白底几乎不可见，本轮语法 token 真正接通后显形）→ 换 VSCode Light 系（keyword #0000ff/string #a31515/number #098658/comment #008000/function #795e26/type #267f99/variable #001080/operator #383838），默认浅色-渐变 同步并按新主色重设语法渐变第二色（向深一档）；非内置主题 payload 优先于 json，连带清 userData 中 默认浅色-渐变 的 payload 让改动回流。**三轮补（覆写元凶）**：直接改 themes/默认深色.json、默认浅色.json 是徒劳——主进程启动对内置主题做基准校验（isBuiltinThemeDefinitionValid 按令牌组全键比较 createBuiltinThemeDefinition 硬编码期望），不符即备份 .corrupted.bak 并重写回硬编码（themes/ 里的 corrupted 备份、--table-cell-text 反复回退的真相）。修硬编码基准（src/main/index.ts createBuiltinThemeDefinition）：①深色基准补 28 个界面键（darkUiExtras：菜单/面板/弹窗/按钮/强调色等，与深色系一致）；②浅色基准补 VSCode Light 语法八色与 --table-cell-text 浅色值——现有 json 值恰与新基准一致，启动校验通过不再重写。主进程改动需重启 dev 生效。验证：typecheck 通过，npm test 全绿(151 单测)。
- [新增] **内置八套配色主题（三色系双版本 + 默认深浅渐变版）**（用户：来三套主题——樱花粉/抹茶绿/蓝色系，每套无渐变+渐变两种；默认深色浅色也各做渐变版）。新增 themes/*.json 八套：樱花粉、樱花粉-渐变、抹茶绿、抹茶绿-渐变、深海蓝、深海蓝-渐变、默认深色-渐变、默认浅色-渐变——渐变版经主题文件新增的 gradients 字段声明（base 组标题栏/状态栏 + 表格与表头组 表头/行选中/行悬浮 水平渐变）；配套：ThemeDefinition.gradients?（shared）+ applyTheme 兜底（该主题尚无用户渐变配置时用主题文件自带的作初始值，用户改过则以其为准）；三套新配色为 31 个 token 全量设计（浅粉/浅绿为浅色系语法配色、深海蓝为深色系）。连带修复：默认浅色.json 的 --table-cell-text 误用深色主题浅灰值（浅底浅字），改 #1f2328。**二轮补**（用户复测：抹茶绿-渐变很多区域还是黑的，输出面板报「仅部分生效，缺失 --bg-input/--border-color/--accent」）：首版只给了 31 个主题令牌，完整主题是 58+ 键——菜单/面板/弹窗/按钮/悬浮/强调色等 27 个界面级变量缺失时残留深色缺省。八套主题全部补至 59 键（樱花粉/抹茶绿浅色系与深海蓝深色系各按色系设计 27 个界面键：bg-hover/active/selection/input、border 族、accent 三态、error/warning/success/info、panel/dialog/menu、按钮次级三态、状态栏悬浮、activity-icon-filter 等；默认深浅渐变版直接取各自全键版），REQUIRED_THEME_COLOR_KEYS 校验全过。**三轮补**（用户复测：渐变效果不明显）：首版第二色取同系近似色肉眼难辨——五套渐变主题的 secondColors 全部拉大对比（如樱花粉标题栏 深玫粉#e87ba0→浅粉、深海蓝标题栏 亮蓝#2f6ba8→深蓝、状态栏均跨明度两档），语法组按用户要求同步开启——五套渐变主题的语法八色各配第二色（同色相亮/深一档：深色系文字向亮渐变、浅色系向深渐变保证可读性），文字渐变经既有 background-clip:text 通道生效；连带清除 userData/theme-config.json 中已用主题的旧渐变 payload（主题文件 gradients 兜底仅在 payload 无渐变时生效，不清则 json 更新不回流）。验证：typecheck 通过，npm test 全绿(151 单测)。
- [新增] **主题分组渐变**（用户：主题管理器每个分组加「是否渐变」，勾选后每行主色左边多一个第二色配置、两色组成渐变，渐变类型下拉只在启用时可见）。实现：①shared——ThemeGroupGradient{enabled/type/secondColors}（类型：水平/垂直/对角/径向），ThemeTokenPayload.gradients?（旧主题缺省空=全关，normalizeThemeGradients 归一，导入导出/保存/草稿链自然携带）；②应用层——applyThemeGradientsToRoot 为每 token 生成派生变量 `<token>-gradient`（渐变 CSS）与 `<token>-fill`（transparent，供文字 clip），关闭即移除回落纯色；主色改动经草稿链自动重算渐变端色；重置本组连带关渐变；③主题管理器 UI——组头「☑渐变 + 类型下拉」（流程线组除外），行内启用时主色左侧显示第二色 picker（缺省回落主色）；④CSS 接线——背景类：标题栏/状态栏/编辑器背景/表头背景/行选中(15处)/行悬浮(5处) 均 `var(--x-gradient, 原纯色链)`；文字类：语法高亮十个着色类 + 代码行变量 经 background-clip:text + -webkit-text-fill-color 双变量方案（未启用时 background:none + currentColor 零成本）。范围说明：主/次/弱背景 token 消费面全域分散，本轮未接线（勾选渐变时这三项暂无视觉效果）；表格边框/图标类不适用渐变。验证：typecheck 通过，npm test 全绿(151 单测+47 契约)。
- [新增] **主题「表格与表头」组新增「表格文本」配置项**（用户：表格里的文本用独立配置色）。theme-tokens.ts 组内加 table-cell-text（--table-cell-text，主题管理器面板表驱动自动出现），theme.ts 缺省 #d4d4d4；表格内容文本三处接线（程序集/子程序/DLL命令名 eProcolor、声明表格名称列 .eyc-decl-table .Variablescolor、数组列 eArraycolor）→ var(--table-cell-text, 回落 --table-text)——旧主题未定义该项时平滑回落编辑器文本色。类型列/备注列/勾选标记保持各自语义色。验证：typecheck 通过，npm test 全绿(151 单测)。
- [修复] **编辑器变量与命令同色 + 主题「语法高亮」部分色项不生效**（用户：变量跟命令一个颜色要区分；主题管理器里改色感觉没作用）。根因：主题 token 体系（--syntax-keyword/string/number/comment/function/type/variable/operator 八项）齐全且已注入 :root，但表格编辑器多个着色类没接对应 token——变量名类 Variablescolor 用的是普通文本色 --table-text（面板改「变量」无感）、标点 eyc-punct 挂在 --syntax-keyword 上（改「运算符」无感）、数字字面量与变量引用（cls 空）根本没有专色类。修复：①Variablescolor → --syntax-variable、eyc-punct → --syntax-operator（回退 keyword）、cometwolr（对象.方法）→ --syntax-function（函数族）；②新增 eyc-numcolor → --syntax-number；③渲染侧对 cls 空的干净标识符：已知变量引用上 Variablescolor、纯数字字面量上 eyc-numcolor——主题面板八个语法色项现在全部实际生效，变量（浅蓝缺省）与命令（米黄缺省）一眼可辨。**二轮补**（用户复测）：①格式化后的赋值行 `变量 ＝ …` 赋值目标仍是函数色——colorize 的赋值目标判定只看紧邻 span，标识符与 ＝ 之间的空格 span 挡住判定、落到无括号命令分支染成 funccolor（老缺陷，此前变量/函数色近似未暴露）——判定改为跳过空白 span 找下一个非空白 span；②声明表格内的名称列按用户要求回**主文本色**——变量名列（Variablescolor，.eyc-decl-table 内覆盖）与程序集名/子程序名/DLL命令名列（eProcolor，原挂 --syntax-keyword 导致改「关键字」时表格跟着变色，该类五个使用点全在表格、直接改主文本色），语法色只作用于代码行。验证：typecheck 通过，npm test 全绿(151 单测+47 契约)。
- [新增] **悬停命令弹说明窗（照易语言）**（用户：鼠标悬停到命令上弹出当前命令的说明窗）。实现（EycTableEditor）：代码行里的命令 span（funccolor 支持库/DLL 命令、cometwolr 成员命令；用户子程序与未知命令不弹）onMouseEnter 420ms 后在命令下方弹出米黄浮动说明窗——`<返回类型> 命令名 （参数类型 参数名，［可选参数］） - 所属支持库` + 「解释：描述」，数据本地取自命令目录（allCommandsRef/dllCompletionItemsRef/memberCommandsRef，无 IPC）；成员命令按尾段名查成员目录；右缘防溢出回收；移开/滚动即消隐（pointer-events: none 不挡编辑）。样式照易语言系统 tooltip（米黄底黑字、返回类型蓝色、库名棕色）。验证：typecheck 通过，npm test 全绿(151 单测+47 契约)。
- [优化] **调试输出 文本值带全角引号（照易语言类型标记）**（用户对照：易语言 `调试输出("1")` 显示 `* “1”`、`调试输出(1)` 显示 `* 1`，我们两者都裸显示）。实现：生成侧预置 `yc_dbg_fmt` C++ 重载族——文本（wchar_t*）包全角引号、数值族裸值、数组/字节集/大整数转发既有类型头打印，**按实参静态类型自动分发**（文本变量/文本命令返回值/字面量全覆盖，转译器零类型推断）；spec_Trace 生成分支所有参数（含单参）统一经 yc_dbg_fmt，数组参数仍按转译期元素类别先选专用打印（f64/text 位模式运行时不可辨）。TRANSPILE_CACHE_VERSION 15→16。验证：真机运行九场景——文本字面量/文本变量/到文本(5) 均 “…”、数值裸、数组字节集类型头、多参混合 `* “甲”  2  “内容甲”`，全部与易语言一致；npm test 全绿(151 单测+47 契约)。
- [优化] **调试输出 格式逐字符对齐易语言**（用户对照：易语言输出 `* 数组:4{1,2,3,4}`/`* 数组:2{“文本1”,“文本2”}`/`* 字节集:3{1,2,3}`，我们只有 `{1, 2, 3, 4}`）。实现：①spec_Trace（lib/spec/impl/windows.cpp）每行文本前自动加「* 」前缀（krnln 文档明载「该文本之前被自动加上一个星号」，stdout 与 OutputDebugString 通道都加）；②四个打印 helper 对齐——整数/浮点数组 `数组:成员数{e,e}`、文本数组 `数组:成员数{“e”,“e”}`（元素带全角引号）、字节集 `字节集:长度{e,e}`，元素逗号后不留空格。TRANSPILE_CACHE_VERSION 14→15。验证：用户对照代码真机运行输出与易语言逐字符一致（四行全对）；npm test 全绿(151 单测+47 契约)。
- [新增] **文本型数组 + 字节集字面量**（用户对照易语言：`.局部变量 变量, 文本型, , "0"` + `变量 ＝ { “文本1”, “文本2” }` 编译报「暂不支持 文本型 数组」；`变量3(字节集) ＝ { 1, 2, 3 }` 是易语言字节集常量语法）。实现（compiler.ts）：①数组元素存储类别三态化（int 直存 / f64 双精度位模式 / text 堆拷贝文本指针位模式），文本型数组进统一 vector<long long> 运行时——声明放行、下标读还原指针 `(wchar_t*)(intptr_t)yc_ary_at(…)`、下标写与 加入成员/插入成员 值参经 yc_value_to_text 堆拷贝后存指针（拷贝语义）、字面量 yc_ary_lit_text（元素引号开头自动推断 text）、打印 yc_ary_to_text_str（{文本1, 文本2} 格式）、数组排序 对非整数族统一拦截；②字节集字面量赋值——`字节集变量 ＝ { 1, 2, 3 }` 按元素构造 YC_BIN{…}，新增 yc_value_to_text(YC_BIN) 重载（{1, 2, 3} 格式，调试输出 字节集 直接可读）。TRANSPILE_CACHE_VERSION 13→14。验证：用户对照代码全编译+真机运行——文本数组打印 {文本1, 文本2}/整数数组 {1, 2}/字节集 {1, 2, 3}/文本下标读 变量[2]=文本2/加入成员 “文本3” 后 取数组成员数=3 且 变量[3]=文本3，全部与易语言一致；npm test 全绿(151 单测+47 契约)。
- [优化] **赋值右值数组字面量格式化（照易语言）**（用户对比：编辑器落盘 `变量 ＝ {“文本1”,“文本2”}`、易语言为 `变量 ＝ { “文本1”, “文本2” }`）。formatOps（提交时运算符格式化，字符串已占位符保护）追加间距规范：全角｛｝统一半角、{ 后与 } 前留空格、逗号统一为「, 」（连带参数区 `调试输出 (1,2)` → `(1, 2)` 与易语言一致）；字符串字面量内的花括号/逗号不受影响。验证：新增 3 条单测（赋值右值文本/数字字面量、字符串内不动、参数区逗号），npm test 全绿(151 单测+47 契约)。
- [修复] **展开参数行对数组字面量 { 1, 2, 3 } 撕裂显示/写回损坏**（用户截图：`调试输出 ({ 1, 2, 3 })` 展开显示「欲输出值: { 1」）。编辑器侧四处切分/配平不认花括号（与编译器侧上一条同族问题）：parseCallArgs（展开参数值提取）与 replaceCallArg（参数编辑写回定位）花括号计深度——{ } 内逗号不再当参数分隔符；isArgParensBalanced（实时写回守卫）与 balanceArgParens（提交配平）纳入花括号——`{ 1` 中间态不实时写回（防级联损坏，与此前圆括号同款守卫）、提交时自动补 }。验证：新增 3 条单测（切分单参/混合参、替换不撕裂、配平判定与补全），npm test 全绿(148 单测+47 契约)。
- [新增] **数组字面量 { 1, 2, 3 } 全链路 + 调试输出增强**（用户：`调试输出 ({ 1, 2, 3 })` 生成的 C++ 语法坏掉编译失败——参数切分把花括号内的逗号当参数分隔符撕碎）。实现（compiler.ts）：①splitArguments 与诊断侧 splitTopLevelArgs 花括号计深度（{ } 内逗号不切）；②表达式层数组字面量转译——`{ e1, e2, … }` → 构造临时 vector 的 yc_ary_lit(…)（任一元素含小数点 → yc_ary_lit_f64 按 double 位模式存），配平判定引号感知；③数组变量整体赋值字面量 `数组 ＝ { 7, 8, 9 }`（元素存储形态按数组声明的元素类型定，非按字面量推断）；④yc_value_to_text 增加 vector 重载（{1, 2, 3} 格式）+ yc_ary_to_text_f64 浮点数组打印（%.15g），通用型参数对浮点族数组变量/浮点字面量自动选用——`调试输出 (数组变量)` 直接打印数组内容；⑤调试输出 多参修复——spec_Trace 是单参 ABI，多值拼成一行（值间双空格）单次输出（此前生成两参调用编译错）；⑥**连带修复编译前签名校验误炸**：校验阶段探测「命令有无后端路径」时协议解析器会真的生成代码（含参数转译），彼时数组变量集合未填充 → 数组命令实参转换抛错逃出成「编译异常」——校验期试生成包 try/catch，抛错视为「有路径」，真实转译错误留给主转译阶段带上下文再报。TRANSPILE_CACHE_VERSION 12→13。验证：无头编译+真机运行——`{1, 2, 3}`/`{1.5, 2.5}`（浮点正确显示）/字面量赋值后打印 `{7, 8, 9}`/`取数组成员数+下标 多参一行 3  8` 全部正确；npm test 全绿(145 单测+47 契约)。
- [修复] **注释行删 ' 后的空格时光标跳到行末尾 / 空格删不掉**（用户：`' 调试输出()` 光标在「调」左侧按退格，空格没删掉、光标只是左移或跳行尾）。根因：输入归一化会给注释前缀强制补空格（'调 → ' 调），编辑期间删除该空格立即被还原——归一化结果与编辑前 state 相同，setEditVal 同值不触发受控重渲染，随后 liveUpdate 回流把 DOM 强制同步时光标被重置到行尾。修复（EycTableEditor.tsx）：**编辑期间不再强补空格**——注释空格归一（'内容 → ' 内容）从 live 输入归一化（normalizeCodeLineInput）挪到编辑结束落盘（formatCommandLine 注释行分支），删除那个空格就是真删除（用户确认的期望语义）；连带保留「归一化完全还原用户修改」场景的 DOM 直写防御分支（对其它潜在还原改写仍有效）。typecheck 通过，npm test 全绿(145 单测+47 契约)。
- [修复] **输出调试文本 无任何输出**（用户：`输出调试文本 (“123”)` 没反应）。根因：krnln impl 里 `krnln_OutputDebugText` 是空桩（`(...) { touchNonStub(); }`，varargs 什么都不做）。修复（lib/krnln/impl/windows.cpp）：实现为 **写 stdout（fputs+换行+fflush）** —— IDE 运行程序经 execFile 管道捕获子进程 stdout 流式转发输出面板（info 行），即时可见；同时经 utf8ToWide + OutputDebugStringW 发系统调试器通道（DebugView 可见）。CORE_LIBRARY_EXPECTED_SHA256 同步更新 windows.cpp 哈希；产物缓存指纹含 platformImplStamps（impl 内容哈希）会自动失配重链接，无需 bump 转译缓存。验证：拷贝项目写 `输出调试文本 (“123”)`+中文各一行 → 无头编译 success → PowerShell 起 exe 重定向 stdout 捕获到 `123` 与 `中文调试内容`（UTF-8 中文正确）；npm test 全绿(145 单测)。
- [优化] **局部变量表与子程序表/参数表之间留 4px 间距**（用户：两表贴死看不出分组；首版只加块级 margin 无效——局部变量段在 eycBlocks 里是并入子程序表块的第二段表头，同一 <table> 内 margin 落不进去）。实现：①eycBlocks 把 .局部变量 段**拆成独立 localVar 表格块**（原 he=1/11 时 addHdr 并入 sub 块 → 一律 flush+newTbl；折叠范围计算本就按「下一个非 localVar 表格块」预期此结构，勾选格判定按行类型不受表归属影响）；②表格块容器按 tableType===localVar 加 eyc-localvar-block 类 + margin-top: 4px。行号区在块容器内随块整体下移不错位（表内 border 方案会因 border-collapse 取大规则致 gutter 与表行增量差 1px 且吃掉上表底线，故弃用）；块级虚拟滚动 spacer 本为近似估算，4px 在误差容忍内。**二轮补**（用户复测：两表之间多出一行无行号的可输入行）：拆块副作用——「sub 表块后不是代码行则插虚拟输入行」规则把 sub 与 localVar 块之间当成了空子程序间隙；虚拟行规则改为「声明区（sub/localVar 表）之后既非代码行也非 localVar 表时才插」，子程序表与局部变量表之间是声明区内部不插，空子程序（只有声明+变量）虚拟行落在变量表之后与原行为一致。typecheck 通过，npm test 全绿(145 单测+47 契约)。

### v0.0.4-beta.9（2026-07-09）

- [新增] **数组支持扩展：浮点族元素 + 多维数组 + 链式下标**（用户：`.局部变量 矩阵A, 双精度小数型, , "100,100"` 编译报「暂不支持 双精度小数型 数组」——性能测试用到 100×100 双精度矩阵乘法）。实现（compiler.ts）：①数组跟踪 Set→Map（名 → 元素类型+各维尺寸）；②浮点族（小数型/双精度小数型）元素按 double 位模式存进统一的 vector<long long>（预置 yc_f64_bits/yc_f64_from_bits，memcpy 位转换）——下标读包 from_bits、下标赋值包 bits((double)(…))、加入成员/插入成员 的元素值参同款转换；浮点数组的 数组排序 转译期拦截（位模式排序对负数错序，待 impl 加比较器版）；③多维数组 "100,100" 行主序扁平存储（总量预填 0），链式下标 `矩阵A [i] [j]` 折算一基线性下标 ((i)-1)*100+(j)——表达式改写与下标赋值语句两侧都支持（matchBracketGroup 配平+组数校验，组数≠维数转译报错）；④**splitDeclParts 不引号感知导致 "100,100" 被切碎**（parts[3]="100）——新增 splitDeclPartsQuoted（引号感知）用于 局部变量/程序集变量/参数 三处声明解析；⑤数组参数维度未知按动态一维（多维传参在被调方按线性一基访问）。TRANSPILE_CACHE_VERSION 11→12。验证：无头编译用户项目 success=true；生成 cpp 抽查（矩阵C(10000,0)/((i)-1)*100+(j) 线性折算/乘累加三读一写位转换全对）；**真机闭环**（起 exe+BM_CLICK+WM_GETTEXT）跑通六项性能测试且校验值全部数学正确——整数累加 5000000050000000=Σ(1..1e8)、浮点校验 22026.4549≈e^10、素数 78498 个（1~100万真值）、矩阵乘法 31ms；npm test 全绿(145 单测+47 契约)。
- [修复] **真/假 后紧跟半角逗号/右括号时误报「未定义变量」**（用户：`重定义数组 (排序数组, 假, 数组大小)`、`数组排序 (排序数组, 真)` 报 未定义变量"假"/"真"）。根因：着色器（editorCoreUtils colorExpr）逻辑字面量 真/假 的词边界 lookahead 集合只有 空白/半角左括号/全角括号逗号/等号——**半角 , 和 ) 不在集合里** → `真)`、`假,` 不被认成常量字面量、落到普通 token（cls=''）→ 诊断的干净标识符检查按未定义变量上报；且/或 的边界集合同样漏半角括号（`或)` 会报未知命令）。修复：两处词边界统一改为「后面不是标识符字符」（$| [^一-龥A-Za-z0-9_]），任何半/全角标点都算边界；防御兜底——真/假 并入 EycTableEditor reservedNameSet（易语言保留字，变量名非法判定/插入变量提示同步豁免）。此前单测未暴露：spec 的 run() 帮助函数把 真/假 预置进了 validCommandNames/reservedNames，掩盖了组件真实集合缺失。验证：新增 2 条单测（用户原文两行用不含真/假豁免的保守集合直测不报/半角括号紧跟的 或 不报未知命令且真未知照报），typecheck 通过，npm test 全绿(145 单测+47 契约)。
- [新增] **行多选状态下粘贴 = 覆盖粘贴**（用户：多选选到了子程序时粘贴应覆盖——文档和剪贴板都有 _子程序1_被单击、多选里含这个子程序 → 粘贴覆盖它；完整选中=完整覆盖；选到一半、下面留有未选中的普通代码 → 粘贴内容顶上、残留代码自然归入该子程序（追加语义）；不多选粘贴维持现行重名改名 原名_1/_2 方案）。实现：buildMultiLinePasteResult 新增 replaceLineIndices 参数——有选区时先从文档剔除选中行（.程序集 声明行受保护不删），后续全部判定（重名改名/局部变量归位/插入点）基于剔除后的文本：插入点固定为选区首行原位（不走子程序边界对齐，保证残留尾部能接上粘贴的同名子程序）；重名判定天然实现覆盖语义——选区覆盖到声明行的同名子程序在剔除后文档中已不存在故保留原名，未被选中的同名子程序仍照改名；选区里剪贴板没有对应物的子程序（如完整选中 被单击+被双击 两个、剪贴板只有 被单击）随选区剔除一并删除（用户确认的期望语义，契约用例锁定）。三个粘贴入口（Ctrl+V window 监听/applyCustomPasteShortcut/右键菜单 pasteFromClipboardAtContext）经 selectedLinesRef 统一传入最新选区（ref 避免选区塞进回调依赖）。验证：新增 3 条契约用例（完整选中同名子程序覆盖不改名不残留旧代码/部分选中留尾时尾行归入覆盖后子程序且声明唯一/选区在别处时重名照旧改 _1/完整选中双子程序而剪贴板只有其一时另一个被删除），typecheck 通过，npm test 全绿(143 单测+47 契约)。
- [修复] **且/或 组合条件转译无运算符优先级 → 快速排序死循环（点按钮未响应）**（用户：算法测试挪到按钮事件后点击即未响应）。根因：translateExpressionToC 的表达式切分只有 比较→加减→乘除 三层、没有逻辑运算层，`i ＜ j 且 数组 [j] ≥ 基准` 中 且 先被替换成 &&，随后比较切分在第一个 ＜ 处开切 → 生成 `while (i < (j && yc_ary_at(数组, j) >= 基准))`——i≥1 时右侧恒为 0/1、条件恒假 → 内层循环体永不执行、i/j 不再变化 → 外层 while (i < j) 死循环挂死 UI 线程。此缺陷影响一切「比较 且/或 比较」组合条件（如果/判断循环 都会生成错误语义），此前未爆是因为组合条件+循环变量自增依赖的场景才可见。修复：新增 findTopLevelLogical（引号感知+括号深度 0，先切 ||（优先级最低）再切 &&，同级取最右切分点保持左结合），置于比较切分之前，两侧递归转译 → `((i < j) && (yc_ary_at(数组, j) >= 基准))`。TRANSPILE_CACHE_VERSION 10→11。验证：无头编译用户项目 success=true、生成 while 条件抽查正确；**真机闭环**——PowerShell 起 exe、EnumWindows 按 PID 找窗、PostMessage BM_CLICK 点按钮1、WM_GETTEXT 读编辑框：冒泡/选择/快速排序+线性/二分搜索+字符串拼接全部跑完出结果（冒泡 16 毫秒，其余在 GetTickCount 精度内归 0），进程无卡死；npm test 全绿(143 单测+43 契约)。
- [修复] **支持库常量（#换行符 等）编译失败：常量清单根本没被装载**（用户：`＋ #换行符` 编译报 `use of undeclared identifier '换行符'`）。三层问题：①libraryManager 元数据候选文件列表没有 `*.constants.json`——krnln.constants.json（183 个常量）从未进 getLibInfo，生成 cpp 里「支持库常量定义」#define 块整个缺失（顺带支持裸数组形状的常量文件）；②清单常量名带 # 前缀（"#备注型"），直接进 #define 是非法宏名——collectLibraryConstants 剥 #（与表达式引用侧 replaceConstantRefs 的剥 # 对齐）；③krnln.constants.json 缺文本常量——补 #换行符(CRLF)/#回车符(CR)/#制表符(TAB)/#引号 四个（type=text），并修 collectLibraryConstants 对 text 型值不 trim（否则 "
- [新增] **编译器数组支持（动态数组全链路）**（用户：算法性能测试程序编译报 `subscripted value is not an array` ×N + `数组 [j] ＝ 数组 [j ＋ 1]()` 乱码发射——转译器完全没有数组概念，而 krnln impl 早有完整的 std::vector<long long> 数组运行时 krnln_AddElement/GetAryElementCount/ReDim 等）。实现（compiler.ts）：①声明——.局部变量/.程序集变量 第 4 字段（数组尺寸，"0"=动态）→ `std::vector<long long>`（定长预填 0），非整数族元素（文本/小数等）转译期报「暂不支持 X 数组」；②参数——.参数 带「数组」标志 → `std::vector<long long>&`（签名/类方法/跨文件前向声明三处；标志判定改 parts.slice(2) 防参数名恰叫「数组」误判，isByRef 同修）；③下标——一基下标经模块级 currentTranspileArrayVars 追踪改写为 `yc_ary_at(名称, 下标)` 引用形式（预置 static inline helper，越界回落哑元不崩溃；括号形式兼容既有按括号感知的表达式切分，方括号会被加/乘法切分撕裂故不可留）；改写引号感知+配平方括号+嵌套内层先转，钩在 translateExpressionToC 入口；④下标赋值——`数组 [i] ＝ 右值` 语句分支 → `yc_ary_at(...) = (long long)(右值)`；⑤数组命令 ABI——加入成员/取数组成员数/清除数组/重定义数组/删除成员/插入成员/取数组下标/复制数组/数组排序 按符号显式给定 (void*, long long/int) 形参表（清单里是通用型，原通用映射把实参转文本指针与 impl 错位；数组实参传 (void*)&名称，非数组变量转译期报错），加入成员多值展开为逐次调用；⑥＼ 整除补进 convertFullWidthOps（整数下 C / 即截断除法）。TRANSPILE_CACHE_VERSION 9→10。
- [新增] **编译前全项目错误拦截（未打开的文件也检查，输出面板按文件报错误）**（用户：打开项目但没打开文件、文件里有错误，点运行/编译也要拦截，并在输出面板报哪个文件有错——既有拦截只看当前打开文件的问题面板 fileProblems，未打开文件的错误漏放行到编译器才炸）。实现：①新增 projectDiagnosticsSweep.ts `sweepFileDiagnostics`——对单个源文件构造与 EycTableEditor 逐项对齐的诊断上下文（validCommandNames=目录+DLL+本文件子程序/DLL声明+变量+FLOW_KW+且或别名、allKnownVarNames=本文件变量+项目全局变量+关联窗口控件、reservedNames、commandSignatures、controlPropTypes 折算），调 buildEditorDiagnosticsProblems，保证「打开该文件看到的问题」=「编译拦截报的问题」；②Editor 句柄新增 `sweepProjectDiagnostics()`——readDir 找 .epp → parseEpp 拿项目文件清单（EYC/EGV/ECS/EDT/ELL），打开的文件用标签页最新内容（含未保存）、未打开读盘，efw 表单同理（打开用 formData、未打开 JSON 解析），目录经 getAllCommands+buildCompletionCatalog、全局变量/项目 DLL 命令用 Editor 既有 state；失败兜底返回空（宁可放行给编译器报错，不误拦）；③App 两个编译入口（handleCompile/handleCompileRun）在既有问题面板拦截之后、真正编译之前 `await sweepAndReportProjectErrors()`——有错误则输出面板（编译页签）报「编译已取消：N 个文件存在 M 个错误」+ 按文件列出每条「第 L 行: 消息」（上限 50 条，超出提示打开对应文件看问题面板）。**连带修复**：菜单「编译→普通编译」与 Ctrl+F7 的 build:compile 动作在 handleMenuAction 里根本没有 case（点了无反应的既有 bug，E2E 时暴露）——补上映射到 handleCompile。验证：新增 4 条单测（未知命令扫出/干净文件与全局变量+且或不误报/控件属性类型检查随窗口上下文生效/本文档 DLL 声明不误报+括号不配平照报）；Playwright 双路 E2E——拷贝项目注入错误到未打开的 窗口1.eyc → Ctrl+F7 输出面板报「编译已取消：1 个文件存在 1 个错误 / 窗口1.eyc（1 个错误）: 第 8 行: 未知命令…」拦截成功；干净项目 → 编译成功不误拦（期间顺带自证：未打开 efw 的输入方式=输入整数 + 到文本(n) 也被扫出，控件属性类型检查在 sweep 全链路生效）；typecheck 通过，npm test 全绿(143 单测+43 契约)。
- [修复] **逻辑运算符 且/或 误报「未知命令」**（用户：`.判断循环首 (i ＜ j 且 数组 [j] ≥ 基准)` 问题面板报 未知命令"且"，但核心支持库明明有「并且」命令）。根因：着色器（editorCoreUtils colorExpr）故意把 且/或 着成 funccolor（命令色，因为它们是「并且/或者」命令的运算符别名，照易语言），但诊断的 funccolor 检查按命令目录查名——目录里只有「并且」「或者」本名，没有别名 且/或 → 误报。修复：editorCoreUtils 导出 `LOGIC_OPERATOR_ALIASES`（且/或，语言级运算符别名——编译器直接转 &&/||，与命令目录无关）；诊断 funccolor 检查豁免该集合（editorDiagnosticsShared，面板）；EycTableEditor validCommandNames 集合并入别名（行内红标同步豁免）。验证：新增 2 条单测（用户场景 且 不报/或 豁免且真未知命令仍照报），typecheck 通过，npm test 全绿(139 单测+43 契约)。
- [新增] **控件属性赋值的数据类型检查（内容 随 输入方式 折算，照易语言）**（用户：设计器把编辑框1「输入方式」设为「输入整数」后，`编辑框1.内容 ＝ 到文本(n)` 仍不报错——此时 内容 实为整数型，应报数据类型问题，`编辑框1.内容 ＝ n` 才合法；同理别的控件也要验证）。实现：①editorDiagnosticsShared 新增 `buildControlPropTypes(controls, units)`——由关联窗口控件实例+支持库窗口单元定义折算「控件名 → 属性名 → 类型名」表，单元带「输入方式」属性时（编辑框族）「内容」类型随实例值折算（输入字节/短整数/整数/长整数/小数/双精度小数/日期时间 → 对应类型，通常/只读/密码/整数文本/小数文本 → 保持文本型，实例未存值用单元 defaultValue）；②诊断请求新增 controlPropTypes 字段（hook+worker 透传），第二循环对 `控件.属性 ＝ 右值` 做族级匹配——属性类型查表、右值经既有 inferArgTypeFamily 推断（字面量/变量/嵌套命令返回类型），族不同报「属性"编辑框1.内容"类型不匹配：需要 整数型，当前为 文本型」（与命令参数检查同文案风格），右值无法推断保守跳过；通用属性一并生效（如 `编辑框1.宽度 ＝ "abc"` 报需要整数型）；③数据链路：Editor.tsx activeWindowControls 随附控件实例 properties（输入方式值）→ EycTableEditor windowControlTypes prop 类型扩展 → diagControlPropTypes memo（windowControlTypes+windowUnits）进诊断请求。验证：新增 6 条单测（折算表 2 条：输入整数→整数型/缺省与文本类方式→文本型+未知单元不入表；诊断 4 条：用户场景报错/整数右值不报/缺省方式反向报错与到文本不报/宽度赋文本报+右值不可推断保守跳过），typecheck 通过，npm test 全绿(137 单测+43 契约)。
- [修复] **控件文本属性赋值右值为支持库命令时编译失败**（用户：`编辑框1.内容 ＝ 到文本(n)`，编辑框1 已放置在窗口上，编译报 `use of undeclared identifier '编辑框1'`）。根因：转译器赋值分支里只有 exprGenerator 命令走了「控件文本属性 → yc_set_control_text」通道，右值是 **ycmd 原生命令**（到文本→krnln_str 等）或**通用支持库命令**（yc_invoke_support_cmd）时落到通用赋值发射 `{ 编辑框1.内容 = 表达式; }`——把控件属性当 C++ 左值原样发进 cpp。修复（compiler.ts）：赋值分支在原生/通用命令发射前补 propMatch+isTextProp 分流——原生命令取其表达式形态、通用命令新增 generateYcGenericCommandTextExpr（把 yc_invoke_support_cmd 调用包成返回 wchar_t* 的 lambda，非文本返回值经 yc_value_to_text 转文本），统一发射 `yc_set_control_text(L"控件名", 表达式)`；非文本表达式自动 yc_value_to_text（与既有分支一致）。TRANSPILE_CACHE_VERSION 8→9 强制重转译。验证：typecheck 通过；Playwright 无头编译用户实际项目（windows窗口程序-新项目1）success=true，生成行为 `yc_set_control_text(L"编辑框1", ([&]() -> wchar_t* { ... krnln_str ... })())`，全文无裸左值发射。
- [新增] **行内括号配平诊断**（用户：`编辑框1.内容 ＝ 到文本(n)))` 多两个右括号问题面板不报——参数类型检查只扫描到自己找到的配对右括号为止，多余的尾随右括号谁都不管）。诊断第二循环（不依赖命令目录）对每个代码行做括号配平：maskQuotedText 掩掉字符串内容、按引号感知规则截断行尾 ' 注释段（注释里的括号不计，与 eycBlocks/editorCoreUtils 同规则 i>0），再扫半/全角括号 `(（)）` 配栈——右括号多 → 报「括号不匹配：多余 N 个右括号」（列=首个多余右括号处），左括号多 → 报「括号不匹配：缺少 N 个右括号」（列=首个未配对左括号处）；两者可同时报（如 `)(`）。易语言语句单行成立故按行配平合法，流程行（.如果/.循环尾 等）天然配平不受影响。验证：新增 3 条单测（用户场景多余 2 个右括号/缺少 1 个右括号/字符串内与行尾注释里的括号不误报），typecheck 通过，npm test 全绿(131 单测+43 契约)。
- [新增] **字符串开头的裸语句报无效语句**（用户：整行只写 `"编辑框1.内容"` 问题面板不报——引号内容被 maskQuotedText 掩掉后什么检查都够不到）。与"数字开头裸语句"同一检查点扩展：非赋值代码行以引号（半/全角）开头 → 报「无效语句"…"：字符串不能单独作为语句」（超长字面量截断 24 字符）；字符串作参数/赋值右值不受影响。验证：新增 2 条单测（半/全角引号裸行报、参数与右值不误报），npm test 全绿(128 单测+43 契约)。
- [新增] **成员引用的对象存在性诊断**（用户：设计器没放置编辑框，代码写 `编辑框1.内容` 问题面板不报错）。诊断（editorDiagnosticsShared，hasCommandCatalog 块）对每个代码行扫描 `对象.成员` 的**首段对象名**（引号内容 maskQuotedText 掩掉；链式 a.b.c 只查首段，后段无类型信息）：既不在 validCommandNames、也不在 allKnownVarNames（**关联窗口的控件名/窗口自身已并入该集合**，来自 activeWindowControls）、也非保留字 → 报「未定义的窗口组件或对象"编辑框1"」。数字开头（小数 1.5）天然不匹配标识符头不误报。与上一条成员补全收紧同一存在性判据（补全不弹 ↔ 诊断报错，行为一致）。问题面板先行、行内红标未同步（同参数类型检查的取舍）。验证：新增 3 条单测（未放置报错/已知对象不报/字符串内与链式后段不误报），npm test 全绿(126 单测+43 契约)。
- [优化] **成员补全只对真实存在的对象弹出**（用户：设计器没放置编辑框，代码里敲 `编辑框1.` 却弹出标题/宽度/内容等成员窗）。根因：buildMemberCompletionSource 对未知对象名按「去尾数字」猜类型（编辑框1→编辑框）铺出该窗口单元的属性。现收紧：对象必须真实存在——**关联窗口上放置的组件/窗口自身**（windowControlTypeMap，来自 activeWindowControls：当前 .eyc 关联的 .efw 的 formData）或**有类型的变量/参数**（userVarTypeMap/用户变量补全项）——才提供成员补全；未知对象返回**空列表**（弹窗不显示；不能返 null——那会走 `memberSource || defaultSourceList` 兜底弹出全量命令列表）；对象名为空的原兜底不变。连带收紧：跨窗口 `其它窗口.控件.`、裸类型名 `编辑框.` 也不再弹（均非"对应窗口上真实存在"）。验证：新增 4 条单测（未放置不弹、放置后弹属性、按变量类型弹不依赖命名、空对象名回退默认），npm test 全绿(123 单测+43 契约)。


### v0.0.4-beta.8（2026-07-08）

- [新增] **未知标识符回车 → 「插入变量」提示框**（照易语言）——代码行整行输入裸的未知标识符（如「我是」，非命令/变量/流程关键字/保留字）按回车，弹出「未知命令"我是"。是否将"我是"插入为变量？」，可选 **局部变量 / 程序集变量 / 全局变量 / 取消**（局部为默认焦点、Enter 直接确认）。插入：局部变量→所属子程序声明区末尾、程序集变量→顶部程序集变量区末尾（均默认整数型，可在表格里改类型）、**全局变量→经既有 onRouteDeclarationPaste 路由到 .egv 文件**；插入后自动恢复回车前正在编辑的下一行（本文件插入使行号 +1 时按位移校正）。正常回车流程（新行+光标下移）不受影响、弹窗叠加其上；Esc/点遮罩/取消关闭。实现：applyEnterKey 代码行分支检测裸标识符（validCommandNames/allKnownVarNames/reservedNameSet 三集合判未知）→ setInsertVarPrompt；`applyInsertVariable(scope)` 执行插入与恢复。typecheck 通过；npm test 全绿(119 单测+43 契约)。
- [修复] **纯数字裸语句不报错**（用户截图：整行只写 `123555` 无任何问题提示，而 `到大这` 会报未知命令）。数字开头的 token 三个既有检查都够不到：不是 funccolor（未知命令检查）、不匹配标识符正则（未定义变量检查）、参数类型检查只查括号内。新增语句级检查（editorDiagnosticsShared 第二循环）：**非赋值代码行以数字开头**（半/全角，含 `1.txt` 式杂项 token）→ 报「无效语句"…"：语句不能以数字开头」；赋值行交给未知变量检查避免同行双报；数字作参数/右值不受影响。验证：新增 3 条单测（纯数字裸行/1.txt 裸行/参数与右值不误报），npm test 全绿(119 单测+43 契约)。
- [新增] **打勾格（公开/静态/传址/参考/可空/数组）支持键盘**——此前子程序表的「公开」、变量表的「静态」等勾选格只能鼠标点击切换，方向键导航会跳过它们。现在：方向键可**停进**打勾格（td 画焦点描边、无输入框、焦点在 wrapper），**空格=切换勾选**（与鼠标点击同效，经 tryToggleTableBooleanCell 同一入口），方向键继续走统一导航（打勾格无文本、任意 ←/→ 都跨格），Esc 退出。实现：EditState 加 `booleanCell` 焦点态；模块级 `getBooleanToggleCellIndexes(lineType, tableType)` 与切换函数的判定一一对应（dll/ptrCmd/sub/localVar ci2、globalVar ci3、dataTypeMember ci2、subParam 按表类型 ci2-4）；导航序列的行内单元格带 boolean 标记、`enterNavCell` 按格类型分流（普通格 startEditCell / 打勾格设焦点态）；垂直导航同列优先补 ci 匹配（打勾格无 fieldIdx）；window keydown 顶部接管该状态的 空格/方向/Esc（经 ref 调用避免监听依赖膨胀）；commit 对 booleanCell 直接退出（无文本可提交、防误写）。typecheck 通过；npm test 全绿(116 单测+43 契约)。（用户：光标进单元格后方向键不能往下继续移动）。此前 applyArrowNavigationKey 对 cellIndex≥0 一律早退、方向键无效，只能 Esc/点别处离开。现在单元格内 ↑/↓＝提交并移动到**相邻可编辑行**：按 blocksRef（visibleBlocks 文档序）构建可编辑序列（非虚拟代码行 + 表格非表头行），相邻是表格行取**同 fieldIdx** 的单元格（无同列取该行首个可编辑格，startEditCell 进入）、相邻是代码行则 startEditLine+focusInlineInputAt(0) 进入行编辑——表格↔代码行之间无缝穿行（子程序表内 参数/局部变量 行间也逐行走）；←/→ 保持输入框原生水平移动。复用 commit+setTimeout 的既有导航模式（与代码行 upOrDown 一致，等提交落盘后按最新 blocks 定位）。**二轮补**（用户复测）：①代码行按 ↑ 相邻是表格时整表跳过（旧路径 resolveVisibleCodeLineTarget 只认代码行）——代码行 ↑/↓ 先查统一可编辑序列（buildEditableNavEntries），相邻是表格行则进其单元格，仍是代码行才走原 applyCodeLineNavigation（保留隐藏行解析/水平位保持，契约源码锁不动）；②单元格内 → 在文本末尾跨到本行下一可编辑格（光标置格首）、← 在行首跨到上一格（光标置格尾，经 pendingInputCaretRef 布局期恢复），中间位置保持原生水平移动。**三轮补**（用户复测：行末最后一格继续按 → 光标消失）：横向到行边界后**跨行环游**——→ 进下一可编辑行（表格行取首格、代码行进行首）、← 进上一行（表格行取末格、代码行进行尾）；文档边界重新进入当前格；垂直导航同款边界问题一并修（↑/↓ 无相邻行时回落当前行，光标永不消失）。根因：先 commit 再找目标、无目标时直接 return 导致编辑已关光标丢失。typecheck 通过；npm test 全绿(116 单测+43 契约)。
- [修复] **表格单元格编辑态按 ↑/↓ 出不来**（用户：光标进单元格后方向键不能往下继续移动）。此前 applyArrowNavigationKey 对 cellIndex≥0 一律早退、方向键无效，只能 Esc/点别处离开。现在单元格内 ↑/↓＝提交并移动到**相邻可编辑行**：按 blocksRef（visibleBlocks 文档序）构建可编辑序列（非虚拟代码行 + 表格非表头行），相邻是表格行取**同 fieldIdx** 的单元格（无同列取该行首个可编辑格，startEditCell 进入）、相邻是代码行则 startEditLine+focusInlineInputAt(0) 进入行编辑——表格↔代码行之间无缝穿行（子程序表内 参数/局部变量 行间也逐行走）；←/→ 保持输入框原生水平移动。复用 commit+setTimeout 的既有导航模式（与代码行 upOrDown 一致，等提交落盘后按最新 blocks 定位）。**二轮补**（用户复测）：①代码行按 ↑ 相邻是表格时整表跳过（旧路径 resolveVisibleCodeLineTarget 只认代码行）——代码行 ↑/↓ 先查统一可编辑序列（buildEditableNavEntries），相邻是表格行则进其单元格，仍是代码行才走原 applyCodeLineNavigation（保留隐藏行解析/水平位保持，契约源码锁不动）；②单元格内 → 在文本末尾跨到本行下一可编辑格（光标置格首）、← 在行首跨到上一格（光标置格尾，经 pendingInputCaretRef 布局期恢复），中间位置保持原生水平移动。**三轮补**（用户复测：行末最后一格继续按 → 光标消失）：横向到行边界后**跨行环游**——→ 进下一可编辑行（表格行取首格、代码行进行首）、← 进上一行（表格行取末格、代码行进行尾）；文档边界重新进入当前格；垂直导航同款边界问题一并修（↑/↓ 无相邻行时回落当前行，光标永不消失）。根因：先 commit 再找目标、无目标时直接 return 导致编辑已关光标丢失。typecheck 通过；npm test 全绿(116 单测+43 契约)。
- [新增] **正在编辑的行绘制焦点背景**——进入编辑态（代码行内联编辑/表格单元格编辑/参数展开编辑）的行整行绘制与行选中同款的焦点背景色（`--table-selection-bg`），gutter 行号区一并着色；表格行含 td 本体覆盖与右侧空白补色（照 eyc-line-selected 全套模式新增 eyc-line-editing）。接线：代码行 row 包裹层（lineIndex+isVirtual 匹配）、表格数据行 tr、表格 gutter cell 三处按 editCell 匹配加类。**悬停豁免**（用户反馈悬停色盖住编辑背景显得错乱）：照 eyc-line-selected 的豁免模式，编辑行的 `.eyc-code-line:hover` 与 `.eyc-data-row:hover`（后者随全套模式已带）保持焦点背景不被悬停色覆盖。typecheck 通过；npm test 全绿(116 单测+43 契约)。
- [新增] **行号区交互**：鼠标悬停行号显示**左右镜像的箭头光标**（按 Windows 标准箭头几何 1:1 翻转自绘、白填充黑描边、热点在右上尖端）；**单击行号=选中整行**（不进入编辑态），**按住拖动=整行多选**，Shift+单击扩展选择。（光标方案反复过：CSS 无法翻转系统当前主题光标、只能自绘图片→自绘长系统默认样、与用户自定义光标主题不符→改手型 pointer→用户仍选定镜像箭头，最终定稿镜像箭头。）实现：新增 `handleGutterMouseDown`——stopPropagation 不冒泡到行 mousedown（避免乐观进编辑）、提交现有编辑、置 selectedLines/dragAnchor、`wasDragSelect=true` 让后续拖动直接走既有 onMove 的 rangeSet 多选且吞掉随后的 click；接到代码行与表格行两处 gutter cell（表头/虚拟行/diff 占位不接）；折叠按钮自带 stopPropagation 不受影响、断点圆点无独立交互一并按整行选中。光标 CSS 排除粘性子程序头 gutter 与占位 gutter。typecheck 通过；npm test 全绿(116 单测+43 契约)。（用户截图：同一子程序中部出现第二张局部变量表）。①粘贴目标在子程序内时，片段中"不属于片段内子程序"的 .局部变量 行（片段首个 .子程序 头之前的）不再按光标位置内联（那会在子程序中部形成第二张局部变量表），改为并入目标子程序头部声明区（参数/局部变量块末尾）；片段自带子程序的局部变量随子程序原样保留；纯局部变量粘贴不留内联空洞、选中范围指向声明区插入处；记账注意"光标恰在声明区末尾"须让变量插在内联代码之前（insertAt < declPos 才平移声明位，== 不平移）。②同子程序重复局部变量名的**问题面板诊断本来就有**（buildEditorDiagnosticsProblems checkLocalVars，经用户原文单测验证 3 条全报、跨子程序不误报），缺的是表格行内提示——新增 `duplicateDeclLineSet`（诊断结果中 重复定义/同名 类错误行）并入变量表名称格标红条件（与既有 invalidVarNameLineSet 同一渲染点 eyc-cell-invalid）。验证：新增契约用例（纯变量归位/混合粘贴变量在代码上方且代码沿用光标缩进/片段内子程序变量不被抽走），npm test 全绿(116 单测+42 契约)。③**手工输入的 .局部变量 也自动归位**（用户追加）：编辑会话结束（editCell→null 且确有编辑发生）时跑 `relocateMisplacedLocalVarLines(text)`（editorPasteUtils 导出纯函数）——逐子程序把声明区之外的 .局部变量 行按相对顺序并入声明区末尾，无错位返回 null 不写回；打开文件不触发（hadEditSessionRef 门闩）、会话进行中不动（正在编辑的行不能被抽走、行号不能在编辑态平移，这也是不挂在 commit 十余处写文本点的原因）；归位的 onChange 与提交在 600ms 内合并为同一步全局撤销。顺带效果：点开旧文件里已存在的第二张表的行再离开，也会自动归位修复。契约用例（用户场景三行归位保序/无错位返 null/多子程序各归各），npm test 全绿(116 单测+43 契约)。
- [修复] **粘贴 `.局部变量` 行归位到子程序声明区 + 重复变量名表格行内标红**（用户截图：同一子程序中部出现第二张局部变量表）。①粘贴目标在子程序内时，片段中"不属于片段内子程序"的 .局部变量 行（片段首个 .子程序 头之前的）不再按光标位置内联（那会在子程序中部形成第二张局部变量表），改为并入目标子程序头部声明区（参数/局部变量块末尾）；片段自带子程序的局部变量随子程序原样保留；纯局部变量粘贴不留内联空洞、选中范围指向声明区插入处；记账注意"光标恰在声明区末尾"须让变量插在内联代码之前（insertAt < declPos 才平移声明位，== 不平移）。②同子程序重复局部变量名的**问题面板诊断本来就有**（buildEditorDiagnosticsProblems checkLocalVars，经用户原文单测验证 3 条全报、跨子程序不误报），缺的是表格行内提示——新增 `duplicateDeclLineSet`（诊断结果中 重复定义/同名 类错误行）并入变量表名称格标红条件（与既有 invalidVarNameLineSet 同一渲染点 eyc-cell-invalid）。验证：新增契约用例（纯变量归位/混合粘贴变量在代码上方且代码沿用光标缩进/片段内子程序变量不被抽走），npm test 全绿(116 单测+42 契约)。③**手工输入的 .局部变量 也自动归位**（用户追加）：编辑会话结束（editCell→null 且确有编辑发生）时跑 `relocateMisplacedLocalVarLines(text)`（editorPasteUtils 导出纯函数）——逐子程序把声明区之外的 .局部变量 行按相对顺序并入声明区末尾，无错位返回 null 不写回；打开文件不触发（hadEditSessionRef 门闩）、会话进行中不动（正在编辑的行不能被抽走、行号不能在编辑态平移，这也是不挂在 commit 十余处写文本点的原因）；归位的 onChange 与提交在 600ms 内合并为同一步全局撤销。顺带效果：点开旧文件里已存在的第二张表的行再离开，也会自动归位修复。契约用例（用户场景三行归位保序/无错位返 null/多子程序各归各），npm test 全绿(116 单测+43 契约)。
- [修复] **粘贴重名子程序改为自动改名（原名_1，_1 占用则 _2 依次类推）**——此前粘贴与现有子程序重名的子程序走"并入同名子程序"合并语义：整块从插入内容剔除、只把正文并进旧子程序，**复制一个只有声明行的子程序（如 `.子程序 子程序1, , , `）粘贴会被整块剔除、看起来毫无反应**（用户实测报告）。现改为：重名子程序整块保留、只把 `.子程序` 声明行的名称字段改成首个未占用的 `原名_N`（占用集=现有子程序名+本次粘贴中已定名，剪贴板内自身重名也依次编号）；不重名照旧原名插入。实现：editorPasteUtils `renameDuplicatePastedSubs` 取代 `splitPastedLinesForDuplicateSubMerge`/`appendSubBodiesToExistingText`（连带删除合并机制与 append 回填分支）。验证：新增契约用例（声明行单独粘贴→改名插入不再没反应、_1 占用升 _2、带正文整块改名、不重名原样），npm test 全绿(116 单测+41 契约)。
- [新增] **复制/粘贴的流程结构补尾护栏**——复制选区含流程命令头（如果/判断开始/循环首…）但没选到配对结束（如只复制了 判断开始+case+默认、没有判断结束）时，**剪贴板自动补上缺失的结束行**；**粘贴**（含从外部编辑器粘进来的代码）同样在插入前补齐缺失结束，半截结构不再破坏流程线。实现：eycFlow 新增纯函数 `appendMissingFlowEnds(lines)`——栈扫描：FLOW_START 头入栈、同尾结束出栈（结束越过内层未闭合时内层按 implicitClose 语义视为隐式闭合、不补）、否则/默认分支不入栈、**裸「判断」一律不视为可闭合头**（它是 case 分支，补判断结束只会造孤立结构）；文末未闭合的按 LIFO 补结束行（沿用头行缩进与前导点号，循环尾带空括号、其余裸关键字）。接入：复制侧 3 处（getSelectedSourceText + 键盘 Ctrl+C/Ctrl+X，在 filterClipboardSelectionIndices 之后）；粘贴侧 buildMultiLinePasteResult（路由声明剔除后统一补齐，覆盖 Ctrl+V/右键粘贴/自定义粘贴全部路径）。契约测试的 TS 加载器为 editorPasteUtils 补 './eycFlow' 依赖注入（4 处）。验证：新增 6 条单测（用户场景 判断开始+默认 补判断结束、如果+否则、嵌套 LIFO 缩进、循环尾带括号、裸判断不补、隐式闭合/孤立结束不补），npm test 全绿(116 单测+40 契约)。
- [修复] **下划线开头窗口名（_启动窗口）的引用改写规则对齐**。命名约定（用户澄清，与编译器/双击窗体生成一致）：**程序集名**=`窗口程序集_`+剥前导下划线的核心名（`_启动窗口`→`窗口程序集_启动窗口`，前导下划线兼作分隔符，直接拼原名得到双下划线匹配不上）；**窗口事件名**=`_`+原始窗口名+`_事件`（`_启动窗口` 的事件是 `__启动窗口_创建完毕`，**双下划线合法**，编译器 `windowEventPrefix` 原始名拼法本来就是对的）；跨窗口成员引用（`旧名.`）用原始名；控件/菜单事件维持剥下划线家族（编译器兼容桩与 buildEventSubName 一致）。改动：①渲染层 `applyWindowRenameToContent`（属性面板改名+撤销改名共用）与②主进程 `renameWindow`：**程序集模式改用核心名**（原来拼原名对 `_启动窗口` 匹配不上、程序集名不会跟着改）、事件/成员引用保持原始名；③菜单项改名同步 `handleMenuItemRenames`：事件名拼法与编译器菜单桩一致（剥前导下划线）；粘贴窗口同规则（程序集=核心名、事件/成员=原始名）。（中间一度误把窗口事件也改成剥下划线并动了编译器 windowEventPrefix，经用户纠正已还原——窗口事件族用原始名。）验证：typecheck web+node、npm test 全绿(110 单测+40 契约，含真编译回归)。
- [新增] 项目管理器**窗口节点右键菜单：复制窗口 / 粘贴窗口 / 删除窗口**（`_启动窗口` 不可删除，删除项灰显并提示）。复制=记住该窗口；粘贴=统一命名「窗口N」（取首个未占用序号；用户纠正过初版的「原名+序号」），复制 .efw（改 name/sourceFile，title 原来等于窗口名时跟着改）+ 复制关联 .eyc 并把窗口引用改到新名——**程序集/事件名的拼法要剥掉窗口名的前导下划线**（`_启动窗口` 的程序集是 `窗口程序集_启动窗口`、事件是 `_启动窗口_创建完毕`，前导下划线兼作分隔符；初版直接拼 srcName 得到双下划线匹配不上、复制体程序集名没改成新名，用户实测发现）：`窗口程序集_剥名`、`_剥名_` 用剥后的名替换、`原名.` 用原始名替换；粘贴后自动打开新窗口；删除=确认后 .efw 与关联 .eyc（以 efw 的 sourceFile 为准）一并移入回收站、关闭对应标签页、清理项目树，被删窗口若是当前剪贴板内容则清空剪贴板。实现：Sidebar `ProjectNodeAction` 扩 copyWindow/pasteWindow/deleteWindow + nodeContextMenu kind 加 'window' + `canPasteWindow` prop（无复制体时粘贴灰显）+ 菜单项 :disabled 样式；App `copiedWindowFile` state + handleProjectNodeAction 三个动作（复用 addFile/removeFile/closeFileTab/openFileByPath 与 insert:window 的树更新样板）。typecheck 通过；npm test 全绿(110 单测+40 契约)。
- [新增] 项目管理器**各分类节点右键弹出对应的「新建」菜单**——此前只有「程序集」分类有右键菜单，现补齐：窗口→新建窗口；程序集→新建程序集/新建类模块（原有）；全局变量→新建全局变量；常量表→新建常量；自定义数据类型→新建自定义数据类型；DLL命令→新建DLL命令/新建指针命令。动作全部复用插入菜单同款逻辑（insert:window/globalVar/constant/dataType/dllCmd/ptrCmd）。实现：Sidebar 模块级 `CATEGORY_CONTEXT_MENU_ITEMS`（分类 id→菜单项，兼作右键判定集）+ `ProjectNodeAction` 扩 6 个动作 + nodeContextMenu state 加 categoryId；App `handleProjectNodeAction` 映射到 handleMenuAction。资源分类暂无新建动作、不弹菜单。typecheck 通过；npm test 全绿(110 单测+40 契约)。


### v0.0.4-beta.7（2026-07-08）

- [修复] 判断结构流程线：**「判断」case 与 默认 之间的 body 多于一行时，内侧竖线在 case 的下一行断开**（用户结构：判断开始/空/判断()/空/空/默认/空/判断结束，第8行处内侧线缺失）。根因：内侧线由两段逻辑各标一段——case 衔接逻辑只标「case 的上一可见行 → case 行」（进入段），隐藏默认的延续段只标「默认的上一可见行 → 末端 ▼」；body 单行时两段恰好首尾相接，多行时中间行两段都不覆盖 → 断开。修复：case 衔接逻辑（EycTableEditor flow 重映射 memo 的判断 branch+start 重叠处理）补一段**向下延续**——从 case 行的下一可见行起逐行给同深度 through 段补 hasInnerVert(+FromAbove)，直到接上已标记的锚行（默认/下一 case 的上一可见行）或遇到同深度 branch/start/end 边界为止；body 单行时首行即锚行、立即停止，既有场景（20场景审计基线中 s04/s07/s10 等判断场景）零变化。验证：npm test 全绿(110 单测+40 契约)；用户结构 HMR 复核。
- [修复] 嵌套结构流程线：**分支（如 否则）的内容行本身是内层结构时（否则体首行就是嵌套如果），外层分支的右向箭头与横虚线停在本格 right:1ch、与内层结构的外侧竖线之间留一截缺口**（用户截图：第5行如果的否则分支箭头在第7行没接到嵌套如果的流程线上）。修复：渲染层 renderFlowSegsLine 普通路径对「branch 格 + 同行紧邻下一深度是 start 格」加专用类 `eyc-flow-arrow-into-inner`（复用既有 hasInnerStartBelow/stagger 判定语境、但限定紧邻 `slots[d+1]?.type==='start'`）；CSS 将该格的 `::after` 横线、`.eyc-flow-arrow-right`、以及 hasInnerLink 变体的 `.eyc-flow-outer-horz`/`.eyc-flow-outer-arrow` 的 right 从 1ch 改为 **-3ch**——横线穿过内层格、箭头尖画到内层命令文本左边（下一格 right:1ch 处，即嵌套「如」字左边、与普通分支箭头到正文的间距一致；初版 -4px 只到内层外侧竖线，用户要求继续延伸到字前）；规则放在 stagger 规则之后仅覆盖 right、保留 stagger 的 top:35% 错位。同深度 branch+start 重叠（判断 case 行，overlap-slot 路径）与普通正文行不受影响。验证：新增 4 条渲染单测（紧邻内层加类、普通行不加、overlap 不加、隔格不加），npm test 全绿(110 单测+40 契约)。
- [新增] **命令实参数据类型匹配检查**（问题面板）——此前所有命令的参数都不查类型：`写到文件 (1.txt, …)` 文件名参数是文本型、裸写 `1.txt`（未加引号）不报任何问题。现诊断按命令签名逐参检查：①**数字开头的非法单词**（如 `1.txt`、`3x`，易语言标识符不能数字开头）→ 报「参数"文件名"的值 1.txt 不是有效表达式；文本内容需加引号（如：“1.txt”）」；②**类型族不匹配**（数值族/文本/逻辑/字节集/日期时间五族）→ 报「参数"X"类型不匹配：需要 文本型，当前为 整数型」，如整数传文本型参、文本传整数型参、数值传逻辑型条件。实参类型推断：字符串字面量/数值字面量/真假/作用域变量类型（复用 buildTypeScopes/resolveVarTypeAtLine）/嵌套命令调用返回类型（并**递归检查嵌套调用自身的参数**）/纯数值表达式（复用 isPureNumericExpression）；**保守跳过**无法可靠推断的（含运算符的表达式、#常量、成员访问 如 编辑框1.内容、未知变量、省略参数），通用型/数组参数/自定义类型不参与判定，杜绝误报。实现：editorDiagnosticsShared 加 `commandSignatures` 请求字段 + `checkCommandArgTypes`（引号感知的调用扫描、顶层逗号切参、列号精确到参数起始）；EycTableEditor 构造签名表（支持库命令+DLL命令；用户子程序补全项无参数类型、天然跳过）经 worker 管线透传。验证：新增 12 条单测（用户场景 1.txt、引号后不报、各族错配、作用域变量、嵌套返回类型、嵌套内参、通用型/重复参数、保守跳过、字符串内不误触发、无签名表不检查），npm test 全绿(106 单测+40 契约)。（行内红标暂未同步此检查，问题面板先行。）
- [新增] **展开参数输入框接入自动补全弹窗**——此前只有主代码行会弹补全，参数行输入「到」/「dao」毫无反应，让用户误以为「到字节集」命令不存在（其实核心库有）。四处参数输入框（主参数行/赋值右值/右值参数/嵌套表达式行）onChange 接入 updateCompletion、onKeyDown 接入 applyCompletionPopupKey（↑↓选择、Enter/Tab 采纳、Esc 关闭，先于 Enter 提交判定）；updateCompletion 弹窗定位改为按上下文锚定活动输入框（参数编辑时 inputRef 未渲染、原先弹窗位置不更新）；applyCompletion 的光标读取/回焦同样按上下文选 paramInputRef，且参数上下文视为表达式、命令采纳后自动补括号「（）」光标置于括号内；补 useEffect 编辑态结束统一关弹窗防残留。applyCompletionPopupKey 上下箭头的回焦仅代码行上下文做（参数框焦点本在自身，经 editCellRef 判定、deps 不变以保契约源码锁）。验证：npm test 全绿(94 单测+40 契约)。
- [修复] 展开参数里输入**未配平括号**（如给「写到文件」的数据参数敲「到字节集(」）时，上方命令行**每敲一键在右边多出一组重复的同参数命令**（用户截图：`写到文件 (1.txt,到字节集()(,到字节集(),到字节集()()`）。根因链：①liveUpdate 每键把参数值实时写回主命令行，值处于「到字节集(」中间态时，未配平的开括号把外层调用自身的右括号吞掉、整行括号结构破裂；②下一键 replaceCallArg 在破行上找不到外层闭括号（found=false）→ 参数边界丢失 → 走"索引超出则追加"分支，每键在行尾追加一组 `,到字节集(...)`（级联损坏）。修复：editorTextUtils 新增 `isArgParensBalanced`（括号/引号配平判定，字符串字面量内不计）与 `balanceArgParens`（按半/全角逐个补齐缺失的右括号/引号，深度为负不修交给诊断）；liveUpdate 参数分支加守卫——**未配平不写回**（输入框本地照常编辑，配平后再同步）；提交路径（参数/赋值右值/右值参数三分支）统一 `balanceArgParens` 兜底补平（「到字节集(」失焦→「到字节集()」）。顺带查证：**「到字节集」核心支持库里有**（krnln.ToBin，1 参数通用型、返回字节集、含 windows 实现），非命令缺失。验证：新增 10 条单测（配平判定/补平/字符串内括号不计/旧 bug 机制链复现/新守卫链路），npm test 全绿(94 单测+40 契约)。
- [新增] 表格代码编辑器：**行多选状态下按空格键等同删除**（易语言代码没有空格语义，空格键让给删除）——走与 Delete/Backspace 完全相同的多选删除路径（最小结构保护、配对标记收集、溶解、ensureMinimalFlowBodies 等）。仅在有行选区时劫持空格；输入框内编辑正常文本时空格照常输入（window 监听器对 INPUT/TEXTAREA 提前返回，天然不受影响）。实现：EycTableEditor.tsx 多选删除块的触发条件加 `e.key === ' ' && !ctrl && selectedLines.size > 0`。typecheck 通过；npm test 全绿(84 单测+40 契约)。


### v0.0.4-beta.6（2026-07-07）

- [新增] 全局撤销/重做：跨代码编辑器与可视化设计器、按动作时间顺序全局撤销/重做（Editor 层单一快照栈，代码/设计器 onChange 同栈、撤销自动切到对应标签、600ms 合并连续同类改动）；Ctrl+Z/Y、工具栏、右键均走此栈，EycTableEditor 内部旧栈废弃、避免与 App 快捷键双重撤销；窗口/控件改名也纳入撤销（因 renameWindow 对称，撤销=反向再改一次名，复用完整改名机制）。
- [新增] 可视化设计器右键菜单（照易语言）：撤销/删除/复制/剪切/粘贴/位置及尺寸(对齐子菜单)/到最顶层/到最底层/锁定/解除锁定/菜单编辑器/预览/跳回先前位置/到对应窗口程序集/查看数据类型定义。控件跨窗体剪贴板、层级重排 controls 数组、锁定禁拖/缩放、Ctrl+C/X/V/T 捕获阶段避与 App 冲突；到对应窗口程序集(Ctrl+U)、跳回先前位置(Ctrl+J 全局)、查看数据类型定义弹框列属性/事件。
- [新增] 窗口预览（Ctrl+Enter）：以当前窗体为启动窗口编译运行、跳过所有 .eyc 转译（利用编译器为事件生成的 WEAK 空实现），得到真实系统窗口外观、点击无逻辑；加 `CompileOptions.previewWindow`，不受问题面板阻断。
- [新增] 菜单编辑器（Ctrl+E）：仿易语言/VB6 对话框编辑窗口菜单（存 .efw 的 menu 字段、随全局撤销）；编译器生成 Win32 菜单（CreateMenu/CreatePopupMenu/AppendMenuW/SetMenu + WM_COMMAND→`_名_被选择()` 弱空实现，预览也显示菜单栏）；设计器画布渲染菜单栏、单击菜单项生成/跳转 `_名_被选择` 事件；名称跟随标题/必填/不能数字开头/不能重复/改名同步源代码 `_旧_被选择`→`_新_被选择`。
- [优化] 对齐工具条与顶部工具栏按钮悬停由填充背景改为亮色圆角描边环（inset box-shadow 跟随圆角、不撑大盒子）。
- 说明：用户可见变更见《版本更新说明.md》v0.0.4-beta.6；完整开发细节见提交 `v0.0.4-beta.6`。

### v0.0.4-beta.5（2026-07-06）

- [优化] 顶部工具栏的**对齐按钮**（左/右/顶/底对齐、水平/垂直居中、相同宽/高/大小 共 9 个）不再常驻顶部工具栏（此前在代码编辑器里也显示、无意义），**独立到可视化设计器内、标尺下方**——做成一条悬浮对齐工具条，只在设计窗口时出现，选中 ≥2 个控件时可用。实现：对齐逻辑从「工具栏 → App `setAlignAction` → VisualDesigner `alignAction` prop → effect 应用」改为 **VisualDesigner 内部 `applyAlign` 函数**直接被标尺下方的工具条按钮调用；连带清掉 Toolbar 的对齐按钮组与 `onAlign`/`hasControlSelected` props、App→Editor→VisualDesigner 的 `alignAction`/`onAlignDone` 透传链与 `multiSelectCount` 死状态。新增 `.vd-align-bar` 悬浮在标尺(24px)下方左侧（top:28/left:28）。typecheck 通过；npm test 全绿(84 单测+40 契约)。（放置为悬浮小工具条，位置可按需再调。）

- [修复] 剪切/删除某流程段**唯一的一行正文**（如「如果」体里只有 `调试输出` 一行、没有额外空行）后，该段**塌成空瘪结构**（「如果」直接紧贴「否则」、中间一条 body 都不剩）；且**结构尾部（如果结束后）也没留普通空行**，导致没法在结构外面接着写命令。上一版的 protectMinimalFlowInPartialSelection 只能**保护已存在的空 body**，救不了「body 全是正文、没有空行可留」。修复：新增删除后兜底 `ensureMinimalFlowBodies`，接入三条多选删除路径（applyFlowAwareDeletion / 键盘 Ctrl+X / 键盘 Delete），做两件事：**(a)** 凡「开启标记(head/branch)紧跟同深度闭合标记(branch/end)、中间无 body」的空瘪段，补一条空行占位；**(b)** 顶层结构结束(depth-0 end)正好落在子程序末尾（到文件尾、或紧跟下一个 `.子程序/.程序集`）时，补一条**尾部普通空行**，好在结构外接着写。**顺带修复**空的「如果」体在表格模式塌行/显示不全（段内始终有空 body 即正常显示可点入）。验证 .tmp-harness/repro-flowedit7：如果体仅 `调试输出`→剪切后 `如果/空/否则/空/如果结束/空`（如果段留一条空 body、不再紧贴否则，如果结束后有尾部空行）；repro-flowedit1/2/5、repro-cutclip 回归不变；npm test 全绿(84 单测+40 契约)。

- [修复] 表格模式下多选**剪切/复制**流程结构内的正文（如只选「调试输出」剪切）时，**剪贴板内容会多出隐藏的配对标记**（`否则`、`如果结束` 等），用户报"剪切的文本多了否则和如果结束、应该只剪切调试输出出来"。根因：范围拖选用 `rangeSet(anchor, li)` 填满整个 index 区间，把区间内**表格模式隐藏的配对标记**（否则/默认/如果结束/判断结束/如果真结束）也纳进选区；而 `getSelectedSourceText` 与键盘 Ctrl+C/Ctrl+X 直接把选区**所有**行拼进剪贴板，隐藏标记就漏了出来（粘贴会得到孤立的否则/如果结束）。修复：新增模块级 `filterClipboardSelectionIndices(ls, sorted)`——隐藏标记若其**结构头**（如果/判断开始/循环首 等，向上找同深度「有 start 非 branch」行）**不在选区里**（用户只选了中间可见正文、没打算连标记一起剪），从剪贴板剔除；头也在选区=整体复制该结构则**保留**标记（粘贴可完整重建）。接入全部剪贴板路径：getSelectedSourceText（右键剪切/复制、copySelectionToClipboard 等共用）+ 键盘 Ctrl+C + 键盘 Ctrl+X。验证 .tmp-harness/repro-cutclip（剪切「调试输出」→剪贴板只含 `调试输出`+可见空行、无否则/如果结束）、repro-cutclip2（选中含如果头整体复制→剪贴板完整含 `.如果/.否则/.如果结束`）；npm test 全绿(84 单测+40 契约)。

- [修复] 多选**剪切/删除某流程段的整段 body（正文 + 尾随空行）**后，该段被删空瘪（头紧跟下一个标记、中间无 body），最小结构没保住。例：如果段 body=`[调试输出, 空行]`，从「调试输出」拖选到下方空行右键剪切 → 如果段变成头紧跟否则、中间无 body（用户实测：只剩「如果 + 一条空行」）。根因：最小占位保护 protectMinimalFlowInPartialSelection 里空 body 的保护判据只认「**上一行**是流程标记」（段首那条空 body），漏了「**下一行**是本段分支/结束标记」的段尾空 body——用户结构里那条空行的上一行是正文(调试输出)，故 `isSkel(i-1)` 判否、没被保护，连同正文一起删掉、段落空瘪。修复：空 body 保护判据补上 `nextIsSectionEnd`（下一行有同深度的 branch/end 段）——段首**或**段尾的边界空行都保护，从而给该段留一条最小空 body。验证 .tmp-harness/repro-flowedit5：复现用户剪切→结果 `如果/空/否则/空/如果结束`（如果段留一条空 body、不再空瘪）；repro-flowedit1/2 回归不变；npm test 全绿(84 单测+40 契约)。

- [修复] 流程结构体内「命令头/分支标记 与 正文命令 之间」多敲出来的空行现在可正常删除。例：`如果（a ＝ 1）`↵空行↵`调试输出(a)`，那条空行之前按 Backspace 会被「最小结构保护」误拦（只上移光标不删行）——因为保护判据只看「上一行是流程标记」。实际上只要下方还有正文（如 调试输出），这条空行就是回车多敲的多余行、应可删。修复：applyEmptyDeleteKey 的保护判据加「下一行也得是本结构的分支/结束标记(branch/end)」——只有**上紧邻头/分支、下紧邻分支/结束、中间无正文**的「唯一最小占位 body」才保护；下方是正文（只有 through 穿越段）时放行正常删除（删该行 + 光标上移到上一行命令本身行末尾）。多选删除的最小结构保护语义不变（选中整段 body 删除时仍保留一条最小空 body）。验证 .tmp-harness/repro-flowedit1：如果与调试输出间的空行 Backspace→删除、光标到如果行末尾(col10、调试输出上移)；否则区最小空 body→仍保护不删。npm test 全绿(84 单测+40 契约)。

- [优化] 输入**数字开头**的内容不再弹补全窗（如 `.如果真（1）` 里敲 `1` 会去 fuzzy 匹配含「1」的命令/变量，打扰输入）。易语言标识符不能以数字开头，故数字起始的词绝不是命令/变量/类型/类名。实现：updateCompletion 在解析出当前词后加一道守卫——`!hashMode && !isMemberAccess && /^[0-9０-９]/.test(word)` 即 `setAcVisible(false)` 返回（半/全角数字都拦；#常量、成员访问 `obj.` 的前导字符非标识符起始，不受影响）。验证：.tmp-harness/repro-numac2 stash 对比——有修复时输入「1」补全项为空、输入「变」仍出「变量1」；无修复时「1」会匹配出「变量1」+类方法（正是用户报的打扰）。npm test 全绿(84 单测+40 契约)。

### v0.0.4-beta.4（2026-07-06）

- [修复] 窗口程序里把**控件文本（`编辑框1.内容` 等）赋值给文本型变量**编译报错：`assigning to 'wchar_t*' from 'const wchar_t*' discards qualifiers`（`a = yc_get_control_text(L"编辑框1")`，a 是文本型=`wchar_t*`）。根因：`yc_get_control_text` 用 4 槽轮转缓冲、返回 `const wchar_t*`，而文本型变量是 `wchar_t*`，赋值丢弃 const。轮转缓冲对「赋值即拷贝」的值语义**本身也是错的**——`a=编辑框.内容` 后再读 ≥4 个控件文本，a 所指的槽被复用、内容被悄悄覆盖（潜在数据错乱）。修复：`yc_get_control_text` 改为每次 `malloc` 独立堆拷贝、返回 `wchar_t*`，与其余所有文本取值函数（`yc_wcsdup_text` 系）一致，一并解决 const 报错与别名隐患；声明(extern)与定义同步改、移除轮转缓冲全局量、`TRANSPILE_CACHE_VERSION` 7→8 令缓存 .cpp 重生。验证：真编译用户实际窗口项目通过（无 `discards qualifiers`、`success=true`，temp/main.cpp 已生成 `wchar_t* yc_get_control_text`）；npm test 全绿(84 单测+40 契约，含 compilerAuditFixes 真编译回归)。

### v0.0.4-beta.3（2026-07-06）

- [修复] 变量汇总面板左侧（与预览缩略图 minimap 交界处）出现一条贯穿全高的**蓝色竖条**：新开 IDE / 打开项目就在，最大化 / 收展变量面板 / 开关输出面板 / 打开设置对话框都能让它消失。**真凶（用户 DevTools 定位）= 全局键盘焦点描边落到了编辑器可聚焦容器上**。global.css 有 `:focus-visible { outline: var(--focus-ring-size) solid var(--border-focus); outline-offset: 1px }`（蓝 #007fd4）；编辑器主体 `.eyc-table-wrapper`（挂载即被 `focusWrapper()` 程序化聚焦）等容器**右缘恰好压在与变量汇总面板的交界处**，这圈焦点描边的右边就渲染成一条贯穿全高的蓝竖线。为何"任意窗口操作即消失"：点最大化/面板/对话框都带鼠标点击→焦点模态从键盘切到鼠标→`:focus-visible` 熄灭→描边消失（一度被误判为"重绘残影/合成层 ghost"，其实是这个）。`.eyc-table-wrapper{outline:none}` 本想压掉它，但与全局 `:focus-visible` **同为 0,1,0 优先级、按源码/主题注入顺序定胜负**，dev 下全局规则反超→描边显形。修复：加更高优先级规则 `.eyc-table-editor :focus-visible { outline:none }`（后代选择器 0,2,0，稳压全局 0,1,0，不受注入顺序影响）统一盖掉编辑器内的焦点描边；输入框仍各自有 border 焦点提示。**另保留两处相关硬化**：①`.eyc-minimap:focus-visible` 不再画 inset 蓝环（那也是 --border-focus、其右缘同在该交界，键盘聚焦 minimap 时会现同款蓝线；box-shadow 改回常态投影，焦点提示交给视口条）；②`editorRootRef` 挂 `selectstart` 监听、非 input/textarea 一律 preventDefault（防拖动/多选时经非输入区发起原生空选区，另一种潜在蓝条源）。**走过的弯路（留记）**：先后误判为原生文本选择、minimap `:focus-visible` 环、GPU 合成层重绘残影——都因 Playwright harness 复现不出（合成环境无真实键盘焦点/原生选择/GPU 残影），最终靠用户开 DevTools 勾掉 `:root` 的 `--border-focus` 一试即定位。验证：typecheck 通过、npm test 全绿(84 单测+40 契约)；dev 下 HMR 生效、真机复核。

- [修复] 在已有流程结构上方**新输入**一个流程命令（如下方已有「判断」结构，在其上方空行敲「如果」），**还没上屏/失焦自动格式化时**就实时画出一条流程线、且歪画到下方判断结构的「判断结束」位置。根因：驱动流程线与隐藏标记的 useEditorBlocksModel 直接吃 `currentText`——而 live-update 会把正编辑行的半成品（半个「如果」）实时写进 currentText，于是流程模型立刻把它当流程头参与结构计算，画出未完成的流程线。修复：新增 `flowModelText` useMemo，编辑代码行期间把「正编辑行」还原成其**编辑前原始文本**（新增 `editOrigRawLineRef`，在 startEditLine/beginCodeLineEdit 两处捕获），只喂给流程模型（blocks/flowLines）；因 live-update 只改这一行，还原这一行即完全还原编辑前结构，正编辑行本身仍以输入框显示 editVal、不受影响。提交清空 editCell 后 flowModelText 回落 currentText，流程线正常绘制。此改**同时涵盖**上一条头删除瞬时闪标记问题（编辑期头行被还原→结构不破裂）。验证：.tmp-harness/repro-typeflow（下方判断结构上方空行输入「如果」编辑中→流程线仅在判断结构 idx5/6、idx4 无线、不画到下方）、repro-typeflow2（上屏后如果结构 idx4/5/7 有线、下方判断 idx10/11 完好）；typecheck 通过；npm test 全绿(84 单测+40 契约)。

### v0.0.4-beta.2（2026-07-06）

- [修复] 删除流程命令头（如退格清空「如果」头）**过程中**，会瞬时闪现出「否则/如果结束」等本应隐藏的配对标记，其流程线还会画到下方别的结构体的结束位置。根因：编辑头时 live-update 把头写空→结构瞬时破裂→配对标记变孤立→visibleBlocks 的「孤立标记保持可见」规则让它们闪现、流程线按破裂模型画歪。修复：visibleBlocks 加 `editingFlowHead = editCell!=null && wasFlowStartRef.current` 判定——**编辑流程头期间，隐藏标记一律继续隐藏**（不走孤立可见），等提交溶解时随结构一并消失（都溶解了也不需要流程线）；useMemo 依赖加 editCell 使其在编辑起止时重算。验证：.tmp-harness/repro-headedit（删如果头到空的编辑中，可见的隐藏标记=空、如果的流程线消失不画到下方判断；下方判断线完好）、repro-headedit3（失焦后如果溶解 0/0、下方判断完好 1/1）；npm test 全绿(84 单测+40 契约)。

- [修复] 多 case 分支的判断结构，范围多选（含隐藏的默认）删除中间 case 时，默认漏保护被删、整条判断链散架/破坏上边结构。根因：裸「判断」case 在流程模型里既是 branch 又是 **start** 段（判断是 FLOW_START 关键字）。protectMinimalFlowInPartialSelection 找结构头时用「最近的 start 段」，会把默认/判断结束的头误定位到相邻的 case（而非判断开始）；该 case 在选区里时，默认/结束就 selection.has(head)=true 而漏保护 → 被删（范围选会把隐藏默认纳入，拖选不会因隐藏行不入选而侥幸没触发）。修复两点：①找头改为「有 start 但**非 branch**」的行（即真正的判断开始/如果/循环首，跳过既 start 又 branch 的 case）；②裸「判断」case 直接排除出保护（它是额外分支、可删，不属最小结构）。这样范围选含默认时默认按判断开始定位、判断开始不在选区→默认/结束受保护，只删中间 case。验证：单测复刻判据——范围选 {cases+默认,不含头}→默认保护、case 可删；.tmp-harness/repro-judgecase2/3/6 拖选/退格删中间 case 结构完整；npm test 全绿(84 单测+40 契约)。

### v0.0.4-beta.1（2026-07-06）

- [修复] 循环类流程尾命令丢括号 + 隐藏配对标记混进补全弹窗（三处）：①带参数的流程尾命令（如「循环判断尾」带「条件」）自动补全时没加括号；②无参的循环尾（判断循环尾/计次循环尾/变量循环尾）也没括号（易语言惯例命令都带括号，与循环首 `（）` 不一致）；③补全弹窗里出现「否则/判断结束」等隐藏配对标记，手打会造孤立标记。根因①②：formatCommandLine 流程自动补齐的 extra 行构建里 `if (FLOW_KW.has(kw)) return 无括号` 抢在参数判定之前——而循环尾都在 FLOW_KW 里，导致带参尾命令丢括号、无参尾命令也不带括号。修：把参数判定移到 FLOW_KW 之前（有参→带括号铺参数位），再把「一律不加括号」的条件从 FLOW_KW 收窄为 TABLE_MODE_HIDDEN_FLOW_COMMANDS（只有真正隐藏的结构标记 否则/默认/各种结束 不带括号，其余流程尾命令带空括号）。根因③：补全源 defaultSourceList 直接铺了 allCommandsRef 全部命令；修：`allCommandsRef.filter(c => !TABLE_MODE_HIDDEN_FLOW_COMMANDS.has(c.name))` 剔除隐藏标记。验证：.tmp-harness/repro-loopparen2——判断循环首/循环判断首/计次循环首 自动补全后尾行均带括号（`循环判断尾（）` 等；真机有命令目录时「循环判断尾」会铺出「条件」参数位）；npm test 全绿(84 单测+40 契约)。（注：补全过滤因 harness 无命令目录未能端到端跑，逻辑为直接 filter，已 typecheck。）

- [修复] 问题面板不报「表达式/流程条件里引用的未定义变量」——如判断条件 `判断开始（a ≠ 1）` 里 a 未声明却无报错。此前诊断只查赋值目标（assignTarget）和命令名（funccolor），不查被引用的标识符。修复两处：①editorCoreUtils colorExpr 补上 `≠ ≥ ≤ ＜ ＞ ＋ － × ÷` 为运算符（此前只认半角 <>!=<=>=/＝，formatOps 产出的全角比较符没被识别→条件里 `a ≠ 1` 被 pm 兜底正则连着空格整段吞成一个 span，两侧变量分不开），并给 pm 兜底正则加排除这些运算符与空白（`\s`），使标识符被单独切分；②editorDiagnosticsShared 在 span 循环里对 `cls===''` 的干净标识符（`^[一-龥A-Za-z_]\w*$`）判定：不在 validCommandNames（已含库命令+DLL+用户子程序+全部变量/参数+流程关键字）、allKnownVarNames、reservedNames 里 → 报「未定义变量」。#常量走 conscolor、命令调用走 funccolor、数字不匹配标识符正则，均不误报。EycTableEditor 渲染标记（line ~7469）同步：cls==='' 的未知标识符加 eyc-cmd-invalid 红标，与面板一致。验证：单测 A（条件里未声明 a→报）/B（声明后不报）/C（参数 p、循环变量 i、命令 取文本长度、`p ＋ i`/`≥` 均不误报）；npm test 全绿(84 单测+40 契约)。

- [修复] 多选删除会把流程结构删残——之前只保护了退格路径的最小结构，多选（拖选行 + Delete/Ctrl+X）不受保护：选中结构里的空 body 行/默认等标记删除后结构缺胳膊少腿。新规则（用户要求）：多选**若未包含某流程结构的头**（即不是"完整删除整个命令"），则保留该结构的最小结构——骨架行（判断开始/默认/否则/判断结束 等 start/branch/end 段）与每段紧跟骨架后的最小空 body 行都不删；选区**含头**=用户要整块删/溶解，正常删。实现：新增 protectMinimalFlowInPartialSelection(ls, selection)——computeFlowLines 复算，对选区里"是骨架行 或 是紧跟骨架后的空 body 行"且"其结构头不在选区"的行，收进保护集从删除集剔除；三条多选删除路径（applyFlowAwareDeletion、窗口 keydown 的 Ctrl+X line1364、Delete/Backspace line1394）都接入。内容行（非空 body）仍可删（只保护空的最小 body 与骨架），单个流程头选中删除仍走 dissolveSingleFlowHeadIfAny 溶解（含头=删整块，不保护）。验证：.tmp-harness/repro-multidel2（选首 body/选 body+默认+body→结构完整开始1默认1结束1；选整个结构含头→全删；选头+body→溶解）、repro-multidel3（选内容行→内容删、骨架保留）；npm test 全绿(84 单测+40 契约)。
  - **[追加] 只对「非循环」结构做最小保护**（用户细化）：判断/如果/**如果真**→ 选到标记/体做最小保护（如果真在命令行回车仍能在体内加行，故删到只剩「如果真/如果真结束」外壳也保留、不溶解）；只有**各类循环**（计次/判断循环/循环判断/变量）**不保护**，选到尾/体就经 repairBrokenFlowAfterDelete 正常溶解。判据用流程段的 flowKind——protectMinimalFlowInPartialSelection 定位结构头后 `headIsLoop = map.get(headLine).some(s=>s.depth===depth && s.type==='start' && s.flowKind==='loop')`，是 loop 即跳过不保护。（初版误按"有无分支"分，把如果真也当无分支→溶解，被用户纠正：如果真要保护。）验证 repro-iftrue-protect（如果真选 body+尾→如果真/如果真结束保留、只删 body）、repro-loopdis（循环仍溶解、判断仍保护）、repro-multidel2 回归不变。

### v0.0.3-beta.67（2026-07-05）

- [修复] 流程命令条件里的运算符不自动规范：如判断/如果的 `a!=1`（或全角 `a！=1`）不转成 `a ≠ 1`。根因：formatOps（`!=`/`<>`→`≠`、`>=`→`≥`、`<=`→`≤`、`=`→`＝` 等）此前只应用于赋值行（a = expr），流程命令的条件参数（括号内 argText）没走它。修复：formatCommandLine 里对流程命令的 argText 套 formatOps——preferJudgeBranch 分支（line 1992）与流程自动补齐路径（line 2012）都改。全角 `！=` 经 normalizeCodeLineInput 先 `！`→`!` 再由 formatOps 转 `≠`，故两种写法都成 `≠`。formatOps 内会占位保护字符串字面量，不污染引号内容；`=`→`＝` 对条件也正确（易语言判断条件用 `＝`，见 compilerAuditFixes 夹具 `.判断开始 (n ＝ 1)`）。验证：.tmp-harness/repro-neq（`a!=1`/`a！=1`→`a ≠ 1`、`a>=1`→`a ≥ 1`）；npm test 全绿(84 单测+40 契约)。

- [说明] 表格里偶发显示出「默认/判断结束/否则/如果结束」等本应隐藏的流程标记：这是**孤立（未配对）标记**——按设计，只有配对成功（有对应 branch/end 段）的标记才隐藏，孤立标记保持可见以免文件内容凭空消失。其根因是结构被破坏后残留的孤立标记，而本会话一连串结构修复（编辑分支参数散架、如果头删除残留否则、编辑流程头参数散架等）正是消除这些孤立标记的源头。验证 repro-markerhide：编辑有效判断/如果结构的参数后，默认/判断结束/否则/如果结束 均无泄漏。测试员所用 beta.66 尚缺本会话后段修复，故仍会见到；重打新版后应大幅减少。

- [修复] 流程内已有内容时再编辑流程头（如判断开始/如果）的参数，会把内容甩到结构外并重复出默认/判断结束（"像重新溶解又重新格式化了一遍"）。根因链：①编辑已有流程头提交时，formatCommandLine 的 +4 自动缩进（本为"新键入"设计）会把已放置的头**多推一层**（如从 indent0 推到 4）——因 dedent-by-4 只在 baseIndent≥4 时补偿，头在 indent0/其它非 4 缩进时不补偿；②头被推到 indent4 后与下方 indent0 的默认/判断结束**错位**；③下次再改参数，existingFlowStructureIsIntact 扫描发现"标记行缩进 < 头缩进"判结构不完整→shouldDissolveExistingFlow 触发溶解+重排→内容 dedent 出结构、旧默认/判断结束残留、又补一套新的（重复）。修复（最小、不动溶解契约）：commit 里对"编辑已有流程头且关键字不变"（`isSameFlowKeywordEdit = editingExistingFlowStart && cmdCheckName===effectiveOriginalFlowKw`）强制 `mainLine = effectiveFlowOrigIndent + mainLine.trimStart()`——保原缩进、不再 +4 推移，从根上断掉错位→误溶解链。验证：.tmp-harness/repro-editparam3（头 indent0 结构含内容，连续两次改参数→头始终 indent0、内容/默认/判断结束原样不动、无重复无溶解）、repro-editparam2（自然 indent4 结构改参数仍正确）、repro-dissolve-reg（头删空/嵌套溶解回归不受影响）；npm test 全绿(84 单测+40 契约，含 keyboard-dispatch 契约的 preserves/dissolve 两条源码锁未改动)。

### v0.0.3-beta.66（2026-07-04）

- [修复] 删除/退格清空「如果」等**带分支结构**的头后残留「否则」（有时连「如果结束」）。根因：退格清空流程头时 live-update 会把本块的分支/结束行去缩进到比头还浅的位置（否则/如果结束 从 indent4 掉到 0）；commit 溶解扫描里对「比头浅」的行只识别了**结束行**（endKwSet，之前为修如果真单分支补的）删除，**漏了分支行**（branchKwSet，如 否则/默认）——去缩进的否则落到末尾 `unindentSet` 分支被当普通深层内容左移保留 → 残留一条孤立「否则」。修复：EycTableEditor.tsx commit 溶解的 `lineIndentLen < baseIndentLen` 块里，分支行也删（`if (branchKwSet.has(kw)) { deleteSet.add(i); continue }`，continue 继续扫、遇去缩进的结束行才 break）。验证：.tmp-harness/repro-ifdel*（如果头删空/多退格/拖选删行→否则=0 如果结束=0）、repro-dissolve-reg（判断开始头删空→默认/判断结束都清；如果真无分支头删空→如果真结束清、不受影响；嵌套外层如果头删空→外否则/如果结束清而内层判断开始/默认/判断结束完好不误删）；npm test 全绿(84 单测+40 契约)。

- [修复] 编辑判断分支 case「判断（）」在括号内补参数、失焦提交后被误转成「判断开始」破坏结构（如 `判断 （）` 编辑成 `判断（条件A）` 却变成 `    判断开始（条件A）`，凭空多出一个判断开始头、与外层混成嵌套）。根因：commit 路径的 preferJudgeBranch 只按 shouldPreferJudgeBranchAtLine（默认区规则）判定，而分支 case 位于默认之前→判 false→formatCommandLine 走 FLOW_AUTO_COMPLETE 把裸「判断」转「判断开始」（且 dedent-by-4 令缩进翻乱）。修复：commit 路径新增 `editingExistingJudgeCase = !isVirtual && effectiveOriginalFlowKw === '判断'`（持久化的裸「判断」必为分支，因新键入判断会补全成判断开始），并三处联动——①preferJudgeBranch 纳入它（让 formatCommandLine 按分支格式化、只回「判断（args）」不补全）；②dedent-by-4 加 `!editingExistingJudgeCase` 守卫（保住内层分支的缩进，如嵌套内 indent4 的分支不被反缩进到 0）；③分支插入块加 `!editingExistingJudgeCase`（原地更新、不再重复插一条）。Enter 路径本就经 preferJudgeBranchHere（commandName==='判断'&&isJudgeStructureMarker）原地替换成「判断（args）」不受影响。验证：.tmp-harness/repro-caseparam（顶层 case 加参数→保持 `判断（条件A）` indent0）、repro-caseparam2（嵌套内层 case 加参数→保持 `    判断（条件B）` indent4；默认区键入新判断仍插入新分支）、repro-caseenter（括号内输参+Enter / +End+Enter 均保持分支不转判断开始）；npm test 全绿(84 单测+40 契约)。

### v0.0.3-beta.65（2026-07-04）

- [新增] 流程结构体保护——在结构体内的空 body 行按 Backspace 不再删除/破坏结构，而是把光标上移到上一可见行的末尾（逐行上移，直到命令头行才恢复正常字符删除）。此前在判断/如果等结构的空 body 行按退格会把该 body 行删掉（如默认体行被删、判断结束上移并结构缩水），光标还可能被困在结构内。新规则（**只保护「最小结构体」**——每段正好一条的那行空 body 行；用户在 body 里多回车产生的额外空行可正常删除，只留最小不可删）：①紧跟在流程标记/头行（start/branch/end，即 判断开始/默认/判断结束）**之后的那一条**空 body 行→光标上移到上一可见行末尾、不删行；②上一行若是普通空行（多出来的额外行）或普通命令→放行正常删除（删到只剩最小为止）；③有内容就正常删字符，删空后再退格才上移；④结构紧邻其后的首个空行(trailing)同样保护(否则从下方退回把光标推进结构又困住)；⑤命令头行(判断开始/如果 等有内容行)按到即恢复正常删除/溶解。实现：EycTableEditor.tsx applyEmptyDeleteKey 里对 Backspace 先按 computeFlowLines(prevRef.current) 判 isFlowBody(`curSegs.length>0 && !curSegs.some(type==='start')`)/isFlowTrailing(`curSegs.length===0 && prevSegs.length>0`)**且 prevIsFlowMarker(`prevSegs.some(type∈{start,branch,end})`——上一行是流程标记/头，据此区分「最小 body」与「额外空行」)**，命中则复用 ArrowLeft-at-col0 的 `applyCodeLineNavigation({type:'leftToPrevLineEnd'})`（resolveVisibleCodeLineTarget 跳隐藏行 + focusInlineInputAt('end')），否则落原删除逻辑。start 排除保证嵌套内层判断开始头仍可正常删（溶解）。验证：.tmp-harness/repro-minstruct（body 回车产额外行→额外行 BACK 可删回最小(空行4→3)、最小行 BACK 保护上移不删(空行仍3)）+ repro-bkspc2/3/4（默认体连按上移、trailing 保护、普通空行仍删、头删空仍溶解、嵌套内外完整）；npm test 全绿(84 单测+40 契约)。

- [修复] 嵌套判断内**紧挨着建兄弟判断**失败——外层判断的首个 body 里先嵌一个判断(inner1)，再在 inner1 结束之后（仍属外层 body）键入「判断」想建相邻的兄弟嵌套判断(inner2)，却被错当成 inner1 的分支 case 塞进 inner1（外层/顶层紧挨着正常、三层某些位置又碰巧正常，故极隐蔽）。根因：analyzeEnclosingJudge 的**默认行(defaultLineIdx)按缩进匹配**（`extractFlowKw==='默认' && indentLenOf===controlIndentLen`），但表格模式源码扁平——内层已闭合判断的「默认」与外层控制行同缩进(都 indent4)，于是在外层 body 定位外层默认时，会先撞上内层那个同缩进的「默认」→ 误判当前行(外层 body、在内层默认之后)落在「默认区」→ 加 case 到内层。修复：defaultLineIdx 改为**按 depth 的 judge 分支段定位**（`map.get(i).some(seg=>seg.depth===depth && seg.type==='branch' && seg.flowKind==='judge') && extractFlowKw==='默认'`），与 startLine/endLineIdx 的 depth-based 一致；endLineIdx 同时补 `flowKind==='judge'` 收紧。这样外层 body(depth0)只认外层默认(d0:branch)、跳过内层默认(d1:branch)→ 正确识别为「非默认区」→ 键入判断走嵌套建 inner2。验证：.tmp-harness/repro-inner2.mjs——outer+inner1 后在 inner1 结束后键入判断，现产出 inner2(判断开始/默认/判断结束)兄弟嵌套(idx10 起)、inner1▶next/inner2◀prev 桥接（与外层紧挨着一致）；depth 复算确认 inner1/inner2 同 d1、三层(outer d0>mid d1>innermost d2)各 body 定位正确；line56b 回归(首body→嵌套/默认body→case)仍对；npm test 全绿(84 单测+40 契约)。

- [修复] 键入「判断」的“分支 case vs 嵌套新结构”按**行位置**区分：仅当当前行处在最内层判断结构的**默认区**（默认之后、判断结束之前）时，作为该判断的新分支（case，插到默认之前）；其余位置（尤其是「判断开始」之后的**首个 body 行**）键入「判断」应新建**嵌套**判断结构（自动补全 判断开始/默认/判断结束）。用户明确此规则：判断结构自格式化后 body 行键入判断=嵌套、默认区行键入判断=加分支；加分支打「判断」、嵌套打「判断开始」。实现：EycTableEditor.tsx 新增 analyzeEnclosingJudge(lines,lineIndex)——用已修正的 computeFlowLines 复算（当前行中和成空 body 避免自污染）找出包裹当前行的最内层判断结构（按段 depth 取最内层，正确处理多层嵌套），返回控制缩进+默认行下标+结束行下标；shouldPreferJudgeBranchAtLine 改为“当前行在默认区内（lineIndex>默认行 且 <结束行；无默认则不作分支）才返回 true”；commit 与 Enter 两条插入路径按 analyzeEnclosingJudge 把「判断（）」+空 body 插到最内层判断的默认之前。注：表格模式代码左对齐显示（源码缩进被剥离），嵌套层级完全由**流程线深度**呈现（computeFlowLines 按关键字+栈定深度，扁平源亦得正确 d0/d1），故嵌套判断源码虽与外层同缩进但流程线正确显示为内层。验证：.tmp-harness/repro-line56b（首个 body 键入判断→嵌套；默认 body 键入判断→case）、repro-nest-indent（body 内「判断开始」正确嵌套）、depth 复算确认扁平嵌套源 d1 内层；npm test 全绿（84 单测+40 契约）。（历史：初版 analyzeEnclosingJudge 曾简化为“判断结构内一律 case”，经用户反馈“首个 body 键入判断应嵌套而非 case”后按行位置细化。）

### v0.0.3-beta.64（2026-07-04）

- [修复] 运行程序、被运行程序退出时主进程崩溃弹 "A JavaScript error occurred in the main process"（TypeError ERR_INVALID_ARG_TYPE: "input" must be ArrayBuffer/ArrayBufferView, received string 'libpng warning: iCCP: cHRM...'）。根因：运行路径 execFile(exePath) 未指定 encoding，默认 'utf8' 会对子进程 stdout/stderr 调 setEncoding，于是 'data' 事件回调收到的是字符串而非 Buffer；而流式解码用的 `TextDecoder.decode(data,{stream:true})` 只接受 Buffer/ArrayBufferView，传字符串抛 ERR_INVALID_ARG_TYPE，作为主进程未捕获异常令整个应用崩溃。被运行程序退出瞬间 libpng 往 stderr 写 iCCP/cHRM 告警恰好触发（空程序平时无输出故不显）。此前用 toString('utf-8') 不崩，改流式 TextDecoder 后引入。修复：compiler.ts 运行路径 execFile 加 `encoding: 'buffer'`（data 恢复为 Buffer，流式解码正常）；并加防御——decodeChunk 对字符串直接返回、不再对非 Buffer 调 decode。验证：独立 Node 脚本复现——默认 encoding 下 execFile 'data' 为 string 且 TextDecoder.decode 抛 ERR_INVALID_ARG_TYPE（与用户报错一致）；encoding:'buffer' 下为 Buffer 且 decode 正常；主进程 typecheck 通过、npm test 全绿。

- [修复] 多层嵌套结构拖选/Shift 多选删除后残留结束语句（判断结束/如果真结束/如果结束）。根因：表格模式下结束/分支行（否则/如果结束/默认/判断结束/如果真结束）隐藏，拖选只能选到可见行、range 止于最后一条可见命令，而配对的隐藏结束行常落在选区下方（未纳入删除）；已有 repairBrokenFlowAfterDelete 只处理"删了结束→溶解起始"的反方向，管不了"删了起始→结束成孤儿"。修复：新增 collectMatchingFlowMarkers(ls, 起始来源)——为每个被删流程起始行按同缩进+同名嵌套自平衡计数向下扫出配对结束行与分支行下标，纳入删除集；三条多选删除路径（applyFlowAwareDeletion 右键、Ctrl+X、Delete/Backspace）都接入。两个关键点：①起始来源需含被 sub-anchor 规则清空(forceBlank)的行——子程序首个流程起始行被保留为空行时同样丢失起始关键字，其结束也要清；②作为分支的「判断」(isEnclosedJudgeBranchLine)跳过、不带出整块判断结束（否则删单个判断分支会误删块尾）。验证：.tmp-harness/repro-nestdel.mjs（如果>如果真>判断开始/判断/默认 三层嵌套，拖选删除→0 残留；Ctrl+A 仍 0 残留）、repro-nestdel-partial（只选内层判断块→只删内层、外层如果/如果真/如果真结束/如果结束完好）；repro-deljudge（删单个判断分支→两块判断开始/默认/判断结束各 2 完好不误删）、repro-head-dissolve/repro-flow-dissolve 回归全过；npm test 全绿。

- [优化] 连续「如果真」块之间补上连通桥接（与「判断」一致）：此前连续两个如果真块，前块的如果真结束在正文行画一个向下三角箭头、后块如果真另起流程线互不相连；现改为前块结束向下拐到后块的如果真起始行、向右将箭头指向该命令（用户要求"第6行的如果真结束箭头应绘到第7行的如果真上向右拐弯指向命令"）。实现：eycFlow.ts 把桥接从"仅判断"推广到"同类连续块"——FLOW_LINK_COMMANDS 加入「如果真」，桥接循环改用 resolveFlowKind 判定前后块必须同类（judge→judge 或 ifTrue→ifTrue），跨类型（如如果真结束→判断、判断结束→如果真、如果结束→如果真）一律不桥接；EycTableEditor.tsx displayFlowLines 的"隐藏如果真结束→上一可见行画向下三角(endArrowOnly)"重映射在该结束带 hasNextFlow（桥接）时跳过，改由后块起始行的 hasPrevFlowEnd 画连接箭头。验证：.tmp-harness/repro-iftrue-bridge.mjs（连续如果真现与连续判断渲染一致：前块正文无向下三角、后块起始 has-prev-end+连接箭头；末块仍保留向下三角）+ 截图 fix-iftrue-consecutive.png；repro-bridge-crosstype.mjs 跨类型均 0 连接件；新增 eycFlow.spec 单测锁同类桥接/跨类型不桥接。**桥接连接件接外侧线**：如果真是单分支结构（无内侧分支线），其块间桥接的竖线/横线应接在外侧主结构线（left:4px、与前块外侧竖线对齐）而非内侧线（1ch+4px）——editorFlowRenderUtils 对 flowKind==="ifTrue" 的 link 元素加 eyc-flow-link-outer 类，CSS 覆盖 left:4px；判断因有内侧分支线系统仍用内侧位置（语义上正确，二者有意不同）。验证：measure-bridge.mjs 实测 如果真 link-vert.left=4px（=外侧 through::before.left）、判断=11px。npm test 全绿。

- [修复] 如果真结构溶解不完整——删/清空「如果真」头后残留孤立的「如果真结束」（还被去缩进到 0 层）。根因：如果真是单分支结构（只有如果真结束、无 否则/默认 分支），退格清空头到空时其「如果真结束」会被去缩进到比头还浅的位置（如从 indent4 掉到 0，而 如果/判断 的 否则/如果结束/默认 会留在头缩进上——差异点）；两处溶解扫描（dissolveFlowStructureAtHead 约 line 900、commit 内联溶解约 line 4283）都只匹配"与头同缩进"的结束行，够不到被去缩进的如果真结束→残留。修复：两处都补"结束行缩进 < 头缩进且 depth==0 且 kw===endKw 也识别为结束并删除+break"，容忍单分支结构结束行被去缩进的情形。验证：.tmp-harness/repro-iftrue*.mjs——简单/auto-complete/缩进错位 的如果真经 右键删/退格清空失焦/退格删空行 三路径均完整溶解无残留；repro-iftrue-nest 嵌套如果真只溶解目标结构、另一层完好（未过删）；如果头三方式溶解、循环尾溶解、判断分支不误溶解等回归全过；npm test 全绿。

- [修复] 承上条「删判断分支误溶解」——补齐 commit（编辑）路径：点进「判断」分支行、逐字符退格清空后失焦提交，同样会走 commit 的流程头溶解分支（EycTableEditor.tsx ~line 4259 `shouldDissolveExistingFlow && FLOW_START[oldKw]`）把作为分支的「判断」当块头溶解整块。修复：该处也加 `!(oldKw==='判断' && isEnclosedJudgeBranchAt(...))` 守卫（行此刻已空，按原缩进 effectiveFlowOrigIndent 向上扫判定），分支时不溶解、退格清空只把该行留空（等同普通删行）。顺带修 commit 溶解的一处既有缺陷：溶解「判断开始」头时只删了 FLOW_AUTO_COMPLETE pattern 里的 默认/判断结束，漏删同缩进的「判断」case 行（残留孤立「判断」），补 `if (kw==='判断' && (oldKw==='判断开始'||oldKw==='判断')) deleteSet.add`（循环遇判断结束即 break、更深嵌套走 unindent，故 baseIndent 的判断必属当前块；与 dissolveFlowStructureAtHead line 882 对齐）。验证：.tmp-harness/repro-editjudge-empty.mjs——退格清空块1判断分支+失焦→两块判断开始/默认/判断结束各 2 完好；退格清空判断开始头+失焦→整块溶解无残留判断；退格清空如果头→溶解；repro-head-dissolve/repro-flow-dissolve 回归全过；npm test 全绿。

- [修复] 删除判断块里的「判断」分支会错误溶解整个判断块（删掉本块的默认/判断结束、与相邻块混错），应当像普通删行一样只删该分支。根因：「判断」既在 FLOW_START（映射判断结束）又是判断块的分支/case 关键字；删除单行的溶解判定 applyFlowAwareDeletion / dissolveSingleFlowHeadIfAny（EycTableEditor.tsx）只看 FLOW_START[kw] 就把「判断」当起始行溶解。修复：新增 isEnclosedJudgeBranchLine/isEnclosedJudgeBranchAt——同缩进向上先遇 判断开始/判断/默认（处在判断块内=分支）而非 判断结束/子程序边界（=新结构头）；三条删除路径与退格清空编辑路径（applyEmptyCodeLineDelete ~5040，行已清空按原缩进扫）在「判断」是分支时一律走普通删除、不溶解。因 formatCommandLine 会把新建「判断」自动转「判断开始」，正常文档里裸「判断」几乎总是分支；此检查也对畸形输入保守兜底。验证：.tmp-harness/repro-deljudge.mjs（右键删行/退格清空 删块1判断分支→两块判断开始/默认/判断结束各 2 个完好、仅分支被删）；repro-dissolve-still.mjs（删判断开始头仍溶解本块、删如果头仍溶解）；repro-head-dissolve 三方式回归仍溶解；npm test 全绿。

- [修复] 在判断结构的默认体行键入“判断”、按空格上屏后失焦提交（点别处），只补全了括号没补全分支结构——结果“判断（）”留在原位、缩进错乱（如落到 indent0、跑到默认之后），与直接回车不一致（回车正确地把“判断（）”作为新分支插到默认之前的控制缩进）。根因：失焦走 commit 路径，其 preferJudgeBranch 分支块只认 getFlowStructureAround 返回的 prevSection.char==='默认'；但空格上屏后当前行已含“判断（）”，getFlowStructureAround 自污染（把当前行当成新结构起点）→ 判失败 → 落到末尾 splice 原样放置。回车路径（applyEnterKey 的 resolveDefaultLineIdx）本有 findPrevNonEmpty/findNextNonEmpty 兜底（当前行夹在“默认 与 判断结束”之间时按默认行定位），commit 路径缺这层兜底。修复：给 commit 路径的 preferJudgeBranch 块补相同兜底（EycTableEditor.tsx ~line 4301），并把插入改为 `splice(defaultLineIdx, 0, judgeLine, '')`（补空 body 行，与回车一致）。验证：.tmp-harness/repro-judge-full.mjs 真实组件——回车与“空格上屏+失焦”两路径现产出完全相同的结构（判断（）作分支插到默认前、控制缩进对齐、后带空 body）；P1 的“判断体内键入判断→嵌套”回归仍正确；npm test 全绿。

- [修复] 从代码行起拖多选后按 Backspace/Delete 只删了输入框里一个字符、没删中选中的多行。根因：mousedown 在代码行内容区会乐观进入编辑态（输入框聚焦），拖动转为行多选时 onMove 的行多选分支（EycTableEditor.tsx 约 line 1044）设置了 selectedLines 却没退出乐观编辑态、也没把焦点移回 wrapper——输入框仍聚焦，后续 Backspace 落到输入框删单字符（全局选中删除处理器虽在 wrapper 上，但按键被聚焦的输入框先吃掉）。修复：该分支转行多选时 `setEditCell(null)+setAcVisible(false)+optimisticLineEditRef=null+focusWrapper()`；顺带修另一条路径（行内文本拖选拖出该行转行多选，line 1027）补 focusWrapper()，否则输入框卸载后焦点落 body、inEditor 判否致 Backspace 无反应。验证：.tmp-harness/repro-multidel*.mjs 真实组件——拖选后 activeElement 从 INPUT 变回 wrapper、Backspace 正确删除全部选中行；纵向拖选/Shift 点击多选删除、正常单击编辑 Backspace 删字符（行数不变）均无回归；npm test 全绿。

- [修复] 流程线嵌套/复杂嵌套审计（Workflow 多 agent 穷尽排查，12 候选→7 确认，逐条对抗性验证），修复 5 处正确性缺陷：
  - **R1（eycFlow.ts:222 判断分支 merge）**：外退一层启发式（outdentOneLevelJudgeBranch）抢在同缩进精确匹配之前，把与外层对齐的 `.判断` 分支误吞进未闭合内层判断块（跨层污染，外层第二个判断结束段丢失成裸文本）。改成两段式（先精确同缩进就近归属、再退化外退），与 否则/默认、结束行 路径一致。
  - **R2（eycFlow.ts:231 同块）**：`.判断` 分支归属到非栈顶判断块时，漏了隐式闭合其上未闭合的内层结构（如内层 `.如果` 缺 如果结束），导致内层流程线跨越分支行、内层 end 落进新分支体。补上与 否则/默认 路径(line 269-270)一致的 implicitClose。
  - **R3（eycFlow.ts:359 桥接）**：hasNextFlow/hasPrevFlowEnd 桥接只校验后继块是判断，未校验前驱块自身也是判断——`如果结束`/`如果真结束`/`计次循环尾` 紧邻 `判断开始/判断` 时误画桥接连接件（唯一在良构已提交代码上持续可见的缺陷）。加 `if (fb.keyword !== '判断开始' && fb.keyword !== '判断') continue`。
  - **R4（EycTableEditor.tsx:2680 显示层）**：表格模式空分支区（否则/默认 与 结束行之间无可见行，如 autocomplete 刚生成骨架未填内容）被隐藏后，nextVisibleFallback 无边界约束越过 flowEndLine，把外侧结束横线锚到结构之外的可见行，画出指向结构外命令的幽灵分支箭头 ├─►。改为回退目标必须落在 flowEndLine 之前才锚定（空分支区不绘制外侧横线）。
  - **R5（EycTableEditor.css:987 stagger）**：跨深度 stagger 下内层为循环时，loop start 的向右箭头 ▶ 停在 50% 而错位后的横线在 68%，脱节。补 `.eyc-flow-start.eyc-flow-stagger-lower .eyc-flow-arrow-right { top: 68% }`（对照 branch 侧 stagger-upper 35% 箭头规则）。注：finder 首选"删 stagger"会破坏跨深度消歧，经真实组件+CSS 核实采用其备选的箭头跟随方案。
  - **P1（EycTableEditor.tsx shouldPreferJudgeBranchAtLine 默认体启发式）**：在既有判断结构的**体缩进**处键入“判断”应新建**嵌套**判断结构（自动补全 判断开始/默认/判断结束、外层完好），但旧的“默认后首行→分支”启发式在体缩进也命中，把它误当外层分支——导致外层结构被破坏（丢失外层 默认/判断结束）。改：把两处默认体分支判据（adjacent-heuristic 的 `currentIndentLen >= 默认缩进` 改为 `===`；evaluateAround 的 isDefaultBodyFirstLine 追加 `currentIndentLen === 判断控制缩进`），即**仅在控制/分支缩进处键入“判断”才作分支，更深的体缩进一律走嵌套自动补全**。裸“判断”新键入本就走嵌套（currentLineKw 取自已提交空行=''，分支判据不触发），故 first-branch body 与 默认 body 现均正确嵌套、外层完好。守卫：顶层键入“判断”仍自动补全完整结构；编辑已有“判断”分支条件不产生嵌套/重复（preservesExistingFlowStructure 兜底）。验证：.tmp-harness/repro-user-nested.mjs（first-branch body / 默认 body 键入判断 blur+Enter 均嵌套、外层 默认/判断结束 完好）+ 截图 fix-user-nested-judge.png + repro-editbranch.mjs（编辑已有分支不破坏结构）。（注：初版曾反向放宽检测把体缩进也当分支，经用户反馈"外层流程线被破坏"后纠正——用户实际要嵌套而非分支。）
  - 验证：逻辑层 R1/R2/R3 真实运行 computeFlowLines 复现+回归（新增 3 条 eycFlow.spec 单测，npm test 全绿 83 单测+40 契约）；显示/渲染层 R4/R5 用 .tmp-harness 真实组件渲染 DOM 测量+截图确认（幽灵分支消失、循环箭头对齐 68%）。逻辑层跨 4~6 层均匀/混合/判断多分支/跨子程序边界的 ground-truth 全部正确。

- [修复] 删除/清空「流程起始行」时不完整溶解整个结构——用户反馈"子程序里把如果删了，否则和如果结束还留着"。此前只处理"删结束行→起始溶解"，未处理"删起始行→分支/结束成孤儿"；且删掉头后无法可靠反推结构上边界（会误吞头上方同缩进兄弟语句），必须在头还在时定位。新增 dissolveFlowStructureAtHead（删头+分支+结束、正文反缩进），接入全部删/清空起始行路径：①删除行三路径（右键/Ctrl+X/Delete）经新的 applyFlowAwareDeletion / dissolveSingleFlowHeadIfAny——单个起始行→溶解整个结构，其余→原结束行溶解；②编辑清空起始行（backspace 到空+失焦 / 再退格删空行）——用编辑前保留的 codeLineEditOrigValRef+wasFlowOrigIndentRef 判定，且优先于子程序锚点重定向按被编辑行 lineIndex 溶解。关键坑：退格删空起始行时 flow 编辑的 live-update 已把循环体去缩进到 0（比头还浅），溶解扫描不能因"缩进<头缩进"就中断，改为只对头缩进上的流程关键字动作、扫到匹配结束/子程序边界才停。顺带：编辑结束行清空时删掉该行（不留空行）。守卫：嵌套删内层头只溶内层、改参数/删普通行不误溶解。
  - 范围：`EycTableEditor.tsx`（dissolveFlowStructureAtHead/applyFlowAwareDeletion/dissolveSingleFlowHeadIfAny + commit 结束行分支 + applyEmptyCodeLineDelete 重构 + 三删除路径接入）；契约测试更新锁新架构
  - 验证：.tmp-harness repro-head-dissolve（删如果头 右键/编辑成空/删空行 三方式均溶解否则+如果结束）、repro-nested-head（嵌套只溶内层）、repro-user-precise（删尾编辑A/B）、repro-flow-dissolve（删除行）、repro-dissolve-guard（不误溶解）全过；npm test 全绿（80 单测 + 40 契约）

- [修复] 编辑（而非删除行命令）方式清空流程结束行时不溶解——用户反馈"逐字符 Backspace 删掉计次循环尾后失焦，计次循环首和流程线还在"。此前溶解只接在删除行路径（右键删除/Ctrl+X/Delete），而用户是点进结束行单元格逐字符退格删空再失焦，走的是编辑提交/退格删空行两条路径，未接溶解。现两条编辑路径都补上：①代码行提交(commit)：原行是流程结束关键字(FLOW_END_KW，计次/判断/循环/变量循环尾等)且新值不再是该关键字 → 溶解所属结构；②退格删空行(applyEmptyCodeLineDelete)：行此刻已空、关键字丢失，用编辑前保留的 codeLineEditOrigValRef 原值判定，仅当删的正是被编辑行时溶解。守卫：改结束行参数(关键字不变)、编辑普通行到空——均不误溶解。
  - 范围：`EycTableEditor.tsx`（commit 代码行分支开头 + applyEmptyCodeLineDelete 复用 repairBrokenFlowAfterDelete）
  - 验证：.tmp-harness/repro-user-precise.mjs（退格到空+失焦 / 退格到空+删空行+失焦 两路径均溶解循环首+反缩进）、repro-dissolve-guard.mjs（编辑参数/普通行不误溶解）、repro-flow-dissolve.mjs（删除行路径无回归）；npm test 全绿

- [优化] 全项目审计第五批（高风险项，已实现待真机验证）——三项均 headless 无法验证运行时，方案经 workflow 三 agent 并行调研（含联网查 Electron 35→43 breaking changes）后逐项实现、每项 build 验证：
  ①AI API Key safeStorage 加密——三处 key（deepseek/glm/自定义模型）落盘前经 OS keyring 加密（'enc:v1:'+base64 前缀），读时解密；与存量明文迁移共存（无前缀=明文），启动时仅在检测到明文才一次性升级（避免 churn）；Linux 无 keyring 回退明文、解密失败降级空串（绝不崩、不丢其它字段）；加解密只在主进程 readIDESettings/writeIDESettings 两个漏斗内，renderer 始终见明文，浅拷贝加密防污染回传 UI。
  ②Monaco 从 jsdelivr CDN 改本地打包（治离线/内网白屏）——新增 monaco-setup（core-only editor.api + editor.worker?worker + loader.config），Editor 顶部 import；深路径无 TS 类型用 @ts-expect-error（仅供 loader.config，类型无关）；electron.vite manualChunks 把 monaco 切独立 chunk（monaco 4.96MB / app 主包 2.30MB 不含 monaco / editor.worker 540KB emit 正常）。未做（留后续，需真机验证）：lazy 组件抽取（monaco 深织入 Editor.tsx 2800 行，抽取风险高且改不了 headless 验证不了渲染）、renderer 严格 CSP（file:// 下 worker-src 'self' 是否拦 worker 无法 headless 验证，一旦拦全员白屏）。
  ③Electron 34→43 升级——electron ^43 / electron-builder ^26.15 / electron-vite ^5；审计确认无源码级 API 必改项（仅 dialog 默认目录 E43 改 Downloads 属 UX，~7 处缺 defaultPath 的对话框列入验证项）；**npm audit 25→2（1低1中）**，electron43+builder26 带入修补后的传递依赖。
  - 范围：src/main/index.ts（safeStorage 加解密+启动迁移）、src/renderer/src/components/Editor/monaco-setup.ts(新增)+Editor.tsx、electron.vite.config.ts（manualChunks）、package.json（electron/builder/vite-plugin bump）
  - 验证：node/web typecheck + eslint 0 错误；npm test 全绿（80 单测 + 40 契约）；npm run build 通过、monaco 分块正确。
  - **必须真机验证清单**（headless 覆盖不到）：①填/回显 AI key、AI 对话能用、ide-settings.json 里 key 为 enc:v1: 密文、明文迁移升级、密文改坏降级为空不崩；②文本模式编辑器实际渲染、离线可用、monaco worker 在 file:// 起没起；③Electron43 下启动/窗口行为/IPC/编译运行/终端/Android/可视化设计器渲染全链路、dialog 起始目录。

- [优化] 全项目审计第四批（工程/依赖，低风险项）：
  ①`npm audit fix`（semver 内）——25 → 12 漏洞，修掉 vite/undici/form-data 等（含 Windows dev server 任意读、NTLMv2 hash 泄露）；剩 12 项全在 electron-builder 工具链、dev-only 不进产物，需 electron-builder 大版本升级才能修（归入 Electron 升级批）。
  ②依赖重分类——dependencies 从 10 个收敛到 3 个（adm-zip/diff/iconv-lite，仅主进程运行时用）；react/react-dom/zustand/pinyin-pro/monaco-editor/@monaco-editor/react 挪 devDependencies（渲染端已由 vite 打进 bundle，node_modules 那份是 asar 死重）；删除零引用的 @tanstack/react-virtual。预计 asar 从 87MB 瘦到 ~15MB。
  ③CI quality job 加 `npm run build` 步骤——此前构建错误只有手动触发的 e2e 才拦，现每次 push/PR 都跑。
  ④package.json 补 license(MIT)/author/repository/engines(node>=22) 四个缺失字段（LICENSE 文件本已是 MIT）。
  ⑤krnln:mapping:check 假 check 修正——此前与 generate 同命令、无条件写文件污染工作区；加 --check 模式（比对 mapping+summary 忽略时间戳、不写、有差异 exit 1）。
  ⑥生产构建加 hidden sourcemap（main+renderer）+ 从打包排除 *.map（本地存档可还原崩溃栈，不进 asar/不增体积）；清理 dev 异常退出残留的 electron.vite.config.*.mjs 临时文件。
  - 范围：package.json（deps 重分类/scripts/元数据/build.files 排除 map）、package-lock.json（audit fix）、.github/workflows/ci.yml（build 步骤）、electron.vite.config.ts（sourcemap）、scripts/krnln/generate-command-mapping.mjs（--check）
  - 验证：node/web typecheck + eslint 0 错误；npm test 全绿（80 单测 + 40 契约）；npm run build 通过（渲染包 2.34MB 未变）；krnln:mapping:check 不再写文件。
  - 本批未做（三项均"无法 headless 验证 + 核心功能回归风险高"，需真机交互验证，留独立 pass）：AI API Key safeStorage 加密（解密失败会丢 key + 明文迁移边界）、Monaco 从 CDN 改本地打包（曾试：全语言打包致 9.6MB、core-only 仍 7.2MB 且深路径无类型；loader.config 初始化不当会全员白屏，比"仅离线白屏"更糟；应配合懒加载+代码分割）、Monaco 配套的 renderer CSP（与 CDN 加载互斥）、Electron 34 EOL→43 升级（回归面最广）。

- [优化] 全项目审计第三批（渲染热路径性能，5 项）——消除"每次按键贯穿 App 顶层的全项目管线"：
  ①未调用子程序标记每键触发全项目分词扫描（本会话新功能引入）——handleOpenTabsChange 里的 computeUnusedSubNames + 项目树重建 + saveOpenTabs IPC 剥离为 300ms 防抖，每键只留 setState/缓存清理。
  ②AI 聊天存档 600ms 防抖形同虚设（本会话新功能引入）——保存 effect 依赖 chatHistory，cleanup 在每个流式 delta 都判 payload 变化而立即落盘；重构为"cleanup 只清 timer、timer 触发才 stringify+落盘"，流式期间连续 delta 顺延、仅停顿 800ms 后写一次；切项目/卸载在加载 effect 的 cleanup 里落盘旧项目（histories 用 ref、不丢数据）。
  ③编译/运行输出无上限、每条触发整个 App 重渲染——用户程序循环打印上万行致 O(n²) 拷贝 + 内存无界；高频输出改 requestAnimationFrame 批量刷新 + 5000 条上限，清空时同步清缓冲。
  ④Editor 五个项目元数据 effect（全局变量/常量/数据类型/DLL命令/类）依赖 tabs、每键重跑 5 遍全文 eycToYiFormat + readDir + 逐个 readFile——改依赖派生的 debouncedTabs（滞后 300ms），这些声明不随普通代码行输入变化。
  ⑤外部文件修改轮询每键重建定时器并立即读盘——2s 轮询 effect 依赖 tabs；改用 tabsRef 读当前标签、依赖去掉 tabs，只在切换标签时重建。
  - 范围：`App.tsx`（syncProjectMetaFromTabs 防抖 + appendOutputMessage/rAF 批量 + resetOutputMessages）、`AIAssistantPanel.tsx`（flushChatSave + histories ref + 防抖重构）、`Editor.tsx`（debouncedTabs + 外部轮询 ref 化）
  - 验证：node/web typecheck + eslint 0 错误；npm test 全绿（80 单测 + 40 契约）。均为"改何时算不改算什么"、状态最终收敛的防抖/批量改动；渲染热路径单测覆盖不到，建议 dev 手测编辑流畅度与元数据/输出正确性。
  - 本批未做（更大重构/内存天花板，回归风险高，留独立 pass）：撤销栈 200 份全文快照（应 diff 化）、editCell?.lineIndex 依赖的全文 useMemo、Editor/Sidebar/AI面板 memo 化（需先解上游内联 props 锁）、库元数据渲染侧共享缓存。

- [修复] 全项目审计第二批（状态错乱/编码，9 项）：
  ①preload on/off 监听互杀——on 包裹回调致 off 只能 removeAllListeners，多组件订阅同一频道（library:loaded ×4、AI 流式 ×2 并发）时任一卸载摘掉全部；on 改为返回精确取消订阅的 disposer，冲突频道全部迁移（后果：加载新库编辑器补全静默不刷新）。
  ②运行进程 exit/error 回调无身份校验——停止后立刻重跑（产物缓存命中时百毫秒完成）时旧进程延迟 exit 清空新进程全局状态、误报"程序已退出"；加 `runningProcess === proc` 守卫（后果：停止按钮失效、断点续跑失灵）。
  ③库加载状态文件损坏静默全卸——getSavedLoadedNames 的 catch 与非数组分支返回 [] 被当成"用户主动全卸"；改返回 null（走默认全加载）+ 损坏告警（后果：半截 JSON 致所有命令报未知）。
  ④编译 worker 支持库快照冻结——常驻 worker 首次编译后库列表不再刷新，IDE 里新载入库编译报未知命令直到重启；worker 加 reloadLibraries 消息 + 主进程 6 处库变更广播统一通知 worker 重扫。
  ⑤编译零互斥——worker 对并发请求直接并行 compileProject（共享模块级状态、争用 temp/main.cpp）；worker 内加串行队列（compileChain）。
  ⑥.efw 繁简体属性名分叉——磁盘路径查繁体"最大化按鈕"、内存路径与元数据用简体"钮"，关标签页后编译窗口按钮设置被忽略；两种写法都兼容。
  ⑦控制台项目不注入支持库常量——控制台分支 transpileProjectFile 传 [] 而窗口分支传 libraryConstants，控制台用支持库常量报 undeclared；统一传 libraryConstants。
  ⑧运行输出/终端中文 chunk 边界乱码——逐 chunk toString('utf-8') 致多字节字符跨界出 �，终端启发式还会整块误翻 GBK；运行输出改流式 TextDecoder({stream:true})，终端加"扣住不完整 UTF-8 尾部再判定"消除边界假 �。
  ⑨文本变量按 (char*) 重解释编码错——文本型运行时是 wchar_t*(UTF-16)，通用 fne 分发接口要 UTF-8 char*，此前裸 (char*)(变量) 传出乱码；改经 yc_wide_to_utf8 转换（字面量路径不变，本就是 UTF-8 窄串）。
  - 范围：preload/index.ts、App.tsx/Editor.tsx/EycTableEditor.tsx/Sidebar.tsx（disposer 迁移）、compiler.ts（exit 守卫/繁简体/控制台常量/流式解码/文本变量转换）、libraryManager.ts（getSavedLoadedNames）、compile-worker.ts（队列+reloadLibraries）、compileWorkerClient.ts（notifyWorkerLibrariesChanged）、index.ts（broadcastLibrariesChanged/终端流式解码）
  - 验证：compilerAuditFixes.spec.ts 扩至 3 例（新增控制台常量+文本变量 UTF-8 转换真编译）；npm test 全绿（80 单测 + 40 契约）；node/web typecheck + eslint 0 错误

- [修复] 全项目审计第一批（正确性止血 6+2 项）：
  ①字符串拼接被整体错译——`“共” ＋ 到文本(n) ＋ “个”` 因贪婪正则被吞成单个字面量，编译通过但运行原样输出中间的变量与加号；字面量判定收紧为"内部不含同类引号"（translateExpressionToC 与 formatArgForYcCommand 两处）。
  ②编译器不识别"判断开始"多分支结构——表格编辑器落盘 .判断开始/.判断/.默认/.判断结束，编译器只认单分支 .判断，表格模式写判断块必编译失败；现生成 if / else if / else，.默认 之后再现 .判断 分支会报可读错误。
  ③导入大写扩展名 .E 工程会删除用户源文件——basename(path,ext) 剥离区分大小写导致目标目录恰好等于源文件，覆盖导入时 rmSync 删源；现大小写不敏感剥离 + 目标与源重合时中止导入双保险。
  ④rc/ 目录资源修改不触发重编译——产物指纹 stat 的路径与实际加载（优先 rc/）不一致，换图重编译静默复用旧 exe；指纹改用同一解析函数。
  ⑤project:removeFile 缺路径校验（可删授权目录外任意文件）——补 validatePath + 文件名 ../ 过滤；renameWindow/renameClassModule 的名称参数同样补穿越过滤。
  ⑥窗口标题/控件初始文本/项目名未转义直插 C 字面量——含引号或换行的标题生成非法 C 代码；三处接入既有 escapeCString。
  - 新增 tests/unit/compilerAuditFixes.spec.ts：compiler.ts 首个真实编译集成测试（vitest 注入 runtimeEnv/CompilerHost 直接调 compileProject 走 zig 工具链，验证判断开始可编译、拼接正确拆分；无 zig 环境自动跳过），另含 .E 导入目标目录用例。
  - 遗留新发现：源文件名恰为 main.eyc 时转译产物与入口 main.cpp 同名互相覆盖（低概率边界，待修）。

- [新增] 删除尾部配对流程命令 → 溶解关联结构：删除可见的结束行（计次/判断/变量循环尾等）后，失去配对的起始行与分支行一并删除、结构内部整体反缩进一层、内部完整子结构原样保留（删"计次循环尾" → 循环首溶解，只留循环体里的如果/否则/如果结束）。此前行为：非嵌套时直接拒绝删除、嵌套时保护被内层结构劫持而漏删并留下孤立循环首画线残影；旧"补回结束行"修复策略同步替换为溶解语义。安全约束：仅对"本次删除的结束关键字族"生效，文件中原本未闭合/缩进错位的结构不受删除无关行牵连；右键删除/Ctrl+X/Delete 三条删除路径行为一致。
  - 范围：`EycTableEditor.tsx`（repairBrokenFlowAfterDelete 重写为溶解 + deletedFlowEndKws 门槛；deleteLineSelection 移除 wouldBreak 拒绝；三处调用点传被删结束关键字集合）；契约测试同步更新为锁定溶解语义
  - 验证：.tmp-harness/repro-flow-dissolve.mjs（删循环尾溶解/判断循环/嵌套只溶内层/删普通行无联动 全 PASS）；npm test 全绿（77 单测 + 40 契约）

- [修复] 流程线四项缺陷（20 场景审计后修复）：
  ①未配对的流程行凭空消失——"否则/如果结束/默认/判断结束/如果真结束"此前按关键字无条件隐藏，"如果真"里误写"否则"、孤立"如果结束"、缩进错位的结束行都会在界面上消失（文件里有、界面上无）；现只隐藏在流程模型中真正配对成功（有对应 branch/end 段）的行，孤立行保持可见。
  ②缩进错位导致整块流程线静默消失——结束行/分支行与起始行缩进不一致时配对失败、整块无线；现缩进严格匹配失败后回退"就近归属最内层同类结构"，错位代码仍正常画线。
  ③漏写结束行导致内层块无线、正文回退树状字符线（│）与流程线混排——未闭合结构现隐式闭合（子程序边界/被外层结束跨越/被外层分支跨越/文末），且不跨越外层分支边界，统一为流程线视觉。
  ④判断行 branch+start 重叠时的"隐藏下箭头"是行级标志，嵌套判断中误伤同行其他深度的箭头——改为仅作用于重叠深度本身；顺带清理重复拼接的 stagger 类与恒假死代码。
  - 范围：`eycFlow.ts`（回退配对 + implicitClose + FlowStackEntry 提取）、`EycTableEditor.tsx`（visibleBlocks 隐藏前校验配对）、`editorFlowRenderUtils.tsx`（行级 hasAnyJudgeBranchStartOverlap 移除）
  - 验证：新增 tests/unit/eycFlow.spec.ts（7 例：标准配对/错位回退/隐式闭合不越界/文末闭合/孤立行无段/如果真+否则/嵌套判断归属）；.tmp-harness/repro-flow-audit(.2).mjs 20 场景截图回归（s08 恢复画线、s09 混线消失、x1/x2 行可见、x3 误伤消除，其余场景零变化）；npm test 全绿（77 单测 + 40 契约）

### v0.0.3-beta.63（2026-07-03）

- [新增] 系统设置增加"变量汇总面板"开关（默认开启）：关闭后表格模式编辑器右侧变量汇总面板与收起细条均不渲染。设置项 editorShowVarSummaryPanel 随现有设置保存机制持久化，旧配置缺字段回退默认开启。
  - 范围：`src/shared/settings.ts`（接口/默认值/resolve）、`SettingsDialog.tsx`（编辑器分组开关行）、`App.tsx`→`Editor.tsx`→`EycTableEditor.tsx`（showVarSummaryPanel 透传与渲染门控）
  - 验证：web/node typecheck + eslint 0 错误

- [新增] AI 聊天记录按项目持久化：四种模式（聊天/Plan/Agent/编辑）的对话历史保存到项目目录 `.ycide-ai-chat.json`（与标签会话 .ycide-session.json 分文件），关闭重开 IDE 或切换项目自动恢复对应项目的记录；Agent 工具调用卡片（参数/返回）一并保存恢复。写入 600ms 防抖 + 面板卸载即时落盘（关面板/关窗口不丢最后一段）；切换项目用"已加载目录"门闩防止旧历史/空历史覆盖新项目存档；每组最多保留最近 200 条防膨胀；存档损坏时静默回退空历史。
  - 范围：`src/main/index.ts`（project:saveAiChat/loadAiChat）、`src/preload/index.ts`、`AIAssistantPanel.tsx`（projectDir prop + 加载/保存 effect，editMessages 类型统一 ChatEntry[]）、`App.tsx`（传 currentProjectDir）
  - 验证：web/node typecheck + eslint 0 错误（读写为运行时行为，待人工验证：聊天后重启恢复、双项目切换互不串档）

- [优化] AI 助手聊天窗口视觉重构（参考主流聊天产品）：用户消息=右对齐圆角气泡（accent 色调、右下角小圆角）；助手回答=无气泡全宽纯文本（行高 1.6）；思考过程(reasoning)=无气泡可折叠条目（左竖线缩进小灰字；思考中标题"正在思考…"并展开，思考结束自动收起为一行"▸思考过程"，可手动再展开；从存档恢复的思考条目默认收起）；Agent 工具调用=圆角卡片（绿/红状态点 + 单行摘要省略，展开见参数/返回，返回区限高滚动）；"执行中/正在编辑"改无气泡小灰字状态行；聊天区去内框、加大消息间距；空态居中。
  - 范围：`AIAssistantPanel.tsx`（ChatEntry.kind='reasoning' + 编辑模式流式思考消息标记 + renderChatEntry 统一四模式渲染）、`AIAssistantPanel.css`（消息区样式重写）
  - 验证：.tmp-harness ?fixture=ai 静态样式预览截图（气泡/思考/工具卡片/状态行全形态）；折叠交互实测可手动收起（React details open 属性不回弹）；web typecheck + eslint 0 错误

- [修复] AI diff 删除占位行不显示被删内容、多行删除只占一行、表格删除占位行号区无红色背景：①computeDiffLineInfo 已产出被删原文但 Editor 下传表格编辑器时只保留了行号集合，占位行只能渲染空红条——现以 锚点行→被删原文 Map 传递，被删原文按行拆开、每行一个红色占位行（删除线样式、悬停看该行原文、空行保占位、同锚点多 hunk 拼接、清除高亮同步清空），虚拟化行数与小地图删除标记按删除行数联动累加避免滚动错位；②表格删除占位行的行号区历来只有左侧红条无红背景（与代码行占位不一致），补齐红背景并避免代码行占位叠色。
  - 范围：`Editor.tsx`（eycDiffDeletedTextAfterLines 状态与传递）、`EycTableEditor.tsx`（diffDeletedLinesByAnchor 拆行 + 表格/代码行占位逐行渲染 + blockLineSpans/小地图行数联动）、`EycTableEditor.css`（.eyc-diff-deleted-text + 占位 gutter 背景）
  - 验证：.tmp-harness/repro-diff-multiline.mjs（两行删除→两个占位行逐行显示、无合并符）、repro-diff-gutter-bg.mjs（两种占位 gutter 背景一致）；web typecheck + eslint 0 错误

- [新增] AI 修改与人工修改的未保存状态区分标识：AI 编辑（面板"应用建议"与 Agent edit_file 工具，均经 handleAIApplyEdit）产生的未保存更改，项目管理器橙色圆点与标签页白色圆点外包一个圆角矩形；无圆角矩形即人工编辑。人工再编辑（代码/文本/表格/设计器四个入口）即清除 AI 标识回归人工样式；AI 撤销编辑同步清除；分类文件夹的聚合圆点不区分来源。悬停提示"未保存更改（AI 修改）"。
  - 范围：`Editor.tsx`（EditorTab.aiModified + 四个人工 onChange 清除 + 标签圆点样式类）、`Editor.css`、`App.tsx`（handleAIApplyEdit 置位/handleAIUndoEdit 清除）、`Sidebar.tsx`（aiModifiedFileKeys 推导 + 树圆点样式类 + aria/title）、`Sidebar.css`
  - 验证：web typecheck + eslint 0 错误（AI 应用/撤销链路为运行时行为，待人工在 AI 面板验证）

- [新增] 项目管理器"未调用子程序"标记：程序集分类下未被任何代码调用的子程序名称淡化显示（无额外图标），悬停提示"该子程序未被任何代码调用"。判定为全项目文本级标识符分词（"求和"不被"求和2"误判；类方法 对象.方法名 可识别；注释/字符串中出现按已使用处理，宁可漏标不误标）；以 _ 开头的子程序（事件处理/_启动子程序 入口约定）不参与判定。打开项目时全量判定，编辑中随标签页内容即时重算（含未打开模块的标记同步）。
  - 范围：`src/renderer/src/components/Sidebar/unusedSubUtils.ts`（新增，computeUnusedSubNames）、`Sidebar.tsx`（TreeNode.unused + 标记渲染 + aria 标注）、`Sidebar.css`、`App.tsx`（projectSourceContentsRef 源码缓存/buildProjectTreeFromEpp 全量判定/handleOpenTabsChange 实时重算）
  - 验证：tests/unit/unusedSubUtils.spec.ts（8 例：孤儿标记/跨文件调用/_前缀排除/分词防误判/类方法/递归/子程序指针/CRLF）；全量单测 70 通过；web typecheck + eslint 0 错误

- [新增] 表格模式编辑器右侧变量汇总面板：全文件汇总——全局变量（有则显示）/程序集变量 + 每个子程序一组（组内参数在前带"参数"标注、局部变量按声明顺序，无变量的子程序不占面板），光标所在子程序的组名高亮；条目带类型首字配色图标与数组标记；顶部搜索框按名称/类型过滤（组计数显示 匹配数/总数，无匹配的子程序组自动隐藏）；点击条目跳转并选中声明行（自动展开被折叠的子程序）；面板可收起为右侧细条（状态持久化）。资源表文档不显示。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`（varPanelGroups/varPanelCurrentSub/handleVarPanelItemClick + 面板 JSX）、`EycTableEditor.css`（.eyc-var-panel*）
  - 验证：.tmp-harness/repro-var-panel.mjs（63 组全量收集/当前子程序高亮/搜索隐藏无匹配组/点击跳转选中/收起展开持久化）；25 契约测试通过；web typecheck + eslint 0 错误

### v0.0.3-beta.62（2026-06-18）

- [优化] 编译性能：消除一次编译里对支持库目录的重复全量扫描与缓存反复失效。真因 `libraryManager.getList()` 每次都执行全量 `scan()`（重扫库根目录 + 清空 metadataCache / 命令表缓存 / ycmd 注册表缓存），而它在 `loadCompileProtocols`、`collectUsedLibraryFileNames`、主入口组装等编译热路径被调 4~5 次，导致每次编译反复全量扫盘并把已建缓存一遍遍炸掉重建。在"产物缓存命中、跳过编译/链接"的情况下，本机仍耗 ~650ms、慢盘测试机达 6 秒，全部花在这里。
  - 范围：`src/main/libraryManager.ts`（新增 `getCachedList()` 不触发重扫，编译路径改用；`getAllCommands` 按平台缓存命令表）、`src/main/ycmd-registry.ts`（扫描签名 TTL 1500ms→30000ms，覆盖整次编译周期；库增删改经 `scan()` 主动失效，正确性不受影响）、`src/main/compiler.ts`（三处 `getList()`→`getCachedList()`；诊断日志增加细粒度分段计时）
  - 验证：tsc typecheck + `npm run build` 通过；细粒度诊断日志将四个 ~120~245ms 热点全部归因到 `getList()→scan()`
- [优化] 编译移入工作线程（worker_threads）：根治点运行/编译时主进程单线程被同步计算堵死导致界面卡死。先做电脑无关解耦（`runtimeEnv` 注入 appPath/isPackaged 等 + `CompilerHost` 抽象副作用），再把编译搬入常驻 worker；worker 加载失败/超时自动回退主进程编译，保证"能编译"永不被破坏。
  - 范围：新增 `src/main/compile-worker.ts` / `src/main/compileWorkerClient.ts` / `src/main/runtimeEnv.ts`；`compiler.ts`/`libraryManager.ts`/`ycmd-registry.ts` 去 electron 依赖；`index.ts` 的 compiler:compile/run 改走 `compileViaWorker`；`electron.vite.config.ts` 增 worker 入口；`package.json` `asarUnpack` 解包 worker 与编译 chunk
  - 验证：`npm run build` 产出 `out/main/compile-worker.js` + compiler chunk（grep 不到 electron）；实测编译在 worker 内执行（改主进程代码需完全重启 App 才生效）
- [优化] 编辑器光标位置 store 解耦：此前光标行/列存于 App 顶层 useState，编辑器每次点击/按键上报光标都会触发整个 IDE 重渲染（复杂界面单次高达 1~2 秒，表现为"点一行要等 1~2 秒""输入很卡"）。改为独立订阅源 `cursorPosStore`，光标变化只通知状态栏 Ln/Col 小组件；App 内改用 `cursorRef` 供断点/运行到光标等快捷键同步读取。
  - 范围：新增 `src/renderer/src/stores/cursorPosStore.ts`（useSyncExternalStore）；`src/renderer/src/App.tsx`（cursorRef + setCursorPos）；`src/renderer/src/components/StatusBar/StatusBar.tsx`（订阅 store）

### v0.0.3-beta.61（2026-06-13）

- [修复] 连续快速按回车/退格时丢键、卡顿：回车拆行、退格删空行、表格行回车插入原先读 React state 派生的 lines/editVal 闭包，连按时 React 尚未重渲染、闭包过时，多次按键基于同一旧文本计算并互相覆盖（表现为"连按两次回车只新增一行"）。改为以 prevRef.current（最新文本真相）为数据源，并用 flushSync 强制本次状态同步落地，保证下一次按键闭包已刷新。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`（import flushSync；applyCodeLineSplitOnEnter/applyEmptyCodeLineDelete/applyTableRowEnterInsert 三处数据源改 prevRef.current + flushSync）
  - 验证：.tmp-harness/repro-enter-lag.mjs（4x CPU 节流连按5次回车，修复前净增2行→修复后净增5行；退格对称恢复）；repro4/repro9 手势回归逐字一致；62 单测 + 25 契约测试通过
- [新增] 代码编辑器子程序整体折叠：子程序名行的行号右侧新增常驻 +/- 按钮，点击收缩/展开整个子程序（收缩后仅保留声明行，参数/局部变量/代码全部隐藏）；折叠状态以子程序名为键（编辑平移行号不丢失）；编辑落入折叠范围时自动展开兜底。代码行原有的参数展开 +/- 不受影响。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`（collapsedSubNames/subFoldRanges/visibleBlocks 过滤/表格行号槽按钮/startEditLine|startEditCell 自动展开）
  - 验证：.tmp-harness/repro-subfold.mjs（收缩隐藏本子程序代码、邻居子程序不受影响、展开恢复、代码行参数展开按钮原样）；repro4/repro9 手势回归逐字一致
- [新增] 设计器画布右下角缩放控件：倍率按钮点击弹出预设档位（25%最小/30/50/80/100/150/200/300/500/1000%，选择后窗体居中）、1% 步进拖动条（最小到最大倍率全程，缩放不居中保持视点）、拖动条两侧 −/+ 按钮（按 10% 步进，如 157%→147%）、适应窗口按钮（窗体完整可见铺满画布并居中）。取代原右上角只读百分比指示器（其位于工作区内会随滚动漂移）。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx|css`（zoomToCentered/fitZoomToViewport/zoomPresetPercents/vd-zoom-control）
  - 验证：.tmp-flow-verify/verify-zoom-control.mjs（预设 200% 居中偏移≈0、拖动条缩放不归中、−/+ 157%↔147%、适应窗口 104% 居中）

## 已发布归档

### v0.0.3-beta.60（2026-06-12）

- [新增] WebView2 浏览器支持库（lib/webview2）：浏览框组件（YcWebView2Host），设计期 iframe / 运行期 WebView2 所见即所得；免 WebView2Loader.dll（注册表 EdgeUpdate ClientState 发现运行时，动态加载 EmbeddedBrowserWebView.dll）；命令"取WebView2运行时版本"。
  - 范围：`lib/webview2/`（清单/元数据/组件定义/impl/windows.cpp/官方 WebView2.h + EventToken.h shim）、`src/main/compiler.ts`（源码型带窗口单元库生成注册函数调用、库 include 目录、-ladvapi32 -lole32、地址属性入文本通道）
  - 验证：webview-test 项目编译运行截图（example.com 真实渲染）；设计器截图比对一致
- [新增] WebView2 风格组件 网页按钮（YcWebButton）/网页编辑框（YcWebEdit）：HTML 模板 NavigateToString 渲染，WebMessage 桥接 BN_CLICKED/EN_CHANGE 到父窗口 WM_COMMAND，自管 WM_GETTEXT/WM_SETTEXT 文本通道；设计器同款 CSS 预览（模板双侧同步注释）。
  - 范围：`lib/webview2/impl/windows.cpp`、`lib/webview2/webview2.window-units.json`、`src/main/compiler.ts`（isClickable/isEditLike/初始文本回退）、`src/renderer/src/components/Editor/VisualDesigner.tsx|css`
  - 验证：真实鼠标点击网页按钮触发 `_网页按钮1_被单击` 信息框弹出；设计器与运行截图逐控件一致
- [新增] 组件箱分类（toolboxCategory）：窗口单元元数据新字段，缺省"通用Win32"；工具箱顶部分类下拉（默认通用Win32、选择持久化、失效回退）。
  - 范围：`src/main/libraryManager.ts`（两条解析路径+协议 controlBindings）、`src/renderer/src/components/Editor/VisualDesigner.tsx|css`
  - 验证：IDE 截图（分类切换过滤组件列表）
- [新增] 编辑框 19 个属性（边框/颜色/多行/滚动条/对齐/输入方式12种/密码遮盖/转换/调节器/选择区/停留焦点与顺序/备注）+ 属性默认值（defaultValue）机制 + 颜色选择器单元格。
  - 范围：`lib/krnln/window-units.json`（哈希同步）、`src/main/libraryManager.ts`、`src/main/compiler.ts`（buildStdEditCodegen 样式位+EM_*+调节器 buddy+WM_CTLCOLOREDIT 颜色表+停留顺序排序+输入过滤子类）、`src/renderer/src/components/Sidebar/Sidebar.tsx|css`、`VisualDesigner.tsx|css`
  - 验证：editprops-test 项目运行时样式位断言 + 截图；WM_CHAR 过滤断言（输入整数模式 a5x7→57）
- [优化] 编辑器交互性能：共享解析缓存（getBlocksFlowModelCached，点击路径 4 次全量解析→1 次）、mousedown 乐观进入编辑态（与 commit 同批渲染）、overscan 220→80、minimap 先采样后着色、拼音打分首字符锚定预检+首字母串缓存、EycTableEditor memo+调用处 props 稳定化、normalize 预筛。点击→光标 -70%（4×CPU 节流基准 3759→1140ms）。
  - 范围：`EycTableEditor.tsx`、`editorBlocksModel(.Shared).ts`、`useEditorInteractionHandlers.ts`、`pinyin.ts`、`Editor.tsx`、`App.tsx`
  - 验证：.tmp-harness/perf-click-type.mjs A/B 基准；repro4/repro9 手势回归逐字一致
- [修复] 补全弹窗位置漂移：编辑器根节点 zoom（eycScale）未反向补偿 + 布局移动后不跟随；computeCompletionPopupPosition 接 scale，acVisible 期间 rAF 逐帧吸附输入框。
  - 范围：`EycTableEditor.tsx`、`editorCompletionDisplayUtils.ts`
  - 验证：harness 面板动画场景，弹窗恒定 +2px 吸附
- [修复] 生成代码 yc_wide_to_utf8/yc_bin_to_cstr 单一 thread_local 缓冲被同语句多实参互相覆盖（信息框正文/标题变同一文本）→ yc_c_str_slot 8 槽轮转池；TRANSPILE_CACHE_VERSION 4→7（含后续两次 codegen 变更）。
  - 范围：`src/main/compiler.ts`
  - 验证：msgbox-test 项目运行时读对话框 Static 文本断言
- [修复] 整数等非文本表达式赋给控件文本属性（编辑框.内容 ＝ k）生成 yc_set_control_text(名, int) 编译错 → 两处发射点按 isTextExpression 包 yc_value_to_text。
  - 范围：`src/main/compiler.ts`
  - 验证：用户项目 windows窗口程序-新项目1 编译通过 + 运行时显示 "123"
- [修复] g_hMainWnd 在 CreateWindowExW 返回后才赋值，"创建完毕"事件内按名操作控件全部静默失败 → WM_CREATE 开头赋值。
  - 范围：`src/main/compiler.ts`
  - 验证：editprops-test 创建完毕事件设置编辑框内容生效
- [修复] 空内容编辑框运行时显示控件名（parse 层文本回退链对编辑框类不再回退控件名）。
  - 范围：`src/main/compiler.ts`（resolveControlInitialText）
- [新增] 属性面板颜色单元格（EditableColorCell：原生取色器+常用色中文名，存 COLORREF）。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx|css`
  - 验证：IDE 实测截图（红色/黄色色块）

### v0.0.3-beta.59（2026-06-11）

- [修复] 指针读取命令参数升级为长整数型：指针到整数/小数/双精度小数/字节集/文本 的内存指针参数由整数型改为长整数型，修复 x64 下真实内存地址被截断的问题；同步重新生成命令映射与帮助说明，更新核心库固定哈希。
  - 范围：`lib/krnln/krnln.commands.ycmd.json`、`lib/krnln/generated/command-mapping.windows.json`、`lib/krnln/系统核心支持库帮助说明文件.txt`、`src/main/libraryManager.ts`
  - 验证：`npm run krnln:mapping:generate` 513/513

- [优化] 赋值行参数展开改为两级结构：`变量 ＝ 命令(…)` 展开第一级显示"※被赋值的变量或变量数组"与"※用作赋予的值或资源"，命令自身参数（如 内存数据指针）挂在第二级，点击"用作赋予的值"旁的 + 再展开。此前赋值行被命令展开分支抢占，直接平铺命令参数。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`（两处展开分支的优先级条件）
  - 验证：Playwright 真实应用展开 `版本号 ＝ 指针到整数()`，两级结构与二级"内存数据指针"均正确

- [修复] 代码行行尾单引号注释破坏命令实参解析：如 `调试输出 (版本号)   ' 输出 6(主版本)` 会被转译成打印空文本（注释内的括号干扰实参提取）。转译主循环现在先剥离行尾注释（忽略字符串内的 `'`），整行注释行为不变；TRANSPILE_CACHE_VERSION 升至 4 失效旧缓存。
  - 范围：`src/main/compiler.ts`（stripTrailingEycComment + 转译主循环）
  - 验证：复现项目（窗口程序同款代码）编译运行输出 表地址 与 版本号 6

- [修复] 支持库"实际使用"扫描漏掉嵌套在实参中的命令调用：如 `版本号 ＝ Sciter取版本(指针到长整数(表地址 ＋ 16), 0)` 中的 `指针到长整数` 不会被识别，krnln 未参与链接导致"链接器错误: 未定义符号 krnln_p2int64"（窗口程序因别处用到 krnln 而偶然不触发，控制台程序必现）。collectUsedLibraryFileNames 现在额外扫描行内所有"标识符+左括号"并解析到所属支持库。
  - 范围：`src/main/compiler.ts`（collectUsedLibraryFileNames）
  - 验证：控制台复现项目此前链接失败，修复后编译运行成功

- [修复] 新建指针命令后表格中命令名显示为"指针命令1,"（带行尾逗号）：splitCSV 只把"逗号+空格"当分隔符，行尾裸逗号被并入字段。修正 splitCSV（eycBlocks 与 Editor.tsx 内联副本）将行尾裸逗号视为分隔符，并去掉插入模板中的尾随逗号；同时补上 editorCoreUtils DECL_PREFIXES 缺失的 `.指针命令 `，否则指针命令行的单元格编辑与公开/传址/数组切换不生效。
  - 范围：`src/renderer/src/components/Editor/eycBlocks.ts`、`editorCoreUtils.ts`、`EycTableEditor.tsx`、`Editor.tsx`、`src/renderer/src/App.tsx`
  - 验证：`npm run typecheck`、`npm run test:unit`（新增行尾逗号回归用例，62 通过）

- [优化] DLL命令编辑器（.ell）右键菜单首项由"新子程序"改为"N.新DLL命令"，并新增"Z.新指针命令"；两者均在文件末尾追加声明并滚动高亮新行，菜单打开时按 N/Z 键可直接触发。其他文档类型右键菜单保持"新子程序"不变。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`npm run typecheck`、`npm run lint`、Playwright 启动真实应用分别右键 .ell 与 .eyc 标签页核对菜单项

- [新增] 编译器支持 `.指针命令`（函数指针间接调用）：在 `.ell` 文件中与 `.DLL命令` 平级声明，调用时第一个实参固定传入函数地址（长整数型），编译器按声明签名生成类型化间接调用包装。解决类 Sciter（仅导出 `SciterAPI`，所有功能通过函数指针表暴露）这类 DLL 在易语言式 DLL 命令机制下无法调用的问题。
  - 范围：`src/main/compiler.ts`（解析/签名表/代码生成/缓存版本号 bump 至 3）
  - 验证：`npm run typecheck`、`vitest`、真实 sciter.dll 端到端编译运行（取 SciterAPI → 指针到长整数 读函数表 → 指针命令调 SciterVersion 成功返回 6.0）

- [新增] 核心支持库新增 `指针到长整数`（p2int64）命令：从指定内存地址读取 8 字节长整数，用于在 x64 下从函数指针表（如 DLL 返回的接口结构）读取函数地址，配合指针命令使用。
  - 范围：`lib/krnln/krnln.commands.ycmd.json`、`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`lib/krnln/系统核心支持库帮助说明文件.txt`
  - 验证：端到端编译运行通过

- [新增] UI 支持指针命令：`.ell` 表格编辑器渲染指针命令表（含公开切换、参数传址/数组切换）、代码补全与参数详情展开（自动显示隐式“函数地址”首参）、项目树 DLL命令分类下显示指针命令节点、插入菜单新增“指针命令(P)”。
  - 范围：`src/renderer/src/components/Editor/eycBlocks.ts`、`eycTableModel.ts`、`EycTableEditor.tsx`、`Editor.tsx`、`editorTableRowUtils.ts`、`editorCommandDisplayUtils.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/main/index.ts`
  - 验证：`npm run typecheck`、`npm run lint`、`tests/unit/eycBlocks.spec.ts` 新增用例

- [修复] krnln 命令映射生成器正则可跨行贪婪匹配进函数体，导致大小写包装函数（如 `krnln_LockWindowUpdate`）被误判为未声明；同步修正 `libraryManager.ts` 中过期的核心库 SHA256 固定哈希表（原哈希与仓库文件不符，正式版打包启动校验会失败）。
  - 范围：`scripts/krnln/generate-command-mapping.mjs`、`src/main/libraryManager.ts`
  - 验证：`npm run krnln:mapping:generate` 输出 513/513 implemented

### v0.0.3-beta.55（2026-05-15）

- [修复] 编辑框运行态默认文本与设计器对齐：编译生成控件时，编辑框/超级编辑框/文本框不再将控件名回退为初始文本；仅在显式设置文本时才显示内容，避免设计器为空而运行后出现默认文字。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [修复] 可视化设计器编辑框默认底色与运行态对齐：编辑框/超级编辑框预览改为优先读取控件背景属性，未设置时默认白底，避免深色主题下显示为黑色而与编译运行结果不一致。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 参数递归展开接入流程层级配色：参数区连线与箭头改为复用流程命令的层级颜色算法（`resolveFlowLineColors`），多级参数展开时可按层级显示彩色线条。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [修复] 参数递归展开子级箭头层级偏移修正：子级箭头定位显式叠加当前层缩进变量，箭头锚点改为按“本层起始竖线”计算，避免误落到上一级位置。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [修复] 参数递归展开子级箭头绘制基准修正：子级箭头改为在参数子级容器顶部绘制（不再悬挂在上一行底部重叠区域），减少跨行基准导致的对齐漂移。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [修复] 参数递归展开子级箭头中心线对齐：子级箭头改为按中心点锚定下一级竖线起点（`translateX(-50%)`），避免按左边界定位导致“看起来未对齐”的偏移。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [修复] 参数递归展开子级箭头锚点修正：箭头改为锚定下一级竖线起点位置，不再对齐到父级收口结束点。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [修复] 参数递归展开子级连线对齐修正：二级展开三角箭头与该层竖线中心线重新对齐，消除子级箭头与竖线左右错位。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [修复] 参数递归展开连线收口修正：当某参数行继续展开下级子树时，父级该行的竖线在本行中线收口，避免出现“应为 L 形却显示为 ├”的错连线。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [优化] 命令参数展开支持表达式递归查看：代码行参数值现可继续展开 `+` 表达式（显示“被加数或文本或字节集 / 加数或文本或字节集”），并可进一步展开嵌套命令参数（如 `到文本(d)` 展示“待转换的数据：d”）。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [优化] “正在生成C++代码”阶段提速与可观测性增强：项目元数据（全局变量/常量/资源/子程序/数据类型/DLL 命令）改为按项目指纹缓存复用，避免每次重复全量扫描；同时新增元数据阶段耗时日志。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 运行阶段进一步提速：新增可执行产物指纹缓存（源 `.cpp`、资源、静态库、目标平台/架构/模式等），命中时直接跳过编译与链接，复用上次 `exe`。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 转换阶段增量化：为 `.eyc/.egv/.ecs/.edt/.ell -> .cpp` 增加指纹缓存（源内容 + 编译上下文 + 断点 + 常量上下文），未变化文件直接复用已生成 `.cpp`，避免每次全量转换。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [修复] 消除文本字面量赋值告警：文本型变量接收 `L"..."` 字面量时改为走 `yc_value_to_text(...)` 复制，避免 `wchar_t*` 直接绑定字符串常量触发 `-Wwritable-strings`。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 支持库编译协议加载提速：改为按“已加载支持库 + 协议文件路径/时间戳/大小”签名复用缓存，仅在签名变化时重读协议，减少重复解析开销。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 运行模式编译提速：点击“运行”时编译器从 `O2` 调整为 `O0` 快速编译路径，减少日常调试等待时间；“编译(项目平台)”仍保持 `O2` 发布优化。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 数据类型命名统一：将“大数”更名为“大整数型”，编辑器类型补全与诊断按新名称展示；编译器与诊断仍兼容旧名称“大数”，避免历史工程立即失效。
  - 范围：`src/renderer/src/components/Editor/editorCoreUtils.ts`、`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`、`src/main/compiler.ts`
  - 验证：`get_errors`

- [修复] 工具栏图标按钮偶发需点击两次才响应：鼠标按下时立即执行按钮动作，并对后续 click 做去重，减少首次点击被焦点/上下文清理吞掉的情况，同时保留键盘触发能力。
  - 范围：`src/renderer/src/components/Toolbar/Toolbar.tsx`
  - 验证：`get_errors`

- [修复] 编辑期长整数溢出诊断与大数类型对齐：当赋值目标变量为“大数”时，不再错误提示“超出长整数范围”。
  - 范围：`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`

- [优化] 大数除零行为改进：大数除法/取模遇到除数为 0 时输出显式运行时错误（stderr），并在调试器附加时触发中断，避免静默返回造成误判。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 大数运算能力补齐：新增大数取模 `%`（含全角 `％`）与运行时 `yc_big_mod`，表达式翻译可在大数上下文下正确分流到大数运算。
  - 范围：`src/main/compiler.ts`、`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`

- [优化] 大数场景转换语义补齐：表达式翻译内建支持 `到文本(...)` 与 `到数值(...)`，其中大数目标上下文下 `到数值` 走 `yc_value_to_big(...)`，避免精度语义丢失。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [新增] 新增内置数据类型“大数”：编译后端引入 `YC_BIG` 任意精度整数实现，支持 `+ - * /` 与比较运算，并支持超出长整数范围字面量在大数上下文保真计算。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 编辑器类型体系补齐“大数”：内置类型补全新增“大数”，并将其纳入数值类型诊断集合（用于文本型赋值类型检查）。
  - 范围：`src/renderer/src/components/Editor/editorCoreUtils.ts`、`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`

- [修复] 编译后端文本变量拼接语义修复：表达式翻译新增变量类型感知，`文本型 + 文本型` 生成 `yc_text_concat(...)`，避免误生成为 `wchar_t*` 指针相加导致编译错误（如 `e = t1 + t2`）。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [新增] 编辑期类型诊断增强：当文本型变量直接接收数值型右值（如 `e = a + b`、`e = a`，且右值为数值型）时，问题面板立即报错并触发行内红色波浪线。
  - 范围：`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`

- [修复] EYC 文本模式编辑器接入 Monaco 诊断标记：问题面板诊断结果同步为编辑区红色波浪线（含错误/警告级别）。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`
  - 验证：`get_errors`

- [新增] 编辑期问题面板新增“长整数常量超范围”诊断：在代码书写阶段实时检查整数常量，超出 `[-9223372036854775808, 9223372036854775807]` 立即报错，无需等待编译。
  - 范围：`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`

- [修复] 生成代码编译失败收敛：`spec` 调试输出协议翻译补命令上下文（支持 `到文本(...)` 参与表达式转换），并修复“文本型变量接收数值表达式”时的类型不兼容（自动转换为文本）。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [修复] 表达式翻译增加整数字面量 64 位边界钳制，避免超长整数直接触发 C++ “integer literal is too large” 编译错误。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [修复] ycmd 后端接入校验补齐英文命令名匹配：`CommandSignatureDef` 保留 `englishName` 并在协议匹配时传入，修复仅配置 `commandEnglishName` 的协议被误判“未接入后端实现”（如 `spec.Trace`）。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [新增] `spec` 支持库最小后端协议映射：新增 `spec.protocol.json`，优先在编译期直连 `Trace/Verify/Delay`（分别对应输出调试、检查、中断延时）。
  - 范围：`lib/spec/spec.protocol.json`
  - 验证：`get_errors`

- [修复] 编译后端支持 `repeatable` 参数封送：ycmd 泛型命令调用在参数超出固定声明长度时，正确复用末尾可重复参数的数据类型，不再错误回退为“整数型”。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] ycmd 命令参数新增 `repeatable` 语义透传：主进程解析、支持库管理与前端提示链路已打通；调用格式与参数说明可显示“可重复追加”参数。
  - 范围：`src/main/ycmd-registry.ts`、`src/main/libraryManager.ts`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/editorCoreUtils.ts`、`src/renderer/src/components/Editor/editorCompletionCatalogUtils.ts`
  - 验证：`get_errors`

- [修复] 系统特殊功能支持库 `spec` 可追加参数命令补充 `repeatable` 标记，覆盖 `Trace/Verify/CallFunction/DelayCallFunction/GetText/DelayCallFunction_precisely`。
  - 范围：`lib/spec/spec.commands.ycmd.json`
  - 验证：`get_errors`

- [修复] 系统特殊功能支持库 `spec` 命令参数元数据全量对齐外部源码：补齐 `MachineCode/GetVarAddress/GetSubAddress/GetVarDataAddr/Verify/CallFunction/Delay/DelayCallFunction/AllocMem/FreeMem/ESysFunction/GetText/GetText_Set/GetText_Get/DelayCallFunction_precisely` 的参数定义（名称、类型、可省略）。
  - 范围：`lib/spec/spec.commands.ycmd.json`
  - 验证：`get_errors`

- [修复] 系统特殊功能支持库 `spec.Trace` 参数元数据补齐：`调试输出` 命令从“无参数”修正为首个可省略参数“欲输出值（通用型）”，与外部源定义对齐，恢复调用提示可读性。
  - 范围：`lib/spec/spec.commands.ycmd.json`
  - 验证：`get_errors`

- [优化] 可视化设计器组件箱图标视觉简化：移除图标外层矩形边框与底色，仅保留图标本体显示，降低视觉噪音。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [修复] 可视化设计器组件箱状态恢复竞态修复：启动时先完成本地状态读取与应用，再开启状态写回，避免重启后将“浮动”误覆盖为默认“停靠”。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`
  - 验证：`get_errors`

- [优化] 可视化设计器组件箱列表模式支持手动多列并保持：将“列表单列/多列”切换放到工具箱上方按钮区后，多列状态在缩窄宽度时仍保持生效（不自动退回单列）；同时新增组件箱状态/位置持久化（停靠左右、停靠宽度、浮动开关、视图模式、多列开关、浮动位置与尺寸）。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [修复] 可视化设计器组件箱两种显示模式的宽度联动修正：图标模式移除固定宽度锁死，支持停靠态拖拽调宽；列表/图标模式下组件项图标、字号、间距与卡片尺寸会随组件箱宽度同步缩放，不再固定大小显示。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器组件工具箱交互增强：固定停靠支持左右切换并可拖拽边缘调整宽度（右停靠从左边框拖拽、左停靠从右边框拖拽）；浮动模式新增容器边界检测，拖动与缩放均不会超出 IDE 设计器区域。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [修复] 第三方支持库组件缺失 `icon` 字段时图标兜底修正：改为统一显示“未知”图标，不再按组件中文名误回退到图片等其他图标；仅核心支持库保留名称映射兜底。
  - 范围：`src/renderer/src/components/Icon/Icon.tsx`、`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/assets/icons/QuestionMark.svg`
  - 验证：`get_errors`

- [修复] 工具箱“按钮”图标映射修正：将 `按钮` 与 `Button_16x.png` 从 `image-button` 改为 `button`，并接入 VS 图标库 `Button.svg`；同时修正主进程图标优先级为“组件 `icon` 字段优先、索引仅兜底”，避免被索引覆盖后显示错误。
  - 范围：`src/main/libraryManager.ts`、`src/renderer/src/components/Icon/Icon.tsx`、`src/renderer/src/assets/icons/Button.svg`
  - 验证：`get_errors`

- [修复] 组件定义中的公共属性彻底去重（含“窗口”组件）：`window-units.json` 不再内嵌 `标题/左边/顶边/宽度/高度/可视/禁止`，统一由主进程固定公共属性注入，避免属性面板重复与维护分叉。
  - 范围：`lib/krnln/window-units.json`、`src/main/libraryManager.ts`
  - 验证：`node -e "...TOTAL_COMMON_IN_JSON=0"`、`get_errors`

- [优化] 支持库组件图标改为组件 JSON 直连：核心支持库组件在 `window-units.json` 内为对应组件写入 `icon` 路径（如 `icon/BlueIcons/*_16x.png`），渲染层优先按组件元数据展示图标；核心库不再依赖 `icon.json` 索引文件，第三方支持库沿用组件元数据同口径接入。
  - 范围：`lib/krnln/window-units.json`、`lib/krnln/icon/BlueIcons/*`、`lib/krnln/icon/DarkIcons/*`、`lib/krnln/icon/WhiteIcons/*`、`src/main/libraryManager.ts`、`src/renderer/src/components/Icon/Icon.tsx`、`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/components/Editor/VisualDesigner.tsx`
  - 验证：`get_errors`

- [优化] 公共属性“鼠标指针”改为固定枚举下拉显示：按 `0~15` 显示为 `0.默认型`、`1.标准箭头型`…`15.自定义型`，并在组件属性面板中统一按枚举项选择。
  - 范围：`src/main/libraryManager.ts`、`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`

- [修复] 组件公共属性显示顺序修正：属性面板改为对所有窗口组件统一前置显示固定公共属性（左边/顶边/宽度/高度/标记/可视/禁止/鼠标指针），其后再显示组件自身属性；并对同名属性去重避免重复行。
  - 范围：`src/main/libraryManager.ts`
  - 验证：`get_errors`

- [优化] 组件公共属性与 lib2.h 对齐：参考 `elib/lib2.h` 固定属性定义，补齐默认窗口组件属性中的“标记(tag)”与“鼠标指针(MousePointer)”，并将“禁止”英文名对齐为 `disable`；同时补充无元数据场景下属性面板的对应编辑项。
  - 范围：`src/main/libraryManager.ts`、`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`

- [优化] 可视化设计器窗口默认居中并支持滚轮缩放：设计画布改为窗口居中展示，按住 `Ctrl`（macOS 为 `Command`）后滚轮可放大/缩小，普通滚轮保留滚动画布；画布聚焦后支持键盘 `+/-/0` 缩放控制并显示当前缩放百分比。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [修复] 修复可视化设计器最大化后窗体偏右与缩放显示不同步：将缩放百分比浮层移出居中布局流，恢复窗体始终居中；同时统一窗体最小可视尺寸与缩放壳尺寸计算，避免缩放后窗体背景与内部控件显示越界或留白。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器缩放与画布导航增强：缩放上限提升到 `1000%`；按住 `Ctrl/Command + 滚轮` 时以鼠标位置为焦点缩放（鼠标不在容器内时回退到中心缩放）；设计器引入固定大工作区并常驻横向/纵向滚动条；新增顶部与左侧像素标尺并随滚动与缩放联动。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器新增空格拖动画布视角：按住 `Space` 后左键拖拽可平移画布可见区域，无需手动拖滚动条；并增加 `grab/grabbing` 光标反馈。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器像素标尺细化：标尺改为每 `10` 单位绘制刻度线，并在每 `100` 单位显示数字文本。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器像素标尺层级优化：在每 `100` 单位长刻度与文本基础上，补充每 `50` 单位中刻度，提升读数分层。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器标尺密度自适应缩放倍率：按当前缩放自动计算刻度步长，随倍率动态调整刻度与文本密度，避免放大后过稀、缩小后过密。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`
  - 验证：`get_errors`

- [修复] 修复可视化设计器空格键误触发窗口最大化/还原：在设计器中对 `Space` 采用捕获阶段拦截默认行为，并在画布交互时主动归焦到画布区域，避免标题栏按钮保留焦点后被空格激活。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`
  - 验证：`get_errors`

- [优化] 可视化设计器视图状态持久化：按当前设计文件保存并恢复缩放倍率与画布滚动视角，重新打开后可回到上次浏览位置。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`
  - 验证：`get_errors`

- [优化] 资源管理器支持库查看树结构增强：在“数据类型”分组下新增窗口组件层级展示，支持展开到“属性/事件”子节点，并按组件图标展示，结构贴近易语言原生支持库浏览树。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`

- [优化] 支持库提示面板增强：点击支持库树中的命令/数据类型/属性/事件节点后，在固定提示面板展示对应说明信息；事件节点可显示“事件名称 + 触发说明 + 无返回值提示”。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/components/Sidebar/Sidebar.css`
  - 验证：`get_errors`

- [修复] 支持库提示面板位置修正：支持库树点击说明不再显示在侧栏内部，改为写入底部“输出/终端/提示/问题/调试”区域中的“提示”面板，并自动切换到该标签。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/App.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/Sidebar/Sidebar.css`
  - 验证：`get_errors`

- [修复] 支持库命令分类恢复：修正 ycmd 解析层将命令分类固定为 `ycmd` 的问题，核心支持库命令现可按“流程控制/算术运算/逻辑比较/磁盘操作/数组操作/文本操作”等分类在支持库树中分组展示。
  - 范围：`src/main/ycmd-registry.ts`
  - 验证：`get_errors`

- [优化] 支持库命令提示改为完整模板：点击命令节点后，提示面板按“调用格式/英文名称/详细说明/参数说明/操作系统支持”输出；并对“如果(ife)”提供与原生风格一致的详细说明文本。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`

- [新增] 新增系统特殊功能支持库 `spec`（独立于核心支持库）：基于开发期外部 `D:\chungbin\ycIDE-html\支持库源码\spec` 命令定义离线生成项目内支持库清单，运行时仅读取 `lib/spec` 产物。
  - 范围：`lib/spec/spec.library.json`、`lib/spec/spec.commands.ycmd.json`、`lib/spec/impl/windows.cpp`、`scripts/krnln/import-library-metadata.mjs`
  - 验证：`node scripts/krnln/import-library-metadata.mjs`、`get_errors`

- [优化] 核心支持库命令说明与分类改为帮助说明对齐：新增离线导入脚本解析 `系统核心支持库帮助说明文件.txt` 并同步 `krnln.commands.ycmd.json` 的摘要、分类、参数说明和平台支持字段。
  - 范围：`lib/krnln/系统核心支持库帮助说明文件.txt`、`lib/krnln/krnln.commands.ycmd.json`、`lib/krnln/krnln.library.json`、`scripts/krnln/import-library-metadata.mjs`
  - 验证：`node scripts/krnln/import-library-metadata.mjs`、`get_errors`

- [修复] 资源管理器支持库树提示去硬编码：移除命令与组件说明的固定解释模板（含“如果”特判说明），提示内容改为优先使用支持库元数据，仅保留“暂无说明。”最小回退。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/main/ycmd-registry.ts`、`src/main/libraryManager.ts`
  - 验证：`get_errors`

- [新增] 新建子程序自动补局部变量：在新子程序声明下方自动插入 `.局部变量 局_变量N, 整数型`，变量名按 `局_变量1` 递增并避免重名。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [修复] 右键菜单可用态修正：未改动时“撤销/重做”置灰；无选中时“复制/剪切”置灰；剪贴板无文本时“粘贴”置灰，并同步限制助记键仅在可用态触发。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [优化] 调整新建类快捷键：将“新建文件/新建项目”改为 `Ctrl+Alt+N` / `Ctrl+Alt+Shift+N`，并将 `Ctrl+N` 让给“新建子程序”（含标题栏与原生菜单显示同步）。
  - 范围：`src/shared/shortcut-config.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/main/index.ts`
  - 验证：`get_errors`

- [修复] 修复右键菜单“新建子程序”高亮闪烁错位：新建后闪烁改为仅命中 `tr.eyc-data-row` 表格行，并在表格渲染就绪前短重试，避免过渡重渲染阶段闪到文本节点。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [修复] 修复 EYC 右键菜单助记键缺失：补齐 `N` 新子程序单键触发，并同步补全 `E/I/K/G/B` 等字母前缀项的菜单打开态单键执行。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [优化] EYC 表格编辑器右键菜单补充单键助记触发：菜单打开后可直接按 `U/Y/C/T/P/F` 执行“撤销/重做/复制/剪切/粘贴/全选”，并同步更新菜单文案前缀为 `U.撤销` 等助记格式。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [新增] EYC 表格编辑器右键菜单支持定制化分组与动作：新增“新子程序/删除行/插入新行/编译当前行/屏蔽/解除屏蔽”菜单项，并按要求调整分隔顺序与快捷键文案展示。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [修复] 补齐文件夹工作区关闭入口：文件菜单新增可用“关闭工作区”项（复用 `file:closeProject` 关闭流程），文件夹工作区下不再缺少关闭菜单。
  - 范围：`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/renderer/src/App.tsx`、`src/main/index.ts`
  - 验证：`get_errors`

- [修复] 修复 EYC 文本模式 `Ctrl+V` 无法粘贴：全局快捷键在 Monaco/contenteditable 可编辑焦点下不再拦截编辑类快捷键，恢复原生粘贴行为。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [优化] 文件保存改为按当前文件编码写盘：`file:save` 新增编码参数，编辑器保存/保存全部/项目保存/外部冲突“保留IDE内容”均按状态栏当前编码落盘（默认 UTF-8）。
  - 范围：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [修复] 修复“按 GBK 重新打开后误提示文件被外部修改”：外部变更检测改为按当前文件已选编码读取磁盘内容，再与标签保存态比较，避免 UTF-8 误解码导致的误报（ASCII 文件如部分 `.h` 原本不触发误报）。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`
  - 验证：`get_errors`

- [修复] 修复 IDE 主窗口尺寸未持久化：调整窗口大小/位置后会写入主窗口状态，重启时恢复上次宽高、位置与最大化状态，不再回到默认尺寸。
  - 范围：`src/main/index.ts`
  - 验证：`get_errors`

- [新增] 状态栏右下角新增当前活动文件编码显示，并支持点击后选择编码重新打开：新增编码菜单（UTF-8/UTF-8 BOM/GB18030/GBK/Big5/Shift_JIS/UTF-16LE/UTF-16BE/Windows-1252），选择后按指定编码重新读取当前文件。
  - 范围：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/StatusBar/StatusBar.tsx`、`src/renderer/src/components/StatusBar/StatusBar.css`
  - 验证：`get_errors`

- [修复] 修复文件夹工作区文件误判为文件夹导致“双击打不开”问题：目录读取接口对非目录改为返回 `null`，前端树构建可准确区分目录与文件，`button.cpp`、`button.h` 等文件双击可正常打开。
  - 范围：`src/main/index.ts`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [优化] 文件夹工作区文件节点图标改为扩展名占位语言图标：为 `.cpp/.h/.ts/.js/.json/.md` 等文件按扩展名显示占位图标，不再显示文件夹图标。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`

- [优化] 状态栏新增工作区模式标识：当存在工作区时，右侧显示“项目模式: 名称”或“文件夹工作区: 名称”，便于区分当前上下文并避免误判运行能力。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/StatusBar/StatusBar.tsx`、`src/renderer/src/components/StatusBar/StatusBar.css`
  - 验证：`get_errors`

- [新增] 新增“打开文件夹工作区”入口：在保留现有“打开项目”流程不变的前提下，新增独立菜单/快捷键入口用于选择文件夹；若根目录存在 `.epp` 自动按项目打开，否则按普通文件夹工作区打开并显示目录树。
  - 范围：`src/shared/eprojectImport.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/shortcut-config.ts`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [修复] 修复单文件打开无响应：`buildTabFromPath` 中文本模式回退分支被提前返回拦截，导致 `.cpp` 等非项目文件无法打开；现已恢复为默认文本模式打开。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [优化] 补齐单文件“另存为”能力：新增 `file:saveAs` 菜单动作与保存对话框 IPC，未命名文件执行保存时自动进入“另存为”流程；关闭未命名且已修改标签时选择“保存”也会触发保存为后再关闭。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/shortcut-config.ts`
  - 验证：`get_errors`

- [优化] 支持单文件编辑工作流：新增“新建文件/打开文件”入口（菜单、快捷键、工具栏），可直接打开非项目文本文件（如 `.cpp`、`.py`、`.md`）进行编辑；当当前活动标签为非项目文件时，运行按钮自动禁用，避免误以为可调试运行。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Toolbar/Toolbar.tsx`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/shortcut-config.ts`
  - 验证：`get_errors`

- [优化] 项目管理改为单项目模式：当已有项目打开时再次打开新项目，会先执行当前项目关闭流程（含未保存确认与保存），再加载新项目，避免调试/运行时项目上下文混淆。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [优化] 代码行输入体验优化：支持将常见中文标点自动转为英文符号；当以中文单引号 `‘/’` 起始输入时自动转换为注释前缀 `'`，并在注释符后自动补一个空格，便于后续自动排版。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS `SendMessage` 语义扩展：新增 `WM_MOVE/WM_CLOSE/WM_GETTEXT/WM_GETTEXTLENGTH` 处理分支，补齐位置回写、关闭状态收敛与文本读取返回值，提升跨平台消息调用的一致性与可验证性。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 消息循环与游标兼容入口补齐：新增 `krnln_DoEvents`（冲刷状态化消息队列）及 `krnln_SetWaitCursor/krnln_RestroeCursor`，对位 Windows 侧同名导出，减少跨平台命令调用断档。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 组件消息语义补齐：`SendMessage/PostMessage` 从占位返回升级为状态化消息分发与队列机制（Post 入队、Send 先冲刷后同步分发），并补充 `WM_ACTIVATE/WM_SETFOCUS/WM_KILLFOCUS/WM_ENABLE/WM_SIZE/WM_SETTEXT` 对应状态回写，降低跨平台组件命令“有符号无行为”的差异。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 组件与窗口命令映射继续补齐：新增 `IsCreated/CreateWindowUnit/destroy/Activate/SetFocus/IsFocus/GetClientWidth/GetClientHeight/GetWidth/GetHeight/enable/IsEnabled/move/ZOrder/SetParentWnd/GetHWnd/SetText/GetText` 及重绘/消息相关导出，避免编译链路在非 Windows 平台落到“无实现符号”。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 平台实现继续补齐：Linux 端补上 `PutEnv`、屏幕尺寸/鼠标坐标查询与消息框按钮语义（优先 `zenity`，失败回退控制台）；macOS 端补上 `MsgBox/msgbox` 的按钮类型与返回码映射，不再忽略 `buttons` 参数。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 平台参数语义继续补齐：`krnln_run` 现已处理 `showMode`（隐藏模式重定向到 `/dev/null`，异步模式后台执行）；macOS `MsgBox/msgbox` 接入 `parentWindow` 参数（优先激活父窗口后再弹框），减少参数形同虚设的问题。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 命令映射实现补齐（非消息框）：新增 `CurDir/ChDir/MkDir/RmDir/FileCopy/FileMove/kill/name/IsFileExist/FileLen/GetAttr/SetAttr/GetTempFileName/ChDrive/GetDiskTotalSpace/GetDiskFreeSpace/GetDiskLabel` 的平台实现，文件与目录命令改为真实文件系统行为，磁盘空间命令改为 `statvfs` 回读。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库内置不可分离命令继续协议化：将 `输出调试文本/暂停/检查` 并入 `commandBindings`（新增 `emitBuilder`），并移除 `compilerCommandResolvers` 中对应硬编码；当前解析器仅保留流程兜底，不再承载核心命令规则。
  - 范围：`lib/krnln/window-units.json`、`src/main/compiler.ts`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`

- [优化] 核心支持库协议化第三批收敛：将 `比较/逻辑/磁盘与目录/文件检索与临时文件/写到文件` 命令并入 `commandBindings`（新增 `exprOp/exprBuilder` 协议字段），并从 IDE 内置命令解析表剥离同名硬编码；语句上下文在无 `emit` 时可自动复用协议 `expr` 生成 `(void)` 调用。
  - 范围：`lib/krnln/window-units.json`、`src/main/compiler.ts`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`

- [优化] 协议驱动第二批下沉：新增 `改变驱动器/改变目录/字节集与指针转换` 等命令到核心支持库 `commandBindings`，并同步从 IDE 内置命令表移除对应实现，进一步降低编译器对命令名硬编码依赖。
  - 范围：`lib/krnln/window-units.json`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`

- [优化] 协议驱动继续收敛：已在核心支持库 `commandBindings` 下沉的命令（如 `信息框/标准输出/调试输出/取本机名/是否为调试版/文件与字节集首批命令`）从 IDE 内置命令表移除，确保编译优先且实际依赖支持库协议而非本地硬编码。
  - 范围：`src/main/compilerCommandResolvers.ts`、`src/main/compiler.ts`、`lib/krnln/window-units.json`
  - 验证：`get_errors`

- [优化] 支持库编译协议新增表达式绑定能力：`commandBindings` 支持 `expr` 字段，编译器在表达式翻译与后端路径校验中优先读取协议绑定；核心支持库已下沉首批命令映射（含 `信息框/标准输出/调试输出/取本机名/是否为调试版/文件与字节集常用命令`）。
  - 范围：`src/main/compiler.ts`、`lib/krnln/window-units.json`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`

- [优化] 编译器命令内置表模块化：将 `compiler.ts` 中的内置命令表达式/语句生成器与命令分发逻辑抽离到独立模块，主编译文件仅保留流程编排与接口调用，降低第三方支持库接入时对主编译文件的耦合感知。
  - 范围：`src/main/compiler.ts`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`

- [修复] 信息框窗口标题为空时默认文案修正：统一回退为“信息框”，不再出现错误标题文案。
  - 范围：`src/main/compiler.ts`、`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [优化] 核心支持库数据/对象日期与属性写入去随机化：将 `createRuntimeObject/SetData/GetDateTime/RunMethod/RunDateMethod/GetDateProperty/SetExtra/SetFontAttr/SetItemData/SetProperty` 改为确定性状态迁移（按游标、行列、画布操作数与对象记录回写），减少 tick/随机依赖导致的回归测试波动。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库绘图与资源 getter 语义继续补齐：将 `InvertRect/GetFont/GetHDC/GetPicHeight/GetPicWidth` 从占位返回改为运行时状态驱动（画布反色、字体对象句柄复用/创建、绘图上下文签名、图片尺寸随画布边界回读），提升图形链路可观察性与一致性。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库绘图命令语义补齐：将 `DrawRect/FillRect/LineTo/DrawPic/DrawJBRect/ArcTo/chord/ellipse/pie/polygon/RoundRect/GetPixel/SetPixel` 从空触发改为运行时画布状态驱动，统一记录最近绘图操作、边界、点位与颜色，支持后续通过 `GetPixel` 与编辑状态字段观察绘图副作用。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库数据集游标与对象属性语义继续补齐：增强 `connect/CloseConnect/ExecuteSql/AddLine/AddString/Append/AppendRow/InsertCol/InsertRow/LoadDS/LoadDSFile/RemoveCol/RemoveRow/Requery/RunBoolMethod/GetNumProperty` 的状态联动，统一行列最小约束、`currentRow/caretRow/caretCol` 边界收敛与对象句柄回读，减少“恒成功但状态漂移”的伪语义行为。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库对象命令语义补齐：引入运行时对象句柄表，打通 `CreateArray/CreateFontDispObj/CreatePicDispObj/CreateObject/QueryInterface/RunMethod/RunNumMethod/RunTextMethod/RunDateMethod/RunObjectMethod/RunVariantMethod/GetObject/GetObjectProperty/SetType/ViewObjInf` 的同一对象状态读写，避免随机句柄与不可追踪返回值。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [修复] 核心支持库自动桩区重复定义与状态一致性修复：去除重复函数定义（`SelectAll`、`SetAlignMode`），并补齐 `CloseRecordset/Clear/CloseClient/cls/EmptyCell/EndDoc/GetTextColor` 的状态化语义，保证编辑/打印/网络状态收敛与读写一致。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库编辑/打印/网络/数据游标语义继续补齐：将 `` 等从随机值或恒成功占位改为共享运行时状态驱动；并统一 `Get* / Set*` 行为一致性（同一状态可读回）。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库事务/保存命令语义补齐：将 `connect/CloseConnect/query/select/ExecuteSql/OpenMDB/OpenSqlServerDB/BeginTrans/CommitTrans/RollbackTrans/SaveChange/SaveDS/SaveDSCell/SaveDSCellFile/SaveDSFile/say` 从固定成功返回调整为基于运行时连接与事务状态的行为（含失败分支），避免无脑 `return 1` 掩盖错误。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库 Windows 桩清零收官：继续对自动桩区进行连续替换，将剩余命令全部转为可执行实现（含控制流占位、组件/数据集接口、绘图与对象调用接口等的兜底实现），并完成映射统计闭环。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库 Windows 桩实现连续补齐（文件句柄/通用命令/对象辅助）：新增 `` 等实现，并修复映射统计中的 `undeclared` 回归。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=264 / stubbed=248 / undeclared=0`）

- [优化] 核心支持库 Windows 文本命令簇补齐：新增 `RpSubText/space/string/pstr/StrToUTF8/UTF8ToStr/StrToUTF16/UTF16ToStr` 实现，覆盖子串替换、重复文本、指针到文本与 UTF8/UTF16 基础转换能力。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=185 / stubbed=327 / undeclared=0`）

- [优化] 核心支持库 Windows 时间命令簇补齐：新增 `TimeChg/TimeDiff/GetDaysOfSpecMonth/TimeToText/TimePart/year/month/day/WeekDay/hour/minute/second` 实现，统一复用 OLE 日期转换链路并补齐常见时间增减/提取语义。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=177 / stubbed=335 / undeclared=0`）

- [优化] 核心支持库 Windows 桩实现继续补齐：新增 `SetIntInsideBin/ReadFile/WriteFile/QJCase/BJCase/str/GetSpecTime/SetSysTime/UNum/NumToRMB/NumToText/FileDateTime` 的可运行实现，覆盖文件字节读写、全半角转换、数值格式化与文件时间读取路径。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=165 / stubbed=347 / undeclared=0`）

- [优化] 核心支持库高风险字节集命令补齐一批 Windows 实现：完成 `BinLen/ToBin/BinLeft/BinRight/BinMid/InBin/InBinRev/RpBin/RpSubBin/SpaceBin/bin/pbin/p2int/p2float/p2double/GetIntInsideBin` 的语义实现；并修正映射脚本对自动桩区的判定逻辑，改为按函数体内容识别“真实桩/真实实现”，避免仅按区块误判。
  - 范围：`lib/krnln/impl/windows.cpp`、`scripts/krnln/generate-command-mapping.mjs`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`node --check scripts/krnln/generate-command-mapping.mjs`、`npm run krnln:mapping:generate`（`implemented=153 / stubbed=359 / undeclared=0`）

- [新增] 核心支持库 ABI 契约进入实施版 v1：在 `windows.cpp` 落地 OLE Automation Date 适配基础，并新增 `ToTime/now/GetDatePart/GetTimePart` 真实实现（移出自动桩区）；同时将契约与映射规范升级到 v1，映射产物新增 `abiSpecVersion` 与 `abiContractRef` 字段用于高风险命令追踪。
  - 范围：`lib/krnln/impl/windows.cpp`、`docs/krnln-abi-contract.md`、`docs/krnln-command-mapping.md`、`scripts/krnln/generate-command-mapping.mjs`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`node --check scripts/krnln/generate-command-mapping.mjs`、`npm run krnln:mapping:generate`

- [新增] 启动核心支持库 ABI 约定与命令映射实现：新增 `krnln-abi-contract.md` 与 `krnln-command-mapping.md` 讨论版规范，并落地自动映射脚本 `scripts/krnln/generate-command-mapping.mjs`，可生成 `command-mapping.windows.json` 与映射报告，首轮统计 `total=512 / implemented=133 / stubbed=379 / undeclared=0`。
  - 范围：`docs/krnln-abi-contract.md`、`docs/krnln-command-mapping.md`、`docs/krnln-command-mapping-report.md`、`scripts/krnln/generate-command-mapping.mjs`、`lib/krnln/generated/command-mapping.windows.json`、`package.json`
  - 验证：`node --check scripts/krnln/generate-command-mapping.mjs`、`npm run krnln:mapping:generate`

- [优化] 核心支持库 Windows 命令语义批量落地：将一批高频基础命令从默认桩替换为真实实现，覆盖数学运算（`mod/abs/round/pow/sin/cos/tan/...`）、位运算（`band/bor/bxor/shl/shr/...`）、比较逻辑（`equal/notEqual/less/...`）、文本基础处理（`len/left/right/mid/InStr/UCase/LTrim/...`）与数值转换（`val/ToInt/ToLong/hex/binary/...`）；并修正自动桩生成逻辑为“扫描时排除桩区”，确保仅为未实现命令保留桩函数。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [新增] 一次性补齐核心支持库 Windows 全量导出符号层：根据 `krnln.commands.ycmd.json` 自动生成并落地 443 个缺失 `krnln_*` 导出桩函数，统一按返回类型给默认返回值，避免编译/链接阶段缺符号报错。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [新增] 补全核心支持库 Windows 磁盘命令实现：新增 `ChDrive/GetDiskTotalSpace/GetDiskFreeSpace/GetDiskLabel/SetDiskLabel`，支持按驱动器查询容量与卷标，并可切换当前驱动器。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [新增] 补全核心支持库 Windows 文件系统常用命令实现：新增 `CurDir/ChDir/MkDir/RmDir/FileCopy/FileMove/kill/name/IsFileExist/FileLen/GetAttr/SetAttr/GetTempFileName` 平台实现，覆盖目录切换、递归建删目录、文件复制移动删除、属性读写与临时文件名生成。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [优化] 完善核心支持库 Windows 平台细节实现：`run` 增加显示模式标准化与等待失败处理，系统版本查询优先使用 `RtlGetVersion` 降低新系统误判，`GetAppName` 按类型返回完整路径/主文件名/文件名(含扩展名)/运行目录。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [新增] 完善核心支持库跨平台基础实现：`linux.cpp` 与 `macos.mm` 从占位实现扩展到可用基础命令，实现消息框/鸣叫/延时/运行命令/取执行文件与运行目录/环境变量读写/命令行读取/系统版本与语言等通用入口。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] Windows 运行产物启动失败日志增强：`spawn` 失败场景新增“疑似被安全软件拦截”提示，并输出输出目录引导信息，降低 `spawn EPERM` 排障成本。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [新增] 新增误报申诉包收集脚本：支持收集样本路径、SHA-256、构建上下文（编译器/目标/模式）与附加日志，统一生成 `debug_logs/av-report-*/report.json` 与附件目录。
  - 范围：`scripts/security/collect-antivirus-report.mjs`、`package.json`、`README.md`
  - 验证：`node --check scripts/security/collect-antivirus-report.mjs`

- [新增] 新增 Windows 签名打包入口：增加 `package:win:signed`，通过环境变量注入证书参数（含强制签名开关），为发布链路接入代码签名提供落地脚本。
  - 范围：`scripts/security/package-win-signed.mjs`、`package.json`、`README.md`
  - 验证：`node --check scripts/security/package-win-signed.mjs`

- [修复] 修复 ycmd 命令被编译前置校验一刀切拦截的问题：签名校验阶段改为仅在“无协议映射、无内建命令生成器、无内建表达式生成器、且非同名子程序”时才报未接入后端实现，从而解除 `信息框` 等已接入命令的误拦截。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [新增] 核心支持库 Windows 平台实现扩容：在 `impl/windows.cpp` 批量落地系统命令与组件基础操作（消息框、蜂鸣、延时、取屏幕/鼠标、环境变量、剪贴板、运行命令、取程序路径、窗口创建/销毁/激活/焦点/尺寸/文本/重画等），并增加命令命名兼容入口。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [优化] 核心支持库 Windows 平台实现按命令ID对齐：补充 `krnln.MsgBox` 对应导出入口 `krnln_MsgBox`（并兼容 `krnln_msgbox` / 旧 `krnln_message_box`），统一走同一消息框实现，支持按钮样式与父窗口句柄参数。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [新增] 核心支持库 Windows 平台实现起步：`impl/windows.cpp` 的 `krnln_message_box` 从占位返回改为真实调用 WinAPI `MessageBoxA`，并补充空文本/空标题入参兜底，打通首个可用平台命令实现。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [优化] 核心支持库窗口元数据与编译协议合并为单文件：`window-units.json` 采用 `windowUnits` 组件内聚结构，每个组件项内直接承载类名/样式与事件 `channel/code` 映射，不再使用顶层 `controlBindings/eventBindings` 重复节点；编译器协议加载改为优先读取该文件，并保留对旧 `*.protocol.json` 的兼容回退。
  - 范围：`lib/krnln/window-units.json`、`src/main/compiler.ts`、`src/main/libraryManager.ts`、`lib/krnln/README.md`
  - 验证：`get_errors`

- [优化] 核心支持库完整性校验改为仅在稳定正式版强制执行：开发环境与 `beta/rc/dev` 预发布版本启动时跳过校验，避免开发阶段频繁改动核心支持库后需要同步更新哈希。
  - 范围：`src/main/libraryManager.ts`
  - 验证：`get_errors`

- [新增] 支持库管理接入在线索引和独立 zip 安装：默认读取 `https://ycide.dev/libraries/index.json`，按支持库 ID 下载独立包，校验 SHA-256 后安装到用户数据目录，并在支持库管理窗口显示下载、安装、加载、可更新和失败状态。
  - 范围：`src/main/libraryManager.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/library-store.ts`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.css`
  - 验证：`get_errors`、`npm run build`、`npm run libraries:package`

- [新增] 系统设置新增支持库在线索引地址配置，便于后续切换官网源或镜像源。
  - 范围：`src/shared/settings.ts`、`src/renderer/src/components/SettingsDialog/SettingsDialog.tsx`
  - 验证：`get_errors`、`npm run build`

- [优化] 编译命令签名表改为消费 LibraryManager 的已加载支持库快照，支持本地安装目录覆盖内置支持库，并避免未加载支持库参与编译签名解析。
  - 范围：`src/main/compiler.ts`、`src/main/libraryManager.ts`
  - 验证：`get_errors`、`npm run build`

- [新增] 新增支持库发布打包脚本，可将 `lib/<id>` 生成 `packages/<id>.zip` 和 `index.json`，适配官网独立支持库包部署结构。
  - 范围：`scripts/package-libraries.mjs`、`package.json`、`package-lock.json`
  - 验证：`npm run libraries:package`

- [优化] 支持库管理详情改为合并读取支持库标识文件，内置支持库新增 `<id>.library.json` 示例，并移除详情中的邮政编码、通信地址、电话号码展示，方便第三方支持库提供签名、作者、联系方式和主页等标识信息。
  - 范围：`src/main/libraryManager.ts`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`、`lib/krnln/krnln.library.json`、`lib/krnln/README.md`
  - 验证：`get_errors`、`npm run build`

- [优化] 固化核心支持库发布与校验规则：核心支持库只随 ycIDE 版本打包发布，不进入服务器支持库索引/zip 包，不允许远程安装、卸载或禁用；启动时校验核心支持库关键文件和清单完整性，缺失、损坏或被修改时直接报错。
  - 范围：`src/main/libraryManager.ts`、`src/main/index.ts`、`scripts/package-libraries.mjs`、`lib/krnln/README.md`
  - 验证：`get_errors`、`npm run build`、`npm run libraries:package`

- [优化] 支持库命令、数据类型与窗口组件元数据按顶部工具栏目标平台过滤：切换 Windows/macOS/Linux 后，不支持当前目标平台的支持库不会参与编辑器命令表、设计诊断或编译签名解析，支持库管理中同步显示“当前平台禁用”。
  - 范围：`src/main/ycmd-registry.ts`、`src/main/libraryManager.ts`、`src/main/compiler.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`
  - 验证：`get_errors`、`npm run build`

- [优化] 支持库管理改为独立窗口打开，并扩大可用区域到主题管理器同级窗口尺寸，提升支持库列表、在线安装状态和详情信息的浏览空间。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.css`
  - 验证：`get_errors`、`npm run build`

- [修复] 清理主题管理器在 VS Code Problems 面板中的告警：移除 JSX 内联样式、补齐重命名与流程线配置输入框的可访问标签，并改用主题 token 替代 `color-mix` 兼容性较低的样式。
  - 范围：`src/renderer/src/components/ThemeManager/ThemeManager.tsx`、`src/renderer/src/components/ThemeManager/ThemeManager.css`
  - 验证：`get_errors`、`npm run build`

- [优化] 依据导出的易语言核心支持库帮助说明，直接补全核心支持库清单信息：更新 `krnln.library.json` 为正式作者/签名信息，并新增首批流程控制与算术运算命令 `*.ycmd.json` 元数据。
  - 范围：`lib/krnln/krnln.library.json`、`lib/krnln/*.ycmd.json`
  - 验证：`get_errors`

- [优化] 核心支持库命令清单改为单文件集合结构：新增 `krnln.commands.ycmd.json` 承载多命令数组，新增 `krnln.constants.json` 作为常量独立文件，并让 ycmd 注册表兼容集合与单命令两种格式。
  - 范围：`lib/krnln/krnln.commands.ycmd.json`、`lib/krnln/krnln.constants.json`、`src/main/ycmd-registry.ts`、`src/main/libraryManager.ts`
  - 验证：`get_errors`、`npm run build`
### v0.0.3-beta.54（2026-05-09）
- [优化] 优化 AI 面板停止按钮视觉：移除停止按钮红色外边框，仅保留错误语义色图标与背景高亮，避免边框过重。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复 Icon 组件在 Problems 面板中的 inline style 告警：移除 JSX `style` 属性，改为通过 ref 在运行时设置宽高，保持 `size` 行为不变。
  - 范围：`src/renderer/src/components/Icon/Icon.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树双击文件名无法打开文件：文件节点打开路径改为优先使用真实文件名（含扩展名），并统一“模块/窗口/资源”文件节点的双击与回车打开行为。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复粘贴代码产生重复子程序：当粘贴内容中的子程序名已存在于当前文件时，不再插入重复子程序头，改为将该子程序体内容追加到现有子程序末尾。
  - 范围：`src/renderer/src/components/Editor/editorPasteUtils.ts`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 新增重复子程序诊断：手工输入同名 `.子程序` 时，问题面板会报“子程序重复定义”错误。
  - 范围：`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 清理 VS Code Problems 面板中的 OutputPanel/AIAssistantPanel/VisualDesigner 报错：移除内联样式、补齐菜单与表单可访问性属性、改为 CSS 变量驱动动态尺寸与定位，并补齐 Safari 的 `-webkit-user-select` 兼容前缀。
  - 范围：`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`、`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`（改动文件无报错）
- [优化] 调整标题栏拖拽区域设置方式：移除 CSS 中的区域声明，改为在运行时通过 `-webkit-app-region` 写入拖拽/禁拖拽属性，降低 Problems 面板兼容性提示噪音并保持 Electron 标题栏行为。
  - 范围：`src/renderer/src/components/TitleBar/TitleBar.css`、`src/renderer/src/components/TitleBar/TitleBar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [优化] 优化 AI 面板模式下拉宽度：加宽模式选择框，避免选择 Agent 时文本末尾被遮挡。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`
  - 验证：`get_errors`（改动文件无新增错误）
- [修复] 修复 AI 模型配置持久化被覆盖：模型配置弹窗保存后同步更新 App 内存设置（模型/DeepSeek Key/GLM Key/自定义模型），避免后续保存其它设置时将已保存 Key 覆盖回空值。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复 Agent 模式错误提示显示位置：当执行失败（如 API Key 缺失）时，错误信息优先追加到 AI 返回区，不再只显示在输入框下方状态栏。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复 AI 面板点击发送后偶发“无返回”问题：为聊天/流式聊天 IPC 调用补充异常兜底，发生调用异常时返回明确错误而非卡住等待。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复主进程 `file:readDir` 对文件路径抛出 `ENOTDIR`：读取前新增目录类型校验并统一异常降级为空列表，避免日志刷屏及上层调用被异常打断。
  - 范围：`src/main/index.ts`
  - 验证：`get_errors`（改动文件无报错）
- [新增] 项目树支持项目级右键菜单：在项目根节点可单独执行“保存该项目全部文件”“关闭该项目”，用于多项目并行时按项目粒度管理。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复多项目打开时项目树被后开项目覆盖：项目根节点改为按项目目录生成唯一 ID，打开/新建项目时按末尾追加（同项目则替换原根），刷新项目树仅替换当前项目对应根节点。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [优化] 项目树未保存标记支持分类联动：当“窗口/程序集/全局变量/常量表/自定义数据类型/DLL命令/资源”等分类下任一文件存在未保存修改时，分类节点也显示右侧未保存圆点。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复可视化设计器编辑后项目树未实时显示未保存标记：`handleFormChange` 增加 `onOpenTabsChange` 同步，确保 Sidebar 使用到最新 `.efw` 标签内存状态计算 dirty。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树未显示窗口/源码文件未保存标记：项目树 dirty 匹配键改为优先使用节点文件 ID（含扩展名），不再误用显示标签（无扩展名），使 `.efw/.eyc/...` 的“未保存圆点”能正确显示在右侧边框内侧。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复表格模式流程线在 `如果/如果真` 连续块中的误连通：`如果结束/如果真结束` 后紧跟 `如果/如果真` 时，不再套用“判断”专用的上下双横线桥接效果。
  - 范围：`src/renderer/src/components/Editor/eycFlow.ts`、`tests/contract/eyc-flow-contract.spec.mjs`
  - 验证：`node --test tests/contract/eyc-flow-contract.spec.mjs`
- [修复] 修复资源管理器双击子程序偶发跳错行：编辑导致行号漂移后，项目树双击子程序改为按子程序名实时定位（失败时回退旧行号），不再闪到新输入代码行。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复删除子程序后项目树残留旧节点：编辑器内容变更会实时同步 `.eyc` 子程序节点到项目树，未保存状态下删除 `.子程序` 也会立即从“程序集”树中消失。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复属性面板事件下拉勾选状态不刷新：事件列表“✓ 已存在子程序”改为优先读取当前打开标签页内存内容，并用最新解析结果覆盖缓存；删除子程序后勾选会即时取消。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树双击子程序后落点行为：闪烁定位保持在子程序声明行不变，同时将输入光标置于该子程序首个普通代码行的行首（如子程序在第17行、首行代码在第18行则光标落到18行首）。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树双击子程序后“仅闪烁无光标”问题：将光标进入时机调整为闪烁定位之后并增加重试聚焦，避免滚动重排冲掉编辑态；当子程序内仅空白普通行时也可在行首显示光标。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复双击子程序后输入框未渲染导致“仍看不到光标”：纠正 `startEditLine` 调用中的 `isVirtual` 参数，避免与普通代码行渲染条件不匹配而隐藏编辑输入框。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序内“空行优先”落点：当子程序声明后先有若干空行再有命令时，双击项目树后光标改为落在首条空普通行行首，不再跳到后续命令行。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复可视化设计器创建子程序后的“无行号空行”问题：新建子程序时直接生成真实空普通行；项目树跳转到无普通行子程序时会自动物化一条空普通行并聚焦行首，避免必须手动回车后才正常。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 统一“已存在子程序”导航逻辑：属性面板事件下拉选择与可视化设计器双击组件进入默认子程序时，行为改为与项目树双击一致（子程序声明行高亮闪烁 + 光标定位到子程序首个普通行首列，并在缺失普通行时自动物化空普通行）。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序跳转抖动与多点闪烁：去除 App 层对子程序导航的多重重试调用，并在编辑器侧为子程序导航增加“最后一次生效”的序列去重，改为单次闪烁 + 单次聚焦，避免上下抖动与多个位置依次高亮。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复“组件双击/属性面板事件下拉”跳转闪烁偏位：将 `navigateOrCreateSub` 的“已存在子程序”分支改为复用 `navigateToSubprogram` 同一内部实现，统一三入口（项目树/组件双击/事件下拉）的定位与闪烁规则，并补齐子程序名 trim 匹配。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序声明行闪烁偶发错位：统一跳转内部逻辑新增“优先行号命中同名子程序”策略（组件双击/事件下拉传入已知声明行），并在新闪烁前清理旧 `highlight-flash` 样式，避免残留闪烁造成错位错觉。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序跳转后“先在下方后瞬间居中”导致的闪烁位置偏差：声明行定位由 `center` 调整为 `nearest`，并移除光标聚焦阶段的额外滚动，避免二次跳动造成闪烁观感错位。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序跳转时“滚动未稳定先闪烁”导致的错位：声明行高亮改为在两帧 + 短延时后触发（滚动收敛后再闪烁），并继续受导航序列去重保护，避免瞬态位置上闪烁。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复顶部菜单“插入 -> 子程序”后仅出现虚拟空行的问题：插入结构改为“真实空行 + 子程序声明 + 真实空普通行”，并在插入后将光标置于新子程序首条普通行行首，避免必须手动回车物化。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 新增子程序“保底普通空行”删除规则：子程序首条普通行始终保留为可输入占位；删除命中该行时，若有内容则改为空行保留，若本身为空则改删其下方普通行；多选删除/剪切同样遵循该规则，避免全删后无法进入普通代码行。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）


### v0.0.3-beta.53（2026-05-04）
- [新增] 打开项目对话框支持直接选择 `.e/.ec` 文件并导入：支持读取工程头、处理加密密码与同名目录冲突，导入后进入现有打开流程。
  - 范围：`src/main/index.ts`、`src/main/eproject/*`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/EProjectImportDialog/*`
  - 验证：`npm run test:contract:eproject`、`npm run build`
- [新增] 表格模式流程命令显示优化：`判断开始` 在表格模式显示为 `判断`，文本模式保持 `判断开始`。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [修复] 修复 `.e/.ec` 导入偏移与占位问题：修正工程段长度偏移导致的 `Magic` 报错，并修复仅生成“导入说明/空子程序占位”的问题，按程序段/资源段落地真实 `EYC/EGV/EDT/ELL/ERC` 内容。
  - 范围：`src/main/eproject/sections.ts`、`src/main/eproject/nativeProject.ts`、`src/main/eproject/methodCode.ts`、`src/main/eproject/ycProjectWriter.ts`、`tests/contract/eproject-import-contract.spec.mjs`
  - 验证：`npm run test:contract:eproject`、`npm run build`
- [修复] 修复核心命令与支持库命令映射偏差：补齐 `Lib0` 大规模回退映射及 `181/182/203` 等兼容覆盖，并接入多支持库命令 ID 映射，减少 `_LibXCmdY` 占位回退。
  - 范围：`src/main/eproject/lib0CommandMap.ts`、`src/main/eproject/libCommandMaps.ts`、`src/main/eproject/methodCode.ts`
  - 验证：`node --test tests/contract/eproject-import-contract.spec.mjs tests/contract/eyc-flow-contract.spec.mjs`、`npm run build`
- [修复] 修复表格模式流程线在特殊 `判断` 结构中的断裂与错位：补齐 `branch+start` 同行重叠语义、默认分支连通、上一可见行桥接、上半段按来源条件显示。
  - 范围：`src/renderer/src/components/Editor/eycFlow.ts`、`src/renderer/src/components/Editor/editorFlowRenderUtils.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`
- [修复] 修复“第 7 行下三角箭头残留”系列问题：在同深度及跨深度重叠场景统一屏蔽不应显示的下三角，并补齐 `end/branch/inner/startAtDepth` 全路径漏网点。
  - 范围：`src/renderer/src/components/Editor/editorFlowRenderUtils.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`
- [修复] 修复 `如果真` 无结束与嵌套场景的箭头异常：补回应显示的下箭头、移除额外横向虚线、并改为同深度锚定避免错层。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/editorFlowRenderUtils.tsx`
  - 验证：`get_errors`
- [修复] 修复流程补齐与可见性不一致：手工输入 `.如果` 回车可正确补齐可见 `否则/如果结束`，并修复文本模式可见但表格模式隐藏的问题。
  - 范围：`src/renderer/src/components/Editor/useCodeLineEditor.ts`、`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`node --test tests/contract/eyc-flow-contract.spec.mjs tests/contract/flow-line-theme.spec.mjs tests/contract/editor-helper-utils-contract.spec.mjs`、`npm run build`
- [修复] 修复流程命令缩进与特殊插入不一致：统一 commit/Enter 的 `.判断()` 缩进策略，补齐默认分支首行判定回退与特殊结构定位兜底。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/eycFlow.ts`
  - 验证：`get_errors`
- [修复] 修复表格编辑器偶发回退文本模式与空白闪烁：消除缓存初始化 TDZ、限制前置行预览触发条件，避免编辑中可视区被重置。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/editorBlocksModel.ts`
  - 验证：`get_errors`、`npm run build`
- [修复] 修复并清理 VS Code Problems 面板历史告警：修正 ARIA 与菜单语义、移除 inline style、替换不兼容 CSS 写法并补齐类型收窄。
  - 范围：`src/renderer/src/components/Sidebar/*`、`src/renderer/src/components/Editor/*`、`src/renderer/src/components/NewProjectDialog/*`、`src/shared/theme.ts`、`src/main/libraryManager.ts`、`tsconfig.node.json`
  - 验证：`get_errors`（全工作区无报错）

- [优化] 流程线机制正式收敛为命令驱动：去除旧 marker 兼容分支生效路径，自动补齐输出可见流程命令，打开编辑器时清理旧 marker 残留。
  - 范围：`src/renderer/src/components/Editor/eycFlow.ts`、`src/renderer/src/components/Editor/editorFlowAutoExpandUtils.ts`、`src/renderer/src/components/Editor/useCodeLineEditor.ts`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`node --test tests/contract/eyc-flow-contract.spec.mjs tests/contract/flow-line-theme.spec.mjs tests/contract/editor-helper-utils-contract.spec.mjs`
- [优化] 大文件表格模式性能系统性优化：引入可视区 block/行级裁切、缓存复用、滚动量化节流、minimap 阈值收紧、冻结策略收紧，显著降低主线程重绘压力。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`、`npm run build`
- [优化] 将诊断与表格模型构建迁移至 worker：问题检查与 `buildBlocks + computeFlowLines` 异步化，主线程只接收结果渲染，缓解首开和滚动卡顿。
  - 范围：`src/renderer/src/components/Editor/editorDiagnostics*.ts`、`src/renderer/src/components/Editor/editorBlocksModel*.ts`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`、`npm run build`
- [优化] 补强“判断自动展开”诊断能力：增加结构化日志与 `judge-branch:check` 失败上下文，便于快速定位第 5/7 行结构未命中的根因。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

### v0.0.3.52（2026-04-28）
- [修复] 修复表格编辑器中输入 `.如果` 后通过鼠标失焦提交时未补全流程结构（200C/200D）的问题，避免误判导致下方流程缩进错位。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`npm run build`（通过）
- [修复] 修复自绘标题栏中“文件”菜单左侧仍显示四方块占位图的问题，改为渲染 `ycidelogo.svg` 真实 logo。
  - 范围：`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/renderer/src/components/TitleBar/TitleBar.css`、`src/renderer/src/assets/icons/YcideLogo.svg`
  - 验证：`npm run build`（通过）
- [修复] 修复 Windows 启动后窗口左上角仍显示默认四方块图标的问题：补充应用图标多路径解析、设置 `AppUserModelId`，并在窗口创建后显式设置图标。
  - 范围：`src/main/index.ts`
  - 验证：`npm run build`（通过）
- [优化] 将程序图标统一切换为 `图标文件/svg-logo/ycidelogo.svg`，生成并接入打包图标资源（Windows/macOS/Linux）。
  - 范围：`resources/icon.ico`、`resources/icon.icns`、`resources/icon.png`、`src/main/index.ts`
  - 验证：`npm run build`（通过）

### 2026-04-24
- [优化] AI 助手内置 DeepSeek 模型切换为 DeepSeek-V4-Pro，兼容官方 V4 API 更新（base_url 不变，仅更新 model 参数）。
  - 范围：`src/main/ai-assistant.ts`
  - 验证：`get_errors` 检查无新增错误

- [新增] AI 助手新增内置模型选项：DeepSeek V4-Flash、DeepSeek V3.2，并复用 DeepSeek API Key 配置。
  - 范围：`src/shared/ai.ts`、`src/main/ai-assistant.ts`、`src/main/index.ts`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors` 检查无新增错误

- [新增] AI 助手新增 Plan 模式与 Agent 模式：Plan 可输出结构化执行计划；Agent 可基于目标自动决策并对当前激活文件执行修改。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors` 检查无新增错误

- [优化] Agent 模式升级为项目级执行：可遍历资源管理器项目文件，并按工具调用执行 `list_files/read_file/edit_file/open_file`，不再局限已打开标签页。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors` 检查无新增错误

- [新增] 输出面板实现可交互终端（输出查看、命令输入执行、最近命令展示），并打通 AI 助手 Agent 终端工具调用（`terminal_send`/`terminal_read`/`terminal_last_command`）。
  - 范围：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`
  - 验证：`get_errors` 检查无新增错误

- [优化] 终端交互布局调整为单框形态：将输入行嵌入同一终端容器底部，避免输入与输出分离成两个框。
  - 范围：`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`
  - 验证：`get_errors` 检查无新增错误

- [优化] 终端控制台进一步一体化：移除右侧“执行”按钮，仅保留回车执行，并将输入行并入同一滚动终端区域。
  - 范围：`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`
  - 验证：`get_errors` 检查无新增错误

- [优化] 终端改为按需启动：点击“终端”标签时启动，不再显示“未启动”状态文案；启动后自动输出当前终端工作目录。
  - 范围：`src/main/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`
  - 验证：`get_errors` 检查无新增错误


### 2026-04-23
  - 范围：`src/shared/settings.ts`、`src/renderer/src/components/SettingsDialog/SettingsDialog.tsx`、`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：类型检查无新增错误

## 2026-04-24（终端输出稳定性修复）

- 优化：修复 `cmd.exe` 在中文 Windows 下终端输出乱码问题，新增终端输出解码逻辑，优先 UTF-8，检测到替换符时自动回退 GBK（CP936）解码。
- 优化：终端系统日志（已启动/当前目录）改为去重写入，避免重复触发启动流程时出现连续重复提示。
- 涉及文件：`src/main/index.ts`

## 2026-04-24（终端快捷键与内联输入交互修复）

- 修复：终端支持 `Ctrl+C` 中断当前命令，主进程新增 `terminal:interrupt`，中断后自动重建 shell 会话。
- 优化：终端输入改为控制台内联输入，移除独立输入框；光标在控制台末尾闪烁，输入位置与终端最后一行保持一致。
- 优化：终端区域聚焦后可直接键盘输入，支持 `Enter` 执行、`Backspace` 删除和粘贴文本。
- 涉及文件：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`

## 2026-04-24（Agent 工具结果流式回传修复）

- 修复：Agent 执行工具步骤时，工具日志改为实时回调到 AI 助手消息流，避免“执行完但窗口没有结果”的空白体验。
- 修复：`terminal_send` 工具在执行后自动抓取新增终端输出并回传到 Agent 对话，确保如 `node --version` 这类命令结果可直接显示在 AI 助手窗口。
- 优化：保留最终汇总日志兜底，避免极端情况下流式日志遗漏。
- 涉及文件：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`

## 2026-04-24（AI 助手交互与 Agent 意图路由优化）

- 新增：AI 助手输入区增加“停止”按钮，可立即中止当前会话的流式显示与后续结果写入（聊天 / 计划 / 编辑 / Agent）。
- 修复：AI 聊天记录区域恢复文本多选与复制能力。
- 优化：Agent 模式移除固定占位输出，减少每次执行都出现的重复提示行。
- 修复：当用户目标包含网络连通性语义（如“检查网络连接”）时，Agent 优先走终端 `ping` 诊断流程，而不是先扫描项目源码文件。
- 涉及文件：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`

## 2026-04-24（DeepSeek Agent Function Calling 升级）

- 新增：AI 通信层支持 `tools` / `tool_choice` / `tool_calls` 协议，兼容 DeepSeek 的函数调用 Agent 模式。
- 新增：主进程与预加载桥接 `ai:chatWithTools` 能力，渲染进程可直接发起工具调用会话。
- 优化：Agent 执行器从“JSON 计划解析”升级为“多轮 tool-calling 闭环”（assistant tool_calls -> 本地执行工具 -> tool 结果回传 -> 下一轮）。
- 优化：保留项目级文件与终端工具能力，并继续对网络连通性任务优先使用终端命令诊断。
- 涉及文件：`src/shared/ai.ts`、`src/main/ai-assistant.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`

## 2026-04-24（Agent 工具调用审计可视化）

- 新增：Agent 模式支持按工具调用分组展示审计条目，每次 tool call 可折叠查看“参数/返回”。
- 优化：工具调用参数与返回值支持自动截断，避免超长内容淹没聊天流。
- 优化：保留原有日志流并叠加结构化工具审计，便于快速定位调用链和结果。
- 涉及文件：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`

## 2026-04-24（dev 运行语法错误修复）

- 修复：`App.tsx` 中 Agent 工具循环闭合层级异常，导致 `npm run dev` 时 Vite/Babel 抛出 `Unexpected token`，界面出现大段错误堆栈文本。
- 验证：重新执行 `npm run dev`，主进程 / preload / renderer 均可正常启动，未再出现该语法错误。
- 涉及文件：`src/renderer/src/App.tsx`

- [新增] 系统设置中的布尔项改为左右滑动开关（关闭灰色、开启蓝色）。
  - 范围：`src/renderer/src/components/SettingsDialog/SettingsDialog.tsx`、`src/renderer/src/components/SettingsDialog/SettingsDialog.css`
  - 验证：类型检查无新增错误

- [优化] 代码预览区改为内容随滚动联动，不再仅移动视窗框。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：类型检查无新增错误

- [修复] `.eyc` 标签名回归问题，恢复优先显示 `.程序集` 名称（不足时回退去后缀文件名）。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`
  - 验证：类型检查无新增错误

- [修复] 项目树双击子程序跨文档跳转不到位问题，优化为一次跳转到位。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：类型检查无新增错误

（发版后将对应条目从“待发版条目”移动到这里，并标注版本号）

