# ycIDE 版本开发日志

> 用途：先记录开发变化，再在发版时据此更新 `版本更新说明.md`，避免遗漏。

## 记录规范
- 每次功能开发/修复/优化完成后，追加到“待发版条目”。
- 分类只使用：`[新增]`、`[修复]`、`[优化]`。
- 每条尽量包含：变化摘要、影响范围、验证方式。
- 版本号统一使用 `x.y.z-beta.n` 预发布格式（示例：`0.0.3-beta.54`），不再使用 `0.0.3.54` 这类纯数字补丁格式。
- 发版改版本号时，需同步修改：`src/renderer/src/components/StatusBar/StatusBar.tsx` 与 `src/renderer/src/App.tsx`（`aiIdeContext` 版本字符串）。
- 发版后将对应条目移动到“已发布归档”。
- **提交标题规范**：提交/推送标题以版本号开头，格式 `v<版本号> - <变化摘要>`，示例：`v0.0.3-beta.60 - WebView2 组件体系与工具箱分类、编辑框属性补全、编辑器性能优化`。
- **测试包打包规范**：发给测试人员的包统一用 `npm run package:win:zip` 生成（`scripts/package-win-zip.mjs`），脚本会自动排除 `compiler/` 目录（编译工具链 3GB+ 单独分发）。装有 7-Zip 时产出 7z 格式极限压缩包 `dist/ycIDE-win-unpacked-v<版本>.7z`（约 77MB，LZMA2 -mx=9 solid）；未装 7-Zip 时回退 `.zip`（约 123MB）。注意 .7z 在 Win10 上需要 7-Zip/WinRAR 解压。

## 待发版条目

（暂无）

## 已发布归档

### v0.0.4-beta.9（2026-07-09）

- [新增] **数组支持扩展：浮点族元素 + 多维数组 + 链式下标**（用户：`.局部变量 矩阵A, 双精度小数型, , "100,100"` 编译报「暂不支持 双精度小数型 数组」——性能测试用到 100×100 双精度矩阵乘法）。实现（compiler.ts）：①数组跟踪 Set→Map（名 → 元素类型+各维尺寸）；②浮点族（小数型/双精度小数型）元素按 double 位模式存进统一的 vector<long long>（预置 yc_f64_bits/yc_f64_from_bits，memcpy 位转换）——下标读包 from_bits、下标赋值包 bits((double)(…))、加入成员/插入成员 的元素值参同款转换；浮点数组的 数组排序 转译期拦截（位模式排序对负数错序，待 impl 加比较器版）；③多维数组 "100,100" 行主序扁平存储（总量预填 0），链式下标 `矩阵A [i] [j]` 折算一基线性下标 ((i)-1)*100+(j)——表达式改写与下标赋值语句两侧都支持（matchBracketGroup 配平+组数校验，组数≠维数转译报错）；④**splitDeclParts 不引号感知导致 "100,100" 被切碎**（parts[3]="100）——新增 splitDeclPartsQuoted（引号感知）用于 局部变量/程序集变量/参数 三处声明解析；⑤数组参数维度未知按动态一维（多维传参在被调方按线性一基访问）。TRANSPILE_CACHE_VERSION 11→12。验证：无头编译用户项目 success=true；生成 cpp 抽查（矩阵C(10000,0)/((i)-1)*100+(j) 线性折算/乘累加三读一写位转换全对）；**真机闭环**（起 exe+BM_CLICK+WM_GETTEXT）跑通六项性能测试且校验值全部数学正确——整数累加 5000000050000000=Σ(1..1e8)、浮点校验 22026.4549≈e^10、素数 78498 个（1~100万真值）、矩阵乘法 31ms；npm test 全绿(145 单测+47 契约)。
- [修复] **真/假 后紧跟半角逗号/右括号时误报「未定义变量」**（用户：`重定义数组 (排序数组, 假, 数组大小)`、`数组排序 (排序数组, 真)` 报 未定义变量"假"/"真"）。根因：着色器（editorCoreUtils colorExpr）逻辑字面量 真/假 的词边界 lookahead 集合只有 空白/半角左括号/全角括号逗号/等号——**半角 , 和 ) 不在集合里** → `真)`、`假,` 不被认成常量字面量、落到普通 token（cls=''）→ 诊断的干净标识符检查按未定义变量上报；且/或 的边界集合同样漏半角括号（`或)` 会报未知命令）。修复：两处词边界统一改为「后面不是标识符字符」（$| [^一-龥A-Za-z0-9_]），任何半/全角标点都算边界；防御兜底——真/假 并入 EycTableEditor reservedNameSet（易语言保留字，变量名非法判定/插入变量提示同步豁免）。此前单测未暴露：spec 的 run() 帮助函数把 真/假 预置进了 validCommandNames/reservedNames，掩盖了组件真实集合缺失。验证：新增 2 条单测（用户原文两行用不含真/假豁免的保守集合直测不报/半角括号紧跟的 或 不报未知命令且真未知照报），typecheck 通过，npm test 全绿(145 单测+47 契约)。
- [新增] **行多选状态下粘贴 = 覆盖粘贴**（用户：多选选到了子程序时粘贴应覆盖——文档和剪贴板都有 _子程序1_被单击、多选里含这个子程序 → 粘贴覆盖它；完整选中=完整覆盖；选到一半、下面留有未选中的普通代码 → 粘贴内容顶上、残留代码自然归入该子程序（追加语义）；不多选粘贴维持现行重名改名 原名_1/_2 方案）。实现：buildMultiLinePasteResult 新增 replaceLineIndices 参数——有选区时先从文档剔除选中行（.程序集 声明行受保护不删），后续全部判定（重名改名/局部变量归位/插入点）基于剔除后的文本：插入点固定为选区首行原位（不走子程序边界对齐，保证残留尾部能接上粘贴的同名子程序）；重名判定天然实现覆盖语义——选区覆盖到声明行的同名子程序在剔除后文档中已不存在故保留原名，未被选中的同名子程序仍照改名；选区里剪贴板没有对应物的子程序（如完整选中 被单击+被双击 两个、剪贴板只有 被单击）随选区剔除一并删除（用户确认的期望语义，契约用例锁定）。三个粘贴入口（Ctrl+V window 监听/applyCustomPasteShortcut/右键菜单 pasteFromClipboardAtContext）经 selectedLinesRef 统一传入最新选区（ref 避免选区塞进回调依赖）。验证：新增 3 条契约用例（完整选中同名子程序覆盖不改名不残留旧代码/部分选中留尾时尾行归入覆盖后子程序且声明唯一/选区在别处时重名照旧改 _1/完整选中双子程序而剪贴板只有其一时另一个被删除），typecheck 通过，npm test 全绿(143 单测+47 契约)。
- [修复] **且/或 组合条件转译无运算符优先级 → 快速排序死循环（点按钮未响应）**（用户：算法测试挪到按钮事件后点击即未响应）。根因：translateExpressionToC 的表达式切分只有 比较→加减→乘除 三层、没有逻辑运算层，`i ＜ j 且 数组 [j] ≥ 基准` 中 且 先被替换成 &&，随后比较切分在第一个 ＜ 处开切 → 生成 `while (i < (j && yc_ary_at(数组, j) >= 基准))`——i≥1 时右侧恒为 0/1、条件恒假 → 内层循环体永不执行、i/j 不再变化 → 外层 while (i < j) 死循环挂死 UI 线程。此缺陷影响一切「比较 且/或 比较」组合条件（如果/判断循环 都会生成错误语义），此前未爆是因为组合条件+循环变量自增依赖的场景才可见。修复：新增 findTopLevelLogical（引号感知+括号深度 0，先切 ||（优先级最低）再切 &&，同级取最右切分点保持左结合），置于比较切分之前，两侧递归转译 → `((i < j) && (yc_ary_at(数组, j) >= 基准))`。TRANSPILE_CACHE_VERSION 10→11。验证：无头编译用户项目 success=true、生成 while 条件抽查正确；**真机闭环**——PowerShell 起 exe、EnumWindows 按 PID 找窗、PostMessage BM_CLICK 点按钮1、WM_GETTEXT 读编辑框：冒泡/选择/快速排序+线性/二分搜索+字符串拼接全部跑完出结果（冒泡 16 毫秒，其余在 GetTickCount 精度内归 0），进程无卡死；npm test 全绿(143 单测+43 契约)。
- [修复] **支持库常量（#换行符 等）编译失败：常量清单根本没被装载**（用户：`＋ #换行符` 编译报 `use of undeclared identifier '换行符'`）。三层问题：①libraryManager 元数据候选文件列表没有 `*.constants.json`——krnln.constants.json（183 个常量）从未进 getLibInfo，生成 cpp 里「支持库常量定义」#define 块整个缺失（顺带支持裸数组形状的常量文件）；②清单常量名带 # 前缀（"#备注型"），直接进 #define 是非法宏名——collectLibraryConstants 剥 #（与表达式引用侧 replaceConstantRefs 的剥 # 对齐）；③krnln.constants.json 缺文本常量——补 #换行符(CRLF)/#回车符(CR)/#制表符(TAB)/#引号 四个（type=text），并修 collectLibraryConstants 对 text 型值不 trim（否则 "
- [新增] **编译器数组支持（动态数组全链路）**（用户：算法性能测试程序编译报 `subscripted value is not an array` ×N + `数组 [j] ＝ 数组 [j ＋ 1]()` 乱码发射——转译器完全没有数组概念，而 krnln impl 早有完整的 std::vector<long long> 数组运行时 krnln_AddElement/GetAryElementCount/ReDim 等）。实现（compiler.ts）：①声明——.局部变量/.程序集变量 第 4 字段（数组尺寸，"0"=动态）→ `std::vector<long long>`（定长预填 0），非整数族元素（文本/小数等）转译期报「暂不支持 X 数组」；②参数——.参数 带「数组」标志 → `std::vector<long long>&`（签名/类方法/跨文件前向声明三处；标志判定改 parts.slice(2) 防参数名恰叫「数组」误判，isByRef 同修）；③下标——一基下标经模块级 currentTranspileArrayVars 追踪改写为 `yc_ary_at(名称, 下标)` 引用形式（预置 static inline helper，越界回落哑元不崩溃；括号形式兼容既有按括号感知的表达式切分，方括号会被加/乘法切分撕裂故不可留）；改写引号感知+配平方括号+嵌套内层先转，钩在 translateExpressionToC 入口；④下标赋值——`数组 [i] ＝ 右值` 语句分支 → `yc_ary_at(...) = (long long)(右值)`；⑤数组命令 ABI——加入成员/取数组成员数/清除数组/重定义数组/删除成员/插入成员/取数组下标/复制数组/数组排序 按符号显式给定 (void*, long long/int) 形参表（清单里是通用型，原通用映射把实参转文本指针与 impl 错位；数组实参传 (void*)&名称，非数组变量转译期报错），加入成员多值展开为逐次调用；⑥＼ 整除补进 convertFullWidthOps（整数下 C / 即截断除法）。TRANSPILE_CACHE_VERSION 9→10。
- [新增] **编译前全项目错误拦截（未打开的文件也检查，输出面板按文件报错误）**（用户：打开项目但没打开文件、文件里有错误，点运行/编译也要拦截，并在输出面板报哪个文件有错——既有拦截只看当前打开文件的问题面板 fileProblems，未打开文件的错误漏放行到编译器才炸）。实现：①新增 projectDiagnosticsSweep.ts `sweepFileDiagnostics`——对单个源文件构造与 EycTableEditor 逐项对齐的诊断上下文（validCommandNames=目录+DLL+本文件子程序/DLL声明+变量+FLOW_KW+且或别名、allKnownVarNames=本文件变量+项目全局变量+关联窗口控件、reservedNames、commandSignatures、controlPropTypes 折算），调 buildEditorDiagnosticsProblems，保证「打开该文件看到的问题」=「编译拦截报的问题」；②Editor 句柄新增 `sweepProjectDiagnostics()`——readDir 找 .epp → parseEpp 拿项目文件清单（EYC/EGV/ECS/EDT/ELL），打开的文件用标签页最新内容（含未保存）、未打开读盘，efw 表单同理（打开用 formData、未打开 JSON 解析），目录经 getAllCommands+buildCompletionCatalog、全局变量/项目 DLL 命令用 Editor 既有 state；失败兜底返回空（宁可放行给编译器报错，不误拦）；③App 两个编译入口（handleCompile/handleCompileRun）在既有问题面板拦截之后、真正编译之前 `await sweepAndReportProjectErrors()`——有错误则输出面板（编译页签）报「编译已取消：N 个文件存在 M 个错误」+ 按文件列出每条「第 L 行: 消息」（上限 50 条，超出提示打开对应文件看问题面板）。**连带修复**：菜单「编译→普通编译」与 Ctrl+F7 的 build:compile 动作在 handleMenuAction 里根本没有 case（点了无反应的既有 bug，E2E 时暴露）——补上映射到 handleCompile。验证：新增 4 条单测（未知命令扫出/干净文件与全局变量+且或不误报/控件属性类型检查随窗口上下文生效/本文档 DLL 声明不误报+括号不配平照报）；Playwright 双路 E2E——拷贝项目注入错误到未打开的 窗口1.eyc → Ctrl+F7 输出面板报「编译已取消：1 个文件存在 1 个错误 / 窗口1.eyc（1 个错误）: 第 8 行: 未知命令…」拦截成功；干净项目 → 编译成功不误拦（期间顺带自证：未打开 efw 的输入方式=输入整数 + 到文本(n) 也被扫出，控件属性类型检查在 sweep 全链路生效）；typecheck 通过，npm test 全绿(143 单测+43 契约)。
- [修复] **逻辑运算符 且/或 误报「未知命令」**（用户：`.判断循环首 (i ＜ j 且 数组 [j] ≥ 基准)` 问题面板报 未知命令"且"，但核心支持库明明有「并且」命令）。根因：着色器（editorCoreUtils colorExpr）故意把 且/或 着成 funccolor（命令色，因为它们是「并且/或者」命令的运算符别名，照易语言），但诊断的 funccolor 检查按命令目录查名——目录里只有「并且」「或者」本名，没有别名 且/或 → 误报。修复：editorCoreUtils 导出 `LOGIC_OPERATOR_ALIASES`（且/或，语言级运算符别名——编译器直接转 &&/||，与命令目录无关）；诊断 funccolor 检查豁免该集合（editorDiagnosticsShared，面板）；EycTableEditor validCommandNames 集合并入别名（行内红标同步豁免）。验证：新增 2 条单测（用户场景 且 不报/或 豁免且真未知命令仍照报），typecheck 通过，npm test 全绿(139 单测+43 契约)。
- [新增] **控件属性赋值的数据类型检查（内容 随 输入方式 折算，照易语言）**（用户：设计器把编辑框1「输入方式」设为「输入整数」后，`编辑框1.内容 ＝ 到文本(n)` 仍不报错——此时 内容 实为整数型，应报数据类型问题，`编辑框1.内容 ＝ n` 才合法；同理别的控件也要验证）。实现：①editorDiagnosticsShared 新增 `buildControlPropTypes(controls, units)`——由关联窗口控件实例+支持库窗口单元定义折算「控件名 → 属性名 → 类型名」表，单元带「输入方式」属性时（编辑框族）「内容」类型随实例值折算（输入字节/短整数/整数/长整数/小数/双精度小数/日期时间 → 对应类型，通常/只读/密码/整数文本/小数文本 → 保持文本型，实例未存值用单元 defaultValue）；②诊断请求新增 controlPropTypes 字段（hook+worker 透传），第二循环对 `控件.属性 ＝ 右值` 做族级匹配——属性类型查表、右值经既有 inferArgTypeFamily 推断（字面量/变量/嵌套命令返回类型），族不同报「属性"编辑框1.内容"类型不匹配：需要 整数型，当前为 文本型」（与命令参数检查同文案风格），右值无法推断保守跳过；通用属性一并生效（如 `编辑框1.宽度 ＝ "abc"` 报需要整数型）；③数据链路：Editor.tsx activeWindowControls 随附控件实例 properties（输入方式值）→ EycTableEditor windowControlTypes prop 类型扩展 → diagControlPropTypes memo（windowControlTypes+windowUnits）进诊断请求。验证：新增 6 条单测（折算表 2 条：输入整数→整数型/缺省与文本类方式→文本型+未知单元不入表；诊断 4 条：用户场景报错/整数右值不报/缺省方式反向报错与到文本不报/宽度赋文本报+右值不可推断保守跳过），typecheck 通过，npm test 全绿(137 单测+43 契约)。
- [修复] **控件文本属性赋值右值为支持库命令时编译失败**（用户：`编辑框1.内容 ＝ 到文本(n)`，编辑框1 已放置在窗口上，编译报 `use of undeclared identifier '编辑框1'`）。根因：转译器赋值分支里只有 exprGenerator 命令走了「控件文本属性 → yc_set_control_text」通道，右值是 **ycmd 原生命令**（到文本→krnln_str 等）或**通用支持库命令**（yc_invoke_support_cmd）时落到通用赋值发射 `{ 编辑框1.内容 = 表达式; }`——把控件属性当 C++ 左值原样发进 cpp。修复（compiler.ts）：赋值分支在原生/通用命令发射前补 propMatch+isTextProp 分流——原生命令取其表达式形态、通用命令新增 generateYcGenericCommandTextExpr（把 yc_invoke_support_cmd 调用包成返回 wchar_t* 的 lambda，非文本返回值经 yc_value_to_text 转文本），统一发射 `yc_set_control_text(L"控件名", 表达式)`；非文本表达式自动 yc_value_to_text（与既有分支一致）。TRANSPILE_CACHE_VERSION 8→9 强制重转译。验证：typecheck 通过；Playwright 无头编译用户实际项目（windows窗口程序-新项目1）success=true，生成行为 `yc_set_control_text(L"编辑框1", ([&]() -> wchar_t* { ... krnln_str ... })())`，全文无裸左值发射。
- [新增] **行内括号配平诊断**（用户：`编辑框1.内容 ＝ 到文本(n)))` 多两个右括号问题面板不报——参数类型检查只扫描到自己找到的配对右括号为止，多余的尾随右括号谁都不管）。诊断第二循环（不依赖命令目录）对每个代码行做括号配平：maskQuotedText 掩掉字符串内容、按引号感知规则截断行尾 ' 注释段（注释里的括号不计，与 eycBlocks/editorCoreUtils 同规则 i>0），再扫半/全角括号 `(（)）` 配栈——右括号多 → 报「括号不匹配：多余 N 个右括号」（列=首个多余右括号处），左括号多 → 报「括号不匹配：缺少 N 个右括号」（列=首个未配对左括号处）；两者可同时报（如 `)(`）。易语言语句单行成立故按行配平合法，流程行（.如果/.循环尾 等）天然配平不受影响。验证：新增 3 条单测（用户场景多余 2 个右括号/缺少 1 个右括号/字符串内与行尾注释里的括号不误报），typecheck 通过，npm test 全绿(131 单测+43 契约)。
- [新增] **字符串开头的裸语句报无效语句**（用户：整行只写 `"编辑框1.内容"` 问题面板不报——引号内容被 maskQuotedText 掩掉后什么检查都够不到）。与"数字开头裸语句"同一检查点扩展：非赋值代码行以引号（半/全角）开头 → 报「无效语句"…"：字符串不能单独作为语句」（超长字面量截断 24 字符）；字符串作参数/赋值右值不受影响。验证：新增 2 条单测（半/全角引号裸行报、参数与右值不误报），npm test 全绿(128 单测+43 契约)。
- [新增] **成员引用的对象存在性诊断**（用户：设计器没放置编辑框，代码写 `编辑框1.内容` 问题面板不报错）。诊断（editorDiagnosticsShared，hasCommandCatalog 块）对每个代码行扫描 `对象.成员` 的**首段对象名**（引号内容 maskQuotedText 掩掉；链式 a.b.c 只查首段，后段无类型信息）：既不在 validCommandNames、也不在 allKnownVarNames（**关联窗口的控件名/窗口自身已并入该集合**，来自 activeWindowControls）、也非保留字 → 报「未定义的窗口组件或对象"编辑框1"」。数字开头（小数 1.5）天然不匹配标识符头不误报。与上一条成员补全收紧同一存在性判据（补全不弹 ↔ 诊断报错，行为一致）。问题面板先行、行内红标未同步（同参数类型检查的取舍）。验证：新增 3 条单测（未放置报错/已知对象不报/字符串内与链式后段不误报），npm test 全绿(126 单测+43 契约)。
- [优化] **成员补全只对真实存在的对象弹出**（用户：设计器没放置编辑框，代码里敲 `编辑框1.` 却弹出标题/宽度/内容等成员窗）。根因：buildMemberCompletionSource 对未知对象名按「去尾数字」猜类型（编辑框1→编辑框）铺出该窗口单元的属性。现收紧：对象必须真实存在——**关联窗口上放置的组件/窗口自身**（windowControlTypeMap，来自 activeWindowControls：当前 .eyc 关联的 .efw 的 formData）或**有类型的变量/参数**（userVarTypeMap/用户变量补全项）——才提供成员补全；未知对象返回**空列表**（弹窗不显示；不能返 null——那会走 `memberSource || defaultSourceList` 兜底弹出全量命令列表）；对象名为空的原兜底不变。连带收紧：跨窗口 `其它窗口.控件.`、裸类型名 `编辑框.` 也不再弹（均非"对应窗口上真实存在"）。验证：新增 4 条单测（未放置不弹、放置后弹属性、按变量类型弹不依赖命名、空对象名回退默认），npm test 全绿(123 单测+43 契约)。


### v0.0.4-beta.8（2026-07-08）

- [新增] **未知标识符回车 → 「插入变量」提示框**（照易语言）——代码行整行输入裸的未知标识符（如「我是」，非命令/变量/流程关键字/保留字）按回车，弹出「未知命令"我是"。是否将"我是"插入为变量？」，可选 **局部变量 / 程序集变量 / 全局变量 / 取消**（局部为默认焦点、Enter 直接确认）。插入：局部变量→所属子程序声明区末尾、程序集变量→顶部程序集变量区末尾（均默认整数型，可在表格里改类型）、**全局变量→经既有 onRouteDeclarationPaste 路由到 .egv 文件**；插入后自动恢复回车前正在编辑的下一行（本文件插入使行号 +1 时按位移校正）。正常回车流程（新行+光标下移）不受影响、弹窗叠加其上；Esc/点遮罩/取消关闭。实现：applyEnterKey 代码行分支检测裸标识符（validCommandNames/allKnownVarNames/reservedNameSet 三集合判未知）→ setInsertVarPrompt；`applyInsertVariable(scope)` 执行插入与恢复。typecheck 通过；npm test 全绿(119 单测+43 契约)。
- [修复] **纯数字裸语句不报错**（用户截图：整行只写 `123555` 无任何问题提示，而 `到大这` 会报未知命令）。数字开头的 token 三个既有检查都够不到：不是 funccolor（未知命令检查）、不匹配标识符正则（未定义变量检查）、参数类型检查只查括号内。新增语句级检查（editorDiagnosticsShared 第二循环）：**非赋值代码行以数字开头**（半/全角，含 `1.txt` 式杂项 token）→ 报「无效语句"…"：语句不能以数字开头」；赋值行交给未知变量检查避免同行双报；数字作参数/右值不受影响。验证：新增 3 条单测（纯数字裸行/1.txt 裸行/参数与右值不误报），npm test 全绿(119 单测+43 契约)。
- [新增] **打勾格（公开/静态/传址/参考/可空/数组）支持键盘**——此前子程序表的「公开」、变量表的「静态」等勾选格只能鼠标点击切换，方向键导航会跳过它们。现在：方向键可**停进**打勾格（td 画焦点描边、无输入框、焦点在 wrapper），**空格=切换勾选**（与鼠标点击同效，经 tryToggleTableBooleanCell 同一入口），方向键继续走统一导航（打勾格无文本、任意 ←/→ 都跨格），Esc 退出。实现：EditState 加 `booleanCell` 焦点态；模块级 `getBooleanToggleCellIndexes(lineType, tableType)` 与切换函数的判定一一对应（dll/ptrCmd/sub/localVar ci2、globalVar ci3、dataTypeMember ci2、subParam 按表类型 ci2-4）；导航序列的行内单元格带 boolean 标记、`enterNavCell` 按格类型分流（普通格 startEditCell / 打勾格设焦点态）；垂直导航同列优先补 ci 匹配（打勾格无 fieldIdx）；window keydown 顶部接管该状态的 空格/方向/Esc（经 ref 调用避免监听依赖膨胀）；commit 对 booleanCell 直接退出（无文本可提交、防误写）。typecheck 通过；npm test 全绿(116 单测+43 契约)。（用户：光标进单元格后方向键不能往下继续移动）。此前 applyArrowNavigationKey 对 cellIndex≥0 一律早退、方向键无效，只能 Esc/点别处离开。现在单元格内 ↑/↓＝提交并移动到**相邻可编辑行**：按 blocksRef（visibleBlocks 文档序）构建可编辑序列（非虚拟代码行 + 表格非表头行），相邻是表格行取**同 fieldIdx** 的单元格（无同列取该行首个可编辑格，startEditCell 进入）、相邻是代码行则 startEditLine+focusInlineInputAt(0) 进入行编辑——表格↔代码行之间无缝穿行（子程序表内 参数/局部变量 行间也逐行走）；←/→ 保持输入框原生水平移动。复用 commit+setTimeout 的既有导航模式（与代码行 upOrDown 一致，等提交落盘后按最新 blocks 定位）。**二轮补**（用户复测）：①代码行按 ↑ 相邻是表格时整表跳过（旧路径 resolveVisibleCodeLineTarget 只认代码行）——代码行 ↑/↓ 先查统一可编辑序列（buildEditableNavEntries），相邻是表格行则进其单元格，仍是代码行才走原 applyCodeLineNavigation（保留隐藏行解析/水平位保持，契约源码锁不动）；②单元格内 → 在文本末尾跨到本行下一可编辑格（光标置格首）、← 在行首跨到上一格（光标置格尾，经 pendingInputCaretRef 布局期恢复），中间位置保持原生水平移动。**三轮补**（用户复测：行末最后一格继续按 → 光标消失）：横向到行边界后**跨行环游**——→ 进下一可编辑行（表格行取首格、代码行进行首）、← 进上一行（表格行取末格、代码行进行尾）；文档边界重新进入当前格；垂直导航同款边界问题一并修（↑/↓ 无相邻行时回落当前行，光标永不消失）。根因：先 commit 再找目标、无目标时直接 return 导致编辑已关光标丢失。typecheck 通过；npm test 全绿(116 单测+43 契约)。
- [修复] **表格单元格编辑态按 ↑/↓ 出不来**（用户：光标进单元格后方向键不能往下继续移动）。此前 applyArrowNavigationKey 对 cellIndex≥0 一律早退、方向键无效，只能 Esc/点别处离开。现在单元格内 ↑/↓＝提交并移动到**相邻可编辑行**：按 blocksRef（visibleBlocks 文档序）构建可编辑序列（非虚拟代码行 + 表格非表头行），相邻是表格行取**同 fieldIdx** 的单元格（无同列取该行首个可编辑格，startEditCell 进入）、相邻是代码行则 startEditLine+focusInlineInputAt(0) 进入行编辑——表格↔代码行之间无缝穿行（子程序表内 参数/局部变量 行间也逐行走）；←/→ 保持输入框原生水平移动。复用 commit+setTimeout 的既有导航模式（与代码行 upOrDown 一致，等提交落盘后按最新 blocks 定位）。**二轮补**（用户复测）：①代码行按 ↑ 相邻是表格时整表跳过（旧路径 resolveVisibleCodeLineTarget 只认代码行）——代码行 ↑/↓ 先查统一可编辑序列（buildEditableNavEntries），相邻是表格行则进其单元格，仍是代码行才走原 applyCodeLineNavigation（保留隐藏行解析/水平位保持，契约源码锁不动）；②单元格内 → 在文本末尾跨到本行下一可编辑格（光标置格首）、← 在行首跨到上一格（光标置格尾，经 pendingInputCaretRef 布局期恢复），中间位置保持原生水平移动。**三轮补**（用户复测：行末最后一格继续按 → 光标消失）：横向到行边界后**跨行环游**——→ 进下一可编辑行（表格行取首格、代码行进行首）、← 进上一行（表格行取末格、代码行进行尾）；文档边界重新进入当前格；垂直导航同款边界问题一并修（↑/↓ 无相邻行时回落当前行，光标永不消失）。根因：先 commit 再找目标、无目标时直接 return 导致编辑已关光标丢失。typecheck 通过；npm test 全绿(116 单测+43 契约)。
- [新增] **正在编辑的行绘制焦点背景**——进入编辑态（代码行内联编辑/表格单元格编辑/参数展开编辑）的行整行绘制与行选中同款的焦点背景色（`--table-selection-bg`），gutter 行号区一并着色；表格行含 td 本体覆盖与右侧空白补色（照 eyc-line-selected 全套模式新增 eyc-line-editing）。接线：代码行 row 包裹层（lineIndex+isVirtual 匹配）、表格数据行 tr、表格 gutter cell 三处按 editCell 匹配加类。**悬停豁免**（用户反馈悬停色盖住编辑背景显得错乱）：照 eyc-line-selected 的豁免模式，编辑行的 `.eyc-code-line:hover` 与 `.eyc-data-row:hover`（后者随全套模式已带）保持焦点背景不被悬停色覆盖。typecheck 通过；npm test 全绿(116 单测+43 契约)。
- [新增] **行号区交互**：鼠标悬停行号显示**左右镜像的箭头光标**（按 Windows 标准箭头几何 1:1 翻转自绘、白填充黑描边、热点在右上尖端）；**单击行号=选中整行**（不进入编辑态），**按住拖动=整行多选**，Shift+单击扩展选择。（光标方案反复过：CSS 无法翻转系统当前主题光标、只能自绘图片→自绘长系统默认样、与用户自定义光标主题不符→改手型 pointer→用户仍选定镜像箭头，最终定稿镜像箭头。）实现：新增 `handleGutterMouseDown`——stopPropagation 不冒泡到行 mousedown（避免乐观进编辑）、提交现有编辑、置 selectedLines/dragAnchor、`wasDragSelect=true` 让后续拖动直接走既有 onMove 的 rangeSet 多选且吞掉随后的 click；接到代码行与表格行两处 gutter cell（表头/虚拟行/diff 占位不接）；折叠按钮自带 stopPropagation 不受影响、断点圆点无独立交互一并按整行选中。光标 CSS 排除粘性子程序头 gutter 与占位 gutter。typecheck 通过；npm test 全绿(116 单测+43 契约)。（用户截图：同一子程序中部出现第二张局部变量表）。①粘贴目标在子程序内时，片段中"不属于片段内子程序"的 .局部变量 行（片段首个 .子程序 头之前的）不再按光标位置内联（那会在子程序中部形成第二张局部变量表），改为并入目标子程序头部声明区（参数/局部变量块末尾）；片段自带子程序的局部变量随子程序原样保留；纯局部变量粘贴不留内联空洞、选中范围指向声明区插入处；记账注意"光标恰在声明区末尾"须让变量插在内联代码之前（insertAt < declPos 才平移声明位，== 不平移）。②同子程序重复局部变量名的**问题面板诊断本来就有**（buildEditorDiagnosticsProblems checkLocalVars，经用户原文单测验证 3 条全报、跨子程序不误报），缺的是表格行内提示——新增 `duplicateDeclLineSet`（诊断结果中 重复定义/同名 类错误行）并入变量表名称格标红条件（与既有 invalidVarNameLineSet 同一渲染点 eyc-cell-invalid）。验证：新增契约用例（纯变量归位/混合粘贴变量在代码上方且代码沿用光标缩进/片段内子程序变量不被抽走），npm test 全绿(116 单测+42 契约)。③**手工输入的 .局部变量 也自动归位**（用户追加）：编辑会话结束（editCell→null 且确有编辑发生）时跑 `relocateMisplacedLocalVarLines(text)`（editorPasteUtils 导出纯函数）——逐子程序把声明区之外的 .局部变量 行按相对顺序并入声明区末尾，无错位返回 null 不写回；打开文件不触发（hadEditSessionRef 门闩）、会话进行中不动（正在编辑的行不能被抽走、行号不能在编辑态平移，这也是不挂在 commit 十余处写文本点的原因）；归位的 onChange 与提交在 600ms 内合并为同一步全局撤销。顺带效果：点开旧文件里已存在的第二张表的行再离开，也会自动归位修复。契约用例（用户场景三行归位保序/无错位返 null/多子程序各归各），npm test 全绿(116 单测+43 契约)。
- [修复] **粘贴 `.局部变量` 行归位到子程序声明区 + 重复变量名表格行内标红**（用户截图：同一子程序中部出现第二张局部变量表）。①粘贴目标在子程序内时，片段中"不属于片段内子程序"的 .局部变量 行（片段首个 .子程序 头之前的）不再按光标位置内联（那会在子程序中部形成第二张局部变量表），改为并入目标子程序头部声明区（参数/局部变量块末尾）；片段自带子程序的局部变量随子程序原样保留；纯局部变量粘贴不留内联空洞、选中范围指向声明区插入处；记账注意"光标恰在声明区末尾"须让变量插在内联代码之前（insertAt < declPos 才平移声明位，== 不平移）。②同子程序重复局部变量名的**问题面板诊断本来就有**（buildEditorDiagnosticsProblems checkLocalVars，经用户原文单测验证 3 条全报、跨子程序不误报），缺的是表格行内提示——新增 `duplicateDeclLineSet`（诊断结果中 重复定义/同名 类错误行）并入变量表名称格标红条件（与既有 invalidVarNameLineSet 同一渲染点 eyc-cell-invalid）。验证：新增契约用例（纯变量归位/混合粘贴变量在代码上方且代码沿用光标缩进/片段内子程序变量不被抽走），npm test 全绿(116 单测+42 契约)。③**手工输入的 .局部变量 也自动归位**（用户追加）：编辑会话结束（editCell→null 且确有编辑发生）时跑 `relocateMisplacedLocalVarLines(text)`（editorPasteUtils 导出纯函数）——逐子程序把声明区之外的 .局部变量 行按相对顺序并入声明区末尾，无错位返回 null 不写回；打开文件不触发（hadEditSessionRef 门闩）、会话进行中不动（正在编辑的行不能被抽走、行号不能在编辑态平移，这也是不挂在 commit 十余处写文本点的原因）；归位的 onChange 与提交在 600ms 内合并为同一步全局撤销。顺带效果：点开旧文件里已存在的第二张表的行再离开，也会自动归位修复。契约用例（用户场景三行归位保序/无错位返 null/多子程序各归各），npm test 全绿(116 单测+43 契约)。
- [修复] **粘贴重名子程序改为自动改名（原名_1，_1 占用则 _2 依次类推）**——此前粘贴与现有子程序重名的子程序走"并入同名子程序"合并语义：整块从插入内容剔除、只把正文并进旧子程序，**复制一个只有声明行的子程序（如 `.子程序 子程序1, , , `）粘贴会被整块剔除、看起来毫无反应**（用户实测报告）。现改为：重名子程序整块保留、只把 `.子程序` 声明行的名称字段改成首个未占用的 `原名_N`（占用集=现有子程序名+本次粘贴中已定名，剪贴板内自身重名也依次编号）；不重名照旧原名插入。实现：editorPasteUtils `renameDuplicatePastedSubs` 取代 `splitPastedLinesForDuplicateSubMerge`/`appendSubBodiesToExistingText`（连带删除合并机制与 append 回填分支）。验证：新增契约用例（声明行单独粘贴→改名插入不再没反应、_1 占用升 _2、带正文整块改名、不重名原样），npm test 全绿(116 单测+41 契约)。
- [新增] **复制/粘贴的流程结构补尾护栏**——复制选区含流程命令头（如果/判断开始/循环首…）但没选到配对结束（如只复制了 判断开始+case+默认、没有判断结束）时，**剪贴板自动补上缺失的结束行**；**粘贴**（含从外部编辑器粘进来的代码）同样在插入前补齐缺失结束，半截结构不再破坏流程线。实现：eycFlow 新增纯函数 `appendMissingFlowEnds(lines)`——栈扫描：FLOW_START 头入栈、同尾结束出栈（结束越过内层未闭合时内层按 implicitClose 语义视为隐式闭合、不补）、否则/默认分支不入栈、**裸「判断」一律不视为可闭合头**（它是 case 分支，补判断结束只会造孤立结构）；文末未闭合的按 LIFO 补结束行（沿用头行缩进与前导点号，循环尾带空括号、其余裸关键字）。接入：复制侧 3 处（getSelectedSourceText + 键盘 Ctrl+C/Ctrl+X，在 filterClipboardSelectionIndices 之后）；粘贴侧 buildMultiLinePasteResult（路由声明剔除后统一补齐，覆盖 Ctrl+V/右键粘贴/自定义粘贴全部路径）。契约测试的 TS 加载器为 editorPasteUtils 补 './eycFlow' 依赖注入（4 处）。验证：新增 6 条单测（用户场景 判断开始+默认 补判断结束、如果+否则、嵌套 LIFO 缩进、循环尾带括号、裸判断不补、隐式闭合/孤立结束不补），npm test 全绿(116 单测+40 契约)。
- [修复] **下划线开头窗口名（_启动窗口）的引用改写规则对齐**。命名约定（用户澄清，与编译器/双击窗体生成一致）：**程序集名**=`窗口程序集_`+剥前导下划线的核心名（`_启动窗口`→`窗口程序集_启动窗口`，前导下划线兼作分隔符，直接拼原名得到双下划线匹配不上）；**窗口事件名**=`_`+原始窗口名+`_事件`（`_启动窗口` 的事件是 `__启动窗口_创建完毕`，**双下划线合法**，编译器 `windowEventPrefix` 原始名拼法本来就是对的）；跨窗口成员引用（`旧名.`）用原始名；控件/菜单事件维持剥下划线家族（编译器兼容桩与 buildEventSubName 一致）。改动：①渲染层 `applyWindowRenameToContent`（属性面板改名+撤销改名共用）与②主进程 `renameWindow`：**程序集模式改用核心名**（原来拼原名对 `_启动窗口` 匹配不上、程序集名不会跟着改）、事件/成员引用保持原始名；③菜单项改名同步 `handleMenuItemRenames`：事件名拼法与编译器菜单桩一致（剥前导下划线）；粘贴窗口同规则（程序集=核心名、事件/成员=原始名）。（中间一度误把窗口事件也改成剥下划线并动了编译器 windowEventPrefix，经用户纠正已还原——窗口事件族用原始名。）验证：typecheck web+node、npm test 全绿(110 单测+40 契约，含真编译回归)。
- [新增] 项目管理器**窗口节点右键菜单：复制窗口 / 粘贴窗口 / 删除窗口**（`_启动窗口` 不可删除，删除项灰显并提示）。复制=记住该窗口；粘贴=统一命名「窗口N」（取首个未占用序号；用户纠正过初版的「原名+序号」），复制 .efw（改 name/sourceFile，title 原来等于窗口名时跟着改）+ 复制关联 .eyc 并把窗口引用改到新名——**程序集/事件名的拼法要剥掉窗口名的前导下划线**（`_启动窗口` 的程序集是 `窗口程序集_启动窗口`、事件是 `_启动窗口_创建完毕`，前导下划线兼作分隔符；初版直接拼 srcName 得到双下划线匹配不上、复制体程序集名没改成新名，用户实测发现）：`窗口程序集_剥名`、`_剥名_` 用剥后的名替换、`原名.` 用原始名替换；粘贴后自动打开新窗口；删除=确认后 .efw 与关联 .eyc（以 efw 的 sourceFile 为准）一并移入回收站、关闭对应标签页、清理项目树，被删窗口若是当前剪贴板内容则清空剪贴板。实现：Sidebar `ProjectNodeAction` 扩 copyWindow/pasteWindow/deleteWindow + nodeContextMenu kind 加 'window' + `canPasteWindow` prop（无复制体时粘贴灰显）+ 菜单项 :disabled 样式；App `copiedWindowFile` state + handleProjectNodeAction 三个动作（复用 addFile/removeFile/closeFileTab/openFileByPath 与 insert:window 的树更新样板）。typecheck 通过；npm test 全绿(110 单测+40 契约)。
- [新增] 项目管理器**各分类节点右键弹出对应的「新建」菜单**——此前只有「程序集」分类有右键菜单，现补齐：窗口→新建窗口；程序集→新建程序集/新建类模块（原有）；全局变量→新建全局变量；常量表→新建常量；自定义数据类型→新建自定义数据类型；DLL命令→新建DLL命令/新建指针命令。动作全部复用插入菜单同款逻辑（insert:window/globalVar/constant/dataType/dllCmd/ptrCmd）。实现：Sidebar 模块级 `CATEGORY_CONTEXT_MENU_ITEMS`（分类 id→菜单项，兼作右键判定集）+ `ProjectNodeAction` 扩 6 个动作 + nodeContextMenu state 加 categoryId；App `handleProjectNodeAction` 映射到 handleMenuAction。资源分类暂无新建动作、不弹菜单。typecheck 通过；npm test 全绿(110 单测+40 契约)。


### v0.0.4-beta.7（2026-07-08）

- [修复] 判断结构流程线：**「判断」case 与 默认 之间的 body 多于一行时，内侧竖线在 case 的下一行断开**（用户结构：判断开始/空/判断()/空/空/默认/空/判断结束，第8行处内侧线缺失）。根因：内侧线由两段逻辑各标一段——case 衔接逻辑只标「case 的上一可见行 → case 行」（进入段），隐藏默认的延续段只标「默认的上一可见行 → 末端 ▼」；body 单行时两段恰好首尾相接，多行时中间行两段都不覆盖 → 断开。修复：case 衔接逻辑（EycTableEditor flow 重映射 memo 的判断 branch+start 重叠处理）补一段**向下延续**——从 case 行的下一可见行起逐行给同深度 through 段补 hasInnerVert(+FromAbove)，直到接上已标记的锚行（默认/下一 case 的上一可见行）或遇到同深度 branch/start/end 边界为止；body 单行时首行即锚行、立即停止，既有场景（20场景审计基线中 s04/s07/s10 等判断场景）零变化。验证：npm test 全绿(110 单测+40 契约)；用户结构 HMR 复核。
- [修复] 嵌套结构流程线：**分支（如 否则）的内容行本身是内层结构时（否则体首行就是嵌套如果），外层分支的右向箭头与横虚线停在本格 right:1ch、与内层结构的外侧竖线之间留一截缺口**（用户截图：第5行如果的否则分支箭头在第7行没接到嵌套如果的流程线上）。修复：渲染层 renderFlowSegsLine 普通路径对「branch 格 + 同行紧邻下一深度是 start 格」加专用类 `eyc-flow-arrow-into-inner`（复用既有 hasInnerStartBelow/stagger 判定语境、但限定紧邻 `slots[d+1]?.type==='start'`）；CSS 将该格的 `::after` 横线、`.eyc-flow-arrow-right`、以及 hasInnerLink 变体的 `.eyc-flow-outer-horz`/`.eyc-flow-outer-arrow` 的 right 从 1ch 改为 **-3ch**——横线穿过内层格、箭头尖画到内层命令文本左边（下一格 right:1ch 处，即嵌套「如」字左边、与普通分支箭头到正文的间距一致；初版 -4px 只到内层外侧竖线，用户要求继续延伸到字前）；规则放在 stagger 规则之后仅覆盖 right、保留 stagger 的 top:35% 错位。同深度 branch+start 重叠（判断 case 行，overlap-slot 路径）与普通正文行不受影响。验证：新增 4 条渲染单测（紧邻内层加类、普通行不加、overlap 不加、隔格不加），npm test 全绿(110 单测+40 契约)。
- [新增] **命令实参数据类型匹配检查**（问题面板）——此前所有命令的参数都不查类型：`写到文件 (1.txt, …)` 文件名参数是文本型、裸写 `1.txt`（未加引号）不报任何问题。现诊断按命令签名逐参检查：①**数字开头的非法单词**（如 `1.txt`、`3x`，易语言标识符不能数字开头）→ 报「参数"文件名"的值 1.txt 不是有效表达式；文本内容需加引号（如：“1.txt”）」；②**类型族不匹配**（数值族/文本/逻辑/字节集/日期时间五族）→ 报「参数"X"类型不匹配：需要 文本型，当前为 整数型」，如整数传文本型参、文本传整数型参、数值传逻辑型条件。实参类型推断：字符串字面量/数值字面量/真假/作用域变量类型（复用 buildTypeScopes/resolveVarTypeAtLine）/嵌套命令调用返回类型（并**递归检查嵌套调用自身的参数**）/纯数值表达式（复用 isPureNumericExpression）；**保守跳过**无法可靠推断的（含运算符的表达式、#常量、成员访问 如 编辑框1.内容、未知变量、省略参数），通用型/数组参数/自定义类型不参与判定，杜绝误报。实现：editorDiagnosticsShared 加 `commandSignatures` 请求字段 + `checkCommandArgTypes`（引号感知的调用扫描、顶层逗号切参、列号精确到参数起始）；EycTableEditor 构造签名表（支持库命令+DLL命令；用户子程序补全项无参数类型、天然跳过）经 worker 管线透传。验证：新增 12 条单测（用户场景 1.txt、引号后不报、各族错配、作用域变量、嵌套返回类型、嵌套内参、通用型/重复参数、保守跳过、字符串内不误触发、无签名表不检查），npm test 全绿(106 单测+40 契约)。（行内红标暂未同步此检查，问题面板先行。）
- [新增] **展开参数输入框接入自动补全弹窗**——此前只有主代码行会弹补全，参数行输入「到」/「dao」毫无反应，让用户误以为「到字节集」命令不存在（其实核心库有）。四处参数输入框（主参数行/赋值右值/右值参数/嵌套表达式行）onChange 接入 updateCompletion、onKeyDown 接入 applyCompletionPopupKey（↑↓选择、Enter/Tab 采纳、Esc 关闭，先于 Enter 提交判定）；updateCompletion 弹窗定位改为按上下文锚定活动输入框（参数编辑时 inputRef 未渲染、原先弹窗位置不更新）；applyCompletion 的光标读取/回焦同样按上下文选 paramInputRef，且参数上下文视为表达式、命令采纳后自动补括号「（）」光标置于括号内；补 useEffect 编辑态结束统一关弹窗防残留。applyCompletionPopupKey 上下箭头的回焦仅代码行上下文做（参数框焦点本在自身，经 editCellRef 判定、deps 不变以保契约源码锁）。验证：npm test 全绿(94 单测+40 契约)。
- [修复] 展开参数里输入**未配平括号**（如给「写到文件」的数据参数敲「到字节集(」）时，上方命令行**每敲一键在右边多出一组重复的同参数命令**（用户截图：`写到文件 (1.txt,到字节集()(,到字节集(),到字节集()()`）。根因链：①liveUpdate 每键把参数值实时写回主命令行，值处于「到字节集(」中间态时，未配平的开括号把外层调用自身的右括号吞掉、整行括号结构破裂；②下一键 replaceCallArg 在破行上找不到外层闭括号（found=false）→ 参数边界丢失 → 走"索引超出则追加"分支，每键在行尾追加一组 `,到字节集(...)`（级联损坏）。修复：editorTextUtils 新增 `isArgParensBalanced`（括号/引号配平判定，字符串字面量内不计）与 `balanceArgParens`（按半/全角逐个补齐缺失的右括号/引号，深度为负不修交给诊断）；liveUpdate 参数分支加守卫——**未配平不写回**（输入框本地照常编辑，配平后再同步）；提交路径（参数/赋值右值/右值参数三分支）统一 `balanceArgParens` 兜底补平（「到字节集(」失焦→「到字节集()」）。顺带查证：**「到字节集」核心支持库里有**（krnln.ToBin，1 参数通用型、返回字节集、含 windows 实现），非命令缺失。验证：新增 10 条单测（配平判定/补平/字符串内括号不计/旧 bug 机制链复现/新守卫链路），npm test 全绿(94 单测+40 契约)。
- [新增] 表格代码编辑器：**行多选状态下按空格键等同删除**（易语言代码没有空格语义，空格键让给删除）——走与 Delete/Backspace 完全相同的多选删除路径（最小结构保护、配对标记收集、溶解、ensureMinimalFlowBodies 等）。仅在有行选区时劫持空格；输入框内编辑正常文本时空格照常输入（window 监听器对 INPUT/TEXTAREA 提前返回，天然不受影响）。实现：EycTableEditor.tsx 多选删除块的触发条件加 `e.key === ' ' && !ctrl && selectedLines.size > 0`。typecheck 通过；npm test 全绿(84 单测+40 契约)。


### v0.0.4-beta.6（2026-07-07）

- [新增] 全局撤销/重做：跨代码编辑器与可视化设计器、按动作时间顺序全局撤销/重做（Editor 层单一快照栈，代码/设计器 onChange 同栈、撤销自动切到对应标签、600ms 合并连续同类改动）；Ctrl+Z/Y、工具栏、右键均走此栈，EycTableEditor 内部旧栈废弃、避免与 App 快捷键双重撤销；窗口/控件改名也纳入撤销（因 renameWindow 对称，撤销=反向再改一次名，复用完整改名机制）。
- [新增] 可视化设计器右键菜单（照易语言）：撤销/删除/复制/剪切/粘贴/位置及尺寸(对齐子菜单)/到最顶层/到最底层/锁定/解除锁定/菜单编辑器/预览/跳回先前位置/到对应窗口程序集/查看数据类型定义。控件跨窗体剪贴板、层级重排 controls 数组、锁定禁拖/缩放、Ctrl+C/X/V/T 捕获阶段避与 App 冲突；到对应窗口程序集(Ctrl+U)、跳回先前位置(Ctrl+J 全局)、查看数据类型定义弹框列属性/事件。
- [新增] 窗口预览（Ctrl+Enter）：以当前窗体为启动窗口编译运行、跳过所有 .eyc 转译（利用编译器为事件生成的 WEAK 空实现），得到真实系统窗口外观、点击无逻辑；加 `CompileOptions.previewWindow`，不受问题面板阻断。
- [新增] 菜单编辑器（Ctrl+E）：仿易语言/VB6 对话框编辑窗口菜单（存 .efw 的 menu 字段、随全局撤销）；编译器生成 Win32 菜单（CreateMenu/CreatePopupMenu/AppendMenuW/SetMenu + WM_COMMAND→`_名_被选择()` 弱空实现，预览也显示菜单栏）；设计器画布渲染菜单栏、单击菜单项生成/跳转 `_名_被选择` 事件；名称跟随标题/必填/不能数字开头/不能重复/改名同步源代码 `_旧_被选择`→`_新_被选择`。
- [优化] 对齐工具条与顶部工具栏按钮悬停由填充背景改为亮色圆角描边环（inset box-shadow 跟随圆角、不撑大盒子）。
- 说明：用户可见变更见《版本更新说明.md》v0.0.4-beta.6；完整开发细节见提交 `v0.0.4-beta.6`。

### v0.0.4-beta.5（2026-07-06）

- [优化] 顶部工具栏的**对齐按钮**（左/右/顶/底对齐、水平/垂直居中、相同宽/高/大小 共 9 个）不再常驻顶部工具栏（此前在代码编辑器里也显示、无意义），**独立到可视化设计器内、标尺下方**——做成一条悬浮对齐工具条，只在设计窗口时出现，选中 ≥2 个控件时可用。实现：对齐逻辑从「工具栏 → App `setAlignAction` → VisualDesigner `alignAction` prop → effect 应用」改为 **VisualDesigner 内部 `applyAlign` 函数**直接被标尺下方的工具条按钮调用；连带清掉 Toolbar 的对齐按钮组与 `onAlign`/`hasControlSelected` props、App→Editor→VisualDesigner 的 `alignAction`/`onAlignDone` 透传链与 `multiSelectCount` 死状态。新增 `.vd-align-bar` 悬浮在标尺(24px)下方左侧（top:28/left:28）。typecheck 通过；npm test 全绿(84 单测+40 契约)。（放置为悬浮小工具条，位置可按需再调。）

- [修复] 剪切/删除某流程段**唯一的一行正文**（如「如果」体里只有 `调试输出` 一行、没有额外空行）后，该段**塌成空瘪结构**（「如果」直接紧贴「否则」、中间一条 body 都不剩）；且**结构尾部（如果结束后）也没留普通空行**，导致没法在结构外面接着写命令。上一版的 protectMinimalFlowInPartialSelection 只能**保护已存在的空 body**，救不了「body 全是正文、没有空行可留」。修复：新增删除后兜底 `ensureMinimalFlowBodies`，接入三条多选删除路径（applyFlowAwareDeletion / 键盘 Ctrl+X / 键盘 Delete），做两件事：**(a)** 凡「开启标记(head/branch)紧跟同深度闭合标记(branch/end)、中间无 body」的空瘪段，补一条空行占位；**(b)** 顶层结构结束(depth-0 end)正好落在子程序末尾（到文件尾、或紧跟下一个 `.子程序/.程序集`）时，补一条**尾部普通空行**，好在结构外接着写。**顺带修复**空的「如果」体在表格模式塌行/显示不全（段内始终有空 body 即正常显示可点入）。验证 .tmp-harness/repro-flowedit7：如果体仅 `调试输出`→剪切后 `如果/空/否则/空/如果结束/空`（如果段留一条空 body、不再紧贴否则，如果结束后有尾部空行）；repro-flowedit1/2/5、repro-cutclip 回归不变；npm test 全绿(84 单测+40 契约)。

- [修复] 表格模式下多选**剪切/复制**流程结构内的正文（如只选「调试输出」剪切）时，**剪贴板内容会多出隐藏的配对标记**（`否则`、`如果结束` 等），用户报"剪切的文本多了否则和如果结束、应该只剪切调试输出出来"。根因：范围拖选用 `rangeSet(anchor, li)` 填满整个 index 区间，把区间内**表格模式隐藏的配对标记**（否则/默认/如果结束/判断结束/如果真结束）也纳进选区；而 `getSelectedSourceText` 与键盘 Ctrl+C/Ctrl+X 直接把选区**所有**行拼进剪贴板，隐藏标记就漏了出来（粘贴会得到孤立的否则/如果结束）。修复：新增模块级 `filterClipboardSelectionIndices(ls, sorted)`——隐藏标记若其**结构头**（如果/判断开始/循环首 等，向上找同深度「有 start 非 branch」行）**不在选区里**（用户只选了中间可见正文、没打算连标记一起剪），从剪贴板剔除；头也在选区=整体复制该结构则**保留**标记（粘贴可完整重建）。接入全部剪贴板路径：getSelectedSourceText（右键剪切/复制、copySelectionToClipboard 等共用）+ 键盘 Ctrl+C + 键盘 Ctrl+X。验证 .tmp-harness/repro-cutclip（剪切「调试输出」→剪贴板只含 `调试输出`+可见空行、无否则/如果结束）、repro-cutclip2（选中含如果头整体复制→剪贴板完整含 `.如果/.否则/.如果结束`）；npm test 全绿(84 单测+40 契约)。

- [修复] 多选**剪切/删除某流程段的整段 body（正文 + 尾随空行）**后，该段被删空瘪（头紧跟下一个标记、中间无 body），最小结构没保住。例：如果段 body=`[调试输出, 空行]`，从「调试输出」拖选到下方空行右键剪切 → 如果段变成头紧跟否则、中间无 body（用户实测：只剩「如果 + 一条空行」）。根因：最小占位保护 protectMinimalFlowInPartialSelection 里空 body 的保护判据只认「**上一行**是流程标记」（段首那条空 body），漏了「**下一行**是本段分支/结束标记」的段尾空 body——用户结构里那条空行的上一行是正文(调试输出)，故 `isSkel(i-1)` 判否、没被保护，连同正文一起删掉、段落空瘪。修复：空 body 保护判据补上 `nextIsSectionEnd`（下一行有同深度的 branch/end 段）——段首**或**段尾的边界空行都保护，从而给该段留一条最小空 body。验证 .tmp-harness/repro-flowedit5：复现用户剪切→结果 `如果/空/否则/空/如果结束`（如果段留一条空 body、不再空瘪）；repro-flowedit1/2 回归不变；npm test 全绿(84 单测+40 契约)。

- [修复] 流程结构体内「命令头/分支标记 与 正文命令 之间」多敲出来的空行现在可正常删除。例：`如果（a ＝ 1）`↵空行↵`调试输出(a)`，那条空行之前按 Backspace 会被「最小结构保护」误拦（只上移光标不删行）——因为保护判据只看「上一行是流程标记」。实际上只要下方还有正文（如 调试输出），这条空行就是回车多敲的多余行、应可删。修复：applyEmptyDeleteKey 的保护判据加「下一行也得是本结构的分支/结束标记(branch/end)」——只有**上紧邻头/分支、下紧邻分支/结束、中间无正文**的「唯一最小占位 body」才保护；下方是正文（只有 through 穿越段）时放行正常删除（删该行 + 光标上移到上一行命令本身行末尾）。多选删除的最小结构保护语义不变（选中整段 body 删除时仍保留一条最小空 body）。验证 .tmp-harness/repro-flowedit1：如果与调试输出间的空行 Backspace→删除、光标到如果行末尾(col10、调试输出上移)；否则区最小空 body→仍保护不删。npm test 全绿(84 单测+40 契约)。

- [优化] 输入**数字开头**的内容不再弹补全窗（如 `.如果真（1）` 里敲 `1` 会去 fuzzy 匹配含「1」的命令/变量，打扰输入）。易语言标识符不能以数字开头，故数字起始的词绝不是命令/变量/类型/类名。实现：updateCompletion 在解析出当前词后加一道守卫——`!hashMode && !isMemberAccess && /^[0-9０-９]/.test(word)` 即 `setAcVisible(false)` 返回（半/全角数字都拦；#常量、成员访问 `obj.` 的前导字符非标识符起始，不受影响）。验证：.tmp-harness/repro-numac2 stash 对比——有修复时输入「1」补全项为空、输入「变」仍出「变量1」；无修复时「1」会匹配出「变量1」+类方法（正是用户报的打扰）。npm test 全绿(84 单测+40 契约)。

### v0.0.4-beta.4（2026-07-06）

- [修复] 窗口程序里把**控件文本（`编辑框1.内容` 等）赋值给文本型变量**编译报错：`assigning to 'wchar_t*' from 'const wchar_t*' discards qualifiers`（`a = yc_get_control_text(L"编辑框1")`，a 是文本型=`wchar_t*`）。根因：`yc_get_control_text` 用 4 槽轮转缓冲、返回 `const wchar_t*`，而文本型变量是 `wchar_t*`，赋值丢弃 const。轮转缓冲对「赋值即拷贝」的值语义**本身也是错的**——`a=编辑框.内容` 后再读 ≥4 个控件文本，a 所指的槽被复用、内容被悄悄覆盖（潜在数据错乱）。修复：`yc_get_control_text` 改为每次 `malloc` 独立堆拷贝、返回 `wchar_t*`，与其余所有文本取值函数（`yc_wcsdup_text` 系）一致，一并解决 const 报错与别名隐患；声明(extern)与定义同步改、移除轮转缓冲全局量、`TRANSPILE_CACHE_VERSION` 7→8 令缓存 .cpp 重生。验证：真编译用户实际窗口项目通过（无 `discards qualifiers`、`success=true`，temp/main.cpp 已生成 `wchar_t* yc_get_control_text`）；npm test 全绿(84 单测+40 契约，含 compilerAuditFixes 真编译回归)。

### v0.0.4-beta.3（2026-07-06）

- [修复] 变量汇总面板左侧（与预览缩略图 minimap 交界处）出现一条贯穿全高的**蓝色竖条**：新开 IDE / 打开项目就在，最大化 / 收展变量面板 / 开关输出面板 / 打开设置对话框都能让它消失。**真凶（用户 DevTools 定位）= 全局键盘焦点描边落到了编辑器可聚焦容器上**。global.css 有 `:focus-visible { outline: var(--focus-ring-size) solid var(--border-focus); outline-offset: 1px }`（蓝 #007fd4）；编辑器主体 `.eyc-table-wrapper`（挂载即被 `focusWrapper()` 程序化聚焦）等容器**右缘恰好压在与变量汇总面板的交界处**，这圈焦点描边的右边就渲染成一条贯穿全高的蓝竖线。为何"任意窗口操作即消失"：点最大化/面板/对话框都带鼠标点击→焦点模态从键盘切到鼠标→`:focus-visible` 熄灭→描边消失（一度被误判为"重绘残影/合成层 ghost"，其实是这个）。`.eyc-table-wrapper{outline:none}` 本想压掉它，但与全局 `:focus-visible` **同为 0,1,0 优先级、按源码/主题注入顺序定胜负**，dev 下全局规则反超→描边显形。修复：加更高优先级规则 `.eyc-table-editor :focus-visible { outline:none }`（后代选择器 0,2,0，稳压全局 0,1,0，不受注入顺序影响）统一盖掉编辑器内的焦点描边；输入框仍各自有 border 焦点提示。**另保留两处相关硬化**：①`.eyc-minimap:focus-visible` 不再画 inset 蓝环（那也是 --border-focus、其右缘同在该交界，键盘聚焦 minimap 时会现同款蓝线；box-shadow 改回常态投影，焦点提示交给视口条）；②`editorRootRef` 挂 `selectstart` 监听、非 input/textarea 一律 preventDefault（防拖动/多选时经非输入区发起原生空选区，另一种潜在蓝条源）。**走过的弯路（留记）**：先后误判为原生文本选择、minimap `:focus-visible` 环、GPU 合成层重绘残影——都因 Playwright harness 复现不出（合成环境无真实键盘焦点/原生选择/GPU 残影），最终靠用户开 DevTools 勾掉 `:root` 的 `--border-focus` 一试即定位。验证：typecheck 通过、npm test 全绿(84 单测+40 契约)；dev 下 HMR 生效、真机复核。

- [修复] 在已有流程结构上方**新输入**一个流程命令（如下方已有「判断」结构，在其上方空行敲「如果」），**还没上屏/失焦自动格式化时**就实时画出一条流程线、且歪画到下方判断结构的「判断结束」位置。根因：驱动流程线与隐藏标记的 useEditorBlocksModel 直接吃 `currentText`——而 live-update 会把正编辑行的半成品（半个「如果」）实时写进 currentText，于是流程模型立刻把它当流程头参与结构计算，画出未完成的流程线。修复：新增 `flowModelText` useMemo，编辑代码行期间把「正编辑行」还原成其**编辑前原始文本**（新增 `editOrigRawLineRef`，在 startEditLine/beginCodeLineEdit 两处捕获），只喂给流程模型（blocks/flowLines）；因 live-update 只改这一行，还原这一行即完全还原编辑前结构，正编辑行本身仍以输入框显示 editVal、不受影响。提交清空 editCell 后 flowModelText 回落 currentText，流程线正常绘制。此改**同时涵盖**上一条头删除瞬时闪标记问题（编辑期头行被还原→结构不破裂）。验证：.tmp-harness/repro-typeflow（下方判断结构上方空行输入「如果」编辑中→流程线仅在判断结构 idx5/6、idx4 无线、不画到下方）、repro-typeflow2（上屏后如果结构 idx4/5/7 有线、下方判断 idx10/11 完好）；typecheck 通过；npm test 全绿(84 单测+40 契约)。

### v0.0.4-beta.2（2026-07-06）

- [修复] 删除流程命令头（如退格清空「如果」头）**过程中**，会瞬时闪现出「否则/如果结束」等本应隐藏的配对标记，其流程线还会画到下方别的结构体的结束位置。根因：编辑头时 live-update 把头写空→结构瞬时破裂→配对标记变孤立→visibleBlocks 的「孤立标记保持可见」规则让它们闪现、流程线按破裂模型画歪。修复：visibleBlocks 加 `editingFlowHead = editCell!=null && wasFlowStartRef.current` 判定——**编辑流程头期间，隐藏标记一律继续隐藏**（不走孤立可见），等提交溶解时随结构一并消失（都溶解了也不需要流程线）；useMemo 依赖加 editCell 使其在编辑起止时重算。验证：.tmp-harness/repro-headedit（删如果头到空的编辑中，可见的隐藏标记=空、如果的流程线消失不画到下方判断；下方判断线完好）、repro-headedit3（失焦后如果溶解 0/0、下方判断完好 1/1）；npm test 全绿(84 单测+40 契约)。

- [修复] 多 case 分支的判断结构，范围多选（含隐藏的默认）删除中间 case 时，默认漏保护被删、整条判断链散架/破坏上边结构。根因：裸「判断」case 在流程模型里既是 branch 又是 **start** 段（判断是 FLOW_START 关键字）。protectMinimalFlowInPartialSelection 找结构头时用「最近的 start 段」，会把默认/判断结束的头误定位到相邻的 case（而非判断开始）；该 case 在选区里时，默认/结束就 selection.has(head)=true 而漏保护 → 被删（范围选会把隐藏默认纳入，拖选不会因隐藏行不入选而侥幸没触发）。修复两点：①找头改为「有 start 但**非 branch**」的行（即真正的判断开始/如果/循环首，跳过既 start 又 branch 的 case）；②裸「判断」case 直接排除出保护（它是额外分支、可删，不属最小结构）。这样范围选含默认时默认按判断开始定位、判断开始不在选区→默认/结束受保护，只删中间 case。验证：单测复刻判据——范围选 {cases+默认,不含头}→默认保护、case 可删；.tmp-harness/repro-judgecase2/3/6 拖选/退格删中间 case 结构完整；npm test 全绿(84 单测+40 契约)。

### v0.0.4-beta.1（2026-07-06）

- [修复] 循环类流程尾命令丢括号 + 隐藏配对标记混进补全弹窗（三处）：①带参数的流程尾命令（如「循环判断尾」带「条件」）自动补全时没加括号；②无参的循环尾（判断循环尾/计次循环尾/变量循环尾）也没括号（易语言惯例命令都带括号，与循环首 `（）` 不一致）；③补全弹窗里出现「否则/判断结束」等隐藏配对标记，手打会造孤立标记。根因①②：formatCommandLine 流程自动补齐的 extra 行构建里 `if (FLOW_KW.has(kw)) return 无括号` 抢在参数判定之前——而循环尾都在 FLOW_KW 里，导致带参尾命令丢括号、无参尾命令也不带括号。修：把参数判定移到 FLOW_KW 之前（有参→带括号铺参数位），再把「一律不加括号」的条件从 FLOW_KW 收窄为 TABLE_MODE_HIDDEN_FLOW_COMMANDS（只有真正隐藏的结构标记 否则/默认/各种结束 不带括号，其余流程尾命令带空括号）。根因③：补全源 defaultSourceList 直接铺了 allCommandsRef 全部命令；修：`allCommandsRef.filter(c => !TABLE_MODE_HIDDEN_FLOW_COMMANDS.has(c.name))` 剔除隐藏标记。验证：.tmp-harness/repro-loopparen2——判断循环首/循环判断首/计次循环首 自动补全后尾行均带括号（`循环判断尾（）` 等；真机有命令目录时「循环判断尾」会铺出「条件」参数位）；npm test 全绿(84 单测+40 契约)。（注：补全过滤因 harness 无命令目录未能端到端跑，逻辑为直接 filter，已 typecheck。）

- [修复] 问题面板不报「表达式/流程条件里引用的未定义变量」——如判断条件 `判断开始（a ≠ 1）` 里 a 未声明却无报错。此前诊断只查赋值目标（assignTarget）和命令名（funccolor），不查被引用的标识符。修复两处：①editorCoreUtils colorExpr 补上 `≠ ≥ ≤ ＜ ＞ ＋ － × ÷` 为运算符（此前只认半角 <>!=<=>=/＝，formatOps 产出的全角比较符没被识别→条件里 `a ≠ 1` 被 pm 兜底正则连着空格整段吞成一个 span，两侧变量分不开），并给 pm 兜底正则加排除这些运算符与空白（`\s`），使标识符被单独切分；②editorDiagnosticsShared 在 span 循环里对 `cls===''` 的干净标识符（`^[一-龥A-Za-z_]\w*$`）判定：不在 validCommandNames（已含库命令+DLL+用户子程序+全部变量/参数+流程关键字）、allKnownVarNames、reservedNames 里 → 报「未定义变量」。#常量走 conscolor、命令调用走 funccolor、数字不匹配标识符正则，均不误报。EycTableEditor 渲染标记（line ~7469）同步：cls==='' 的未知标识符加 eyc-cmd-invalid 红标，与面板一致。验证：单测 A（条件里未声明 a→报）/B（声明后不报）/C（参数 p、循环变量 i、命令 取文本长度、`p ＋ i`/`≥` 均不误报）；npm test 全绿(84 单测+40 契约)。

- [修复] 多选删除会把流程结构删残——之前只保护了退格路径的最小结构，多选（拖选行 + Delete/Ctrl+X）不受保护：选中结构里的空 body 行/默认等标记删除后结构缺胳膊少腿。新规则（用户要求）：多选**若未包含某流程结构的头**（即不是"完整删除整个命令"），则保留该结构的最小结构——骨架行（判断开始/默认/否则/判断结束 等 start/branch/end 段）与每段紧跟骨架后的最小空 body 行都不删；选区**含头**=用户要整块删/溶解，正常删。实现：新增 protectMinimalFlowInPartialSelection(ls, selection)——computeFlowLines 复算，对选区里"是骨架行 或 是紧跟骨架后的空 body 行"且"其结构头不在选区"的行，收进保护集从删除集剔除；三条多选删除路径（applyFlowAwareDeletion、窗口 keydown 的 Ctrl+X line1364、Delete/Backspace line1394）都接入。内容行（非空 body）仍可删（只保护空的最小 body 与骨架），单个流程头选中删除仍走 dissolveSingleFlowHeadIfAny 溶解（含头=删整块，不保护）。验证：.tmp-harness/repro-multidel2（选首 body/选 body+默认+body→结构完整开始1默认1结束1；选整个结构含头→全删；选头+body→溶解）、repro-multidel3（选内容行→内容删、骨架保留）；npm test 全绿(84 单测+40 契约)。
  - **[追加] 只对「非循环」结构做最小保护**（用户细化）：判断/如果/**如果真**→ 选到标记/体做最小保护（如果真在命令行回车仍能在体内加行，故删到只剩「如果真/如果真结束」外壳也保留、不溶解）；只有**各类循环**（计次/判断循环/循环判断/变量）**不保护**，选到尾/体就经 repairBrokenFlowAfterDelete 正常溶解。判据用流程段的 flowKind——protectMinimalFlowInPartialSelection 定位结构头后 `headIsLoop = map.get(headLine).some(s=>s.depth===depth && s.type==='start' && s.flowKind==='loop')`，是 loop 即跳过不保护。（初版误按"有无分支"分，把如果真也当无分支→溶解，被用户纠正：如果真要保护。）验证 repro-iftrue-protect（如果真选 body+尾→如果真/如果真结束保留、只删 body）、repro-loopdis（循环仍溶解、判断仍保护）、repro-multidel2 回归不变。

### v0.0.3-beta.67（2026-07-05）

- [修复] 流程命令条件里的运算符不自动规范：如判断/如果的 `a!=1`（或全角 `a！=1`）不转成 `a ≠ 1`。根因：formatOps（`!=`/`<>`→`≠`、`>=`→`≥`、`<=`→`≤`、`=`→`＝` 等）此前只应用于赋值行（a = expr），流程命令的条件参数（括号内 argText）没走它。修复：formatCommandLine 里对流程命令的 argText 套 formatOps——preferJudgeBranch 分支（line 1992）与流程自动补齐路径（line 2012）都改。全角 `！=` 经 normalizeCodeLineInput 先 `！`→`!` 再由 formatOps 转 `≠`，故两种写法都成 `≠`。formatOps 内会占位保护字符串字面量，不污染引号内容；`=`→`＝` 对条件也正确（易语言判断条件用 `＝`，见 compilerAuditFixes 夹具 `.判断开始 (n ＝ 1)`）。验证：.tmp-harness/repro-neq（`a!=1`/`a！=1`→`a ≠ 1`、`a>=1`→`a ≥ 1`）；npm test 全绿(84 单测+40 契约)。

- [说明] 表格里偶发显示出「默认/判断结束/否则/如果结束」等本应隐藏的流程标记：这是**孤立（未配对）标记**——按设计，只有配对成功（有对应 branch/end 段）的标记才隐藏，孤立标记保持可见以免文件内容凭空消失。其根因是结构被破坏后残留的孤立标记，而本会话一连串结构修复（编辑分支参数散架、如果头删除残留否则、编辑流程头参数散架等）正是消除这些孤立标记的源头。验证 repro-markerhide：编辑有效判断/如果结构的参数后，默认/判断结束/否则/如果结束 均无泄漏。测试员所用 beta.66 尚缺本会话后段修复，故仍会见到；重打新版后应大幅减少。

- [修复] 流程内已有内容时再编辑流程头（如判断开始/如果）的参数，会把内容甩到结构外并重复出默认/判断结束（"像重新溶解又重新格式化了一遍"）。根因链：①编辑已有流程头提交时，formatCommandLine 的 +4 自动缩进（本为"新键入"设计）会把已放置的头**多推一层**（如从 indent0 推到 4）——因 dedent-by-4 只在 baseIndent≥4 时补偿，头在 indent0/其它非 4 缩进时不补偿；②头被推到 indent4 后与下方 indent0 的默认/判断结束**错位**；③下次再改参数，existingFlowStructureIsIntact 扫描发现"标记行缩进 < 头缩进"判结构不完整→shouldDissolveExistingFlow 触发溶解+重排→内容 dedent 出结构、旧默认/判断结束残留、又补一套新的（重复）。修复（最小、不动溶解契约）：commit 里对"编辑已有流程头且关键字不变"（`isSameFlowKeywordEdit = editingExistingFlowStart && cmdCheckName===effectiveOriginalFlowKw`）强制 `mainLine = effectiveFlowOrigIndent + mainLine.trimStart()`——保原缩进、不再 +4 推移，从根上断掉错位→误溶解链。验证：.tmp-harness/repro-editparam3（头 indent0 结构含内容，连续两次改参数→头始终 indent0、内容/默认/判断结束原样不动、无重复无溶解）、repro-editparam2（自然 indent4 结构改参数仍正确）、repro-dissolve-reg（头删空/嵌套溶解回归不受影响）；npm test 全绿(84 单测+40 契约，含 keyboard-dispatch 契约的 preserves/dissolve 两条源码锁未改动)。

### v0.0.3-beta.66（2026-07-04）

- [修复] 删除/退格清空「如果」等**带分支结构**的头后残留「否则」（有时连「如果结束」）。根因：退格清空流程头时 live-update 会把本块的分支/结束行去缩进到比头还浅的位置（否则/如果结束 从 indent4 掉到 0）；commit 溶解扫描里对「比头浅」的行只识别了**结束行**（endKwSet，之前为修如果真单分支补的）删除，**漏了分支行**（branchKwSet，如 否则/默认）——去缩进的否则落到末尾 `unindentSet` 分支被当普通深层内容左移保留 → 残留一条孤立「否则」。修复：EycTableEditor.tsx commit 溶解的 `lineIndentLen < baseIndentLen` 块里，分支行也删（`if (branchKwSet.has(kw)) { deleteSet.add(i); continue }`，continue 继续扫、遇去缩进的结束行才 break）。验证：.tmp-harness/repro-ifdel*（如果头删空/多退格/拖选删行→否则=0 如果结束=0）、repro-dissolve-reg（判断开始头删空→默认/判断结束都清；如果真无分支头删空→如果真结束清、不受影响；嵌套外层如果头删空→外否则/如果结束清而内层判断开始/默认/判断结束完好不误删）；npm test 全绿(84 单测+40 契约)。

- [修复] 编辑判断分支 case「判断（）」在括号内补参数、失焦提交后被误转成「判断开始」破坏结构（如 `判断 （）` 编辑成 `判断（条件A）` 却变成 `    判断开始（条件A）`，凭空多出一个判断开始头、与外层混成嵌套）。根因：commit 路径的 preferJudgeBranch 只按 shouldPreferJudgeBranchAtLine（默认区规则）判定，而分支 case 位于默认之前→判 false→formatCommandLine 走 FLOW_AUTO_COMPLETE 把裸「判断」转「判断开始」（且 dedent-by-4 令缩进翻乱）。修复：commit 路径新增 `editingExistingJudgeCase = !isVirtual && effectiveOriginalFlowKw === '判断'`（持久化的裸「判断」必为分支，因新键入判断会补全成判断开始），并三处联动——①preferJudgeBranch 纳入它（让 formatCommandLine 按分支格式化、只回「判断（args）」不补全）；②dedent-by-4 加 `!editingExistingJudgeCase` 守卫（保住内层分支的缩进，如嵌套内 indent4 的分支不被反缩进到 0）；③分支插入块加 `!editingExistingJudgeCase`（原地更新、不再重复插一条）。Enter 路径本就经 preferJudgeBranchHere（commandName==='判断'&&isJudgeStructureMarker）原地替换成「判断（args）」不受影响。验证：.tmp-harness/repro-caseparam（顶层 case 加参数→保持 `判断（条件A）` indent0）、repro-caseparam2（嵌套内层 case 加参数→保持 `    判断（条件B）` indent4；默认区键入新判断仍插入新分支）、repro-caseenter（括号内输参+Enter / +End+Enter 均保持分支不转判断开始）；npm test 全绿(84 单测+40 契约)。

### v0.0.3-beta.65（2026-07-04）

- [新增] 流程结构体保护——在结构体内的空 body 行按 Backspace 不再删除/破坏结构，而是把光标上移到上一可见行的末尾（逐行上移，直到命令头行才恢复正常字符删除）。此前在判断/如果等结构的空 body 行按退格会把该 body 行删掉（如默认体行被删、判断结束上移并结构缩水），光标还可能被困在结构内。新规则（**只保护「最小结构体」**——每段正好一条的那行空 body 行；用户在 body 里多回车产生的额外空行可正常删除，只留最小不可删）：①紧跟在流程标记/头行（start/branch/end，即 判断开始/默认/判断结束）**之后的那一条**空 body 行→光标上移到上一可见行末尾、不删行；②上一行若是普通空行（多出来的额外行）或普通命令→放行正常删除（删到只剩最小为止）；③有内容就正常删字符，删空后再退格才上移；④结构紧邻其后的首个空行(trailing)同样保护(否则从下方退回把光标推进结构又困住)；⑤命令头行(判断开始/如果 等有内容行)按到即恢复正常删除/溶解。实现：EycTableEditor.tsx applyEmptyDeleteKey 里对 Backspace 先按 computeFlowLines(prevRef.current) 判 isFlowBody(`curSegs.length>0 && !curSegs.some(type==='start')`)/isFlowTrailing(`curSegs.length===0 && prevSegs.length>0`)**且 prevIsFlowMarker(`prevSegs.some(type∈{start,branch,end})`——上一行是流程标记/头，据此区分「最小 body」与「额外空行」)**，命中则复用 ArrowLeft-at-col0 的 `applyCodeLineNavigation({type:'leftToPrevLineEnd'})`（resolveVisibleCodeLineTarget 跳隐藏行 + focusInlineInputAt('end')），否则落原删除逻辑。start 排除保证嵌套内层判断开始头仍可正常删（溶解）。验证：.tmp-harness/repro-minstruct（body 回车产额外行→额外行 BACK 可删回最小(空行4→3)、最小行 BACK 保护上移不删(空行仍3)）+ repro-bkspc2/3/4（默认体连按上移、trailing 保护、普通空行仍删、头删空仍溶解、嵌套内外完整）；npm test 全绿(84 单测+40 契约)。

- [修复] 嵌套判断内**紧挨着建兄弟判断**失败——外层判断的首个 body 里先嵌一个判断(inner1)，再在 inner1 结束之后（仍属外层 body）键入「判断」想建相邻的兄弟嵌套判断(inner2)，却被错当成 inner1 的分支 case 塞进 inner1（外层/顶层紧挨着正常、三层某些位置又碰巧正常，故极隐蔽）。根因：analyzeEnclosingJudge 的**默认行(defaultLineIdx)按缩进匹配**（`extractFlowKw==='默认' && indentLenOf===controlIndentLen`），但表格模式源码扁平——内层已闭合判断的「默认」与外层控制行同缩进(都 indent4)，于是在外层 body 定位外层默认时，会先撞上内层那个同缩进的「默认」→ 误判当前行(外层 body、在内层默认之后)落在「默认区」→ 加 case 到内层。修复：defaultLineIdx 改为**按 depth 的 judge 分支段定位**（`map.get(i).some(seg=>seg.depth===depth && seg.type==='branch' && seg.flowKind==='judge') && extractFlowKw==='默认'`），与 startLine/endLineIdx 的 depth-based 一致；endLineIdx 同时补 `flowKind==='judge'` 收紧。这样外层 body(depth0)只认外层默认(d0:branch)、跳过内层默认(d1:branch)→ 正确识别为「非默认区」→ 键入判断走嵌套建 inner2。验证：.tmp-harness/repro-inner2.mjs——outer+inner1 后在 inner1 结束后键入判断，现产出 inner2(判断开始/默认/判断结束)兄弟嵌套(idx10 起)、inner1▶next/inner2◀prev 桥接（与外层紧挨着一致）；depth 复算确认 inner1/inner2 同 d1、三层(outer d0>mid d1>innermost d2)各 body 定位正确；line56b 回归(首body→嵌套/默认body→case)仍对；npm test 全绿(84 单测+40 契约)。

- [修复] 键入「判断」的“分支 case vs 嵌套新结构”按**行位置**区分：仅当当前行处在最内层判断结构的**默认区**（默认之后、判断结束之前）时，作为该判断的新分支（case，插到默认之前）；其余位置（尤其是「判断开始」之后的**首个 body 行**）键入「判断」应新建**嵌套**判断结构（自动补全 判断开始/默认/判断结束）。用户明确此规则：判断结构自格式化后 body 行键入判断=嵌套、默认区行键入判断=加分支；加分支打「判断」、嵌套打「判断开始」。实现：EycTableEditor.tsx 新增 analyzeEnclosingJudge(lines,lineIndex)——用已修正的 computeFlowLines 复算（当前行中和成空 body 避免自污染）找出包裹当前行的最内层判断结构（按段 depth 取最内层，正确处理多层嵌套），返回控制缩进+默认行下标+结束行下标；shouldPreferJudgeBranchAtLine 改为“当前行在默认区内（lineIndex>默认行 且 <结束行；无默认则不作分支）才返回 true”；commit 与 Enter 两条插入路径按 analyzeEnclosingJudge 把「判断（）」+空 body 插到最内层判断的默认之前。注：表格模式代码左对齐显示（源码缩进被剥离），嵌套层级完全由**流程线深度**呈现（computeFlowLines 按关键字+栈定深度，扁平源亦得正确 d0/d1），故嵌套判断源码虽与外层同缩进但流程线正确显示为内层。验证：.tmp-harness/repro-line56b（首个 body 键入判断→嵌套；默认 body 键入判断→case）、repro-nest-indent（body 内「判断开始」正确嵌套）、depth 复算确认扁平嵌套源 d1 内层；npm test 全绿（84 单测+40 契约）。（历史：初版 analyzeEnclosingJudge 曾简化为“判断结构内一律 case”，经用户反馈“首个 body 键入判断应嵌套而非 case”后按行位置细化。）

### v0.0.3-beta.64（2026-07-04）

- [修复] 运行程序、被运行程序退出时主进程崩溃弹 "A JavaScript error occurred in the main process"（TypeError ERR_INVALID_ARG_TYPE: "input" must be ArrayBuffer/ArrayBufferView, received string 'libpng warning: iCCP: cHRM...'）。根因：运行路径 execFile(exePath) 未指定 encoding，默认 'utf8' 会对子进程 stdout/stderr 调 setEncoding，于是 'data' 事件回调收到的是字符串而非 Buffer；而流式解码用的 `TextDecoder.decode(data,{stream:true})` 只接受 Buffer/ArrayBufferView，传字符串抛 ERR_INVALID_ARG_TYPE，作为主进程未捕获异常令整个应用崩溃。被运行程序退出瞬间 libpng 往 stderr 写 iCCP/cHRM 告警恰好触发（空程序平时无输出故不显）。此前用 toString('utf-8') 不崩，改流式 TextDecoder 后引入。修复：compiler.ts 运行路径 execFile 加 `encoding: 'buffer'`（data 恢复为 Buffer，流式解码正常）；并加防御——decodeChunk 对字符串直接返回、不再对非 Buffer 调 decode。验证：独立 Node 脚本复现——默认 encoding 下 execFile 'data' 为 string 且 TextDecoder.decode 抛 ERR_INVALID_ARG_TYPE（与用户报错一致）；encoding:'buffer' 下为 Buffer 且 decode 正常；主进程 typecheck 通过、npm test 全绿。

- [修复] 多层嵌套结构拖选/Shift 多选删除后残留结束语句（判断结束/如果真结束/如果结束）。根因：表格模式下结束/分支行（否则/如果结束/默认/判断结束/如果真结束）隐藏，拖选只能选到可见行、range 止于最后一条可见命令，而配对的隐藏结束行常落在选区下方（未纳入删除）；已有 repairBrokenFlowAfterDelete 只处理"删了结束→溶解起始"的反方向，管不了"删了起始→结束成孤儿"。修复：新增 collectMatchingFlowMarkers(ls, 起始来源)——为每个被删流程起始行按同缩进+同名嵌套自平衡计数向下扫出配对结束行与分支行下标，纳入删除集；三条多选删除路径（applyFlowAwareDeletion 右键、Ctrl+X、Delete/Backspace）都接入。两个关键点：①起始来源需含被 sub-anchor 规则清空(forceBlank)的行——子程序首个流程起始行被保留为空行时同样丢失起始关键字，其结束也要清；②作为分支的「判断」(isEnclosedJudgeBranchLine)跳过、不带出整块判断结束（否则删单个判断分支会误删块尾）。验证：.tmp-harness/repro-nestdel.mjs（如果>如果真>判断开始/判断/默认 三层嵌套，拖选删除→0 残留；Ctrl+A 仍 0 残留）、repro-nestdel-partial（只选内层判断块→只删内层、外层如果/如果真/如果真结束/如果结束完好）；repro-deljudge（删单个判断分支→两块判断开始/默认/判断结束各 2 完好不误删）、repro-head-dissolve/repro-flow-dissolve 回归全过；npm test 全绿。

- [优化] 连续「如果真」块之间补上连通桥接（与「判断」一致）：此前连续两个如果真块，前块的如果真结束在正文行画一个向下三角箭头、后块如果真另起流程线互不相连；现改为前块结束向下拐到后块的如果真起始行、向右将箭头指向该命令（用户要求"第6行的如果真结束箭头应绘到第7行的如果真上向右拐弯指向命令"）。实现：eycFlow.ts 把桥接从"仅判断"推广到"同类连续块"——FLOW_LINK_COMMANDS 加入「如果真」，桥接循环改用 resolveFlowKind 判定前后块必须同类（judge→judge 或 ifTrue→ifTrue），跨类型（如如果真结束→判断、判断结束→如果真、如果结束→如果真）一律不桥接；EycTableEditor.tsx displayFlowLines 的"隐藏如果真结束→上一可见行画向下三角(endArrowOnly)"重映射在该结束带 hasNextFlow（桥接）时跳过，改由后块起始行的 hasPrevFlowEnd 画连接箭头。验证：.tmp-harness/repro-iftrue-bridge.mjs（连续如果真现与连续判断渲染一致：前块正文无向下三角、后块起始 has-prev-end+连接箭头；末块仍保留向下三角）+ 截图 fix-iftrue-consecutive.png；repro-bridge-crosstype.mjs 跨类型均 0 连接件；新增 eycFlow.spec 单测锁同类桥接/跨类型不桥接。**桥接连接件接外侧线**：如果真是单分支结构（无内侧分支线），其块间桥接的竖线/横线应接在外侧主结构线（left:4px、与前块外侧竖线对齐）而非内侧线（1ch+4px）——editorFlowRenderUtils 对 flowKind==="ifTrue" 的 link 元素加 eyc-flow-link-outer 类，CSS 覆盖 left:4px；判断因有内侧分支线系统仍用内侧位置（语义上正确，二者有意不同）。验证：measure-bridge.mjs 实测 如果真 link-vert.left=4px（=外侧 through::before.left）、判断=11px。npm test 全绿。

- [修复] 如果真结构溶解不完整——删/清空「如果真」头后残留孤立的「如果真结束」（还被去缩进到 0 层）。根因：如果真是单分支结构（只有如果真结束、无 否则/默认 分支），退格清空头到空时其「如果真结束」会被去缩进到比头还浅的位置（如从 indent4 掉到 0，而 如果/判断 的 否则/如果结束/默认 会留在头缩进上——差异点）；两处溶解扫描（dissolveFlowStructureAtHead 约 line 900、commit 内联溶解约 line 4283）都只匹配"与头同缩进"的结束行，够不到被去缩进的如果真结束→残留。修复：两处都补"结束行缩进 < 头缩进且 depth==0 且 kw===endKw 也识别为结束并删除+break"，容忍单分支结构结束行被去缩进的情形。验证：.tmp-harness/repro-iftrue*.mjs——简单/auto-complete/缩进错位 的如果真经 右键删/退格清空失焦/退格删空行 三路径均完整溶解无残留；repro-iftrue-nest 嵌套如果真只溶解目标结构、另一层完好（未过删）；如果头三方式溶解、循环尾溶解、判断分支不误溶解等回归全过；npm test 全绿。

- [修复] 承上条「删判断分支误溶解」——补齐 commit（编辑）路径：点进「判断」分支行、逐字符退格清空后失焦提交，同样会走 commit 的流程头溶解分支（EycTableEditor.tsx ~line 4259 `shouldDissolveExistingFlow && FLOW_START[oldKw]`）把作为分支的「判断」当块头溶解整块。修复：该处也加 `!(oldKw==='判断' && isEnclosedJudgeBranchAt(...))` 守卫（行此刻已空，按原缩进 effectiveFlowOrigIndent 向上扫判定），分支时不溶解、退格清空只把该行留空（等同普通删行）。顺带修 commit 溶解的一处既有缺陷：溶解「判断开始」头时只删了 FLOW_AUTO_COMPLETE pattern 里的 默认/判断结束，漏删同缩进的「判断」case 行（残留孤立「判断」），补 `if (kw==='判断' && (oldKw==='判断开始'||oldKw==='判断')) deleteSet.add`（循环遇判断结束即 break、更深嵌套走 unindent，故 baseIndent 的判断必属当前块；与 dissolveFlowStructureAtHead line 882 对齐）。验证：.tmp-harness/repro-editjudge-empty.mjs——退格清空块1判断分支+失焦→两块判断开始/默认/判断结束各 2 完好；退格清空判断开始头+失焦→整块溶解无残留判断；退格清空如果头→溶解；repro-head-dissolve/repro-flow-dissolve 回归全过；npm test 全绿。

- [修复] 删除判断块里的「判断」分支会错误溶解整个判断块（删掉本块的默认/判断结束、与相邻块混错），应当像普通删行一样只删该分支。根因：「判断」既在 FLOW_START（映射判断结束）又是判断块的分支/case 关键字；删除单行的溶解判定 applyFlowAwareDeletion / dissolveSingleFlowHeadIfAny（EycTableEditor.tsx）只看 FLOW_START[kw] 就把「判断」当起始行溶解。修复：新增 isEnclosedJudgeBranchLine/isEnclosedJudgeBranchAt——同缩进向上先遇 判断开始/判断/默认（处在判断块内=分支）而非 判断结束/子程序边界（=新结构头）；三条删除路径与退格清空编辑路径（applyEmptyCodeLineDelete ~5040，行已清空按原缩进扫）在「判断」是分支时一律走普通删除、不溶解。因 formatCommandLine 会把新建「判断」自动转「判断开始」，正常文档里裸「判断」几乎总是分支；此检查也对畸形输入保守兜底。验证：.tmp-harness/repro-deljudge.mjs（右键删行/退格清空 删块1判断分支→两块判断开始/默认/判断结束各 2 个完好、仅分支被删）；repro-dissolve-still.mjs（删判断开始头仍溶解本块、删如果头仍溶解）；repro-head-dissolve 三方式回归仍溶解；npm test 全绿。

- [修复] 在判断结构的默认体行键入“判断”、按空格上屏后失焦提交（点别处），只补全了括号没补全分支结构——结果“判断（）”留在原位、缩进错乱（如落到 indent0、跑到默认之后），与直接回车不一致（回车正确地把“判断（）”作为新分支插到默认之前的控制缩进）。根因：失焦走 commit 路径，其 preferJudgeBranch 分支块只认 getFlowStructureAround 返回的 prevSection.char==='默认'；但空格上屏后当前行已含“判断（）”，getFlowStructureAround 自污染（把当前行当成新结构起点）→ 判失败 → 落到末尾 splice 原样放置。回车路径（applyEnterKey 的 resolveDefaultLineIdx）本有 findPrevNonEmpty/findNextNonEmpty 兜底（当前行夹在“默认 与 判断结束”之间时按默认行定位），commit 路径缺这层兜底。修复：给 commit 路径的 preferJudgeBranch 块补相同兜底（EycTableEditor.tsx ~line 4301），并把插入改为 `splice(defaultLineIdx, 0, judgeLine, '')`（补空 body 行，与回车一致）。验证：.tmp-harness/repro-judge-full.mjs 真实组件——回车与“空格上屏+失焦”两路径现产出完全相同的结构（判断（）作分支插到默认前、控制缩进对齐、后带空 body）；P1 的“判断体内键入判断→嵌套”回归仍正确；npm test 全绿。

- [修复] 从代码行起拖多选后按 Backspace/Delete 只删了输入框里一个字符、没删中选中的多行。根因：mousedown 在代码行内容区会乐观进入编辑态（输入框聚焦），拖动转为行多选时 onMove 的行多选分支（EycTableEditor.tsx 约 line 1044）设置了 selectedLines 却没退出乐观编辑态、也没把焦点移回 wrapper——输入框仍聚焦，后续 Backspace 落到输入框删单字符（全局选中删除处理器虽在 wrapper 上，但按键被聚焦的输入框先吃掉）。修复：该分支转行多选时 `setEditCell(null)+setAcVisible(false)+optimisticLineEditRef=null+focusWrapper()`；顺带修另一条路径（行内文本拖选拖出该行转行多选，line 1027）补 focusWrapper()，否则输入框卸载后焦点落 body、inEditor 判否致 Backspace 无反应。验证：.tmp-harness/repro-multidel*.mjs 真实组件——拖选后 activeElement 从 INPUT 变回 wrapper、Backspace 正确删除全部选中行；纵向拖选/Shift 点击多选删除、正常单击编辑 Backspace 删字符（行数不变）均无回归；npm test 全绿。

- [修复] 流程线嵌套/复杂嵌套审计（Workflow 多 agent 穷尽排查，12 候选→7 确认，逐条对抗性验证），修复 5 处正确性缺陷：
  - **R1（eycFlow.ts:222 判断分支 merge）**：外退一层启发式（outdentOneLevelJudgeBranch）抢在同缩进精确匹配之前，把与外层对齐的 `.判断` 分支误吞进未闭合内层判断块（跨层污染，外层第二个判断结束段丢失成裸文本）。改成两段式（先精确同缩进就近归属、再退化外退），与 否则/默认、结束行 路径一致。
  - **R2（eycFlow.ts:231 同块）**：`.判断` 分支归属到非栈顶判断块时，漏了隐式闭合其上未闭合的内层结构（如内层 `.如果` 缺 如果结束），导致内层流程线跨越分支行、内层 end 落进新分支体。补上与 否则/默认 路径(line 269-270)一致的 implicitClose。
  - **R3（eycFlow.ts:359 桥接）**：hasNextFlow/hasPrevFlowEnd 桥接只校验后继块是判断，未校验前驱块自身也是判断——`如果结束`/`如果真结束`/`计次循环尾` 紧邻 `判断开始/判断` 时误画桥接连接件（唯一在良构已提交代码上持续可见的缺陷）。加 `if (fb.keyword !== '判断开始' && fb.keyword !== '判断') continue`。
  - **R4（EycTableEditor.tsx:2680 显示层）**：表格模式空分支区（否则/默认 与 结束行之间无可见行，如 autocomplete 刚生成骨架未填内容）被隐藏后，nextVisibleFallback 无边界约束越过 flowEndLine，把外侧结束横线锚到结构之外的可见行，画出指向结构外命令的幽灵分支箭头 ├─►。改为回退目标必须落在 flowEndLine 之前才锚定（空分支区不绘制外侧横线）。
  - **R5（EycTableEditor.css:987 stagger）**：跨深度 stagger 下内层为循环时，loop start 的向右箭头 ▶ 停在 50% 而错位后的横线在 68%，脱节。补 `.eyc-flow-start.eyc-flow-stagger-lower .eyc-flow-arrow-right { top: 68% }`（对照 branch 侧 stagger-upper 35% 箭头规则）。注：finder 首选"删 stagger"会破坏跨深度消歧，经真实组件+CSS 核实采用其备选的箭头跟随方案。
  - **P1（EycTableEditor.tsx shouldPreferJudgeBranchAtLine 默认体启发式）**：在既有判断结构的**体缩进**处键入“判断”应新建**嵌套**判断结构（自动补全 判断开始/默认/判断结束、外层完好），但旧的“默认后首行→分支”启发式在体缩进也命中，把它误当外层分支——导致外层结构被破坏（丢失外层 默认/判断结束）。改：把两处默认体分支判据（adjacent-heuristic 的 `currentIndentLen >= 默认缩进` 改为 `===`；evaluateAround 的 isDefaultBodyFirstLine 追加 `currentIndentLen === 判断控制缩进`），即**仅在控制/分支缩进处键入“判断”才作分支，更深的体缩进一律走嵌套自动补全**。裸“判断”新键入本就走嵌套（currentLineKw 取自已提交空行=''，分支判据不触发），故 first-branch body 与 默认 body 现均正确嵌套、外层完好。守卫：顶层键入“判断”仍自动补全完整结构；编辑已有“判断”分支条件不产生嵌套/重复（preservesExistingFlowStructure 兜底）。验证：.tmp-harness/repro-user-nested.mjs（first-branch body / 默认 body 键入判断 blur+Enter 均嵌套、外层 默认/判断结束 完好）+ 截图 fix-user-nested-judge.png + repro-editbranch.mjs（编辑已有分支不破坏结构）。（注：初版曾反向放宽检测把体缩进也当分支，经用户反馈"外层流程线被破坏"后纠正——用户实际要嵌套而非分支。）
  - 验证：逻辑层 R1/R2/R3 真实运行 computeFlowLines 复现+回归（新增 3 条 eycFlow.spec 单测，npm test 全绿 83 单测+40 契约）；显示/渲染层 R4/R5 用 .tmp-harness 真实组件渲染 DOM 测量+截图确认（幽灵分支消失、循环箭头对齐 68%）。逻辑层跨 4~6 层均匀/混合/判断多分支/跨子程序边界的 ground-truth 全部正确。

- [修复] 删除/清空「流程起始行」时不完整溶解整个结构——用户反馈"子程序里把如果删了，否则和如果结束还留着"。此前只处理"删结束行→起始溶解"，未处理"删起始行→分支/结束成孤儿"；且删掉头后无法可靠反推结构上边界（会误吞头上方同缩进兄弟语句），必须在头还在时定位。新增 dissolveFlowStructureAtHead（删头+分支+结束、正文反缩进），接入全部删/清空起始行路径：①删除行三路径（右键/Ctrl+X/Delete）经新的 applyFlowAwareDeletion / dissolveSingleFlowHeadIfAny——单个起始行→溶解整个结构，其余→原结束行溶解；②编辑清空起始行（backspace 到空+失焦 / 再退格删空行）——用编辑前保留的 codeLineEditOrigValRef+wasFlowOrigIndentRef 判定，且优先于子程序锚点重定向按被编辑行 lineIndex 溶解。关键坑：退格删空起始行时 flow 编辑的 live-update 已把循环体去缩进到 0（比头还浅），溶解扫描不能因"缩进<头缩进"就中断，改为只对头缩进上的流程关键字动作、扫到匹配结束/子程序边界才停。顺带：编辑结束行清空时删掉该行（不留空行）。守卫：嵌套删内层头只溶内层、改参数/删普通行不误溶解。
  - 范围：`EycTableEditor.tsx`（dissolveFlowStructureAtHead/applyFlowAwareDeletion/dissolveSingleFlowHeadIfAny + commit 结束行分支 + applyEmptyCodeLineDelete 重构 + 三删除路径接入）；契约测试更新锁新架构
  - 验证：.tmp-harness repro-head-dissolve（删如果头 右键/编辑成空/删空行 三方式均溶解否则+如果结束）、repro-nested-head（嵌套只溶内层）、repro-user-precise（删尾编辑A/B）、repro-flow-dissolve（删除行）、repro-dissolve-guard（不误溶解）全过；npm test 全绿（80 单测 + 40 契约）

- [修复] 编辑（而非删除行命令）方式清空流程结束行时不溶解——用户反馈"逐字符 Backspace 删掉计次循环尾后失焦，计次循环首和流程线还在"。此前溶解只接在删除行路径（右键删除/Ctrl+X/Delete），而用户是点进结束行单元格逐字符退格删空再失焦，走的是编辑提交/退格删空行两条路径，未接溶解。现两条编辑路径都补上：①代码行提交(commit)：原行是流程结束关键字(FLOW_END_KW，计次/判断/循环/变量循环尾等)且新值不再是该关键字 → 溶解所属结构；②退格删空行(applyEmptyCodeLineDelete)：行此刻已空、关键字丢失，用编辑前保留的 codeLineEditOrigValRef 原值判定，仅当删的正是被编辑行时溶解。守卫：改结束行参数(关键字不变)、编辑普通行到空——均不误溶解。
  - 范围：`EycTableEditor.tsx`（commit 代码行分支开头 + applyEmptyCodeLineDelete 复用 repairBrokenFlowAfterDelete）
  - 验证：.tmp-harness/repro-user-precise.mjs（退格到空+失焦 / 退格到空+删空行+失焦 两路径均溶解循环首+反缩进）、repro-dissolve-guard.mjs（编辑参数/普通行不误溶解）、repro-flow-dissolve.mjs（删除行路径无回归）；npm test 全绿

- [优化] 全项目审计第五批（高风险项，已实现待真机验证）——三项均 headless 无法验证运行时，方案经 workflow 三 agent 并行调研（含联网查 Electron 35→43 breaking changes）后逐项实现、每项 build 验证：
  ①AI API Key safeStorage 加密——三处 key（deepseek/glm/自定义模型）落盘前经 OS keyring 加密（'enc:v1:'+base64 前缀），读时解密；与存量明文迁移共存（无前缀=明文），启动时仅在检测到明文才一次性升级（避免 churn）；Linux 无 keyring 回退明文、解密失败降级空串（绝不崩、不丢其它字段）；加解密只在主进程 readIDESettings/writeIDESettings 两个漏斗内，renderer 始终见明文，浅拷贝加密防污染回传 UI。
  ②Monaco 从 jsdelivr CDN 改本地打包（治离线/内网白屏）——新增 monaco-setup（core-only editor.api + editor.worker?worker + loader.config），Editor 顶部 import；深路径无 TS 类型用 @ts-expect-error（仅供 loader.config，类型无关）；electron.vite manualChunks 把 monaco 切独立 chunk（monaco 4.96MB / app 主包 2.30MB 不含 monaco / editor.worker 540KB emit 正常）。未做（留后续，需真机验证）：lazy 组件抽取（monaco 深织入 Editor.tsx 2800 行，抽取风险高且改不了 headless 验证不了渲染）、renderer 严格 CSP（file:// 下 worker-src 'self' 是否拦 worker 无法 headless 验证，一旦拦全员白屏）。
  ③Electron 34→43 升级——electron ^43 / electron-builder ^26.15 / electron-vite ^5；审计确认无源码级 API 必改项（仅 dialog 默认目录 E43 改 Downloads 属 UX，~7 处缺 defaultPath 的对话框列入验证项）；**npm audit 25→2（1低1中）**，electron43+builder26 带入修补后的传递依赖。
  - 范围：src/main/index.ts（safeStorage 加解密+启动迁移）、src/renderer/src/components/Editor/monaco-setup.ts(新增)+Editor.tsx、electron.vite.config.ts（manualChunks）、package.json（electron/builder/vite-plugin bump）
  - 验证：node/web typecheck + eslint 0 错误；npm test 全绿（80 单测 + 40 契约）；npm run build 通过、monaco 分块正确。
  - **必须真机验证清单**（headless 覆盖不到）：①填/回显 AI key、AI 对话能用、ide-settings.json 里 key 为 enc:v1: 密文、明文迁移升级、密文改坏降级为空不崩；②文本模式编辑器实际渲染、离线可用、monaco worker 在 file:// 起没起；③Electron43 下启动/窗口行为/IPC/编译运行/终端/Android/可视化设计器渲染全链路、dialog 起始目录。

- [优化] 全项目审计第四批（工程/依赖，低风险项）：
  ①`npm audit fix`（semver 内）——25 → 12 漏洞，修掉 vite/undici/form-data 等（含 Windows dev server 任意读、NTLMv2 hash 泄露）；剩 12 项全在 electron-builder 工具链、dev-only 不进产物，需 electron-builder 大版本升级才能修（归入 Electron 升级批）。
  ②依赖重分类——dependencies 从 10 个收敛到 3 个（adm-zip/diff/iconv-lite，仅主进程运行时用）；react/react-dom/zustand/pinyin-pro/monaco-editor/@monaco-editor/react 挪 devDependencies（渲染端已由 vite 打进 bundle，node_modules 那份是 asar 死重）；删除零引用的 @tanstack/react-virtual。预计 asar 从 87MB 瘦到 ~15MB。
  ③CI quality job 加 `npm run build` 步骤——此前构建错误只有手动触发的 e2e 才拦，现每次 push/PR 都跑。
  ④package.json 补 license(MIT)/author/repository/engines(node>=22) 四个缺失字段（LICENSE 文件本已是 MIT）。
  ⑤krnln:mapping:check 假 check 修正——此前与 generate 同命令、无条件写文件污染工作区；加 --check 模式（比对 mapping+summary 忽略时间戳、不写、有差异 exit 1）。
  ⑥生产构建加 hidden sourcemap（main+renderer）+ 从打包排除 *.map（本地存档可还原崩溃栈，不进 asar/不增体积）；清理 dev 异常退出残留的 electron.vite.config.*.mjs 临时文件。
  - 范围：package.json（deps 重分类/scripts/元数据/build.files 排除 map）、package-lock.json（audit fix）、.github/workflows/ci.yml（build 步骤）、electron.vite.config.ts（sourcemap）、scripts/krnln/generate-command-mapping.mjs（--check）
  - 验证：node/web typecheck + eslint 0 错误；npm test 全绿（80 单测 + 40 契约）；npm run build 通过（渲染包 2.34MB 未变）；krnln:mapping:check 不再写文件。
  - 本批未做（三项均"无法 headless 验证 + 核心功能回归风险高"，需真机交互验证，留独立 pass）：AI API Key safeStorage 加密（解密失败会丢 key + 明文迁移边界）、Monaco 从 CDN 改本地打包（曾试：全语言打包致 9.6MB、core-only 仍 7.2MB 且深路径无类型；loader.config 初始化不当会全员白屏，比"仅离线白屏"更糟；应配合懒加载+代码分割）、Monaco 配套的 renderer CSP（与 CDN 加载互斥）、Electron 34 EOL→43 升级（回归面最广）。

- [优化] 全项目审计第三批（渲染热路径性能，5 项）——消除"每次按键贯穿 App 顶层的全项目管线"：
  ①未调用子程序标记每键触发全项目分词扫描（本会话新功能引入）——handleOpenTabsChange 里的 computeUnusedSubNames + 项目树重建 + saveOpenTabs IPC 剥离为 300ms 防抖，每键只留 setState/缓存清理。
  ②AI 聊天存档 600ms 防抖形同虚设（本会话新功能引入）——保存 effect 依赖 chatHistory，cleanup 在每个流式 delta 都判 payload 变化而立即落盘；重构为"cleanup 只清 timer、timer 触发才 stringify+落盘"，流式期间连续 delta 顺延、仅停顿 800ms 后写一次；切项目/卸载在加载 effect 的 cleanup 里落盘旧项目（histories 用 ref、不丢数据）。
  ③编译/运行输出无上限、每条触发整个 App 重渲染——用户程序循环打印上万行致 O(n²) 拷贝 + 内存无界；高频输出改 requestAnimationFrame 批量刷新 + 5000 条上限，清空时同步清缓冲。
  ④Editor 五个项目元数据 effect（全局变量/常量/数据类型/DLL命令/类）依赖 tabs、每键重跑 5 遍全文 eycToYiFormat + readDir + 逐个 readFile——改依赖派生的 debouncedTabs（滞后 300ms），这些声明不随普通代码行输入变化。
  ⑤外部文件修改轮询每键重建定时器并立即读盘——2s 轮询 effect 依赖 tabs；改用 tabsRef 读当前标签、依赖去掉 tabs，只在切换标签时重建。
  - 范围：`App.tsx`（syncProjectMetaFromTabs 防抖 + appendOutputMessage/rAF 批量 + resetOutputMessages）、`AIAssistantPanel.tsx`（flushChatSave + histories ref + 防抖重构）、`Editor.tsx`（debouncedTabs + 外部轮询 ref 化）
  - 验证：node/web typecheck + eslint 0 错误；npm test 全绿（80 单测 + 40 契约）。均为"改何时算不改算什么"、状态最终收敛的防抖/批量改动；渲染热路径单测覆盖不到，建议 dev 手测编辑流畅度与元数据/输出正确性。
  - 本批未做（更大重构/内存天花板，回归风险高，留独立 pass）：撤销栈 200 份全文快照（应 diff 化）、editCell?.lineIndex 依赖的全文 useMemo、Editor/Sidebar/AI面板 memo 化（需先解上游内联 props 锁）、库元数据渲染侧共享缓存。

- [修复] 全项目审计第二批（状态错乱/编码，9 项）：
  ①preload on/off 监听互杀——on 包裹回调致 off 只能 removeAllListeners，多组件订阅同一频道（library:loaded ×4、AI 流式 ×2 并发）时任一卸载摘掉全部；on 改为返回精确取消订阅的 disposer，冲突频道全部迁移（后果：加载新库编辑器补全静默不刷新）。
  ②运行进程 exit/error 回调无身份校验——停止后立刻重跑（产物缓存命中时百毫秒完成）时旧进程延迟 exit 清空新进程全局状态、误报"程序已退出"；加 `runningProcess === proc` 守卫（后果：停止按钮失效、断点续跑失灵）。
  ③库加载状态文件损坏静默全卸——getSavedLoadedNames 的 catch 与非数组分支返回 [] 被当成"用户主动全卸"；改返回 null（走默认全加载）+ 损坏告警（后果：半截 JSON 致所有命令报未知）。
  ④编译 worker 支持库快照冻结——常驻 worker 首次编译后库列表不再刷新，IDE 里新载入库编译报未知命令直到重启；worker 加 reloadLibraries 消息 + 主进程 6 处库变更广播统一通知 worker 重扫。
  ⑤编译零互斥——worker 对并发请求直接并行 compileProject（共享模块级状态、争用 temp/main.cpp）；worker 内加串行队列（compileChain）。
  ⑥.efw 繁简体属性名分叉——磁盘路径查繁体"最大化按鈕"、内存路径与元数据用简体"钮"，关标签页后编译窗口按钮设置被忽略；两种写法都兼容。
  ⑦控制台项目不注入支持库常量——控制台分支 transpileProjectFile 传 [] 而窗口分支传 libraryConstants，控制台用支持库常量报 undeclared；统一传 libraryConstants。
  ⑧运行输出/终端中文 chunk 边界乱码——逐 chunk toString('utf-8') 致多字节字符跨界出 �，终端启发式还会整块误翻 GBK；运行输出改流式 TextDecoder({stream:true})，终端加"扣住不完整 UTF-8 尾部再判定"消除边界假 �。
  ⑨文本变量按 (char*) 重解释编码错——文本型运行时是 wchar_t*(UTF-16)，通用 fne 分发接口要 UTF-8 char*，此前裸 (char*)(变量) 传出乱码；改经 yc_wide_to_utf8 转换（字面量路径不变，本就是 UTF-8 窄串）。
  - 范围：preload/index.ts、App.tsx/Editor.tsx/EycTableEditor.tsx/Sidebar.tsx（disposer 迁移）、compiler.ts（exit 守卫/繁简体/控制台常量/流式解码/文本变量转换）、libraryManager.ts（getSavedLoadedNames）、compile-worker.ts（队列+reloadLibraries）、compileWorkerClient.ts（notifyWorkerLibrariesChanged）、index.ts（broadcastLibrariesChanged/终端流式解码）
  - 验证：compilerAuditFixes.spec.ts 扩至 3 例（新增控制台常量+文本变量 UTF-8 转换真编译）；npm test 全绿（80 单测 + 40 契约）；node/web typecheck + eslint 0 错误

- [修复] 全项目审计第一批（正确性止血 6+2 项）：
  ①字符串拼接被整体错译——`“共” ＋ 到文本(n) ＋ “个”` 因贪婪正则被吞成单个字面量，编译通过但运行原样输出中间的变量与加号；字面量判定收紧为"内部不含同类引号"（translateExpressionToC 与 formatArgForYcCommand 两处）。
  ②编译器不识别"判断开始"多分支结构——表格编辑器落盘 .判断开始/.判断/.默认/.判断结束，编译器只认单分支 .判断，表格模式写判断块必编译失败；现生成 if / else if / else，.默认 之后再现 .判断 分支会报可读错误。
  ③导入大写扩展名 .E 工程会删除用户源文件——basename(path,ext) 剥离区分大小写导致目标目录恰好等于源文件，覆盖导入时 rmSync 删源；现大小写不敏感剥离 + 目标与源重合时中止导入双保险。
  ④rc/ 目录资源修改不触发重编译——产物指纹 stat 的路径与实际加载（优先 rc/）不一致，换图重编译静默复用旧 exe；指纹改用同一解析函数。
  ⑤project:removeFile 缺路径校验（可删授权目录外任意文件）——补 validatePath + 文件名 ../ 过滤；renameWindow/renameClassModule 的名称参数同样补穿越过滤。
  ⑥窗口标题/控件初始文本/项目名未转义直插 C 字面量——含引号或换行的标题生成非法 C 代码；三处接入既有 escapeCString。
  - 新增 tests/unit/compilerAuditFixes.spec.ts：compiler.ts 首个真实编译集成测试（vitest 注入 runtimeEnv/CompilerHost 直接调 compileProject 走 zig 工具链，验证判断开始可编译、拼接正确拆分；无 zig 环境自动跳过），另含 .E 导入目标目录用例。
  - 遗留新发现：源文件名恰为 main.eyc 时转译产物与入口 main.cpp 同名互相覆盖（低概率边界，待修）。

- [新增] 删除尾部配对流程命令 → 溶解关联结构：删除可见的结束行（计次/判断/变量循环尾等）后，失去配对的起始行与分支行一并删除、结构内部整体反缩进一层、内部完整子结构原样保留（删"计次循环尾" → 循环首溶解，只留循环体里的如果/否则/如果结束）。此前行为：非嵌套时直接拒绝删除、嵌套时保护被内层结构劫持而漏删并留下孤立循环首画线残影；旧"补回结束行"修复策略同步替换为溶解语义。安全约束：仅对"本次删除的结束关键字族"生效，文件中原本未闭合/缩进错位的结构不受删除无关行牵连；右键删除/Ctrl+X/Delete 三条删除路径行为一致。
  - 范围：`EycTableEditor.tsx`（repairBrokenFlowAfterDelete 重写为溶解 + deletedFlowEndKws 门槛；deleteLineSelection 移除 wouldBreak 拒绝；三处调用点传被删结束关键字集合）；契约测试同步更新为锁定溶解语义
  - 验证：.tmp-harness/repro-flow-dissolve.mjs（删循环尾溶解/判断循环/嵌套只溶内层/删普通行无联动 全 PASS）；npm test 全绿（77 单测 + 40 契约）

- [修复] 流程线四项缺陷（20 场景审计后修复）：
  ①未配对的流程行凭空消失——"否则/如果结束/默认/判断结束/如果真结束"此前按关键字无条件隐藏，"如果真"里误写"否则"、孤立"如果结束"、缩进错位的结束行都会在界面上消失（文件里有、界面上无）；现只隐藏在流程模型中真正配对成功（有对应 branch/end 段）的行，孤立行保持可见。
  ②缩进错位导致整块流程线静默消失——结束行/分支行与起始行缩进不一致时配对失败、整块无线；现缩进严格匹配失败后回退"就近归属最内层同类结构"，错位代码仍正常画线。
  ③漏写结束行导致内层块无线、正文回退树状字符线（│）与流程线混排——未闭合结构现隐式闭合（子程序边界/被外层结束跨越/被外层分支跨越/文末），且不跨越外层分支边界，统一为流程线视觉。
  ④判断行 branch+start 重叠时的"隐藏下箭头"是行级标志，嵌套判断中误伤同行其他深度的箭头——改为仅作用于重叠深度本身；顺带清理重复拼接的 stagger 类与恒假死代码。
  - 范围：`eycFlow.ts`（回退配对 + implicitClose + FlowStackEntry 提取）、`EycTableEditor.tsx`（visibleBlocks 隐藏前校验配对）、`editorFlowRenderUtils.tsx`（行级 hasAnyJudgeBranchStartOverlap 移除）
  - 验证：新增 tests/unit/eycFlow.spec.ts（7 例：标准配对/错位回退/隐式闭合不越界/文末闭合/孤立行无段/如果真+否则/嵌套判断归属）；.tmp-harness/repro-flow-audit(.2).mjs 20 场景截图回归（s08 恢复画线、s09 混线消失、x1/x2 行可见、x3 误伤消除，其余场景零变化）；npm test 全绿（77 单测 + 40 契约）

### v0.0.3-beta.63（2026-07-03）

- [新增] 系统设置增加"变量汇总面板"开关（默认开启）：关闭后表格模式编辑器右侧变量汇总面板与收起细条均不渲染。设置项 editorShowVarSummaryPanel 随现有设置保存机制持久化，旧配置缺字段回退默认开启。
  - 范围：`src/shared/settings.ts`（接口/默认值/resolve）、`SettingsDialog.tsx`（编辑器分组开关行）、`App.tsx`→`Editor.tsx`→`EycTableEditor.tsx`（showVarSummaryPanel 透传与渲染门控）
  - 验证：web/node typecheck + eslint 0 错误

- [新增] AI 聊天记录按项目持久化：四种模式（聊天/Plan/Agent/编辑）的对话历史保存到项目目录 `.ycide-ai-chat.json`（与标签会话 .ycide-session.json 分文件），关闭重开 IDE 或切换项目自动恢复对应项目的记录；Agent 工具调用卡片（参数/返回）一并保存恢复。写入 600ms 防抖 + 面板卸载即时落盘（关面板/关窗口不丢最后一段）；切换项目用"已加载目录"门闩防止旧历史/空历史覆盖新项目存档；每组最多保留最近 200 条防膨胀；存档损坏时静默回退空历史。
  - 范围：`src/main/index.ts`（project:saveAiChat/loadAiChat）、`src/preload/index.ts`、`AIAssistantPanel.tsx`（projectDir prop + 加载/保存 effect，editMessages 类型统一 ChatEntry[]）、`App.tsx`（传 currentProjectDir）
  - 验证：web/node typecheck + eslint 0 错误（读写为运行时行为，待人工验证：聊天后重启恢复、双项目切换互不串档）

- [优化] AI 助手聊天窗口视觉重构（参考主流聊天产品）：用户消息=右对齐圆角气泡（accent 色调、右下角小圆角）；助手回答=无气泡全宽纯文本（行高 1.6）；思考过程(reasoning)=无气泡可折叠条目（左竖线缩进小灰字；思考中标题"正在思考…"并展开，思考结束自动收起为一行"▸思考过程"，可手动再展开；从存档恢复的思考条目默认收起）；Agent 工具调用=圆角卡片（绿/红状态点 + 单行摘要省略，展开见参数/返回，返回区限高滚动）；"执行中/正在编辑"改无气泡小灰字状态行；聊天区去内框、加大消息间距；空态居中。
  - 范围：`AIAssistantPanel.tsx`（ChatEntry.kind='reasoning' + 编辑模式流式思考消息标记 + renderChatEntry 统一四模式渲染）、`AIAssistantPanel.css`（消息区样式重写）
  - 验证：.tmp-harness ?fixture=ai 静态样式预览截图（气泡/思考/工具卡片/状态行全形态）；折叠交互实测可手动收起（React details open 属性不回弹）；web typecheck + eslint 0 错误

- [修复] AI diff 删除占位行不显示被删内容、多行删除只占一行、表格删除占位行号区无红色背景：①computeDiffLineInfo 已产出被删原文但 Editor 下传表格编辑器时只保留了行号集合，占位行只能渲染空红条——现以 锚点行→被删原文 Map 传递，被删原文按行拆开、每行一个红色占位行（删除线样式、悬停看该行原文、空行保占位、同锚点多 hunk 拼接、清除高亮同步清空），虚拟化行数与小地图删除标记按删除行数联动累加避免滚动错位；②表格删除占位行的行号区历来只有左侧红条无红背景（与代码行占位不一致），补齐红背景并避免代码行占位叠色。
  - 范围：`Editor.tsx`（eycDiffDeletedTextAfterLines 状态与传递）、`EycTableEditor.tsx`（diffDeletedLinesByAnchor 拆行 + 表格/代码行占位逐行渲染 + blockLineSpans/小地图行数联动）、`EycTableEditor.css`（.eyc-diff-deleted-text + 占位 gutter 背景）
  - 验证：.tmp-harness/repro-diff-multiline.mjs（两行删除→两个占位行逐行显示、无合并符）、repro-diff-gutter-bg.mjs（两种占位 gutter 背景一致）；web typecheck + eslint 0 错误

- [新增] AI 修改与人工修改的未保存状态区分标识：AI 编辑（面板"应用建议"与 Agent edit_file 工具，均经 handleAIApplyEdit）产生的未保存更改，项目管理器橙色圆点与标签页白色圆点外包一个圆角矩形；无圆角矩形即人工编辑。人工再编辑（代码/文本/表格/设计器四个入口）即清除 AI 标识回归人工样式；AI 撤销编辑同步清除；分类文件夹的聚合圆点不区分来源。悬停提示"未保存更改（AI 修改）"。
  - 范围：`Editor.tsx`（EditorTab.aiModified + 四个人工 onChange 清除 + 标签圆点样式类）、`Editor.css`、`App.tsx`（handleAIApplyEdit 置位/handleAIUndoEdit 清除）、`Sidebar.tsx`（aiModifiedFileKeys 推导 + 树圆点样式类 + aria/title）、`Sidebar.css`
  - 验证：web typecheck + eslint 0 错误（AI 应用/撤销链路为运行时行为，待人工在 AI 面板验证）

- [新增] 项目管理器"未调用子程序"标记：程序集分类下未被任何代码调用的子程序名称淡化显示（无额外图标），悬停提示"该子程序未被任何代码调用"。判定为全项目文本级标识符分词（"求和"不被"求和2"误判；类方法 对象.方法名 可识别；注释/字符串中出现按已使用处理，宁可漏标不误标）；以 _ 开头的子程序（事件处理/_启动子程序 入口约定）不参与判定。打开项目时全量判定，编辑中随标签页内容即时重算（含未打开模块的标记同步）。
  - 范围：`src/renderer/src/components/Sidebar/unusedSubUtils.ts`（新增，computeUnusedSubNames）、`Sidebar.tsx`（TreeNode.unused + 标记渲染 + aria 标注）、`Sidebar.css`、`App.tsx`（projectSourceContentsRef 源码缓存/buildProjectTreeFromEpp 全量判定/handleOpenTabsChange 实时重算）
  - 验证：tests/unit/unusedSubUtils.spec.ts（8 例：孤儿标记/跨文件调用/_前缀排除/分词防误判/类方法/递归/子程序指针/CRLF）；全量单测 70 通过；web typecheck + eslint 0 错误

- [新增] 表格模式编辑器右侧变量汇总面板：全文件汇总——全局变量（有则显示）/程序集变量 + 每个子程序一组（组内参数在前带"参数"标注、局部变量按声明顺序，无变量的子程序不占面板），光标所在子程序的组名高亮；条目带类型首字配色图标与数组标记；顶部搜索框按名称/类型过滤（组计数显示 匹配数/总数，无匹配的子程序组自动隐藏）；点击条目跳转并选中声明行（自动展开被折叠的子程序）；面板可收起为右侧细条（状态持久化）。资源表文档不显示。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`（varPanelGroups/varPanelCurrentSub/handleVarPanelItemClick + 面板 JSX）、`EycTableEditor.css`（.eyc-var-panel*）
  - 验证：.tmp-harness/repro-var-panel.mjs（63 组全量收集/当前子程序高亮/搜索隐藏无匹配组/点击跳转选中/收起展开持久化）；25 契约测试通过；web typecheck + eslint 0 错误

### v0.0.3-beta.62（2026-06-18）

- [优化] 编译性能：消除一次编译里对支持库目录的重复全量扫描与缓存反复失效。真因 `libraryManager.getList()` 每次都执行全量 `scan()`（重扫库根目录 + 清空 metadataCache / 命令表缓存 / ycmd 注册表缓存），而它在 `loadCompileProtocols`、`collectUsedLibraryFileNames`、主入口组装等编译热路径被调 4~5 次，导致每次编译反复全量扫盘并把已建缓存一遍遍炸掉重建。在"产物缓存命中、跳过编译/链接"的情况下，本机仍耗 ~650ms、慢盘测试机达 6 秒，全部花在这里。
  - 范围：`src/main/libraryManager.ts`（新增 `getCachedList()` 不触发重扫，编译路径改用；`getAllCommands` 按平台缓存命令表）、`src/main/ycmd-registry.ts`（扫描签名 TTL 1500ms→30000ms，覆盖整次编译周期；库增删改经 `scan()` 主动失效，正确性不受影响）、`src/main/compiler.ts`（三处 `getList()`→`getCachedList()`；诊断日志增加细粒度分段计时）
  - 验证：tsc typecheck + `npm run build` 通过；细粒度诊断日志将四个 ~120~245ms 热点全部归因到 `getList()→scan()`
- [优化] 编译移入工作线程（worker_threads）：根治点运行/编译时主进程单线程被同步计算堵死导致界面卡死。先做电脑无关解耦（`runtimeEnv` 注入 appPath/isPackaged 等 + `CompilerHost` 抽象副作用），再把编译搬入常驻 worker；worker 加载失败/超时自动回退主进程编译，保证"能编译"永不被破坏。
  - 范围：新增 `src/main/compile-worker.ts` / `src/main/compileWorkerClient.ts` / `src/main/runtimeEnv.ts`；`compiler.ts`/`libraryManager.ts`/`ycmd-registry.ts` 去 electron 依赖；`index.ts` 的 compiler:compile/run 改走 `compileViaWorker`；`electron.vite.config.ts` 增 worker 入口；`package.json` `asarUnpack` 解包 worker 与编译 chunk
  - 验证：`npm run build` 产出 `out/main/compile-worker.js` + compiler chunk（grep 不到 electron）；实测编译在 worker 内执行（改主进程代码需完全重启 App 才生效）
- [优化] 编辑器光标位置 store 解耦：此前光标行/列存于 App 顶层 useState，编辑器每次点击/按键上报光标都会触发整个 IDE 重渲染（复杂界面单次高达 1~2 秒，表现为"点一行要等 1~2 秒""输入很卡"）。改为独立订阅源 `cursorPosStore`，光标变化只通知状态栏 Ln/Col 小组件；App 内改用 `cursorRef` 供断点/运行到光标等快捷键同步读取。
  - 范围：新增 `src/renderer/src/stores/cursorPosStore.ts`（useSyncExternalStore）；`src/renderer/src/App.tsx`（cursorRef + setCursorPos）；`src/renderer/src/components/StatusBar/StatusBar.tsx`（订阅 store）

### v0.0.3-beta.61（2026-06-13）

- [修复] 连续快速按回车/退格时丢键、卡顿：回车拆行、退格删空行、表格行回车插入原先读 React state 派生的 lines/editVal 闭包，连按时 React 尚未重渲染、闭包过时，多次按键基于同一旧文本计算并互相覆盖（表现为"连按两次回车只新增一行"）。改为以 prevRef.current（最新文本真相）为数据源，并用 flushSync 强制本次状态同步落地，保证下一次按键闭包已刷新。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`（import flushSync；applyCodeLineSplitOnEnter/applyEmptyCodeLineDelete/applyTableRowEnterInsert 三处数据源改 prevRef.current + flushSync）
  - 验证：.tmp-harness/repro-enter-lag.mjs（4x CPU 节流连按5次回车，修复前净增2行→修复后净增5行；退格对称恢复）；repro4/repro9 手势回归逐字一致；62 单测 + 25 契约测试通过
- [新增] 代码编辑器子程序整体折叠：子程序名行的行号右侧新增常驻 +/- 按钮，点击收缩/展开整个子程序（收缩后仅保留声明行，参数/局部变量/代码全部隐藏）；折叠状态以子程序名为键（编辑平移行号不丢失）；编辑落入折叠范围时自动展开兜底。代码行原有的参数展开 +/- 不受影响。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`（collapsedSubNames/subFoldRanges/visibleBlocks 过滤/表格行号槽按钮/startEditLine|startEditCell 自动展开）
  - 验证：.tmp-harness/repro-subfold.mjs（收缩隐藏本子程序代码、邻居子程序不受影响、展开恢复、代码行参数展开按钮原样）；repro4/repro9 手势回归逐字一致
- [新增] 设计器画布右下角缩放控件：倍率按钮点击弹出预设档位（25%最小/30/50/80/100/150/200/300/500/1000%，选择后窗体居中）、1% 步进拖动条（最小到最大倍率全程，缩放不居中保持视点）、拖动条两侧 −/+ 按钮（按 10% 步进，如 157%→147%）、适应窗口按钮（窗体完整可见铺满画布并居中）。取代原右上角只读百分比指示器（其位于工作区内会随滚动漂移）。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx|css`（zoomToCentered/fitZoomToViewport/zoomPresetPercents/vd-zoom-control）
  - 验证：.tmp-flow-verify/verify-zoom-control.mjs（预设 200% 居中偏移≈0、拖动条缩放不归中、−/+ 157%↔147%、适应窗口 104% 居中）

## 已发布归档

### v0.0.3-beta.60（2026-06-12）

- [新增] WebView2 浏览器支持库（lib/webview2）：浏览框组件（YcWebView2Host），设计期 iframe / 运行期 WebView2 所见即所得；免 WebView2Loader.dll（注册表 EdgeUpdate ClientState 发现运行时，动态加载 EmbeddedBrowserWebView.dll）；命令"取WebView2运行时版本"。
  - 范围：`lib/webview2/`（清单/元数据/组件定义/impl/windows.cpp/官方 WebView2.h + EventToken.h shim）、`src/main/compiler.ts`（源码型带窗口单元库生成注册函数调用、库 include 目录、-ladvapi32 -lole32、地址属性入文本通道）
  - 验证：webview-test 项目编译运行截图（example.com 真实渲染）；设计器截图比对一致
- [新增] WebView2 风格组件 网页按钮（YcWebButton）/网页编辑框（YcWebEdit）：HTML 模板 NavigateToString 渲染，WebMessage 桥接 BN_CLICKED/EN_CHANGE 到父窗口 WM_COMMAND，自管 WM_GETTEXT/WM_SETTEXT 文本通道；设计器同款 CSS 预览（模板双侧同步注释）。
  - 范围：`lib/webview2/impl/windows.cpp`、`lib/webview2/webview2.window-units.json`、`src/main/compiler.ts`（isClickable/isEditLike/初始文本回退）、`src/renderer/src/components/Editor/VisualDesigner.tsx|css`
  - 验证：真实鼠标点击网页按钮触发 `_网页按钮1_被单击` 信息框弹出；设计器与运行截图逐控件一致
- [新增] 组件箱分类（toolboxCategory）：窗口单元元数据新字段，缺省"通用Win32"；工具箱顶部分类下拉（默认通用Win32、选择持久化、失效回退）。
  - 范围：`src/main/libraryManager.ts`（两条解析路径+协议 controlBindings）、`src/renderer/src/components/Editor/VisualDesigner.tsx|css`
  - 验证：IDE 截图（分类切换过滤组件列表）
- [新增] 编辑框 19 个属性（边框/颜色/多行/滚动条/对齐/输入方式12种/密码遮盖/转换/调节器/选择区/停留焦点与顺序/备注）+ 属性默认值（defaultValue）机制 + 颜色选择器单元格。
  - 范围：`lib/krnln/window-units.json`（哈希同步）、`src/main/libraryManager.ts`、`src/main/compiler.ts`（buildStdEditCodegen 样式位+EM_*+调节器 buddy+WM_CTLCOLOREDIT 颜色表+停留顺序排序+输入过滤子类）、`src/renderer/src/components/Sidebar/Sidebar.tsx|css`、`VisualDesigner.tsx|css`
  - 验证：editprops-test 项目运行时样式位断言 + 截图；WM_CHAR 过滤断言（输入整数模式 a5x7→57）
- [优化] 编辑器交互性能：共享解析缓存（getBlocksFlowModelCached，点击路径 4 次全量解析→1 次）、mousedown 乐观进入编辑态（与 commit 同批渲染）、overscan 220→80、minimap 先采样后着色、拼音打分首字符锚定预检+首字母串缓存、EycTableEditor memo+调用处 props 稳定化、normalize 预筛。点击→光标 -70%（4×CPU 节流基准 3759→1140ms）。
  - 范围：`EycTableEditor.tsx`、`editorBlocksModel(.Shared).ts`、`useEditorInteractionHandlers.ts`、`pinyin.ts`、`Editor.tsx`、`App.tsx`
  - 验证：.tmp-harness/perf-click-type.mjs A/B 基准；repro4/repro9 手势回归逐字一致
- [修复] 补全弹窗位置漂移：编辑器根节点 zoom（eycScale）未反向补偿 + 布局移动后不跟随；computeCompletionPopupPosition 接 scale，acVisible 期间 rAF 逐帧吸附输入框。
  - 范围：`EycTableEditor.tsx`、`editorCompletionDisplayUtils.ts`
  - 验证：harness 面板动画场景，弹窗恒定 +2px 吸附
- [修复] 生成代码 yc_wide_to_utf8/yc_bin_to_cstr 单一 thread_local 缓冲被同语句多实参互相覆盖（信息框正文/标题变同一文本）→ yc_c_str_slot 8 槽轮转池；TRANSPILE_CACHE_VERSION 4→7（含后续两次 codegen 变更）。
  - 范围：`src/main/compiler.ts`
  - 验证：msgbox-test 项目运行时读对话框 Static 文本断言
- [修复] 整数等非文本表达式赋给控件文本属性（编辑框.内容 ＝ k）生成 yc_set_control_text(名, int) 编译错 → 两处发射点按 isTextExpression 包 yc_value_to_text。
  - 范围：`src/main/compiler.ts`
  - 验证：用户项目 windows窗口程序-新项目1 编译通过 + 运行时显示 "123"
- [修复] g_hMainWnd 在 CreateWindowExW 返回后才赋值，"创建完毕"事件内按名操作控件全部静默失败 → WM_CREATE 开头赋值。
  - 范围：`src/main/compiler.ts`
  - 验证：editprops-test 创建完毕事件设置编辑框内容生效
- [修复] 空内容编辑框运行时显示控件名（parse 层文本回退链对编辑框类不再回退控件名）。
  - 范围：`src/main/compiler.ts`（resolveControlInitialText）
- [新增] 属性面板颜色单元格（EditableColorCell：原生取色器+常用色中文名，存 COLORREF）。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx|css`
  - 验证：IDE 实测截图（红色/黄色色块）

### v0.0.3-beta.59（2026-06-11）

- [修复] 指针读取命令参数升级为长整数型：指针到整数/小数/双精度小数/字节集/文本 的内存指针参数由整数型改为长整数型，修复 x64 下真实内存地址被截断的问题；同步重新生成命令映射与帮助说明，更新核心库固定哈希。
  - 范围：`lib/krnln/krnln.commands.ycmd.json`、`lib/krnln/generated/command-mapping.windows.json`、`lib/krnln/系统核心支持库帮助说明文件.txt`、`src/main/libraryManager.ts`
  - 验证：`npm run krnln:mapping:generate` 513/513

- [优化] 赋值行参数展开改为两级结构：`变量 ＝ 命令(…)` 展开第一级显示"※被赋值的变量或变量数组"与"※用作赋予的值或资源"，命令自身参数（如 内存数据指针）挂在第二级，点击"用作赋予的值"旁的 + 再展开。此前赋值行被命令展开分支抢占，直接平铺命令参数。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`（两处展开分支的优先级条件）
  - 验证：Playwright 真实应用展开 `版本号 ＝ 指针到整数()`，两级结构与二级"内存数据指针"均正确

- [修复] 代码行行尾单引号注释破坏命令实参解析：如 `调试输出 (版本号)   ' 输出 6(主版本)` 会被转译成打印空文本（注释内的括号干扰实参提取）。转译主循环现在先剥离行尾注释（忽略字符串内的 `'`），整行注释行为不变；TRANSPILE_CACHE_VERSION 升至 4 失效旧缓存。
  - 范围：`src/main/compiler.ts`（stripTrailingEycComment + 转译主循环）
  - 验证：复现项目（窗口程序同款代码）编译运行输出 表地址 与 版本号 6

- [修复] 支持库"实际使用"扫描漏掉嵌套在实参中的命令调用：如 `版本号 ＝ Sciter取版本(指针到长整数(表地址 ＋ 16), 0)` 中的 `指针到长整数` 不会被识别，krnln 未参与链接导致"链接器错误: 未定义符号 krnln_p2int64"（窗口程序因别处用到 krnln 而偶然不触发，控制台程序必现）。collectUsedLibraryFileNames 现在额外扫描行内所有"标识符+左括号"并解析到所属支持库。
  - 范围：`src/main/compiler.ts`（collectUsedLibraryFileNames）
  - 验证：控制台复现项目此前链接失败，修复后编译运行成功

- [修复] 新建指针命令后表格中命令名显示为"指针命令1,"（带行尾逗号）：splitCSV 只把"逗号+空格"当分隔符，行尾裸逗号被并入字段。修正 splitCSV（eycBlocks 与 Editor.tsx 内联副本）将行尾裸逗号视为分隔符，并去掉插入模板中的尾随逗号；同时补上 editorCoreUtils DECL_PREFIXES 缺失的 `.指针命令 `，否则指针命令行的单元格编辑与公开/传址/数组切换不生效。
  - 范围：`src/renderer/src/components/Editor/eycBlocks.ts`、`editorCoreUtils.ts`、`EycTableEditor.tsx`、`Editor.tsx`、`src/renderer/src/App.tsx`
  - 验证：`npm run typecheck`、`npm run test:unit`（新增行尾逗号回归用例，62 通过）

- [优化] DLL命令编辑器（.ell）右键菜单首项由"新子程序"改为"N.新DLL命令"，并新增"Z.新指针命令"；两者均在文件末尾追加声明并滚动高亮新行，菜单打开时按 N/Z 键可直接触发。其他文档类型右键菜单保持"新子程序"不变。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`npm run typecheck`、`npm run lint`、Playwright 启动真实应用分别右键 .ell 与 .eyc 标签页核对菜单项

- [新增] 编译器支持 `.指针命令`（函数指针间接调用）：在 `.ell` 文件中与 `.DLL命令` 平级声明，调用时第一个实参固定传入函数地址（长整数型），编译器按声明签名生成类型化间接调用包装。解决类 Sciter（仅导出 `SciterAPI`，所有功能通过函数指针表暴露）这类 DLL 在易语言式 DLL 命令机制下无法调用的问题。
  - 范围：`src/main/compiler.ts`（解析/签名表/代码生成/缓存版本号 bump 至 3）
  - 验证：`npm run typecheck`、`vitest`、真实 sciter.dll 端到端编译运行（取 SciterAPI → 指针到长整数 读函数表 → 指针命令调 SciterVersion 成功返回 6.0）

- [新增] 核心支持库新增 `指针到长整数`（p2int64）命令：从指定内存地址读取 8 字节长整数，用于在 x64 下从函数指针表（如 DLL 返回的接口结构）读取函数地址，配合指针命令使用。
  - 范围：`lib/krnln/krnln.commands.ycmd.json`、`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`lib/krnln/系统核心支持库帮助说明文件.txt`
  - 验证：端到端编译运行通过

- [新增] UI 支持指针命令：`.ell` 表格编辑器渲染指针命令表（含公开切换、参数传址/数组切换）、代码补全与参数详情展开（自动显示隐式“函数地址”首参）、项目树 DLL命令分类下显示指针命令节点、插入菜单新增“指针命令(P)”。
  - 范围：`src/renderer/src/components/Editor/eycBlocks.ts`、`eycTableModel.ts`、`EycTableEditor.tsx`、`Editor.tsx`、`editorTableRowUtils.ts`、`editorCommandDisplayUtils.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/main/index.ts`
  - 验证：`npm run typecheck`、`npm run lint`、`tests/unit/eycBlocks.spec.ts` 新增用例

- [修复] krnln 命令映射生成器正则可跨行贪婪匹配进函数体，导致大小写包装函数（如 `krnln_LockWindowUpdate`）被误判为未声明；同步修正 `libraryManager.ts` 中过期的核心库 SHA256 固定哈希表（原哈希与仓库文件不符，正式版打包启动校验会失败）。
  - 范围：`scripts/krnln/generate-command-mapping.mjs`、`src/main/libraryManager.ts`
  - 验证：`npm run krnln:mapping:generate` 输出 513/513 implemented

### v0.0.3-beta.55（2026-05-15）

- [修复] 编辑框运行态默认文本与设计器对齐：编译生成控件时，编辑框/超级编辑框/文本框不再将控件名回退为初始文本；仅在显式设置文本时才显示内容，避免设计器为空而运行后出现默认文字。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [修复] 可视化设计器编辑框默认底色与运行态对齐：编辑框/超级编辑框预览改为优先读取控件背景属性，未设置时默认白底，避免深色主题下显示为黑色而与编译运行结果不一致。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 参数递归展开接入流程层级配色：参数区连线与箭头改为复用流程命令的层级颜色算法（`resolveFlowLineColors`），多级参数展开时可按层级显示彩色线条。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [修复] 参数递归展开子级箭头层级偏移修正：子级箭头定位显式叠加当前层缩进变量，箭头锚点改为按“本层起始竖线”计算，避免误落到上一级位置。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [修复] 参数递归展开子级箭头绘制基准修正：子级箭头改为在参数子级容器顶部绘制（不再悬挂在上一行底部重叠区域），减少跨行基准导致的对齐漂移。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [修复] 参数递归展开子级箭头中心线对齐：子级箭头改为按中心点锚定下一级竖线起点（`translateX(-50%)`），避免按左边界定位导致“看起来未对齐”的偏移。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [修复] 参数递归展开子级箭头锚点修正：箭头改为锚定下一级竖线起点位置，不再对齐到父级收口结束点。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [修复] 参数递归展开子级连线对齐修正：二级展开三角箭头与该层竖线中心线重新对齐，消除子级箭头与竖线左右错位。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [修复] 参数递归展开连线收口修正：当某参数行继续展开下级子树时，父级该行的竖线在本行中线收口，避免出现“应为 L 形却显示为 ├”的错连线。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [优化] 命令参数展开支持表达式递归查看：代码行参数值现可继续展开 `+` 表达式（显示“被加数或文本或字节集 / 加数或文本或字节集”），并可进一步展开嵌套命令参数（如 `到文本(d)` 展示“待转换的数据：d”）。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`

- [优化] “正在生成C++代码”阶段提速与可观测性增强：项目元数据（全局变量/常量/资源/子程序/数据类型/DLL 命令）改为按项目指纹缓存复用，避免每次重复全量扫描；同时新增元数据阶段耗时日志。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 运行阶段进一步提速：新增可执行产物指纹缓存（源 `.cpp`、资源、静态库、目标平台/架构/模式等），命中时直接跳过编译与链接，复用上次 `exe`。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 转换阶段增量化：为 `.eyc/.egv/.ecs/.edt/.ell -> .cpp` 增加指纹缓存（源内容 + 编译上下文 + 断点 + 常量上下文），未变化文件直接复用已生成 `.cpp`，避免每次全量转换。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [修复] 消除文本字面量赋值告警：文本型变量接收 `L"..."` 字面量时改为走 `yc_value_to_text(...)` 复制，避免 `wchar_t*` 直接绑定字符串常量触发 `-Wwritable-strings`。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 支持库编译协议加载提速：改为按“已加载支持库 + 协议文件路径/时间戳/大小”签名复用缓存，仅在签名变化时重读协议，减少重复解析开销。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 运行模式编译提速：点击“运行”时编译器从 `O2` 调整为 `O0` 快速编译路径，减少日常调试等待时间；“编译(项目平台)”仍保持 `O2` 发布优化。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 数据类型命名统一：将“大数”更名为“大整数型”，编辑器类型补全与诊断按新名称展示；编译器与诊断仍兼容旧名称“大数”，避免历史工程立即失效。
  - 范围：`src/renderer/src/components/Editor/editorCoreUtils.ts`、`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`、`src/main/compiler.ts`
  - 验证：`get_errors`

- [修复] 工具栏图标按钮偶发需点击两次才响应：鼠标按下时立即执行按钮动作，并对后续 click 做去重，减少首次点击被焦点/上下文清理吞掉的情况，同时保留键盘触发能力。
  - 范围：`src/renderer/src/components/Toolbar/Toolbar.tsx`
  - 验证：`get_errors`

- [修复] 编辑期长整数溢出诊断与大数类型对齐：当赋值目标变量为“大数”时，不再错误提示“超出长整数范围”。
  - 范围：`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`

- [优化] 大数除零行为改进：大数除法/取模遇到除数为 0 时输出显式运行时错误（stderr），并在调试器附加时触发中断，避免静默返回造成误判。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 大数运算能力补齐：新增大数取模 `%`（含全角 `％`）与运行时 `yc_big_mod`，表达式翻译可在大数上下文下正确分流到大数运算。
  - 范围：`src/main/compiler.ts`、`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`

- [优化] 大数场景转换语义补齐：表达式翻译内建支持 `到文本(...)` 与 `到数值(...)`，其中大数目标上下文下 `到数值` 走 `yc_value_to_big(...)`，避免精度语义丢失。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [新增] 新增内置数据类型“大数”：编译后端引入 `YC_BIG` 任意精度整数实现，支持 `+ - * /` 与比较运算，并支持超出长整数范围字面量在大数上下文保真计算。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] 编辑器类型体系补齐“大数”：内置类型补全新增“大数”，并将其纳入数值类型诊断集合（用于文本型赋值类型检查）。
  - 范围：`src/renderer/src/components/Editor/editorCoreUtils.ts`、`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`

- [修复] 编译后端文本变量拼接语义修复：表达式翻译新增变量类型感知，`文本型 + 文本型` 生成 `yc_text_concat(...)`，避免误生成为 `wchar_t*` 指针相加导致编译错误（如 `e = t1 + t2`）。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [新增] 编辑期类型诊断增强：当文本型变量直接接收数值型右值（如 `e = a + b`、`e = a`，且右值为数值型）时，问题面板立即报错并触发行内红色波浪线。
  - 范围：`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`

- [修复] EYC 文本模式编辑器接入 Monaco 诊断标记：问题面板诊断结果同步为编辑区红色波浪线（含错误/警告级别）。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`
  - 验证：`get_errors`

- [新增] 编辑期问题面板新增“长整数常量超范围”诊断：在代码书写阶段实时检查整数常量，超出 `[-9223372036854775808, 9223372036854775807]` 立即报错，无需等待编译。
  - 范围：`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`

- [修复] 生成代码编译失败收敛：`spec` 调试输出协议翻译补命令上下文（支持 `到文本(...)` 参与表达式转换），并修复“文本型变量接收数值表达式”时的类型不兼容（自动转换为文本）。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [修复] 表达式翻译增加整数字面量 64 位边界钳制，避免超长整数直接触发 C++ “integer literal is too large” 编译错误。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [修复] ycmd 后端接入校验补齐英文命令名匹配：`CommandSignatureDef` 保留 `englishName` 并在协议匹配时传入，修复仅配置 `commandEnglishName` 的协议被误判“未接入后端实现”（如 `spec.Trace`）。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [新增] `spec` 支持库最小后端协议映射：新增 `spec.protocol.json`，优先在编译期直连 `Trace/Verify/Delay`（分别对应输出调试、检查、中断延时）。
  - 范围：`lib/spec/spec.protocol.json`
  - 验证：`get_errors`

- [修复] 编译后端支持 `repeatable` 参数封送：ycmd 泛型命令调用在参数超出固定声明长度时，正确复用末尾可重复参数的数据类型，不再错误回退为“整数型”。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [优化] ycmd 命令参数新增 `repeatable` 语义透传：主进程解析、支持库管理与前端提示链路已打通；调用格式与参数说明可显示“可重复追加”参数。
  - 范围：`src/main/ycmd-registry.ts`、`src/main/libraryManager.ts`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/editorCoreUtils.ts`、`src/renderer/src/components/Editor/editorCompletionCatalogUtils.ts`
  - 验证：`get_errors`

- [修复] 系统特殊功能支持库 `spec` 可追加参数命令补充 `repeatable` 标记，覆盖 `Trace/Verify/CallFunction/DelayCallFunction/GetText/DelayCallFunction_precisely`。
  - 范围：`lib/spec/spec.commands.ycmd.json`
  - 验证：`get_errors`

- [修复] 系统特殊功能支持库 `spec` 命令参数元数据全量对齐外部源码：补齐 `MachineCode/GetVarAddress/GetSubAddress/GetVarDataAddr/Verify/CallFunction/Delay/DelayCallFunction/AllocMem/FreeMem/ESysFunction/GetText/GetText_Set/GetText_Get/DelayCallFunction_precisely` 的参数定义（名称、类型、可省略）。
  - 范围：`lib/spec/spec.commands.ycmd.json`
  - 验证：`get_errors`

- [修复] 系统特殊功能支持库 `spec.Trace` 参数元数据补齐：`调试输出` 命令从“无参数”修正为首个可省略参数“欲输出值（通用型）”，与外部源定义对齐，恢复调用提示可读性。
  - 范围：`lib/spec/spec.commands.ycmd.json`
  - 验证：`get_errors`

- [优化] 可视化设计器组件箱图标视觉简化：移除图标外层矩形边框与底色，仅保留图标本体显示，降低视觉噪音。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [修复] 可视化设计器组件箱状态恢复竞态修复：启动时先完成本地状态读取与应用，再开启状态写回，避免重启后将“浮动”误覆盖为默认“停靠”。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`
  - 验证：`get_errors`

- [优化] 可视化设计器组件箱列表模式支持手动多列并保持：将“列表单列/多列”切换放到工具箱上方按钮区后，多列状态在缩窄宽度时仍保持生效（不自动退回单列）；同时新增组件箱状态/位置持久化（停靠左右、停靠宽度、浮动开关、视图模式、多列开关、浮动位置与尺寸）。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [修复] 可视化设计器组件箱两种显示模式的宽度联动修正：图标模式移除固定宽度锁死，支持停靠态拖拽调宽；列表/图标模式下组件项图标、字号、间距与卡片尺寸会随组件箱宽度同步缩放，不再固定大小显示。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器组件工具箱交互增强：固定停靠支持左右切换并可拖拽边缘调整宽度（右停靠从左边框拖拽、左停靠从右边框拖拽）；浮动模式新增容器边界检测，拖动与缩放均不会超出 IDE 设计器区域。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [修复] 第三方支持库组件缺失 `icon` 字段时图标兜底修正：改为统一显示“未知”图标，不再按组件中文名误回退到图片等其他图标；仅核心支持库保留名称映射兜底。
  - 范围：`src/renderer/src/components/Icon/Icon.tsx`、`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/assets/icons/QuestionMark.svg`
  - 验证：`get_errors`

- [修复] 工具箱“按钮”图标映射修正：将 `按钮` 与 `Button_16x.png` 从 `image-button` 改为 `button`，并接入 VS 图标库 `Button.svg`；同时修正主进程图标优先级为“组件 `icon` 字段优先、索引仅兜底”，避免被索引覆盖后显示错误。
  - 范围：`src/main/libraryManager.ts`、`src/renderer/src/components/Icon/Icon.tsx`、`src/renderer/src/assets/icons/Button.svg`
  - 验证：`get_errors`

- [修复] 组件定义中的公共属性彻底去重（含“窗口”组件）：`window-units.json` 不再内嵌 `标题/左边/顶边/宽度/高度/可视/禁止`，统一由主进程固定公共属性注入，避免属性面板重复与维护分叉。
  - 范围：`lib/krnln/window-units.json`、`src/main/libraryManager.ts`
  - 验证：`node -e "...TOTAL_COMMON_IN_JSON=0"`、`get_errors`

- [优化] 支持库组件图标改为组件 JSON 直连：核心支持库组件在 `window-units.json` 内为对应组件写入 `icon` 路径（如 `icon/BlueIcons/*_16x.png`），渲染层优先按组件元数据展示图标；核心库不再依赖 `icon.json` 索引文件，第三方支持库沿用组件元数据同口径接入。
  - 范围：`lib/krnln/window-units.json`、`lib/krnln/icon/BlueIcons/*`、`lib/krnln/icon/DarkIcons/*`、`lib/krnln/icon/WhiteIcons/*`、`src/main/libraryManager.ts`、`src/renderer/src/components/Icon/Icon.tsx`、`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/components/Editor/VisualDesigner.tsx`
  - 验证：`get_errors`

- [优化] 公共属性“鼠标指针”改为固定枚举下拉显示：按 `0~15` 显示为 `0.默认型`、`1.标准箭头型`…`15.自定义型`，并在组件属性面板中统一按枚举项选择。
  - 范围：`src/main/libraryManager.ts`、`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`

- [修复] 组件公共属性显示顺序修正：属性面板改为对所有窗口组件统一前置显示固定公共属性（左边/顶边/宽度/高度/标记/可视/禁止/鼠标指针），其后再显示组件自身属性；并对同名属性去重避免重复行。
  - 范围：`src/main/libraryManager.ts`
  - 验证：`get_errors`

- [优化] 组件公共属性与 lib2.h 对齐：参考 `elib/lib2.h` 固定属性定义，补齐默认窗口组件属性中的“标记(tag)”与“鼠标指针(MousePointer)”，并将“禁止”英文名对齐为 `disable`；同时补充无元数据场景下属性面板的对应编辑项。
  - 范围：`src/main/libraryManager.ts`、`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`

- [优化] 可视化设计器窗口默认居中并支持滚轮缩放：设计画布改为窗口居中展示，按住 `Ctrl`（macOS 为 `Command`）后滚轮可放大/缩小，普通滚轮保留滚动画布；画布聚焦后支持键盘 `+/-/0` 缩放控制并显示当前缩放百分比。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [修复] 修复可视化设计器最大化后窗体偏右与缩放显示不同步：将缩放百分比浮层移出居中布局流，恢复窗体始终居中；同时统一窗体最小可视尺寸与缩放壳尺寸计算，避免缩放后窗体背景与内部控件显示越界或留白。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器缩放与画布导航增强：缩放上限提升到 `1000%`；按住 `Ctrl/Command + 滚轮` 时以鼠标位置为焦点缩放（鼠标不在容器内时回退到中心缩放）；设计器引入固定大工作区并常驻横向/纵向滚动条；新增顶部与左侧像素标尺并随滚动与缩放联动。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器新增空格拖动画布视角：按住 `Space` 后左键拖拽可平移画布可见区域，无需手动拖滚动条；并增加 `grab/grabbing` 光标反馈。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器像素标尺细化：标尺改为每 `10` 单位绘制刻度线，并在每 `100` 单位显示数字文本。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器像素标尺层级优化：在每 `100` 单位长刻度与文本基础上，补充每 `50` 单位中刻度，提升读数分层。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`

- [优化] 可视化设计器标尺密度自适应缩放倍率：按当前缩放自动计算刻度步长，随倍率动态调整刻度与文本密度，避免放大后过稀、缩小后过密。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`
  - 验证：`get_errors`

- [修复] 修复可视化设计器空格键误触发窗口最大化/还原：在设计器中对 `Space` 采用捕获阶段拦截默认行为，并在画布交互时主动归焦到画布区域，避免标题栏按钮保留焦点后被空格激活。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`
  - 验证：`get_errors`

- [优化] 可视化设计器视图状态持久化：按当前设计文件保存并恢复缩放倍率与画布滚动视角，重新打开后可回到上次浏览位置。
  - 范围：`src/renderer/src/components/Editor/VisualDesigner.tsx`
  - 验证：`get_errors`

- [优化] 资源管理器支持库查看树结构增强：在“数据类型”分组下新增窗口组件层级展示，支持展开到“属性/事件”子节点，并按组件图标展示，结构贴近易语言原生支持库浏览树。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`

- [优化] 支持库提示面板增强：点击支持库树中的命令/数据类型/属性/事件节点后，在固定提示面板展示对应说明信息；事件节点可显示“事件名称 + 触发说明 + 无返回值提示”。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/components/Sidebar/Sidebar.css`
  - 验证：`get_errors`

- [修复] 支持库提示面板位置修正：支持库树点击说明不再显示在侧栏内部，改为写入底部“输出/终端/提示/问题/调试”区域中的“提示”面板，并自动切换到该标签。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/App.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/Sidebar/Sidebar.css`
  - 验证：`get_errors`

- [修复] 支持库命令分类恢复：修正 ycmd 解析层将命令分类固定为 `ycmd` 的问题，核心支持库命令现可按“流程控制/算术运算/逻辑比较/磁盘操作/数组操作/文本操作”等分类在支持库树中分组展示。
  - 范围：`src/main/ycmd-registry.ts`
  - 验证：`get_errors`

- [优化] 支持库命令提示改为完整模板：点击命令节点后，提示面板按“调用格式/英文名称/详细说明/参数说明/操作系统支持”输出；并对“如果(ife)”提供与原生风格一致的详细说明文本。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`

- [新增] 新增系统特殊功能支持库 `spec`（独立于核心支持库）：基于开发期外部 `D:\chungbin\ycIDE-html\支持库源码\spec` 命令定义离线生成项目内支持库清单，运行时仅读取 `lib/spec` 产物。
  - 范围：`lib/spec/spec.library.json`、`lib/spec/spec.commands.ycmd.json`、`lib/spec/impl/windows.cpp`、`scripts/krnln/import-library-metadata.mjs`
  - 验证：`node scripts/krnln/import-library-metadata.mjs`、`get_errors`

- [优化] 核心支持库命令说明与分类改为帮助说明对齐：新增离线导入脚本解析 `系统核心支持库帮助说明文件.txt` 并同步 `krnln.commands.ycmd.json` 的摘要、分类、参数说明和平台支持字段。
  - 范围：`lib/krnln/系统核心支持库帮助说明文件.txt`、`lib/krnln/krnln.commands.ycmd.json`、`lib/krnln/krnln.library.json`、`scripts/krnln/import-library-metadata.mjs`
  - 验证：`node scripts/krnln/import-library-metadata.mjs`、`get_errors`

- [修复] 资源管理器支持库树提示去硬编码：移除命令与组件说明的固定解释模板（含“如果”特判说明），提示内容改为优先使用支持库元数据，仅保留“暂无说明。”最小回退。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/main/ycmd-registry.ts`、`src/main/libraryManager.ts`
  - 验证：`get_errors`

- [新增] 新建子程序自动补局部变量：在新子程序声明下方自动插入 `.局部变量 局_变量N, 整数型`，变量名按 `局_变量1` 递增并避免重名。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [修复] 右键菜单可用态修正：未改动时“撤销/重做”置灰；无选中时“复制/剪切”置灰；剪贴板无文本时“粘贴”置灰，并同步限制助记键仅在可用态触发。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [优化] 调整新建类快捷键：将“新建文件/新建项目”改为 `Ctrl+Alt+N` / `Ctrl+Alt+Shift+N`，并将 `Ctrl+N` 让给“新建子程序”（含标题栏与原生菜单显示同步）。
  - 范围：`src/shared/shortcut-config.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/main/index.ts`
  - 验证：`get_errors`

- [修复] 修复右键菜单“新建子程序”高亮闪烁错位：新建后闪烁改为仅命中 `tr.eyc-data-row` 表格行，并在表格渲染就绪前短重试，避免过渡重渲染阶段闪到文本节点。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [修复] 修复 EYC 右键菜单助记键缺失：补齐 `N` 新子程序单键触发，并同步补全 `E/I/K/G/B` 等字母前缀项的菜单打开态单键执行。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [优化] EYC 表格编辑器右键菜单补充单键助记触发：菜单打开后可直接按 `U/Y/C/T/P/F` 执行“撤销/重做/复制/剪切/粘贴/全选”，并同步更新菜单文案前缀为 `U.撤销` 等助记格式。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [新增] EYC 表格编辑器右键菜单支持定制化分组与动作：新增“新子程序/删除行/插入新行/编译当前行/屏蔽/解除屏蔽”菜单项，并按要求调整分隔顺序与快捷键文案展示。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [修复] 补齐文件夹工作区关闭入口：文件菜单新增可用“关闭工作区”项（复用 `file:closeProject` 关闭流程），文件夹工作区下不再缺少关闭菜单。
  - 范围：`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/renderer/src/App.tsx`、`src/main/index.ts`
  - 验证：`get_errors`

- [修复] 修复 EYC 文本模式 `Ctrl+V` 无法粘贴：全局快捷键在 Monaco/contenteditable 可编辑焦点下不再拦截编辑类快捷键，恢复原生粘贴行为。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [优化] 文件保存改为按当前文件编码写盘：`file:save` 新增编码参数，编辑器保存/保存全部/项目保存/外部冲突“保留IDE内容”均按状态栏当前编码落盘（默认 UTF-8）。
  - 范围：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [修复] 修复“按 GBK 重新打开后误提示文件被外部修改”：外部变更检测改为按当前文件已选编码读取磁盘内容，再与标签保存态比较，避免 UTF-8 误解码导致的误报（ASCII 文件如部分 `.h` 原本不触发误报）。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`
  - 验证：`get_errors`

- [修复] 修复 IDE 主窗口尺寸未持久化：调整窗口大小/位置后会写入主窗口状态，重启时恢复上次宽高、位置与最大化状态，不再回到默认尺寸。
  - 范围：`src/main/index.ts`
  - 验证：`get_errors`

- [新增] 状态栏右下角新增当前活动文件编码显示，并支持点击后选择编码重新打开：新增编码菜单（UTF-8/UTF-8 BOM/GB18030/GBK/Big5/Shift_JIS/UTF-16LE/UTF-16BE/Windows-1252），选择后按指定编码重新读取当前文件。
  - 范围：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/StatusBar/StatusBar.tsx`、`src/renderer/src/components/StatusBar/StatusBar.css`
  - 验证：`get_errors`

- [修复] 修复文件夹工作区文件误判为文件夹导致“双击打不开”问题：目录读取接口对非目录改为返回 `null`，前端树构建可准确区分目录与文件，`button.cpp`、`button.h` 等文件双击可正常打开。
  - 范围：`src/main/index.ts`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [优化] 文件夹工作区文件节点图标改为扩展名占位语言图标：为 `.cpp/.h/.ts/.js/.json/.md` 等文件按扩展名显示占位图标，不再显示文件夹图标。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`

- [优化] 状态栏新增工作区模式标识：当存在工作区时，右侧显示“项目模式: 名称”或“文件夹工作区: 名称”，便于区分当前上下文并避免误判运行能力。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/StatusBar/StatusBar.tsx`、`src/renderer/src/components/StatusBar/StatusBar.css`
  - 验证：`get_errors`

- [新增] 新增“打开文件夹工作区”入口：在保留现有“打开项目”流程不变的前提下，新增独立菜单/快捷键入口用于选择文件夹；若根目录存在 `.epp` 自动按项目打开，否则按普通文件夹工作区打开并显示目录树。
  - 范围：`src/shared/eprojectImport.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/shortcut-config.ts`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [修复] 修复单文件打开无响应：`buildTabFromPath` 中文本模式回退分支被提前返回拦截，导致 `.cpp` 等非项目文件无法打开；现已恢复为默认文本模式打开。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [优化] 补齐单文件“另存为”能力：新增 `file:saveAs` 菜单动作与保存对话框 IPC，未命名文件执行保存时自动进入“另存为”流程；关闭未命名且已修改标签时选择“保存”也会触发保存为后再关闭。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/shortcut-config.ts`
  - 验证：`get_errors`

- [优化] 支持单文件编辑工作流：新增“新建文件/打开文件”入口（菜单、快捷键、工具栏），可直接打开非项目文本文件（如 `.cpp`、`.py`、`.md`）进行编辑；当当前活动标签为非项目文件时，运行按钮自动禁用，避免误以为可调试运行。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Toolbar/Toolbar.tsx`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/shortcut-config.ts`
  - 验证：`get_errors`

- [优化] 项目管理改为单项目模式：当已有项目打开时再次打开新项目，会先执行当前项目关闭流程（含未保存确认与保存），再加载新项目，避免调试/运行时项目上下文混淆。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`

- [优化] 代码行输入体验优化：支持将常见中文标点自动转为英文符号；当以中文单引号 `‘/’` 起始输入时自动转换为注释前缀 `'`，并在注释符后自动补一个空格，便于后续自动排版。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS `SendMessage` 语义扩展：新增 `WM_MOVE/WM_CLOSE/WM_GETTEXT/WM_GETTEXTLENGTH` 处理分支，补齐位置回写、关闭状态收敛与文本读取返回值，提升跨平台消息调用的一致性与可验证性。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 消息循环与游标兼容入口补齐：新增 `krnln_DoEvents`（冲刷状态化消息队列）及 `krnln_SetWaitCursor/krnln_RestroeCursor`，对位 Windows 侧同名导出，减少跨平台命令调用断档。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 组件消息语义补齐：`SendMessage/PostMessage` 从占位返回升级为状态化消息分发与队列机制（Post 入队、Send 先冲刷后同步分发），并补充 `WM_ACTIVATE/WM_SETFOCUS/WM_KILLFOCUS/WM_ENABLE/WM_SIZE/WM_SETTEXT` 对应状态回写，降低跨平台组件命令“有符号无行为”的差异。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 组件与窗口命令映射继续补齐：新增 `IsCreated/CreateWindowUnit/destroy/Activate/SetFocus/IsFocus/GetClientWidth/GetClientHeight/GetWidth/GetHeight/enable/IsEnabled/move/ZOrder/SetParentWnd/GetHWnd/SetText/GetText` 及重绘/消息相关导出，避免编译链路在非 Windows 平台落到“无实现符号”。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 平台实现继续补齐：Linux 端补上 `PutEnv`、屏幕尺寸/鼠标坐标查询与消息框按钮语义（优先 `zenity`，失败回退控制台）；macOS 端补上 `MsgBox/msgbox` 的按钮类型与返回码映射，不再忽略 `buttons` 参数。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 平台参数语义继续补齐：`krnln_run` 现已处理 `showMode`（隐藏模式重定向到 `/dev/null`，异步模式后台执行）；macOS `MsgBox/msgbox` 接入 `parentWindow` 参数（优先激活父窗口后再弹框），减少参数形同虚设的问题。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库 Linux/macOS 命令映射实现补齐（非消息框）：新增 `CurDir/ChDir/MkDir/RmDir/FileCopy/FileMove/kill/name/IsFileExist/FileLen/GetAttr/SetAttr/GetTempFileName/ChDrive/GetDiskTotalSpace/GetDiskFreeSpace/GetDiskLabel` 的平台实现，文件与目录命令改为真实文件系统行为，磁盘空间命令改为 `statvfs` 回读。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] 核心支持库内置不可分离命令继续协议化：将 `输出调试文本/暂停/检查` 并入 `commandBindings`（新增 `emitBuilder`），并移除 `compilerCommandResolvers` 中对应硬编码；当前解析器仅保留流程兜底，不再承载核心命令规则。
  - 范围：`lib/krnln/window-units.json`、`src/main/compiler.ts`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`

- [优化] 核心支持库协议化第三批收敛：将 `比较/逻辑/磁盘与目录/文件检索与临时文件/写到文件` 命令并入 `commandBindings`（新增 `exprOp/exprBuilder` 协议字段），并从 IDE 内置命令解析表剥离同名硬编码；语句上下文在无 `emit` 时可自动复用协议 `expr` 生成 `(void)` 调用。
  - 范围：`lib/krnln/window-units.json`、`src/main/compiler.ts`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`

- [优化] 协议驱动第二批下沉：新增 `改变驱动器/改变目录/字节集与指针转换` 等命令到核心支持库 `commandBindings`，并同步从 IDE 内置命令表移除对应实现，进一步降低编译器对命令名硬编码依赖。
  - 范围：`lib/krnln/window-units.json`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`

- [优化] 协议驱动继续收敛：已在核心支持库 `commandBindings` 下沉的命令（如 `信息框/标准输出/调试输出/取本机名/是否为调试版/文件与字节集首批命令`）从 IDE 内置命令表移除，确保编译优先且实际依赖支持库协议而非本地硬编码。
  - 范围：`src/main/compilerCommandResolvers.ts`、`src/main/compiler.ts`、`lib/krnln/window-units.json`
  - 验证：`get_errors`

- [优化] 支持库编译协议新增表达式绑定能力：`commandBindings` 支持 `expr` 字段，编译器在表达式翻译与后端路径校验中优先读取协议绑定；核心支持库已下沉首批命令映射（含 `信息框/标准输出/调试输出/取本机名/是否为调试版/文件与字节集常用命令`）。
  - 范围：`src/main/compiler.ts`、`lib/krnln/window-units.json`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`

- [优化] 编译器命令内置表模块化：将 `compiler.ts` 中的内置命令表达式/语句生成器与命令分发逻辑抽离到独立模块，主编译文件仅保留流程编排与接口调用，降低第三方支持库接入时对主编译文件的耦合感知。
  - 范围：`src/main/compiler.ts`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`

- [修复] 信息框窗口标题为空时默认文案修正：统一回退为“信息框”，不再出现错误标题文案。
  - 范围：`src/main/compiler.ts`、`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [优化] 核心支持库数据/对象日期与属性写入去随机化：将 `createRuntimeObject/SetData/GetDateTime/RunMethod/RunDateMethod/GetDateProperty/SetExtra/SetFontAttr/SetItemData/SetProperty` 改为确定性状态迁移（按游标、行列、画布操作数与对象记录回写），减少 tick/随机依赖导致的回归测试波动。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库绘图与资源 getter 语义继续补齐：将 `InvertRect/GetFont/GetHDC/GetPicHeight/GetPicWidth` 从占位返回改为运行时状态驱动（画布反色、字体对象句柄复用/创建、绘图上下文签名、图片尺寸随画布边界回读），提升图形链路可观察性与一致性。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库绘图命令语义补齐：将 `DrawRect/FillRect/LineTo/DrawPic/DrawJBRect/ArcTo/chord/ellipse/pie/polygon/RoundRect/GetPixel/SetPixel` 从空触发改为运行时画布状态驱动，统一记录最近绘图操作、边界、点位与颜色，支持后续通过 `GetPixel` 与编辑状态字段观察绘图副作用。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库数据集游标与对象属性语义继续补齐：增强 `connect/CloseConnect/ExecuteSql/AddLine/AddString/Append/AppendRow/InsertCol/InsertRow/LoadDS/LoadDSFile/RemoveCol/RemoveRow/Requery/RunBoolMethod/GetNumProperty` 的状态联动，统一行列最小约束、`currentRow/caretRow/caretCol` 边界收敛与对象句柄回读，减少“恒成功但状态漂移”的伪语义行为。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库对象命令语义补齐：引入运行时对象句柄表，打通 `CreateArray/CreateFontDispObj/CreatePicDispObj/CreateObject/QueryInterface/RunMethod/RunNumMethod/RunTextMethod/RunDateMethod/RunObjectMethod/RunVariantMethod/GetObject/GetObjectProperty/SetType/ViewObjInf` 的同一对象状态读写，避免随机句柄与不可追踪返回值。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [修复] 核心支持库自动桩区重复定义与状态一致性修复：去除重复函数定义（`SelectAll`、`SetAlignMode`），并补齐 `CloseRecordset/Clear/CloseClient/cls/EmptyCell/EndDoc/GetTextColor` 的状态化语义，保证编辑/打印/网络状态收敛与读写一致。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库编辑/打印/网络/数据游标语义继续补齐：将 `` 等从随机值或恒成功占位改为共享运行时状态驱动；并统一 `Get* / Set*` 行为一致性（同一状态可读回）。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库事务/保存命令语义补齐：将 `connect/CloseConnect/query/select/ExecuteSql/OpenMDB/OpenSqlServerDB/BeginTrans/CommitTrans/RollbackTrans/SaveChange/SaveDS/SaveDSCell/SaveDSCellFile/SaveDSFile/say` 从固定成功返回调整为基于运行时连接与事务状态的行为（含失败分支），避免无脑 `return 1` 掩盖错误。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库 Windows 桩清零收官：继续对自动桩区进行连续替换，将剩余命令全部转为可执行实现（含控制流占位、组件/数据集接口、绘图与对象调用接口等的兜底实现），并完成映射统计闭环。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）

- [优化] 核心支持库 Windows 桩实现连续补齐（文件句柄/通用命令/对象辅助）：新增 `` 等实现，并修复映射统计中的 `undeclared` 回归。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=264 / stubbed=248 / undeclared=0`）

- [优化] 核心支持库 Windows 文本命令簇补齐：新增 `RpSubText/space/string/pstr/StrToUTF8/UTF8ToStr/StrToUTF16/UTF16ToStr` 实现，覆盖子串替换、重复文本、指针到文本与 UTF8/UTF16 基础转换能力。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=185 / stubbed=327 / undeclared=0`）

- [优化] 核心支持库 Windows 时间命令簇补齐：新增 `TimeChg/TimeDiff/GetDaysOfSpecMonth/TimeToText/TimePart/year/month/day/WeekDay/hour/minute/second` 实现，统一复用 OLE 日期转换链路并补齐常见时间增减/提取语义。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=177 / stubbed=335 / undeclared=0`）

- [优化] 核心支持库 Windows 桩实现继续补齐：新增 `SetIntInsideBin/ReadFile/WriteFile/QJCase/BJCase/str/GetSpecTime/SetSysTime/UNum/NumToRMB/NumToText/FileDateTime` 的可运行实现，覆盖文件字节读写、全半角转换、数值格式化与文件时间读取路径。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=165 / stubbed=347 / undeclared=0`）

- [优化] 核心支持库高风险字节集命令补齐一批 Windows 实现：完成 `BinLen/ToBin/BinLeft/BinRight/BinMid/InBin/InBinRev/RpBin/RpSubBin/SpaceBin/bin/pbin/p2int/p2float/p2double/GetIntInsideBin` 的语义实现；并修正映射脚本对自动桩区的判定逻辑，改为按函数体内容识别“真实桩/真实实现”，避免仅按区块误判。
  - 范围：`lib/krnln/impl/windows.cpp`、`scripts/krnln/generate-command-mapping.mjs`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`node --check scripts/krnln/generate-command-mapping.mjs`、`npm run krnln:mapping:generate`（`implemented=153 / stubbed=359 / undeclared=0`）

- [新增] 核心支持库 ABI 契约进入实施版 v1：在 `windows.cpp` 落地 OLE Automation Date 适配基础，并新增 `ToTime/now/GetDatePart/GetTimePart` 真实实现（移出自动桩区）；同时将契约与映射规范升级到 v1，映射产物新增 `abiSpecVersion` 与 `abiContractRef` 字段用于高风险命令追踪。
  - 范围：`lib/krnln/impl/windows.cpp`、`docs/krnln-abi-contract.md`、`docs/krnln-command-mapping.md`、`scripts/krnln/generate-command-mapping.mjs`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`node --check scripts/krnln/generate-command-mapping.mjs`、`npm run krnln:mapping:generate`

- [新增] 启动核心支持库 ABI 约定与命令映射实现：新增 `krnln-abi-contract.md` 与 `krnln-command-mapping.md` 讨论版规范，并落地自动映射脚本 `scripts/krnln/generate-command-mapping.mjs`，可生成 `command-mapping.windows.json` 与映射报告，首轮统计 `total=512 / implemented=133 / stubbed=379 / undeclared=0`。
  - 范围：`docs/krnln-abi-contract.md`、`docs/krnln-command-mapping.md`、`docs/krnln-command-mapping-report.md`、`scripts/krnln/generate-command-mapping.mjs`、`lib/krnln/generated/command-mapping.windows.json`、`package.json`
  - 验证：`node --check scripts/krnln/generate-command-mapping.mjs`、`npm run krnln:mapping:generate`

- [优化] 核心支持库 Windows 命令语义批量落地：将一批高频基础命令从默认桩替换为真实实现，覆盖数学运算（`mod/abs/round/pow/sin/cos/tan/...`）、位运算（`band/bor/bxor/shl/shr/...`）、比较逻辑（`equal/notEqual/less/...`）、文本基础处理（`len/left/right/mid/InStr/UCase/LTrim/...`）与数值转换（`val/ToInt/ToLong/hex/binary/...`）；并修正自动桩生成逻辑为“扫描时排除桩区”，确保仅为未实现命令保留桩函数。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [新增] 一次性补齐核心支持库 Windows 全量导出符号层：根据 `krnln.commands.ycmd.json` 自动生成并落地 443 个缺失 `krnln_*` 导出桩函数，统一按返回类型给默认返回值，避免编译/链接阶段缺符号报错。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [新增] 补全核心支持库 Windows 磁盘命令实现：新增 `ChDrive/GetDiskTotalSpace/GetDiskFreeSpace/GetDiskLabel/SetDiskLabel`，支持按驱动器查询容量与卷标，并可切换当前驱动器。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [新增] 补全核心支持库 Windows 文件系统常用命令实现：新增 `CurDir/ChDir/MkDir/RmDir/FileCopy/FileMove/kill/name/IsFileExist/FileLen/GetAttr/SetAttr/GetTempFileName` 平台实现，覆盖目录切换、递归建删目录、文件复制移动删除、属性读写与临时文件名生成。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [优化] 完善核心支持库 Windows 平台细节实现：`run` 增加显示模式标准化与等待失败处理，系统版本查询优先使用 `RtlGetVersion` 降低新系统误判，`GetAppName` 按类型返回完整路径/主文件名/文件名(含扩展名)/运行目录。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [新增] 完善核心支持库跨平台基础实现：`linux.cpp` 与 `macos.mm` 从占位实现扩展到可用基础命令，实现消息框/鸣叫/延时/运行命令/取执行文件与运行目录/环境变量读写/命令行读取/系统版本与语言等通用入口。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`

- [优化] Windows 运行产物启动失败日志增强：`spawn` 失败场景新增“疑似被安全软件拦截”提示，并输出输出目录引导信息，降低 `spawn EPERM` 排障成本。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [新增] 新增误报申诉包收集脚本：支持收集样本路径、SHA-256、构建上下文（编译器/目标/模式）与附加日志，统一生成 `debug_logs/av-report-*/report.json` 与附件目录。
  - 范围：`scripts/security/collect-antivirus-report.mjs`、`package.json`、`README.md`
  - 验证：`node --check scripts/security/collect-antivirus-report.mjs`

- [新增] 新增 Windows 签名打包入口：增加 `package:win:signed`，通过环境变量注入证书参数（含强制签名开关），为发布链路接入代码签名提供落地脚本。
  - 范围：`scripts/security/package-win-signed.mjs`、`package.json`、`README.md`
  - 验证：`node --check scripts/security/package-win-signed.mjs`

- [修复] 修复 ycmd 命令被编译前置校验一刀切拦截的问题：签名校验阶段改为仅在“无协议映射、无内建命令生成器、无内建表达式生成器、且非同名子程序”时才报未接入后端实现，从而解除 `信息框` 等已接入命令的误拦截。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`

- [新增] 核心支持库 Windows 平台实现扩容：在 `impl/windows.cpp` 批量落地系统命令与组件基础操作（消息框、蜂鸣、延时、取屏幕/鼠标、环境变量、剪贴板、运行命令、取程序路径、窗口创建/销毁/激活/焦点/尺寸/文本/重画等），并增加命令命名兼容入口。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [优化] 核心支持库 Windows 平台实现按命令ID对齐：补充 `krnln.MsgBox` 对应导出入口 `krnln_MsgBox`（并兼容 `krnln_msgbox` / 旧 `krnln_message_box`），统一走同一消息框实现，支持按钮样式与父窗口句柄参数。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [新增] 核心支持库 Windows 平台实现起步：`impl/windows.cpp` 的 `krnln_message_box` 从占位返回改为真实调用 WinAPI `MessageBoxA`，并补充空文本/空标题入参兜底，打通首个可用平台命令实现。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`

- [优化] 核心支持库窗口元数据与编译协议合并为单文件：`window-units.json` 采用 `windowUnits` 组件内聚结构，每个组件项内直接承载类名/样式与事件 `channel/code` 映射，不再使用顶层 `controlBindings/eventBindings` 重复节点；编译器协议加载改为优先读取该文件，并保留对旧 `*.protocol.json` 的兼容回退。
  - 范围：`lib/krnln/window-units.json`、`src/main/compiler.ts`、`src/main/libraryManager.ts`、`lib/krnln/README.md`
  - 验证：`get_errors`

- [优化] 核心支持库完整性校验改为仅在稳定正式版强制执行：开发环境与 `beta/rc/dev` 预发布版本启动时跳过校验，避免开发阶段频繁改动核心支持库后需要同步更新哈希。
  - 范围：`src/main/libraryManager.ts`
  - 验证：`get_errors`

- [新增] 支持库管理接入在线索引和独立 zip 安装：默认读取 `https://ycide.dev/libraries/index.json`，按支持库 ID 下载独立包，校验 SHA-256 后安装到用户数据目录，并在支持库管理窗口显示下载、安装、加载、可更新和失败状态。
  - 范围：`src/main/libraryManager.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/library-store.ts`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.css`
  - 验证：`get_errors`、`npm run build`、`npm run libraries:package`

- [新增] 系统设置新增支持库在线索引地址配置，便于后续切换官网源或镜像源。
  - 范围：`src/shared/settings.ts`、`src/renderer/src/components/SettingsDialog/SettingsDialog.tsx`
  - 验证：`get_errors`、`npm run build`

- [优化] 编译命令签名表改为消费 LibraryManager 的已加载支持库快照，支持本地安装目录覆盖内置支持库，并避免未加载支持库参与编译签名解析。
  - 范围：`src/main/compiler.ts`、`src/main/libraryManager.ts`
  - 验证：`get_errors`、`npm run build`

- [新增] 新增支持库发布打包脚本，可将 `lib/<id>` 生成 `packages/<id>.zip` 和 `index.json`，适配官网独立支持库包部署结构。
  - 范围：`scripts/package-libraries.mjs`、`package.json`、`package-lock.json`
  - 验证：`npm run libraries:package`

- [优化] 支持库管理详情改为合并读取支持库标识文件，内置支持库新增 `<id>.library.json` 示例，并移除详情中的邮政编码、通信地址、电话号码展示，方便第三方支持库提供签名、作者、联系方式和主页等标识信息。
  - 范围：`src/main/libraryManager.ts`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`、`lib/krnln/krnln.library.json`、`lib/krnln/README.md`
  - 验证：`get_errors`、`npm run build`

- [优化] 固化核心支持库发布与校验规则：核心支持库只随 ycIDE 版本打包发布，不进入服务器支持库索引/zip 包，不允许远程安装、卸载或禁用；启动时校验核心支持库关键文件和清单完整性，缺失、损坏或被修改时直接报错。
  - 范围：`src/main/libraryManager.ts`、`src/main/index.ts`、`scripts/package-libraries.mjs`、`lib/krnln/README.md`
  - 验证：`get_errors`、`npm run build`、`npm run libraries:package`

- [优化] 支持库命令、数据类型与窗口组件元数据按顶部工具栏目标平台过滤：切换 Windows/macOS/Linux 后，不支持当前目标平台的支持库不会参与编辑器命令表、设计诊断或编译签名解析，支持库管理中同步显示“当前平台禁用”。
  - 范围：`src/main/ycmd-registry.ts`、`src/main/libraryManager.ts`、`src/main/compiler.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`
  - 验证：`get_errors`、`npm run build`

- [优化] 支持库管理改为独立窗口打开，并扩大可用区域到主题管理器同级窗口尺寸，提升支持库列表、在线安装状态和详情信息的浏览空间。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.css`
  - 验证：`get_errors`、`npm run build`

- [修复] 清理主题管理器在 VS Code Problems 面板中的告警：移除 JSX 内联样式、补齐重命名与流程线配置输入框的可访问标签，并改用主题 token 替代 `color-mix` 兼容性较低的样式。
  - 范围：`src/renderer/src/components/ThemeManager/ThemeManager.tsx`、`src/renderer/src/components/ThemeManager/ThemeManager.css`
  - 验证：`get_errors`、`npm run build`

- [优化] 依据导出的易语言核心支持库帮助说明，直接补全核心支持库清单信息：更新 `krnln.library.json` 为正式作者/签名信息，并新增首批流程控制与算术运算命令 `*.ycmd.json` 元数据。
  - 范围：`lib/krnln/krnln.library.json`、`lib/krnln/*.ycmd.json`
  - 验证：`get_errors`

- [优化] 核心支持库命令清单改为单文件集合结构：新增 `krnln.commands.ycmd.json` 承载多命令数组，新增 `krnln.constants.json` 作为常量独立文件，并让 ycmd 注册表兼容集合与单命令两种格式。
  - 范围：`lib/krnln/krnln.commands.ycmd.json`、`lib/krnln/krnln.constants.json`、`src/main/ycmd-registry.ts`、`src/main/libraryManager.ts`
  - 验证：`get_errors`、`npm run build`
### v0.0.3-beta.54（2026-05-09）
- [优化] 优化 AI 面板停止按钮视觉：移除停止按钮红色外边框，仅保留错误语义色图标与背景高亮，避免边框过重。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复 Icon 组件在 Problems 面板中的 inline style 告警：移除 JSX `style` 属性，改为通过 ref 在运行时设置宽高，保持 `size` 行为不变。
  - 范围：`src/renderer/src/components/Icon/Icon.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树双击文件名无法打开文件：文件节点打开路径改为优先使用真实文件名（含扩展名），并统一“模块/窗口/资源”文件节点的双击与回车打开行为。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复粘贴代码产生重复子程序：当粘贴内容中的子程序名已存在于当前文件时，不再插入重复子程序头，改为将该子程序体内容追加到现有子程序末尾。
  - 范围：`src/renderer/src/components/Editor/editorPasteUtils.ts`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 新增重复子程序诊断：手工输入同名 `.子程序` 时，问题面板会报“子程序重复定义”错误。
  - 范围：`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 清理 VS Code Problems 面板中的 OutputPanel/AIAssistantPanel/VisualDesigner 报错：移除内联样式、补齐菜单与表单可访问性属性、改为 CSS 变量驱动动态尺寸与定位，并补齐 Safari 的 `-webkit-user-select` 兼容前缀。
  - 范围：`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`、`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`（改动文件无报错）
- [优化] 调整标题栏拖拽区域设置方式：移除 CSS 中的区域声明，改为在运行时通过 `-webkit-app-region` 写入拖拽/禁拖拽属性，降低 Problems 面板兼容性提示噪音并保持 Electron 标题栏行为。
  - 范围：`src/renderer/src/components/TitleBar/TitleBar.css`、`src/renderer/src/components/TitleBar/TitleBar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [优化] 优化 AI 面板模式下拉宽度：加宽模式选择框，避免选择 Agent 时文本末尾被遮挡。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`
  - 验证：`get_errors`（改动文件无新增错误）
- [修复] 修复 AI 模型配置持久化被覆盖：模型配置弹窗保存后同步更新 App 内存设置（模型/DeepSeek Key/GLM Key/自定义模型），避免后续保存其它设置时将已保存 Key 覆盖回空值。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复 Agent 模式错误提示显示位置：当执行失败（如 API Key 缺失）时，错误信息优先追加到 AI 返回区，不再只显示在输入框下方状态栏。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复 AI 面板点击发送后偶发“无返回”问题：为聊天/流式聊天 IPC 调用补充异常兜底，发生调用异常时返回明确错误而非卡住等待。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复主进程 `file:readDir` 对文件路径抛出 `ENOTDIR`：读取前新增目录类型校验并统一异常降级为空列表，避免日志刷屏及上层调用被异常打断。
  - 范围：`src/main/index.ts`
  - 验证：`get_errors`（改动文件无报错）
- [新增] 项目树支持项目级右键菜单：在项目根节点可单独执行“保存该项目全部文件”“关闭该项目”，用于多项目并行时按项目粒度管理。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复多项目打开时项目树被后开项目覆盖：项目根节点改为按项目目录生成唯一 ID，打开/新建项目时按末尾追加（同项目则替换原根），刷新项目树仅替换当前项目对应根节点。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [优化] 项目树未保存标记支持分类联动：当“窗口/程序集/全局变量/常量表/自定义数据类型/DLL命令/资源”等分类下任一文件存在未保存修改时，分类节点也显示右侧未保存圆点。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复可视化设计器编辑后项目树未实时显示未保存标记：`handleFormChange` 增加 `onOpenTabsChange` 同步，确保 Sidebar 使用到最新 `.efw` 标签内存状态计算 dirty。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树未显示窗口/源码文件未保存标记：项目树 dirty 匹配键改为优先使用节点文件 ID（含扩展名），不再误用显示标签（无扩展名），使 `.efw/.eyc/...` 的“未保存圆点”能正确显示在右侧边框内侧。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复表格模式流程线在 `如果/如果真` 连续块中的误连通：`如果结束/如果真结束` 后紧跟 `如果/如果真` 时，不再套用“判断”专用的上下双横线桥接效果。
  - 范围：`src/renderer/src/components/Editor/eycFlow.ts`、`tests/contract/eyc-flow-contract.spec.mjs`
  - 验证：`node --test tests/contract/eyc-flow-contract.spec.mjs`
- [修复] 修复资源管理器双击子程序偶发跳错行：编辑导致行号漂移后，项目树双击子程序改为按子程序名实时定位（失败时回退旧行号），不再闪到新输入代码行。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复删除子程序后项目树残留旧节点：编辑器内容变更会实时同步 `.eyc` 子程序节点到项目树，未保存状态下删除 `.子程序` 也会立即从“程序集”树中消失。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复属性面板事件下拉勾选状态不刷新：事件列表“✓ 已存在子程序”改为优先读取当前打开标签页内存内容，并用最新解析结果覆盖缓存；删除子程序后勾选会即时取消。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树双击子程序后落点行为：闪烁定位保持在子程序声明行不变，同时将输入光标置于该子程序首个普通代码行的行首（如子程序在第17行、首行代码在第18行则光标落到18行首）。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树双击子程序后“仅闪烁无光标”问题：将光标进入时机调整为闪烁定位之后并增加重试聚焦，避免滚动重排冲掉编辑态；当子程序内仅空白普通行时也可在行首显示光标。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复双击子程序后输入框未渲染导致“仍看不到光标”：纠正 `startEditLine` 调用中的 `isVirtual` 参数，避免与普通代码行渲染条件不匹配而隐藏编辑输入框。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序内“空行优先”落点：当子程序声明后先有若干空行再有命令时，双击项目树后光标改为落在首条空普通行行首，不再跳到后续命令行。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复可视化设计器创建子程序后的“无行号空行”问题：新建子程序时直接生成真实空普通行；项目树跳转到无普通行子程序时会自动物化一条空普通行并聚焦行首，避免必须手动回车后才正常。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 统一“已存在子程序”导航逻辑：属性面板事件下拉选择与可视化设计器双击组件进入默认子程序时，行为改为与项目树双击一致（子程序声明行高亮闪烁 + 光标定位到子程序首个普通行首列，并在缺失普通行时自动物化空普通行）。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序跳转抖动与多点闪烁：去除 App 层对子程序导航的多重重试调用，并在编辑器侧为子程序导航增加“最后一次生效”的序列去重，改为单次闪烁 + 单次聚焦，避免上下抖动与多个位置依次高亮。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复“组件双击/属性面板事件下拉”跳转闪烁偏位：将 `navigateOrCreateSub` 的“已存在子程序”分支改为复用 `navigateToSubprogram` 同一内部实现，统一三入口（项目树/组件双击/事件下拉）的定位与闪烁规则，并补齐子程序名 trim 匹配。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序声明行闪烁偶发错位：统一跳转内部逻辑新增“优先行号命中同名子程序”策略（组件双击/事件下拉传入已知声明行），并在新闪烁前清理旧 `highlight-flash` 样式，避免残留闪烁造成错位错觉。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序跳转后“先在下方后瞬间居中”导致的闪烁位置偏差：声明行定位由 `center` 调整为 `nearest`，并移除光标聚焦阶段的额外滚动，避免二次跳动造成闪烁观感错位。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序跳转时“滚动未稳定先闪烁”导致的错位：声明行高亮改为在两帧 + 短延时后触发（滚动收敛后再闪烁），并继续受导航序列去重保护，避免瞬态位置上闪烁。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复顶部菜单“插入 -> 子程序”后仅出现虚拟空行的问题：插入结构改为“真实空行 + 子程序声明 + 真实空普通行”，并在插入后将光标置于新子程序首条普通行行首，避免必须手动回车物化。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 新增子程序“保底普通空行”删除规则：子程序首条普通行始终保留为可输入占位；删除命中该行时，若有内容则改为空行保留，若本身为空则改删其下方普通行；多选删除/剪切同样遵循该规则，避免全删后无法进入普通代码行。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）


### v0.0.3-beta.53（2026-05-04）
- [新增] 打开项目对话框支持直接选择 `.e/.ec` 文件并导入：支持读取工程头、处理加密密码与同名目录冲突，导入后进入现有打开流程。
  - 范围：`src/main/index.ts`、`src/main/eproject/*`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/EProjectImportDialog/*`
  - 验证：`npm run test:contract:eproject`、`npm run build`
- [新增] 表格模式流程命令显示优化：`判断开始` 在表格模式显示为 `判断`，文本模式保持 `判断开始`。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [修复] 修复 `.e/.ec` 导入偏移与占位问题：修正工程段长度偏移导致的 `Magic` 报错，并修复仅生成“导入说明/空子程序占位”的问题，按程序段/资源段落地真实 `EYC/EGV/EDT/ELL/ERC` 内容。
  - 范围：`src/main/eproject/sections.ts`、`src/main/eproject/nativeProject.ts`、`src/main/eproject/methodCode.ts`、`src/main/eproject/ycProjectWriter.ts`、`tests/contract/eproject-import-contract.spec.mjs`
  - 验证：`npm run test:contract:eproject`、`npm run build`
- [修复] 修复核心命令与支持库命令映射偏差：补齐 `Lib0` 大规模回退映射及 `181/182/203` 等兼容覆盖，并接入多支持库命令 ID 映射，减少 `_LibXCmdY` 占位回退。
  - 范围：`src/main/eproject/lib0CommandMap.ts`、`src/main/eproject/libCommandMaps.ts`、`src/main/eproject/methodCode.ts`
  - 验证：`node --test tests/contract/eproject-import-contract.spec.mjs tests/contract/eyc-flow-contract.spec.mjs`、`npm run build`
- [修复] 修复表格模式流程线在特殊 `判断` 结构中的断裂与错位：补齐 `branch+start` 同行重叠语义、默认分支连通、上一可见行桥接、上半段按来源条件显示。
  - 范围：`src/renderer/src/components/Editor/eycFlow.ts`、`src/renderer/src/components/Editor/editorFlowRenderUtils.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`
- [修复] 修复“第 7 行下三角箭头残留”系列问题：在同深度及跨深度重叠场景统一屏蔽不应显示的下三角，并补齐 `end/branch/inner/startAtDepth` 全路径漏网点。
  - 范围：`src/renderer/src/components/Editor/editorFlowRenderUtils.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`
- [修复] 修复 `如果真` 无结束与嵌套场景的箭头异常：补回应显示的下箭头、移除额外横向虚线、并改为同深度锚定避免错层。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/editorFlowRenderUtils.tsx`
  - 验证：`get_errors`
- [修复] 修复流程补齐与可见性不一致：手工输入 `.如果` 回车可正确补齐可见 `否则/如果结束`，并修复文本模式可见但表格模式隐藏的问题。
  - 范围：`src/renderer/src/components/Editor/useCodeLineEditor.ts`、`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`node --test tests/contract/eyc-flow-contract.spec.mjs tests/contract/flow-line-theme.spec.mjs tests/contract/editor-helper-utils-contract.spec.mjs`、`npm run build`
- [修复] 修复流程命令缩进与特殊插入不一致：统一 commit/Enter 的 `.判断()` 缩进策略，补齐默认分支首行判定回退与特殊结构定位兜底。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/eycFlow.ts`
  - 验证：`get_errors`
- [修复] 修复表格编辑器偶发回退文本模式与空白闪烁：消除缓存初始化 TDZ、限制前置行预览触发条件，避免编辑中可视区被重置。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/editorBlocksModel.ts`
  - 验证：`get_errors`、`npm run build`
- [修复] 修复并清理 VS Code Problems 面板历史告警：修正 ARIA 与菜单语义、移除 inline style、替换不兼容 CSS 写法并补齐类型收窄。
  - 范围：`src/renderer/src/components/Sidebar/*`、`src/renderer/src/components/Editor/*`、`src/renderer/src/components/NewProjectDialog/*`、`src/shared/theme.ts`、`src/main/libraryManager.ts`、`tsconfig.node.json`
  - 验证：`get_errors`（全工作区无报错）

- [优化] 流程线机制正式收敛为命令驱动：去除旧 marker 兼容分支生效路径，自动补齐输出可见流程命令，打开编辑器时清理旧 marker 残留。
  - 范围：`src/renderer/src/components/Editor/eycFlow.ts`、`src/renderer/src/components/Editor/editorFlowAutoExpandUtils.ts`、`src/renderer/src/components/Editor/useCodeLineEditor.ts`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`node --test tests/contract/eyc-flow-contract.spec.mjs tests/contract/flow-line-theme.spec.mjs tests/contract/editor-helper-utils-contract.spec.mjs`
- [优化] 大文件表格模式性能系统性优化：引入可视区 block/行级裁切、缓存复用、滚动量化节流、minimap 阈值收紧、冻结策略收紧，显著降低主线程重绘压力。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`、`npm run build`
- [优化] 将诊断与表格模型构建迁移至 worker：问题检查与 `buildBlocks + computeFlowLines` 异步化，主线程只接收结果渲染，缓解首开和滚动卡顿。
  - 范围：`src/renderer/src/components/Editor/editorDiagnostics*.ts`、`src/renderer/src/components/Editor/editorBlocksModel*.ts`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`、`npm run build`
- [优化] 补强“判断自动展开”诊断能力：增加结构化日志与 `judge-branch:check` 失败上下文，便于快速定位第 5/7 行结构未命中的根因。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

### v0.0.3.52（2026-04-28）
- [修复] 修复表格编辑器中输入 `.如果` 后通过鼠标失焦提交时未补全流程结构（200C/200D）的问题，避免误判导致下方流程缩进错位。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`npm run build`（通过）
- [修复] 修复自绘标题栏中“文件”菜单左侧仍显示四方块占位图的问题，改为渲染 `ycidelogo.svg` 真实 logo。
  - 范围：`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/renderer/src/components/TitleBar/TitleBar.css`、`src/renderer/src/assets/icons/YcideLogo.svg`
  - 验证：`npm run build`（通过）
- [修复] 修复 Windows 启动后窗口左上角仍显示默认四方块图标的问题：补充应用图标多路径解析、设置 `AppUserModelId`，并在窗口创建后显式设置图标。
  - 范围：`src/main/index.ts`
  - 验证：`npm run build`（通过）
- [优化] 将程序图标统一切换为 `图标文件/svg-logo/ycidelogo.svg`，生成并接入打包图标资源（Windows/macOS/Linux）。
  - 范围：`resources/icon.ico`、`resources/icon.icns`、`resources/icon.png`、`src/main/index.ts`
  - 验证：`npm run build`（通过）

### 2026-04-24
- [优化] AI 助手内置 DeepSeek 模型切换为 DeepSeek-V4-Pro，兼容官方 V4 API 更新（base_url 不变，仅更新 model 参数）。
  - 范围：`src/main/ai-assistant.ts`
  - 验证：`get_errors` 检查无新增错误

- [新增] AI 助手新增内置模型选项：DeepSeek V4-Flash、DeepSeek V3.2，并复用 DeepSeek API Key 配置。
  - 范围：`src/shared/ai.ts`、`src/main/ai-assistant.ts`、`src/main/index.ts`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors` 检查无新增错误

- [新增] AI 助手新增 Plan 模式与 Agent 模式：Plan 可输出结构化执行计划；Agent 可基于目标自动决策并对当前激活文件执行修改。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors` 检查无新增错误

- [优化] Agent 模式升级为项目级执行：可遍历资源管理器项目文件，并按工具调用执行 `list_files/read_file/edit_file/open_file`，不再局限已打开标签页。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors` 检查无新增错误

- [新增] 输出面板实现可交互终端（输出查看、命令输入执行、最近命令展示），并打通 AI 助手 Agent 终端工具调用（`terminal_send`/`terminal_read`/`terminal_last_command`）。
  - 范围：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`
  - 验证：`get_errors` 检查无新增错误

- [优化] 终端交互布局调整为单框形态：将输入行嵌入同一终端容器底部，避免输入与输出分离成两个框。
  - 范围：`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`
  - 验证：`get_errors` 检查无新增错误

- [优化] 终端控制台进一步一体化：移除右侧“执行”按钮，仅保留回车执行，并将输入行并入同一滚动终端区域。
  - 范围：`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`
  - 验证：`get_errors` 检查无新增错误

- [优化] 终端改为按需启动：点击“终端”标签时启动，不再显示“未启动”状态文案；启动后自动输出当前终端工作目录。
  - 范围：`src/main/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`
  - 验证：`get_errors` 检查无新增错误


### 2026-04-23
  - 范围：`src/shared/settings.ts`、`src/renderer/src/components/SettingsDialog/SettingsDialog.tsx`、`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：类型检查无新增错误

## 2026-04-24（终端输出稳定性修复）

- 优化：修复 `cmd.exe` 在中文 Windows 下终端输出乱码问题，新增终端输出解码逻辑，优先 UTF-8，检测到替换符时自动回退 GBK（CP936）解码。
- 优化：终端系统日志（已启动/当前目录）改为去重写入，避免重复触发启动流程时出现连续重复提示。
- 涉及文件：`src/main/index.ts`

## 2026-04-24（终端快捷键与内联输入交互修复）

- 修复：终端支持 `Ctrl+C` 中断当前命令，主进程新增 `terminal:interrupt`，中断后自动重建 shell 会话。
- 优化：终端输入改为控制台内联输入，移除独立输入框；光标在控制台末尾闪烁，输入位置与终端最后一行保持一致。
- 优化：终端区域聚焦后可直接键盘输入，支持 `Enter` 执行、`Backspace` 删除和粘贴文本。
- 涉及文件：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`

## 2026-04-24（Agent 工具结果流式回传修复）

- 修复：Agent 执行工具步骤时，工具日志改为实时回调到 AI 助手消息流，避免“执行完但窗口没有结果”的空白体验。
- 修复：`terminal_send` 工具在执行后自动抓取新增终端输出并回传到 Agent 对话，确保如 `node --version` 这类命令结果可直接显示在 AI 助手窗口。
- 优化：保留最终汇总日志兜底，避免极端情况下流式日志遗漏。
- 涉及文件：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`

## 2026-04-24（AI 助手交互与 Agent 意图路由优化）

- 新增：AI 助手输入区增加“停止”按钮，可立即中止当前会话的流式显示与后续结果写入（聊天 / 计划 / 编辑 / Agent）。
- 修复：AI 聊天记录区域恢复文本多选与复制能力。
- 优化：Agent 模式移除固定占位输出，减少每次执行都出现的重复提示行。
- 修复：当用户目标包含网络连通性语义（如“检查网络连接”）时，Agent 优先走终端 `ping` 诊断流程，而不是先扫描项目源码文件。
- 涉及文件：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`

## 2026-04-24（DeepSeek Agent Function Calling 升级）

- 新增：AI 通信层支持 `tools` / `tool_choice` / `tool_calls` 协议，兼容 DeepSeek 的函数调用 Agent 模式。
- 新增：主进程与预加载桥接 `ai:chatWithTools` 能力，渲染进程可直接发起工具调用会话。
- 优化：Agent 执行器从“JSON 计划解析”升级为“多轮 tool-calling 闭环”（assistant tool_calls -> 本地执行工具 -> tool 结果回传 -> 下一轮）。
- 优化：保留项目级文件与终端工具能力，并继续对网络连通性任务优先使用终端命令诊断。
- 涉及文件：`src/shared/ai.ts`、`src/main/ai-assistant.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`

## 2026-04-24（Agent 工具调用审计可视化）

- 新增：Agent 模式支持按工具调用分组展示审计条目，每次 tool call 可折叠查看“参数/返回”。
- 优化：工具调用参数与返回值支持自动截断，避免超长内容淹没聊天流。
- 优化：保留原有日志流并叠加结构化工具审计，便于快速定位调用链和结果。
- 涉及文件：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`

## 2026-04-24（dev 运行语法错误修复）

- 修复：`App.tsx` 中 Agent 工具循环闭合层级异常，导致 `npm run dev` 时 Vite/Babel 抛出 `Unexpected token`，界面出现大段错误堆栈文本。
- 验证：重新执行 `npm run dev`，主进程 / preload / renderer 均可正常启动，未再出现该语法错误。
- 涉及文件：`src/renderer/src/App.tsx`

- [新增] 系统设置中的布尔项改为左右滑动开关（关闭灰色、开启蓝色）。
  - 范围：`src/renderer/src/components/SettingsDialog/SettingsDialog.tsx`、`src/renderer/src/components/SettingsDialog/SettingsDialog.css`
  - 验证：类型检查无新增错误

- [优化] 代码预览区改为内容随滚动联动，不再仅移动视窗框。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：类型检查无新增错误

- [修复] `.eyc` 标签名回归问题，恢复优先显示 `.程序集` 名称（不足时回退去后缀文件名）。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`
  - 验证：类型检查无新增错误

- [修复] 项目树双击子程序跨文档跳转不到位问题，优化为一次跳转到位。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：类型检查无新增错误

（发版后将对应条目从“待发版条目”移动到这里，并标注版本号）

