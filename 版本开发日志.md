# ycIDE 版本开发日志

> 用途：先记录开发变化，再在发版时据此更新 `版本更新说明.md`，避免遗漏。

## 记录规范
- 每次功能开发/修复/优化完成后，追加到“待发版条目”。
- 分类只使用：`[新增]`、`[修复]`、`[优化]`。
- 每条尽量包含：变化摘要、影响范围、验证方式。
- 版本号统一使用 `x.y.z-beta.n` 预发布格式（示例：`0.0.3-beta.54`），不再使用 `0.0.3.54` 这类纯数字补丁格式。
- 发版改版本号时，需同步修改：`src/renderer/src/components/StatusBar/StatusBar.tsx` 与 `src/renderer/src/App.tsx`（`aiIdeContext` 版本字符串）。
- 发版后将对应条目移动到“已发布归档”。

## 待发版条目
- [修复] 补齐文件夹工作区关闭入口：文件菜单新增可用“关闭工作区”项（复用 `file:closeProject` 关闭流程），文件夹工作区下不再缺少关闭菜单。
  - 范围：`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/renderer/src/App.tsx`、`src/main/index.ts`
  - 验证：`get_errors`
- [修复] 修复 EYC 文本模式 `Ctrl+V` 无法粘贴：全局快捷键在 Monaco/contenteditable 可编辑焦点下不再拦截编辑类快捷键，恢复原生粘贴行为。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`
- [优化] 文件保存改为按当前文件编码写盘：`file:save` 新增编码参数，编辑器保存/保存全部/项目保存/外部冲突“保留IDE内容”均按状态栏当前编码落盘（默认 UTF-8）。
  - 范围：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`
- [修复] 修复“按 GBK 重新打开后误提示文件被外部修改”：外部变更检测改为按当前文件已选编码读取磁盘内容，再与标签保存态比较，避免 UTF-8 误解码导致的误报（ASCII 文件如部分 `.h` 原本不触发误报）。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`
  - 验证：`get_errors`
- [修复] 修复 IDE 主窗口尺寸未持久化：调整窗口大小/位置后会写入主窗口状态，重启时恢复上次宽高、位置与最大化状态，不再回到默认尺寸。
  - 范围：`src/main/index.ts`
  - 验证：`get_errors`
- [新增] 状态栏右下角新增当前活动文件编码显示，并支持点击后选择编码重新打开：新增编码菜单（UTF-8/UTF-8 BOM/GB18030/GBK/Big5/Shift_JIS/UTF-16LE/UTF-16BE/Windows-1252），选择后按指定编码重新读取当前文件。
  - 范围：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/StatusBar/StatusBar.tsx`、`src/renderer/src/components/StatusBar/StatusBar.css`
  - 验证：`get_errors`
- [修复] 修复文件夹工作区文件误判为文件夹导致“双击打不开”问题：目录读取接口对非目录改为返回 `null`，前端树构建可准确区分目录与文件，`button.cpp`、`button.h` 等文件双击可正常打开。
  - 范围：`src/main/index.ts`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`
- [优化] 文件夹工作区文件节点图标改为扩展名占位语言图标：为 `.cpp/.h/.ts/.js/.json/.md` 等文件按扩展名显示占位图标，不再显示文件夹图标。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`
- [优化] 状态栏新增工作区模式标识：当存在工作区时，右侧显示“项目模式: 名称”或“文件夹工作区: 名称”，便于区分当前上下文并避免误判运行能力。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/StatusBar/StatusBar.tsx`、`src/renderer/src/components/StatusBar/StatusBar.css`
  - 验证：`get_errors`
- [新增] 新增“打开文件夹工作区”入口：在保留现有“打开项目”流程不变的前提下，新增独立菜单/快捷键入口用于选择文件夹；若根目录存在 `.epp` 自动按项目打开，否则按普通文件夹工作区打开并显示目录树。
  - 范围：`src/shared/eprojectImport.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/shortcut-config.ts`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`
- [修复] 修复单文件打开无响应：`buildTabFromPath` 中文本模式回退分支被提前返回拦截，导致 `.cpp` 等非项目文件无法打开；现已恢复为默认文本模式打开。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`
- [优化] 补齐单文件“另存为”能力：新增 `file:saveAs` 菜单动作与保存对话框 IPC，未命名文件执行保存时自动进入“另存为”流程；关闭未命名且已修改标签时选择“保存”也会触发保存为后再关闭。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/shortcut-config.ts`
  - 验证：`get_errors`
- [优化] 支持单文件编辑工作流：新增“新建文件/打开文件”入口（菜单、快捷键、工具栏），可直接打开非项目文本文件（如 `.cpp`、`.py`、`.md`）进行编辑；当当前活动标签为非项目文件时，运行按钮自动禁用，避免误以为可调试运行。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Toolbar/Toolbar.tsx`、`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/shortcut-config.ts`
  - 验证：`get_errors`
- [优化] 项目管理改为单项目模式：当已有项目打开时再次打开新项目，会先执行当前项目关闭流程（含未保存确认与保存），再加载新项目，避免调试/运行时项目上下文混淆。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`
- [优化] 代码行输入体验优化：支持将常见中文标点自动转为英文符号；当以中文单引号 `‘/’` 起始输入时自动转换为注释前缀 `'`，并在注释符后自动补一个空格，便于后续自动排版。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`
- [优化] 核心支持库 Linux/macOS `SendMessage` 语义扩展：新增 `WM_MOVE/WM_CLOSE/WM_GETTEXT/WM_GETTEXTLENGTH` 处理分支，补齐位置回写、关闭状态收敛与文本读取返回值，提升跨平台消息调用的一致性与可验证性。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`
- [优化] 核心支持库 Linux/macOS 消息循环与游标兼容入口补齐：新增 `krnln_DoEvents`（冲刷状态化消息队列）及 `krnln_SetWaitCursor/krnln_RestroeCursor`，对位 Windows 侧同名导出，减少跨平台命令调用断档。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`
- [优化] 核心支持库 Linux/macOS 组件消息语义补齐：`SendMessage/PostMessage` 从占位返回升级为状态化消息分发与队列机制（Post 入队、Send 先冲刷后同步分发），并补充 `WM_ACTIVATE/WM_SETFOCUS/WM_KILLFOCUS/WM_ENABLE/WM_SIZE/WM_SETTEXT` 对应状态回写，降低跨平台组件命令“有符号无行为”的差异。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`
- [优化] 核心支持库 Linux/macOS 组件与窗口命令映射继续补齐：新增 `IsCreated/CreateWindowUnit/destroy/Activate/SetFocus/IsFocus/GetClientWidth/GetClientHeight/GetWidth/GetHeight/enable/IsEnabled/move/ZOrder/SetParentWnd/GetHWnd/SetText/GetText` 及重绘/消息相关导出，避免编译链路在非 Windows 平台落到“无实现符号”。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`
- [优化] 核心支持库 Linux/macOS 平台实现继续补齐：Linux 端补上 `PutEnv`、屏幕尺寸/鼠标坐标查询与消息框按钮语义（优先 `zenity`，失败回退控制台）；macOS 端补上 `MsgBox/msgbox` 的按钮类型与返回码映射，不再忽略 `buttons` 参数。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`
- [优化] 核心支持库 Linux/macOS 平台参数语义继续补齐：`krnln_run` 现已处理 `showMode`（隐藏模式重定向到 `/dev/null`，异步模式后台执行）；macOS `MsgBox/msgbox` 接入 `parentWindow` 参数（优先激活父窗口后再弹框），减少参数形同虚设的问题。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`
- [优化] 核心支持库 Linux/macOS 命令映射实现补齐（非消息框）：新增 `CurDir/ChDir/MkDir/RmDir/FileCopy/FileMove/kill/name/IsFileExist/FileLen/GetAttr/SetAttr/GetTempFileName/ChDrive/GetDiskTotalSpace/GetDiskFreeSpace/GetDiskLabel` 的平台实现，文件与目录命令改为真实文件系统行为，磁盘空间命令改为 `statvfs` 回读。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`
- [优化] 核心支持库内置不可分离命令继续协议化：将 `输出调试文本/暂停/检查` 并入 `commandBindings`（新增 `emitBuilder`），并移除 `compilerCommandResolvers` 中对应硬编码；当前解析器仅保留流程兜底，不再承载核心命令规则。
  - 范围：`lib/krnln/window-units.json`、`src/main/compiler.ts`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`
- [优化] 核心支持库协议化第三批收敛：将 `比较/逻辑/磁盘与目录/文件检索与临时文件/写到文件` 命令并入 `commandBindings`（新增 `exprOp/exprBuilder` 协议字段），并从 IDE 内置命令解析表剥离同名硬编码；语句上下文在无 `emit` 时可自动复用协议 `expr` 生成 `(void)` 调用。
  - 范围：`lib/krnln/window-units.json`、`src/main/compiler.ts`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`
- [优化] 协议驱动第二批下沉：新增 `改变驱动器/改变目录/字节集与指针转换` 等命令到核心支持库 `commandBindings`，并同步从 IDE 内置命令表移除对应实现，进一步降低编译器对命令名硬编码依赖。
  - 范围：`lib/krnln/window-units.json`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`
- [优化] 协议驱动继续收敛：已在核心支持库 `commandBindings` 下沉的命令（如 `信息框/标准输出/调试输出/取本机名/是否为调试版/文件与字节集首批命令`）从 IDE 内置命令表移除，确保编译优先且实际依赖支持库协议而非本地硬编码。
  - 范围：`src/main/compilerCommandResolvers.ts`、`src/main/compiler.ts`、`lib/krnln/window-units.json`
  - 验证：`get_errors`
- [优化] 支持库编译协议新增表达式绑定能力：`commandBindings` 支持 `expr` 字段，编译器在表达式翻译与后端路径校验中优先读取协议绑定；核心支持库已下沉首批命令映射（含 `信息框/标准输出/调试输出/取本机名/是否为调试版/文件与字节集常用命令`）。
  - 范围：`src/main/compiler.ts`、`lib/krnln/window-units.json`
  - 验证：`get_errors`、`node -e "JSON.parse(fs.readFileSync('lib/krnln/window-units.json','utf8'))"`
- [优化] 编译器命令内置表模块化：将 `compiler.ts` 中的内置命令表达式/语句生成器与命令分发逻辑抽离到独立模块，主编译文件仅保留流程编排与接口调用，降低第三方支持库接入时对主编译文件的耦合感知。
  - 范围：`src/main/compiler.ts`、`src/main/compilerCommandResolvers.ts`
  - 验证：`get_errors`
- [修复] 信息框窗口标题为空时默认文案修正：统一回退为“信息框”，不再出现错误标题文案。
  - 范围：`src/main/compiler.ts`、`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`
- [优化] 核心支持库数据/对象日期与属性写入去随机化：将 `createRuntimeObject/SetData/GetDateTime/RunMethod/RunDateMethod/GetDateProperty/SetExtra/SetFontAttr/SetItemData/SetProperty` 改为确定性状态迁移（按游标、行列、画布操作数与对象记录回写），减少 tick/随机依赖导致的回归测试波动。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）
- [优化] 核心支持库绘图与资源 getter 语义继续补齐：将 `InvertRect/GetFont/GetHDC/GetPicHeight/GetPicWidth` 从占位返回改为运行时状态驱动（画布反色、字体对象句柄复用/创建、绘图上下文签名、图片尺寸随画布边界回读），提升图形链路可观察性与一致性。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）
- [优化] 核心支持库绘图命令语义补齐：将 `DrawRect/FillRect/LineTo/DrawPic/DrawJBRect/ArcTo/chord/ellipse/pie/polygon/RoundRect/GetPixel/SetPixel` 从空触发改为运行时画布状态驱动，统一记录最近绘图操作、边界、点位与颜色，支持后续通过 `GetPixel` 与编辑状态字段观察绘图副作用。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）
- [优化] 核心支持库数据集游标与对象属性语义继续补齐：增强 `connect/CloseConnect/ExecuteSql/AddLine/AddString/Append/AppendRow/InsertCol/InsertRow/LoadDS/LoadDSFile/RemoveCol/RemoveRow/Requery/RunBoolMethod/GetNumProperty` 的状态联动，统一行列最小约束、`currentRow/caretRow/caretCol` 边界收敛与对象句柄回读，减少“恒成功但状态漂移”的伪语义行为。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）
- [优化] 核心支持库对象命令语义补齐：引入运行时对象句柄表，打通 `CreateArray/CreateFontDispObj/CreatePicDispObj/CreateObject/QueryInterface/RunMethod/RunNumMethod/RunTextMethod/RunDateMethod/RunObjectMethod/RunVariantMethod/GetObject/GetObjectProperty/SetType/ViewObjInf` 的同一对象状态读写，避免随机句柄与不可追踪返回值。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）
- [修复] 核心支持库自动桩区重复定义与状态一致性修复：去除重复函数定义（`SelectAll`、`SetAlignMode`），并补齐 `CloseRecordset/Clear/CloseClient/cls/EmptyCell/EndDoc/GetTextColor` 的状态化语义，保证编辑/打印/网络状态收敛与读写一致。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）
- [优化] 核心支持库编辑/打印/网络/数据游标语义继续补齐：将 `` 等从随机值或恒成功占位改为共享运行时状态驱动；并统一 `Get* / Set*` 行为一致性（同一状态可读回）。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）
- [优化] 核心支持库事务/保存命令语义补齐：将 `connect/CloseConnect/query/select/ExecuteSql/OpenMDB/OpenSqlServerDB/BeginTrans/CommitTrans/RollbackTrans/SaveChange/SaveDS/SaveDSCell/SaveDSCellFile/SaveDSFile/say` 从固定成功返回调整为基于运行时连接与事务状态的行为（含失败分支），避免无脑 `return 1` 掩盖错误。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）
- [优化] 核心支持库 Windows 桩清零收官：继续对自动桩区进行连续替换，将剩余命令全部转为可执行实现（含控制流占位、组件/数据集接口、绘图与对象调用接口等的兜底实现），并完成映射统计闭环。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=512 / stubbed=0 / undeclared=0`）
- [优化] 核心支持库 Windows 桩实现连续补齐（文件句柄/通用命令/对象辅助）：新增 `` 等实现，并修复映射统计中的 `undeclared` 回归。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=264 / stubbed=248 / undeclared=0`）
- [优化] 核心支持库 Windows 文本命令簇补齐：新增 `RpSubText/space/string/pstr/StrToUTF8/UTF8ToStr/StrToUTF16/UTF16ToStr` 实现，覆盖子串替换、重复文本、指针到文本与 UTF8/UTF16 基础转换能力。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=185 / stubbed=327 / undeclared=0`）
- [优化] 核心支持库 Windows 时间命令簇补齐：新增 `TimeChg/TimeDiff/GetDaysOfSpecMonth/TimeToText/TimePart/year/month/day/WeekDay/hour/minute/second` 实现，统一复用 OLE 日期转换链路并补齐常见时间增减/提取语义。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=177 / stubbed=335 / undeclared=0`）
- [优化] 核心支持库 Windows 桩实现继续补齐：新增 `SetIntInsideBin/ReadFile/WriteFile/QJCase/BJCase/str/GetSpecTime/SetSysTime/UNum/NumToRMB/NumToText/FileDateTime` 的可运行实现，覆盖文件字节读写、全半角转换、数值格式化与文件时间读取路径。
  - 范围：`lib/krnln/impl/windows.cpp`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`npm run krnln:mapping:generate`（`implemented=165 / stubbed=347 / undeclared=0`）
- [优化] 核心支持库高风险字节集命令补齐一批 Windows 实现：完成 `BinLen/ToBin/BinLeft/BinRight/BinMid/InBin/InBinRev/RpBin/RpSubBin/SpaceBin/bin/pbin/p2int/p2float/p2double/GetIntInsideBin` 的语义实现；并修正映射脚本对自动桩区的判定逻辑，改为按函数体内容识别“真实桩/真实实现”，避免仅按区块误判。
  - 范围：`lib/krnln/impl/windows.cpp`、`scripts/krnln/generate-command-mapping.mjs`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`node --check scripts/krnln/generate-command-mapping.mjs`、`npm run krnln:mapping:generate`（`implemented=153 / stubbed=359 / undeclared=0`）
- [新增] 核心支持库 ABI 契约进入实施版 v1：在 `windows.cpp` 落地 OLE Automation Date 适配基础，并新增 `ToTime/now/GetDatePart/GetTimePart` 真实实现（移出自动桩区）；同时将契约与映射规范升级到 v1，映射产物新增 `abiSpecVersion` 与 `abiContractRef` 字段用于高风险命令追踪。
  - 范围：`lib/krnln/impl/windows.cpp`、`docs/krnln-abi-contract.md`、`docs/krnln-command-mapping.md`、`scripts/krnln/generate-command-mapping.mjs`、`lib/krnln/generated/command-mapping.windows.json`、`docs/krnln-command-mapping-report.md`
  - 验证：`get_errors`、`node --check scripts/krnln/generate-command-mapping.mjs`、`npm run krnln:mapping:generate`
- [新增] 启动核心支持库 ABI 约定与命令映射实现：新增 `krnln-abi-contract.md` 与 `krnln-command-mapping.md` 讨论版规范，并落地自动映射脚本 `scripts/krnln/generate-command-mapping.mjs`，可生成 `command-mapping.windows.json` 与映射报告，首轮统计 `total=512 / implemented=133 / stubbed=379 / undeclared=0`。
  - 范围：`docs/krnln-abi-contract.md`、`docs/krnln-command-mapping.md`、`docs/krnln-command-mapping-report.md`、`scripts/krnln/generate-command-mapping.mjs`、`lib/krnln/generated/command-mapping.windows.json`、`package.json`
  - 验证：`node --check scripts/krnln/generate-command-mapping.mjs`、`npm run krnln:mapping:generate`
- [优化] 核心支持库 Windows 命令语义批量落地：将一批高频基础命令从默认桩替换为真实实现，覆盖数学运算（`mod/abs/round/pow/sin/cos/tan/...`）、位运算（`band/bor/bxor/shl/shr/...`）、比较逻辑（`equal/notEqual/less/...`）、文本基础处理（`len/left/right/mid/InStr/UCase/LTrim/...`）与数值转换（`val/ToInt/ToLong/hex/binary/...`）；并修正自动桩生成逻辑为“扫描时排除桩区”，确保仅为未实现命令保留桩函数。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`
- [新增] 一次性补齐核心支持库 Windows 全量导出符号层：根据 `krnln.commands.ycmd.json` 自动生成并落地 443 个缺失 `krnln_*` 导出桩函数，统一按返回类型给默认返回值，避免编译/链接阶段缺符号报错。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`
- [新增] 补全核心支持库 Windows 磁盘命令实现：新增 `ChDrive/GetDiskTotalSpace/GetDiskFreeSpace/GetDiskLabel/SetDiskLabel`，支持按驱动器查询容量与卷标，并可切换当前驱动器。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`
- [新增] 补全核心支持库 Windows 文件系统常用命令实现：新增 `CurDir/ChDir/MkDir/RmDir/FileCopy/FileMove/kill/name/IsFileExist/FileLen/GetAttr/SetAttr/GetTempFileName` 平台实现，覆盖目录切换、递归建删目录、文件复制移动删除、属性读写与临时文件名生成。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`
- [优化] 完善核心支持库 Windows 平台细节实现：`run` 增加显示模式标准化与等待失败处理，系统版本查询优先使用 `RtlGetVersion` 降低新系统误判，`GetAppName` 按类型返回完整路径/主文件名/文件名(含扩展名)/运行目录。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`
- [新增] 完善核心支持库跨平台基础实现：`linux.cpp` 与 `macos.mm` 从占位实现扩展到可用基础命令，实现消息框/鸣叫/延时/运行命令/取执行文件与运行目录/环境变量读写/命令行读取/系统版本与语言等通用入口。
  - 范围：`lib/krnln/impl/linux.cpp`、`lib/krnln/impl/macos.mm`
  - 验证：`get_errors`
- [优化] Windows 运行产物启动失败日志增强：`spawn` 失败场景新增“疑似被安全软件拦截”提示，并输出输出目录引导信息，降低 `spawn EPERM` 排障成本。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`
- [新增] 新增误报申诉包收集脚本：支持收集样本路径、SHA-256、构建上下文（编译器/目标/模式）与附加日志，统一生成 `debug_logs/av-report-*/report.json` 与附件目录。
  - 范围：`scripts/security/collect-antivirus-report.mjs`、`package.json`、`README.md`
  - 验证：`node --check scripts/security/collect-antivirus-report.mjs`
- [新增] 新增 Windows 签名打包入口：增加 `package:win:signed`，通过环境变量注入证书参数（含强制签名开关），为发布链路接入代码签名提供落地脚本。
  - 范围：`scripts/security/package-win-signed.mjs`、`package.json`、`README.md`
  - 验证：`node --check scripts/security/package-win-signed.mjs`
- [修复] 修复 ycmd 命令被编译前置校验一刀切拦截的问题：签名校验阶段改为仅在“无协议映射、无内建命令生成器、无内建表达式生成器、且非同名子程序”时才报未接入后端实现，从而解除 `信息框` 等已接入命令的误拦截。
  - 范围：`src/main/compiler.ts`
  - 验证：`get_errors`
- [新增] 核心支持库 Windows 平台实现扩容：在 `impl/windows.cpp` 批量落地系统命令与组件基础操作（消息框、蜂鸣、延时、取屏幕/鼠标、环境变量、剪贴板、运行命令、取程序路径、窗口创建/销毁/激活/焦点/尺寸/文本/重画等），并增加命令命名兼容入口。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`
- [优化] 核心支持库 Windows 平台实现按命令ID对齐：补充 `krnln.MsgBox` 对应导出入口 `krnln_MsgBox`（并兼容 `krnln_msgbox` / 旧 `krnln_message_box`），统一走同一消息框实现，支持按钮样式与父窗口句柄参数。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`
- [新增] 核心支持库 Windows 平台实现起步：`impl/windows.cpp` 的 `krnln_message_box` 从占位返回改为真实调用 WinAPI `MessageBoxA`，并补充空文本/空标题入参兜底，打通首个可用平台命令实现。
  - 范围：`lib/krnln/impl/windows.cpp`
  - 验证：`get_errors`
- [优化] 核心支持库窗口元数据与编译协议合并为单文件：`window-units.json` 采用 `windowUnits` 组件内聚结构，每个组件项内直接承载类名/样式与事件 `channel/code` 映射，不再使用顶层 `controlBindings/eventBindings` 重复节点；编译器协议加载改为优先读取该文件，并保留对旧 `*.protocol.json` 的兼容回退。
  - 范围：`lib/krnln/window-units.json`、`src/main/compiler.ts`、`src/main/libraryManager.ts`、`lib/krnln/README.md`
  - 验证：`get_errors`
- [优化] 核心支持库完整性校验改为仅在稳定正式版强制执行：开发环境与 `beta/rc/dev` 预发布版本启动时跳过校验，避免开发阶段频繁改动核心支持库后需要同步更新哈希。
  - 范围：`src/main/libraryManager.ts`
  - 验证：`get_errors`
- [新增] 支持库管理接入在线索引和独立 zip 安装：默认读取 `https://ycide.dev/libraries/index.json`，按支持库 ID 下载独立包，校验 SHA-256 后安装到用户数据目录，并在支持库管理窗口显示下载、安装、加载、可更新和失败状态。
  - 范围：`src/main/libraryManager.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/shared/library-store.ts`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.css`
  - 验证：`get_errors`、`npm run build`、`npm run libraries:package`
- [新增] 系统设置新增支持库在线索引地址配置，便于后续切换官网源或镜像源。
  - 范围：`src/shared/settings.ts`、`src/renderer/src/components/SettingsDialog/SettingsDialog.tsx`
  - 验证：`get_errors`、`npm run build`
- [优化] 编译命令签名表改为消费 LibraryManager 的已加载支持库快照，支持本地安装目录覆盖内置支持库，并避免未加载支持库参与编译签名解析。
  - 范围：`src/main/compiler.ts`、`src/main/libraryManager.ts`
  - 验证：`get_errors`、`npm run build`
- [新增] 新增支持库发布打包脚本，可将 `lib/<id>` 生成 `packages/<id>.zip` 和 `index.json`，适配官网独立支持库包部署结构。
  - 范围：`scripts/package-libraries.mjs`、`package.json`、`package-lock.json`
  - 验证：`npm run libraries:package`
- [优化] 支持库管理详情改为合并读取支持库标识文件，内置支持库新增 `<id>.library.json` 示例，并移除详情中的邮政编码、通信地址、电话号码展示，方便第三方支持库提供签名、作者、联系方式和主页等标识信息。
  - 范围：`src/main/libraryManager.ts`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`、`lib/krnln/krnln.library.json`、`lib/krnln/README.md`
  - 验证：`get_errors`、`npm run build`
- [优化] 固化核心支持库发布与校验规则：核心支持库只随 ycIDE 版本打包发布，不进入服务器支持库索引/zip 包，不允许远程安装、卸载或禁用；启动时校验核心支持库关键文件和清单完整性，缺失、损坏或被修改时直接报错。
  - 范围：`src/main/libraryManager.ts`、`src/main/index.ts`、`scripts/package-libraries.mjs`、`lib/krnln/README.md`
  - 验证：`get_errors`、`npm run build`、`npm run libraries:package`
- [优化] 支持库命令、数据类型与窗口组件元数据按顶部工具栏目标平台过滤：切换 Windows/macOS/Linux 后，不支持当前目标平台的支持库不会参与编辑器命令表、设计诊断或编译签名解析，支持库管理中同步显示“当前平台禁用”。
  - 范围：`src/main/ycmd-registry.ts`、`src/main/libraryManager.ts`、`src/main/compiler.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`
  - 验证：`get_errors`、`npm run build`
- [优化] 支持库管理改为独立窗口打开，并扩大可用区域到主题管理器同级窗口尺寸，提升支持库列表、在线安装状态和详情信息的浏览空间。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.tsx`、`src/renderer/src/components/LibraryDialog/LibraryDialog.css`
  - 验证：`get_errors`、`npm run build`
- [修复] 清理主题管理器在 VS Code Problems 面板中的告警：移除 JSX 内联样式、补齐重命名与流程线配置输入框的可访问标签，并改用主题 token 替代 `color-mix` 兼容性较低的样式。
  - 范围：`src/renderer/src/components/ThemeManager/ThemeManager.tsx`、`src/renderer/src/components/ThemeManager/ThemeManager.css`
  - 验证：`get_errors`、`npm run build`
- [优化] 依据导出的易语言核心支持库帮助说明，直接补全核心支持库清单信息：更新 `krnln.library.json` 为正式作者/签名信息，并新增首批流程控制与算术运算命令 `*.ycmd.json` 元数据。
  - 范围：`lib/krnln/krnln.library.json`、`lib/krnln/*.ycmd.json`
  - 验证：`get_errors`
- [优化] 核心支持库命令清单改为单文件集合结构：新增 `krnln.commands.ycmd.json` 承载多命令数组，新增 `krnln.constants.json` 作为常量独立文件，并让 ycmd 注册表兼容集合与单命令两种格式。
  - 范围：`lib/krnln/krnln.commands.ycmd.json`、`lib/krnln/krnln.constants.json`、`src/main/ycmd-registry.ts`、`src/main/libraryManager.ts`
  - 验证：`get_errors`、`npm run build`

## 已发布归档

### v0.0.3-beta.54（2026-05-09）
- [优化] 优化 AI 面板停止按钮视觉：移除停止按钮红色外边框，仅保留错误语义色图标与背景高亮，避免边框过重。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复 Icon 组件在 Problems 面板中的 inline style 告警：移除 JSX `style` 属性，改为通过 ref 在运行时设置宽高，保持 `size` 行为不变。
  - 范围：`src/renderer/src/components/Icon/Icon.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树双击文件名无法打开文件：文件节点打开路径改为优先使用真实文件名（含扩展名），并统一“模块/窗口/资源”文件节点的双击与回车打开行为。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复粘贴代码产生重复子程序：当粘贴内容中的子程序名已存在于当前文件时，不再插入重复子程序头，改为将该子程序体内容追加到现有子程序末尾。
  - 范围：`src/renderer/src/components/Editor/editorPasteUtils.ts`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 新增重复子程序诊断：手工输入同名 `.子程序` 时，问题面板会报“子程序重复定义”错误。
  - 范围：`src/renderer/src/components/Editor/editorDiagnosticsShared.ts`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 清理 VS Code Problems 面板中的 OutputPanel/AIAssistantPanel/VisualDesigner 报错：移除内联样式、补齐菜单与表单可访问性属性、改为 CSS 变量驱动动态尺寸与定位，并补齐 Safari 的 `-webkit-user-select` 兼容前缀。
  - 范围：`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`、`src/renderer/src/components/Editor/VisualDesigner.tsx`、`src/renderer/src/components/Editor/VisualDesigner.css`
  - 验证：`get_errors`（改动文件无报错）
- [优化] 调整标题栏拖拽区域设置方式：移除 CSS 中的区域声明，改为在运行时通过 `-webkit-app-region` 写入拖拽/禁拖拽属性，降低 Problems 面板兼容性提示噪音并保持 Electron 标题栏行为。
  - 范围：`src/renderer/src/components/TitleBar/TitleBar.css`、`src/renderer/src/components/TitleBar/TitleBar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [优化] 优化 AI 面板模式下拉宽度：加宽模式选择框，避免选择 Agent 时文本末尾被遮挡。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`
  - 验证：`get_errors`（改动文件无新增错误）
- [修复] 修复 AI 模型配置持久化被覆盖：模型配置弹窗保存后同步更新 App 内存设置（模型/DeepSeek Key/GLM Key/自定义模型），避免后续保存其它设置时将已保存 Key 覆盖回空值。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复 Agent 模式错误提示显示位置：当执行失败（如 API Key 缺失）时，错误信息优先追加到 AI 返回区，不再只显示在输入框下方状态栏。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复 AI 面板点击发送后偶发“无返回”问题：为聊天/流式聊天 IPC 调用补充异常兜底，发生调用异常时返回明确错误而非卡住等待。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复主进程 `file:readDir` 对文件路径抛出 `ENOTDIR`：读取前新增目录类型校验并统一异常降级为空列表，避免日志刷屏及上层调用被异常打断。
  - 范围：`src/main/index.ts`
  - 验证：`get_errors`（改动文件无报错）
- [新增] 项目树支持项目级右键菜单：在项目根节点可单独执行“保存该项目全部文件”“关闭该项目”，用于多项目并行时按项目粒度管理。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复多项目打开时项目树被后开项目覆盖：项目根节点改为按项目目录生成唯一 ID，打开/新建项目时按末尾追加（同项目则替换原根），刷新项目树仅替换当前项目对应根节点。
  - 范围：`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [优化] 项目树未保存标记支持分类联动：当“窗口/程序集/全局变量/常量表/自定义数据类型/DLL命令/资源”等分类下任一文件存在未保存修改时，分类节点也显示右侧未保存圆点。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复可视化设计器编辑后项目树未实时显示未保存标记：`handleFormChange` 增加 `onOpenTabsChange` 同步，确保 Sidebar 使用到最新 `.efw` 标签内存状态计算 dirty。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树未显示窗口/源码文件未保存标记：项目树 dirty 匹配键改为优先使用节点文件 ID（含扩展名），不再误用显示标签（无扩展名），使 `.efw/.eyc/...` 的“未保存圆点”能正确显示在右侧边框内侧。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复表格模式流程线在 `如果/如果真` 连续块中的误连通：`如果结束/如果真结束` 后紧跟 `如果/如果真` 时，不再套用“判断”专用的上下双横线桥接效果。
  - 范围：`src/renderer/src/components/Editor/eycFlow.ts`、`tests/contract/eyc-flow-contract.spec.mjs`
  - 验证：`node --test tests/contract/eyc-flow-contract.spec.mjs`
- [修复] 修复资源管理器双击子程序偶发跳错行：编辑导致行号漂移后，项目树双击子程序改为按子程序名实时定位（失败时回退旧行号），不再闪到新输入代码行。
  - 范围：`src/renderer/src/components/Sidebar/Sidebar.tsx`、`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复删除子程序后项目树残留旧节点：编辑器内容变更会实时同步 `.eyc` 子程序节点到项目树，未保存状态下删除 `.子程序` 也会立即从“程序集”树中消失。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/App.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复属性面板事件下拉勾选状态不刷新：事件列表“✓ 已存在子程序”改为优先读取当前打开标签页内存内容，并用最新解析结果覆盖缓存；删除子程序后勾选会即时取消。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Sidebar/Sidebar.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树双击子程序后落点行为：闪烁定位保持在子程序声明行不变，同时将输入光标置于该子程序首个普通代码行的行首（如子程序在第17行、首行代码在第18行则光标落到18行首）。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复项目树双击子程序后“仅闪烁无光标”问题：将光标进入时机调整为闪烁定位之后并增加重试聚焦，避免滚动重排冲掉编辑态；当子程序内仅空白普通行时也可在行首显示光标。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复双击子程序后输入框未渲染导致“仍看不到光标”：纠正 `startEditLine` 调用中的 `isVirtual` 参数，避免与普通代码行渲染条件不匹配而隐藏编辑输入框。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序内“空行优先”落点：当子程序声明后先有若干空行再有命令时，双击项目树后光标改为落在首条空普通行行首，不再跳到后续命令行。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复可视化设计器创建子程序后的“无行号空行”问题：新建子程序时直接生成真实空普通行；项目树跳转到无普通行子程序时会自动物化一条空普通行并聚焦行首，避免必须手动回车后才正常。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 统一“已存在子程序”导航逻辑：属性面板事件下拉选择与可视化设计器双击组件进入默认子程序时，行为改为与项目树双击一致（子程序声明行高亮闪烁 + 光标定位到子程序首个普通行首列，并在缺失普通行时自动物化空普通行）。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序跳转抖动与多点闪烁：去除 App 层对子程序导航的多重重试调用，并在编辑器侧为子程序导航增加“最后一次生效”的序列去重，改为单次闪烁 + 单次聚焦，避免上下抖动与多个位置依次高亮。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复“组件双击/属性面板事件下拉”跳转闪烁偏位：将 `navigateOrCreateSub` 的“已存在子程序”分支改为复用 `navigateToSubprogram` 同一内部实现，统一三入口（项目树/组件双击/事件下拉）的定位与闪烁规则，并补齐子程序名 trim 匹配。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序声明行闪烁偶发错位：统一跳转内部逻辑新增“优先行号命中同名子程序”策略（组件双击/事件下拉传入已知声明行），并在新闪烁前清理旧 `highlight-flash` 样式，避免残留闪烁造成错位错觉。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序跳转后“先在下方后瞬间居中”导致的闪烁位置偏差：声明行定位由 `center` 调整为 `nearest`，并移除光标聚焦阶段的额外滚动，避免二次跳动造成闪烁观感错位。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复子程序跳转时“滚动未稳定先闪烁”导致的错位：声明行高亮改为在两帧 + 短延时后触发（滚动收敛后再闪烁），并继续受导航序列去重保护，避免瞬态位置上闪烁。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 修复顶部菜单“插入 -> 子程序”后仅出现虚拟空行的问题：插入结构改为“真实空行 + 子程序声明 + 真实空普通行”，并在插入后将光标置于新子程序首条普通行行首，避免必须手动回车物化。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）
- [修复] 新增子程序“保底普通空行”删除规则：子程序首条普通行始终保留为可输入占位；删除命中该行时，若有内容则改为空行保留，若本身为空则改删其下方普通行；多选删除/剪切同样遵循该规则，避免全删后无法进入普通代码行。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`（改动文件无报错）


### v0.0.3-beta.53（2026-05-04）
- [新增] 打开项目对话框支持直接选择 `.e/.ec` 文件并导入：支持读取工程头、处理加密密码与同名目录冲突，导入后进入现有打开流程。
  - 范围：`src/main/index.ts`、`src/main/eproject/*`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/EProjectImportDialog/*`
  - 验证：`npm run test:contract:eproject`、`npm run build`
- [新增] 表格模式流程命令显示优化：`判断开始` 在表格模式显示为 `判断`，文本模式保持 `判断开始`。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

- [修复] 修复 `.e/.ec` 导入偏移与占位问题：修正工程段长度偏移导致的 `Magic` 报错，并修复仅生成“导入说明/空子程序占位”的问题，按程序段/资源段落地真实 `EYC/EGV/EDT/ELL/ERC` 内容。
  - 范围：`src/main/eproject/sections.ts`、`src/main/eproject/nativeProject.ts`、`src/main/eproject/methodCode.ts`、`src/main/eproject/ycProjectWriter.ts`、`tests/contract/eproject-import-contract.spec.mjs`
  - 验证：`npm run test:contract:eproject`、`npm run build`
- [修复] 修复核心命令与支持库命令映射偏差：补齐 `Lib0` 大规模回退映射及 `181/182/203` 等兼容覆盖，并接入多支持库命令 ID 映射，减少 `_LibXCmdY` 占位回退。
  - 范围：`src/main/eproject/lib0CommandMap.ts`、`src/main/eproject/libCommandMaps.ts`、`src/main/eproject/methodCode.ts`
  - 验证：`node --test tests/contract/eproject-import-contract.spec.mjs tests/contract/eyc-flow-contract.spec.mjs`、`npm run build`
- [修复] 修复表格模式流程线在特殊 `判断` 结构中的断裂与错位：补齐 `branch+start` 同行重叠语义、默认分支连通、上一可见行桥接、上半段按来源条件显示。
  - 范围：`src/renderer/src/components/Editor/eycFlow.ts`、`src/renderer/src/components/Editor/editorFlowRenderUtils.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`
- [修复] 修复“第 7 行下三角箭头残留”系列问题：在同深度及跨深度重叠场景统一屏蔽不应显示的下三角，并补齐 `end/branch/inner/startAtDepth` 全路径漏网点。
  - 范围：`src/renderer/src/components/Editor/editorFlowRenderUtils.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`
- [修复] 修复 `如果真` 无结束与嵌套场景的箭头异常：补回应显示的下箭头、移除额外横向虚线、并改为同深度锚定避免错层。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/editorFlowRenderUtils.tsx`
  - 验证：`get_errors`
- [修复] 修复流程补齐与可见性不一致：手工输入 `.如果` 回车可正确补齐可见 `否则/如果结束`，并修复文本模式可见但表格模式隐藏的问题。
  - 范围：`src/renderer/src/components/Editor/useCodeLineEditor.ts`、`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`node --test tests/contract/eyc-flow-contract.spec.mjs tests/contract/flow-line-theme.spec.mjs tests/contract/editor-helper-utils-contract.spec.mjs`、`npm run build`
- [修复] 修复流程命令缩进与特殊插入不一致：统一 commit/Enter 的 `.判断()` 缩进策略，补齐默认分支首行判定回退与特殊结构定位兜底。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/eycFlow.ts`
  - 验证：`get_errors`
- [修复] 修复表格编辑器偶发回退文本模式与空白闪烁：消除缓存初始化 TDZ、限制前置行预览触发条件，避免编辑中可视区被重置。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/editorBlocksModel.ts`
  - 验证：`get_errors`、`npm run build`
- [修复] 修复并清理 VS Code Problems 面板历史告警：修正 ARIA 与菜单语义、移除 inline style、替换不兼容 CSS 写法并补齐类型收窄。
  - 范围：`src/renderer/src/components/Sidebar/*`、`src/renderer/src/components/Editor/*`、`src/renderer/src/components/NewProjectDialog/*`、`src/shared/theme.ts`、`src/main/libraryManager.ts`、`tsconfig.node.json`
  - 验证：`get_errors`（全工作区无报错）

- [优化] 流程线机制正式收敛为命令驱动：去除旧 marker 兼容分支生效路径，自动补齐输出可见流程命令，打开编辑器时清理旧 marker 残留。
  - 范围：`src/renderer/src/components/Editor/eycFlow.ts`、`src/renderer/src/components/Editor/editorFlowAutoExpandUtils.ts`、`src/renderer/src/components/Editor/useCodeLineEditor.ts`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`node --test tests/contract/eyc-flow-contract.spec.mjs tests/contract/flow-line-theme.spec.mjs tests/contract/editor-helper-utils-contract.spec.mjs`
- [优化] 大文件表格模式性能系统性优化：引入可视区 block/行级裁切、缓存复用、滚动量化节流、minimap 阈值收紧、冻结策略收紧，显著降低主线程重绘压力。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.css`
  - 验证：`get_errors`、`npm run build`
- [优化] 将诊断与表格模型构建迁移至 worker：问题检查与 `buildBlocks + computeFlowLines` 异步化，主线程只接收结果渲染，缓解首开和滚动卡顿。
  - 范围：`src/renderer/src/components/Editor/editorDiagnostics*.ts`、`src/renderer/src/components/Editor/editorBlocksModel*.ts`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`、`npm run build`
- [优化] 补强“判断自动展开”诊断能力：增加结构化日志与 `judge-branch:check` 失败上下文，便于快速定位第 5/7 行结构未命中的根因。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`get_errors`

### v0.0.3.52（2026-04-28）
- [修复] 修复表格编辑器中输入 `.如果` 后通过鼠标失焦提交时未补全流程结构（200C/200D）的问题，避免误判导致下方流程缩进错位。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：`npm run build`（通过）
- [修复] 修复自绘标题栏中“文件”菜单左侧仍显示四方块占位图的问题，改为渲染 `ycidelogo.svg` 真实 logo。
  - 范围：`src/renderer/src/components/TitleBar/TitleBar.tsx`、`src/renderer/src/components/TitleBar/TitleBar.css`、`src/renderer/src/assets/icons/YcideLogo.svg`
  - 验证：`npm run build`（通过）
- [修复] 修复 Windows 启动后窗口左上角仍显示默认四方块图标的问题：补充应用图标多路径解析、设置 `AppUserModelId`，并在窗口创建后显式设置图标。
  - 范围：`src/main/index.ts`
  - 验证：`npm run build`（通过）
- [优化] 将程序图标统一切换为 `图标文件/svg-logo/ycidelogo.svg`，生成并接入打包图标资源（Windows/macOS/Linux）。
  - 范围：`resources/icon.ico`、`resources/icon.icns`、`resources/icon.png`、`src/main/index.ts`
  - 验证：`npm run build`（通过）

### 2026-04-24
- [优化] AI 助手内置 DeepSeek 模型切换为 DeepSeek-V4-Pro，兼容官方 V4 API 更新（base_url 不变，仅更新 model 参数）。
  - 范围：`src/main/ai-assistant.ts`
  - 验证：`get_errors` 检查无新增错误

- [新增] AI 助手新增内置模型选项：DeepSeek V4-Flash、DeepSeek V3.2，并复用 DeepSeek API Key 配置。
  - 范围：`src/shared/ai.ts`、`src/main/ai-assistant.ts`、`src/main/index.ts`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors` 检查无新增错误

- [新增] AI 助手新增 Plan 模式与 Agent 模式：Plan 可输出结构化执行计划；Agent 可基于目标自动决策并对当前激活文件执行修改。
  - 范围：`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors` 检查无新增错误

- [优化] Agent 模式升级为项目级执行：可遍历资源管理器项目文件，并按工具调用执行 `list_files/read_file/edit_file/open_file`，不再局限已打开标签页。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`
  - 验证：`get_errors` 检查无新增错误

- [新增] 输出面板实现可交互终端（输出查看、命令输入执行、最近命令展示），并打通 AI 助手 Agent 终端工具调用（`terminal_send`/`terminal_read`/`terminal_last_command`）。
  - 范围：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`
  - 验证：`get_errors` 检查无新增错误

- [优化] 终端交互布局调整为单框形态：将输入行嵌入同一终端容器底部，避免输入与输出分离成两个框。
  - 范围：`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`
  - 验证：`get_errors` 检查无新增错误

- [优化] 终端控制台进一步一体化：移除右侧“执行”按钮，仅保留回车执行，并将输入行并入同一滚动终端区域。
  - 范围：`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`
  - 验证：`get_errors` 检查无新增错误

- [优化] 终端改为按需启动：点击“终端”标签时启动，不再显示“未启动”状态文案；启动后自动输出当前终端工作目录。
  - 范围：`src/main/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`
  - 验证：`get_errors` 检查无新增错误


### 2026-04-23
  - 范围：`src/shared/settings.ts`、`src/renderer/src/components/SettingsDialog/SettingsDialog.tsx`、`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/Editor.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：类型检查无新增错误

## 2026-04-24（终端输出稳定性修复）

- 优化：修复 `cmd.exe` 在中文 Windows 下终端输出乱码问题，新增终端输出解码逻辑，优先 UTF-8，检测到替换符时自动回退 GBK（CP936）解码。
- 优化：终端系统日志（已启动/当前目录）改为去重写入，避免重复触发启动流程时出现连续重复提示。
- 涉及文件：`src/main/index.ts`

## 2026-04-24（终端快捷键与内联输入交互修复）

- 修复：终端支持 `Ctrl+C` 中断当前命令，主进程新增 `terminal:interrupt`，中断后自动重建 shell 会话。
- 优化：终端输入改为控制台内联输入，移除独立输入框；光标在控制台末尾闪烁，输入位置与终端最后一行保持一致。
- 优化：终端区域聚焦后可直接键盘输入，支持 `Enter` 执行、`Backspace` 删除和粘贴文本。
- 涉及文件：`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.tsx`、`src/renderer/src/components/OutputPanel/OutputPanel.css`

## 2026-04-24（Agent 工具结果流式回传修复）

- 修复：Agent 执行工具步骤时，工具日志改为实时回调到 AI 助手消息流，避免“执行完但窗口没有结果”的空白体验。
- 修复：`terminal_send` 工具在执行后自动抓取新增终端输出并回传到 Agent 对话，确保如 `node --version` 这类命令结果可直接显示在 AI 助手窗口。
- 优化：保留最终汇总日志兜底，避免极端情况下流式日志遗漏。
- 涉及文件：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`

## 2026-04-24（AI 助手交互与 Agent 意图路由优化）

- 新增：AI 助手输入区增加“停止”按钮，可立即中止当前会话的流式显示与后续结果写入（聊天 / 计划 / 编辑 / Agent）。
- 修复：AI 聊天记录区域恢复文本多选与复制能力。
- 优化：Agent 模式移除固定占位输出，减少每次执行都出现的重复提示行。
- 修复：当用户目标包含网络连通性语义（如“检查网络连接”）时，Agent 优先走终端 `ping` 诊断流程，而不是先扫描项目源码文件。
- 涉及文件：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`

## 2026-04-24（DeepSeek Agent Function Calling 升级）

- 新增：AI 通信层支持 `tools` / `tool_choice` / `tool_calls` 协议，兼容 DeepSeek 的函数调用 Agent 模式。
- 新增：主进程与预加载桥接 `ai:chatWithTools` 能力，渲染进程可直接发起工具调用会话。
- 优化：Agent 执行器从“JSON 计划解析”升级为“多轮 tool-calling 闭环”（assistant tool_calls -> 本地执行工具 -> tool 结果回传 -> 下一轮）。
- 优化：保留项目级文件与终端工具能力，并继续对网络连通性任务优先使用终端命令诊断。
- 涉及文件：`src/shared/ai.ts`、`src/main/ai-assistant.ts`、`src/main/index.ts`、`src/preload/index.ts`、`src/renderer/src/App.tsx`

## 2026-04-24（Agent 工具调用审计可视化）

- 新增：Agent 模式支持按工具调用分组展示审计条目，每次 tool call 可折叠查看“参数/返回”。
- 优化：工具调用参数与返回值支持自动截断，避免超长内容淹没聊天流。
- 优化：保留原有日志流并叠加结构化工具审计，便于快速定位调用链和结果。
- 涉及文件：`src/renderer/src/App.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.tsx`、`src/renderer/src/components/AIAssistantPanel/AIAssistantPanel.css`

## 2026-04-24（dev 运行语法错误修复）

- 修复：`App.tsx` 中 Agent 工具循环闭合层级异常，导致 `npm run dev` 时 Vite/Babel 抛出 `Unexpected token`，界面出现大段错误堆栈文本。
- 验证：重新执行 `npm run dev`，主进程 / preload / renderer 均可正常启动，未再出现该语法错误。
- 涉及文件：`src/renderer/src/App.tsx`

- [新增] 系统设置中的布尔项改为左右滑动开关（关闭灰色、开启蓝色）。
  - 范围：`src/renderer/src/components/SettingsDialog/SettingsDialog.tsx`、`src/renderer/src/components/SettingsDialog/SettingsDialog.css`
  - 验证：类型检查无新增错误

- [优化] 代码预览区改为内容随滚动联动，不再仅移动视窗框。
  - 范围：`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：类型检查无新增错误

- [修复] `.eyc` 标签名回归问题，恢复优先显示 `.程序集` 名称（不足时回退去后缀文件名）。
  - 范围：`src/renderer/src/components/Editor/Editor.tsx`
  - 验证：类型检查无新增错误

- [修复] 项目树双击子程序跨文档跳转不到位问题，优化为一次跳转到位。
  - 范围：`src/renderer/src/App.tsx`、`src/renderer/src/components/Editor/EycTableEditor.tsx`
  - 验证：类型检查无新增错误

（发版后将对应条目从“待发版条目”移动到这里，并标注版本号）

