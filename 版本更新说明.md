# ycIDE 版本更新说明

## ycIDE - 开发测试版 v0.0.5-beta.21(2026-07-19)

---

### 问题修复

- [修复] **用「载入」打开的窗口视觉补齐**：载入的窗口现在支持 **窗口背景图**、按钮的 **底色 / 文本色**、**图形按钮** 的四态图片显示——此前这些只有启动窗口才有，载入的窗口会丢背景图、按钮变灰白、图形按钮不显示。（图形按钮的鼠标悬停点燃 / 点击切换选中仍未接）
- [修复] **透明标签的文字重叠**：透明标签改标题、改字号、调用「刷新显示」时，旧文字会被正确擦掉，不再新旧叠成两层——文字放大动画也不会一帧帧叠成一团。
- [修复] **`选择` 命令返回文本显示成一串数字**：`标签.标题 ＝ 选择 (条件, “无解”, “共 ” ＋ 到文本 (数量) ＋ “ 种解”)` 现在正确显示文本，不再显示成整数（如 2820598）。
- [修复] **设计器：铺满窗口的控件挡住画新控件**：把图片框等控件拉满整个窗口后，从工具箱选中组件仍可**直接在它上面拖画**新控件，鼠标也正确显示为十字——不用再先把它挪开。
- [修复] **设计器：标签「是否自动折行」所见即所得**：设计器按该属性实时渲染折行效果，不再出现「设计时多行、运行后只有一行」的不一致（运行时行为一直是对的，是设计器没跟着属性渲染）。

## ycIDE - 开发测试版 v0.0.5-beta.20(2026-07-19)

---

### 优化

- [优化] **中文 / emoji 文本命令现在按「字符」正确处理**：文本操作类命令不再按字节数中文，长度、切分、查找、替换全部按字符：
  - `取文本长度 (“中”)` 得 **1**（此前得 3）
  - `取文本左边 (“中文”, 1)` 得 **“中”**（此前会切出半个字的乱码）
  - `字符 (20013)` 得 **“中”**、`字符 (128512)` 得 **“😀”**——现可取任意 Unicode 字符（不再限 0~255）
  - `取代码 (“中”)` 得 **20013**
  - `寻找文本` / `倒找文本` 返回按字符计的位置，`文本替换` / `子文本替换` 的位置与长度也按字符
  - 涉及命令：取文本长度、取文本左/右/中、字符、取代码、寻找文本、倒找文本、文本替换、子文本替换

## ycIDE - 开发测试版 v0.0.5-beta.19(2026-07-19)

---

### 问题修复

- [修复] **文本命令用 `＋` 拼接不再报错**：如 `字符 (214) ＋ 字符 (208)`、`取文本左边 (…) ＋ 取文本中间 (…)` 这类「返回文本的命令」直接用 `＋` 连接（或做文本比较）此前会报「运算符两边类型不能这样运算」，现已修复——所有返回文本型的命令都能正常参与 `＋` 连接与比较。
- [修复] **取命令行命令可用**：`取命令行 (命令行数组)` 现能正确把程序启动时的命令行参数逐段填入文本数组变量（此前编译报错）。
- [修复] **流程命令括号内改参数不再打乱结构**：像 `.计次循环首 (10, i)` 这样不展开参数行、直接在括号里修改参数时，不会再把循环体和 `.计次循环尾` 挪到流程外面。

## ycIDE - 开发测试版 v0.0.5-beta.18(2026-07-19)

---

### 新增功能

- [新增] **编译报错双行号显示 + 点击跳转**：编译报错现在同时显示「表格行(文本行)」（如 `选题窗口.eyc:94(103)`）；编译出错会自动跳到第一个报错位置，每条报错都能点击、直接定位到对应代码行（未打开的文件先显示文本行号，打开后自动换成表格行号）。
- [新增] **多项选择命令**：`多项选择 (索引, 候选1, 候选2, …)` 按索引从候选列表返回一个值（对齐易语言，最后一个参数可重复添加）。
- [新增] **列表框「列表项目」可在代码中读写**：如 `列表框.列表项目 ＝ { }` 清空列表；也可先取出保存、再赋回恢复（列表框 / 组合框 / 选择列表框通用）。
- [新增] **可视组件通用方法**：所有控件现支持 `移动`、`获取焦点`、`可有焦点`、`取用户区宽度/高度`、`重画`、`部分重画`、`刷新显示`、`发送信息 / 投递信息`、`调整层次`、`激活`、`取窗口句柄` 等窗口公共方法（`移动` 省略的参数保持当前值）。
- [新增] **字体大小可在代码中读写**：`控件.字体.字体大小 ＝ 值` / 读取 `控件.字体.字体大小`。
- [新增] **补齐核心库常量**：新增 150 个内置常量，含全部键代码（`#回车键`、`#Esc键`、`#空格键`、`#退格键`、光标键、F1-F16、A-Z、0-9 键、Shift/Ctrl/Alt 键等）。

### 问题修复

- [修复] **自定义常量数值被误判为文本**：`.常量 加, "1"` 这类数值常量参与整数比较（`方法 ＝ #加`）不再报编译错。
- [修复] **注释里的特殊符号导致编译失败**：注释内容含 `*/`、`\` 等符号（如 `用+-*/ \(…)`）不再撑破生成代码。

## ycIDE - 开发测试版 v0.0.5-beta.17(2026-07-18)

---

### 新增功能

- [新增] **脚本语言支持组件（对齐易语言官方 script.fne）**：新增「脚本组件」非可视功能组件，基于系统 IActiveScript 引擎运行 JScript / VBScript 脚本，无需易语言运行时或外部 DLL。从工具箱拖入窗口后不可见，运行期通过方法/属性驱动。
  - 方法：`脚本组件1.执行 (脚本代码)`（执行代码并定义过程/函数，返回真/假，出错详情见「错误信息」；代码可省略则复用上次）、`脚本组件1.计算表达式 (表达式)`（求值并返回结果文本）、`脚本组件1.运行 (过程或函数名, 参数…)`（调用已定义的过程/函数，可追加多个参数，返回结果文本）、`脚本组件1.清除 ()`（清除已定义代码并重置引擎）。
  - 属性：`语言`（JScript / VBScript，切换后自动重建引擎）、`错误信息`（只读，含出错行列与描述）、`超时`（毫秒，脚本执行超时后可弹框选择中止；配合常量「无超时」=-1 表示不限制）。
  - 自由命令：`执行脚本表达式 (表达式 [, 语言])`——无需在窗口放置组件即可即时求值，语言默认 JScript。
  - 常量：`无超时`(-1)。

- [优化] **可视化设计器工具箱改为两级分类**：工具箱顶部分类下拉现按「核心组件 / 第三方组件」分组，每类下再按型细分「Win32控件 / GDI控件 / WebView2控件 / 功能组件」等子类。核心支持库(krnln)的组件归「核心组件」（标准控件→Win32控件、画板/外形框→GDI控件、时钟/通用对话框/菜单等非可视→功能组件）；其余支持库一律归「第三方组件」，并按各自在窗口组件定义中声明的「组件箱分类」自动落入对应子类（如 WebView2 库→WebView2控件、脚本组件→功能组件）。第三方支持库只需声明自己的型即可自动归类。

- [新增] **图形按钮控件（对齐易语言 PicBtn，四态图片按钮）**：新增「图形按钮」控件，以图片表现按钮，支持正常 / 点燃（鼠标悬停）/ 按下 / 禁止四态图片与透明颜色；「类型」可选按钮（单击触发）或选择框（单击切换选中，选中态显示按下图片）；「被单击」为默认事件，并与图片框共用 7 个鼠标事件（左/右键按下·放开、双击、位置移动、滚轮）。现正确归入「核心组件 / Win32控件」，可从工具箱直接拖出使用。

---

## ycIDE - 开发测试版 v0.0.5-beta.16(2026-07-17)

---

### 新增功能

- [新增] **多维数组完整支持（对齐易语言）**：`重定义数组 (数组, 假, 3, 4)` 的「数组对应维的上限值」现在可按维重复提供（二维、三维……），成员总数=各维乘积；`数组 [行] [列]`、`数组 [页] [行] [列]` 链式下标读写全部打通（静态声明维度如 `"3,4"` 与运行期 重定义数组 动态定维——包括维数是变量的情况——均可）；`取数组下标 (数组, 维)` 按维返回该维成员数；`复制数组` 会连维度一起复制。

### 修复问题

- [修复] **数组命令三处与易语言不齐的语义**：`重定义数组 (a, 假, 5)` 此前会分配 6 个成员（现为 5）；`取数组下标` 此前返回成员数-1（现返回该维成员数，一维=成员总数）；`插入成员`/`删除成员` 的「位置」此前按 0 基处理导致插错位/删错位（现为一基，`插入成员 (a, 3, 30)` 正确插成第 3 个成员）。

- [修复] **逻辑型变量使用 赋值/连续赋值/交换变量/强制交换变量 会崩溃**：`连续赋值 (真, 逻辑1, 逻辑2)` 一执行程序即退出。根因是类型标签表缺逻辑型专属标签、按 4 字节整数读写 1 字节逻辑型变量。

- [修复] **字符串里的「真/假/且/或」被转译改写**：字符串字面量中出现这些字（如显示文本 `“重定义数组 (a, 假, 5)”`）会被当成逻辑值/运算符替换成 `0/1/&&/||`，现在只在字符串外替换、字面量原样保留。

- [修复] **表格编辑器两个退格问题**：① 命令行行首按退格此前无反应，现在会删除上方空行并将当前行上移（连按逐个删除，遇声明行安全停止）；② 空行紧邻 `如果真` 时在空行上按退格会误删下方 `如果真` 头、导致 `如果真结束` 孤儿显露，现在正确删除空行自身、结构保持完好。

- [修复] **判断结构默认区里嵌套结构内键入「判断」被误提升为外层分支**：在 默认 区刚展开的 如果真 体内输入 判断 后回车/失焦，会被错误上提为外层判断的 case 分支（插到 默认 之前、如果真被挤下移、结构重排破坏）。现在只有当判断结构是当前行的最内层包裹时才作为分支，否则正常新建嵌套判断结构。


项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：<https://github.com/chungbinb/ycIDE>
html版本：<https://github.com/chungbinb/ycIDE-electron>
QQ群：767523155（ycIDE易承语言交流群）

---

## ycIDE - 开发测试版 v0.0.5-beta.15(2026-07-17)

---

### 新增功能

- [新增] **图片框完整鼠标事件（对齐易语言）**：新增 鼠标左键被按下、鼠标左键被放开、被双击、鼠标右键被按下、鼠标右键被放开、鼠标位置被移动（均带 横向位置/纵向位置/功能键状态 参数）与 滚轮被滚动（带 滚动距离/功能键状态 参数）共 7 个事件。功能键状态与易语言一致：Ctrl 键为 1、Shift 键为 2、Alt 键为 4，可组合；事件返回值为 逻辑型，返回真可拦截系统对该消息的缺省处理（例如在 鼠标左键被按下 中返回真，后续缺省处理不再发生）。双击设计器中的图片框，默认生成 鼠标左键被按下 事件。
  **注意**：图片框原来的「被单击」事件已移除（易语言图片框本无此事件）。旧项目里写过 `_图片框N_被单击` 的子程序不会报错，但不会再被触发，请改用 鼠标左键被按下。

- [新增] **事件处理子程序自动带返回类型**：双击控件或在属性面板事件栏选择事件时，生成的 .子程序 存根除自动带参数外，现在还会自动填写返回类型（如图片框鼠标事件的 逻辑型）。

### 其他

- [文档] README 贡献人员名单新增 3 位；许可证说明醒目化：顶部加 MIT 徽章与摘要，明确可自由使用、修改、分发（含商用），但衍生作品须保留版权与许可声明并注明基于 ycIDE。

项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：<https://github.com/chungbinb/ycIDE>
html版本：<https://github.com/chungbinb/ycIDE-electron>
QQ群：767523155（ycIDE易承语言交流群）

---

## ycIDE - 开发测试版 v0.0.5-beta.14(2026-07-17)

---

### 新增功能

- [新增] **取变量地址 / 取变量数据地址 可以用了**：以前调用这两个命令会编译失败（链接错误）。现在完整实现——`取变量数据地址` 对文本/字节集/数组返回数据缓冲区地址（长度为 0 返回 0），其它类型与 `取变量地址` 相同。
  **注意 64 位环境地址请用长整数型变量接收**：写成整数型会在问题面板直接报错并拒绝编译（地址会被截断成无效值）；`指针到整数`、`指针到字节集` 等命令读到无效地址时现在返回 0/空字节集，不会再让程序崩溃。

- [新增] **AI 面板显示编辑进行中状态**：AI 修改文件时实时显示「正在编辑 xx…」（以前只有完成后才出现 +N/-N 摘要）。

- [新增] **AI 面板自动滚动**：AI 输出内容超过可视区后自动跟随到最新内容；往上翻阅历史时不会被打扰，滚回底部后恢复跟随。

### 修复问题

- [修复] **到文本(字节集) 显示成 `字节集:41{232,191,153,…}` 数字串**：现在按文本解出原文（与 `到字节集` 互为往返，读入文件 的内容能直接转成文本了）。`调试输出` 仍显示字节列表形态。

- [修复] **到文本(日期时间型) 显示成 `46220.4139…` 数字**：现在显示「2026年7月17日9时56分37秒」，时刻为零只显示日期（与易语言一致）。`取文件时间` 的结果可以直接拼进文本了。

- [修复] **寻找文件 只返回一次就没了**：以前续查（首参数留空）永远返回空、还会把 `.` 目录项混出来。现在可以循环续查遍历目录，并按属性过滤：0=无任何特殊属性的普通文件、省略=除子目录外全部文件、含 16 时目录（连同 `.` 和 `..`）一起返回。

- [修复] **到字节集(数值) 给的是数字文本的字节**：`到字节集 (到整数 (123))` 以前得到 "123" 三个字符的字节，现在按易语言给二进制原始字节（整数 4 字节 / 短整数 2 / 字节 1 / 长整数 8 / 小数 4 / 双精度 8）。文本仍按 UTF-8。

- [修复] **取字节集数据 的起始位置**：照帮助从 1 开始计（省略默认为 1）；读 8 字节长整数不再被错误截断成 32 位。
  提示：`取字节集内整数`/`置字节集内整数` 的第二个参数是「偏移」，从 0 开始——两族命令基准不同，以各自帮助为准。

- [修复] **到文本(小数型) 带一串噪声位**：3.14159 以前显示成 3.14159011840820，现在按小数型精度显示 3.14159。

- [修复] **AI 修改代码后，表格行 diff 底色缺两列**：数据类型列和备注列现在与整行底色连续。

---

## ycIDE - 开发测试版 v0.0.5-beta.13(2026-07-16)

---

### 新增功能

- [新增] **通用对话框组件可以在代码里用了**：以前调用会报「方法“打开”暂不支持在代码中调用」。现在按帮助文件语义完整实现：

  ```
  .如果真 (打开文件通用对话框.打开 ())
      文件号 ＝ 打开文件 (打开文件通用对话框.文件名, , )
      显示文件编辑框.内容 ＝ 读入文本 (文件号, )
      关闭文件 (文件号)
  .如果真结束
  ```

  - 「类型」属性支持全部四种：打开文件、保存文件、字体选择、打开帮助；
  - 标题/文件名/过滤器/初始目录/默认文件后缀/文件必须存在/文件覆盖提示 等 22 个属性可在属性面板设置、也可在代码里读写；
  - 过滤器照易语言格式：`文本文件（*.txt）|*.txt|所有文件（*.*）|*.*`；
  - 用户点确定后「文件名」「初始过滤器」（字体选择则是字体各属性）自动回填，取消返回假。

### 修复问题

- [修复] **打开文件/读入文件/写到文件 打不开中文路径**：文件名或目录带中文（如桌面上的 `订阅链接狗狗云.txt`）时 `打开文件` 返回 0。现在中文路径完全正常。

- [修复] **读入文本 读「ANSI」编码的 txt 显示乱码**：现在自动识别编码——记事本常见的三种（ANSI/GBK、带 BOM 的 UTF-8、Unicode/UTF-16）都能正确读出，BOM 不会残留在文本开头。

- [修复] **不带点的 `结束 ()`、`返回 ()`、`跳出循环 ()`、`到循环尾 ()` 没有任何效果**：以前这些命令写成无点形态会被静默忽略（程序不退出、循环跳不出）。现在两种写法完全等效；「如果/循环」这类块结构关键字写成无点形态会给出明确的中文报错而不是静默失效。

---

## ycIDE - 开发测试版 v0.0.5-beta.12(2026-07-16)

---

### 修复问题

- [修复] **代码行里先敲空格再敲流程命令，流程线和文字对不齐**：比如先打一串空格再输入「如果」回车，生成的结构会带着这串空格落盘，流程线画在左边、文字却跑到很右边，还会多出一行看不见的空格行。现在行首的空格一律无效（空格只在文本字面量里有意义，比如 `文本型变量 ＝ “这种才允许      空格”` 不受影响）。

- [修复] **嵌套结构里的注释「失焦往右漂、点一下又弹回来」**：多层循环/判断里写的注释，编辑时位置正常，一失焦就往右挪一截。现在显示位置和编辑位置永远一致——都紧跟流程线。以前文件里已经错位的行（包括上面那种带空格的结构）不用改，打开就直接显示正常了。

- [修复] **焦点在表格单元格里粘贴多行代码，整段代码全挤进单元格**：比如正在编辑变量名时按 Ctrl+V 粘贴一段代码，会变成 `.局部变量 i信息框 （1） 信息框 （2）…` 这种。现在多行内容自动作为代码行粘贴到子程序里（和在代码行里粘贴一样），单行内容仍然正常粘进单元格。

- [修复] **表格行鼠标经过时只有「类型」「备注」列有背景色**：现在整行所有列（变量名/静态/数组等）都有一致的 hover 背景。

- [修复] **双击问题面板定位到表格行时，闪烁高亮各列深浅不一**（类型/备注列偏深、其它列偏浅）：现在整行闪烁颜色统一。

### 新增功能

- [新增] **问题面板双行号**：以前只显示文本模式的行号（和表格左侧的行号对不上）。现在同时显示两个：「第 13 行, 第 1 列（文本 第 17 行）」——前面是表格模式行号（就是编辑器行号槽里看到的数字），括号里是文本模式行号。

---

## ycIDE - 开发测试版 v0.0.5-beta.11(2026-07-16)

---

### 修复问题

- [修复] **新建的编辑框运行后是黑底**：设计器里明明是白底，一运行就变黑，而且你根本没设过颜色。原因跟颜色属性无关 —— 是内部颜色表的一个占位项被错误匹配上了，导致所有没设过颜色的控件都拿到「全黑」。
  - 触发条件很刁，所以之前一直没被发现：**只要窗口上有一个标签，编辑框就是白的**；只有「按钮 + 编辑框」这种没有任何控件设过颜色的窗口才会全黑。
  - 同样受影响的还有列表框。

- [修复] **`≈`（近似等于）没法用**：写 `甲 ≈ 乙` 会编译失败，报一串看不懂的 `unexpected character <U+2248>`。现在 `≈` 和它的同义写法 `?=` 都能用了：

  ```
  局_结果 ＝ “Hello World” ≈ “Hello”     ' 真
  局_结果 ＝ “中华人民” ≈ “中华”          ' 真
  ```

  注意它的语义是**「后者是不是前者的开头」**（帮助原文：当比较文本在被比较文本的首部被包容时返回真），**区分大小写**，也不会把全角半角当成一样 —— `“Hello” ≈ “hello”` 是**假**。这一点与易语言实测一致。

- [修复] **`到文本 (逻辑型)` 印出「1」/「0」，而不是「真」/「假」**：

  ```
  局_逻辑 ＝ 真
  调试输出 (到文本 (局_逻辑))       ' 以前印 1，现在印 真
  调试输出 (到文本 (10 ＝ 5))       ' 以前印 0，现在印 假
  ```

  整数型仍然印数字，不受影响。

- [修复] **文本比较的 `＜` `＞` `≤` `≥` 结果是乱的**：这个比较严重 —— 以前比的根本不是文本内容，而是变量在内存里的地址，所以结果既不是字典序也不是数值大小，而是**碰运气**。最直观的例子是 `“10” ≤ “10”` 得「假」、同时 `“10” ≥ “10”` 得「真」，自相矛盾。现在统一按**字典序**比较（与易语言一致）：

  ```
  局_结果 ＝ “5” ＜ “10”      ' 假 —— 文本按字典序，“5” 排在 “10” 后面（首字符 5 > 1）
  局_结果 ＝ “10” ＜ “5”      ' 真
  ```

  顺带修好了「同一个比较，写在命令参数里结果就不对」的问题 —— `到文本 (甲 ＝ 乙)` 以前和 `局_结果 ＝ 甲 ＝ 乙` 会给出不同答案，现在一致了。

- [修复] **控制台程序里用文本比较会链接失败**（`未定义符号: yc_text_compare`）。窗口程序不受影响。

---

## ycIDE - 开发测试版 v0.0.5-beta.10(2026-07-16)

---

### 修复问题

- [修复] **`÷` 相除算出来没有小数**：`局_整数1 ÷ 局_整数2`（20 ÷ 7）此前得到 **2**，小数部分整个丢了。原因是 `÷`（相除）和 `＼`（整除）被当成了同一种除法。易语言里这两个不一样：

  ```
  局_小数 ＝ 20 ÷ 7      ' 相除：恒得小数 → 2.857142857142857
  局_整数 ＝ 20 ＼ 7      ' 整除：只要整数商 → 2
  ```

  现在 `÷` 恒返回双精度小数，`＼` 与 `％`（求余数）行为不变。左边已经是小数的写法（如 `30 × #pi ÷ 180`）此前碰巧是对的，所以这个问题只在**两边都是整数**时才现形。

- [修复] **你打印的 `＋`、`＝`、`÷` 会变样**：引号里的全角运算符被当成运算符改写了 —— 写 `“20 ＋ 7 ＝ 27”`，打印出来是 `20 + 7 == 27`。现在引号内的文本一律原样输出。

- [修复] **`四舍五入` 的第二个参数（被舍入的位置）没生效**：`四舍五入 (3.14159, 2)` 此前恒返回 **3** 而不是 3.14 —— 不管你填几，都等于舍入到整数。现在三档语义都按帮助工作：

  ```
  四舍五入 (1056.65, 1)    ' → 1056.7   保留小数点右边 1 位
  四舍五入 (1056.65, 0)    ' → 1057     舍入到整数（省略该参数时的默认）
  四舍五入 (1056.65, -1)   ' → 1060     舍入到小数点左边 1 位
  ```

- [修复] **整个用括号括起来的表达式算错甚至编译不过**：`(“甲” ＋ “乙”)` 这类写法此前**根本编译不过**，`(20 ＋ 7) × 2` 这类也走了错误的路径。现在括号内的表达式会被正常翻译。

- [修复] **把命令的返回值直接当条件会编译失败**：`.如果 (是否运算正确 (局_小数))` 这类写法此前会报一串难懂的 C++ 错误（`lambda … is not contextually convertible to 'bool'`）。涉及 `.如果`、`.如果真`、`.判断开始`、`.判断循环首`、`.循环判断尾` 等全部流程语句。

### 新增功能

- [新增] **编译报错终于说人话了**：此前编译出错，给你看的是英文 C++ 报错 + 一个指向 `temp/*.cpp`（你从没写过的中间文件）的行号。现在会告诉你**是哪个 .eyc 文件的第几行**、把那一行原样回显出来、配一句中文说明，并且区分「是你写错了」还是「是 ycIDE 生成代码的问题」—— 后者会明说「不是你的代码写错了，请反馈」。认不出来的报错会保留英文原文，不会不懂装懂。

### 已知差异（不打算改）

- 涉及 `#pi` 的三角函数结果与易语言有约 1e-11 的差别（`求正弦 (30 × #pi ÷ 180)` 我们得 `0.5`，易语言得 `0.499999999987`）。这是**易语言的 `#pi` 被截断到了 10 位小数**（`3.1415926535`），而 ycIDE 用的是完整双精度 π（`3.141592653589793`）。sin(30°) 数学上就是 0.5，我们的结果是对的，故不跟随。

---

## ycIDE - 开发测试版 v0.0.5-beta.9(2026-07-15)

---

### 新增功能

- [新增] **`分割文本` 能用了，返回数组的命令有救了**：此前 `分割文本` 的定义整条都是别的命令的(挂着"数据源.分解"的行号/列号参数、返回值标成"无返回值")，实现也只是个永远返回空数组的占位桩 —— 写了等于没写。现在补上了**数组返回机制**，`分割文本` 是第一个真正跑起来的：
  ```
  .局部变量 结果, 文本型, , "0"
  结果 ＝ 分割文本 (“甲,乙,丙”, “,”)     ' 结果[1]=甲  结果[2]=乙  结果[3]=丙
  复制数组 (结果, 分割文本 (“x,y”, “,”))  ' 帮助里的规范用法也支持
  ```
  - 语义逐条照帮助文件做：分隔符**省略**→默认半角逗号；分隔符给**空文本**→整段不分割；待分割文本为空→空数组；给了"要返回的子文本数目 n"→最多 n 段、末段是余下全文。
  - 同批修好的还有 **`取配置节名`**(读 ini 的所有节名)与 **`多文件对话框`**(一次选多个文件，返回文件全路径数组) —— 这两条此前也是桩，分别返回写死的 `"default"` 和空文本。
  - **`取选中项` 改名为 `取所有被选择项目`**：这是它在易语言里的真名(此前的名字是编的)。真实现其实一直都在，只是名字对不上、提示面板找不到它。

- [新增] **"拼音处理"整类 5 条命令全部能用了**：`取所有发音`、`取发音数目`、`取拼音`、`取声母`、`取韵母` —— 此前**全是占位桩**，`取发音数目` 不管什么字都返回 1，`取拼音`/`取声母`/`取韵母` 分别返回写死的 `"PY"`/`"SM"`/`"YM"`。现在内置了 **6763 个国标(GB2312)汉字的拼音表**，多音字 699 个：
  ```
  取所有发音 (“行”)     ' → {“xing”, “hang”, “heng”}   常用音排在第一个
  取发音数目 (“长”)     ' → 2
  取拼音 (“长”, 2)      ' → “zhang”
  取声母 (“重”, 1)      ' → “zh”      取韵母 (“重”, 1) → “ong”
  ```
  - 照帮助文件的语义：只取文本首部的第一个汉字；不是国标汉字就返回空数组/0/空文本；索引一基。

### 问题修复

- [修复] **多音字命令搜不到**（用户反馈：`#换行符` 只能用 `#hxf` 搜到，按正确读法敲 `#hhf` 反而搜不到）。原因是补全匹配对每个汉字只认**一个"常用音"**——"行"的常用音是 xíng，可 `换行符` 里它读 **háng**。现在**多音字的每种读法都能搜到**（`hxf` 和 `hhf` 都行，全拼 `huanxing`/`huanhang` 也都行）。
  - 全量扫描了 925 个命令/常量/控件成员名，**共 18 个受影响**，现已一并修好：
    - **行**(xíng/háng)：`#换行符`、`插入文本行`、`插入行`、`读入一行`、`加入行`、`取行高`、`写文本行`、`置行高`、`是否允许多行`、`允许多行表头`
    - **重**(zhòng/chóng)：`#重试钮`、`#重试取消钮`、`#放弃重试忽略钮`、`取重复文本`、`取重复字节集`、`重新查询`
    - **奇**(qí/jī)：`#仅打印奇数页`　·　**调**(tiáo/diào)：`DLL命令调用转向`

- [修复] **写 `对象.移动(…)` 这类命令以前能编过、跑起来却是错的**：提示列表里有一批名字以"对象."开头的命令（`对象.移动`、`对象.获取焦点`、`对象.发送信息` 等 24 条）。这个"对象."在易语言的帮助里只是个**占位符**（代表"任意对象"），并不是真能写出来的命令名——但它们混进了命令列表，补全能补出来，写出来还能编过，然后调用时少传一个参数（对象本身），运行结果是错的。现在写这种形式会**直接报错并告诉你正确写法**：
  ```
  对象.移动 (1, 2, 3, 4)
  → “对象.移动”是对象成员命令，不能这样直接调用；请写成「控件名.移动(…)」的形式
  ```
  - 注：`按钮1.移动(…)` 这种正确写法目前仍会提示"暂不支持在代码中调用"——这些控件方法还没接上，是**另一件待做的功能**，本次只是先把上面那条会出错的歪路堵死。

- [修复] **【重要】"变量操作"族 5 条命令以前全是错的**：`赋值`、`连续赋值`、`交换变量`、`强制交换变量`、`数组清零` —— 这些命令要**按引用改写你的变量**，但支持库内部收到的其实是"变量值的文字形式"，连变量地址都不是；实现里则把收到的指针当数据直接写进去。两头都错，结果就是这 5 条要么没反应、要么写坏数据。现已修好——命令实现仍在支持库里，只是编译器改为把"变量地址 + 类型标记"一起交过去(类型编译期本来就知道，而光给一个地址支持库无从判断该按整数还是文本去赋值)。5 条都真的生效了：
  ```
  赋值 (计数, 7)                  ' 计数=7
  连续赋值 (5, a, b, c)           ' a=b=c=5  (注意这条的参数顺序：值在前)
  交换变量 (a, b)                 ' 整数/文本都能换
  数组清零 (数组)                 ' 成员全置零，成员数不变
  ```
  - 顺带修好 **`赋值` 的命令定义也是张冠李戴的**(提示面板显示的是 `〈逻辑型〉(欲写入数据, 成员索引)`，实为 `〈无返回值〉(被赋值的变量, 值)`)，`连续赋值` 的尾参现在也能重复添加了。
  - 传字面量给这些命令(如 `交换变量 (1, 2)`)现在会明确报"需要变量作为参数"，而不是编译时抛一串 C++ 报错。

- [修复] **`取统一文本长度` 的"转换到宽文本"开关以前完全不认**(和 `取统一文本` 同一个毛病)；`字符`/`到短整数`/`指针到文本` 的参数与返回宽度也已与帮助对齐。

- [修复] **【重要】往文本数组里存值以前根本编译不过**：`加入成员 (文本数组, "甲")` 和 `文本数组 [1] ＝ "甲"` 这两种最常见的写法都会编译报错(`no matching conversion from 'YC_TEXT'`)——也就是说文本数组建好了却存不进东西。现已修好。

- [新增] **"字节集数组"能用了**：可以 `.局部变量 数据, 字节集, , "0"` 声明，`加入成员`/`插入成员`/下标读写/`取数组成员数`/`复制数组`/`调试输出` 都支持。同时 **`分割字节集`** 也真正实现了(此前是永远返回空数组的桩)。

- [修复] **【重要】字节集以前装不了二进制数据 —— 遇到 0 字节就整条截断**。支持库内部一直把字节集当"以 0 结尾的字符串"在传，可字节集本来就是任意二进制。后果是一大批命令在真实数据上直接是错的：
  | 你写的 | 以前的结果 | 现在 |
  |---|---|---|
  | `取字节集长度 (取空白字节集 (5))` | **0** | 5 |
  | `取字节集长度 (文本到UTF16 (“A”))` | **1** | 4 |
  | `写到文件` 一段含 0 字节的数据再 `读入文件` | **只剩第一个 0 之前的部分** | 原样 |
  | `置字节集内整数 (字节集, 0, 值)` 后读回 | **恒为 0**(改动被丢弃) | 真的写进去了 |
  | `到字节集 (“A”) ＋ 到字节集 (“B”)` | **编译报错** | 正常连接 |
  - 现已把字节集改成按长度传递，**读写二进制文件、UTF16 编码转换、图片/网络数据这些场景才真正可用**。涉及 34 条命令(取字节集长度/左边/右边/中间、寻找/倒找、替换、取空白/取重复、指针到字节集、取/置字节集内整数、读入/写到文件、读入/写出/插入字节集、文本到UTF8/UTF16 与反向、取统一文本、分割字节集 等)。
  - 顺带修好的：`文本到UTF8` 以前没按帮助加"结束零字符"；`取统一文本` 的"转换到宽文本"/"添加结束零字符"两个开关以前完全不认(帮助说省略时默认都为真，实际按假处理)；`取字节集数据` 的类型常量以前整个错位(帮助是 1=字节型/2=短整数型/3=整数型/4=长整数型…，实现却按 1=整数/2=小数/3=双精度)；`置字节集内整数` 以前写越界不做任何检查(缓冲区溢出)。

- [修复] **调用还没实现的数组命令，现在会直接告诉你"尚未实现"**，不再静默返回垃圾值(而不是像以前那样看着能编过、跑起来拿到一个乱数)。数组元素类型现支持 **整数族 / 小数族 / 文本型 / 字节集** 四类，其它类型会明确报出来。

- [修复] **另有 3 个命令的定义也张冠李戴**（同一个原因，提示面板显示的参数/返回值全是别的命令的）→ 已按帮助文档改回：
  - **画板.取图片**：此前显示"无参数"→ 实为 `〈字节集〉(［输出宽度］,［输出高度］)`
  - **选择列表框.选中项目**：此前是 `〈整数型〉(欲选择的项目文本)` → 实为 `〈逻辑型〉(项目索引,［选择状态］)`
  - **打印机.置自定义纸张类型**：此前是 `〈文本型数组〉(纸张定义)` → 实为 `〈逻辑型〉(名称,宽,高,［上/下/左/右边距］)`
- [修复] **`打开文件` 命令的定义完全是错的**（重要）：命令提示里显示的是「ODBC数据源连接文本 / 是否只读 / 不显示ODBC连接对话框」、返回逻辑型、归类到「控制台操作」—— 那其实是「外部数据库.打开」的说明。现已按帮助文档改回：`〈整数型〉打开文件(文本型 欲打开的文件名称，［整数型 打开方式］，［整数型 共享方式］)`，归类「文件读写」，成功返回文件号。
  - 同时修正了它的**实际行为**：`#写出 / #重写 / #改写 / #改读` 此前全是错的（`#重写` 变成了追加而不是清空、`#改写 / #改读` 直接退化成只读、省略「打开方式」也变成只读）→ 现在六种方式都按帮助文档语义执行；**「共享方式」也真正生效了**（此前被完全忽略），`#禁止读 / #禁止写 / #禁止读写` 会实际限制其它进程。
- [修复] **在展开的参数行上按回车，光标（焦点）会丢失**，键盘导航直接断掉、必须回头点鼠标 → 现已修正。回车规则统一为：
  - **可重复参数**（如 `加入文本`、`写出文本`、`调试输出` 的最后一个参数）：**回车就新增一行**，不管当前值是否为空、按几次加几行；想退出按 Esc 或点别处。
  - **不可重复参数**：回车依次前进到下一个参数；到最后一个参数再回车 → **光标落到下一行代码**继续编辑（此前会直接丢焦点，所有命令都受影响、不只可重复参数）。
- [修复] **光标正停在某个参数行上时，这一行没法被多选**（只能先把焦点挪开才选得中），导致回车新增出来的空参数行想选中删掉却选不了 → 现在**Ctrl / Shift 点选**和**直接按住左键拖过去**都能选中它，选中后按 Delete 即可删除。（在正在编辑的那行里按住左键拖动：拖出输入框才转为跨行多选，在框内拖仍是正常选文字。）

---

## ycIDE - 开发测试版 v0.0.5-beta.8(2026-07-15)

---

### 新增功能

- [新增] **可以一次加入多个值了(参数可重复的命令)**：在展开的参数行上**按回车**就会追加下一个值行，照易语言"依次执行"的语义逐个生效。
  - 编辑框：`编辑框1.加入文本("甲", "乙", "丙")` —— 三个都会依次追加到编辑框末尾。
  - 文件/输出类：**写出文本、写文本行、插入文本、插入文本行、写出字节集、插入字节集、标准输出、输出调试文本、写出数据** 也都支持一次给多个值。
  - （此前这些命令只认第一个值，多填的会被丢掉或直接报"参数数量不匹配"。）

### 问题修复

- [修复] **日期时间相关命令结果不对**（重要）：`取现行时间`、`取年份 / 取月份 / 取日 / 取小时 / 取分钟 / 取秒`、`增减时间`、`取时间间隔` 等一批命令，此前内部按错误的数据宽度传递，取到的是垃圾值 → 现已全部修正。日期时间型变量本身也修正为正确的存储方式。
- [修复] **`相加`、`等于 / 不等于 / 大于 / 小于 / 大于或等于 / 小于或等于` 这几个命令结果不对** → 现已修正（注意：这几个命令目前按数值语义处理，暂不支持文本形态如 `相加("a","b")` 文本连接）。
- [修复] **`写出数据` 命令定义错误**（缺少"文件号"参数、返回值标注错误）→ 已按帮助文档修正为 `写出数据(文件号, 数据…)`，返回逻辑型。

---

## ycIDE - 开发测试版 v0.0.5-beta.7(2026-07-14)

---

### 新增功能

- [新增] **集成终端升级成"真正的终端"**：现在能跑交互式命令行工具了（例如 GitHub Copilot CLI、带界面 / 进度条的工具），并且支持多种文本颜色显示。（此前是"伪终端"，交互式工具跑不起来、只有单一文本色。）
  - 直接在终端里输入即可，方向键 / Ctrl+C 等都正常：有选中文字时 Ctrl+C 是复制、没选中时是中断当前命令。
  - **右键菜单**：复制 / 粘贴 / 全选 / 清屏；也能用鼠标直接选中终端里的文字。
- [新增] **代码里可以给编辑框追加文本**：`编辑框1.加入文本("内容")`——在编辑框内容末尾追加（多行编辑框做运行日志很方便）。

### 体验优化

- [优化] **新建控件默认给一个白色背景**（如标签），不再显示"默认"而跟着窗口背景色变——需要透明时可另用控件的"透明"相关属性，这样新建控件的观感更可控。

### 问题修复

- [修复] **新建空项目什么都不做、直接运行会报链接错误**（一堆"未定义符号 krnln_..."）→ 现在空窗口项目也能正常编译运行。
- [修复] **图片框「显示方式」设为"缩放图片"后，运行时图片框被图片原始尺寸撑大**（设计器预览正常、一运行就变大）→ 现在图片框保持设计尺寸、图片自动缩放铺满。
- [修复] **在代码编辑器里点控件方法（如 `编辑框1.加入文本`）的提示，显示"未在已加载的支持库中找到此命令"、参数是空的** → 现在正确显示方法的调用格式和参数。
- [修复] **控件方法那一行（如 `编辑框1.加入文本(a)`）左边不出「+」展开按钮、参数展不开** → 现在能展开，逐行填「欲加入文本」参数（跟普通命令一样）。

---

## ycIDE - 开发测试版 v0.0.5-beta.6(2026-07-14)

---

### 新增功能

- [新增] **代码里可以设置控件「文本颜色」了，并支持多种颜色写法**：
  - 例：`标签1.文本颜色 ＝ #红色`（名色）、`＝ #ff0000`（十六进制，也支持 `#f00` 三位）、`＝ rgb(255, 0, 0)`、`＝ rgba(255, 0, 0, 128)`（注意：透明度 alpha 在文字上画不出来、会被忽略）。
  - 这些颜色写法是通用的整数值，哪里都能用（存进变量、传给命令等）。
  - 支持的控件：标签 / 编辑框 / 选择框 / 单选框 / 列表框 / 组合框 / 分组框 / 月历 / 画板（超级链接框暂不生效）。
  - 编辑器里 `#ff0000` / `#红色` / `rgb(...)` 前面会显示对应颜色的小色块；在代码编辑器（Monaco）里点色块还能弹出取色器直接改颜色。
- [新增] **可以用韩文 / 日文等做变量名、子程序名了**（此前只允许中文和英文）。
- [新增] **展开参数行按住鼠标左键拖动，可以跨行多选参数**（配合原有的 Ctrl / Shift 点选），选中后按 Delete 批量删除实参。

### 体验优化

- [优化] **展开参数行里按回车会跳到下一个参数继续填**——可以一路回车把一条命令的参数逐个填完，不用每个都点一下。
- [优化] **正在编辑的行和被选中的行现在颜色不一样了**（编辑=淡灰「当前行」、选中=蓝），之前两者同色看不清选中；展开参数编辑时也只高亮正在编辑的那一行，不再整块发蓝。
- [优化] **写错的变量名 / 未定义的标识符，编译时给出清晰的中文错误**（带文件和行号，如「未定义的变量或标识符“xxx”，若这是一段文本请用引号括起来」），不再抛难懂的英文 C++ 编译错误；编辑器里也会用红色波浪线标出。

---

## ycIDE - 开发测试版 v0.0.5-beta.5(2026-07-13)

---

### 新增功能

- [新增] **标签「底图」「渐变背景」属性生效了**（设计器预览和运行时都会显示，此前只能在面板里选、两边都不显示）。标签 / 画板的底图方式新增「图片缩放」（拉伸铺满控件）模式。
- [新增] **展开参数行支持多选删除**：Ctrl / Shift 点选参数行后，按 Delete / Backspace / 空格 批量删除实参。

### 问题修复

- [修复] **滑块条运行后背景发黑** → 现在自动融入窗体底色。
- [修复] **进度条等控件在设计器里颜色不对**（深色主题下发黑）→ 固定成 Win32 原生观感（含滚动条 / 选择夹表头 / 月历 / 列表框边框等一批）。
- [修复] **标签背景颜色设为白色时在设计器里显示成透明**（「默认」用哨兵区分，纯黑 `#000000` 现在也能正常设置）。
- [修复] **展开参数行里输入多个值时命令行会堆出大量重复参数**（可重复参数如「调试输出」）→ 改成「逐值一行」，回车 / 逗号新增下一个值行。
- [修复] **窗口带菜单栏时，设计器纵向标尺的高亮 / 鼠标游标整体上移一条菜单栏**。
- [修复] **命令悬停提示弹窗点别处后不消失、偶尔弹到屏幕左上角**。
- [修复] **字体颜色与独立「文本颜色」属性双向同步**（改其一另一个跟随，所见即所得）。
- [修复] **一批控件属性 / 时钟周期事件在代码里读写 / 触发**（详见 beta.4 说明的延续项）。

---

## ycIDE - 开发测试版 v0.0.5-beta.4(2026-07-12)

---

### 问题修复

- [修复] **又一批控件属性现在可在代码里读写**（续上版）：
  - **选择夹.现行子夹**（代码切换/读取当前子夹页，自动同步页内控件显隐）
  - **时钟.时钟周期**（毫秒；置 0 停止时钟）
  - **编辑框 / 组合框选区**：`起始选择位置`（-1=光标移到末尾）、`被选择字符数`（-1=全选）、`被选择文本`（写入即替换选中内容）、`最大允许长度` / `最大文本长度`（0=不限制）
  - **画板画笔状态 11 项**：`画笔类型 / 画笔粗细 / 画出方式 / 刷子类型 / 绘画单位 / 自动重画 / 画笔颜色 / 刷子颜色 / 画板背景色 / 文本颜色 / 文本背景颜色`——画图时运行中改画笔即刻生效
  - **月历.滚动月数 / 最多选择天数**
  - **窗口自身属性**：`窗口名.标题 / 宽度 / 高度 / 左边 / 顶边 / 可视 / 禁止` 在代码里读写现在真正作用于窗口（此前赋值无效果）
- [修复] **时钟「周期事件」现在会真正触发**——此前时钟控件不产生任何事件（半成品），现按时钟周期派发到 `_时钟名_周期事件` 子程序。
- [修复] **浏览框.地址 赋值导航**编译报错（beta.2 重构引入的回归）。

### 优化

- [优化] **不支持在代码中访问的控件属性 / 方法，编译时给出清晰的中文错误**（带源文件和行号，如「标签“标签1”的属性“边框”暂不支持在代码中赋值」），不再抛出难懂的英文 C++ 编译错误。

---

## ycIDE - 开发测试版 v0.0.5-beta.3(2026-07-11)

---

### 问题修复

- [修复] **一批控件属性此前在代码里读写会编译报错，现已修复**（与上版进度条数值属性同类的系统性问题）：
  - **通用**：`控件.可视` / `控件.禁止`（显隐、禁用）、`控件.左边` / `顶边` / `宽度` / `高度`（移动、改大小）、`控件.标记`
  - **选择框 / 单选框.选中**、**组合框 / 列表框 / 选择列表框.现行选中项**
  - **日期框 / 月历.今天 / 最小日期 / 最大日期**（月历另含 首选择日 / 尾选择日）
  - 例如 `按钮1.可视 ＝ 假`、`按钮1.宽度 ＝ 200`、`选择框1.选中 ＝ 真`、`组合框1.现行选中项 ＝ 0`、`日期框1.今天 ＝ “2026/07/11”` 现在都能正常编译运行。
  - （尚未覆盖的少数：选择夹「现行子夹」、时钟「时钟周期」、编辑框选区类属性，以及边框 / 对齐等设计期样式属性——后续版本再补。）

---

## ycIDE - 开发测试版 v0.0.5-beta.2(2026-07-11)

---

### 问题修复

- [修复] **进度条 / 滑块条 / 横向 / 纵向滚动条的「位置 / 最小位置 / 最大位置」等数值属性**现在可在代码里读写了（例如 `进度条1.位置 ＝ 进度条1.位置 ＋ 1`）——此前这样写会编译报错。

### 优化

- [优化] **控件成员访问机制重构**：控件的属性读写（如 `编辑框1.内容`、`进度条1.位置`）与成员方法调用（如 `组合框1.加入项目(…)`、`列表框1.取项目文本(…)`）改为由支持库配置声明式驱动、运行时下沉到系统核心支持库。**对日常使用无变化**；第三方支持库开发者从此可自行为控件扩展属性 / 方法而无需改动 IDE 本体。
  - 此项改动覆盖面较广，**建议重点回归**各控件的属性设置与成员方法：进度条 / 滑块条位置、编辑框 / 标签内容、组合框 / 列表框 / 选择列表框的加入 / 删除 / 取项目文本 / 取项目数等。

---

## ycIDE - 开发测试版 v0.0.5-beta.1(2026-07-11)

---

### 新增

- [新增] **画板控件完整落地（含绘图运行时）**：可在画板上画点/直线/椭圆/弧线/弦/饼/矩形/圆角矩形/渐变矩形/多边形、写文本、画图片、取像素/取图片，支持画笔（7 种线型 / 16 种画出方式）、刷子（56 种）、绘画单位换算、自动重画、底图、「绘画」事件——编译出的程序里**真正能绘图**。
- [新增] **可视控件全部补齐（22 个）**：画板 / 选择夹 / 组合框 / 列表框 / 选择列表框 / 进度条 / 滑块条 / 横纵滚动条 / 日期框 / 月历 / 超级链接框 / 图片框 / 外形框 / 分组框 / 选择框 / 单选框 / 标签 等控件的专属属性、成员方法、设计器预览、编译运行时全部落地（此前多为空壳，仅有公共属性）。
- [新增] **选择夹「子夹页」**：完整可视化容器——点标签头切页、控件拖进画板体区即归属该页、切页自动显隐；子夹标题弹窗编辑（仿菜单编辑器）。
- [新增] **控件字体 / 文本颜色 / 按钮底色**：字体选择器（可拖动浮动对话框）、文本颜色并入字体设置、按钮底色（自绘按钮）。
- [新增] **窗口属性扩展**：底图 + 底图方式、外形（矩形 / 圆角矩形 / 椭圆）+ 圆角半径、图标（任意图片格式，无需 .ico）。
- [新增] **编辑框调节器**三模式（无 / 自动 / 手动）+ 可设上下限。
- [新增] **跨窗口成员补全**：在任一窗口代码里引用别的窗口名即可解析其控件 / 子程序 / 属性 / 方法（不再依赖设计器打开）。
- [新增] **内置八套配色主题**（三色系双版本 + 默认深浅渐变版）+ 主题分组渐变 + 悬停命令弹说明窗（照易语言）。
- [新增] 数组字面量 `{ 1, 2, 3 }` 全链路、文本型数组、字节集字面量、逻辑整除运算符 `＼`。

### 优化

- [优化] **颜色选择器**：支持直接输入 / 粘贴 / 复制 `#RRGGBB`；按住滑动取色不再卡顿。
- [优化] **字体选择器 / 字体对话框**改为可拖动浮动窗，不再被侧栏裁剪遮挡。
- [优化] 调试输出格式逐字符对齐易语言（全角引号、类型标记、多值对齐）。

### 问题修复

- [修复] **颜色选择器按住滑动导致整个 IDE 崩溃**（属性面板 / 字体对话框 / 主题管理器三处取色器全部节流修复）。
- [修复] **独立横向 / 纵向滚动条**滑块现在可跟随点击 / 拖动移动。
- [修复] 窗口外形从「圆角矩形」切换到「矩形」时属性面板崩溃。
- [修复] 切换主题后局部残留上一主题配色（此前需重启才恢复）。
- [修复] 文本处理相关内存泄漏（文本型改用值语义自动管理）。
- [修复] 注释行删除 `'` 后的空格时光标跳到行末尾 / 空格删不掉。
- [修复] 图片框背景色设为白色时在设计器显示成透明。

---

## ycIDE - 开发测试版 v0.0.4-beta.9(2026-07-09)

---

### 新增

- [新增] **编译器数组支持（全链路）**：`.局部变量 数组, 整数型, , "0"` 动态数组 / 定长数组 / **多维数组**（`"100,100"`，`矩阵 [i] [j]` 链式下标）/ **数组参数**（含递归传递）全部可编译运行；**小数型/双精度小数型数组**同样支持；一基下标、越界安全回落。配套命令 加入成员 / 取数组成员数 / 重定义数组 / 数组排序 / 清除数组 / 插入成员 / 删除成员 / 复制数组 按真实 ABI 接通（此前会编译报错或行为错乱）。
- [新增] **支持库文本常量**：`#换行符`（回车换行）、`#回车符`、`#制表符`、`#引号` 可在代码中直接使用；修复支持库常量清单未被装载导致所有 `#常量` 编译报错的问题。
- [新增] **编译前全项目错误拦截**：点编译/运行时对项目**全部源文件（含未打开的）**跑问题面板同款检查，有错误则取消编译并在输出面板按文件列出「第几行、什么错」；未打开窗口的控件上下文（如输入方式类型检查）同样生效。
- [新增] **控件属性赋值类型检查**：编辑框「输入方式」设为数值/日期类时，「内容」按实际类型检查（如 输入整数 时 `编辑框1.内容 ＝ 到文本(n)` 报类型不匹配、`＝ n` 才合法）；其它控件属性（宽度/标题等）按支持库定义的类型一并检查。
- [新增] **行多选覆盖粘贴**：多选选中子程序后粘贴＝覆盖（完整选中完整覆盖；选一半时未选中的尾部代码自动归入粘贴后的子程序；选区里剪贴板没有对应物的子程序随选区删除）；不多选粘贴维持重名自动改名 原名_1。
- [新增] 逻辑整除运算符 `＼`。

### 问题修复

- [修复] **组合条件死循环**：`.判断循环首 (i ＜ j 且 数组 [j] ≥ 基准)` 这类「比较 且/或 比较」条件此前生成错误代码导致死循环（点按钮未响应）——表达式转译补上逻辑运算符优先级。
- [修复] **控件文本属性赋值编译失败**：`编辑框1.内容 ＝ 到文本(n)` 等右值为支持库命令时报 undeclared identifier。
- [修复] 逻辑运算符 `且`/`或` 误报「未知命令」；`真`/`假` 后紧跟半角逗号/括号时误报「未定义变量」。
- [修复] 行内**括号不匹配**（多余/缺少右括号）现在会在问题面板报错。
- [修复] 菜单「编译→普通编译」与 Ctrl+F7 此前点击无反应。
- [修复] 成员补全只对窗口上真实放置的组件弹出；`编辑框1.内容` 引用不存在的组件、整行只写字符串等语法问题在问题面板报错。

---

## ycIDE - 开发测试版 v0.0.4-beta.8(2026-07-08)

---

### 新增

- [新增] **未知命令回车提示插入变量**：代码行输入未知名称（如「我是」）按回车，弹出提示可一键插入为**局部变量 / 程序集变量 / 全局变量**（局部为默认，全局自动写入全局变量文件），插入后光标回到下一行继续写。
- [新增] **行号区交互**：悬停行号显示镜像箭头光标；**单击行号选中整行，按住拖动整行多选**，Shift+单击扩展选择。
- [新增] **表格键盘导航**：单元格里 ↑/↓ 在行间移动（同列优先、表格与代码行之间无缝穿行）；→ 在文本末尾跨到下一格、← 在格首跨到上一格，行尾继续按则进入下一行；**公开/静态/传址等勾选格也能用方向键停入、按空格切换勾选**。
- [新增] **正在编辑的行显示焦点背景**（行号区一并着色），悬停其它行的悬停色不会盖住它。
- [新增] **项目管理器右键菜单**：各分类（窗口/程序集/全局变量/常量表/数据类型/DLL命令）右键可直接新建对应内容；**窗口节点**右键支持**复制 / 粘贴 / 删除**窗口（粘贴统一命名 窗口N 并同步改写程序集与事件引用；`_启动窗口` 不可删除）。
- [新增] **复制/粘贴自动补全流程结束**：复制或粘贴（含外部代码）只有流程头没有配对结束（如判断开始没有判断结束）时，自动补上结束行，不再破坏流程线。

### 问题修复

- [修复] 粘贴**重名子程序**不再没反应：自动改名为 原名_1（占用则 _2 依次类推）整块插入。
- [修复] 粘贴或手工输入的 `.局部变量` 自动归位到子程序头部声明区，同一子程序不再出现第二张局部变量表；**同子程序重复的变量名在表格里名称格标红**（问题面板同步报错）。
- [修复] 整行只写**纯数字**（如 `123555`）现在会报「无效语句」。
- [修复] 下划线开头窗口名（`_启动窗口`）改名/复制时程序集名（`窗口程序集_启动窗口`）未跟随改写的问题；窗口事件保持 `__启动窗口_创建完毕` 双下划线约定不变。

---

## ycIDE - 开发测试版 v0.0.4-beta.7(2026-07-08)

---

### 新增

- [新增] **命令参数数据类型检查**：命令的参数现在会按参数定义检查数据类型并在问题面板报错——如文本型参数写了没加引号的 `1.txt` 会提示「不是有效表达式；文本内容需加引号」；整数型参数传文本、逻辑型条件传数值、类型不符的变量/嵌套命令返回值等都会报「类型不匹配：需要 X，当前为 Y」。无法确定类型的表达式（含运算符、常量、控件属性等）和通用型参数不检查，不会误报。
- [新增] **展开参数输入框支持自动补全**：在展开的参数行里输入命令/变量（如敲「到」或拼音「dao」）现在会弹出补全窗，↑↓选择、Enter 采纳（命令自动补括号、光标进括号内），与主代码行一致。此前参数行不弹补全，容易误以为命令不存在。
- [新增] **行多选时按空格键等同删除**：多选行后按空格即删除所选（与 Delete 相同，含流程结构最小保护）；正常输入文本时空格不受影响。

### 问题修复

- [修复] 展开参数里输入**未配平的括号**（如敲到「到字节集(」一半）时，上方命令行每敲一键就重复追加一组同参数命令、整行括号结构损坏。现在中间态不写回、提交时自动补齐缺失的右括号。
- [修复] 流程线两处：①**分支（否则）的内容行本身是嵌套结构**时，外层分支的右向箭头停在半路、与嵌套结构之间留缺口——现在延伸到嵌套命令文字左边；②**判断 case 与 默认 之间的内容多于一行**时，内侧竖线在 case 下一行断开——现在全程连续。

---

## ycIDE - 开发测试版 v0.0.4-beta.6(2026-07-07)

---

### 新增

- [新增] **全局撤销/重做**：撤销/重做现在是跨编辑器、按操作顺序的——在代码编辑器改了代码、再切到可视化设计器移动控件，撤销会先撤控件移动、再撤代码修改，每一步都能倒序回退（Ctrl+Z / Ctrl+Y、工具栏、右键均可）。**窗口和控件改名也能撤销**（会连带把改名撤回）。
- [新增] **可视化设计器右键菜单**（照易语言窗体设计器）：撤销、删除、复制、剪切、粘贴、位置及尺寸（对齐 / 相同大小子菜单）、到最顶层、到最底层、锁定、解除锁定、菜单编辑器、预览、跳回先前位置、到对应窗口程序集、查看数据类型定义。控件可跨窗体复制粘贴；锁定的控件不能拖动/缩放；「查看数据类型定义」会弹框列出该控件类型的属性和事件。
- [新增] **窗口预览**（Ctrl+Enter）：把当前窗口直接编译运行成真实系统窗口来看外观，但**不编译事件源代码**——控件都在位、点击无逻辑，用来快速看窗口样子，即使代码还没写好或有错也能预览。
- [新增] **菜单编辑器**（Ctrl+E）：像易语言那样编辑窗口菜单（标题/名称/快捷键/选中/禁止/可视/分隔条、多级子菜单、上移下移升级降级）。菜单会在编译运行/预览时显示在窗口顶部；在设计器画布上也能看到菜单栏，**单击菜单项自动生成或跳转到对应的「被选择」事件子程序**。菜单项名称跟随标题自动填、必填、不能数字开头、不能重复；改名后源代码里的事件引用会跟着一起改。

### 优化

- [优化] 对齐工具条和顶部工具栏按钮的鼠标悬停效果改为**亮色圆角描边**，更清晰。

---

## ycIDE - 开发测试版 v0.0.4-beta.5(2026-07-06)

---

### 优化

- [优化] 输入**数字开头**的内容（如 `如果真（1）` 里的 `1`）不再弹出补全窗打扰——数字不可能是命令/变量名。

### 问题修复

- [修复] 流程结构体内「命令头/分支 与 正文命令 之间」多敲出来的空行，现在按 Backspace 可以正常删除（删该行、光标回到上一行命令末尾）；每段最小的那条空占位行仍受保护、不会误删。
- [修复] 多选**剪切/删除**流程结构里的正文时保护"最小结构"：剪掉某段唯一的正文行后，该段不再塌成「如果紧贴否则」，而是自动留一条空行占位；结构结束（如果结束）若正好在子程序末尾，也会自动补一条尾部空行，好在结构外面接着写命令。空的分支体在表格模式的显示塌行/不全也一并修复。
- [修复] 剪切/复制流程结构内的正文时，**剪贴板内容不再多出隐藏的配对标记**（否则、如果结束 等）——只选中间正文时只剪正文；选中含结构头整体复制时才带上完整标记（粘贴可重建）。

---

## ycIDE - 开发测试版 v0.0.4-beta.4(2026-07-06)

---

### 问题修复

- [修复] 窗口程序里把**控件文本(如 `编辑框1.内容`)赋值给文本型变量**时编译报错(`discards qualifiers`)。原因是控件文本读取函数返回的是"共享缓冲"而非独立拷贝，既与文本型变量类型不符、又会在连续读取多个控件文本时相互覆盖。现改为每次返回独立的文本拷贝，编译通过、也不会再串内容。

---

## ycIDE - 开发测试版 v0.0.4-beta.3(2026-07-06)

---

### 问题修复

- [修复] 变量汇总面板左侧那条**蓝色竖条**去掉了。它其实是全局键盘焦点描边(色值 #007fd4)落在了编辑器主体容器上、而容器右缘正好贴着变量面板边界，就显成一条贯穿全高的蓝线；最大化/收展面板等一操作又会消失。现用更高优先级规则统一盖掉编辑器内多余的焦点描边(输入框自身的焦点提示保留)。
- [修复] 在已有流程结构**上方**新输入一个流程命令(如下方已有「判断」、在其上方空行敲「如果」)时，还没上屏就实时画出一条流程线、且歪画到下方结构的结束位置。现在编辑期把流程模型冻结在编辑前，等上屏自动格式化后再绘制流程线。

---

## ycIDE - 开发测试版 v0.0.4-beta.2(2026-07-06)

---

### 问题修复

- [修复] 多个 case 分支的判断结构，范围多选删除中间的 case 时不再连带删掉「默认」、把整条判断链删散——现只删选中的 case，判断开始/默认/判断结束骨架保留。
- [修复] 删除流程命令头（如退格清空「如果」头）的过程中，不再瞬时闪现出「否则/如果结束」等标记、流程线也不再画到下方别的结构上；等提交溶解时才随结构一并消失。

---

## ycIDE - 开发测试版 v0.0.4-beta.1(2026-07-06)

---

### 问题修复

- [修复] 未定义变量现在会报错：在流程条件（如 `判断开始（a ≠ 1）`）或表达式里引用了未声明的变量，之前问题面板不提示，现在会标红并在问题面板报「未定义变量」。
- [修复] 循环类流程尾命令自动补全后带上括号：带参数的「循环判断尾（条件）」不再丢括号；无参的判断循环尾/计次循环尾等也统一带括号，与循环首一致。
- [修复] 补全弹窗不再列出「否则/判断结束/默认/如果结束」等隐藏的配对结构标记——它们由结构自动生成，手打只会造成孤立标记。
- [修复] 多选（拖选行）删除不再把流程结构删残：未选中整个命令的头时，判断/如果/如果真会保留其最小骨架（不残缺）；各类循环（计次/判断循环/循环判断/变量循环）在选到尾或体时仍正常整体溶解。

---

## ycIDE - 开发测试版 v0.0.3-beta.67(2026-07-05)

---

### 问题修复

- [修复] 流程内已有内容时再修改流程头（判断开始/如果等）的参数，会把结构里的内容甩到结构外、并重复出现「默认/判断结束」——现只原地更新参数、保持缩进与结构不变。
- [修复] 判断/如果等流程条件里的运算符现在自动规范：`a!=1`（或全角 `a！=1`）自动转成 `a ≠ 1`，`a>=1` → `a ≥ 1`、`a<=1` → `a ≤ 1`，与赋值行一致。
- [修复] 表格里偶发冒出本应隐藏的「默认/判断结束/否则/如果结束」等流程标记：这类"孤立标记"来自结构被破坏后的残留，本版修复了产生它们的几处根因（编辑分支/流程头参数、删除带分支结构的头等），显著减少泄漏。

---

## ycIDE - 开发测试版 v0.0.3-beta.66(2026-07-04)

---

### 问题修复

- [修复] 给判断的分支「判断（）」在括号里补参数（如改成「判断（条件）」）后失焦，不再被误转成「判断开始」而破坏结构——现原地保留为分支、只更新参数，缩进也不乱（嵌套内层分支同样保持）。
- [修复] 删除/退格清空「如果」等带分支结构的命令头后，不再残留孤立的「否则」（有时连「如果结束」）——现整块干净溶解；嵌套时只溶解目标那层，内层结构完好。

### 体验优化

- [优化] 流程结构体退格保护收窄为「只保护最小结构体」：结构体内在 body 里多按回车产生的额外空行现在可以正常删除，只有最小的那一条空行受保护、不会误删破坏结构，光标也不会困在结构里。

---

## ycIDE - 开发测试版 v0.0.3-beta.65(2026-07-04)

---

### 问题修复

- [修复] 判断结构键入「判断」的"加分支 / 建嵌套"判定更精准（承接上版）：现按行位置区分——在「默认」区（默认之后、判断结束之前）键入「判断」，作为该判断的新分支；在结构体内其他位置（如「判断开始」之后的首行）键入「判断」，则新建嵌套的子判断结构。加分支打「判断」、嵌套打「判断开始」。
- [修复] 嵌套判断内"紧挨着"建不出兄弟判断：在已嵌套的内层判断结束之后键入「判断」，此前会被误当成内层判断的一个分支塞进去，现能正确新建相邻的兄弟嵌套判断，并与前一个自动连通桥接（与外层表现一致）。多层嵌套下同样正确。

### 体验优化

- [新增] 流程结构体退格保护：在判断/如果等结构的空行内按退格键，不再删除或破坏结构，而是把光标平滑上移到上一行末尾；逐行上移，直到命令行本身才恢复正常字符删除。避免了误删结构体内空行、以及光标被困在结构里出不来的问题。

---

## ycIDE - 开发测试版 v0.0.3-beta.64(2026-07-04)

---

### 问题修复

- [修复] 流程线在深层/复杂嵌套下的一系列绘制错误：多层判断嵌套、判断多分支、循环/如果/判断混合嵌套时，会出现"指向空处的幽灵分支箭头""分支线画在错误深度""如果结束/循环尾紧跟判断时多画一段连接件""否则里是循环时循环箭头与横线脱节"等问题，现已逐一修正（经多场景穷尽审计）。
- [修复] 判断结构编辑更符合预期：
  - 在判断体内键入"判断"回车，正确补全为嵌套的完整判断结构（判断开始/默认/判断结束），且外层结构保持完好。
  - 在默认分支体内键入"判断"后按空格上屏再点击别处失焦，现在与直接回车一致地补全为新分支（此前失焦只补括号、位置/缩进错乱）。
  - 删除或退格清空判断块里的"判断"分支时，只删该分支（等同普通删行），不再错误溶解整个判断块（此前会连本块的默认/判断结束一起删掉、与相邻块混错）。
- [修复] 从代码行按住向下拖选多行后，按 Backspace/Delete 现在正确删除选中的多行（此前只删掉了输入框里的一个字符）。

### 底层优化

- [优化] 编译性能、依赖体积、编辑器渲染热路径与本地资源加载、敏感信息存储等多项底层审计优化，提升稳定性与响应速度。

---

## ycIDE - 开发测试版 v0.0.3-beta.63(2026-07-03)

---

### 新增功能

- [新增] 代码编辑器右侧变量汇总面板：全文件汇总程序集变量/全局变量，并按子程序分组显示各自的参数与局部变量（光标所在子程序的组名高亮）；条目带类型配色图标与数组标记；顶部搜索框按名称/类型过滤；点击变量直接跳转到声明行；面板可收起为细条，也可在 系统设置→编辑器 里整体关闭。
- [新增] 项目管理器未调用子程序提示：程序集分类下从未被任何代码调用的子程序名称灰显（事件子程序与 _启动子程序 等入口不参与判定），悬停可见说明；编辑代码时实时更新。
- [新增] AI 修改标识：AI 编辑产生的未保存更改，项目管理器橙色圆点与文件标签白色圆点外会包一个圆角矩形，与人工编辑（无圆角矩形）区分；人工再编辑该文件即恢复人工样式。
- [新增] AI 聊天记录按项目保存：四种模式的对话历史随项目保存，关闭重开 IDE 自动恢复；不同项目的记录互不干扰。

### 功能优化

- [优化] AI 助手聊天窗口全新样式：自己发送的消息为右侧气泡，AI 回答为无边框正文；思考过程折叠展示（思考中自动展开、结束后自动收起，可点开回看）；Agent 工具调用显示为带状态点的卡片。

### 问题修复

- [修复] AI 编辑的删除 diff 显示不完整：红色删除行现在逐行显示被删除的原始内容（带删除线，多行删除显示为多行红色块），删除行的行号区也恢复红色背景。

---

## ycIDE - 开发测试版 v0.0.3-beta.62(2026-06-18)

---

### 性能优化

- [优化] 编译速度大幅提升：修复了"项目代码没有任何改动、重复编译仍然很慢"的问题。此前每次编译都会反复多次全量扫描支持库目录并清空内部缓存，在机械盘/移动盘或被杀毒软件实时监控的非系统盘上尤为明显（实测可达 6 秒）；现已让支持库信息在一次编译内只扫描一次并全程复用，无改动的重复编译应明显加快。
- [优化] 编译/运行不再卡住界面：编译工作已移入独立的工作线程，点击"运行"或"编译"时主界面不再短暂无响应；工作线程异常时自动回退到原有方式，保证始终可编译。
- [优化] 编辑器光标响应优化：修复在复杂界面下"点一行要等一两秒光标才落下、输入很卡"的问题——光标移动不再触发整个界面重绘，仅更新状态栏的行列号显示。

---

## ycIDE - 开发测试版 v0.0.3-beta.61(2026-06-13)

---

### 新增功能

- [新增] 代码编辑器子程序整体折叠：子程序名行的行号右侧新增常驻的 +/- 按钮，点击可收缩/展开整个子程序（收缩后仅保留子程序声明行，参数、局部变量、代码全部隐藏）。折叠状态跟随子程序名记忆，编辑其他位置导致行号变化时不丢失。代码行原有的参数展开 +/- 按钮保持不变。
- [新增] 可视化设计器画布右下角缩放控件：点击倍率数字弹出预设档位（最小倍率/30%/50%/80%/100%/150%/200%/300%/500%/1000%，选择后窗体自动居中）；1% 步进拖动条可精细调节（拖动时保持当前视点不跳动）；拖动条两侧 −/+ 按钮按 10% 步进；最左侧"适应窗口"按钮一键缩放到窗体铺满画布并居中。

### 问题修复

- [修复] 连续快速按回车或退格键时丢键、卡顿的问题：此前连按两次回车经常只新增一行、连按退格部分按键无效，原因是快速连按时编辑器状态更新跟不上、后续按键基于过期数据计算并互相覆盖。现已改为每次按键都基于最新文本、并强制状态即时落地，连按多少次就准确响应多少次。

---

## ycIDE - 开发测试版 v0.0.3-beta.60(2026-06-12)

---

### 新增功能

- [新增] WebView2 浏览器支持库（webview2）：新增"浏览框"组件，基于系统 Edge WebView2 运行时渲染网页，可视化设计器画布与编译后程序渲染一致（所见即所得）；免分发 WebView2Loader.dll（自动发现系统运行时），运行时缺失时组件显示提示文字。新增命令"取WebView2运行时版本"用于环境检测。
- [新增] WebView2 风格组件"网页按钮"/"网页编辑框"：HTML 渲染的现代风格控件（圆角渐变按钮、聚焦光圈输入框），设计期与运行期外观一致；事件（被单击/内容被改变）与文本属性（标题/内容）用法与 Win32 同类组件完全相同。与"通用Win32"组件构成两套风格，Win7 等无 WebView2 环境继续使用 Win32 组件。
- [新增] 组件工具箱分类：工具箱顶部新增分类下拉框（默认"通用Win32"，选择持久化），支持库组件可声明"组件箱分类"（toolboxCategory）自定分组；未声明分类的组件（含核心库与第三方库）默认归入"通用Win32"。
- [新增] 编辑框组件补全 19 个属性：边框（无边框/单线边框/凹入式）、文本颜色、背景颜色、隐藏选择、最大允许长度、是否允许多行、滚动条、对齐方式、输入方式（12 种：通常/只读/密码/整数文本/小数文本/字节/短整数/整数/长整数/小数/双精度/日期时间，限定输入模式带字符过滤）、密码遮盖字符、转换方式（大小写）、数值调节器（方式/底限值/上限值）、起始选择位置、被选择字符数、可停留焦点、停留顺序、备注。属性面板、画布预览与编译运行全链路生效。
- [新增] 属性面板颜色类属性改为颜色选择器：色块点击弹出系统取色对话框，常用色显示中文名（黑色/白色等），不再手填颜色数值。
- [新增] 支持库组件属性支持声明默认值（defaultValue），属性面板按默认值显示。

### 性能优化

- [优化] 编辑器交互性能大幅优化，点击换行、输入、删除的卡顿明显缓解（点击到光标出现耗时下降约 70%）：同一文本只做一次全量解析（共享解析缓存）；鼠标按下即进入编辑态（减少一轮渲染且不再等待鼠标抬起）；虚拟化渲染窗口收紧；小地图预览先采样后着色；补全拼音打分增加首字符锚定预检；React.memo 隔离顶层状态变化引起的编辑器级联重渲染。
- [修复] 补全弹窗位置漂移：编辑器缩放（字号非 13 时）未做坐标反向补偿导致弹窗位置系统性偏移，且输出面板开合等布局变化后弹窗不跟随；现弹窗实时吸附输入光标。

### 问题修复

- [修复] 信息框等多文本参数命令显示错乱：两个以上文本参数会互相覆盖（如提示信息与窗口标题显示成同一文本），运行时文本转换函数改为轮转缓冲池。
- [修复] 整数等非文本值赋给 编辑框.内容 等控件文本属性时 C++ 编译报错，现自动转换为文本。
- [修复] "创建完毕"事件中按控件名操作控件（如设置编辑框内容）全部静默无效的问题（主窗口句柄赋值时机过晚）。
- [修复] 空内容编辑框运行时错误显示控件名（如"编辑框1"）的问题。

---

## ycIDE - 开发测试版 v0.0.3-beta.59(2026-06-11)

---

### 新增功能

- [新增] 指针命令（函数指针间接调用）：在 DLL命令表（.ell）中与 DLL命令 平级声明 `.指针命令 命令名, 返回类型`＋`.参数` 行；调用时第一个实参固定传入函数地址（长整数型），编译器按声明签名生成类型化间接调用。专为只导出接口函数表的 DLL（如 sciter.dll 仅导出 SciterAPI，所有功能通过 ISciterAPI 函数指针表暴露）设计，已用真实 sciter.dll 验证完整调用链路。
- [新增] 核心支持库新增"指针到长整数"（p2int64）命令：从指定内存地址读取 8 字节长整数，用于在 x64 下从函数指针表中读取函数地址，配合指针命令使用。
- [新增] 指针命令全套编辑体验：表格编辑器渲染指针命令表（公开/传址/数组可点选切换）、代码补全与参数详情展开自动显示隐式"函数地址"首参、项目树 DLL命令分类下显示指针命令节点、插入菜单新增"指针命令(P)"。
- [优化] DLL命令编辑器（.ell）右键菜单首项由"新子程序"改为"N.新DLL命令"，并新增"Z.新指针命令"；插入后自动滚动定位并高亮新行，菜单打开时按 N/Z 键可直接触发。其他文档类型右键菜单不变。
- [优化] 赋值行参数展开改为两级结构：`变量 ＝ 命令(…)` 展开第一级显示"※被赋值的变量或变量数组"与"※用作赋予的值或资源"，命令自身参数挂在第二级（点击 + 再展开），与易语言展开习惯一致。

### 问题修复

- [修复] 代码行行尾单引号注释破坏命令实参解析：如 `调试输出 (版本号)   ' 注释` 会被转译成打印空文本（注释内若含括号干扰更大）。转译时现在正确剥离行尾注释（字符串内的 ' 不受影响）。
- [修复] 支持库"实际使用"扫描漏掉嵌套在实参中的命令调用（如 `外部命令(指针到长整数(地址), 0)` 中的内层命令），导致所属支持库未参与链接、报"未定义符号"链接错误。
- [修复] 指针到整数/小数/双精度小数/字节集/文本 的内存指针参数由整数型升级为长整数型，修复 x64 下真实内存地址被截断、读取错误位置的问题。
- [修复] 新建指针命令后表格中命令名显示为"指针命令1,"（带行尾逗号）的问题；同时修复指针命令行的单元格编辑与公开/传址/数组切换不生效的问题。
- [修复] krnln 命令映射生成器误判：正则跨行贪婪匹配导致部分包装函数（如 对象.禁止重画/允许重画）被标记为未实现；同步修正核心库完整性校验的过期哈希表（避免未来正式版打包后启动校验失败）。

---

## ycIDE - 开发测试版 v0.0.3-beta.58(2026-06-11)

---

### 新增功能

- [新增] 编译器支持类模块对象语义：类模块编译为 C++ 结构体，程序集变量成为成员变量，公开/私有子程序成为成员方法，对象创建/销毁时自动调用 _初始化/_销毁；`.局部变量 x, 类名` 与 `对象.方法(实参)` 调用按原生语义工作。
- [新增] 编译器支持子程序返回值：函数签名携带声明的返回类型，`.返回(表达式)` 返回真实值；有返回类型的子程序末尾自动补默认返回值。
- [新增] 项目管理器右键菜单：程序集分类上可新建程序集/类模块；程序集或类模块节点上可新建子程序、删除（未被引用直接删除，被引用时弹确认；文件移入回收站并同步关闭标签页/更新 .epp）。
- [新增] 对象成员补全：变量类型为项目类时，输入 `变量名.` 弹出该类公开子程序补全（含签名提示、拼音匹配），上屏自动补全调用括号。
- [新增] 类公开方法调用行支持展开参数详情（行号旁 +/- 按钮，参数行可直接编辑实参）。

### 问题修复

- [修复] 修复表格单元格（尤其类型单元格）中文输入法候选框闪退的问题：IME 组合期间不再做值改写/光标干预/补全弹窗更新，组合提交后统一处理。
- [修复] 修复类模块文件处于打开状态时其类名从类型补全中消失的问题，类型单元格输入拼音（如 lei）可正确关联项目类。
- [修复] 修复编辑态在最后一行按 ↓ 方向键输入焦点消失的问题，现在停留在最后一行；在最上方代码行按 ↑ 会进入文档最上方的可编辑处——程序集名表格单元格。
- [修复] 修复第一个 判断 命令（源码关键字为 判断开始）点击获得焦点后编辑框内显示为"判断开始"的问题；现在编辑态与非编辑显示一致均为"判断"，提交/实时写回时自动还原真实关键字，结构不受影响。
- [修复] 新建子程序不再自动附带默认局部变量。

---

## ycIDE - 开发测试版 v0.0.3-beta.57(2026-06-10)

---

### 新增功能

- [新增] 代码行行内文本拖选：行未获得焦点时，在行内横向按住鼠标拖动即可直接选中行内文本（拖动过程实时显示选中背景），与已聚焦时的拖选体验一致；纵向拖动或拖到其他行仍为整行多选，拖出当前行会自动转为行多选。
- [新增] 智能引号：光标右侧已是引号时再输入引号视为收尾、自动跳过不重复插入；输入法把 “” 成对上屏时光标自动落到引号对中间，避免自动配对堆出 """" 的情况。
- [新增] 命令行括号保护：命令调用行（含 如果/如果真 等流程行）右括号之后不允许输入内容。
- [新增] 展开参数行支持嵌套表达式编辑：被加数/加数、子命令参数等更深层参数行可直接点击编辑，提交时精确替换对应子表达式。
- [新增] 展开参数行的嵌套子树改为行号右侧 +/- 按钮手动展开收缩，不再随焦点变化自动展开/收缩。
- [新增] 编译耗时显示自动换算单位，如 `5996 毫秒/5.9 秒`、`599600 毫秒/9 分钟 59.6 秒`，支持到小时/天。

### 问题修复

- [修复] 修复 如果/如果真 等流程命令在失去焦点提交后，配对的 否则/如果结束 结构行被误删除（结构溶解）的问题。
- [修复] 修复在括号内输入 “”、（）、＝ 等全角字符后光标跳到行尾、后续输入落到括号外造成内容重复的问题。
- [修复] 修复 信息框() 编译后点击按钮不弹出的问题：底层向 MessageBox 传入了无效父窗口句柄导致静默失败；同时改用宽字符 API 避免中文乱码。
- [修复] 修复展开参数行编辑时显示蓝色输入框边框的问题，参数行编辑现与代码行一致只显示输入光标，宽度随内容自适应。
- [修复] 修复展开参数行首次点击时光标落到末尾、需要第二次点击才能定位的问题，现在首次点击即按点击位置定位光标。
- [修复] 修复编译"未检测到输入变化、直接复用上次产物"的快路径永远不生效的问题；代码未改动时再次运行将直接复用产物，跳过编译与资源步骤。

---

## ycIDE - 开发测试版 v0.0.3-beta.56(2026-06-09)

---

### 新增功能

- [新增] 新建项目目标平台扩展：新增 Android、iOS、HarmonyOS 目标平台选项，并根据桌面端/移动端自动切换可选项目类型。
- [新增] 移动应用项目支持窗口设计器：移动应用默认创建窗口设计文件和对应源码文件，Android/HarmonyOS 默认尺寸为 360×800，iOS 默认尺寸为 390×844。
- [新增] 新建项目默认名称按“目标平台 + 项目类型 + 新项目序号”自动生成，例如 `windows窗口程序-新项目1`、`android移动应用-新项目2`。
- [新增] 系统设置新增 Android 运行配置：可配置 Android SDK、ADB、Gradle、Android Gradle Plugin、compileSdk/minSdk/targetSdk、应用包名、模拟器启动命令等。
- [新增] Android 移动应用运行链路雏形：运行 Android 移动应用时生成临时 Gradle WebView 工程，构建 debug APK，并支持通过 ADB 安装启动到模拟器/设备。
- [新增] Android 构建环境自动探测：可自动从 PATH、常见 Android SDK 目录、Gradle Wrapper 缓存中查找 ADB、SDK 和 Gradle，减少手动配置成本。
- [新增] 可视化设计器新增组件悬停边框：鼠标移到控件上时显示实线提示框，选中状态仍使用虚线和缩放点。
- [新增] 可视化设计器支持画布拖拽框选多组件：在画布空白区域按住鼠标左键拖动可多选控件。
- [新增] 可视化设计器标尺交互增强：选中控件时在上方/左侧标尺显示控件范围，多选时显示所有选中控件的整体范围；鼠标移动时标尺显示当前位置参考线。

### 问题修复

- [修复] 修复打开很小的 `.eyc` 文件时先长时间显示原始文本、随后才渲染表格的问题，打开时直接进入表格式源码视图。
- [修复] 修复新建项目后误提示“文件已被外部修改”的问题。
- [修复] 修复新建项目未先关闭旧项目的问题；现在会走关闭当前项目流程，避免新旧项目上下文混在一起。
- [修复] 新建项目目标目录已存在时新增覆盖确认，避免静默覆盖或继续写入已有目录。
- [修复] 修复 Windows 窗口程序编译时 `LoadCursorW` 参数类型不匹配、核心支持库重复符号定义、OLE Automation 时间函数未链接等错误。
- [修复] 修复 Windows 窗口程序运行视觉仍是旧式 Win32 控件的问题：窗口程序默认嵌入 Common Controls v6 manifest，并初始化 Common Controls。
- [修复] 修复 Android Gradle 构建在中文项目路径下失败的问题：Android 临时工程改为生成到系统临时目录，并写入 `android.overridePathCheck=true`。
- [修复] 修复可视化设计器新增标尺联动后启动报错 `Cannot access 'getCanvasPoint' before initialization` 的问题。
- [修复] 修复双击组件创建事件子程序时自动生成默认局部变量的问题；新事件子程序只生成子程序声明和必要参数。
- [修复] 修复源码中 `编辑框1.内容 ＝ ""` 这类控件文本比较未正确转换，导致生成 C++ 使用未声明标识符的问题。
- [修复] 修复流程语句不匹配时错误落到 C++ 编译阶段才暴露的问题；现在 `.否则/.如果结束` 无匹配时会在源码转换阶段提示对应源码行。
- [修复] 移除可视化设计器选中或悬停控件时边框下方显示的控件名称标签。

### 优化调整

- [优化] Android APK 构建不再强制要求用户手填 Gradle 路径；优先使用系统 PATH、用户 Gradle Wrapper 缓存或设置中指定路径。
- [优化] Android 运行日志补充 Gradle/SDK 查找失败提示和路径示例，便于定位环境问题。
- [优化] 移动端项目类型与窗口设计器关系调整：移动应用仍保留窗口设计能力，但项目类型不再与桌面窗口/控制台/DLL 混用。
- [优化] 可视化设计器多选后标尺范围按选中控件整体边界显示，方便对齐与测量。
- [优化] 编译器对支持库平台源码参与构建和缓存指纹的处理更完整，避免支持库实现变化后复用旧产物。

### 测试重点

- [测试] 新建项目：分别选择 Windows 窗口程序、Android 移动应用、iOS 移动应用、HarmonyOS 移动应用，确认默认项目名、项目类型列表、默认窗口尺寸和旧项目关闭流程正常。
- [测试] Windows 窗口程序：创建按钮、编辑框，双击按钮生成事件后编写 `编辑框1.内容` 读写逻辑，确认可编译运行且事件子程序不再自动生成默认局部变量。
- [测试] 可视化设计器：验证控件悬停边框、单选/多选、拖拽框选、标尺范围高亮、鼠标位置参考线，以及控件框下方不再显示控件名。
- [测试] Android 运行：在已安装 Android SDK、Gradle 缓存和模拟器的机器上，选择 Android 移动应用后点击运行，确认能生成临时工程、构建 APK、安装并启动到模拟器。

### 已知说明

- [说明] iOS 和 HarmonyOS 当前已进入项目创建和设计器配置阶段，原生编译/真机运行后端尚未实现。
- [说明] Android 当前为 WebView 工程运行雏形，适合验证项目创建、设计器到 APK 的基础链路，后续仍需继续完善事件逻辑、组件行为映射和正式发布打包能力。
- [说明] 历史源码中已经自动生成过的 `.局部变量 局_变量N` 不会自动删除；本版本只保证后续新建事件子程序不再自动生成。

## ycIDE - 开发测试版 v0.0.3-beta.55(2026-05-15)

---

### 新增功能

- [新增] 新增内置数据类型“大整数型”：编译后端引入 `YC_BIG` 任意精度整数实现，支持 `+ - * /` 与比较运算，并支持超出长整数范围字面量在大数上下文保真计算。
- [新增] 编辑期类型诊断增强：当文本型变量直接接收数值型右值（如 `e = a + b`、`e = a`，且右值为数值型）时，问题面板立即报错并触发行内红色波浪线。
- [新增] 编辑期问题面板新增“长整数常量超范围”诊断：在代码书写阶段实时检查整数常量，超出 `[-9223372036854775808, 9223372036854775807]` 立即报错，无需等待编译。
- [新增] `spec` 支持库最小后端协议映射：新增 `spec.protocol.json`，优先在编译期直连 `Trace/Verify/Delay`（分别对应输出调试、检查、中断延时）。
- [新增] 新增系统特殊功能支持库 `spec`（独立于核心支持库）：基于开发期外部 `D:\chungbin\ycIDE-html\支持库源码\spec` 命令定义离线生成项目内支持库清单，运行时仅读取 `lib/spec` 产物。
- [新增] 新建子程序自动补局部变量：在新子程序声明下方自动插入 `.局部变量 局_变量N, 整数型`，变量名按 `局_变量1` 递增并避免重名。
- [新增] EYC 表格编辑器右键菜单支持定制化分组与动作：新增“新子程序/删除行/插入新行/编译当前行/屏蔽/解除屏蔽”菜单项，并按要求调整分隔顺序与快捷键文案展示。
- [新增] 状态栏右下角新增当前活动文件编码显示，并支持点击后选择编码重新打开：新增编码菜单（UTF-8/UTF-8 BOM/GB18030/GBK/Big5/Shift_JIS/UTF-16LE/UTF-16BE/Windows-1252），选择后按指定编码重新读取当前文件。
- [新增] 新增“打开文件夹工作区”入口：在保留现有“打开项目”流程不变的前提下，新增独立菜单/快捷键入口用于选择文件夹；若根目录存在 `.epp` 自动按项目打开，否则按普通文件夹工作区打开并显示目录树。
- [新增] 核心支持库 ABI 契约进入实施版 v1：在 `windows.cpp` 落地 OLE Automation Date 适配基础，并新增 `ToTime/now/GetDatePart/GetTimePart` 真实实现（移出自动桩区）；同时将契约与映射规范升级到 v1，映射产物新增 `abiSpecVersion` 与 `abiContractRef` 字段用于高风险命令追踪。
- [新增] 启动核心支持库 ABI 约定与命令映射实现：新增 `krnln-abi-contract.md` 与 `krnln-command-mapping.md` 讨论版规范，并落地自动映射脚本 `scripts/krnln/generate-command-mapping.mjs`，可生成 `command-mapping.windows.json` 与映射报告，首轮统计 `total=512 / implemented=133 / stubbed=379 / undeclared=0`。
- [新增] 一次性补齐核心支持库 Windows 全量导出符号层：根据 `krnln.commands.ycmd.json` 自动生成并落地 443 个缺失 `krnln_*` 导出桩函数，统一按返回类型给默认返回值，避免编译/链接阶段缺符号报错。
- [新增] 补全核心支持库 Windows 磁盘命令实现：新增 `ChDrive/GetDiskTotalSpace/GetDiskFreeSpace/GetDiskLabel/SetDiskLabel`，支持按驱动器查询容量与卷标，并可切换当前驱动器。
- [新增] 补全核心支持库 Windows 文件系统常用命令实现：新增 `CurDir/ChDir/MkDir/RmDir/FileCopy/FileMove/kill/name/IsFileExist/FileLen/GetAttr/SetAttr/GetTempFileName` 平台实现，覆盖目录切换、递归建删目录、文件复制移动删除、属性读写与临时文件名生成。
- [新增] 完善核心支持库跨平台基础实现：`linux.cpp` 与 `macos.mm` 从占位实现扩展到可用基础命令，实现消息框/鸣叫/延时/运行命令/取执行文件与运行目录/环境变量读写/命令行读取/系统版本与语言等通用入口。
- [新增] 新增误报申诉包收集脚本：支持收集样本路径、SHA-256、构建上下文（编译器/目标/模式）与附加日志，统一生成 `debug_logs/av-report-*/report.json` 与附件目录。
- [新增] 新增 Windows 签名打包入口：增加 `package:win:signed`，通过环境变量注入证书参数（含强制签名开关），为发布链路接入代码签名提供落地脚本。
- [新增] 核心支持库 Windows 平台实现扩容：在 `impl/windows.cpp` 批量落地系统命令与组件基础操作（消息框、蜂鸣、延时、取屏幕/鼠标、环境变量、剪贴板、运行命令、取程序路径、窗口创建/销毁/激活/焦点/尺寸/文本/重画等），并增加命令命名兼容入口。
- [新增] 核心支持库 Windows 平台实现起步：`impl/windows.cpp` 的 `krnln_message_box` 从占位返回改为真实调用 WinAPI `MessageBoxA`，并补充空文本/空标题入参兜底，打通首个可用平台命令实现。
- [新增] 支持库管理接入在线索引和独立 zip 安装：默认读取 `https://ycide.dev/libraries/index.json`，按支持库 ID 下载独立包，校验 SHA-256 后安装到用户数据目录，并在支持库管理窗口显示下载、安装、加载、可更新和失败状态。
- [新增] 系统设置新增支持库在线索引地址配置，便于后续切换官网源或镜像源。
- [新增] 新增支持库发布打包脚本，可将 `lib/<id>` 生成 `packages/<id>.zip` 和 `index.json`，适配官网独立支持库包部署结构。

### 问题修复

- [修复] 编辑框运行态默认文本与设计器对齐：编译生成控件时，编辑框/超级编辑框/文本框不再将控件名回退为初始文本；仅在显式设置文本时才显示内容，避免设计器为空而运行后出现默认文字。
- [修复] 可视化设计器编辑框默认底色与运行态对齐：编辑框/超级编辑框预览改为优先读取控件背景属性，未设置时默认白底，避免深色主题下显示为黑色而与编译运行结果不一致。
- [修复] 参数递归展开子级箭头层级偏移修正：子级箭头定位显式叠加当前层缩进变量，箭头锚点改为按“本层起始竖线”计算，避免误落到上一级位置。
- [修复] 参数递归展开子级箭头绘制基准修正：子级箭头改为在参数子级容器顶部绘制（不再悬挂在上一行底部重叠区域），减少跨行基准导致的对齐漂移。
- [修复] 参数递归展开子级箭头中心线对齐：子级箭头改为按中心点锚定下一级竖线起点（`translateX(-50%)`），避免按左边界定位导致“看起来未对齐”的偏移。
- [修复] 参数递归展开子级箭头锚点修正：箭头改为锚定下一级竖线起点位置，不再对齐到父级收口结束点。
- [修复] 参数递归展开子级连线对齐修正：二级展开三角箭头与该层竖线中心线重新对齐，消除子级箭头与竖线左右错位。
- [修复] 参数递归展开连线收口修正：当某参数行继续展开下级子树时，父级该行的竖线在本行中线收口，避免出现“应为 L 形却显示为 ├”的错连线。
- [修复] 消除文本字面量赋值告警：文本型变量接收 `L"..."` 字面量时改为走 `yc_value_to_text(...)` 复制，避免 `wchar_t*` 直接绑定字符串常量触发 `-Wwritable-strings`。
- [修复] 工具栏图标按钮偶发需点击两次才响应：鼠标按下时立即执行按钮动作，并对后续 click 做去重，减少首次点击被焦点/上下文清理吞掉的情况，同时保留键盘触发能力。
- [修复] 编辑期长整数溢出诊断与大数类型对齐：当赋值目标变量为“大数”时，不再错误提示“超出长整数范围”。
- [修复] 编译后端文本变量拼接语义修复：表达式翻译新增变量类型感知，`文本型 + 文本型` 生成 `yc_text_concat(...)`，避免误生成为 `wchar_t*` 指针相加导致编译错误（如 `e = t1 + t2`）。
- [修复] EYC 文本模式编辑器接入 Monaco 诊断标记：问题面板诊断结果同步为编辑区红色波浪线（含错误/警告级别）。
- [修复] 生成代码编译失败收敛：`spec` 调试输出协议翻译补命令上下文（支持 `到文本(...)` 参与表达式转换），并修复“文本型变量接收数值表达式”时的类型不兼容（自动转换为文本）。
- [修复] 表达式翻译增加整数字面量 64 位边界钳制，避免超长整数直接触发 C++ “integer literal is too large” 编译错误。
- [修复] ycmd 后端接入校验补齐英文命令名匹配：`CommandSignatureDef` 保留 `englishName` 并在协议匹配时传入，修复仅配置 `commandEnglishName` 的协议被误判“未接入后端实现”（如 `spec.Trace`）。
- [修复] 编译后端支持 `repeatable` 参数封送：ycmd 泛型命令调用在参数超出固定声明长度时，正确复用末尾可重复参数的数据类型，不再错误回退为“整数型”。
- [修复] 系统特殊功能支持库 `spec` 可追加参数命令补充 `repeatable` 标记，覆盖 `Trace/Verify/CallFunction/DelayCallFunction/GetText/DelayCallFunction_precisely`。
- [修复] 系统特殊功能支持库 `spec` 命令参数元数据全量对齐外部源码：补齐 `MachineCode/GetVarAddress/GetSubAddress/GetVarDataAddr/Verify/CallFunction/Delay/DelayCallFunction/AllocMem/FreeMem/ESysFunction/GetText/GetText_Set/GetText_Get/DelayCallFunction_precisely` 的参数定义（名称、类型、可省略）。
- [修复] 系统特殊功能支持库 `spec.Trace` 参数元数据补齐：`调试输出` 命令从“无参数”修正为首个可省略参数“欲输出值（通用型）”，与外部源定义对齐，恢复调用提示可读性。
- [修复] 可视化设计器组件箱状态恢复竞态修复：启动时先完成本地状态读取与应用，再开启状态写回，避免重启后将“浮动”误覆盖为默认“停靠”。
- [修复] 可视化设计器组件箱两种显示模式的宽度联动修正：图标模式移除固定宽度锁死，支持停靠态拖拽调宽；列表/图标模式下组件项图标、字号、间距与卡片尺寸会随组件箱宽度同步缩放，不再固定大小显示。
- [修复] 第三方支持库组件缺失 `icon` 字段时图标兜底修正：改为统一显示“未知”图标，不再按组件中文名误回退到图片等其他图标；仅核心支持库保留名称映射兜底。
- [修复] 工具箱“按钮”图标映射修正：将 `按钮` 与 `Button_16x.png` 从 `image-button` 改为 `button`，并接入 VS 图标库 `Button.svg`；同时修正主进程图标优先级为“组件 `icon` 字段优先、索引仅兜底”，避免被索引覆盖后显示错误。
- [修复] 组件定义中的公共属性彻底去重（含“窗口”组件）：`window-units.json` 不再内嵌 `标题/左边/顶边/宽度/高度/可视/禁止`，统一由主进程固定公共属性注入，避免属性面板重复与维护分叉。
- [修复] 组件公共属性显示顺序修正：属性面板改为对所有窗口组件统一前置显示固定公共属性（左边/顶边/宽度/高度/标记/可视/禁止/鼠标指针），其后再显示组件自身属性；并对同名属性去重避免重复行。
- [修复] 修复可视化设计器最大化后窗体偏右与缩放显示不同步：将缩放百分比浮层移出居中布局流，恢复窗体始终居中；同时统一窗体最小可视尺寸与缩放壳尺寸计算，避免缩放后窗体背景与内部控件显示越界或留白。
- [修复] 修复可视化设计器空格键误触发窗口最大化/还原：在设计器中对 `Space` 采用捕获阶段拦截默认行为，并在画布交互时主动归焦到画布区域，避免标题栏按钮保留焦点后被空格激活。
- [修复] 支持库提示面板位置修正：支持库树点击说明不再显示在侧栏内部，改为写入底部“输出/终端/提示/问题/调试”区域中的“提示”面板，并自动切换到该标签。
- [修复] 支持库命令分类恢复：修正 ycmd 解析层将命令分类固定为 `ycmd` 的问题，核心支持库命令现可按“流程控制/算术运算/逻辑比较/磁盘操作/数组操作/文本操作”等分类在支持库树中分组展示。
- [修复] 资源管理器支持库树提示去硬编码：移除命令与组件说明的固定解释模板（含“如果”特判说明），提示内容改为优先使用支持库元数据，仅保留“暂无说明。”最小回退。
- [修复] 右键菜单可用态修正：未改动时“撤销/重做”置灰；无选中时“复制/剪切”置灰；剪贴板无文本时“粘贴”置灰，并同步限制助记键仅在可用态触发。
- [修复] 修复右键菜单“新建子程序”高亮闪烁错位：新建后闪烁改为仅命中 `tr.eyc-data-row` 表格行，并在表格渲染就绪前短重试，避免过渡重渲染阶段闪到文本节点。
- [修复] 修复 EYC 右键菜单助记键缺失：补齐 `N` 新子程序单键触发，并同步补全 `E/I/K/G/B` 等字母前缀项的菜单打开态单键执行。
- [修复] 补齐文件夹工作区关闭入口：文件菜单新增可用“关闭工作区”项（复用 `file:closeProject` 关闭流程），文件夹工作区下不再缺少关闭菜单。
- [修复] 修复 EYC 文本模式 `Ctrl+V` 无法粘贴：全局快捷键在 Monaco/contenteditable 可编辑焦点下不再拦截编辑类快捷键，恢复原生粘贴行为。
- [修复] 修复“按 GBK 重新打开后误提示文件被外部修改”：外部变更检测改为按当前文件已选编码读取磁盘内容，再与标签保存态比较，避免 UTF-8 误解码导致的误报（ASCII 文件如部分 `.h` 原本不触发误报）。
- [修复] 修复 IDE 主窗口尺寸未持久化：调整窗口大小/位置后会写入主窗口状态，重启时恢复上次宽高、位置与最大化状态，不再回到默认尺寸。
- [修复] 修复文件夹工作区文件误判为文件夹导致“双击打不开”问题：目录读取接口对非目录改为返回 `null`，前端树构建可准确区分目录与文件，`button.cpp`、`button.h` 等文件双击可正常打开。
- [修复] 修复单文件打开无响应：`buildTabFromPath` 中文本模式回退分支被提前返回拦截，导致 `.cpp` 等非项目文件无法打开；现已恢复为默认文本模式打开。
- [修复] 信息框窗口标题为空时默认文案修正：统一回退为“信息框”，不再出现错误标题文案。
- [修复] 核心支持库自动桩区重复定义与状态一致性修复：去除重复函数定义（`SelectAll`、`SetAlignMode`），并补齐 `CloseRecordset/Clear/CloseClient/cls/EmptyCell/EndDoc/GetTextColor` 的状态化语义，保证编辑/打印/网络状态收敛与读写一致。
- [修复] 修复 ycmd 命令被编译前置校验一刀切拦截的问题：签名校验阶段改为仅在“无协议映射、无内建命令生成器、无内建表达式生成器、且非同名子程序”时才报未接入后端实现，从而解除 `信息框` 等已接入命令的误拦截。
- [修复] 清理主题管理器在 VS Code Problems 面板中的告警：移除 JSX 内联样式、补齐重命名与流程线配置输入框的可访问标签，并改用主题 token 替代 `color-mix` 兼容性较低的样式。

### 优化调整

- [优化] 参数递归展开接入流程层级配色：参数区连线与箭头改为复用流程命令的层级颜色算法（`resolveFlowLineColors`），多级参数展开时可按层级显示彩色线条。
- [优化] 命令参数展开支持表达式递归查看：代码行参数值现可继续展开 `+` 表达式（显示“被加数或文本或字节集 / 加数或文本或字节集”），并可进一步展开嵌套命令参数（如 `到文本(d)` 展示“待转换的数据：d”）。
- [优化] “正在生成C++代码”阶段提速与可观测性增强：项目元数据（全局变量/常量/资源/子程序/数据类型/DLL 命令）改为按项目指纹缓存复用，避免每次重复全量扫描；同时新增元数据阶段耗时日志。
- [优化] 运行阶段进一步提速：新增可执行产物指纹缓存（源 `.cpp`、资源、静态库、目标平台/架构/模式等），命中时直接跳过编译与链接，复用上次 `exe`。
- [优化] 转换阶段增量化：为 `.eyc/.egv/.ecs/.edt/.ell -> .cpp` 增加指纹缓存（源内容 + 编译上下文 + 断点 + 常量上下文），未变化文件直接复用已生成 `.cpp`，避免每次全量转换。
- [优化] 支持库编译协议加载提速：改为按“已加载支持库 + 协议文件路径/时间戳/大小”签名复用缓存，仅在签名变化时重读协议，减少重复解析开销。
- [优化] 运行模式编译提速：点击“运行”时编译器从 `O2` 调整为 `O0` 快速编译路径，减少日常调试等待时间；“编译(项目平台)”仍保持 `O2` 发布优化。
- [优化] 数据类型命名统一：将“大数”更名为“大整数型”，编辑器类型补全与诊断按新名称展示；编译器与诊断仍兼容旧名称“大数”，避免历史工程立即失效。
- [优化] 大数除零行为改进：大数除法/取模遇到除数为 0 时输出显式运行时错误（stderr），并在调试器附加时触发中断，避免静默返回造成误判。
- [优化] 大数运算能力补齐：新增大数取模 `%`（含全角 `％`）与运行时 `yc_big_mod`，表达式翻译可在大数上下文下正确分流到大数运算。
- [优化] 大数场景转换语义补齐：表达式翻译内建支持 `到文本(...)` 与 `到数值(...)`，其中大数目标上下文下 `到数值` 走 `yc_value_to_big(...)`，避免精度语义丢失。
- [优化] 编辑器类型体系补齐“大数”：内置类型补全新增“大数”，并将其纳入数值类型诊断集合（用于文本型赋值类型检查）。
- [优化] ycmd 命令参数新增 `repeatable` 语义透传：主进程解析、支持库管理与前端提示链路已打通；调用格式与参数说明可显示“可重复追加”参数。
- [优化] 可视化设计器组件箱图标视觉简化：移除图标外层矩形边框与底色，仅保留图标本体显示，降低视觉噪音。
- [优化] 可视化设计器组件箱列表模式支持手动多列并保持：将“列表单列/多列”切换放到工具箱上方按钮区后，多列状态在缩窄宽度时仍保持生效（不自动退回单列）；同时新增组件箱状态/位置持久化（停靠左右、停靠宽度、浮动开关、视图模式、多列开关、浮动位置与尺寸）。
- [优化] 可视化设计器组件工具箱交互增强：固定停靠支持左右切换并可拖拽边缘调整宽度（右停靠从左边框拖拽、左停靠从右边框拖拽）；浮动模式新增容器边界检测，拖动与缩放均不会超出 IDE 设计器区域。
- [优化] 支持库组件图标改为组件 JSON 直连：核心支持库组件在 `window-units.json` 内为对应组件写入 `icon` 路径（如 `icon/BlueIcons/*_16x.png`），渲染层优先按组件元数据展示图标；核心库不再依赖 `icon.json` 索引文件，第三方支持库沿用组件元数据同口径接入。
- [优化] 公共属性“鼠标指针”改为固定枚举下拉显示：按 `0~15` 显示为 `0.默认型`、`1.标准箭头型`…`15.自定义型`，并在组件属性面板中统一按枚举项选择。
- [优化] 组件公共属性与 lib2.h 对齐：参考 `elib/lib2.h` 固定属性定义，补齐默认窗口组件属性中的“标记(tag)”与“鼠标指针(MousePointer)”，并将“禁止”英文名对齐为 `disable`；同时补充无元数据场景下属性面板的对应编辑项。
- [优化] 可视化设计器窗口默认居中并支持滚轮缩放：设计画布改为窗口居中展示，按住 `Ctrl`（macOS 为 `Command`）后滚轮可放大/缩小，普通滚轮保留滚动画布；画布聚焦后支持键盘 `+/-/0` 缩放控制并显示当前缩放百分比。
- [优化] 可视化设计器缩放与画布导航增强：缩放上限提升到 `1000%`；按住 `Ctrl/Command + 滚轮` 时以鼠标位置为焦点缩放（鼠标不在容器内时回退到中心缩放）；设计器引入固定大工作区并常驻横向/纵向滚动条；新增顶部与左侧像素标尺并随滚动与缩放联动。
- [优化] 可视化设计器新增空格拖动画布视角：按住 `Space` 后左键拖拽可平移画布可见区域，无需手动拖滚动条；并增加 `grab/grabbing` 光标反馈。
- [优化] 可视化设计器像素标尺细化：标尺改为每 `10` 单位绘制刻度线，并在每 `100` 单位显示数字文本。
- [优化] 可视化设计器像素标尺层级优化：在每 `100` 单位长刻度与文本基础上，补充每 `50` 单位中刻度，提升读数分层。
- [优化] 可视化设计器标尺密度自适应缩放倍率：按当前缩放自动计算刻度步长，随倍率动态调整刻度与文本密度，避免放大后过稀、缩小后过密。
- [优化] 可视化设计器视图状态持久化：按当前设计文件保存并恢复缩放倍率与画布滚动视角，重新打开后可回到上次浏览位置。
- [优化] 资源管理器支持库查看树结构增强：在“数据类型”分组下新增窗口组件层级展示，支持展开到“属性/事件”子节点，并按组件图标展示，结构贴近易语言原生支持库浏览树。
- [优化] 支持库提示面板增强：点击支持库树中的命令/数据类型/属性/事件节点后，在固定提示面板展示对应说明信息；事件节点可显示“事件名称 + 触发说明 + 无返回值提示”。
- [优化] 支持库命令提示改为完整模板：点击命令节点后，提示面板按“调用格式/英文名称/详细说明/参数说明/操作系统支持”输出；并对“如果(ife)”提供与原生风格一致的详细说明文本。
- [优化] 核心支持库命令说明与分类改为帮助说明对齐：新增离线导入脚本解析 `系统核心支持库帮助说明文件.txt` 并同步 `krnln.commands.ycmd.json` 的摘要、分类、参数说明和平台支持字段。
- [优化] 调整新建类快捷键：将“新建文件/新建项目”改为 `Ctrl+Alt+N` / `Ctrl+Alt+Shift+N`，并将 `Ctrl+N` 让给“新建子程序”（含标题栏与原生菜单显示同步）。
- [优化] EYC 表格编辑器右键菜单补充单键助记触发：菜单打开后可直接按 `U/Y/C/T/P/F` 执行“撤销/重做/复制/剪切/粘贴/全选”，并同步更新菜单文案前缀为 `U.撤销` 等助记格式。
- [优化] 文件保存改为按当前文件编码写盘：`file:save` 新增编码参数，编辑器保存/保存全部/项目保存/外部冲突“保留IDE内容”均按状态栏当前编码落盘（默认 UTF-8）。
- [优化] 文件夹工作区文件节点图标改为扩展名占位语言图标：为 `.cpp/.h/.ts/.js/.json/.md` 等文件按扩展名显示占位图标，不再显示文件夹图标。
- [优化] 状态栏新增工作区模式标识：当存在工作区时，右侧显示“项目模式: 名称”或“文件夹工作区: 名称”，便于区分当前上下文并避免误判运行能力。
- [优化] 补齐单文件“另存为”能力：新增 `file:saveAs` 菜单动作与保存对话框 IPC，未命名文件执行保存时自动进入“另存为”流程；关闭未命名且已修改标签时选择“保存”也会触发保存为后再关闭。
- [优化] 支持单文件编辑工作流：新增“新建文件/打开文件”入口（菜单、快捷键、工具栏），可直接打开非项目文本文件（如 `.cpp`、`.py`、`.md`）进行编辑；当当前活动标签为非项目文件时，运行按钮自动禁用，避免误以为可调试运行。
- [优化] 项目管理改为单项目模式：当已有项目打开时再次打开新项目，会先执行当前项目关闭流程（含未保存确认与保存），再加载新项目，避免调试/运行时项目上下文混淆。
- [优化] 代码行输入体验优化：支持将常见中文标点自动转为英文符号；当以中文单引号 `‘/’` 起始输入时自动转换为注释前缀 `'`，并在注释符后自动补一个空格，便于后续自动排版。
- [优化] 核心支持库 Linux/macOS `SendMessage` 语义扩展：新增 `WM_MOVE/WM_CLOSE/WM_GETTEXT/WM_GETTEXTLENGTH` 处理分支，补齐位置回写、关闭状态收敛与文本读取返回值，提升跨平台消息调用的一致性与可验证性。
- [优化] 核心支持库 Linux/macOS 消息循环与游标兼容入口补齐：新增 `krnln_DoEvents`（冲刷状态化消息队列）及 `krnln_SetWaitCursor/krnln_RestroeCursor`，对位 Windows 侧同名导出，减少跨平台命令调用断档。
- [优化] 核心支持库 Linux/macOS 组件消息语义补齐：`SendMessage/PostMessage` 从占位返回升级为状态化消息分发与队列机制（Post 入队、Send 先冲刷后同步分发），并补充 `WM_ACTIVATE/WM_SETFOCUS/WM_KILLFOCUS/WM_ENABLE/WM_SIZE/WM_SETTEXT` 对应状态回写，降低跨平台组件命令“有符号无行为”的差异。
- [优化] 核心支持库 Linux/macOS 组件与窗口命令映射继续补齐：新增 `IsCreated/CreateWindowUnit/destroy/Activate/SetFocus/IsFocus/GetClientWidth/GetClientHeight/GetWidth/GetHeight/enable/IsEnabled/move/ZOrder/SetParentWnd/GetHWnd/SetText/GetText` 及重绘/消息相关导出，避免编译链路在非 Windows 平台落到“无实现符号”。
- [优化] 核心支持库 Linux/macOS 平台实现继续补齐：Linux 端补上 `PutEnv`、屏幕尺寸/鼠标坐标查询与消息框按钮语义（优先 `zenity`，失败回退控制台）；macOS 端补上 `MsgBox/msgbox` 的按钮类型与返回码映射，不再忽略 `buttons` 参数。
- [优化] 核心支持库 Linux/macOS 平台参数语义继续补齐：`krnln_run` 现已处理 `showMode`（隐藏模式重定向到 `/dev/null`，异步模式后台执行）；macOS `MsgBox/msgbox` 接入 `parentWindow` 参数（优先激活父窗口后再弹框），减少参数形同虚设的问题。
- [优化] 核心支持库 Linux/macOS 命令映射实现补齐（非消息框）：新增 `CurDir/ChDir/MkDir/RmDir/FileCopy/FileMove/kill/name/IsFileExist/FileLen/GetAttr/SetAttr/GetTempFileName/ChDrive/GetDiskTotalSpace/GetDiskFreeSpace/GetDiskLabel` 的平台实现，文件与目录命令改为真实文件系统行为，磁盘空间命令改为 `statvfs` 回读。
- [优化] 核心支持库内置不可分离命令继续协议化：将 `输出调试文本/暂停/检查` 并入 `commandBindings`（新增 `emitBuilder`），并移除 `compilerCommandResolvers` 中对应硬编码；当前解析器仅保留流程兜底，不再承载核心命令规则。
- [优化] 核心支持库协议化第三批收敛：将 `比较/逻辑/磁盘与目录/文件检索与临时文件/写到文件` 命令并入 `commandBindings`（新增 `exprOp/exprBuilder` 协议字段），并从 IDE 内置命令解析表剥离同名硬编码；语句上下文在无 `emit` 时可自动复用协议 `expr` 生成 `(void)` 调用。
- [优化] 协议驱动第二批下沉：新增 `改变驱动器/改变目录/字节集与指针转换` 等命令到核心支持库 `commandBindings`，并同步从 IDE 内置命令表移除对应实现，进一步降低编译器对命令名硬编码依赖。
- [优化] 协议驱动继续收敛：已在核心支持库 `commandBindings` 下沉的命令（如 `信息框/标准输出/调试输出/取本机名/是否为调试版/文件与字节集首批命令`）从 IDE 内置命令表移除，确保编译优先且实际依赖支持库协议而非本地硬编码。
- [优化] 支持库编译协议新增表达式绑定能力：`commandBindings` 支持 `expr` 字段，编译器在表达式翻译与后端路径校验中优先读取协议绑定；核心支持库已下沉首批命令映射（含 `信息框/标准输出/调试输出/取本机名/是否为调试版/文件与字节集常用命令`）。
- [优化] 编译器命令内置表模块化：将 `compiler.ts` 中的内置命令表达式/语句生成器与命令分发逻辑抽离到独立模块，主编译文件仅保留流程编排与接口调用，降低第三方支持库接入时对主编译文件的耦合感知。
- [优化] 核心支持库数据/对象日期与属性写入去随机化：将 `createRuntimeObject/SetData/GetDateTime/RunMethod/RunDateMethod/GetDateProperty/SetExtra/SetFontAttr/SetItemData/SetProperty` 改为确定性状态迁移（按游标、行列、画布操作数与对象记录回写），减少 tick/随机依赖导致的回归测试波动。
- [优化] 核心支持库绘图与资源 getter 语义继续补齐：将 `InvertRect/GetFont/GetHDC/GetPicHeight/GetPicWidth` 从占位返回改为运行时状态驱动（画布反色、字体对象句柄复用/创建、绘图上下文签名、图片尺寸随画布边界回读），提升图形链路可观察性与一致性。
- [优化] 核心支持库绘图命令语义补齐：将 `DrawRect/FillRect/LineTo/DrawPic/DrawJBRect/ArcTo/chord/ellipse/pie/polygon/RoundRect/GetPixel/SetPixel` 从空触发改为运行时画布状态驱动，统一记录最近绘图操作、边界、点位与颜色，支持后续通过 `GetPixel` 与编辑状态字段观察绘图副作用。
- [优化] 核心支持库数据集游标与对象属性语义继续补齐：增强 `connect/CloseConnect/ExecuteSql/AddLine/AddString/Append/AppendRow/InsertCol/InsertRow/LoadDS/LoadDSFile/RemoveCol/RemoveRow/Requery/RunBoolMethod/GetNumProperty` 的状态联动，统一行列最小约束、`currentRow/caretRow/caretCol` 边界收敛与对象句柄回读，减少“恒成功但状态漂移”的伪语义行为。
- [优化] 核心支持库对象命令语义补齐：引入运行时对象句柄表，打通 `CreateArray/CreateFontDispObj/CreatePicDispObj/CreateObject/QueryInterface/RunMethod/RunNumMethod/RunTextMethod/RunDateMethod/RunObjectMethod/RunVariantMethod/GetObject/GetObjectProperty/SetType/ViewObjInf` 的同一对象状态读写，避免随机句柄与不可追踪返回值。
- [优化] 核心支持库编辑/打印/网络/数据游标语义继续补齐：将 `` 等从随机值或恒成功占位改为共享运行时状态驱动；并统一 `Get* / Set*` 行为一致性（同一状态可读回）。
- [优化] 核心支持库事务/保存命令语义补齐：将 `connect/CloseConnect/query/select/ExecuteSql/OpenMDB/OpenSqlServerDB/BeginTrans/CommitTrans/RollbackTrans/SaveChange/SaveDS/SaveDSCell/SaveDSCellFile/SaveDSFile/say` 从固定成功返回调整为基于运行时连接与事务状态的行为（含失败分支），避免无脑 `return 1` 掩盖错误。
- [优化] 核心支持库 Windows 桩清零收官：继续对自动桩区进行连续替换，将剩余命令全部转为可执行实现（含控制流占位、组件/数据集接口、绘图与对象调用接口等的兜底实现），并完成映射统计闭环。
- [优化] 核心支持库 Windows 桩实现连续补齐（文件句柄/通用命令/对象辅助）：新增 `` 等实现，并修复映射统计中的 `undeclared` 回归。
- [优化] 核心支持库 Windows 文本命令簇补齐：新增 `RpSubText/space/string/pstr/StrToUTF8/UTF8ToStr/StrToUTF16/UTF16ToStr` 实现，覆盖子串替换、重复文本、指针到文本与 UTF8/UTF16 基础转换能力。
- [优化] 核心支持库 Windows 时间命令簇补齐：新增 `TimeChg/TimeDiff/GetDaysOfSpecMonth/TimeToText/TimePart/year/month/day/WeekDay/hour/minute/second` 实现，统一复用 OLE 日期转换链路并补齐常见时间增减/提取语义。
- [优化] 核心支持库 Windows 桩实现继续补齐：新增 `SetIntInsideBin/ReadFile/WriteFile/QJCase/BJCase/str/GetSpecTime/SetSysTime/UNum/NumToRMB/NumToText/FileDateTime` 的可运行实现，覆盖文件字节读写、全半角转换、数值格式化与文件时间读取路径。
- [优化] 核心支持库高风险字节集命令补齐一批 Windows 实现：完成 `BinLen/ToBin/BinLeft/BinRight/BinMid/InBin/InBinRev/RpBin/RpSubBin/SpaceBin/bin/pbin/p2int/p2float/p2double/GetIntInsideBin` 的语义实现；并修正映射脚本对自动桩区的判定逻辑，改为按函数体内容识别“真实桩/真实实现”，避免仅按区块误判。
- [优化] 核心支持库 Windows 命令语义批量落地：将一批高频基础命令从默认桩替换为真实实现，覆盖数学运算（`mod/abs/round/pow/sin/cos/tan/...`）、位运算（`band/bor/bxor/shl/shr/...`）、比较逻辑（`equal/notEqual/less/...`）、文本基础处理（`len/left/right/mid/InStr/UCase/LTrim/...`）与数值转换（`val/ToInt/ToLong/hex/binary/...`）；并修正自动桩生成逻辑为“扫描时排除桩区”，确保仅为未实现命令保留桩函数。
- [优化] 完善核心支持库 Windows 平台细节实现：`run` 增加显示模式标准化与等待失败处理，系统版本查询优先使用 `RtlGetVersion` 降低新系统误判，`GetAppName` 按类型返回完整路径/主文件名/文件名(含扩展名)/运行目录。
- [优化] Windows 运行产物启动失败日志增强：`spawn` 失败场景新增“疑似被安全软件拦截”提示，并输出输出目录引导信息，降低 `spawn EPERM` 排障成本。
- [优化] 核心支持库 Windows 平台实现按命令ID对齐：补充 `krnln.MsgBox` 对应导出入口 `krnln_MsgBox`（并兼容 `krnln_msgbox` / 旧 `krnln_message_box`），统一走同一消息框实现，支持按钮样式与父窗口句柄参数。
- [优化] 核心支持库窗口元数据与编译协议合并为单文件：`window-units.json` 采用 `windowUnits` 组件内聚结构，每个组件项内直接承载类名/样式与事件 `channel/code` 映射，不再使用顶层 `controlBindings/eventBindings` 重复节点；编译器协议加载改为优先读取该文件，并保留对旧 `*.protocol.json` 的兼容回退。
- [优化] 核心支持库完整性校验改为仅在稳定正式版强制执行：开发环境与 `beta/rc/dev` 预发布版本启动时跳过校验，避免开发阶段频繁改动核心支持库后需要同步更新哈希。
- [优化] 编译命令签名表改为消费 LibraryManager 的已加载支持库快照，支持本地安装目录覆盖内置支持库，并避免未加载支持库参与编译签名解析。
- [优化] 支持库管理详情改为合并读取支持库标识文件，内置支持库新增 `<id>.library.json` 示例，并移除详情中的邮政编码、通信地址、电话号码展示，方便第三方支持库提供签名、作者、联系方式和主页等标识信息。
- [优化] 固化核心支持库发布与校验规则：核心支持库只随 ycIDE 版本打包发布，不进入服务器支持库索引/zip 包，不允许远程安装、卸载或禁用；启动时校验核心支持库关键文件和清单完整性，缺失、损坏或被修改时直接报错。
- [优化] 支持库命令、数据类型与窗口组件元数据按顶部工具栏目标平台过滤：切换 Windows/macOS/Linux 后，不支持当前目标平台的支持库不会参与编辑器命令表、设计诊断或编译签名解析，支持库管理中同步显示“当前平台禁用”。
- [优化] 支持库管理改为独立窗口打开，并扩大可用区域到主题管理器同级窗口尺寸，提升支持库列表、在线安装状态和详情信息的浏览空间。
- [优化] 依据导出的易语言核心支持库帮助说明，直接补全核心支持库清单信息：更新 `krnln.library.json` 为正式作者/签名信息，并新增首批流程控制与算术运算命令 `*.ycmd.json` 元数据。
- [优化] 核心支持库命令清单改为单文件集合结构：新增 `krnln.commands.ycmd.json` 承载多命令数组，新增 `krnln.constants.json` 作为常量独立文件，并让 ycmd 注册表兼容集合与单命令两种格式。

项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：<https://github.com/chungbinb/ycIDE>
html版本：<https://github.com/chungbinb/ycIDE-electron>
QQ群：767523155（ycIDE易承语言交流群）


## ycIDE - 开发测试版 v0.0.3-beta.54(2026-05-09)

---

### 新增功能

- [新增]项目树支持项目级右键菜单：在项目根节点可单独执行“保存该项目全部文件”“关闭该项目”，用于多项目并行时按项目粒度管理。

### 问题修复

- [修复]修复 Icon 组件在 Problems 面板中的 inline style 告警：移除 JSX `style` 属性，改为通过 ref 在运行时设置宽高，保持 `size` 行为不变。
- [修复]修复项目树双击文件名无法打开文件：文件节点打开路径改为优先使用真实文件名（含扩展名），并统一“模块/窗口/资源”文件节点的双击与回车打开行为。
- [修复]修复粘贴代码产生重复子程序：当粘贴内容中的子程序名已存在于当前文件时，不再插入重复子程序头，改为将该子程序体内容追加到现有子程序末尾。
- [修复]新增重复子程序诊断：手工输入同名 `.子程序` 时，问题面板会报“子程序重复定义”错误。
- [修复]清理 VS Code Problems 面板中的 OutputPanel/AIAssistantPanel/VisualDesigner 报错：移除内联样式、补齐菜单与表单可访问性属性、改为 CSS 变量驱动动态尺寸与定位，并补齐 Safari 的 `-webkit-user-select` 兼容前缀。
- [修复]修复 AI 模型配置持久化被覆盖：模型配置弹窗保存后同步更新 App 内存设置（模型/DeepSeek Key/GLM Key/自定义模型），避免后续保存其它设置时将已保存 Key 覆盖回空值。
- [修复]修复 Agent 模式错误提示显示位置：当执行失败（如 API Key 缺失）时，错误信息优先追加到 AI 返回区，不再只显示在输入框下方状态栏。
- [修复]修复 AI 面板点击发送后偶发“无返回”问题：为聊天/流式聊天 IPC 调用补充异常兜底，发生调用异常时返回明确错误而非卡住等待。
- [修复]修复主进程 `file:readDir` 对文件路径抛出 `ENOTDIR`：读取前新增目录类型校验并统一异常降级为空列表，避免日志刷屏及上层调用被异常打断。
- [修复]修复多项目打开时项目树被后开项目覆盖：项目根节点改为按项目目录生成唯一 ID，打开/新建项目时按末尾追加（同项目则替换原根），刷新项目树仅替换当前项目对应根节点。
- [修复]修复可视化设计器编辑后项目树未实时显示未保存标记：`handleFormChange` 增加 `onOpenTabsChange` 同步，确保 Sidebar 使用到最新 `.efw` 标签内存状态计算 dirty。
- [修复]修复项目树未显示窗口/源码文件未保存标记：项目树 dirty 匹配键改为优先使用节点文件 ID（含扩展名），不再误用显示标签（无扩展名），使 `.efw/.eyc/...` 的“未保存圆点”能正确显示在右侧边框内侧。
- [修复]修复表格模式流程线在 `如果/如果真` 连续块中的误连通：`如果结束/如果真结束` 后紧跟 `如果/如果真` 时，不再套用“判断”专用的上下双横线桥接效果。
- [修复]修复资源管理器双击子程序偶发跳错行：编辑导致行号漂移后，项目树双击子程序改为按子程序名实时定位（失败时回退旧行号），不再闪到新输入代码行。
- [修复]修复删除子程序后项目树残留旧节点：编辑器内容变更会实时同步 `.eyc` 子程序节点到项目树，未保存状态下删除 `.子程序` 也会立即从“程序集”树中消失。
- [修复]修复属性面板事件下拉勾选状态不刷新：事件列表“✓ 已存在子程序”改为优先读取当前打开标签页内存内容，并用最新解析结果覆盖缓存；删除子程序后勾选会即时取消。
- [修复]修复项目树双击子程序后落点行为：闪烁定位保持在子程序声明行不变，同时将输入光标置于该子程序首个普通代码行的行首（如子程序在第17行、首行代码在第18行则光标落到18行首）。
- [修复]修复项目树双击子程序后“仅闪烁无光标”问题：将光标进入时机调整为闪烁定位之后并增加重试聚焦，避免滚动重排冲掉编辑态；当子程序内仅空白普通行时也可在行首显示光标。
- [修复]修复双击子程序后输入框未渲染导致“仍看不到光标”：纠正 `startEditLine` 调用中的 `isVirtual` 参数，避免与普通代码行渲染条件不匹配而隐藏编辑输入框。
- [修复]修复子程序内“空行优先”落点：当子程序声明后先有若干空行再有命令时，双击项目树后光标改为落在首条空普通行行首，不再跳到后续命令行。
- [修复]修复可视化设计器创建子程序后的“无行号空行”问题：新建子程序时直接生成真实空普通行；项目树跳转到无普通行子程序时会自动物化一条空普通行并聚焦行首，避免必须手动回车后才正常。
- [修复]统一“已存在子程序”导航逻辑：属性面板事件下拉选择与可视化设计器双击组件进入默认子程序时，行为改为与项目树双击一致（子程序声明行高亮闪烁 + 光标定位到子程序首个普通行首列，并在缺失普通行时自动物化空普通行）。
- [修复]修复子程序跳转抖动与多点闪烁：去除 App 层对子程序导航的多重重试调用，并在编辑器侧为子程序导航增加“最后一次生效”的序列去重，改为单次闪烁 + 单次聚焦，避免上下抖动与多个位置依次高亮。
- [修复]修复“组件双击/属性面板事件下拉”跳转闪烁偏位：将 `navigateOrCreateSub` 的“已存在子程序”分支改为复用 `navigateToSubprogram` 同一内部实现，统一三入口（项目树/组件双击/事件下拉）的定位与闪烁规则，并补齐子程序名 trim 匹配。
- [修复]修复子程序声明行闪烁偶发错位：统一跳转内部逻辑新增“优先行号命中同名子程序”策略（组件双击/事件下拉传入已知声明行），并在新闪烁前清理旧 `highlight-flash` 样式，避免残留闪烁造成错位错觉。
- [修复]修复子程序跳转后“先在下方后瞬间居中”导致的闪烁位置偏差：声明行定位由 `center` 调整为 `nearest`，并移除光标聚焦阶段的额外滚动，避免二次跳动造成闪烁观感错位。
- [修复]修复子程序跳转时“滚动未稳定先闪烁”导致的错位：声明行高亮改为在两帧 + 短延时后触发（滚动收敛后再闪烁），并继续受导航序列去重保护，避免瞬态位置上闪烁。
- [修复]修复顶部菜单“插入 -> 子程序”后仅出现虚拟空行的问题：插入结构改为“真实空行 + 子程序声明 + 真实空普通行”，并在插入后将光标置于新子程序首条普通行行首，避免必须手动回车物化。
- [修复]新增子程序“保底普通空行”删除规则：子程序首条普通行始终保留为可输入占位；删除命中该行时，若有内容则改为空行保留，若本身为空则改删其下方普通行；多选删除/剪切同样遵循该规则，避免全删后无法进入普通代码行。

### 优化调整

- [优化]优化 AI 面板停止按钮视觉：移除停止按钮红色外边框，仅保留错误语义色图标与背景高亮，避免边框过重。
- [优化]调整标题栏拖拽区域设置方式：移除 CSS 中的区域声明，改为在运行时通过 `-webkit-app-region` 写入拖拽/禁拖拽属性，降低 Problems 面板兼容性提示噪音并保持 Electron 标题栏行为。
- [优化]优化 AI 面板模式下拉宽度：加宽模式选择框，避免选择 Agent 时文本末尾被遮挡。
- [优化]项目树未保存标记支持分类联动：当“窗口/程序集/全局变量/常量表/自定义数据类型/DLL命令/资源”等分类下任一文件存在未保存修改时，分类节点也显示右侧未保存圆点。

项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：<https://github.com/chungbinb/ycIDE>
html版本：<https://github.com/chungbinb/ycIDE-electron>
QQ群：767523155（ycIDE易承语言交流群）

---
## ycIDE - 开发测试版 v0.0.3-beta.53(2026-05-04)

---

### 新增功能

- [新增]打开项目对话框支持直接选择 `.e/.ec` 文件导入，支持读取工程头、密码输入与同名目录冲突处理。
- [新增]表格模式代码显示优化：流程命令 `判断开始` 在表格模式下显示为 `判断`，文本模式保持原始命令不变。

### 问题修复

- [修复]修复 `.e/.ec` 导入读取原生工程段长度偏移错误导致的部分项目 `Magic` 报错。
- [修复]修复 `.e/.ec` 导入只生成“导入说明”或空子程序占位的问题，导入后可生成真实 `EYC/EGV/EDT/ELL/ERC` 内容。
- [修复]修复核心命令号映射偏差：补齐 `Lib0` 回退映射并覆盖 `181/182/203` 兼容点，减少 `_Lib0CmdXXX` 占位显示。
- [修复]修复非核心支持库命令映射缺失：补充多支持库命令 ID 映射，减少 `_LibXCmdY` 回退。
- [修复]修复特殊结构下（`判断开始` 与后续 `判断` 分离）内侧线缺失、断段与贯穿不完整问题，补齐上一行起始横线及跨行内侧竖线连通。
- [修复]修复“第 7 行下三角箭头残留”系列问题，统一屏蔽重叠分支下不应显示的下箭头。
- [修复]修复内侧线“上半段显示规则”异常：仅对“从上方延续”的行显示上半段，避免首行误绘。
- [修复]修复 `如果真` 结束箭头在隐藏结束行后的显示异常：补回向下三角并去除多余横向虚线。
- [修复]修复嵌套 `如果真` 场景中结束箭头锚定错层与漏显问题，改为按同深度独立锚定与绘制。
- [修复]修复手工输入 `.如果` 回车未补齐可见 `否则/如果结束` 的问题，并修复文本/表格模式可见性不一致。
- [修复]修复表格编辑器偶发“临时回退文本模式”与编辑中空白闪烁问题。
- [修复]修复并清理 VS Code Problems 面板剩余告警（ARIA、inline style、类型收窄、不兼容 CSS）。

### 优化调整

- [优化]流程线与流程命令解析进一步收敛为“命令驱动”路径，去除旧 marker 兼容分支的生效逻辑，降低流程缩进与连线异常来源。
- [优化]表格模式性能系统性优化：可视区 block/行级裁切、滚动节流、缓存复用、minimap 阈值收紧，降低大文件卡顿。
- [优化]将语法诊断与表格模型构建迁移到 Worker，减少主线程阻塞与首开空白时间。
- [优化]补强“判断自动展开”诊断日志，新增关键判定上下文，便于定位第 5/7 行结构未命中原因。
- [优化]调整首屏渲染策略：先渲染轻量前置行，再异步切换完整模型，改善大文件打开体验。

项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：<https://github.com/chungbinb/ycIDE>
html版本：<https://github.com/chungbinb/ycIDE-electron>
QQ群：767523155（ycIDE易承语言交流群）

---

## ycIDE - 开发测试版 v0.0.3.52(2026-04-28)

---

### 新增功能

- [新增]AI 助手新增 Plan 模式与 Agent 模式，并支持项目级文件工具调用和终端工具调用闭环。
- [新增]输出面板支持可交互终端，支持命令输入、输出查看与最近命令展示。

### 问题修复

- [修复]重构表格模式流程线绘制，流程线改为基于可见 `.如果/.否则/.如果结束/.判断/.默认/.判断结束` 等语法结构计算，不再依赖或渲染 200C/200D/2060 零宽标记。
- [修复]表格编辑器流程自动补齐改为插入可见分支与结束行，旧隐藏标记内容在打开或粘贴时会清理成可见流程文本，避免隐藏字符干扰显示。
- [修复]修复手工输入 `.如果` 后按回车未自动补齐 `否则/如果结束` 可见流程结构的问题。
- [修复]修复流程自动补齐出的 `否则/如果结束` 在文本模式可见但表格模式被旧样式隐藏的问题。
- [修复]修复 Windows 启动后窗口左上角图标仍显示默认四方块的问题。
- [修复]修复自绘标题栏中“文件”菜单左侧仍显示四方块占位图的问题，改为显示程序 logo。

### 优化调整

- [优化]程序图标统一切换为 `ycidelogo.svg` 并接入 Windows/macOS/Linux 打包资源。
- [优化]优化表格模式流程线计算，减少大文件中分支、结束和相邻流程连接判断的重复扫描。
- [优化]AI 助手内置模型升级与扩展（DeepSeek-V4-Pro、DeepSeek V4-Flash、DeepSeek V3.2）。
- [优化]终端交互体验优化（按需启动、输入区一体化、中文输出解码稳定性）。

项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：<https://github.com/chungbinb/ycIDE>
html版本：<https://github.com/chungbinb/ycIDE-electron>
QQ群：767523155（ycIDE易承语言交流群）

---

## ycIDE - 开发测试版 v0.0.3.51(2026-04-23)

---

### 新增功能

- [新增]预览地图，可在系统设置里面开关，默认开启
- [新增]顶部子程序冻结，可在系统设置里面开关，默认关闭



### 问题修复

- [修复]撤销功能无响应



### 优化调整

- [优化]优化大文档滚动性能，根据可视区域裁切



项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（ycIDE易承语言交流群）

---

## ycIDE - 开发测试版 v0.0.2.50(2026-04-21)

---

### 新增功能

- [新增]表格和文本模式切换



### 问题修复

- [修复]表格模式下撤销重做问题
- [修复]复制的命令优化修复为完全兼容易语言格式



### 优化调整

- [优化]优化文件标签和资源管理器里的文件名显示方式



项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（ycIDE易承语言交流群）

---

## ycIDE - 开发测试版 v0.0.2.49(2026-04-20)

---

### 新增功能

### 问题修复

- [修复]嵌套流程内侧线会偶现的问题
- [修复]命令括号会偶现不可见问题
- [修复]点击命令最右边空白处光标不闪烁在最右边的问题
- [修复]复制粘贴修复，现在也兼容易语言代码粘贴和复制代码到易语言使用

### 优化调整

- [优化]优化命令括号显示为细括号跟编辑态一致

项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（ycIDE易承语言交流群）

---

## ycIDE - 开发测试版 v0.0.2.48(2026-04-18)

---

### 新增功能
- [新增]新增内容宽度超出编辑器后编辑器下方会出现横向滚动条



### 问题修复
- [修复]回车或者删除退格偶发的丢失焦点问题
- [修复]嵌套流程保存后重新打开丢失嵌套问题
- [修复]嵌套流程线断开不对齐问题


### 优化调整
- [优化]编译时缺少编译器会明确提示层级目录
- [优化]拆分主组件为模块化



项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（ycIDE易承语言交流群）

---

## ycIDE - 开发测试版 v0.0.2.46(2026-04-17)
---

### 新增功能
- [新增]资源管理器和输出面板的标签页支持右键菜单快速切换到顶部或底部，并自动记忆上次位置
- [新增]流程命令编辑行为增强，删除流程命令后可自动完成结构熔接，减少手动整理缩进


### 问题修复
- [修复]如果真命令的流程线和区块判定
- [修复]复杂流程嵌套下可能出现的命令误删和越界匹配问题
- [修复]括号在不同编辑状态下显示不一致的问题，统一为中文全角括号自动格式化

### 优化调整
- [优化]折叠按钮视觉简化为无边框样式并降低符号字重，减少界面干扰
- [优化]选中行与悬停态样式优先级，避免选中高亮被 hover 状态覆盖


项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（易承语言开发交流群）

---

## ycIDE - 开发测试版 v0.0.2.45(2026-04-16)
---

### 新增功能
- [新增]内置 AI 助手功能，支持在 IDE 内进行开发辅助交互
- [新增]基础无障碍支持能力，补齐键盘可达与可读性相关体验


### 问题修复
- [修复]键盘操作与焦点切换中的部分回归问题，减少交互中断

### 优化调整
- [优化]补充并完善 UI 回归测试覆盖，提升后续迭代稳定性


项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（易承语言开发交流群）

---

## ycIDE - 开发测试版 v0.0.0.38(2026-03-25)
---

### 新增功能
- [新增]对象成员也加入了语法检查不会再报未知变量了




### 问题修复
- [修复]修改IPC通道导致的支持库管理失效
- [修复]代码会变英文问题
- [修复]部分之前硬编码的编译转换导致有些代码会执行两次
- [修复]侧边主活动栏的提示是英文问题，问题出在svg图标内嵌的tite上

### 优化调整
- [优化]组件箱图标模式下浮动排列
- [优化]调试输出信息明朗化npm



项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（易承语言开发交流群）

---

## ycIDE - 开发测试版 v0.0.0.37(2026-03-23)
---

### 新增功能
- [新增]侧边主活动栏
- [新增]文件标签支持上下调整
- [新增]插入资源



### 问题修复


### 优化调整
- [优化]补全窗口图标修改优化


项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（易承语言开发交流群）
<img src=https://img.522.plus/i/u/2026/03/21/e5tp0p.gif>

---

## ycIDE - 开发测试版 v0.0.0.36(2026-03-22)
---

### 新增功能
- [新增]代码编辑器控件名补全
- [新增]子程序补全
- [新增]子程序事件提示
- [新增]迁移易语言70来个支持库大部分完成，剩下核心支持库还有少部分待完善。


### 问题修复


### 优化调整



项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（易承语言开发交流群）
<img src=https://img.522.plus/i/u/2026/03/21/e5tp0p.gif>

---

## ycIDE - 开发测试版 v0.0.0.35(2026-03-20)
---

### 新增功能
- [新增]全局变量编辑
- [新增]自定义数据类型编辑
- [新增]常量表编辑
- [新增]DLL 命令编辑

### 问题修复


### 优化调整
- [优化]目前重点在做支持库后规范化支持库的接口，然后就可以让第三方开发支持库了


项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（易承语言开发交流群）
<img src=https://img.522.plus/i/u/2026/03/21/e5tp0p.gif>

---

## ycIDE - 开发测试版 v0.0.0.34(2026-03-18)
---

### 新增功能
- [新增]新增一个ycUI支持库，使用D2D绘制组件，告别win32的丑陋组件，目前仅有一个按钮
- [新增]属性面板现在支持修改标题和可视属性了，现在在属性面板里可以直接修改控件的标题和可视属性了，修改后会立即反映在设计器里了

### 问题修复
- [修复]单行多选支持单个文本选择了，之前必须整行选择

### 优化调整
- [优化]点击不会有蓝色整行选中的情况了，现在点击代码行的空白处或者命令行的空白处都不会再出现整行被选中的情况了，应该会更符合用户的使用习惯了


项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（易承语言开发交流群）

---

## ycIDE - 开发测试版 v0.0.0.33(2026-03-17)
---

### 新增功能
- [新增]在状态栏显示当前编辑器版本号，现在在状态栏的右侧会显示当前的ycIDE版本号，方便用户查看和确认自己使用的版本
- [新增]支持复制代码到易语言使用和粘贴易语言的代码进来使用，现在在编辑器里复制代码会自动转换为易语言的格式，粘贴易语言代码也会自动转换为编辑器的格式了，方便用户在ycIDE和易语言之间进行代码的复制粘贴了
- [新增]属性面板的事件下拉选择框现在支持选择事件了


### 问题修复
- [修复]流程控制语句（如 如果、判断）在输入完成命令后失去焦点后会没有格式化命令的问题，现在在输入完成流程控制语句后会自动格式化命令并保持焦点，确保代码结构正确和编辑体验流畅

### 优化调整
- [优化]输出面板和资源管理器改动为和易语言IDE一样的风格，现在输出面板和资源管理器的界面风格已经调整为和易语言IDE一致了，应该会更符合易语言用户的使用习惯了
- [优化]单行获得焦点后可以跨行多选了
- [优化]点击最后面的空白处也可以获得焦点了，现在在代码行的最后面点击空白处会将光标定位到最后一行的末尾

项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（易承语言开发交流群）

---

## ycIDE - 开发测试版 v0.0.0.32 (2026-03-16)
---

### 新增功能
- [新增]修改变量名后同步修改代码块里的变量引用，现在修改变量名后会自动更新代码块内所有对该变量的引用，确保代码一致性和正确性


### 问题修复
- [修复]流程线在某些情况下会连续绘制横线虚线的问题，现在流程线的虚线绘制逻辑已经优化，应该不会再出现连续绘制横线的情况了

### 优化调整
- [优化]普通代码行命令修改为跟易语言的一样自动格式化为中文全角符号，现在在编辑器里输入命令时会自动将命令格式化为中文全角符号，和易语言的代码风格保持一致了

项目已完全开源在github，欢迎大家提交PR和issue，或者加入QQ群交流讨论！
开源地址：（当前主要是更新html版本为主，GDI版本暂时不考虑更新了）
GDI版本：https://github.com/chungbinb/ycIDE
html版本：https://github.com/chungbinb/ycIDE-electron
QQ群：767523155（易承语言开发交流群）

---

## ycIDE - 开发测试版 v0.0.0.31 (2026-03-15)

### 新增功能
- [新增]按上下左右方向键可以移动光标进行上下左右移动


### 问题修复
- [修复]鼠标多次点击不同的命令行有几率破坏代码结构的问题
- [修复]命令能被破坏问题，增加了对命令行的保护，防止被误操作破坏，现在在命令行上回车会自动往下一行插入空行，现在只能将命令全部选中后删除才能破坏命令行了

### 优化调整

---

## ycIDE - 开发测试版 v0.0.0.30 (2026-03-15)

### 新增功能
- [新增]属性面板现在支持直接编辑属性值，点击属性值即可进入编辑模式，输入新值后按回车确认修改


### 问题修复
- [修复]流程控制语句（如 如果、判断）在某些情况下会破坏代码结构的问题
- [修复]编译后底部控件被截断问题，缺少标题栏高度计算


### 优化调整
- [优化]移除工具栏右侧冗余的"支持库管理"按钮（已在菜单"工具"中提供）
- [优化]可视化设计器中窗口标题现在改为跟win11默认样式一样，并且将选中窗口整体改为选中客户区
