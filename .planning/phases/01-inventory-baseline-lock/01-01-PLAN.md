---
phase: 01-inventory-baseline-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/migration/inventory-baseline.mjs
  - scripts/migration/classify-arch.mjs
  - scripts/migration/classify-encoding.mjs
  - tests/migration/inventory-baseline.spec.mjs
  - tests/migration/fixtures/README.md
  - .planning/baselines/inventory-baseline.json
  - package.json
  - .planning/phases/01-inventory-baseline-lock/README.md
autonomous: true
requirements:
  - INVT-01
  - INVT-02
  - INVT-03
must_haves:
  truths:
    - "Maintainer can run one command to regenerate the full unmigrated library list from `第三方相关文件` vs `支持库源码` (per INVT-01)."
    - "Maintainer can see `archState` and `encodingState` for every unmigrated library in the authoritative output (per INVT-02)."
    - "Maintainer can read one baseline manifest that contains `coveragePct` and `remainingCount` without cross-checking other files (per INVT-03)."
  artifacts:
    - path: "scripts/migration/inventory-baseline.mjs"
      provides: "Deterministic baseline generator CLI with check/regenerate modes"
    - path: "scripts/migration/classify-arch.mjs"
      provides: "Architecture classification (`x86-only|mixed|x64-ready`)"
    - path: "scripts/migration/classify-encoding.mjs"
      provides: "Encoding classification (`gbk|mixed|utf-8`) with conservative heuristics"
    - path: ".planning/baselines/inventory-baseline.json"
      provides: "Single authoritative phase baseline report with totals and per-library rows"
    - path: "tests/migration/inventory-baseline.spec.mjs"
      provides: "Automated assertions for INVT-01/02/03 behaviors"
  key_links:
    - from: "scripts/migration/inventory-baseline.mjs"
      to: "第三方相关文件/易语言的功能库, 第三方相关文件/易语言的界面库"
      via: "scoped directory enumeration"
      pattern: "hard-coded scope roots + deterministic traversal"
    - from: "scripts/migration/inventory-baseline.mjs"
      to: "支持库源码"
      via: "normalized name set-diff"
      pattern: "migratedNames.has(name)"
    - from: "scripts/migration/inventory-baseline.mjs"
      to: ".planning/baselines/inventory-baseline.json"
      via: "single-writer manifest serialization"
      pattern: "writeFile.*inventory-baseline.json"
---

<objective>
Lock Phase 1 inventory scope into a deterministic, test-backed baseline pipeline that enumerates unmigrated libraries, classifies each unmigrated item, and publishes authoritative coverage metrics.

Purpose: Establish reliable inventory truth before any conversion/adaptation work, preventing drift and hidden assumptions in later phases.
Output: Inventory CLI + classifiers + tests + committed baseline manifest + usage documentation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-inventory-baseline-lock/01-RESEARCH.md
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase 1 baseline contracts and failing checks first</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-inventory-baseline-lock/01-RESEARCH.md
    - package.json
  </read_first>
  <files>tests/migration/inventory-baseline.spec.mjs, tests/migration/fixtures/README.md, package.json</files>
  <behavior>
    - Test 1 (INVT-01): scoped roots produce deterministic candidate list and unmigrated subset from set-diff.
    - Test 2 (INVT-02): architecture and encoding classifiers return only allowed enums and never omit unmigrated rows.
    - Test 3 (INVT-03): manifest includes totals.candidateCount, totals.migratedCount, totals.remainingCount, totals.coveragePct with mathematically consistent values.
  </behavior>
  <action>Create a Node test file that imports the planned generator entrypoint and asserts INVT-01/02/03 outcomes using fixture-driven expectations. Add npm scripts `inventory:baseline`, `inventory:check`, and `test:migration` in package.json so verification is one-command repeatable. Keep tests red until generator/classifier files exist (explicit RED stage).</action>
  <verify>
    <automated>npm run test:migration</automated>
  </verify>
  <acceptance_criteria>
    - Running `npm run test:migration` fails initially for missing implementation (RED proven).
    - Test names and assertions explicitly reference INVT-01, INVT-02, INVT-03.
    - package.json exposes executable scripts for regenerate/check/test flows.
  </acceptance_criteria>
  <done>Test scaffold and command surface exist, and RED state is demonstrated before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic inventory generator and conservative classifiers</name>
  <read_first>
    - .planning/phases/01-inventory-baseline-lock/01-RESEARCH.md
    - .planning/codebase/CONVENTIONS.md
    - tests/migration/inventory-baseline.spec.mjs
  </read_first>
  <files>scripts/migration/inventory-baseline.mjs, scripts/migration/classify-arch.mjs, scripts/migration/classify-encoding.mjs</files>
  <behavior>
    - For each scoped third-party library, generator emits one row with `isMigrated` based on normalized folder-name intersection against `支持库源码` (INVT-01).
    - For each unmigrated row, generator emits `archState` in {x86-only,mixed,x64-ready} and `encodingState` in {gbk,mixed,utf-8} using conservative evidence-based rules only (INVT-02).
    - Generator computes totals and coverage fields from row data and writes/validates one manifest path only (INVT-03).
  </behavior>
  <action>Implement the CLI and helper modules with requirement-scoped roots only (`第三方相关文件/易语言的功能库`, `第三方相关文件/易语言的界面库`, `支持库源码`) per Phase 1 boundaries. Exclude non-authoritative output trees (`out`, `dist`, `test-results`) from scans. Provide `--regenerate` to write `.planning/baselines/inventory-baseline.json` and `--check-*` modes used by tests. Use conservative classification: when evidence conflicts/insufficient, classify as `mixed` rather than guessing.</action>
  <verify>
    <automated>npm run test:migration</automated>
  </verify>
  <acceptance_criteria>
    - Tests from Task 1 pass (GREEN achieved).
    - `npm run inventory:check` exits 0 and validates required manifest keys and metric consistency.
    - No phase-2+ behavior (conversion/adaptation/promotion) is introduced.
  </acceptance_criteria>
  <done>Inventory baseline implementation is deterministic, requirement-complete for INVT-01/02/03, and fully test-covered by migration checks.</done>
</task>

<task type="auto">
  <name>Task 3: Publish authoritative baseline artifact and operator instructions</name>
  <read_first>
    - scripts/migration/inventory-baseline.mjs
    - .planning/ROADMAP.md
  </read_first>
  <files>.planning/baselines/inventory-baseline.json, .planning/phases/01-inventory-baseline-lock/README.md</files>
  <action>Run the generator in regenerate mode to produce `.planning/baselines/inventory-baseline.json` and commit it as the single authoritative Phase 1 baseline (per INVT-03). Add a concise phase README documenting command usage, manifest schema, and expected operator workflow (`inventory:baseline` then `inventory:check`), including explicit statement that runtime userData files are non-authoritative.</action>
  <verify>
    <automated>npm run inventory:baseline && npm run inventory:check</automated>
  </verify>
  <acceptance_criteria>
    - Baseline JSON exists at `.planning/baselines/inventory-baseline.json` and contains non-empty `libraries` plus `totals.coveragePct` and `totals.remainingCount`.
    - Phase README contains exact commands to regenerate and validate.
    - Running verify command is idempotent (second run yields no structural drift).
  </acceptance_criteria>
  <done>Authoritative baseline artifact and execution instructions are present, reproducible, and ready for Phase 2 dependency.</done>
</task>

</tasks>

<verification>
- `npm run test:migration` passes.
- `npm run inventory:baseline && npm run inventory:check` passes.
- `.planning/baselines/inventory-baseline.json` is committed and machine-readable.
</verification>

<success_criteria>
1. INVT-01: Unmigrated inventory list is fully regenerated from scoped roots with deterministic set-diff.
2. INVT-02: Every unmigrated library row has architecture and encoding classification in required enums.
3. INVT-03: Single baseline manifest reports coverage percentage and remaining count from computed totals.
</success_criteria>

<output>
After completion, create `.planning/phases/01-inventory-baseline-lock/01-01-SUMMARY.md`
</output>
